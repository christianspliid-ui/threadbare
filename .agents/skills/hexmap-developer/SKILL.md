---
name: hexmap-developer
description: >
  SPLIT: This skill has been split into hexmap-core and hexmap-layers. Load
  hexmap-core for architecture, coordinates, zoom, color system, lessons
  learned. Load hexmap-layers for signifiers, agent sprites, fog-of-war,
  testing, visual verification, debugging. Load both for full HexMapV2 work.
  Triggers on "hex map", "HexMapV2", "Three.js", "WebGL", "hex renderer",
  "d3-zoom", "coastline", "signifier", "terrain palette", "zoom tier",
  "InstancedMesh", "hex coordinate", "hex grid", "render layer", "stencil",
  "fog of war", "agent sprite", "portrait".
---

# HexMap Developer — Redirected

This skill has been split into two focused skills:

- **`hexmap-core`** — Architecture, technology stack, coordinate system, render layers, zoom system, coastline overview, color system, constants, performance, lessons learned, NFP checklist. Load this first — it is required before any HexMapV2 code work.

- **`hexmap-layers`** — Directory structure, signifier system, agent rendering, fog-of-war, testing strategy, visual verification, debugging toolkit, integration points. Load alongside `hexmap-core` when building or modifying a specific visual layer.

Load both skills for comprehensive HexMapV2 development work.
