---
phase: 09-start-screen
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/components/StartPage/SettingsModal.tsx
  - src/components/StartPage/CreditsModal.tsx
  - src/components/StartPage/StartPage.tsx
autonomous: false
requirements: []

must_haves:
  truths:
    - "Clicking Settings in the menu opens a modal overlay with volume slider, fog toggle, and version display"
    - "Clicking Credits in the menu opens a modal overlay with game title, author, technology credits, and closing lore line"
    - "Both modals dismiss on Escape key, backdrop click, or close button"
    - "Volume slider in Settings adjusts the theme music volume in real-time"
  artifacts:
    - path: "src/components/StartPage/SettingsModal.tsx"
      provides: "Settings modal with volume slider, fog default toggle, version/seed display"
      exports: ["SettingsModal"]
    - path: "src/components/StartPage/CreditsModal.tsx"
      provides: "Credits modal with game title, author, technology, closing lore"
      exports: ["CreditsModal"]
  key_links:
    - from: "src/components/StartPage/SettingsModal.tsx"
      to: "src/components/shared/Modal.tsx"
      via: "Modal, Modal.Header, Modal.Body composition"
      pattern: "import.*Modal.*from.*shared/Modal"
    - from: "src/components/StartPage/CreditsModal.tsx"
      to: "src/components/shared/Modal.tsx"
      via: "Modal, Modal.Header, Modal.Body composition"
      pattern: "import.*Modal.*from.*shared/Modal"
    - from: "src/components/StartPage/StartPage.tsx"
      to: "src/components/StartPage/useThemeMusic.ts"
      via: "setVolume prop passed from useThemeMusic hook to SettingsModal"
      pattern: "setVolume"
---

<objective>
Replace stub Settings and Credits modals with full implementations: volume slider wired to audio, fog default toggle, version display, and scrollable credits content.

Purpose: Complete the start screen feature set with functional modal overlays.
Output: Both modals work as specified, start screen is feature-complete.
</objective>

<execution_context>
@C:/Users/chris/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-start-screen/09-CONTEXT.md
@.planning/phases/09-start-screen/09-UI-SPEC.md
@.planning/phases/09-start-screen/09-01-SUMMARY.md
@src/components/StartPage/StartPage.tsx
@src/components/StartPage/startPageConstants.ts
@src/components/StartPage/useThemeMusic.ts
@src/components/shared/Modal.tsx

<interfaces>
From src/components/shared/Modal.tsx:
```typescript
interface ModalProps {
  open: boolean;
  onClose: () => void;
  maxWidth?: number;
  animation?: 'anim-fade' | 'anim-fade-up';
  children: React.ReactNode;
}
// Composition: Modal.Header({ children, onClose? }), Modal.Body({ children }), Modal.Footer({ children })
```

From src/components/StartPage/useThemeMusic.ts (from Plan 01 -- includes setVolume):
```typescript
export function useThemeMusic(src: string): {
  play: () => void;
  fadeOut: () => Promise<void>;
  muted: boolean;
  toggleMute: () => void;
  setVolume: (v: number) => void;
}
```

From src/components/StartPage/startPageConstants.ts:
```typescript
export const VERSION_STAMP_TEXT = 'v0.1.0 · prototype';
export const THEME_VOLUME_DEFAULT = 0.4;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Settings and Credits modals</name>
  <files>src/components/StartPage/SettingsModal.tsx, src/components/StartPage/CreditsModal.tsx, src/components/StartPage/StartPage.tsx</files>
  <read_first>
    - src/components/StartPage/SettingsModal.tsx (current stub from Plan 01)
    - src/components/StartPage/CreditsModal.tsx (current stub from Plan 01)
    - src/components/StartPage/StartPage.tsx (current component -- need to see how modals are wired, how useThemeMusic is called)
    - src/components/StartPage/useThemeMusic.ts (hook API -- confirm setVolume is exposed from Plan 01)
    - src/components/StartPage/startPageConstants.ts (VERSION_STAMP_TEXT, THEME_VOLUME_DEFAULT)
    - src/components/shared/Modal.tsx (Modal API: Header, Body, Footer composition)
    - .planning/phases/09-start-screen/09-UI-SPEC.md (interaction contract, copywriting contract)
    - .planning/phases/09-start-screen/09-CONTEXT.md (Settings stub contents, Credits contents)
  </read_first>
  <action>
**Part A: Wire setVolume from useThemeMusic to SettingsModal in StartPage.tsx**

Plan 01 already created useThemeMusic with `setVolume` in its return type. Update StartPage.tsx to:
1. Destructure `setVolume` from the useThemeMusic hook call (alongside play, fadeOut, muted, toggleMute)
2. Add a `const [volume, setVolumeState] = useState(THEME_VOLUME_DEFAULT)` for the slider's controlled value
3. Create `handleVolumeChange = (v: number) => { setVolumeState(v); setVolume(v); }` to sync UI state and audio
4. Pass `volume`, `onVolumeChange={handleVolumeChange}`, `muted`, `onToggleMute={toggleMute}` as props to SettingsModal

**Part B: Replace SettingsModal.tsx stub**

Replace the stub content in `src/components/StartPage/SettingsModal.tsx` with:

```typescript
import { useState } from 'react';
import { Modal } from '../shared/Modal';
import { VERSION_STAMP_TEXT, THEME_VOLUME_DEFAULT } from './startPageConstants';

interface SettingsModalProps {
  open: boolean;
  onClose: () => void;
  volume: number;
  onVolumeChange: (v: number) => void;
  muted: boolean;
  onToggleMute: () => void;
}
```

Modal content (all using inline styles with CSS custom properties):

1. **Volume control section:**
   - Label: "Music Volume" in `--font-display`, `--text-sm`, `--text-muted`, uppercase, `letter-spacing: 0.1em`
   - HTML `<input type="range" min="0" max="1" step="0.05">` styled with accent-color `var(--accent-gold)`
   - Value bound to `volume` prop, onChange calls `onVolumeChange(parseFloat(e.target.value))`
   - Mute checkbox or the existing mute toggle info: "Muted" label next to a simple toggle

2. **Fog default toggle section:**
   - Label: "Fog of War on Start" in same style as volume label
   - `<input type="checkbox">` -- this is a STUB; it reads/writes to `localStorage.getItem('threadbare_fog_default')`
   - Default: unchecked (fog off by default, matching existing behavior per CLAUDE.md `?fog` param)

3. **Version display section:**
   - Label: "Version" in same style
   - Value: `VERSION_STAMP_TEXT` in `--text-secondary`
   - Seed display: "Seed: 42" (hardcoded for now, no dynamic seed access from start screen)

4. **Layout:** Vertical stack with `gap: var(--space-6)` between sections. Each section has label + control with `gap: var(--space-2)`.

**Part C: Replace CreditsModal.tsx stub**

Replace the stub content in `src/components/StartPage/CreditsModal.tsx` with:

```typescript
import { Modal } from '../shared/Modal';

interface CreditsModalProps {
  open: boolean;
  onClose: () => void;
}
```

Modal content (scrollable via Modal.Body):

1. **Game title:** "THREADBARE" in `--font-display`, `--accent-gold`, centered, `letter-spacing: 0.15em`
2. **Separator:** 1px line in `--border-subtle`, `margin: var(--space-4) 0`
3. **Author section:** "A game by [Author Name]" in `--text-secondary` (use placeholder "A game by the creator" -- the user hasn't specified their name)
4. **Technology section:**
   - Heading: "Built With" in `--font-display`, `--text-muted`, uppercase, small
   - List items in `--text-secondary`: "React", "Three.js", "TypeScript", "Vite"
   - Simple centered list, no bullets
5. **Separator**
6. **Closing lore line:** "The threads continue." in italic `--font-body`, `--text-secondary`, centered
7. **All text centered**, `max-width` inherited from Modal (600px default is fine)
  </action>
  <verify>
    <automated>cd "C:/Users/chris/Dev/Projects/TheFantasyWorldSimulator" && npx tsc --noEmit 2>&1 | head -20 && npx vitest run src/components/StartPage 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/StartPage/SettingsModal.tsx contains `input type="range"`
    - src/components/StartPage/SettingsModal.tsx contains `VERSION_STAMP_TEXT`
    - src/components/StartPage/SettingsModal.tsx contains `onVolumeChange`
    - src/components/StartPage/CreditsModal.tsx contains `THREADBARE`
    - src/components/StartPage/CreditsModal.tsx contains `The threads continue`
    - src/components/StartPage/CreditsModal.tsx contains `Built With`
    - src/components/StartPage/StartPage.tsx imports SettingsModal and CreditsModal
    - src/components/StartPage/StartPage.tsx passes setVolume-derived handler to SettingsModal
    - `npx tsc --noEmit` exits 0
    - `npx vitest run src/components/StartPage` exits 0
  </acceptance_criteria>
  <done>Settings modal shows volume slider (wired to audio via setVolume from useThemeMusic), fog toggle (localStorage stub), and version display. Credits modal shows game title, author placeholder, technology list, and closing lore. Both modals use existing Modal primitive with gold border and dark backdrop. TypeScript, build, and tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete start screen</name>
  <what-built>
    Complete start screen with:
    - Full-bleed title art background with gradient overlay
    - "THREADBARE" gold title with text shadow
    - Two-line lore fragment in italic
    - Plain text menu: New World, Settings, Credits
    - Mute toggle (bottom-left) and version stamp (bottom-right)
    - Theme music plays on first click, fades in over 3s
    - "New World" fades out audio and page, transitions to worldgen
    - Settings modal with volume slider, fog toggle, version
    - Credits modal with game title, tech credits, closing lore
    - Image/audio fail-soft (works without assets)
  </what-built>
  <how-to-verify>
    1. Open http://localhost:5173 -- start screen should appear with title art, gradient, title text, lore, menu
    2. Click anywhere -- ambient drone music should begin fading in
    3. Click the mute icon (bottom-left) -- music should mute. Refresh page -- mute state should persist
    4. Click "Settings" -- modal should open with volume slider, fog toggle, version
    5. Adjust volume slider -- music volume should change in real-time
    6. Close Settings (Escape or backdrop click or X button)
    7. Click "Credits" -- modal should open with game title, tech credits, closing lore
    8. Close Credits
    9. Click "New World" -- page should fade out over ~600ms, music should fade, then worldgen screen appears
    10. Navigate to http://localhost:5173/?view=game -- should skip start screen entirely and load game view
    11. Navigate to http://localhost:5173/?view=hexv2 -- should skip start screen and load hex renderer
    12. Check that there is no "Continue" menu item visible
    13. Verify 1920x1080 viewport: nothing scrolls, nothing below the fold
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` -- zero TypeScript errors
2. `npx vite build` -- production build succeeds
3. `npm test` -- all tests pass (no regressions)
4. Visual check: start screen renders correctly at 1920x1080
5. Settings and Credits modals open/close correctly
6. Audio plays, mutes, persists mute, fades out on New World
</verification>

<success_criteria>
- Settings modal has working volume slider that adjusts music volume via setVolume from useThemeMusic hook
- Credits modal has game title, technology credits, and closing lore line
- Both modals use existing Modal primitive (no custom modal implementation)
- Both modals dismiss via Escape, backdrop click, and close button
- Start screen is visually complete and feature-complete per 09-UI-SPEC.md
- Human verification passes
</success_criteria>

<output>
After completion, create `.planning/phases/09-start-screen/09-02-SUMMARY.md`
</output>
