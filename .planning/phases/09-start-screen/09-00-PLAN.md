---
phase: 09-start-screen
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/components/StartPage/__tests__/StartPage.test.tsx
  - src/components/StartPage/__tests__/useThemeMusic.test.ts
  - src/__tests__/App.test.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Wave 0 test scaffolds exist so all downstream plan verify commands can run"
  artifacts:
    - path: "src/components/StartPage/__tests__/StartPage.test.tsx"
      provides: "Component render test scaffold for StartPage, SettingsModal, CreditsModal"
      contains: "describe.*StartPage"
    - path: "src/components/StartPage/__tests__/useThemeMusic.test.ts"
      provides: "Unit test scaffold for useThemeMusic hook"
      contains: "describe.*useThemeMusic"
    - path: "src/__tests__/App.test.tsx"
      provides: "Integration test scaffold for GamePhase transitions and dev shortcuts"
      contains: "describe.*App"
  key_links: []
---

<objective>
Create Wave 0 test scaffolds for Phase 9 so all downstream plans have working automated verify commands.

Purpose: Nyquist compliance -- every implementation task can run automated tests immediately after completion.
Output: Three test files with pending/skip test cases matching VALIDATION.md requirements.
</objective>

<execution_context>
@C:/Users/chris/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-start-screen/09-CONTEXT.md
@.planning/phases/09-start-screen/09-VALIDATION.md
@src/components/shared/__tests__/Modal.test.tsx

<interfaces>
<!-- Test pattern from existing codebase -->
From src/components/shared/__tests__/Modal.test.tsx:
```typescript
// @vitest-environment jsdom
import { describe, it, expect, vi } from 'vitest';
import { render, screen, fireEvent } from '@testing-library/react';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Wave 0 test scaffolds</name>
  <files>src/components/StartPage/__tests__/StartPage.test.tsx, src/components/StartPage/__tests__/useThemeMusic.test.ts, src/__tests__/App.test.tsx</files>
  <read_first>
    - src/components/shared/__tests__/Modal.test.tsx (test pattern: vitest-environment jsdom, @testing-library/react imports)
    - .planning/phases/09-start-screen/09-VALIDATION.md (required test cases mapped to requirements SC-1 through SC-6)
    - .planning/phases/09-start-screen/09-CONTEXT.md (component API shape: StartPage props, useThemeMusic return type)
  </read_first>
  <action>
Create three test scaffold files following the existing `__tests__/` directory pattern. Each file uses `// @vitest-environment jsdom`, imports from vitest and @testing-library/react, and contains `describe` blocks with `it.todo()` cases that match the VALIDATION.md requirements. The tests are placeholders -- they compile and run (all skip/todo) so verify commands succeed immediately.

**File 1: `src/components/StartPage/__tests__/StartPage.test.tsx`**

```typescript
// @vitest-environment jsdom
import { describe, it } from 'vitest';

describe('StartPage', () => {
  it.todo('renders title text THREADBARE');           // SC-1
  it.todo('renders lore fragment');                    // SC-1
  it.todo('renders menu items: New World, Settings, Credits'); // SC-1
  it.todo('does not render Continue menu item');       // SC-1
  it.todo('renders mute toggle button');               // SC-1
  it.todo('renders version stamp');                    // SC-1
  it.todo('calls onNewWorld when New World is clicked'); // SC-2
  it.todo('degrades gracefully when bg image fails');  // SC-6
});

describe('SettingsModal', () => {
  it.todo('renders volume slider');                    // SC-5
  it.todo('renders fog default toggle');               // SC-5
  it.todo('renders version display');                  // SC-5
  it.todo('volume slider calls onVolumeChange');       // SC-5
});

describe('CreditsModal', () => {
  it.todo('renders game title');                       // SC-5
  it.todo('renders technology credits');               // SC-5
  it.todo('renders closing lore line');                // SC-5
});
```

**File 2: `src/components/StartPage/__tests__/useThemeMusic.test.ts`**

```typescript
// @vitest-environment jsdom
import { describe, it } from 'vitest';

describe('useThemeMusic', () => {
  it.todo('returns play, fadeOut, muted, toggleMute'); // SC-4
  it.todo('play() calls audio.play()');                // SC-4
  it.todo('fadeOut() returns a Promise');               // SC-4
  it.todo('toggleMute persists to localStorage');      // SC-4
  it.todo('reads initial mute state from localStorage'); // SC-4
  it.todo('setVolume clamps between 0 and 1');         // SC-4
});
```

**File 3: `src/__tests__/App.test.tsx`**

```typescript
// @vitest-environment jsdom
import { describe, it } from 'vitest';

describe('App', () => {
  describe('GamePhase start screen', () => {
    it.todo('defaults to start phase when no URL params');  // SC-2
    it.todo('transitions from start to worldgen on New World'); // SC-2
  });

  describe('dev view shortcuts', () => {
    it.todo('?view=game bypasses start screen');   // SC-3
    it.todo('?view=hexv2 bypasses start screen');  // SC-3
  });
});
```

Implementation notes:
- Use `it.todo()` (not `it.skip()`) so vitest reports them as "todo" not "skipped" -- cleaner output
- Do NOT import the actual components yet (they don't exist in Wave 0). The `it.todo()` pattern requires no imports.
- Plan 01 and 02 executors will fill in test implementations as they create the production code
- Create `src/components/StartPage/__tests__/` directory and `src/__tests__/` directory if they don't exist
  </action>
  <verify>
    <automated>cd "C:/Users/chris/Dev/Projects/TheFantasyWorldSimulator" && npx vitest run src/components/StartPage/__tests__ src/__tests__/App.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>All three test files exist, vitest runs them successfully (all cases reported as "todo"), and downstream plan verify commands will not fail due to missing test files.</done>
</task>

</tasks>

<verification>
1. `npx vitest run src/components/StartPage/__tests__` -- runs without error, shows todo tests
2. `npx vitest run src/__tests__/App.test.tsx` -- runs without error, shows todo tests
3. All three files follow existing `__tests__/` directory convention
</verification>

<success_criteria>
- Three test scaffold files exist at the paths listed in VALIDATION.md Wave 0 Requirements
- All files run successfully under vitest (exit code 0)
- Test cases match VALIDATION.md requirement mapping (SC-1 through SC-6)
</success_criteria>

<output>
After completion, create `.planning/phases/09-start-screen/09-00-SUMMARY.md`
</output>
