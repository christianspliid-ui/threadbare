---
phase: 06-locations-agents
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/components/HexMapV2/locations/locationIconRegistry.ts
autonomous: true
requirements:
  - LIART-01
  - LIART-02
  - LIART-03
  - LIART-04
  - LIART-05
  - LIART-06
  - LIART-07
  - LIART-08
  - LIART-09
  - LIART-10
  - LIART-11
  - LIART-12
  - LIART-13
  - LIART-14
  - LIART-15
  - LIART-16
  - LIART-17
must_haves:
  truths:
    - "All 17 location types have production-quality hand-drawn SVG path data"
    - "Each icon has 2-4 opacity layers with sun-from-right shadow baked in"
    - "Ruin variants look like broken versions of their intact counterparts"
    - "Icons are visually distinct at their assigned size class"
  artifacts:
    - path: "src/components/HexMapV2/locations/locationIconRegistry.ts"
      provides: "17 production SVG icon definitions"
      contains: "LOCATION_ICON_REGISTRY"
  key_links:
    - from: "src/components/HexMapV2/locations/locationIconRegistry.ts"
      to: "src/components/HexMapV2/locations/locationIconTextures.ts"
      via: "registry consumed by texture cache builder"
      pattern: "LOCATION_ICON_REGISTRY"
---

<objective>
Replace all 17 placeholder SVG location icons with production-quality hand-drawn silhouettes matching the established art style standard (black #1a1a1a, sun-from-right shadow, multi-layer opacity, organic hand-drawn paths).

Purpose: The registry structure and rendering pipeline from Plan 01 are working. This plan fills in the actual art. Each icon must be recognizable at its assigned size class and stylistically unified with the terrain signifiers from Phase 5.

Output: All 17 location icons are production-ready hand-drawn SVG paths in the registry.
</objective>

<execution_context>
@C:/Users/chris/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-locations-agents/06-CONTEXT.md
@.planning/phases/06-locations-agents/06-RESEARCH.md
@.planning/phases/06-locations-agents/06-UI-SPEC.md
@.planning/phases/06-locations-agents/06-01-SUMMARY.md

<interfaces>
<!-- From Plan 01, executor needs these types: -->

From src/components/HexMapV2/locations/locationIconRegistry.ts (created in Plan 01):
```typescript
export type LocationType = 'capital' | 'city' | 'town' | 'hamlet' | 'castle' | 'fort' | 'tower' | 'temple' | 'shrine' | 'ruins' | 'ruined_city' | 'ruined_tower' | 'ruined_village' | 'mining' | 'camp' | 'battleground' | 'unexplored_poi';
export type LocationSizeClass = 'full' | 'medium' | 'small' | 'tiny';
export interface LocationIconDef {
  paths: Array<{ d: string; opacity: number }>;
  viewBox: string;
  sizeClass: LocationSizeClass;
}
export const LOCATION_SIZE_SCALE: Record<LocationSizeClass, number>;
export const LOCATION_ICON_REGISTRY: Record<LocationType, LocationIconDef>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Production SVG icons for settlements and fortifications (LIART-01 through LIART-07)</name>
  <files>src/components/HexMapV2/locations/locationIconRegistry.ts</files>
  <read_first>
    src/components/HexMapV2/locations/locationIconRegistry.ts,
    src/components/HexMapV2/signifiers/signifierRegistry.ts,
    Design/deepforest-hand-drawn.svg,
    Design/hills-hand-drawn.svg,
    Design/mountain-hand-drawn.svg,
    Design/steppes-hand-drawn.svg,
    Design/hex-icon-preview.html
  </read_first>
  <action>
    Replace the placeholder SVG paths in LOCATION_ICON_REGISTRY for 7 location types with production hand-drawn art. Each icon must follow the art style standard from Phase 5:
    - Fill color: `#1a1a1a` (rendered by the texture builder, not baked into paths)
    - Each icon has 2-4 path layers at distinct opacities (0.2, 0.4, 0.65, 1.0) for depth
    - Sun-from-right: shadow shapes on the LEFT side of structures (viewer's perspective)
    - Organic paths: no perfect arcs, no mechanical precision — irregular hand-drawn curves
    - ViewBox: `0 0 100 100` (normalized square)
    - Filled paths only — no strokes, no gradients

    Icons to create:

    LIART-01 capital (sizeClass: full): Large castle with banner element. Main silhouette is a crenellated castle with central tall keep and flanking lower towers. Banner/flag on a pole atop the keep. Shadow layer on left wall face. 3-4 paths.

    LIART-02 city (sizeClass: full): Walled town silhouette. Multiple towers of varying height behind a crenellated curtain wall. 2-3 visible towers, wall spans full width. Shadow on left faces. 3 paths.

    LIART-03 town (sizeClass: medium): Building cluster with central spire. 3-4 buildings of varying height clustered together, tallest building has a pointed spire. Organic rooflines. 3 paths.

    LIART-04 hamlet (sizeClass: small): Small house cluster. 2-3 simple peaked-roof structures, small and modest. Thatched/simple roofline silhouettes. 2-3 paths.

    LIART-05 castle (sizeClass: medium): Fortified tower with crenellations. Single main tower with crenellated top, thicker/stockier than the town spire. Buttressed base. 3 paths.

    LIART-06 fort (sizeClass: medium): Square fortification. Low-profile rectangular fortification with corner turrets. Distinctly military — wider than tall. 2-3 paths.

    LIART-07 tower (sizeClass: small): Single tall tower. Slim vertical silhouette, pointed or flat top, much taller than wide. Distinct from castle (thinner, no crenellations). 2-3 paths.

    For each icon, check hex-icon-preview.html for existing SVG symbols as reference starting points. Rework to match the sun-from-right multi-layer treatment.

    Verify visual distinction: capital must be clearly larger and more complex than city; castle must be heavier than tower; hamlet must be simpler than town.
  </action>
  <verify>
    <automated>cd "C:/Users/chris/Dev/Projects/TheFantasyWorldSimulator" && npx vitest run src/components/HexMapV2/locations/__tests__/locationIconRegistry.test.ts 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - LOCATION_ICON_REGISTRY.capital.paths has 3+ entries with opacity values spanning 0.2-1.0
    - LOCATION_ICON_REGISTRY.city.paths has 3+ entries
    - LOCATION_ICON_REGISTRY.town.paths has 2+ entries
    - LOCATION_ICON_REGISTRY.hamlet.paths has 2+ entries
    - LOCATION_ICON_REGISTRY.castle.paths has 2+ entries
    - LOCATION_ICON_REGISTRY.fort.paths has 2+ entries
    - LOCATION_ICON_REGISTRY.tower.paths has 2+ entries
    - All 7 entries have viewBox === '0 0 100 100'
    - No path.d contains 'A' arc commands with perfectly round radii (organic paths only)
    - Registry test passes
  </acceptance_criteria>
  <done>7 settlement/fortification icons have production-quality multi-layer SVG paths matching the Phase 5 art style, each visually distinct at its size class.</done>
</task>

<task type="auto">
  <name>Task 2: Production SVG icons for religious, ruins, utility, and special types (LIART-08 through LIART-17)</name>
  <files>src/components/HexMapV2/locations/locationIconRegistry.ts</files>
  <read_first>
    src/components/HexMapV2/locations/locationIconRegistry.ts,
    Design/hex-icon-preview.html
  </read_first>
  <action>
    Replace the remaining 10 placeholder SVG paths in LOCATION_ICON_REGISTRY with production hand-drawn art. Same art style as Task 1.

    Icons to create:

    LIART-08 temple (sizeClass: medium): Domed or multi-spired building. Central dome with flanking smaller spires/pinnacles. Religious architecture — distinct from castle/fort. 3 paths.

    LIART-09 shrine (sizeClass: small): Small arch or standing stone arrangement. Could be a dolmen (two uprights + capstone) or small archway. Simple, mystical. 2-3 paths.

    LIART-10 ruins (sizeClass: small): Broken building. Crumbled walls with gaps, partial structure remaining. Generic ruin silhouette. 2-3 paths.

    LIART-11 ruined_city (sizeClass: medium): Broken castle/city. Use the city (LIART-02) as base — remove sections of wall, topple one tower, add rubble shapes at base. Must read as "this was once a city." 3 paths.

    LIART-12 ruined_tower (sizeClass: small): Broken tower. Use tower (LIART-07) as base — truncate the top at an irregular angle, add crack lines in silhouette. Must read as "broken tower." 2-3 paths.

    LIART-13 ruined_village (sizeClass: small): Broken houses. Use hamlet (LIART-04) as base — collapse one roof, lean another structure, add gap in wall. Must read as "abandoned/destroyed village." 2-3 paths.

    LIART-14 mining (sizeClass: small): Pick and mine entrance or headframe. Triangular headframe structure above a dark opening, or crossed pickaxe silhouette. Industrial/utilitarian. 2-3 paths.

    LIART-15 camp (sizeClass: small): Tent silhouette. Single conical tent or 2-tent cluster. Peaked fabric shapes, possibly with smoke wisps. 2 paths.

    LIART-16 battleground (sizeClass: small): Crossed swords silhouette. Two swords crossing in X formation. Simple, iconic, immediately readable. 2-3 paths.

    LIART-17 unexplored_poi (sizeClass: tiny): Question mark or generic pointed marker. Simple upside-down teardrop with question mark cutout, or just a "?" glyph as organic path. 2 paths.

    CRITICAL for ruins: ruined_city, ruined_tower, and ruined_village must look like BROKEN versions of their intact counterparts (city, tower, hamlet). Not generic rubble. Read the intact version's paths first, then create damaged variants.
  </action>
  <verify>
    <automated>cd "C:/Users/chris/Dev/Projects/TheFantasyWorldSimulator" && npx vitest run src/components/HexMapV2/locations/__tests__/locationIconRegistry.test.ts 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - LOCATION_ICON_REGISTRY has exactly 17 entries (Object.keys length === 17)
    - All 10 new entries have non-empty paths arrays (2+ entries each)
    - All 10 new entries have viewBox === '0 0 100 100'
    - ruined_city.sizeClass === 'medium', ruined_tower.sizeClass === 'small', ruined_village.sizeClass === 'small'
    - unexplored_poi.sizeClass === 'tiny'
    - Registry test passes confirming all 17 types present
  </acceptance_criteria>
  <done>All 17 location icons have production-quality hand-drawn SVG paths. Ruin variants visibly correspond to their intact counterparts. Full LIART catalog complete.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/components/HexMapV2/locations/__tests__/locationIconRegistry.test.ts` — all tests pass
- `npm run build` — zero TypeScript errors
- All 17 LocationType values have entries in LOCATION_ICON_REGISTRY
- Every entry has 2+ paths with opacity layers
- No placeholder SVG paths remain
</verification>

<success_criteria>
All 17 location icons are production-quality hand-drawn SVG silhouettes matching the Phase 5 art style. Icons are visually distinct at their assigned size classes. Ruin variants are recognizable as broken versions of intact counterparts.
</success_criteria>

<output>
After completion, create `.planning/phases/06-locations-agents/06-02-SUMMARY.md`
</output>
