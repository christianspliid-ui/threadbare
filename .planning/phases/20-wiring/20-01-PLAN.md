---
phase: 20-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Game/GameView.tsx
  - src/components/Game/hooks/useTargetActions.ts
autonomous: true
requirements:
  - WIRE-01
  - WIRE-02

must_haves:
  truths:
    - "Clicking a notification that references a hex pans and zooms the camera smoothly to that hex"
    - "Avatar position is resolved and available in the targetActions context for range gating"
  artifacts:
    - path: "src/components/Game/GameView.tsx"
      provides: "onFocusHex callback using animateCameraTo via centerOn"
      contains: "centerOn"
    - path: "src/components/Game/hooks/useTargetActions.ts"
      provides: "avatarPos passed to getTargetActionSlots"
      contains: "getAvatarHexPosition"
  key_links:
    - from: "ToastStack/AlertBar onNavigate"
      to: "useNotificationNavigation onFocusHex"
      via: "handleNotificationNavigate callback"
      pattern: "onFocusHex.*col.*row"
    - from: "useTargetActions"
      to: "targetActions.ts range gating"
      via: "avatarPos parameter"
      pattern: "avatarPos.*getAvatarHexPosition"
---

<objective>
Verify and confirm WIRE-01 (notification hex camera animation) and WIRE-02 (avatar position in targetActions) are working correctly.

Purpose: Both wiring paths appear to already be connected based on code inspection. WIRE-01 uses `centerOn` which delegates to `animateCameraTo` with smooth eased animation. WIRE-02 already calls `getAvatarHexPosition()` and passes `avatarPos` to `getTargetActionSlots()`. This plan verifies both chains work end-to-end at runtime and documents findings.

Output: Verified wiring with test confirmation or targeted fixes if broken.
</objective>

<execution_context>
@C:/Users/chris/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-wiring/20-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify WIRE-01 camera animation chain and WIRE-02 avatar position chain</name>
  <files>src/components/Game/GameView.tsx, src/components/Game/hooks/useTargetActions.ts</files>
  <read_first>
    - src/components/Game/GameView.tsx (lines 427-436 — onFocusHex callback)
    - src/components/HexMapV2/HexMapV2.tsx (lines 392-400 — centerOn imperative handle)
    - src/components/HexMapV2/camera/CameraAnimator.ts (lines 45-69 — animateCameraTo implementation)
    - src/components/Game/hooks/useNotificationNavigation.ts (full file — navigation dispatch)
    - src/components/Game/hooks/useTargetActions.ts (full file — avatarPos usage)
    - src/engine/targetActions.ts (line 179 area — range gating with avatarPos)
    - src/engine/visibility.ts (line 32 area — getAvatarHexPosition implementation)
  </read_first>
  <action>
    **WIRE-01 verification:** The notification hex camera chain is:
    1. ToastStack/AlertBar click triggers `onNavigate(target)` → `handleNotificationNavigate`
    2. `useNotificationNavigation` dispatches `case 'hex': deps.onFocusHex(target.col, target.row)`
    3. GameView.tsx:429 `onFocusHex` calls `hexMapRef.current.centerOn(px.x, -px.y, RETINUE_EYE_ZOOM_SCALE)`
    4. HexMapV2 `centerOn` calls `animateCameraTo(canvas, zoom, x, y, scale, JUMP_TO_DURATION_MS)`

    This chain is already wired. The `centerOn` imperative handle already delegates to `animateCameraTo` with smooth eased animation (not instant snap). `RETINUE_EYE_ZOOM_SCALE = 20` provides close/location-level zoom.

    Verify by writing a test that confirms the chain compiles and the types connect. If any link is broken, fix it.

    Specifically, write a test in `src/components/Game/__tests__/notification-navigation.test.ts` that:
    - Imports `useNotificationNavigation`
    - Verifies the `onFocusHex` callback is called with correct col/row for `kind: 'hex'` targets
    - Verifies the function signature matches the GameView usage

    **WIRE-02 verification:** The avatar position chain is:
    1. `useTargetActions.ts` calls `getAvatarHexPosition(gameState.graph, gameState.ascendantId)`
    2. Result passed as `avatarPos` to `getTargetActionSlots()`
    3. `targetActions.ts:179` uses `avatarPos` for hex distance calculations

    This chain is already wired. Verify by writing a test that confirms `getAvatarHexPosition` returns valid coordinates when given a graph with an ascendant that has a located_at edge, and that `getTargetActionSlots` accepts the avatarPos parameter.

    Write a test in `src/engine/__tests__/avatar-position-chain.test.ts` that:
    - Creates a minimal WorldGraph with an ascendant node, avatar node, location node, and hex
    - Connects them with `thread` and `located_at` edges
    - Calls `getAvatarHexPosition` and asserts it returns `{ col, row }`
    - Verifies the return type matches what `getTargetActionSlots` expects for `avatarPos`

    If either chain is broken at runtime, fix the specific broken link. Based on code inspection, both chains appear fully wired.
  </action>
  <verify>
    <automated>npx vitest run src/components/Game/__tests__/notification-navigation.test.ts src/engine/__tests__/avatar-position-chain.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/Game/__tests__/notification-navigation.test.ts exists and passes
    - src/engine/__tests__/avatar-position-chain.test.ts exists and passes
    - `npm test` exits 0 (no regressions)
    - `npx tsc --noEmit` exits 0 (type-safe)
    - If fix was needed: the specific file is modified and the chain works
    - If no fix needed: tests confirm existing wiring is correct
  </acceptance_criteria>
  <done>Both WIRE-01 and WIRE-02 chains are verified with passing tests. Camera animation uses smooth eased animateCameraTo (not instant snap). Avatar position is resolved and passed to targetActions range gating.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/components/Game/__tests__/notification-navigation.test.ts src/engine/__tests__/avatar-position-chain.test.ts` — both pass
- `npm test` — no regressions
- `npx tsc --noEmit` — clean
</verification>

<success_criteria>
- WIRE-01: The notification→camera chain is verified working with a test
- WIRE-02: The avatar position→targetActions chain is verified working with a test
- No regressions in existing tests or type checking
</success_criteria>

<output>
After completion, create `.planning/phases/20-wiring/20-01-SUMMARY.md`
</output>
