---
phase: 10-sphere-affinity
plan: 07
type: execute
wave: 5
depends_on: [10-03, 10-03b, 10-04, 10-05, 10-06]
files_modified: []
autonomous: false

requirements: [SPHR-09, SPHR-10, SPHR-11]

must_haves:
  truths:
    - "Full sphere pressure pipeline runs without crashes over 30+ ticks"
    - "WorldSoulIndicator displays prose in the top bar"
    - "HexChronicle shows Soul layer paragraph when clicking a hex"
    - "Debug panel Sphere State tab shows raw numbers"
    - "ProseKeyword tooltips appear on hover/focus"
    - "Sphere scores change over time as pressure events resolve"
---

<objective>
Integration smoke test and human visual verification of the complete sphere affinity system.

Purpose: Verify the full pipeline end-to-end — upstream pressure -> resolution -> aggregation -> UI display — runs without crashes and looks correct to human eyes.
Output: Verified working system or list of issues to fix.
</objective>

<execution_context>
@C:/Users/chris/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-sphere-affinity/10-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full integration smoke test via CLI and build verification</name>
  <files></files>
  <read_first>
    src/engine/orchestrator.ts
    src/engine/phaseSpherePressure.ts
    src/engine/phaseSphereAggregation.ts
  </read_first>
  <action>
1. Run `npm test` — all tests pass with no failures.
2. Run `npx tsc --noEmit` — type check clean.
3. Run `npx vite build` — production build succeeds (confirms Vercel will deploy cleanly).
4. Run `npm run cli -- --seed 42` and execute:
   - `tick 30` — run 30 ticks, verify no crashes
   - `status` — verify game state summary shows no errors
   - `eval JSON.stringify(Object.keys(state.graph.nodes).slice(0,3).map(k => ({ id: k, hasSphereAffinity: !!state.graph.nodes[k].properties.sphereAffinity })))` — verify entities have sphereAffinity
   - `eval state.worldSoul.aggregate?.dominantSphere` — verify aggregate computed
   - `eval (state.pendingSpherePressures ?? []).length` — verify accumulator cleared (should be 0 after tick)

5. If any issues found, fix them before proceeding to human verification.
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit && npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - npm test exits 0
    - npx tsc --noEmit exits 0
    - npx vite build exits 0
    - CLI runs 30 ticks without crash
  </acceptance_criteria>
  <done>Full test suite, type check, and production build pass. CLI smoke test confirms sphere system runs end-to-end.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human visual verification of sphere affinity UI</name>
  <files></files>
  <action>
Present the complete sphere affinity system to the user for visual verification. Start the dev server if not already running. Guide the user through the verification steps below.
  </action>
  <verify>User confirms approval or provides issue list</verify>
  <done>User approves the sphere UI or issues are logged for gap closure.</done>
  <what-built>
Complete sphere affinity system:
- Per-entity sphere scores on all graph nodes (hexes, agents, locations)
- Sphere pressure pipeline: upstream phases push pressure -> resolution engine -> score changes
- Global World-Soul aggregate computed from all entity scores
- WorldSoulIndicator prose in top bar
- HexChronicle Soul layer paragraph with IPK keywords
- Action preview showing sphere consequences
- Debug panel Sphere State tab with raw numbers
- ProseKeyword component: bold/underlined/sphere-colored text with tooltips
- Magic power calculation with overchannel
  </what-built>
  <how-to-verify>
1. Start dev server: `npm run dev`
2. Open `http://localhost:5173/?view=game` in browser
3. **WorldSoulIndicator:** Look at the top bar area — should see an italic prose sentence describing the world's dominant sphere character (e.g., "The drums of Force beat steady across the land.")
4. **HexChronicle Soul layer:** Click on a hex — in the hex chronicle panel, look for a "Soul" paragraph describing that hex's sphere character. Sphere names should be **bold**, underlined, and colored.
5. **ProseKeyword tooltips:** Hover over a sphere name in the Soul paragraph — a tooltip should appear with a narrative description of that sphere. Try keyboard: Tab to focus, check tooltip appears.
6. **Debug panel:** Open the debug panel (if there's a toggle). Find "Sphere State" tab. Click a hex or agent — should see all 8 sphere scores with colored bars and progress indicators.
7. **Action preview:** If applicable, open the action drawer and look at sphere-tagged actions. Should see a prose preview of sphere consequences.
8. **Run for 30+ ticks** (let the game play or use debug controls) — verify the WorldSoulIndicator text changes as sphere balance shifts. Check debug panel to see scores changing.
  </how-to-verify>
  <resume-signal>Type "approved" if the sphere UI looks correct and the system is stable, or describe any visual/functional issues</resume-signal>
</task>

</tasks>

<verification>
Full system verified end-to-end via automated tests, CLI smoke test, and human visual inspection.
</verification>

<success_criteria>
The complete sphere affinity system runs without crashes over 30+ ticks. UI components display sphere information through narrative prose. ProseKeyword tooltips are accessible. Debug panel shows raw numbers. Human confirms visual output is correct.
</success_criteria>

<output>
After completion, create `.planning/phases/10-sphere-affinity/10-07-SUMMARY.md`
</output>
