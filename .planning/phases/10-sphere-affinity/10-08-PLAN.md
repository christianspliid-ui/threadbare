---
phase: 10-sphere-affinity
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - src/engine/hexZoom.ts
  - src/engine/__tests__/hexZoom.test.ts
autonomous: true
gap_closure: true
requirements:
  - SPHR-17
  - SPHR-19

must_haves:
  truths:
    - "getHexSphereInfluence reads node.properties.sphereAffinity.scores and normalizes to 0-1 float"
    - "HexChronicle Soul layer receives non-zero sphere influence data for hexes with location nodes that have sphereAffinity"
    - "ProseKeyword tooltips render because sphere keywords now appear in Soul prose"
  artifacts:
    - path: "src/engine/hexZoom.ts"
      provides: "Bridge adapter from SphereAffinity integer scores to SphereInfluence 0-1 floats"
      contains: "sphereAffinity"
    - path: "src/engine/__tests__/hexZoom.test.ts"
      provides: "Updated tests verifying new sphereAffinity-based data path"
      contains: "sphereAffinity"
  key_links:
    - from: "src/engine/hexZoom.ts"
      to: "node.properties.sphereAffinity"
      via: "getHexSphereInfluence reads .sphereAffinity.scores"
      pattern: "sphereAffinity.*scores"
    - from: "src/engine/hexZoom.ts"
      to: "MAX_SPHERE_SCORE"
      via: "import from types/sphereAffinity for normalization divisor"
      pattern: "MAX_SPHERE_SCORE"
---

<objective>
Close the HexChronicle Soul layer data bridge gap identified in Phase 10 verification.

Purpose: `getHexSphereInfluence()` in `hexZoom.ts` reads the old `sphereInfluence`/`sphereBiases` properties (which no longer exist on location nodes after Phase 10). It must read `node.properties.sphereAffinity.scores` (integer 0-10) and normalize to 0-1 float by dividing by `MAX_SPHERE_SCORE=10`. This single fix unblocks both the HexChronicle Soul layer content and ProseKeyword tooltips.
Output: Updated `hexZoom.ts` with working bridge + updated tests
</objective>

<execution_context>
@C:/Users/chris/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-sphere-affinity/10-VERIFICATION.md

<interfaces>
<!-- Key types the executor needs -->

From src/types/sphereAffinity.ts:
```typescript
export interface SphereAffinity {
  scores: Record<SphereName, number>;  // integer 0-10
  progress: Record<SphereName, number>;
}
export const MAX_SPHERE_SCORE = 10;
```

From src/types/index.ts:
```typescript
export const SPHERE_NAMES = ['force', 'matter', 'energy', 'life', 'mind', 'spirit', 'time', 'entropy'] as const;
export type SphereName = typeof SPHERE_NAMES[number];
```

From src/engine/hexZoom.ts (current — to be changed):
```typescript
export type SphereInfluence = Record<SphereName, number>;  // 0-1 float
export function getHexSphereInfluence(graph: WorldGraph, col: number, row: number): SphereInfluence
```

Location nodes store sphereAffinity as:
```typescript
graph.updateNode(locNode.id, { properties: { sphereAffinity: locAffinity } });
// locAffinity is SphereAffinity { scores: Record<SphereName, number>, progress: ... }
```

HexChronicle consumes SphereInfluence (0-1 floats) via getSoulLevel():
```typescript
function getSoulLevel(value: number): HexSoulLevel  // thresholds: 0.2, 0.4, 0.7
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update getHexSphereInfluence to read sphereAffinity and normalize to 0-1</name>
  <files>src/engine/hexZoom.ts, src/engine/__tests__/hexZoom.test.ts</files>
  <read_first>
    - src/engine/hexZoom.ts (current implementation to modify)
    - src/engine/__tests__/hexZoom.test.ts (existing tests to update)
    - src/types/sphereAffinity.ts (SphereAffinity interface, MAX_SPHERE_SCORE constant)
    - src/types/index.ts (SPHERE_NAMES, SphereName)
  </read_first>
  <behavior>
    - Test 1: Location with sphereAffinity { scores: { mind: 5 } } returns sphereInfluence.mind === 0.5 (5/10)
    - Test 2: Location with sphereAffinity { scores: { force: 10 } } returns sphereInfluence.force === 1.0 (10/10)
    - Test 3: Two locations in same hex with sphereAffinity scores aggregate: loc1 mind=3 + loc2 mind=4 => influence.mind === 0.7 (clamped sum (3+4)/10)
    - Test 4: Location with NO sphereAffinity property (legacy node) returns zero influence (fail-soft)
    - Test 5: Location with sphereAffinity where all scores are 0 returns zero influence
    - Test 6: Hex with no locations returns zero influence (existing behavior preserved)
  </behavior>
  <action>
    **In src/engine/hexZoom.ts:**

    1. Add import: `import { MAX_SPHERE_SCORE } from '../types/sphereAffinity';`
    2. Add import: `import type { SphereAffinity as SphereAffinityType } from '../types/sphereAffinity';`
    3. Replace the body of `getHexSphereInfluence()` (lines 51-67). The new implementation:
       - Still initializes `influence` as `Record<SphereName, number>` all zeros
       - Still calls `getLocationsInHex(graph, col, row)` to get location nodes
       - For each location, reads `props.sphereAffinity` as `SphereAffinityType | undefined`
       - If `sphereAffinity` exists and has `.scores`, iterates SPHERE_NAMES and adds `sphereAffinity.scores[s] / MAX_SPHERE_SCORE` to `influence[s]`
       - Falls back to old `sphereInfluence ?? sphereBiases` path if `sphereAffinity` is absent (fail-soft for any legacy nodes)
       - Clamps each final influence value to max 1.0 via `Math.min(1, value)` after aggregation
    4. Update the JSDoc comment on `getHexSphereInfluence` to say: "Aggregated sphere influence from all locations in the hex. Reads SphereAffinity integer scores (0-MAX_SPHERE_SCORE) and normalizes to 0-1 float. Falls back to legacy sphereInfluence/sphereBiases if sphereAffinity absent."

    **In src/engine/__tests__/hexZoom.test.ts:**

    5. Update the `beforeEach` setup: change location node properties from `sphereBiases: { mind: 0.3 }` to `sphereAffinity: { scores: { force: 0, matter: 0, energy: 0, life: 0, mind: 3, spirit: 0, time: 0, entropy: 0 }, progress: { force: 0, matter: 0, energy: 0, life: 0, mind: 0, spirit: 0, time: 0, entropy: 0 } }` (mind=3 means 3/10=0.3 normalized).
    6. Similarly update `loc.temple` from `sphereBiases: { spirit: 0.5 }` to `sphereAffinity: { scores: { ..allZeros, spirit: 5 }, progress: { ..allZeros } }` (spirit=5 means 5/10=0.5).
    7. Similarly update `loc.forge` from `sphereBiases: { force: 0.4 }` to `sphereAffinity: { scores: { ..allZeros, force: 4 }, progress: { ..allZeros } }` (force=4 means 4/10=0.4).
    8. Use a helper function `makeScores(overrides)` that returns `{ force: 0, matter: 0, energy: 0, life: 0, mind: 0, spirit: 0, time: 0, entropy: 0, ...overrides }` to keep test code DRY. Same for `makeProgress()` returning all-zero progress.
    9. Existing test assertions (`influence.mind` toBeCloseTo 0.3, `influence.spirit` toBeCloseTo 0.5) should still pass with the new data model.
    10. Add a test for the legacy fallback: a location with `sphereBiases: { mind: 0.3 }` and no `sphereAffinity` still returns `influence.mind` close to 0.3.
    11. Add a test for clamping: two locations in same hex with mind=8 each — result clamped to 1.0 not 1.6.
  </action>
  <verify>
    <automated>cd C:/Users/chris/Dev/Projects/TheFantasyWorldSimulator && npx vitest run src/engine/__tests__/hexZoom.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - src/engine/hexZoom.ts contains `import { MAX_SPHERE_SCORE } from '../types/sphereAffinity'`
    - src/engine/hexZoom.ts getHexSphereInfluence body contains `sphereAffinity` (not just `sphereInfluence` or `sphereBiases`)
    - src/engine/hexZoom.ts contains `/ MAX_SPHERE_SCORE` for normalization
    - src/engine/hexZoom.ts contains `Math.min(1,` for clamping
    - src/engine/hexZoom.ts still contains `sphereInfluence ?? sphereBiases` as fallback path
    - src/engine/__tests__/hexZoom.test.ts contains `sphereAffinity` in test node properties
    - All hexZoom tests pass
  </acceptance_criteria>
  <done>getHexSphereInfluence reads Phase 10 sphereAffinity integer scores, normalizes to 0-1, clamps, and falls back to legacy properties. Tests cover new path, legacy fallback, and clamping.</done>
</task>

<task type="auto">
  <name>Task 2: Verify full build and type-check pass</name>
  <files>src/engine/hexZoom.ts</files>
  <read_first>
    - src/engine/hexZoom.ts (verify the changes from Task 1)
  </read_first>
  <action>
    1. Run `npx tsc --noEmit` to confirm no type errors introduced
    2. Run `npx vite build` to confirm production build succeeds
    3. Run `npm test` to confirm no regressions across the full test suite
  </action>
  <verify>
    <automated>cd C:/Users/chris/Dev/Projects/TheFantasyWorldSimulator && npx tsc --noEmit && npx vite build && npm test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - `npx vite build` exits 0
    - `npm test` exits 0 with all tests passing
  </acceptance_criteria>
  <done>Full type-check, production build, and test suite pass with no regressions.</done>
</task>

</tasks>

<verification>
- `getHexSphereInfluence()` reads `node.properties.sphereAffinity.scores` and divides by `MAX_SPHERE_SCORE` (10) to produce 0-1 float values
- Legacy fallback to `sphereInfluence`/`sphereBiases` preserved for any nodes without `sphereAffinity`
- Aggregated values clamped to max 1.0
- All existing hexZoom tests updated and passing
- Full test suite passes, type-check clean, production build succeeds
</verification>

<success_criteria>
- HexChronicle Soul layer will receive non-zero sphereInfluence data when clicking hexes with location nodes (Gap 1 closed)
- ProseKeyword tooltips will render on sphere keywords in Soul prose (Gap 2 auto-resolved)
- No regressions in any other tests
</success_criteria>

<output>
After completion, create `.planning/phases/10-sphere-affinity/10-08-SUMMARY.md`
</output>
