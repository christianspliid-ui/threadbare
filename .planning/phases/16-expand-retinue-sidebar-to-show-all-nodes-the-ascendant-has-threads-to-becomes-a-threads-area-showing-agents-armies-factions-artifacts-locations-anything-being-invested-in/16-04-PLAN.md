---
phase: 16-expand-retinue-sidebar-threads-area
plan: 04
type: execute
wave: 1
depends_on: ["16-02"]
files_modified:
  - src/components/Game/GameView.tsx
  - src/components/Game/__tests__/GameView-interaction.test.tsx
autonomous: true
gap_closure: true
requirements: [THRD-04, THRD-07]

must_haves:
  truths:
    - "Pressing Escape while ThreadDetailView is open closes it"
    - "Pressing Escape while ThreadDetailView is closed does nothing harmful"
  artifacts:
    - path: "src/components/Game/GameView.tsx"
      provides: "Escape key handler for ThreadDetailView"
      contains: "addEventListener.*keydown"
    - path: "src/components/Game/__tests__/GameView-interaction.test.tsx"
      provides: "Test for Escape key closing ThreadDetailView"
      contains: "Escape.*thread"
  key_links:
    - from: "GameView.tsx useEffect"
      to: "handleThreadDetailClose"
      via: "keydown event listener on Escape"
      pattern: "key.*Escape.*handleThreadDetailClose"
---

<objective>
Close the single verification gap from Phase 16: wire an Escape key handler in GameView.tsx so pressing Escape dismisses the ThreadDetailView floating panel.

Purpose: VERIFICATION.md truth #7 ("Escape key closes the detail view") failed because no keydown listener was added. This is ~8 lines of code plus a test.
Output: Updated GameView.tsx with useEffect for Escape handling, plus test coverage.
</objective>

<execution_context>
@C:/Users/chris/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-expand-retinue-sidebar-to-show-all-nodes-the-ascendant-has-threads-to-becomes-a-threads-area-showing-agents-armies-factions-artifacts-locations-anything-being-invested-in/16-VERIFICATION.md
@.planning/phases/16-expand-retinue-sidebar-to-show-all-nodes-the-ascendant-has-threads-to-becomes-a-threads-area-showing-agents-armies-factions-artifacts-locations-anything-being-invested-in/16-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Escape key handler for ThreadDetailView and test</name>
  <files>src/components/Game/GameView.tsx, src/components/Game/__tests__/GameView-interaction.test.tsx</files>
  <read_first>
    - src/components/Game/GameView.tsx (current state — find where selectedThreadNode and handleThreadDetailClose are destructured, and where existing useEffect blocks live)
    - src/components/Game/hooks/useAgentInteraction.ts (lines 429-432 — handleThreadDetailClose signature)
    - src/components/Game/__tests__/GameView-interaction.test.tsx (current test patterns for keyboard events)
  </read_first>
  <action>
1. In GameView.tsx, add a useEffect that:
   - Has `[selectedThreadNode, handleThreadDetailClose]` in its dependency array
   - Only attaches a listener when `selectedThreadNode` is not null (early return otherwise)
   - Adds `document.addEventListener('keydown', handler)` where handler checks `e.key === 'Escape'` and calls `handleThreadDetailClose()`
   - Returns a cleanup function that calls `document.removeEventListener('keydown', handler)`
   - Place this useEffect near the other useEffect blocks in GameView (around the existing keyboard/interaction effects)

2. In GameView-interaction.test.tsx, add a test case:
   - Describe block: reuse existing or add "ThreadDetailView keyboard" describe
   - Test name: "pressing Escape closes ThreadDetailView when open"
   - Setup: render GameView with mock state that has `selectedThreadNode` set (via the useAgentInteraction hook mock)
   - Action: `fireEvent.keyDown(document, { key: 'Escape' })`
   - Assert: `handleThreadDetailClose` was called (or ThreadDetailView is no longer rendered)

The useEffect code should look approximately like:
```typescript
useEffect(() => {
  if (!selectedThreadNode) return;
  const handler = (e: KeyboardEvent) => {
    if (e.key === 'Escape') handleThreadDetailClose();
  };
  document.addEventListener('keydown', handler);
  return () => document.removeEventListener('keydown', handler);
}, [selectedThreadNode, handleThreadDetailClose]);
```
  </action>
  <verify>
    <automated>cd C:/Users/chris/Dev/Projects/TheFantasyWorldSimulator && npx vitest run src/components/Game/__tests__/GameView-interaction.test.tsx --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - GameView.tsx contains `addEventListener('keydown'` within a useEffect that references `selectedThreadNode`
    - GameView.tsx contains `e.key === 'Escape'` or `key === 'Escape'` followed by `handleThreadDetailClose()`
    - GameView.tsx contains `removeEventListener('keydown'` in a cleanup return
    - GameView-interaction.test.tsx contains a test with both `Escape` and `thread` (case-insensitive) in its describe/it block names
    - `npx vitest run src/components/Game/__tests__/GameView-interaction.test.tsx` exits 0
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Escape key closes ThreadDetailView when open. Test passes. Type-check clean.</done>
</task>

</tasks>

<verification>
1. `npx vitest run src/components/Game/__tests__/GameView-interaction.test.tsx` — all tests pass including new Escape key test
2. `npx tsc --noEmit` — type-check clean
3. `npx vite build` — production build succeeds
4. grep confirms: `GameView.tsx` contains `Escape` + `handleThreadDetailClose` in proximity
</verification>

<success_criteria>
- VERIFICATION.md truth #7 ("Escape key closes the detail view") now passes
- No regressions in existing GameView tests
- Type-check and build clean
</success_criteria>

<output>
After completion, create `.planning/phases/16-expand-retinue-sidebar-to-show-all-nodes-the-ascendant-has-threads-to-becomes-a-threads-area-showing-agents-armies-factions-artifacts-locations-anything-being-invested-in/16-04-SUMMARY.md`
</output>
