---
phase: 08-integration
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/HexMapV2/scene/__tests__/SignifierMesh.test.ts
  - src/components/HexMapV2/scene/__tests__/ElevationTicks.test.ts
  - src/components/HexMapV2/palette/__tests__/terrainPalette.test.ts
  - src/types/__tests__/coastline.test.ts
  - src/components/HexMap/__tests__/MovementTrails.test.tsx
autonomous: true
gap_closure: true
requirements: [INTG-06]

must_haves:
  truths:
    - "SignifierMesh tests pass with updated canvas mock (7 tests)"
    - "ElevationTicks tests pass with rewritten assertions matching new quad-mesh + plateau-only logic (7 tests)"
    - "terrainPalette test passes with updated key count (32 not 30)"
    - "coastline test passes with updated shallowWidth (0.28 not 0.19)"
    - "V1 MovementTrails test file is deleted from disk"
  artifacts:
    - path: "src/components/HexMapV2/scene/__tests__/SignifierMesh.test.ts"
      provides: "Canvas mock with translate, clip, beginPath, closePath methods"
    - path: "src/components/HexMapV2/scene/__tests__/ElevationTicks.test.ts"
      provides: "Tests using terrain='plateau' and expecting THREE.Mesh return type"
    - path: "src/components/HexMapV2/palette/__tests__/terrainPalette.test.ts"
      provides: "Assertion for 32 terrain palette keys"
    - path: "src/types/__tests__/coastline.test.ts"
      provides: "Assertion for shallowWidth = 0.28"
  key_links:
    - from: "SignifierMesh.test.ts mock"
      to: "signifierTextures.ts buildSignifierTexture"
      via: "canvas getContext mock must include translate, clip"
      pattern: "ctx\\.translate"
---

<objective>
Fix HexMapV2 test failures caused by uncommitted WIP source changes that modified constants and rendering approaches without updating their corresponding test files. Delete the stale V1 MovementTrails test that references deleted code.

Purpose: Close INTG-06 gap — these 14 test failures are from source/test divergence in working tree changes.
Output: All 5 test files fixed or deleted, 14 fewer test failures.
</objective>

<execution_context>
@C:/Users/chris/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-integration/08-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix SignifierMesh test canvas mock</name>
  <files>src/components/HexMapV2/scene/__tests__/SignifierMesh.test.ts</files>
  <read_first>
    - src/components/HexMapV2/scene/__tests__/SignifierMesh.test.ts
    - src/components/HexMapV2/signifiers/signifierTextures.ts
  </read_first>
  <action>
The SignifierMesh test fails because `buildSignifierTexture()` in signifierTextures.ts calls `ctx.translate()`, `ctx.clip()`, and `ctx.fill(path)` on the canvas 2D context, but the test mock (lines 22-29) only provides `save`, `restore`, `scale`, `fill`, `fillStyle`, `globalAlpha`.

Update the canvas mock's `getContext` return object at line 22 to include ALL methods used by `buildSignifierTexture()`:

```typescript
getContext: () => ({
  save: vi.fn(),
  restore: vi.fn(),
  scale: vi.fn(),
  translate: vi.fn(),
  clip: vi.fn(),
  fill: vi.fn(),
  beginPath: vi.fn(),
  closePath: vi.fn(),
  fillRect: vi.fn(),
  fillStyle: '',
  globalAlpha: 1,
}),
```

This matches the actual canvas 2D API methods invoked in signifierTextures.ts:
- `ctx.translate()` at line 61 and 64
- `ctx.scale()` at line 62 and 65
- `ctx.clip()` at line 69
- `ctx.fill()` at line 76
- `ctx.save()` at line 58
- `ctx.restore()` at line 78

No other changes needed — the test assertions themselves are correct (SIGNIFIER_SPRITE_SCALE=1.3 matches source).
  </action>
  <verify>
    <automated>cd C:/Users/chris/Dev/Projects/TheFantasyWorldSimulator && npx vitest run src/components/HexMapV2/scene/__tests__/SignifierMesh.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - SignifierMesh.test.ts contains `translate: vi.fn()`
    - SignifierMesh.test.ts contains `clip: vi.fn()`
    - All 7 SignifierMesh tests pass (npx vitest run outputs "7 passed")
  </acceptance_criteria>
  <done>All 7 SignifierMesh tests pass — canvas mock matches signifierTextures.ts API usage</done>
</task>

<task type="auto">
  <name>Task 2: Fix ElevationTicks, terrainPalette, coastline tests + delete V1 MovementTrails test</name>
  <files>
    src/components/HexMapV2/scene/__tests__/ElevationTicks.test.ts
    src/components/HexMapV2/palette/__tests__/terrainPalette.test.ts
    src/types/__tests__/coastline.test.ts
    src/components/HexMap/__tests__/MovementTrails.test.tsx
  </files>
  <read_first>
    - src/components/HexMapV2/scene/__tests__/ElevationTicks.test.ts
    - src/components/HexMapV2/scene/ElevationTicks.ts
    - src/components/HexMapV2/palette/__tests__/terrainPalette.test.ts
    - src/types/__tests__/coastline.test.ts
    - src/types/coastline.ts
  </read_first>
  <action>
**A. ElevationTicks.test.ts — full rewrite of assertions:**

The source changed fundamentally:
- Returns `THREE.Mesh` (not `THREE.LineSegments`)
- Only `terrain: 'plateau'` hexes emit ticks (line 182 in source)
- Only edges where neighbor is NOT plateau AND is lower elevation get ticks (lines 194, 199)
- Fixed `TICKS_PER_EDGE = 4` per qualifying edge (not density-based)
- `TICK_THRESHOLD = 0.01` (not 0.25)
- Each tick = 4 vertices (quad, two triangles) not 2 vertices (line segment)
- So 4 ticks per edge = 16 vertices per qualifying edge

Update `makeTile` helper to accept terrain parameter (default 'plateau'):
```typescript
function makeTile(col: number, row: number, elevation: number, terrain: string = 'plateau'): HexTile {
  return {
    coord: { col, row },
    geoParams: { elevation, temperature: 0.5, moisture: 0.5 },
    terrain: terrain as HexTile['terrain'],
  };
}
```

Update `twoAdjacentTiles` to create one plateau and one grassland:
```typescript
function twoAdjacentTiles(elevA: number, elevB: number): HexTile[] {
  return [makeTile(0, 0, elevA, 'plateau'), makeTile(1, 0, elevB, 'grassland')];
}
```

Update `allSameElevationGrid` to use 'plateau' terrain.

Test 1: Change `THREE.LineSegments` to `THREE.Mesh`.
Test 2: Keep as-is (plateau 0.7 vs grassland 0.2 — diff 0.5 > 0.01 threshold). Vertices should be > 0.
Test 3: elevDiff = 0.02 still > 0.01 threshold, so this WILL produce ticks now. Change test: use diff = 0.005 (below 0.01 threshold). `twoAdjacentTiles(0.5, 0.505)` → no ticks. But also need neighbor to be lower: `twoAdjacentTiles(0.505, 0.5)`. Actually the helper puts col=0 as plateau and col=1 as grassland, and the source checks `neighborElev >= tileElev` at line 199. So plateau at 0.505 with grassland neighbor at 0.5 → diff = 0.005 < 0.01 → no ticks. Good.
Test 4: Rewrite — now 4 ticks per qualifying edge, each tick = 4 vertices (quad). So 4 ticks = 16 vertices. Use `twoAdjacentTiles(0.76, 0.50)`. Comment: "TICKS_PER_EDGE=4, each tick is a quad (4 vertices), so 16 vertices per qualifying edge".
Test 5: Same result — any qualifying edge gets exactly 4 ticks = 16 vertices regardless of diff magnitude. Use `twoAdjacentTiles(0.9, 0.0)`.
Test 6 (shared edges): 3-tile row: plateau(0.7) + grassland(0.2) + grassland(0.2). Only edges from plateau to lower non-plateau neighbors qualify. Tile at (0,0) is plateau with elevation 0.7. It has neighbor at (1,0) grassland 0.2 — qualifies (diff 0.5). Tile (2,0) is NOT adjacent to (0,0) in hex grid. So only 1 qualifying edge = 16 vertices.
Test 7: allSameElevationGrid with plateau — all plateau neighbors are skipped (line 194). No ticks produced. 0 vertices.

**B. terrainPalette.test.ts:**
Line 8: Change `toHaveLength(30)` to `toHaveLength(32)`. The palette now includes `volcano` and `light_forest` as new entries (32 total keys).

**C. coastline.test.ts:**
Line 17: Change `toBe(0.19)` to `toBe(0.28)` (shallowWidth was tuned from 0.19 to 0.28 in uncommitted changes).

**D. Delete MovementTrails.test.tsx:**
Delete `src/components/HexMap/__tests__/MovementTrails.test.tsx` from disk. This file is NOT tracked by git (V1 HexMap was deleted in Phase 8 Plan 02), but it exists as an untracked file on disk and vitest discovers it. Run `rm src/components/HexMap/__tests__/MovementTrails.test.tsx`. Also check if the `src/components/HexMap/__tests__/` directory is now empty and delete it if so. And check if `src/components/HexMap/` directory itself has any remaining files and delete the entire directory if empty or only contains stale untracked files.
  </action>
  <verify>
    <automated>cd C:/Users/chris/Dev/Projects/TheFantasyWorldSimulator && npx vitest run src/components/HexMapV2/scene/__tests__/ElevationTicks.test.ts src/components/HexMapV2/palette/__tests__/terrainPalette.test.ts src/types/__tests__/coastline.test.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - ElevationTicks.test.ts Test 1 contains `THREE.Mesh` (not `THREE.LineSegments`)
    - ElevationTicks.test.ts makeTile helper has terrain parameter defaulting to 'plateau'
    - ElevationTicks.test.ts twoAdjacentTiles creates plateau + grassland pair
    - terrainPalette.test.ts contains `toHaveLength(32)`
    - coastline.test.ts contains `toBe(0.28)`
    - File `src/components/HexMap/__tests__/MovementTrails.test.tsx` does not exist on disk
    - All 7 ElevationTicks tests pass
    - All 22 terrainPalette tests pass
    - All 4 coastline tests pass
  </acceptance_criteria>
  <done>All HexMapV2 WIP test mismatches fixed, V1 stale test deleted — 14 fewer test failures</done>
</task>

</tasks>

<verification>
Run all fixed test files together:
```bash
npx vitest run src/components/HexMapV2/scene/__tests__/SignifierMesh.test.ts src/components/HexMapV2/scene/__tests__/ElevationTicks.test.ts src/components/HexMapV2/palette/__tests__/terrainPalette.test.ts src/types/__tests__/coastline.test.ts
```
All must pass. MovementTrails.test.tsx must not exist on disk.
</verification>

<success_criteria>
- 14 test failures eliminated (7 SignifierMesh + 5 ElevationTicks + 1 terrainPalette + 1 MovementTrails)
- coastline.test.ts passes (was already passing in latest run but assertion updated for correctness)
- No test file references deleted V1 HexMap code
</success_criteria>

<output>
After completion, create `.planning/phases/08-integration/08-03-SUMMARY.md`
</output>
