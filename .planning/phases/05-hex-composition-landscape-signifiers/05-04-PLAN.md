---
phase: 05-hex-composition-landscape-signifiers
plan: 04
type: execute
wave: 2
depends_on: ["05-01", "05-02"]
files_modified:
  - src/components/HexMapV2/signifiers/signifierRegistry.ts
autonomous: true
requirements:
  - LSIG-01
  - LART-13
  - LART-14
  - LART-15
  - LART-16
  - LART-17
  - LART-18
  - LART-19
  - LART-20
  - LART-21
  - LART-22
  - LART-23
  - LART-24
  - LART-25
  - LART-26
  - LART-27
  - LART-28
  - LART-29
  - LART-30

must_haves:
  truths:
    - "Highland hexes show recognizable mountain peaks, hill ridges, plateau mesas"
    - "Desert hexes show dunes, rocks, or cracked earth distinct from lowlands"
    - "Cold terrain hexes show snow/ice/tundra signifiers distinct from other types"
    - "Volcanic hexes show crater/vent/lava signifiers"
    - "Broken lands and dead forest show destruction/decay signifiers"
    - "Badlands variants include fine cracks and deep cracks patterns (LART-22 hardened_clay coverage)"
    - "Volcano variants include fresh flow and cooling patterns (LART-28 lava coverage)"
    - "All signifiers have asymmetric sun-from-right shadow"
  artifacts:
    - path: "src/components/HexMapV2/signifiers/signifierRegistry.ts"
      provides: "Production SVG path data for remaining 13+ terrain types: hills, forested_hills, mountains, high_mountains, plateau, mountain_pass, desert, sand_dunes, rocky_desert, badlands, tundra, snow_fields, glacier, volcano, broken_lands, dead_forest"
      contains: "mountains.*paths.*d:"
  key_links:
    - from: "src/components/HexMapV2/signifiers/signifierRegistry.ts"
      to: "src/components/HexMapV2/scene/SignifierMesh.ts"
      via: "SIGNIFIER_REGISTRY consumed by createSignifierMesh at scene init"
      pattern: "SIGNIFIER_REGISTRY"
---

<objective>
Replace placeholder SVG path data with production-quality hand-drawn signifier art for highland, desert, cold, volcanic, and special terrain types (remaining 13+ types, ~46 variants total).

Purpose: Completes the signifier art for all terrain types on the map. After this plan, every land hex displays a recognizable terrain signifier. Badlands and volcano entries include additional variants to satisfy LART-22 (hardened_clay) and LART-28 (lava) requirements which have no dedicated TerrainType.

Output: Updated signifierRegistry.ts with production SVG path data for LART-13 through LART-30.
</objective>

<execution_context>
@C:/Users/chris/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-hex-composition-landscape-signifiers/05-CONTEXT.md
@.planning/phases/05-hex-composition-landscape-signifiers/05-RESEARCH.md

@Design/mountain-hand-drawn.svg
@Design/hills-hand-drawn.svg

<interfaces>
From signifierRegistry.ts (Plans 01 + 03):
```typescript
export interface SignifierPath {
  d: string;       // SVG path data
  opacity: number; // 0.2-0.7 range
}
export interface SignifierVariant {
  paths: SignifierPath[];
  viewBox: string; // '0 0 100 100'
}
export const SIGNIFIER_REGISTRY: Record<string, SignifierVariant[]>;
```

Style standard reference (from Design/mountain-hand-drawn.svg):
- Two mountain peaks with irregular rocky profiles
- Left face more filled (shadow), right face thinner/open
- Path data: complex organic curves, NOT geometric triangles
- Single path element at opacity ~0.5-0.6

Style standard reference (from Design/hills-hand-drawn.svg):
- Three rows of rolling ridgelines, layered front-to-back
- Single path with multiple M...Z subpaths creating depth
- Left slopes more filled, right slopes open/thin

LART-22 (hardened_clay) and LART-28 (lava) have NO TerrainType equivalent. These requirements are satisfied by dedicated variants within related types:
- LART-22: badlands entry includes "fine cracks" and "deep cracks" variants (variants 4 and 5) matching the hardened_clay requirement description
- LART-28: volcano entry includes "fresh flow" and "cooling" variants (variants 4 and 5) matching the lava requirement description
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Production SVG signifiers for highland and desert terrain types (LART-13 through LART-23)</name>
  <files>src/components/HexMapV2/signifiers/signifierRegistry.ts</files>
  <read_first>
    src/components/HexMapV2/signifiers/signifierRegistry.ts,
    Design/mountain-hand-drawn.svg,
    Design/hills-hand-drawn.svg,
    .planning/phases/05-hex-composition-landscape-signifiers/05-CONTEXT.md
  </read_first>
  <action>
    Replace placeholder entries in SIGNIFIER_REGISTRY for these terrain types:

    **hills** (4 variants per LART-13):
    Use Design/hills-hand-drawn.svg as variant 1 (extract path d data directly).
    1. Rolling ridgelines — (from hills-hand-drawn.svg)
    2. Double hill — two rounded hills, left larger with shadow
    3. Single prominent hill — one dome shape, shadow on left slope
    4. Steep hills — sharper profiles, more angular ridgelines

    **forested_hills** (3 variants per LART-14):
    1. Deciduous-topped — hill profiles with tree canopy on top, shadow left
    2. Conifer-topped — hill with pointed tree silhouettes along ridge
    3. Mixed — hill with varied tree shapes, some bare rock showing

    **mountains** (4 variants per LART-15):
    Use Design/mountain-hand-drawn.svg as variant 1 (extract path d data directly).
    1. Double peak — (from mountain-hand-drawn.svg)
    2. Single peak — one prominent peak, jagged ridge, left face filled
    3. Ridge — horizontal mountain ridge, multiple small peaks
    4. Cliff face — vertical left edge (shadow), sloping right, rocky

    **high_mountains** (3 variants per LART-16):
    1. Snow peak — tall peak with white gap at top (lower opacity), rocky base
    2. Twin peaks — two tall peaks with col between, snow on left shoulders
    3. Massive single — very tall single peak, broad base, heavy left shadow

    **plateau** (3 variants per LART-17):
    1. Mesa — flat-topped with steep cliff edges, shadow on left face
    2. Cliff edge — plateau seen from edge, horizontal top line, vertical left face
    3. Stepped — two-level plateau, stair-step profile

    **mountain_pass** (2 variants per LART-18):
    1. Narrow pass — two peaks flanking a low V-shaped gap
    2. Broad saddle — gentle dip between two ridges

    **desert** (3 variants per LART-19 "sand_desert"):
    1. Clean — minimal marks, a few wind ripple curves at low opacity
    2. Wind ripples — parallel curved lines, low opacity (0.25)
    3. Scattered dots — sparse dot pattern suggesting loose sand/pebbles

    **sand_dunes** (3 variants per LART-20):
    1. Rolling dunes — smooth wave shapes, shadow on left faces
    2. Crescent — single crescent/barchan dune shape, left face filled
    3. Tall dune — prominent single dune, steep left face shadow

    **rocky_desert** (3 variants per LART-21):
    1. Scattered rocks — 3-4 angular rock shapes, irregular, left-face shadow
    2. Rock pile — cluster of rocks, larger at center
    3. Flat rocks — low horizontal rock shapes, crack lines

    **badlands** (5 variants — 3 per LART-23 + 2 per LART-22 "hardened_clay"):
    1. Spires — tall thin vertical shapes, eroded pillars (LART-23)
    2. Layered — horizontal bands with erosion, vertical crack lines (LART-23)
    3. Eroded pillars — mushroom-shaped erosion forms, shadow left (LART-23)
    4. Fine cracks — flat cracked earth pattern, thin crack lines radiating from centers, sparse dry surface (LART-22 hardened_clay: fine cracks)
    5. Deep cracks — heavier crack pattern, wider fissures, deeper shadow in cracks, raised clay plate edges (LART-22 hardened_clay: deep cracks)

    Same rules: fill-only, sun-from-right, multi-layer opacity 0.2-0.7, organic paths, viewBox '0 0 100 100'. Use existing hand-drawn SVGs from Design/ as variant 1 for hills and mountains.
  </action>
  <verify>
    <automated>cd C:/Users/chris/Dev/Projects/TheFantasyWorldSimulator && npx vitest run src/components/HexMapV2/signifiers/__tests__/signifierRegistry.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - SIGNIFIER_REGISTRY['hills'] has exactly 4 variants
    - SIGNIFIER_REGISTRY['forested_hills'] has exactly 3 variants
    - SIGNIFIER_REGISTRY['mountains'] has exactly 4 variants
    - SIGNIFIER_REGISTRY['high_mountains'] has exactly 3 variants
    - SIGNIFIER_REGISTRY['plateau'] has exactly 3 variants
    - SIGNIFIER_REGISTRY['mountain_pass'] has exactly 2 variants
    - SIGNIFIER_REGISTRY['desert'] has exactly 3 variants
    - SIGNIFIER_REGISTRY['sand_dunes'] has exactly 3 variants
    - SIGNIFIER_REGISTRY['rocky_desert'] has exactly 3 variants
    - SIGNIFIER_REGISTRY['badlands'] has exactly 5 variants (3 LART-23 + 2 LART-22 hardened_clay)
    - hills variant 0 path data matches Design/hills-hand-drawn.svg
    - mountains variant 0 path data matches Design/mountain-hand-drawn.svg
    - Every path opacity between 0.2 and 0.7
    - Every path d string length > 50 characters
    - All signifierRegistry tests pass
  </acceptance_criteria>
  <done>Highland and desert signifiers have production SVG path data. 10 terrain types, ~33 variants total (including 2 hardened_clay variants in badlands). Tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Production SVG signifiers for cold, volcanic, and special terrain types (LART-24 through LART-30)</name>
  <files>src/components/HexMapV2/signifiers/signifierRegistry.ts</files>
  <read_first>
    src/components/HexMapV2/signifiers/signifierRegistry.ts,
    .planning/phases/05-hex-composition-landscape-signifiers/05-CONTEXT.md
  </read_first>
  <action>
    Replace placeholder entries in SIGNIFIER_REGISTRY for these terrain types:

    **tundra** (3 variants per LART-24):
    1. Lichen — small scattered organic blob shapes, very low profile, thin
    2. Scrub — tiny bush dots, windswept, minimal vertical extent
    3. Bare — very sparse marks, a few small rock shapes and dots

    **snow_fields** (2 variants per LART-25):
    1. Clean — very minimal: a few subtle drift curves at opacity 0.2-0.3
    2. Drift patterns — parallel curved lines suggesting wind-blown snow, low opacity

    **glacier** (2 variants per LART-26):
    1. Crevassed — parallel crack lines (thin dark lines), slight curve suggesting ice flow
    2. Smooth — subtle texture dots, minimal marking, mostly empty

    **volcano** (5 variants — 3 per LART-27 + 2 per LART-28 "lava"):
    1. Active crater — cone shape with open top, smoke wisps (curved lines above), shadow left (LART-27)
    2. Dormant — cone shape, closed top, weathered sides, less dramatic than active (LART-27)
    3. Vent — small cone with steam lines, lower profile (LART-27)
    4. Fresh flow — irregular flowing shapes radiating from center, high-contrast filled areas, hot/active feel with bright opacity layers (LART-28 lava: fresh flow)
    5. Cooling — slower flow shapes with crack lines through cooling surface, partial crust formation, darker/more textured than fresh flow (LART-28 lava: cooling)

    **broken_lands** (2 variants per LART-29):
    1. Cracked — ground with deep crack lines radiating from center, rubble dots
    2. Rubble — scattered angular debris shapes, irregular, chaotic

    **dead_forest** (3 variants per LART-30):
    1. Standing dead — bare trunks (no canopy), thin vertical lines, broken branch stubs
    2. Fallen — horizontal trunk shapes, broken, root mass visible
    3. Charred — blackened trunk stubs, thicker/more filled than standing dead, low profile

    Same rules: fill-only, sun-from-right, multi-layer opacity 0.2-0.7, organic paths, viewBox '0 0 100 100'.

    After completing all entries, add a comment block at the top of SIGNIFIER_REGISTRY documenting:
    ```typescript
    /**
     * TERRAIN TYPE COVERAGE:
     * Direct entries: 25 terrain types (grassland through dead_forest)
     * Fallback mapping: 6 types via TERRAIN_SIGNIFIER_FALLBACK
     *   farmland -> grassland, jungle -> tropical_forest, evergreen_forest -> boreal_forest,
     *   arctic -> snow_fields, great_home_trees -> dense_forest, oasis -> savanna
     * Not in TerrainType: hardened_clay (covered by badlands), lava (covered by volcano)
     * Total: 31 of 33 land TerrainType values covered
     *
     * LART requirement coverage:
     * LART-22 (hardened_clay: 2 variants fine cracks/deep cracks): satisfied by badlands variants 4-5
     * LART-28 (lava: 2 variants fresh flow/cooling): satisfied by volcano variants 4-5
     */
    ```
  </action>
  <verify>
    <automated>cd C:/Users/chris/Dev/Projects/TheFantasyWorldSimulator && npx vitest run src/components/HexMapV2/signifiers/__tests__/signifierRegistry.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - SIGNIFIER_REGISTRY['tundra'] has exactly 3 variants
    - SIGNIFIER_REGISTRY['snow_fields'] has exactly 2 variants
    - SIGNIFIER_REGISTRY['glacier'] has exactly 2 variants
    - SIGNIFIER_REGISTRY['volcano'] has exactly 5 variants (3 LART-27 + 2 LART-28 lava)
    - SIGNIFIER_REGISTRY['broken_lands'] has exactly 2 variants
    - SIGNIFIER_REGISTRY['dead_forest'] has exactly 3 variants
    - Every path opacity between 0.2 and 0.7
    - Every path d string length > 50 characters
    - SIGNIFIER_REGISTRY has at least 25 top-level keys
    - Registry coverage comment exists (grep for "TERRAIN TYPE COVERAGE")
    - Registry coverage comment documents LART-22 -> badlands and LART-28 -> volcano mapping
    - All signifierRegistry tests pass
  </acceptance_criteria>
  <done>All signifier art complete. 25 terrain types with direct entries + 6 fallbacks = 31 of 33 land types covered. Badlands has 5 variants (LART-23 + LART-22), volcano has 5 variants (LART-27 + LART-28). ~86+ total variants. All tests green.</done>
</task>

</tasks>

<verification>
```bash
cd C:/Users/chris/Dev/Projects/TheFantasyWorldSimulator && npx vitest run src/components/HexMapV2/signifiers/ --reporter=verbose && npx tsc --noEmit
```
All registry tests pass. TypeScript compiles. Full signifier art coverage.
</verification>

<success_criteria>
- All 30 LART requirements covered: 25 direct registry entries + LART-22 via badlands variants 4-5 + LART-28 via volcano variants 4-5 + 2 documented type mappings
- 6 additional TerrainType values covered via TERRAIN_SIGNIFIER_FALLBACK
- ~86+ total SVG variants with production path data
- Badlands has 5 variants (3 badlands + 2 hardened_clay), volcano has 5 variants (3 volcanic + 2 lava)
- All paths use fill-only, asymmetric shadow, multi-layer opacity
- hills and mountains variants include actual hand-drawn SVG paths from Design/
- Coverage documentation comment present in registry with explicit LART-22 and LART-28 mapping
- Tests pass, TypeScript compiles
</success_criteria>

<output>
After completion, create `.planning/phases/05-hex-composition-landscape-signifiers/05-04-SUMMARY.md`
</output>
