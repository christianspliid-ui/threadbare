---
phase: m2.5-monster-encounters
plan: 04
type: execute
wave: 3
depends_on: ["m2.5-01", "m2.5-02"]
files_modified:
  - src/components/HexMapV2/locations/locationIconRegistry.ts
  - src/components/HexMapV2/locations/locationIconTextures.ts
  - src/components/Game/HexSidebar.tsx
  - src/components/Game/tabs/ArmiesTabContent.tsx
autonomous: false
requirements:
  - M25-LAIR-ICON
  - M25-HEX-SIDEBAR
  - M25-FACTION-FILTER
  - M25-DIVINE-TARGET

must_haves:
  truths:
    - "Lair icons render on the hex map at correct hex positions with sphere-tinted fill colors"
    - "Clicking a hex with a lair shows lair details in HexSidebar (name, tier, sphere, danger zone)"
    - "Monster factions are filtered out of the player-facing ArmiesTab"
    - "Lair icons scale with tier: minor=small, major=medium, legendary=full"
  artifacts:
    - path: "src/components/HexMapV2/locations/locationIconRegistry.ts"
      provides: "LocationIconDef entries for 'lair' and 'cleared_lair'"
      contains: "'lair'"
    - path: "src/components/HexMapV2/locations/locationIconTextures.ts"
      provides: "Sphere-tint color override for lair icon textures"
      contains: "dominantSphere"
    - path: "src/components/Game/HexSidebar.tsx"
      provides: "Lair detail section with tier badge, sphere badge, named elite"
      contains: "MONSTER LAIR"
    - path: "src/components/Game/tabs/ArmiesTabContent.tsx"
      provides: "isMonsterFaction filter excluding monster factions"
      contains: "isMonsterFaction"
  key_links:
    - from: "src/components/HexMapV2/locations/locationIconRegistry.ts"
      to: "src/components/HexMapV2/locations/locationIconTextures.ts"
      via: "LocationIconDef consumed by texture builder"
      pattern: "lair"
    - from: "src/components/Game/HexSidebar.tsx"
      to: "state.graph"
      via: "Query lair node at selected hex"
      pattern: "locationSubtype.*lair"
---

<objective>
Add lair icon rendering on the hex map, lair detail section in HexSidebar, and filter monster factions from the player-facing ArmiesTab.

Purpose: Makes monster lairs visible and inspectable in the game UI. Ensures monster factions don't pollute the player-facing faction lists.
Output: Lair icons on hex map, HexSidebar lair details, ArmiesTab monster faction filtering.
</objective>

<execution_context>
@C:/Users/chris/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/m2.5-monster-encounters/m2.5-CONTEXT.md
@.planning/phases/m2.5-monster-encounters/m2.5-RESEARCH.md
@.planning/phases/m2.5-monster-encounters/m2.5-UI-SPEC.md
@.planning/phases/m2.5-monster-encounters/m2.5-01-SUMMARY.md
@.planning/phases/m2.5-monster-encounters/m2.5-02-SUMMARY.md

<interfaces>
<!-- UI-SPEC sphere tint colors (from m2.5-UI-SPEC.md) -->
Lair icon sphere fill colors (darkened to 30-35% lightness):
- Force #ff4444 → lair fill #5c1010
- Matter #8b6b4a → lair fill #3d2f1f
- Energy #ffd700 → lair fill #5c4e00
- Life #00cc55 → lair fill #004d1f
- Mind #2288ff → lair fill #0d3366
- Spirit #aa44dd → lair fill #3d1a52
- Time #ff9933 → lair fill #5c3800
- Entropy #5a8a7a → lair fill #1f3330

Lair icon sizeClass by tier:
- Minor: 'small' (0.40 x HEX_SIZE)
- Major: 'medium' (0.60 x HEX_SIZE)
- Legendary: 'full' (0.80 x HEX_SIZE)

Cleared lair: fixed 'small', fill #2a2520, no sphere tint.

From src/components/HexMapV2/locations/locationIconRegistry.ts:
```typescript
// LocationIconDef shape: { paths: string[][], viewBox: string, sizeClass: 'small' | 'medium' | 'full' }
// Check exact interface in the file
```

From src/components/HexMapV2/locations/locationIconTextures.ts:
```typescript
// buildLocationIconTexture builds canvas texture from LocationIconDef
// Check how fill color is applied — likely via ctx.fillStyle
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lair icon registration + sphere-tinted textures + HexSidebar lair section + ArmiesTab filter</name>
  <files>src/components/HexMapV2/locations/locationIconRegistry.ts, src/components/HexMapV2/locations/locationIconTextures.ts, src/components/Game/HexSidebar.tsx, src/components/Game/tabs/ArmiesTabContent.tsx</files>
  <read_first>
    - src/components/HexMapV2/locations/locationIconRegistry.ts (full file — LocationIconDef interface, existing entries for all LocationSubtype values, viewBox convention, sizeClass values)
    - src/components/HexMapV2/locations/locationIconTextures.ts (full file — buildLocationIconTexture, how fill color is applied, canvas rendering pattern)
    - src/components/Game/HexSidebar.tsx (full file — how location data is displayed for selected hex, existing section patterns, how locationSubtype drives content)
    - src/components/Game/tabs/ArmiesTabContent.tsx (full file — how factions are queried and displayed, army card rendering)
    - src/components/HexMapV2/layers/LocationLayer.tsx (how location icons are rendered — consumes registry + textures)
    - .planning/phases/m2.5-monster-encounters/m2.5-UI-SPEC.md (complete UI spec for reference)
  </read_first>
  <action>
    **1. Add lair icons to `locationIconRegistry.ts`:**

    Add two new entries to the registry:

    **`lair` icon:** Cave entrance silhouette — jagged downward arch with stalagmite teeth. Two SVG path layers:
    - Layer 1 (opacity 1.0): Outer cave arch — a rough inverted U shape with jagged edges along top and bottom (teeth/stalactites). Use hand-drawn-style paths matching the organic quality of existing icons (hills, mountains).
    - Layer 2 (opacity 0.7): Inner void fill — a smaller shape inside the arch representing the dark cave interior.
    - viewBox: `'0 0 100 100'`
    - sizeClass: `'small'` (base — but the texture builder will override based on tier)

    **`cleared_lair` icon:** Cracked/broken cave arch:
    - Layer 1 (opacity 1.0): Partial arch — same outer shape as lair but with a horizontal crack bisecting it (gap in the middle of the arch).
    - Layer 2 (opacity 0.2): Ghost fill — very faint interior (communicates "was here, gone now").
    - viewBox: `'0 0 100 100'`
    - sizeClass: `'small'`

    SVG paths must follow the existing convention: path data as arrays of strings in the `paths` array, each string is an SVG `d` attribute.

    **2. Add sphere-tint support to `locationIconTextures.ts`:**

    The existing `buildLocationIconTexture` uses a standard near-black fill for all icons. For lair icons, override the fill color with the sphere-specific darkened color.

    Add a `LAIR_SPHERE_FILL_COLORS` map:
    ```typescript
    const LAIR_SPHERE_FILL_COLORS: Record<string, string> = {
      force: '#5c1010',
      matter: '#3d2f1f',
      energy: '#5c4e00',
      life: '#004d1f',
      mind: '#0d3366',
      spirit: '#3d1a52',
      time: '#5c3800',
      entropy: '#1f3330',
    };
    ```

    Modify the texture building pipeline so that when a location node has `locationSubtype === 'lair'`, the fill color uses `LAIR_SPHERE_FILL_COLORS[dominantSphere]` instead of the default near-black. This may require:
    - Adding an optional `fillColorOverride` parameter to `buildLocationIconTexture`
    - OR checking the location properties when building the texture and applying the override internally
    - Read the existing code carefully to determine the best injection point.

    For `cleared_lair`, use fixed fill `#2a2520` — no sphere tint.

    Also handle sizeClass override by tier:
    - If lair properties have `lairTier === 'major'`, use `'medium'` sizeClass
    - If `lairTier === 'legendary'`, use `'full'` sizeClass
    - This override happens where the LocationLayer maps location nodes to icon textures.

    **3. Add lair detail section to `HexSidebar.tsx`:**

    When the selected hex has a location node with `locationSubtype === 'lair'`:

    Add a "MONSTER LAIR" section following existing section patterns in the sidebar:
    - Section header: `"MONSTER LAIR"` — use existing section heading pattern (`--text-xs`, uppercase, `--text-tertiary`, letter-spacing 0.12em, `--font-display`)
    - Lair name: `--text-sm`, `--font-display`, `--text-primary` — use `"{dominantSphere} Lair"` or the lair node's name
    - Tier badge: inline chip with text `"MINOR"` / `"MAJOR"` / `"LEGENDARY"`. Use `--bg-raised` background, `--border-subtle` border. Legendary tier uses `--accent-gold` border.
    - Sphere badge: inline chip with sphere name + sphere color dot (match existing sphere chip pattern in AgentInfoCard if available)
    - Danger zone label: use DANGER_ZONE_LABELS mapping — 'capital' to 'Safe', 'heartland' to 'Low Risk', 'borderland' to 'Moderate Risk', 'wilderness' to 'Danger Zone'
    - Named elite row (if lair has namedEliteId): `--text-sm`, `--font-display`, `--text-secondary`, prefixed with "Elite:" label in `--text-tertiary`

    For `cleared_lair`: show a muted version — "CLEARED LAIR" header, name, "Cleared — site may be claimed or will remain vulnerable to reinfestation." in `--text-tertiary`.

    **4. Filter monster factions from `ArmiesTabContent.tsx`:**

    In the component where factions are queried for display (look for `getNodesByType('actor')` filtered by `actorType === 'faction'` or similar), add a filter:
    ```typescript
    .filter(f => !f.properties.isMonsterFaction)
    ```

    This excludes monster factions from the player-facing faction list. Monster factions should only be visible through lair interactions and battle encounters.

    If monster armies appear in battle with player-aligned armies, they should still appear in battle cards — check the battle card rendering to ensure monster faction armies show with their faction name but are NOT listed in the faction roster.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - src/components/HexMapV2/locations/locationIconRegistry.ts contains entries for 'lair' and 'cleared_lair' with paths arrays and viewBox '0 0 100 100'
    - src/components/HexMapV2/locations/locationIconTextures.ts contains LAIR_SPHERE_FILL_COLORS map with 8 entries (force through entropy)
    - src/components/Game/HexSidebar.tsx contains 'MONSTER LAIR' section heading text
    - src/components/Game/HexSidebar.tsx contains tier badge rendering with 'MINOR', 'MAJOR', 'LEGENDARY' text
    - src/components/Game/HexSidebar.tsx contains DANGER_ZONE_LABELS reference or equivalent mapping
    - src/components/Game/tabs/ArmiesTabContent.tsx contains 'isMonsterFaction' filter
    - npx tsc --noEmit exits 0
    - npx vite build exits 0
  </acceptance_criteria>
  <done>Lair icons render with sphere tint, HexSidebar shows lair details, monster factions filtered from ArmiesTab</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of monster encounters UI</name>
  <files></files>
  <action>
    Human verifies the complete monster encounters integration visually. All code work is complete — this is visual/functional confirmation only.
  </action>
  <verify>Human confirms visual correctness</verify>
  <done>User approves lair icons, HexSidebar lair details, and ArmiesTab filtering</done>
  <what-built>
    Complete monster encounters integration:
    1. Monster lair icons visible on the hex map with sphere-colored tints
    2. Lair details section in HexSidebar showing tier, sphere, danger zone, named elite
    3. Monster factions filtered from ArmiesTab
    4. Monster lairs seeded at worldgen respecting danger gradient
    5. Lair escalation running every 25 ticks (tier upgrades, sphere feedback)
    6. Army attrition in monster-infested hexes
    7. Monster encounter templates + Adventuring Guild hunt quests
  </what-built>
  <how-to-verify>
    1. Open http://localhost:5173/?view=game in browser
    2. Use Claude in Chrome (NOT Playwright) to verify WebGL content:
       - Zoom to regional level — lair icons should be visible as cave-entrance silhouettes with sphere-colored tints
       - Click a hex with a lair — HexSidebar should show "MONSTER LAIR" section with tier badge, sphere badge, danger zone label
       - Check ArmiesTab — monster factions should NOT appear in the faction roster
    3. Run CLI smoke test: `npm run cli -- --seed 42` then `tick 50` then `status`
       - Check `graph` command for lair nodes
       - Check `events 20` for any lair-related events
       - Verify no crashes
    4. Run `npm test` — full suite should be green
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` — zero type errors
- `npx vite build` — production build succeeds
- `npm test` — full suite green
- Visual: lair icons render on hex map with sphere coloring
- Visual: HexSidebar shows lair details when lair hex selected
- Visual: ArmiesTab does not show monster factions
</verification>

<success_criteria>
- Lair icons visible on hex map at regional+ zoom with sphere-appropriate tint colors
- Lair sizeClass scales with tier (small/medium/full)
- Cleared lairs render as muted, cracked variants
- HexSidebar shows lair name, tier, sphere, danger zone, named elite when present
- Monster factions filtered from ArmiesTab and faction roster
- Production build succeeds (Vercel-safe)
</success_criteria>

<output>
After completion, create `.planning/phases/m2.5-monster-encounters/m2.5-04-SUMMARY.md`
</output>
