---
phase: m2.5-monster-encounters
plan: 03
type: execute
wave: 2
depends_on: ["m2.5-01"]
files_modified:
  - src/data/monster-encounter-content.ts
  - src/data/faction-definitions.ts
  - src/engine/armyAttrition.ts
  - src/engine/__tests__/armyAttrition.test.ts
autonomous: true
requirements:
  - M25-ENCOUNTER-TEMPLATES
  - M25-ARMY-ATTRITION
  - M25-BATTLE-REUSE
  - M25-CLEARING

must_haves:
  truths:
    - "Monster encounter templates fire at lairs and wilderness hexes"
    - "Armies passing through monster-infested hexes take additional Quintessence attrition"
    - "Minor lairs can be cleared via Adventuring Guild agent quests"
    - "Monster armies use the same battle resolution as civilized armies"
  artifacts:
    - path: "src/data/monster-encounter-content.ts"
      provides: "Monster encounter templates following EncounterTemplate shape"
      exports: ["MONSTER_ENCOUNTER_TEMPLATES"]
    - path: "src/engine/armyAttrition.ts"
      provides: "Monster territory attrition modifier"
      contains: "MONSTER_TERRITORY_ATTRITION"
  key_links:
    - from: "src/data/monster-encounter-content.ts"
      to: "src/data/encounter-content.ts"
      via: "Same EncounterTemplate shape, registered into encounter pool"
      pattern: "EncounterTemplate"
    - from: "src/engine/armyAttrition.ts"
      to: "state.graph"
      via: "Query for lair nodes at army hex"
      pattern: "locationSubtype.*lair"
    - from: "src/data/faction-definitions.ts"
      to: "src/data/monster-encounter-content.ts"
      via: "Adventuring Guild questTemplateIds includes monster.hunt.minor"
      pattern: "monster\\.hunt\\.minor"
---

<objective>
Create monster encounter templates and integrate monster territory attrition into the army movement system. Wire monster hunt quests into the Adventuring Guild.

Purpose: Makes monsters tactically relevant — they threaten armies passing through their territory and can be cleared through the existing encounter and quest systems.
Output: Monster encounter templates, army attrition in monster territory, Adventuring Guild monster hunt quests.
</objective>

<execution_context>
@C:/Users/chris/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/m2.5-monster-encounters/m2.5-CONTEXT.md
@.planning/phases/m2.5-monster-encounters/m2.5-RESEARCH.md
@.planning/phases/m2.5-monster-encounters/m2.5-01-SUMMARY.md

<interfaces>
<!-- Key encounter and army types from codebase -->

From src/types/encounter.ts (EncounterTemplate shape — check this file for exact interface):
```typescript
// EncounterTemplate has: id, name, description, locationTypes, threatRating, steps, etc.
// Check exact shape in the file
```

From src/data/encounter-content.ts (pattern for encounter template definitions):
```typescript
// Array of EncounterTemplate objects with locationTypes: LocationSubtype[]
// Wilderness encounters already exist as a pattern
```

From src/data/faction-encounter-content.ts (faction quest template pattern):
```typescript
// ag.quest.monster_hunt already exists — check its shape for the extension pattern
```

From src/data/faction-definitions.ts (Adventuring Guild definition):
```typescript
// Look for the 'adventuring_guild' FactionDefinition entry
// Its questTemplateIds array should be extended with monster hunt templates
```

From src/engine/armyAttrition.ts:
```typescript
// phaseArmyAttrition(state: GameState): void — mutates state
// Iterates army actor nodes, computes Quintessence loss per tick
// Uses named constants for attrition rates
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Monster encounter templates + wire into Adventuring Guild quests</name>
  <files>src/data/monster-encounter-content.ts, src/data/faction-definitions.ts</files>
  <read_first>
    - src/types/encounter.ts (EncounterTemplate interface — exact fields required)
    - src/data/encounter-content.ts (existing encounter template examples — locationTypes, threatRating, steps shape)
    - src/data/faction-encounter-content.ts (ag.quest.monster_hunt template — existing pattern for faction quest encounters)
    - src/data/faction-definitions.ts (Adventuring Guild definition — questTemplateIds array)
    - src/engine/encounterScoring.ts (how encounters are scored and filtered — locationTypes matching)
    - src/engine/encounterFilterPipeline.ts (how locationTypes filter is applied)
  </read_first>
  <action>
    **1. Create `src/data/monster-encounter-content.ts`:**

    Export `MONSTER_ENCOUNTER_TEMPLATES` array following the exact `EncounterTemplate` interface shape from `src/types/encounter.ts`. Templates:

    | ID | locationTypes | threatRating | Purpose |
    |---|---|---|---|
    | `monster.hunt.minor` | `['lair', 'wilderness']` | `'easy'` | Agent clears minor lair via Adventuring Guild quest. Single-step encounter: agent capability check (Iron or Force reach) |
    | `monster.hunt.named_elite` | `['lair']` | `'hard'` | Agent confronts named elite — requires senior-tier capability. Multi-step: approach → confront → resolve |
    | `monster.encounter.ambush` | `['wilderness']` | `'moderate'` | Generic monster ambush in wilderness — no lair required. Encounter fires on agents/armies in wilderness hexes |
    | `monster.encounter.lair_defense` | `['lair']` | `'hard'` | Army encounters lair defenders — triggered when army enters lair hex. Attrition + combat check |
    | `monster.encounter.horde_raid` | `['hamlet', 'town', 'city', 'capital']` | `'deadly'` | Monster army attacks settlement — generated by monster faction ambitions. Multi-step siege variant |

    Each template must match the EXACT shape used by existing templates in `encounter-content.ts` — copy the field structure including `steps` array, `outcomes`, `locationTypes`, etc. Read the existing templates carefully before writing.

    For monster encounter steps: use the same step structure as existing encounters. Monster hunts should test Iron or Force reach capability. Named elite hunts should have higher threshold.

    **2. Wire into encounter system:**

    In `src/data/monster-encounter-content.ts`, also export a registration function or ensure templates are imported where the encounter pool is assembled. Check how `encounter-content.ts` templates are registered — follow the same import pattern.

    **3. Update Adventuring Guild in `src/data/faction-definitions.ts`:**

    Find the Adventuring Guild `FactionDefinition` entry. Add `'monster.hunt.minor'` and `'monster.hunt.named_elite'` to its `questTemplateIds` array. This enables the Adventuring Guild to assign monster hunting quests to capable agents.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test</automated>
  </verify>
  <acceptance_criteria>
    - src/data/monster-encounter-content.ts exports MONSTER_ENCOUNTER_TEMPLATES with at least 4 templates
    - Template 'monster.hunt.minor' has locationTypes containing 'lair'
    - Template 'monster.encounter.ambush' has locationTypes containing 'wilderness'
    - src/data/faction-definitions.ts Adventuring Guild questTemplateIds contains 'monster.hunt.minor'
    - src/data/faction-definitions.ts Adventuring Guild questTemplateIds contains 'monster.hunt.named_elite'
    - All templates follow the EncounterTemplate interface (npx tsc --noEmit exits 0)
    - npm test exits 0
  </acceptance_criteria>
  <done>Monster encounter templates created and registered, Adventuring Guild can assign monster hunts</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Monster territory attrition for armies</name>
  <files>src/engine/armyAttrition.ts, src/engine/__tests__/armyAttrition.test.ts</files>
  <read_first>
    - src/engine/armyAttrition.ts (full file — current phaseArmyAttrition logic, existing constants, how Quintessence is decremented)
    - src/engine/__tests__/armyAttrition.test.ts (existing army attrition tests — extend these)
    - src/types/graph.ts (graph.getNodesByType, getOutgoingEdges — for querying lair at army hex)
  </read_first>
  <behavior>
    - Test 1: Army at a hex with no lair receives standard attrition only (no monster bonus)
    - Test 2: Army at a hex with an active lair receives standard attrition + MONSTER_TERRITORY_ATTRITION bonus
    - Test 3: Army at a hex with a cleared_lair receives NO monster attrition
    - Test 4: Monster attrition is additive (added to existing attrition, not replacing it)
  </behavior>
  <action>
    **Add monster territory attrition to `src/engine/armyAttrition.ts`:**

    1. Add named constants at the top of the file:
    ```typescript
    export const MONSTER_TERRITORY_ATTRITION = 1.2;  // additional Q loss per tick in monster-infested hex
    ```

    2. Inside `phaseArmyAttrition`, after the existing attrition calculation for each army, add a monster territory check:
    - Get the army's current hex (from its `located_at` edge target)
    - Query if any `location` node at that hex has `locationSubtype === 'lair'` (active lair only, not 'cleared_lair')
    - If yes, add `MONSTER_TERRITORY_ATTRITION` to the total attrition for that tick
    - Do NOT double-count: one check per army per tick, regardless of how many lairs are at the hex

    The query function:
    ```typescript
    function hasActiveLairAtHex(state: GameState, hexNodeId: string): boolean {
      return state.graph.getNodesByType('location').some(n =>
        n.properties.locationSubtype === 'lair' &&
        state.graph.getOutgoingEdges(n.id, 'located_at')
          .some(e => e.target === hexNodeId)
      );
    }
    ```

    **Extend existing tests in `src/engine/__tests__/armyAttrition.test.ts`:**

    Add a new `describe('monster territory attrition')` block with the 4 test cases. Use the existing test setup pattern — create mock graph with army nodes, hex nodes, and lair nodes.
  </action>
  <verify>
    <automated>npm test -- --run src/engine/__tests__/armyAttrition.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/engine/armyAttrition.ts contains 'MONSTER_TERRITORY_ATTRITION = 1.2'
    - src/engine/armyAttrition.ts contains 'locationSubtype.*lair' check in phaseArmyAttrition
    - Monster attrition is additive to existing attrition (not a replacement)
    - src/engine/__tests__/armyAttrition.test.ts contains 'monster territory attrition' describe block
    - Test: army at hex with no lair → no MONSTER_TERRITORY_ATTRITION added
    - Test: army at hex with active lair → MONSTER_TERRITORY_ATTRITION added
    - Test: army at hex with cleared_lair → no MONSTER_TERRITORY_ATTRITION added
    - npm test -- --run src/engine/__tests__/armyAttrition.test.ts exits 0
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Armies in monster-infested hexes take additional Quintessence attrition from MONSTER_TERRITORY_ATTRITION</done>
</task>

</tasks>

<verification>
- `npm test` — full suite green
- `npx tsc --noEmit` — zero type errors
- Monster encounter templates compile and match EncounterTemplate interface
- Adventuring Guild can reference monster hunt quest IDs
</verification>

<success_criteria>
- 4+ monster encounter templates created following existing EncounterTemplate shape
- Monster hunt quests wired into Adventuring Guild questTemplateIds
- Army attrition increased in monster-infested hexes by MONSTER_TERRITORY_ATTRITION
- Cleared lairs do NOT trigger monster attrition
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/m2.5-monster-encounters/m2.5-03-SUMMARY.md`
</output>
