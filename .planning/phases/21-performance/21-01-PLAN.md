---
phase: 21-performance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/engine/proseGenerator.ts
  - src/components/Game/AgentInfoCard.tsx
  - src/components/Game/HexChronicle.tsx
  - src/components/Game/LocationView.tsx
  - src/components/Game/hooks/useAgentInteraction.ts
  - src/engine/__tests__/proseGenerator.test.ts
autonomous: true
requirements: [PERF-01]

must_haves:
  truths:
    - "Opening an agent panel a second time (same agent, same tick) returns cached prose without re-running 20+ resolvers"
    - "Advancing a tick invalidates stale prose cache entries"
    - "Different prose modes (summary vs full) for the same entity are cached independently"
  artifacts:
    - path: "src/engine/proseGenerator.ts"
      provides: "Module-level Map cache with tick-based auto-eviction and clearProseCache export"
      contains: "_proseCache"
    - path: "src/engine/__tests__/proseGenerator.test.ts"
      provides: "Cache hit/miss unit tests"
      contains: "cache"
  key_links:
    - from: "src/components/Game/AgentInfoCard.tsx"
      to: "src/engine/proseGenerator.ts"
      via: "generateEntityProse call with tick param"
      pattern: "generateEntityProse.*tick"
    - from: "src/components/Game/LocationView.tsx"
      to: "src/engine/proseGenerator.ts"
      via: "generateEntityProse call with tick param"
      pattern: "generateEntityProse.*tick"
    - from: "src/components/Game/HexChronicle.tsx"
      to: "src/engine/proseGenerator.ts"
      via: "generateEntityProse call with tick param"
      pattern: "generateEntityProse.*tick"
---

<objective>
Add a module-level prose cache to `generateEntityProse` so reopening an agent or location panel within the same tick skips all 20+ resolver calls and returns instantly from cache.

Purpose: The prose system re-runs all resolvers on every panel open. For the same agent on the same tick, the output is identical. Caching eliminates redundant computation.
Output: Cached prose generator with tick-based eviction, updated callers passing tick, and unit tests proving cache behavior.
</objective>

<execution_context>
@C:/Users/chris/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-performance/21-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/engine/proseGenerator.ts:
```typescript
export function generateEntityProse(
  nodeId: string,
  graph: WorldGraph,
  seed: number,
  mode: ProseMode,
): string;
```

From src/types/prose.ts:
```typescript
export type ProseMode = 'summary' | 'full';
```

Callers (current signatures):
- AgentInfoCard.tsx:57 — `generateEntityProse(card.id, graph, seed, 'summary')`
- HexChronicle.tsx:295,341 — `generateEntityProse(loc.id, graph, seed, 'summary')` (inside useMemo loops)
- LocationView.tsx:462,789 — `generateEntityProse(sublocation.id, graph, seed, 'full')`

AgentInfoCard receives `graph` and `seed` as props. `tick` is NOT currently passed.
AgentInfoCard is created inside `useAgentInteraction.ts` which has access to `gameState.tick`.
HexChronicle and LocationView also have access to `gameState` or receive tick-relevant deps.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add prose cache to generateEntityProse and write tests</name>
  <files>src/engine/proseGenerator.ts, src/engine/__tests__/proseGenerator.test.ts</files>
  <read_first>
    - src/engine/proseGenerator.ts
    - src/engine/__tests__/proseGenerator.test.ts
    - src/types/prose.ts
    - .planning/phases/21-performance/21-RESEARCH.md (Pattern 1: Module-Level Prose Cache)
  </read_first>
  <behavior>
    - Test 1: Same (nodeId, tick, mode) on second call returns identical string without re-running resolvers
    - Test 2: Different tick number produces fresh computation (cache miss)
    - Test 3: Same (nodeId, tick) but different mode produces separate cache entries (no cross-mode collision)
    - Test 4: clearProseCache() resets all cached entries
    - Test 5: Existing tests still pass with tick=0 default
  </behavior>
  <action>
    1. In `proseGenerator.ts`, add module-level cache variables:
       ```typescript
       const _proseCache = new Map<string, string>();
       let _lastCachedTick = -1;
       ```

    2. Change `generateEntityProse` signature to add optional tick parameter:
       ```typescript
       export function generateEntityProse(
         nodeId: string,
         graph: WorldGraph,
         seed: number,
         mode: ProseMode,
         tick: number = 0,
       ): string {
       ```

    3. At the top of the function body (before the `getNode` call), add cache logic:
       - If `tick !== _lastCachedTick`, call `_proseCache.clear()` and set `_lastCachedTick = tick`
       - Build key as `${nodeId}:${tick}:${mode}`
       - Check `_proseCache.get(key)` — if defined, return it immediately
       - After computing `result`, call `_proseCache.set(key, result)` before returning

    4. Move the `emitTrace` call to only fire on cache miss (actual computation). Do NOT emit trace on cache hit — per research pitfall #5, avoid noise.

    5. Export `clearProseCache()` function that clears the Map and resets `_lastCachedTick = -1`.

    6. In `proseGenerator.test.ts`:
       - Add `import { clearProseCache } from '../proseGenerator'`
       - Add `beforeEach(() => clearProseCache())` to each describe block (or top-level)
       - Add new describe block "prose cache" with tests for the 5 behaviors listed above
       - For cache-hit detection: call generateEntityProse twice with same args, verify same string reference returned (or spy on a resolver to confirm it's not called twice)
  </action>
  <verify>
    <automated>npx vitest run src/engine/__tests__/proseGenerator.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `_proseCache` exists as module-level `Map<string, string>` in proseGenerator.ts
    - `clearProseCache` is exported from proseGenerator.ts
    - `generateEntityProse` signature includes `tick: number = 0` as 5th parameter
    - Cache key format is `${nodeId}:${tick}:${mode}`
    - Test file contains `clearProseCache()` in beforeEach
    - Test file contains tests for: cache hit, tick invalidation, mode separation, clearProseCache
    - All existing proseGenerator tests still pass (tick defaults to 0)
  </acceptance_criteria>
  <done>generateEntityProse caches results by (nodeId, tick, mode), auto-evicts on tick change, and all tests pass including new cache behavior tests</done>
</task>

<task type="auto">
  <name>Task 2: Wire tick parameter through all callers</name>
  <files>src/components/Game/AgentInfoCard.tsx, src/components/Game/HexChronicle.tsx, src/components/Game/LocationView.tsx, src/components/Game/hooks/useAgentInteraction.ts</files>
  <read_first>
    - src/components/Game/AgentInfoCard.tsx (lines 1-60)
    - src/components/Game/hooks/useAgentInteraction.ts (lines 37-55, 141-160)
    - src/components/Game/HexChronicle.tsx (lines 280-350)
    - src/components/Game/LocationView.tsx (lines 450-470, 780-800)
  </read_first>
  <action>
    **AgentInfoCard.tsx:**
    1. Add `tick?: number` to `AgentInfoCardProps` interface
    2. Destructure `tick` from props
    3. Update the `useMemo` call at line ~57:
       ```typescript
       const agentProse = useMemo(() => {
         if (!graph || seed === undefined || tick === undefined) return '';
         return generateEntityProse(card.id, graph, seed, 'summary', tick);
       }, [card.id, graph, seed, tick]);
       ```

    **useAgentInteraction.ts:**
    4. Find where `AgentInfoCard` data is constructed (the `agentInfoCard` useMemo around line 141). The card data object does NOT directly render AgentInfoCard — it produces `AgentInfoCardData`. The `tick` needs to flow to where `<AgentInfoCard>` JSX is rendered with `graph` and `seed` props.
    5. Search for where `<AgentInfoCard>` component is rendered with `graph={...} seed={...}` props and add `tick={gameState.tick}` there. This is likely in GameView.tsx or a panel component. Check the render sites found earlier (ThreadDetailView, AgentProfileModal usage).

    **HexChronicle.tsx:**
    6. Find where `gameState` or `tick` is available. The component likely receives gameState props or has access via context.
    7. Update both `generateEntityProse` calls (lines ~295 and ~341) to pass tick:
       ```typescript
       const prose = generateEntityProse(loc.id, graph, seed, 'summary', tick);
       ```
    8. Add `tick` to the relevant `useMemo` dependency arrays.

    **LocationView.tsx:**
    9. Update both `generateEntityProse` calls (lines ~462 and ~789) to pass tick:
       ```typescript
       return generateEntityProse(sublocation.id, graph, seed, 'full', tick);
       return generateEntityProse(location.id, graph, seed, 'full', tick);
       ```
    10. Add `tick` to both `useMemo` dependency arrays.

    For HexChronicle and LocationView: if `tick` is not directly available as a prop, trace the component tree to find where `gameState.tick` is accessible and thread it through. The research confirms all callers have access to `gameState.tick`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - Every `generateEntityProse(` call in `.tsx` files includes a tick argument (5th param)
    - `AgentInfoCardProps` interface includes `tick?: number`
    - All `useMemo` arrays that depend on `generateEntityProse` include `tick` in their dependency list
    - `npx tsc --noEmit` passes with zero errors
    - All existing tests pass
  </acceptance_criteria>
  <done>All callers of generateEntityProse pass tick parameter, enabling the cache to detect same-tick re-renders and serve cached prose</done>
</task>

</tasks>

<verification>
1. `npx vitest run src/engine/__tests__/proseGenerator.test.ts --reporter=verbose` — cache tests pass
2. `npx tsc --noEmit` — no type errors from signature change
3. `npx vite build` — production build succeeds
4. `npm test` — full suite green
</verification>

<success_criteria>
- generateEntityProse caches by (nodeId, tick, mode) and auto-evicts when tick advances
- All 5 callers pass tick parameter
- Cache hit/miss/eviction tests pass
- No type errors, full test suite green, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/21-performance/21-01-SUMMARY.md`
</output>
