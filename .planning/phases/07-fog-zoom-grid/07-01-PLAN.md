---
phase: 07-fog-zoom-grid
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/HexMapV2/scene/ZoomVisibilityMatrix.ts
  - src/components/HexMapV2/scene/__tests__/ZoomVisibilityMatrix.test.ts
  - src/components/HexMapV2/scene/FogCulling.ts
  - src/components/HexMapV2/scene/__tests__/FogCulling.test.ts
autonomous: true
requirements: [FOG-01, FOG-02, FOG-03, FOG-04, FOG-05, FOG-06, ZOOM-01, ZOOM-02, ZOOM-03, ZOOM-04]

must_haves:
  truths:
    - "getZoomTier returns correct tier name for any zoom scale k"
    - "ZOOM_VISIBILITY_MATRIX has an entry for every render layer at every tier"
    - "getFadeAlpha returns 0.0-1.0 for the overlap range around tier boundaries"
    - "updateFogColors sets unexplored hexes to #0a0a0c and restores original color for explored/visible"
    - "isLayerVisibleForHex correctly gates layers based on fog state"
    - "computeVisibilityFromSources returns visible for own hex at range 0"
  artifacts:
    - path: "src/components/HexMapV2/scene/ZoomVisibilityMatrix.ts"
      provides: "Zoom tier thresholds, visibility matrix, fade alpha helper, getZoomTier"
      exports: ["ZOOM_TIER_THRESHOLDS", "ZOOM_VISIBILITY_MATRIX", "ZoomTier", "getZoomTier", "getFadeAlpha"]
    - path: "src/components/HexMapV2/scene/FogCulling.ts"
      provides: "Fog color override, per-hex layer gating, visibility computation"
      exports: ["updateFogColors", "isLayerVisibleForHex", "computeVisibilityFromSources", "buildOriginalColorCache", "FOG_CONSTANTS"]
    - path: "src/components/HexMapV2/scene/__tests__/ZoomVisibilityMatrix.test.ts"
      provides: "Unit tests for zoom tier logic"
    - path: "src/components/HexMapV2/scene/__tests__/FogCulling.test.ts"
      provides: "Unit tests for fog culling logic"
  key_links:
    - from: "FogCulling.ts"
      to: "src/types/visibility.ts"
      via: "imports HexVisibilityState, VisibilityMap, visKey"
      pattern: "import.*from.*visibility"
    - from: "ZoomVisibilityMatrix.ts"
      to: "src/components/HexMapV2/scene/RenderLayers.ts"
      via: "matrix keys mirror RenderLayerName values"
      pattern: "ZOOM_VISIBILITY_MATRIX"
---

<objective>
Create the pure-logic modules for fog-of-war culling and zoom visibility matrix — all types, constants, and testable functions with no Three.js scene wiring yet.

Purpose: Establish the fog and zoom contracts that Plan 03 will wire into HexMapV2.tsx. TDD approach ensures correctness before integration.
Output: Two new modules (FogCulling.ts, ZoomVisibilityMatrix.ts) with comprehensive tests.
</objective>

<execution_context>
@C:/Users/chris/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-fog-zoom-grid/07-CONTEXT.md
@.planning/phases/07-fog-zoom-grid/07-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/types/visibility.ts:
```typescript
export type HexVisibilityState = 'unexplored' | 'remembered' | 'visible';
export interface HexVisibility {
  state: HexVisibilityState;
  snapshot?: StaleSnapshot;
}
export type VisibilityMap = Map<string, HexVisibility>;
export interface LOSSource {
  hexCol: number;
  hexRow: number;
  range: number;
}
export const AVATAR_SIGHT_RANGE = 0;
export function visKey(col: number, row: number): string;
```

From src/components/HexMapV2/scene/RenderLayers.ts:
```typescript
export const RENDER_ORDER = {
  HEX_FILL: 0, COASTLINE: 1, GRID: 2, ELEVATION_TICKS: 3,
  RIVERS: 4, ROADS: 5, BORDERS: 6, SIGNIFIERS: 7,
  LOCATIONS: 8, AGENTS: 9, EVENTS: 10, LABELS: 11, FOG: 12,
} as const;
```

From src/components/HexMapV2/scene/HexFillMesh.ts:
```typescript
export function createHexFillMesh(tiles: HexTile[], seed: number, lakeIds?: Int16Array): THREE.InstancedMesh;
// Uses mesh.setColorAt(index, color) for per-instance colors
// Uses getHexColor(terrain, seed, col, row, opts) -> [r, g, b]
```

From src/components/HexMapV2/agents/agentSpriteTypes.ts:
```typescript
export const AGENT_ZOOM_THRESHOLDS = {
  HERO_LOCAL: 15,
  REGIONAL: 5,
  CONTINENTAL: 1.5,
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ZoomVisibilityMatrix — tier logic, visibility matrix, fade alpha</name>
  <files>
    src/components/HexMapV2/scene/ZoomVisibilityMatrix.ts,
    src/components/HexMapV2/scene/__tests__/ZoomVisibilityMatrix.test.ts
  </files>
  <read_first>
    src/components/HexMapV2/scene/RenderLayers.ts,
    src/components/HexMapV2/agents/agentSpriteTypes.ts,
    src/components/HexMapV2/HexMapV2.tsx
  </read_first>
  <behavior>
    - getZoomTier(15) returns 'hero-local'
    - getZoomTier(14.9) returns 'regional'
    - getZoomTier(5) returns 'regional'
    - getZoomTier(4.9) returns 'continental'
    - getZoomTier(1.5) returns 'continental'
    - getZoomTier(1.4) returns 'full-world'
    - getZoomTier(0.5) returns 'full-world'
    - ZOOM_VISIBILITY_MATRIX.signifiers['full-world'] === false
    - ZOOM_VISIBILITY_MATRIX.signifiers['regional'] === true
    - ZOOM_VISIBILITY_MATRIX.agents_portrait['hero-local'] === true
    - ZOOM_VISIBILITY_MATRIX.agents_portrait['regional'] === false
    - ZOOM_VISIBILITY_MATRIX.borders_kingdom['full-world'] === true
    - ZOOM_VISIBILITY_MATRIX.rivers['continental'] === true
    - ZOOM_VISIBILITY_MATRIX.rivers['full-world'] === false
    - ZOOM_VISIBILITY_MATRIX.grid_lines['continental'] === false
    - ZOOM_VISIBILITY_MATRIX.grid_lines['regional'] === true
    - ZOOM_VISIBILITY_MATRIX.events['hero-local'] === true
    - ZOOM_VISIBILITY_MATRIX.events['regional'] === false
    - getFadeAlpha(k=4.5, threshold=5, fadeRange=1.0) returns value between 0 and 1 (in overlap zone)
    - getFadeAlpha(k=6, threshold=5, fadeRange=1.0) returns 1.0 (above threshold+range)
    - getFadeAlpha(k=3, threshold=5, fadeRange=1.0) returns 0.0 (below threshold-range)
    - All 12+ layer keys in the matrix have entries for all 4 tiers
  </behavior>
  <action>
    RED: Write tests in `src/components/HexMapV2/scene/__tests__/ZoomVisibilityMatrix.test.ts` covering all behaviors above.

    GREEN: Create `src/components/HexMapV2/scene/ZoomVisibilityMatrix.ts` with:

    1. `ZOOM_TIER_THRESHOLDS` constant object:
       - `HERO_LOCAL: 15` (k >= 15)
       - `REGIONAL: 5` (k >= 5)
       - `CONTINENTAL: 1.5` (k >= 1.5)
       - `FULL_WORLD: 0` (k < 1.5)

    2. `ZoomTier` type: `'hero-local' | 'regional' | 'continental' | 'full-world'`

    3. `getZoomTier(k: number): ZoomTier` — returns tier using >= checks in descending order.

    4. `ZOOM_VISIBILITY_MATRIX` — a `Record<string, Record<ZoomTier, boolean>>` with these exact entries (from UI-SPEC visibility matrix):
       - `hex_fill`: all true
       - `coastline`: all true
       - `grid_lines`: hero-local true, regional true, continental false, full-world false
       - `elev_ticks`: hero-local true, regional true, continental false, full-world false
       - `rivers`: hero-local true, regional true, continental true, full-world false
       - `roads`: hero-local true, regional true, continental false, full-world false
       - `borders_kingdom`: all true
       - `borders_barony`: hero-local true, regional true, continental true, full-world false
       - `signifiers`: hero-local true, regional true, continental false, full-world false
       - `locations`: hero-local true, regional true, continental false, full-world false
       - `agents_portrait`: hero-local true, rest false
       - `agents_dot`: regional true, rest false
       - `agents_retinue`: continental true, rest false
       - `events`: hero-local true, rest false
       - `labels`: hero-local true, regional true, continental false, full-world false
       - `fog`: all true

    5. `FADE_RANGE` constant = 0.2 (20% of tier threshold as overlap).

    6. `getFadeAlpha(k: number, threshold: number, fadeRange: number): number` — returns:
       - 0.0 if k <= threshold - fadeRange
       - 1.0 if k >= threshold + fadeRange
       - linear lerp (k - (threshold - fadeRange)) / (2 * fadeRange) in between
       - Clamped to [0, 1].

    NOTE: Promote `AGENT_ZOOM_THRESHOLDS` values from `agentSpriteTypes.ts` into `ZOOM_TIER_THRESHOLDS` (they are the same values: 15, 5, 1.5). Do NOT create duplicate constants — have `agentSpriteTypes.ts` import from here if needed, or leave both for now and note for Plan 03 cleanup.
  </action>
  <verify>
    <automated>npx vitest run src/components/HexMapV2/scene/__tests__/ZoomVisibilityMatrix.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - ZoomVisibilityMatrix.ts exports ZOOM_TIER_THRESHOLDS, ZOOM_VISIBILITY_MATRIX, ZoomTier, getZoomTier, getFadeAlpha, FADE_RANGE
    - getZoomTier(15) === 'hero-local' in test
    - getZoomTier(1.4) === 'full-world' in test
    - ZOOM_VISIBILITY_MATRIX has at least 12 layer keys
    - Every layer key has all 4 tier entries (hero-local, regional, continental, full-world)
    - getFadeAlpha returns 0 below threshold-range, 1 above threshold+range, interpolated in between
    - All tests pass with exit code 0
  </acceptance_criteria>
  <done>All zoom tier, visibility matrix, and fade alpha tests pass. Module exports are complete.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FogCulling — color override, layer gating, visibility computation</name>
  <files>
    src/components/HexMapV2/scene/FogCulling.ts,
    src/components/HexMapV2/scene/__tests__/FogCulling.test.ts
  </files>
  <read_first>
    src/types/visibility.ts,
    src/components/HexMapV2/scene/HexFillMesh.ts,
    src/components/HexMapV2/palette/colorUtils.ts,
    src/lib/hexMath.ts
  </read_first>
  <behavior>
    - buildOriginalColorCache with 3 tiles returns Float32Array of length 9 (3*3) and Map of size 3
    - updateFogColors sets unexplored hex instance color to r/g/b matching #0a0a0c
    - updateFogColors restores original color for remembered hex
    - updateFogColors restores original color for visible hex
    - isLayerVisibleForHex('unexplored', 'signifier') returns false
    - isLayerVisibleForHex('unexplored', 'agent') returns false
    - isLayerVisibleForHex('remembered', 'signifier') returns true
    - isLayerVisibleForHex('remembered', 'location') returns true
    - isLayerVisibleForHex('remembered', 'agent') returns false
    - isLayerVisibleForHex('remembered', 'event') returns false
    - isLayerVisibleForHex('visible', 'agent') returns true
    - isLayerVisibleForHex('visible', 'event') returns true
    - computeVisibilityFromSources with one source at (5,5) range 0 returns visible only for (5,5)
    - computeVisibilityFromSources with one source at (5,5) range 1 returns visible for (5,5) and its 6 neighbors
    - computeVisibilityFromSources marks remaining hexes as unchanged (not in returned map)
    - FOG_CONSTANTS.UNEXPLORED_HEX_COLOR === '#0a0a0c'
    - FOG_CONSTANTS.DEFAULT_SIGHT_RANGE === 0
    - FOG_CONSTANTS.ELEVATION_BONUS === 1
  </behavior>
  <action>
    RED: Write tests in `src/components/HexMapV2/scene/__tests__/FogCulling.test.ts`. For tests that need Three.js InstancedMesh, mock `setColorAt` and `instanceColor` — do not create real WebGL context.

    GREEN: Create `src/components/HexMapV2/scene/FogCulling.ts` with:

    1. `FOG_CONSTANTS` object:
       - `UNEXPLORED_HEX_COLOR: '#0a0a0c'` (from V1 HexTile.tsx)
       - `DEFAULT_SIGHT_RANGE: 0` (own hex only, matches AVATAR_SIGHT_RANGE)
       - `ELEVATION_BONUS: 1` (+1 range for elevated positions)
       - `VISIBLE_TO_EXPLORED_DIMOUT_MS: 500` (visible->explored fade, the ONLY animation — per CONTEXT.md instant reveal for unexplored->explored)

    2. `FogLayer` type: `'terrain' | 'signifier' | 'location' | 'label' | 'agent' | 'event' | 'grid'`

    3. `isLayerVisibleForHex(state: HexVisibilityState, layer: FogLayer): boolean`:
       - `unexplored`: only 'terrain' returns true (just the dark fill color)
       - `remembered`: 'terrain', 'signifier', 'location', 'label' return true. 'agent', 'event', 'grid' return false.
       - `visible`: all return true.

    4. `buildOriginalColorCache(tiles: HexTile[], seed: number, lakeIds?: Int16Array): { colors: Float32Array, indexByKey: Map<string, number> }`:
       - Iterates tiles, calls `getHexColor` for each, stores r/g/b in Float32Array (length tiles.length * 3).
       - Builds `Map<string, number>` mapping `"col,row"` to tile array index.
       - Import `getHexColor` from `../palette/colorUtils`.
       - Import `visKey` from `../../../types/visibility` (or inline the key format).

    5. `updateFogColors(fillMesh: THREE.InstancedMesh, visibilityMap: VisibilityMap, indexByKey: Map<string, number>, originalColors: Float32Array): void`:
       - Creates `FOG_COLOR = new THREE.Color('#0a0a0c')` (reuse across calls via module-level const).
       - For each entry in visibilityMap: look up tile index via indexByKey. If undefined, skip (fail-soft).
       - If state === 'unexplored': `fillMesh.setColorAt(idx, FOG_COLOR)`.
       - Else: restore from originalColors array via `color.setRGB(r, g, b, THREE.SRGBColorSpace)` then `fillMesh.setColorAt(idx, color)`.
       - After all updates: `if (fillMesh.instanceColor) fillMesh.instanceColor.needsUpdate = true`.
       - Batch all setColorAt calls first, set needsUpdate once at end.

    6. `computeVisibilityFromSources(sources: LOSSource[], cols: number, rows: number): Map<string, 'visible'>`:
       - For each source, BFS outward using `hexNeighbors` from `hexMath.ts` up to `source.range` steps.
       - Returns a Map of `"col,row"` -> `'visible'` for all hexes within any source's range.
       - Import `hexNeighbors` from `../../../lib/hexMath`.
       - Bounds-check: skip neighbors with col < 0 or col >= cols or row < 0 or row >= rows.
       - This function computes the "currently visible" set only. The caller (HexMapV2.tsx) will diff against the stored VisibilityMap to determine which hexes transition from unexplored->explored or visible->remembered.
  </action>
  <verify>
    <automated>npx vitest run src/components/HexMapV2/scene/__tests__/FogCulling.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - FogCulling.ts exports FOG_CONSTANTS, isLayerVisibleForHex, buildOriginalColorCache, updateFogColors, computeVisibilityFromSources
    - FOG_CONSTANTS.UNEXPLORED_HEX_COLOR === '#0a0a0c'
    - isLayerVisibleForHex('unexplored', 'agent') === false
    - isLayerVisibleForHex('remembered', 'signifier') === true
    - isLayerVisibleForHex('remembered', 'agent') === false
    - isLayerVisibleForHex('visible', 'event') === true
    - All tests pass with exit code 0
  </acceptance_criteria>
  <done>All fog culling tests pass. Module exports are complete with color override, layer gating, and visibility computation.</done>
</task>

</tasks>

<verification>
npx vitest run src/components/HexMapV2/scene/__tests__/ZoomVisibilityMatrix.test.ts src/components/HexMapV2/scene/__tests__/FogCulling.test.ts
</verification>

<success_criteria>
- ZoomVisibilityMatrix.ts and FogCulling.ts exist with all listed exports
- All unit tests pass
- No new dependencies added
- Modules are pure logic (no Three.js scene manipulation beyond setColorAt in updateFogColors)
</success_criteria>

<output>
After completion, create `.planning/phases/07-fog-zoom-grid/07-01-SUMMARY.md`
</output>
