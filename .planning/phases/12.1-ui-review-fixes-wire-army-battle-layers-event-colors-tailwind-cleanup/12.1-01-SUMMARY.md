---
phase: 12.1-ui-review-fixes
plan: 01
subsystem: ui
tags: [hexmap, three.js, armies, battles, wiring, react]

# Dependency graph
requires:
  - phase: 12-conflict-destruction
    provides: ArmyLayer.ts and BattleIndicatorLayer.ts scene modules with buildArmyRenderData/buildBattleIndicatorData
provides:
  - armies and battles props wired from GameState.graph into HexMapV2 in GameView
  - armies and battles props accepted and forwarded in HexV2View dev route
affects:
  - Any future plan that adds visual layers to HexMapV2

# Tech tracking
tech-stack:
  added: []
  patterns: ["useMemo adapter pattern: graph query → typed render data array, follow agents/locations precedent"]

key-files:
  created: []
  modified:
    - src/components/Game/GameView.tsx
    - src/components/HexMapV2/HexV2View.tsx

key-decisions:
  - "useMemo deps include gameState.tick so army/battle data updates every tick not just on graph reference change"
  - "armyRenderData and battleIndicatorData added adjacent to roadPaths memo — same section as other graph adapter memos"

patterns-established:
  - "Graph adapter pattern: buildXxxData(graph) → useMemo with [graph, tick] deps → passed as prop to HexMapV2"

requirements-completed: [UI-REVIEW-P1]

# Metrics
duration: 8min
completed: 2026-03-30
---

# Phase 12.1 Plan 01: Wire Army/Battle Layers Summary

**ArmyLayer and BattleIndicatorLayer scene modules connected to GameView and HexV2View so army sprites and battle indicators render on the hex map from live game state.**

## Performance

- **Duration:** 8 min
- **Started:** 2026-03-30T09:00:00Z
- **Completed:** 2026-03-30T09:01:07Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments
- GameView now builds armyRenderData and battleIndicatorData via useMemo adapters and passes them to HexMapV2
- HexV2View dev route (?view=hexv2) accepts and forwards armies/battles props to HexMapV2
- Type check clean, production build succeeds, all 469 test files pass

## Task Commits

Each task was committed atomically:

1. **Task 1: Wire army/battle data into GameView HexMapV2 rendering** - `54f2828` (feat)
2. **Task 2: Wire army/battle props into HexV2View dev route** - `e609f0d` (feat)

**Plan metadata:** (docs commit below)

## Files Created/Modified
- `src/components/Game/GameView.tsx` - Added imports for buildArmyRenderData/buildBattleIndicatorData, two useMemo adapters, armies/battles props passed to HexMapV2
- `src/components/HexMapV2/HexV2View.tsx` - Added ArmyRenderData/BattleIndicatorData type imports, extended HexV2ViewProps, destructured and forwarded armies/battles to HexMapV2

## Decisions Made
- useMemo deps include gameState.tick in addition to gameState.graph — ensures army/battle data refreshes every tick even if graph object reference is stable between renders
- Placed adapter memos adjacent to existing roadPaths memo (same section as other graph adapters) for code locality

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Army sprites and battle indicators now flow to HexMapV2; visual rendering depends on ArmyLayer/BattleIndicatorLayer scene code (already implemented in Phase 12-07)
- Ready for 12.1-02 (event color categorization) and subsequent plans

## Self-Check

- [x] GameView.tsx contains `import { buildArmyRenderData }` - verified by grep
- [x] GameView.tsx contains `armies={armyRenderData}` - verified by grep
- [x] HexV2View.tsx contains `armies?: ArmyRenderData[]` - verified by grep
- [x] Commits 54f2828 and e609f0d exist - verified by git log
- [x] `npx tsc --noEmit` exits 0 - verified
- [x] `npx vite build` succeeds - verified
- [x] `npm test` 469 files pass - verified

## Self-Check: PASSED

---
*Phase: 12.1-ui-review-fixes*
*Completed: 2026-03-30*
