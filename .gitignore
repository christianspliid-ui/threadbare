# Environment files
.env
.env.*
!.env.example

# Build cache
.cache/

# Logs
logs
Logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
scripts/resize-hex-tiles.mjs
scripts/playtest.mjs

# Git worktrees
.worktrees/

# Coordination file version snapshots & backups
.planning/.versions/
.planning/.snapshots/
.planning/.backlog-snapshots/
.planning/snapshots/
.planning/*.bak*
.planning/*.snapshot-*

.vercel
.env*.local

# QA server state
.qa-server.json

# Playwright MCP screenshots
.playwright-mcp/

# Codesight analysis output (auto-regenerated every session by session-start hook)
.codesight/

# Scheduled-tasks MCP runtime lock (session-specific, ephemeral)
.claude/scheduled_tasks.lock

# Interface playtest run artifacts (ephemeral diagnostics — surface findings as Linear issues)
.playtest-runs/

# Temporary verification screenshots (root level)
*.png
!public/**/*.png
!Docs/**/*.png

# Large assets moved to Obsidian vault (2026-04-06)
Docs/source-pdfs/
Design/*
# ...except the two PM-inbox files the keep-work-flowing-cc task tracks (THR-650).
# (Uses `Design/*`, not `Design/`, so git can re-include files below an otherwise-ignored dir.)
!Design/briefing.md
!Design/user-actions.md
!Design/retros/
# ...and the impediment dashboard's TEMPLATE — but deliberately NOT its render.
#
# `Design/impediment-dashboard.html` is generated from this template plus
# `Docs/impediments.md` on every `prebuild`. It was committed until THR-916, which
# made it a tracked file whose ~6,200 data lines changed on every impediment
# append. `Docs/impediments.md` tolerates that because it is `merge=union`; HTML
# has no such driver, so the moment any impediment-appending PR merged, every other
# open PR conflicted on this one file and the resolutions re-staled each other —
# four hand-resolutions across three PRs in one 40-minute run.
#
# Do NOT `git add -f` the rendered dashboard back (impediment #139 is how it got
# committed the first time). It is reproducible from committed inputs, and
# `check:generated-freshness` asserts both that the generator still produces it and
# that it stays untracked.
!Design/impediment-dashboard.template.html

# Python TTS server
.venv/

# Obsidian vault (local only — generated/edited in place, not shipped in the build)
# These directories live on disk for local tooling (Obsidian app, obsidian MCP, generate-vault).
# Use `npm run generate-vault` or `npm run rebuild-index` to regenerate locally.
/Actions/
/Actors/
/Cosmology/
/Cultures/
/Domains/
/Locations/
/Magic/
/Relationships/
/Terrain/
/Traits/
/Index.md
