# Briefing

**Generated:** 2026-07-26 13:54 local (2026-07-26 11:54 UTC) · by `keep-work-flowing-cc`

This is your inbox. It's rewritten every hour by a Claude Code task. Standing switches you need to flip live in [`Design/user-actions.md`](user-actions.md); this file is the fresh-this-hour view.

## Needs Christian

**Nothing needs you right now — seventeenth hour running with a genuinely empty list.**

One thing shipped this hour and it was a defect fix the crew owns end to end. Nothing new arrived on the board, nothing is gated, and no decision of yours is waiting.

The hosting bill is unchanged and still an FYI rather than a request. The recommendation stands: do nothing until an invoice actually reads wrong.

## Queue

**Backed up — 21 items ready, nothing in flight.** Down one from last hour, and this is the first hour in this file's window where a completion was **not** replaced by anything.

**The arithmetic is a clean drain: 22 − 1 = 21, with zero arrivals.** For the last several hours the pattern was "one ships, one or two of its own defects get filed" — a healthy pattern, but one that kept the count flat or rising. This hour the shipped item found nothing new to file. Worth naming precisely because these briefs have repeatedly said depth is the wrong health signal: composition is, and this hour's composition is simply *smaller*.

**Nothing is gated and nothing is stale.** The oldest item has been waiting two days.

**What shipped is worth a sentence because it is player-visible and had been broken since the day it was written.** Agents earn reputations as they live — *the disgraced*, *the veteran*, the one people speak carefully around. The game was meant to weave those earned titles into the prose that describes them. **It never did, not once.** The code went looking for the label under one name while every label in the game was filed under another, in three separate places. Nothing crashed; the titles simply never appeared. Fixed, with a test that proves a reputation title now actually renders.

**That connects to the largest open finding, and the pair is the real story.** The still-open item — 62 authored labels that point at nothing that exists — is the same failure one layer out: content written in one vocabulary, read in another, with silence as the only symptom. Two instances in two days argues the authoring side and the reading side of trait content have never been systematically checked against each other. It is filed high priority and it is an agent-owned verdict, not yours.

## Freshness

**Home tree: level with the server, nothing stranded.** The same two familiar leftovers persist and neither blocks anything — one tracked settings edit and Friday's weekly-retro write-up, still untracked and still the only copy anywhere. Both are agent work to land.

**Cleanup reaper: alive, ran fourteen minutes ago, nothing awaiting a human decision.** Its counts went *up* this hour rather than down — pickup sessions created more workspaces than it reclaimed. That is ordinary churn, not a fault, and it sits alongside the standing high-priority finding that the same robot carries a latent timing hazard. Both remain true.

**Live-site publishing: genuinely green.** The fix is real code, so the build actually ran, and it completed 47 seconds after the merge. That is the distinction these briefs keep drawing: a skipped build and a successful build both report "success", and this one is the real thing.

**The dead twice-daily job is unchanged: still 87 runs, still 87 failures, still unfiled.** Re-checked rather than carried — a Linear search returns only the ticket that *created* the job, no defect ticket for its failure. It has not fired since just after midnight, so a flat count is expected rather than reassuring. One-line import bug, no live consequence, **the agents' call, not yours** — but ten consecutive hours of "this wants a ticket" with no ticket is itself now the finding, and it needs an executor or retro session to file it, since this task may not.

## What's moving

**One ship, and the board is momentarily empty rather than stalled.** Nothing is in flight, nothing is awaiting merge, and 21 items are claimable — including the traits follow-ups the last two hours produced.

**The blessed encounter rebuild has its engine and is waiting on hands.** Four workstreams — the skills, the interface, the imagery, the content migration — became genuinely startable when the foundation shipped this morning. None has been picked up yet. That is ordinary scheduling, and past experience says these gaps close in minutes once a session engages, so it is deliberately not an ask.

**In your terms:** a promise the game had been quietly failing to keep for months — that people's reputations show up in how they're described — is now kept. Everything else is the machine working. None of it needs you.

---
*Standing asks live in [`Design/user-actions.md`](user-actions.md). This file is regenerated hourly by the `keep-work-flowing-cc` scheduled task; staleness is visible from the Generated timestamp above and the task's `lastRunAt` in `list_scheduled_tasks`.*
