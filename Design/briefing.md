# Briefing

**Generated:** 2026-07-24 07:09 local (2026-07-24 05:09 UTC) · by `keep-work-flowing-cc`

This is your inbox. It's rewritten every hour by a Claude Code task. Standing switches you need to flip live in [`Design/user-actions.md`](user-actions.md); this file is the fresh-this-hour view.

## Needs Christian

**One thing, unchanged from the last brief — the same four clicks.**

**1. All four old Cowork automations can be switched off now.** The replacements are live and producing real runs, and this brief was written by one of them. In the Cowork app, disable: `keep-work-flowing`, `daily-backlog-grooming`, `weekly-workflow-retro`, `weekly-project-hygiene`. Leave `weekly-invoice-check` alone — it's personal, not Threadbare. Until you flip them, four jobs run twice; nothing breaks. Details: [`user-actions.md`](user-actions.md) item 1.

## Queue

**Empty — 0 ready, 0 in dev. The board is fully drained.** World-minted ambitions (THR-726) landed at 06:57 this morning, and nothing was behind it. The executor lane will idle until a design session packages the next work. The good news: the next two candidates already carry your yes from Tuesday's chat review — **items that actually move capability tiers again** (THR-718, high priority) and **item on-use triggers: breakage, consumables, curses** (THR-719). They sit in Todo and just need a design session to move them to Ready for Dev. If you'd rather point the next design pass somewhere else, saying it in a chat session is what feeds the queue.

## Freshness

**Home tree: current and clean.** On `main`, level with the server, no local edits.

**Cleanup reaper: alive and healthy** — last run 06:40 (within the hour), tracking 23 worktrees / 31 branches / 1 stash, nothing flagged for a human decision.

## What's moving

- **World-minted ambitions shipped (THR-726, Done at 06:57, PR #782).** Wars, disasters, and windfalls now plant new wants in mortals — ambitions no longer grow only from within. That completes the three-ticket coupling chain from last week's interface audit (Secrets & Favors, economy-answers-the-god, and this).
- **The overnight shift left tidy follow-ups on the board:** two deferrals filed while retiring the dead item-ability contract — wiring trait-granting effects to a real consumer (THR-737) and 18 action templates missing a required field (THR-736). Both are backlog, not blockers.

---
*Standing asks live in [`Design/user-actions.md`](user-actions.md). This file is regenerated hourly by the `keep-work-flowing-cc` scheduled task; staleness is visible from the Generated timestamp above and the task's `lastRunAt` in `list_scheduled_tasks`.*
