---
needsChristian: thr-907-verdict-askable, thr-947-stuck-closeout, thr-931-stuck-closeout, thr-792-stuck-closeout, thr-961-sound-feel, thr-962-nudge-stage-cues
queue: backed-up
freshness: dirty
deploy: skipped
tasks: ok
---
# Briefing

**Generated:** 2026-08-02 08:57 local (2026-08-02 06:57 UTC) · by `keep-work-flowing-cc`

This is your inbox. It's rewritten every hour by a Claude Code task. Standing switches you need to flip live in [`Design/user-actions.md`](user-actions.md); this file is the fresh-this-hour view.

## Needs Christian

**Three things, and one of them is new: the game makes sounds now, and nobody has heard them but the machine that wrote them.**

### 1. The encounter verdict — play the five-encounter slice and rule on it

*Unchanged, and still the one thing that moves the game forward.* Everything it was waiting on is finished and live. What's left is a sitting, not a decision made cold: play the slice and rule on five things — does the prose read right, does the firing rhythm feel right, can you see the consequences of what you did, is the interface out of the way, and **is deciding actually fun**. "Needs another iteration" is a perfectly good ruling on any of the five; it closes the question and charters the follow-up.

**What it holds:** all encounter-writing work — eleven content tickets — plus one downstream question about four finished capital-city templates (a council mediation, a noble's court, a house unification, and raising a monument). Those four are written, tested, and sitting on a shelf; once the format is settled they either land as-is and get tidied up alongside the seven earlier ones, or the branch is dropped and they're re-written to the new standard. Both are cheap from here. **Nothing decays while you decide** — say the word in chat whenever you want it prepped.

**One caveat, so a sighting during play isn't misread:** there's a known cosmetic defect where a nudge card can print a raw `{they}` instead of a name. It is *not* one of the interface defects you enumerated, it's already ticketed, and it doesn't affect the five things you'd be ruling on.

*— raised again by the orchestrator lane's 05:26Z run, for the ninth run running.*

### 2. The sound design landed overnight — two questions only your ears can answer

The cello drone, the resolve notes and the registration cues shipped this morning. Both questions below are genuinely yours; neither is a technical call an agent can make.

- **[Do the new encounter sounds feel right?](https://linear.app/threadbare/issue/THR-961)** Every knob — volume, tone, timing — is already exposed and ready to turn. This just needs you to play through a few encounters, listen, and say what to change. It is the cheapest item on this list.
- **[Should the encounter screen carry these sounds at all?](https://linear.app/threadbare/issue/THR-962)** The sound design was built for the *older* encounter screen — the one that got quietly replaced by the nudge-card interface months ago, without anyone updating the sound work to match. Re-pointing it at the screen players actually see is a small change. But the old design was a five-beat held breath over a row of three cards, and the nudge stage has a different rhythm entirely. So: does that cue design suit the screen you have now, does it want re-timing, or does the sound spec want rewriting for the new shape? **Worth answering in the same sitting as the slice playthrough** — you'd be listening to it either way.

### 3. Three finished jobs are stuck in the "in progress" column

Unchanged from last hour — nothing arrived, nothing cleared. None of them needs work: all three are done, verified, and merged. The automated lanes are structurally forbidden from marking anything finished, so they sit until you clear them.

- **[Moving the hourly status files off the main branch](https://linear.app/threadbare/issue/THR-947)** — shipped and measured. The four-hour observation window closed cleanly: hourly paperwork used to account for 4 of every 10 merges to the main line, and it is now 0, with the lanes proven still running. This briefing is the thing it moved.
- **[Turning on the fast documentation check](https://linear.app/threadbare/issue/THR-931)** — you already did this on 2026-08-01; it was found afterwards, verified working in both directions, and just never ticked. *One line of our own instructions still claims it isn't done — that correction is an agent's job, not yours, and it is noted below.*
- **[Fixing a wrong note in the weekly housekeeping script](https://linear.app/threadbare/issue/THR-792)** — the correction shipped six days ago under a neighbouring ticket's number, which is why nothing ever closed it.

## Queue

**Backed up — 55 items ready for an agent to pick up.** Planning is comfortably ahead of execution, which is the safe direction. No action needed from you.

Five items have gone quiet (untouched 7+ days): two action-art tickets, the company action cards, and two group-naming defects. All low priority, none blocking anything — flagged only so they don't rot unseen.

**Four jobs are parked mid-flight.** Three are the finished-but-unticked rows above. The fourth is the capital-city template batch, deliberately held behind the encounter verdict — a hold, not a stall, and it is folded into item 1.

## Freshness

Your working copy is **on the main branch and fully up to date** — nothing to pull.

Two files have local edits sitting in it: `.claude/settings.json` and `.claude/settings.local.json`. Both are settings, not game content, and they are **not** blocking anything today — the automatic sync is keeping pace. Worth knowing only because a local edit to a file the project later changes is the one way that sync can stall silently.

The overnight cleanup ran on time (08:40 local). It flags **one** leftover working folder as needing a human decision — those are never deleted automatically, by design.

Production is current: the only commits since the last publish were notes and documentation, so the site correctly did not need rebuilding. Merge checks, open pull requests and all nine scheduled jobs are healthy.

## What's moving

The sound design pass merged overnight and immediately split into the two questions above. Since then the lanes have been draining documentation-only tickets — small, self-contained corrections to our own instructions — which is what they do while the content queue is held. That hold is item 1.

---
*Standing asks live in [`Design/user-actions.md`](user-actions.md). This file is regenerated hourly by the `keep-work-flowing-cc` scheduled task; staleness is visible from the Generated timestamp above.*
