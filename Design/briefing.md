# Briefing

**Generated:** 2026-07-26 15:54 local (2026-07-26 13:54 UTC) · by `keep-work-flowing-cc`

This is your inbox. It's rewritten every hour by a Claude Code task. Standing switches you need to flip live in [`Design/user-actions.md`](user-actions.md); this file is the fresh-this-hour view.

## Needs Christian

**Nothing needs you right now — nineteenth hour running with a genuinely empty list.**

One thing shipped this hour, it was the crew's own to fix, and it filed nothing on its way out. Nothing is gated on a decision of yours, and no new question has appeared.

The hosting bill is unchanged and still an FYI rather than a request. The recommendation stands: do nothing until an invoice actually reads wrong.

## Queue

**Backed up — 20 items ready, nothing in flight.** Down one, and the arithmetic is the clean kind: **21 − 1 shipped + 0 filed = 20.** The work that shipped found nothing new to write down, which is the honest reason a count falls.

**High-priority work halved again, from two items to one.** The survivor is the 62 authored labels that point at nothing — an agent-owned technical call, not yours.

**Nothing is gated and nothing is stale.** The oldest waiting item has been there two days.

**What shipped is a repair to the crew's own tooling, and it proved itself within the hour.** A cleanup robot runs every hour and clears away abandoned workspaces. It could, with bad timing, delete a workspace a session was *still working in* — and when that happened the session's tools would quietly start writing to the wrong copy of the project, with no error anywhere. The fix teaches it to notice someone is still there even when the only sign is edited files rather than saved work.

**The proof is the part worth a sentence.** The cleanup run an hour before the fix left a failure warning on one workspace it could not remove. The first run *after* the fix looked at that same workspace and correctly left it alone, no warning. That is the change working on live evidence rather than on a test.

## Freshness

**Home tree: level with the server, nothing stranded.** The same two familiar leftovers persist and neither blocks anything — one tracked settings edit and Friday's weekly-retro write-up, still untracked and still the only copy anywhere. Both are agent work to land.

**Cleanup reaper: alive, ran fourteen minutes ago, clean, nothing awaiting a human decision.** Workspaces flat, leftover branches down from 34 to 31.

**Live-site publishing: correctly idle, not broken.** This hour's merge touched only crew tooling and a generated report — no game code — so the publisher skipped the build on purpose. The live site is unchanged and still current from the last real build ninety minutes ago. Worth naming because a skipped build and a successful build report the same single word, and only the fine print distinguishes them; that ambiguity is the whole reason a ticket exists to automate the distinction.

**The dead twice-daily job is flat at 88 runs, 88 failures — and still unfiled after twelve hours.** It has not fired since the last brief, so nothing new is known this hour. One-line import bug, no live consequence, **the agents' call, not yours** — but twelve consecutive hours of "this wants a ticket" with no ticket has stopped being a footnote. This surface can only report; it cannot file. It needs an executor or retro session to pick up from here.

## What's moving

**One ship, and the board is momentarily empty rather than stalled.** Nothing in flight, nothing awaiting merge, no open pull requests, 20 items claimable — including the remaining high-priority one.

**The blessed encounter rebuild has its engine and is still waiting on hands.** Four workstreams became genuinely startable when the foundation shipped this morning; none has been picked up. Past experience says these gaps close in minutes once a session engages, so it is deliberately not an ask.

**In your terms:** the machinery that sweeps the workshop learned to tell an empty bench from an occupied one, and demonstrated it on the very next sweep. Everything else is the machine working. None of it needs you.

---
*Standing asks live in [`Design/user-actions.md`](user-actions.md). This file is regenerated hourly by the `keep-work-flowing-cc` scheduled task; staleness is visible from the Generated timestamp above and the task's `lastRunAt` in `list_scheduled_tasks`.*
