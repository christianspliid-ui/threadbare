# Briefing

**Generated:** 2026-07-27 13:54 local (2026-07-27 11:54 UTC) · by `keep-work-flowing-cc`

This is your inbox. It's rewritten every hour by a Claude Code task. Standing switches you need to flip live in [`Design/user-actions.md`](user-actions.md); this file is the fresh-this-hour view.

## Needs Christian

**Nothing needs you right now.** No decision is waiting, no switch needs flipping, and nothing arrived for you this hour.

**Last hour's test, reported whichever way it went: by this brief the second half of the new encounter experience is started, or I name that it isn't. It started.** The crew picked it up and had a finished branch open for merging twelve minutes before this brief, with the automated checks running on it. That's two hours running where the test came out favourably, and I'm reporting it at the volume I'd have used for a failure.

## Queue

**21 jobs ready — eight middling, thirteen minor, none top-priority. One job in hand.**

**The shelf lost its only top-priority job to the workbench rather than to the bin.** Yesterday's pattern was roughly one ship plus one self-found problem per hour, holding the count flat; the last two hours have both moved it down by one — first by shipping, now by starting. Nothing new was filed this hour either.

**What's left on the shelf is all middling or minor** — no top-priority work is waiting behind the job in hand. Nothing is stale (the longest-waiting job has sat three days, well inside the limit) and nothing is blocked.

## Freshness

**Home tree: clean, current, nothing stranded.** It has caught up fully with the server — no lag at all this hour. The same two small leftovers as the last twenty-three hours: a permissions edit to the tool config, and Friday's retro write-up. Both are the crew's to land; neither blocks anything.

**Cleanup reaper: alive, ran fourteen minutes ago, clean, nothing awaiting a human decision.**

**The live site is up to date.** Nothing has changed the game itself since the last publish — only notes and docs — so there was nothing to rebuild.

**Discord: nothing new this hour.** Genuinely empty rather than unread.

## What's moving

**The half of the new encounter experience you'll actually look at is being built right now.** That's the hand of nudges you pick from, the moment fate turns its answer over, and what the world says afterwards. It went from untouched to a finished branch inside this hour, and it is set to merge itself the moment the automated checks come back green.

**The half you don't see was finished last hour** — the rules and worked example the crew writes encounters against. So both halves of the workstream you named as first priority will have landed within about two hours of each other, if this one goes through cleanly.

**The test I'm setting for the next brief, worded so it can't pass while the thing I'm watching for goes wrong:** by 12:53 UTC either that branch is merged *and* the live site is serving it, or I name what stopped it.

---
*Standing asks live in [`Design/user-actions.md`](user-actions.md). This file is regenerated hourly by the `keep-work-flowing-cc` scheduled task; staleness is visible from the Generated timestamp above and the task's `lastRunAt` in `list_scheduled_tasks`.*
