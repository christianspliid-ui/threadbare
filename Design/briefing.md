# Briefing

**Generated:** 2026-07-23 22:10 local (20:10 UTC) · by `keep-work-flowing-cc`

This is your inbox. It's rewritten every hour by a Claude Code task. Standing switches you need to flip live in [`Design/user-actions.md`](user-actions.md); this file is the fresh-this-hour view.

## Needs Christian

**One thing, and it's the same four clicks as before. The second item from the last brief — your project folder parked on an old branch — resolved this hour: the folder is back on `main`, fully current, and the ten retro write-ups that existed only on this machine are now safely backed up.**

**1. All four old Cowork automations can be switched off now.** The replacements are live and producing real runs, and this brief was written by one of them. In the Cowork app, disable: `keep-work-flowing`, `daily-backlog-grooming`, `weekly-workflow-retro`, `weekly-project-hygiene`. Leave `weekly-invoice-check` alone — it's personal, not Threadbare. Until you flip them, four jobs run twice; nothing breaks. Details: [`user-actions.md`](user-actions.md) item 1.

## Queue

**Healthy — 3 ready, executor mid-flight, nothing blocked, nothing stale.** The queue refilled since the last brief: the party-formation design was handed off tonight.

- **In development: the wiki freshness enforcement gate (THR-730, High)** — your directive from this morning; still actively moving (last touched minutes before this run).
- **Ready, top first:** party formation & group mechanics (THR-74, High — adventurers band together, travel as a group, and resolve encounters together; designed and handed off tonight), the world minting ambitions into mortals (THR-726), and a small dead-contract cleanup (THR-722).

## Freshness

**Home tree: current and clean — the parked-branch item is closed.** It's on `main`, level with the server, no local edits. The backup PR that landed tonight carried the ten retro write-ups and the local settings tweaks onto `main`, so the one-machine exposure flagged yesterday is gone too. One harmless leftover: a recovery stash holding a 3-line edit to an already-superseded briefing file — inert, safe to drop whenever a cleanup pass wants to.

**Cleanup reaper: alive and healthy** — last run 21:40 (within the hour), tracking 24 worktrees / 33 branches / 1 stash, nothing flagged for a human decision.

## What's moving

- **Party formation & group mechanics is designed and queued (THR-74, High, plan doc PR #767).** The half of the tavern rework that needed its own pass: heroes recruiting companions through a multi-step social scene, then moving and fighting as a party.
- **The wiki freshness gate is being built right now** (THR-730, High) — "keep the design reference wiki current on every relevant PR" becoming a blocking CI check with an exemption token and a weekly escape-net.
- **The home-tree backup landed (PR #768):** ten retro write-ups from April–July that lived only on this machine are now on `main`.
- **Earlier tonight: Divine Receipt shipped (THR-727, 21:01).** When an action card you played resolves, the game now tells you what actually happened — in story terms, not numbers. Directive to shipped in under a day.
- **The PR board remains at zero open** — new PRs are self-merging on green.

---
*Standing asks live in [`Design/user-actions.md`](user-actions.md). This file is regenerated hourly by the `keep-work-flowing-cc` scheduled task; staleness is visible from the Generated timestamp above and the task's `lastRunAt` in `list_scheduled_tasks`.*
