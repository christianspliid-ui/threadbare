# Briefing

**Generated:** 2026-07-22 00:54 local (22:54 UTC) · by `keep-work-flowing-cc`

This is your inbox. It's rewritten every hour by a Claude Code task — the replacement for the old Cowork "keep-work-flowing" chat brief. Standing switches you need to flip live in [`Design/user-actions.md`](user-actions.md); this file is the fresh-this-hour view.

## Needs Christian

**Nothing is holding work up. One standing switch is still yours, unchanged.**

> **Still yours: three replacement jobs are built, switched off, waiting on you to watch each run once.**

Unchanged since last night — all three present, all three still **off**.

| Job | When it would run | State |
|---|---|---|
| Daily queue grooming | 09:16 daily | built, **off** |
| Wednesday workflow review | 11:13 Wednesdays | built, **off** |
| Sunday tidy-up | 10:10 Sundays | built, **off** |

What they need: **run each once in a normal chat session, read what it produces, say yes.** Then it goes live and its Cowork twin gets switched off in the same step. Start with the daily queue grooming.

Separately and still yours: **switch off `keep-work-flowing` in the Cowork app.** No trial needed — its replacement is live and wrote this brief.

**Open but unforced: the next big direction.** War deepened (battles with real texture, sieges that tighten as they drag, the mark a war leaves on a region once it ends) or the economy made visible (banditry and tolls surfacing as encounters, the map showing what's actually moving along a road). A one-word steer is enough whenever you have one — there is a full lane of work queued in front of it either way, so nothing idles while you decide.

## Queue

**Healthy — seven ready, up one from six.**

- **Six of the seven are the encounter scene-integration slices**, all in Content Architecture: THR-694 (name the entity an encounter is *with*), THR-695 (let aftermath effects point at it), THR-697 (make planted story-seeds actually spawn) — these three high-priority — then THR-696 (cast names in prose), THR-698 (default supporting casts), THR-699 (the content sweep). They're a chain: A→B→C→D→E→F. The three foundations are the three marked high, so priority-sorted pickup naturally takes them first. Nothing is blocked (every slice's prerequisite is itself in this queue), nothing is stale.
- **The seventh is new this hour: THR-702** (medium) — a triage of the armed-auto-merge PRs that stall at `BEHIND`, filed off the merge stumble described below. Agent work, not yours.

## Freshness

**Home tree current.** On `main`, 0 commits behind, no local modifications, nothing untracked, nothing stranded.

The stale-branch cleanup is healthy — last run 00:40, ~14 minutes ago, tracking 22 work folders and 31 branches, one stash, with **nothing flagged for a human decision**.

## What's moving

- **The plan-doc lint scope fix shipped** — THR-692 merged tonight (PR #696). That was the last of the process-lint tidy-ups in flight.
- **Its merge exposed a real gap, now ticketed:** PR #696 sat ~10h with every check green and auto-merge armed, but never landed because `main` moved underneath it (`mergeStateStatus: BEHIND`). THR-702 was filed to close that "arm and forget" gap — this is the same rot the PR-backlog item in `user-actions.md` describes, now getting a systemic fix.
- **The executor's only In Dev item is THR-677** (Cowork prompt port), deliberately unassigned — it waits on your trial approvals above, not on an agent.

---
*Standing asks live in [`Design/user-actions.md`](user-actions.md). This file is regenerated hourly by the `keep-work-flowing-cc` scheduled task; if the timestamp at the top looks old, check the task's `lastRunAt` in the scheduled-task list to tell "nothing new to report" from "task stopped running."*
