# Briefing

**Generated:** 2026-07-22 01:55 local (23:55 UTC) · by `keep-work-flowing-cc`

This is your inbox. It's rewritten every hour by a Claude Code task — the replacement for the old Cowork "keep-work-flowing" chat brief. Standing switches you need to flip live in [`Design/user-actions.md`](user-actions.md); this file is the fresh-this-hour view.

## Needs Christian

**Nothing new landed on your desk this hour — and the foundation of tonight's game-facing work just shipped. One operational switch is still yours, unchanged.**

> **Three replacement jobs are built, switched off, waiting on you to watch each run once.**

Unchanged since 17:54 — all three present, all three still **off**.

| Job | When it would run | State |
|---|---|---|
| Daily queue grooming | 09:16 daily | built, **off** |
| Wednesday workflow review | 11:13 Wednesdays | built, **off** |
| Sunday tidy-up | 10:10 Sundays | built, **off** |

What they need: **run each once in a normal chat session, read what it produces, say yes.** Then it goes live and its Cowork twin gets switched off in the same step. Start with the daily queue grooming.

Separately and still yours: **switch off `keep-work-flowing` in the Cowork app.** No trial needed — its replacement is live and wrote this brief.

**Open but unforced: the next big direction.** War deepened (battles with real texture, sieges that tighten as they drag, the mark a war leaves on a region once it ends) or the economy made visible (banditry and tolls surfacing as encounters, the map showing what's actually moving along a road). A one-word steer is enough whenever you have one — but the lane is full for a while either way, so there's no clock on this.

## Queue

**Healthy — six ready, and the foundation just shipped.**

- The encounter scene-integration plan's **first job landed tonight** (THR-694 → PR #701): encounters can now name the real person or place they're happening *with*, instead of saying "the other party" nine times in one scene. That was the load-bearing piece the other five build on.
- **Six ready for pickup:** the next four scene-integration slices — THR-695 (let aftermath effects point at that named entity) and THR-697 (make planted story-seeds actually spawn), both **high priority**, then THR-696 (cast placeholders in prose), THR-698 (default supporting casts), and THR-699 (the content sweep that cashes the rest in) — plus one new maintenance item, THR-702, a fix so armed pull requests stop stalling silently (see "What's moving").
- **Pickup order is sound.** The two remaining high-priority slices are the two the others depend on, so priority sorting takes them first. Nothing is blocked, nothing is stale.
- **One thing in development:** the Cowork job ports (THR-677), unassigned on purpose — it waits on your trial approvals above, not on an agent.

## Freshness

**Home tree is parked on this task's own scheduled-session branch — a known, harmless state, not the ten-hour park from yesterday.** Nothing unique is stranded there (it's an exact copy of an older `main`), the tree is clean, and local `main` is 5 commits behind the server — under the alarm line. This clears on its own when the session ends and the autosync/cleanup job runs; if it's still there when you open your morning session, that session's own startup check offers the one-line repair automatically. No action needed from you.

The stale-branch cleanup is healthy — last run 01:40, fifteen minutes ago, tracking 22 work folders and 28 branches, with **nothing flagged for a human decision**.

## What's moving

- **Scene-integration Slice A shipped** (THR-694 → PR #701, 23:35) — the first and load-bearing piece of the encounter work. Four more slices are queued behind it.
- **The plan-doc lint fix finally landed** (THR-692 → PR #696) after sitting ~10 hours with every check green, blocked only by needing a refresh against `main`. That exact experience is now a filed job of its own — **THR-702** — to close the "arm the merge and forget it" gap so future pull requests don't stall the same way.
- **The pull-request backlog dropped 15 → 14** as those two merged. Oldest untouched is still #327 from 12 June; the rest are old documentation branches, agent-decidable and not yours.

---
*Standing asks live in [`Design/user-actions.md`](user-actions.md). This file is regenerated hourly by the `keep-work-flowing-cc` scheduled task; if the timestamp at the top looks old, check the task's `lastRunAt` in the scheduled-task list to tell "nothing new to report" from "task stopped running."*
