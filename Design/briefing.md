# Briefing

**Generated:** 2026-07-23 23:10 local (21:10 UTC) · by `keep-work-flowing-cc`

This is your inbox. It's rewritten every hour by a Claude Code task. Standing switches you need to flip live in [`Design/user-actions.md`](user-actions.md); this file is the fresh-this-hour view.

## Needs Christian

**One thing, unchanged from the last brief — the same four clicks.**

**1. All four old Cowork automations can be switched off now.** The replacements are live and producing real runs, and this brief was written by one of them. In the Cowork app, disable: `keep-work-flowing`, `daily-backlog-grooming`, `weekly-workflow-retro`, `weekly-project-hygiene`. Leave `weekly-invoice-check` alone — it's personal, not Threadbare. Until you flip them, four jobs run twice; nothing breaks. Details: [`user-actions.md`](user-actions.md) item 1.

## Queue

**Healthy — 3 ready, executor mid-flight, nothing blocked, nothing stale.**

- **In development: the wiki freshness enforcement gate (THR-730, High)** — your directive from this morning. Its pull request is up with auto-merge armed and checks running, so it should land on its own shortly; the session was still actively touching it minutes before this run.
- **Ready, top first:** party formation & group mechanics (THR-74, High — adventurers band together, travel as a group, and resolve encounters together), the world minting ambitions into mortals (THR-726), and a small dead-contract cleanup (THR-722).

## Freshness

**Home tree: current and clean.** On `main`, level with the server, no local edits. The one harmless leftover from yesterday's repair — a recovery stash holding a 3-line edit to an already-superseded briefing file — remains inert; any cleanup pass may drop it.

**Cleanup reaper: alive and healthy** — last run 22:40 (within the hour), tracking 24 worktrees / 31 branches / 1 stash, nothing flagged for a human decision.

## What's moving

- **The wiki freshness gate is one green CI run from landing (THR-730, High, PR #766).** Auto-merge is armed and checks are in progress. One correction to the last brief: the PR board is not at zero — this one PR is open, and it's healthy, not stuck.
- **Party formation & group mechanics (THR-74, High) sits at the top of the ready queue** — designed and handed off earlier tonight; the executor should reach it once the wiki gate lands.
- **Earlier tonight: Divine Receipt shipped (THR-727, 21:01).** When an action card you played resolves, the game now tells you what actually happened — in story terms, not numbers. Directive to shipped in under a day.

---
*Standing asks live in [`Design/user-actions.md`](user-actions.md). This file is regenerated hourly by the `keep-work-flowing-cc` scheduled task; staleness is visible from the Generated timestamp above and the task's `lastRunAt` in `list_scheduled_tasks`.*
