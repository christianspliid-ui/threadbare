# Briefing

**Generated:** 2026-07-24 04:10 local (2026-07-24 02:10 UTC) · by `keep-work-flowing-cc`

This is your inbox. It's rewritten every hour by a Claude Code task. Standing switches you need to flip live in [`Design/user-actions.md`](user-actions.md); this file is the fresh-this-hour view.

## Needs Christian

**One thing, unchanged from the last brief — the same four clicks.**

**1. All four old Cowork automations can be switched off now.** The replacements are live and producing real runs, and this brief was written by one of them. In the Cowork app, disable: `keep-work-flowing`, `daily-backlog-grooming`, `weekly-workflow-retro`, `weekly-project-hygiene`. Leave `weekly-invoice-check` alone — it's personal, not Threadbare. Until you flip them, four jobs run twice; nothing breaks. Details: [`user-actions.md`](user-actions.md) item 1.

## Queue

**Starved — 1 ready item.** The executor is mid-flight on the dead-contract cleanup (THR-722, last activity within the hour), and the only thing waiting behind it is the world-minted-ambitions feature (THR-726 — world events write themselves into what mortals want). Nothing is blocked or stale — the queue is simply draining faster than design is feeding it. The next design session should plan new work; no action from you unless you have a direction you want designed next.

## Freshness

**Home tree: current and clean.** On `main`, level with the server, no local edits.

**Resolved since the last brief:** the missing command shims (impediment #203) are back — `node_modules/.bin` has its 99 shims again, so `npm run dev` and `npm test` work from the home tree. Nothing for you here.

**Cleanup reaper: alive and healthy** — last run 03:40 (within the hour), tracking 22 worktrees / 30 branches / 1 stash, nothing flagged for a human decision.

## What's moving

- **The executor is still on THR-722** (retiring a written-but-never-read item-ability contract flagged by this week's interface audit) — last activity ~03:14, no idle gap.
- **Overnight recap:** party formation (THR-74, High) shipped Done end to end — adventurers can now band together, with the group engine, group-eligible encounters, and content/docs all merged.

---
*Standing asks live in [`Design/user-actions.md`](user-actions.md). This file is regenerated hourly by the `keep-work-flowing-cc` scheduled task; staleness is visible from the Generated timestamp above and the task's `lastRunAt` in `list_scheduled_tasks`.*
