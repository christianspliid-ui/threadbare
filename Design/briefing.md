# Briefing

**Generated:** 2026-07-25 12:09 local (2026-07-25 10:09 UTC) · by `keep-work-flowing-cc`

This is your inbox. It's rewritten every hour by a Claude Code task. Standing switches you need to flip live in [`Design/user-actions.md`](user-actions.md); this file is the fresh-this-hour view.

## Needs Christian

**Still just the one optional preference, carried unchanged from this morning — nothing is blocked on it, and the safe default is already in place.**

- **Five roadmap projects are marked "in progress" but have no work left in them.** "Agent Coordination Protocol", "Repo Health", "Marketing Site", "Small manual tweaks" and "Procedural Hex Vignettes" have each had their last item finished. The tidy-up rule says close them; each one's own description, though, says it's meant to be a *permanent home* for a kind of work ("where small manual tweaks directly driven by the human goes"), and Hex Vignettes is a paused experiment whose later phases were never written up. This morning's grooming pass closed none of them and recommends leaving them open as standing intake buckets — so "in progress with nothing in it" reads as "no current work", not "stalled". **That recommendation holds by default**; it only needs you if you'd rather the roadmap only ever show projects with live work. **Yes/no, no rush** — ignoring it changes nothing.

Nothing else needs you. The board's only urgent item closed this hour and the queue is draining on its own.

## Queue

**Healthy — 14 ready items, and for the first time in two days there is no urgent item on the board at all.** The self-closing-tickets fix (the last High) finished at 11:14; everything remaining is Medium or Low. Nothing is blocked and nothing has gone stale — every item was touched within the last two days.

What's waiting: two hygiene sweeps from Friday's retro, the group-conflict and Reunite/Sunder company designs, the instruction-file slimming pass, two vocabulary proposals, and a tail of small deferrals. One item is being worked right now — the player-cast variance spec that already carries your "yes, with a safety floor" verdict.

## Freshness

**Home tree: on `main` and level with the server** — zero commits behind this run, down from four at the last brief; nothing parked, nothing stranded. The same two leftovers persist and neither blocks anything: one tracked settings edit (`.claude/settings.local.json`) and one untracked file, Friday's weekly-retro write-up, which is still the only copy anywhere and still agent work to land via a docs pass.

**Cleanup reaper: alive** — last run 11:40 (29 minutes ago), tracking 27 work folders / 36 branches / 1 stash, nothing awaiting a human decision.

## What's moving

- **The self-closing-tickets problem is closed and signed off (11:14).** This was the fault where a ticket marked itself finished because some unrelated pull request happened to mention its number — it hit the same ticket three times in two days. Both halves are now proven rather than assumed: the repo-side fix shipped Thursday, your setting change went in yesterday morning, and the live two-pull-request proof ran an hour ago — the one built to trip the fault left its test ticket untouched, the one built to close properly closed it. The written record landed at 11:12 and the ticket went to Done at 11:14. Tickets still link to their pull requests; they just stop moving themselves.
- **Nothing is queued behind CI.** Zero pull requests open — everything that landed today has merged clean.
- **In flight:** player-cast outcome variance — making a paid miracle roll the same odds mortals do, floored so it can never outright fail, only land crooked. Mid-implementation.

---
*Standing asks live in [`Design/user-actions.md`](user-actions.md). This file is regenerated hourly by the `keep-work-flowing-cc` scheduled task; staleness is visible from the Generated timestamp above and the task's `lastRunAt` in `list_scheduled_tasks`.*
