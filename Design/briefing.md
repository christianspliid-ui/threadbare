# Briefing

**Generated:** 2026-07-22 02:54 local (00:54 UTC) · by `keep-work-flowing-cc`

This is your inbox. It's rewritten every hour by a Claude Code task — the replacement for the old Cowork "keep-work-flowing" chat brief. Standing switches you need to flip live in [`Design/user-actions.md`](user-actions.md); this file is the fresh-this-hour view.

## Needs Christian

**Nothing new needs you this hour.** The queue is healthy and draining on its own, and both foundation pieces of the encounter content work have now shipped. The one standing operational item is unchanged.

> **Still yours, unchanged: three replacement jobs are built, switched off, waiting on you to watch each run once.**

All three present, all three still **off** — same as every run since 17:54 yesterday.

| Job | When it would run | State |
|---|---|---|
| Daily queue grooming | 09:16 daily | built, **off** |
| Wednesday workflow review | 11:13 Wednesdays | built, **off** |
| Sunday tidy-up | 10:10 Sundays | built, **off** |

What they need: **run each once in a normal chat session, read what it produces, say yes.** Then it goes live and its Cowork twin gets switched off in the same step. Start with the daily queue grooming. Separately and still yours: **switch off `keep-work-flowing` in the Cowork app** — no trial needed, its replacement is live and wrote this brief.

**Open but unforced: the next big direction.** War deepened (battles with real texture, sieges that tighten as they drag, the mark a war leaves on a region) or the economy made visible (banditry and tolls surfacing as encounters, the map showing what moves along a road). A one-word steer is enough whenever you have one — the lane is full for a while either way, so there's no clock on this.

## Queue

**Healthy — five ready for pickup.**

- **Both foundation slices are now done.** Naming the entity an encounter is happening *with* (THR-694, PR #701 last hour) and pointing aftermath effects at that named entity (THR-695, PR #703 this hour) have both shipped — the load-bearing pair the rest of the encounter work builds on.
- **Five ready:** the four remaining scene-integration slices — THR-697 (make planted story-seeds actually spawn, marked high), THR-696 (cast placeholders in prose), THR-698 (default supporting casts), THR-699 (the social/tavern/borderland content sweep) — plus THR-702, an infrastructure triage of the aging pull-request backlog (see below).
- **Pickup order is sound.** THR-697 is the only high-priority one left and has no unmet dependency. Nothing is blocked, nothing is stale.
- **One in development:** the Cowork job ports (THR-677), unassigned on purpose — it waits on your trial approvals above, not on an agent.

## Freshness

**Home tree parked, but benign.** It's sitting at a slightly older snapshot (detached, 4 commits back) with **nothing stranded and nothing modified** — the recurring harness park, not damage. Autosync reattaches this kind of loss-free park on its own within the hour; if your morning session opens before it does, that session's own startup check runs the safe three-command repair automatically. No action from you.

The stale-branch cleanup is healthy — last run 02:40, fourteen minutes ago, tracking 21 work folders and 29 branches, with **nothing flagged for a human decision.**

## What's moving

- **The second encounter foundation slice landed this hour** — THR-695 (aftermath effects can point at the named entity, PR #703). With THR-694 (last hour, PR #701) that completes the pair; the four dependent slices are unblocked and top of queue.
- **A new infrastructure ticket, THR-702**, is in the queue: armed auto-merge PRs that sit idle at `BEHIND` never merge themselves, which is the mechanism behind the aging PR backlog. Agent work, not yours.
- **The pull-request backlog is 14**, oldest still #327 from 12 June — the older ones are stale documentation branches, agent-decidable. **Nothing regressed.**

---
*Standing asks live in [`Design/user-actions.md`](user-actions.md). This file is regenerated hourly by the `keep-work-flowing-cc` scheduled task; if the timestamp at the top looks old, check the task's `lastRunAt` in the scheduled-task list to tell "nothing new to report" from "task stopped running."*
