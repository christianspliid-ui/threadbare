# Briefing

**Generated:** 2026-07-24 06:10 local (2026-07-24 04:10 UTC) · by `keep-work-flowing-cc`

This is your inbox. It's rewritten every hour by a Claude Code task. Standing switches you need to flip live in [`Design/user-actions.md`](user-actions.md); this file is the fresh-this-hour view.

## Needs Christian

**One thing, unchanged from the last brief — the same four clicks.**

**1. All four old Cowork automations can be switched off now.** The replacements are live and producing real runs, and this brief was written by one of them. In the Cowork app, disable: `keep-work-flowing`, `daily-backlog-grooming`, `weekly-workflow-retro`, `weekly-project-hygiene`. Leave `weekly-invoice-check` alone — it's personal, not Threadbare. Until you flip them, four jobs run twice; nothing breaks. Details: [`user-actions.md`](user-actions.md) item 1.

## Queue

**Starved — 0 ready items.** The executor is mid-flight on the last thing on the board: world-minted ambitions (THR-726 — world events write themselves into what mortals want), active minutes ago. When it lands, the board is empty. The next design session should plan new work; if you have a direction you'd like designed next, saying it in a chat session is what feeds the queue — otherwise the design lane will pick from the roadmap on its own.

## Freshness

**Home tree: current and clean.** On `main`, level with the server, no local edits.

**Cleanup reaper: alive and healthy** — last run 05:40 (within the hour), tracking 23 worktrees / 31 branches / 1 stash, nothing flagged for a human decision.

## What's moving

- **Overnight: the dead item-ability contract is retired (THR-722, Done at 05:18).** Items that claimed to grant abilities nobody ever read — flagged by this week's interface audit — no longer carry the false promise; the write sites are cleaned up and the contract is off the books.
- **The executor rolled straight onto THR-726** (world-minted ambitions) with no idle gap — the feature that lets wars, disasters, and windfalls plant new wants in mortals instead of ambitions only growing from within.

---
*Standing asks live in [`Design/user-actions.md`](user-actions.md). This file is regenerated hourly by the `keep-work-flowing-cc` scheduled task; staleness is visible from the Generated timestamp above and the task's `lastRunAt` in `list_scheduled_tasks`.*
