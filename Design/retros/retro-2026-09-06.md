# Retrospective — 2026-09-06

## Period
From: 2026-08-28 (exclusive; last retro)
To: 2026-09-06

**Cycle note:** this run fired on Sunday 2026-09-06 as a resume catch-up for the Friday 17:00 UTC slot — the host was offline from ~2026-09-04 16:00 local, and `list_scheduled_tasks` shows `weekly-retro`, `tb-opus-pickup` and `keep-work-flowing-cc` all firing within one second of resume. Volume tells the same story: 30 of the cycle's 47 entries are dated 08-29, then near-silence (2 on 08-30, 1 on 09-01, 5 on 09-02, 3 on 09-03, 6 on 09-04, 0 after). Crons re-verified against the registry this run: all nine restored after the 09-03 pause/resume wipe (#359 ×2).

## Summary
- Drift-scan issues consumed: **none — but the scan is healthy.** The lane fired green on schedule 2026-09-04 14:09 UTC (Actions run 33882151272, 7m08s); its Linear issues were unreadable because this session's Linear MCP is unauthenticated (impediment #974, logged this run). Last cycle's lane-silence watch item resolves: the "missed" 08-28 run actually fired late the same day at 23:23 UTC. The lane has now fired every week since 08-07.
- Impediments logged this cycle (entries dated 08-29 → 09-06): **47 entries / 49 recorded occurrences, ~213 min (~3.6 h) heuristic loss** — roughly a fifth of last cycle's volume, consistent with the lanes running ~1.5 of the 9 days. Deterministic draft via `npm run retro-draft`, re-binned by date; draft deleted after commit per THR-1056.
- Top categories by entries: verification 11, tooling 7, sandbox 5, environment 3, linear-mcp 3, ticket-authoring 3, build-tooling 3. Impact: 2 × L (#940, #943), 16 × M, 29 × S (occurrence-weighted), 0 × Blocked.
- Improvements implemented this session: **5 quick wins** (verification-gates de-flake-owes-re-runs rule, pull-work claim-time browser-verify route, CLAUDE.md close-keyword-on-claimed-tickets ban, registry cron-wipe recurrence note, testing-patterns organic-baseline-n≈1 rule) plus one memory update and impediment #974.
- Improvements backlogged: _Filed in Step 9._
- **Previous retro's backlog: 5 of 5 Done within two days.** THR-1324 (prose remediation sweep, `6dc47f32`), THR-1325 (resume-liveness + hand-created-In-Dev, `aee0b3b3`), THR-1326 (stub producer forensics, `1373d460` — verdict: *nothing was deleting `node_modules`; the precheck's green was the false signal*), THR-1327 (status page, PR #1743), THR-1328 (de-flake the three named closeout tests, `bb2a7078` — two of them misdiagnosed). Four-retro streak of backlog-Done-within-days continues.

## Drift-Scan Signals This Cycle

The scan ran green 09-04 (run 33882151272) but its per-signal Linear issues could not be read: this session's Linear MCP requires authentication that a non-interactive scheduled session cannot perform, and `LINEAR_API_KEY` is unset (#974). So the signals are **produced but unconsumed** this cycle — a consumption failure, not a lane failure. Signal health: the lane itself is demonstrably alive (five consecutive weekly green runs, 08-07 → 09-04). The standing counts (THR-911/912/913/915/747) are one week stale from the retro's point of view.

## Impediment Analytics

Re-binned to entries dated 2026-08-29 → 09-06: 47 entries / 49 occurrences / ~213 min.

### By Category
| Category | Entries |
|---|---:|
| verification | 11 |
| tooling | 7 |
| sandbox | 5 |
| environment | 3 |
| linear-mcp | 3 |
| ticket-authoring | 3 |
| build-tooling | 3 |
| coordination / harness / testing / ci | 2 each |
| git / process / engine-content / engine-perf | 1 each |

### By Impact
| Impact | Occurrences | Ids of note |
|---|---:|---|
| L | 2 | #940 (two DIRTY armed PRs, 7.6 h + 4.6 h, both In-Dev slots frozen), #943 (a ticket's "measured, not guessed" root cause was guessed — cost most of a run to falsify) |
| M | 16 | #929 ×2, #941, #944, #952, #955, #359 ×2, #964, #968, #969 |
| S | 31 | — |

### Root-cause clusters
| Cluster | Volume | Exemplars | Disposition |
|---|---|---|---|
| **CI gate-budget decay + stale verdicts** — the cycle's headline, ~15 h aggregate PR stall in one morning | 6 entries (#929 ×2, #940 L, #941, #944, #951) | `debugTickBatch` 180s budget outgrown in silence (documented "~18s", measured 44.9s on main); two unrelated PRs red on the identical timeout; THR-1352 fixed the constant same day — and its named victim #1717 stayed red another 1.6 h because nothing re-runs a held PR after a gate fix lands | Constants fixed in-cycle (THR-1352, PR #1723). The residual process gap — **a de-flake does not unblock its own victims** — is **quick win 1**. #951 (job-timeout ring one out) recorded, single occurrence |
| Claim-integrity, round three | 2 entries (#942, #955 M) | Hand-created In-Dev tickets invisible to the WIP gate (#942 — fixed same day by THR-1325's claim-comment arbitration); a *sweep* PR wrote a close keyword for a ticket another session had claimed, auto-closing it out from under the builder (~30 min duplicated) | THR-1325 landed 08-29 and covers the hand-created shape. The sweep-scope overlap is new → **quick win 3** (CLAUDE.md close-keyword ban on claimed tickets) |
| Ticket-premise defects caught by executor falsification | 4 entries, 1 × L | #935 (accessor-grep declared a live producer dead), #936 (motivating measurement stale between filing and pickup), #943 (L), #949 (`--include=*.ts` sweep hid the files the scope note reasoned about) | Watch — standing trigger unchanged (first *landed* wrong remedy converts to a gate proposal). Catch rate still 100%, but #943's cost (most of a run) is the class's worst yet |
| Empty-population / n≈1-organic-baseline probes | 3 entries (#959, #969, #971) | Guards resting on 1–3 organic observations flip on any world-shifting change and read as its regression (~1 h misdirected diagnosis on THR-1349); two worldgen entrypoints silently produce empty populations (no rivers/lakes; zero threaded agents) | **Quick win 5** (testing-patterns rule). The two named tests' repairs (#959: seed the route / seed the steeped lair) belong with THR-1349's cutover work — noted for its executor, not a new ticket |
| Build-tooling silent no-ops | 3 entries (#952 M, #958, #966) | `validate-model`/`generate-vault` entry guards never fired once bundled on Windows — the validator was green while validating **nothing** (fixed in-run; the three sibling scripts carry a working `.mjs` arm, verified this retro); UL dashboard status parser drops annotated `**Status:**` values, 7 → 10 terms and growing | #952's class swept and clean. UL parser → **Backlog T-1** (the one ticket this cycle that clears the bar) |
| Mapped `cant-fix` tallies | ~9 entries | python probe hangs (#933, #957, #965 — fifth recorded), `preview_start` unattended refusals (#927, #946, #972), worktree stub (#930, #931, #934 — repair rote, THR-1326's producer verdict landed) | #946's specific edge — the route-decision clause never reached the pickup step, third recurrence — is **quick win 2**. Rest documented, no new information |
| Desktop-app cron wipe on pause/resume | 1 entry ×2 (#359 M) | Second occurrence (2026-09-03): four crons reset to generic defaults, orchestrator fired on top of the executor, briefing ran pre-work | External `cant-fix` upstream; registry note strengthened → **quick win 4**. Crons verified restored this run |
| Linear MCP semantics | 3 entries (#932, #950, #963) | `&` in a name-resolved `project` silently drops the field (entity-encoded rejected, literal accepted); `updatedAt` bumps on mere cross-reference so it is not an activity signal | Memory updated (`reference_linear_save_issue_field_drops` — read-side sibling added). No ticket: no shipped loss, workarounds held |

Unresolved blockers (Workaround Found = No): none this cycle.

## Cross-Signal Patterns

1. **Gate fixes don't propagate to their victims.** The cycle's largest cost (~15 h stall across #940/#941/#944) had three compounding layers: a budget documented at ~18s decayed to 45s+ in silence; `main` read green while docs-only merges skipped the suite for seven hours; and the eventual fix left its own named victim red because auto-merge evaluates the *stored* rollup. Each layer already has its rule row; the missing one — the fix owes its victims a re-run — is now in verification-gates.md.
2. **The claim primitive keeps being bypassed from new directions.** Hand-created In-Dev (fixed, THR-1325), park-shaped decay (fixed, THR-1283), and now a sweep ticket whose scope subsumes a claimed ticket and closes it via keyword. The common shape: every fix so far guards the *claiming* side; #955 is the first breach from the *closing* side, which is why the new rule lives next to the THR-738 keyword discipline rather than in pull-work.
3. **The vacuity discipline has found its mirror image.** After two cycles of killing guards that pass on empty populations, this cycle's failures are guards that *fail on noise* — organic baselines of 1–3 observations. Same defect (population too small to carry a verdict), opposite polarity. The testing-patterns section now states both.
4. **Consumption, not production, is the fragile half of the scan loop.** The drift scan has fired green five weeks running; what broke this cycle is the retro's ability to read it (Linear auth) — and last cycle's "missed week" verdict was also a reading error (the run fired late, not never). Verdicts about lane health should come from `gh run list`, which this retro now demonstrates.
5. **Previous retro's fixes held under fire.** THR-1325's claim-comment arbitration was exercised by #942 the very day it landed; THR-1328's de-flake removed the three named closeout tests from this cycle's rows entirely; THR-1326 closed the stub-producer question (nothing deletes `node_modules` — the precheck's green was false, now abstaining). The stub tallies continue as contained ~2-min repairs, as predicted.

## Guidance Audit

Doctrine versions this retro (from `Docs/guidance-manifest.json`): **prose@2**, **encounter-authoring@1**, **identity-vision@1**, **cosmology@1**, **rules-of-play@1**, **ui-laws@1**, **architecture@1**, **coordination-process@1**.

Ran: **no.** Prose is unchanged at @2 since the 08-28 retro's audit (its first firing), and its remediation sweep THR-1324 landed 08-29 (`6dc47f32`) — the ten drifted surfaces the audit named are disposed. The seven other doctrines are *new registrations* (08-28 → 08-30, context-cleanup rounds 1–5), each born from its own round's audit-and-sweep; a re-audit within a week of registration would re-read surfaces the registering audit just read. Interpretation recorded deliberately: **registration is a baseline, not a version movement.** Monthly cadence next binds: prose ~2026-09-28, the seven registrations ~2026-09-28 → 09-30. The next retro that sees any version bump — or the ~09-28 retro regardless — runs `/guidance-audit`.

## Improvements Made This Session

1. **`Docs/canon/verification-gates.md` § CI — a fix to a gate owes its held PRs a re-run** (#929/#940/#941/#944, ~15 h): the de-flaking run merges `origin/main` into (or `gh run rerun`s) every PR the old gate held red, plus the stale-verdict detection recipe (failing run's `createdAt` vs the test file's last commit on main). ROI 3×4/1 = **12**.
2. **`.claude/skills/pull-work/SKILL.md` Step 3 — the browser-verify route is decided at claim, named in the claim comment** (#946, third recurrence of a written clause not being read at the moment it applies): the rule now sits where the session already is at claim time. ROI 4×2/1 = **8**.
3. **CLAUDE.md § Definition of Done — never write a close keyword for an issue that is In Dev with an assignee** (#955): comment naming your PR instead; the claiming session closes its own ticket. ROI 2×4/1 = **8**.
4. **`Docs/ops/scheduled-tasks-registry.md` — the cron-wipe check is mandatory, not precautionary** (#359 ×2): recurrence recorded, this run's nine-cron re-verification noted. ROI 2×3/1 = **6**.
5. **`.claude/skills/testing-patterns/SKILL.md` § vacuous verification — the n≈1-organic-baseline mirror image** (#959/#969/#971): ask "how many observations is the baseline, and what is the second-smallest?"; construct organic populations deterministically; verify the entrypoint produces the population at all. ROI 2×3/1 = **6**.

Session-local: `reference_linear_save_issue_field_drops` memory extended with #963's read-side fact (`updatedAt` is row-modified, not activity); impediment #974 logged (this run's Linear-auth gap + catch-up fire).

## Improvements Backlogged

**None filed this cycle — filing was impossible, not skipped.** The session's Linear MCP is unauthenticated (#974), so per the retrospective skill's own rule ("an unfiled ticket is recoverable; a ticket citing a phantom source is not") the one qualifying ticket is carried here as a complete draft instead.

**Drafted this cycle (T-1), full body below — could not be filed from this session (Linear MCP unauthenticated, #974).** Any Linear-capable session files it verbatim; the next retro's Rule-5 pass checks that it happened.

> **Title:** UL dashboard generator: status parser drops annotated `**Status:**` values — 10 canonical terms render status-less on `?view=ul`
> **Body:** `generate-ul-dashboard`'s status parser exact-matches bare enum values only. `**Status:** canonical` parses; `**Status:** canonical (seated 2026-09-03, THR-1390)` emits `Unrecognized status value` **plus** a spurious `No **Status:** line found`, and the term renders status-less. Growth: 7 affected terms (impediment #958, 2026-08-30) → 10 (#966, 2026-09-03), and two new THR-1390 terms nearly joined. The annotated form is *better authoring* — it records when and why a term was seated — and currently loses to the parser, forcing the convention backwards ("bare status, provenance in the body"). Fix: match the leading status word, keep the parenthetical as a rendered note, and make the annotated form the recommended one; the footer's prescribed recount has the same shape and gets the same treatment. Membership predicate: any UL shard `**Status:**` line whose value *begins with* a canonical status word parses to that status. Costs ~30 min to fix; not fixing costs a degraded authoring convention plus warning noise that will bury the next genuine `missing_status`. Evidence: impediments #958, #966; report `Design/retros/retro-2026-09-06.md`.
> **Labels/project:** Improvement, Content Architecture (or Continuous Improvement — filer's judgment). Priority Low.
> **First comment (coordination block):** Suggested model: sonnet · Parallel-safe with: all content-table work (sole surfaces: `scripts/generate-ul-dashboard-data.ts` + regenerated `src/data/ul-dashboard.generated.json`) · Mutex with: none — no open ticket edits the generator.

Deliberately **not** filed: the CI-budget family (constants fixed in-cycle by THR-1352; process gap quick-won), #952's entry-guard class (fixed in-run, siblings verified clean this retro), the two n≈1 test repairs from #959 (they belong to THR-1349's executor — seed the trade route, seed the steeped lair — noted here for pickup), the python/preview_start/stub tallies (mapped `cant-fix`, no new information), every single-occurrence quirk (#939, #948, #951, #961, #962, #964, #967 — #967 already has THR-1415, #968 has THR-1412), and the Linear MCP semantics rows (memory-documented, no shipped loss).

## Step 5b — Systemic Wiring Guide Audit

The THR-1301 undertaking-motivations cutover landed 09-04 and the guide already documents the new holding ops (`grantHolding`/`transferHolding` with the ownership-resolved `control:claim`/`control:seize` variants) — the flag the 08-28 retro left ("when THR-1301 cuts over, the guide owes that chapter") is discharged; two wiring-guide edits landed in-cycle (`72366a42`, `e194040b`). No undocumented capability found this pass. #952's structural lesson recorded above stands for the guide too: hand-maintained surfaces need their gates falsified, not trusted.

## Tuning Recommendations

1. **No drift-scan threshold changes** — the signals were produced but unconsumed (#974); tuning on unread data would be guessing. Carry-over: the retro should always verify lane health via `gh run list --workflow=drift-scan.yml` before any silence verdict (two consecutive cycles now show "silence" was a reading artifact).
2. **Step 5c — intent-judge kill-criteria aggregation** (`Docs/judge-metrics/`, header-keyed parse; **23 rows**, up 1 from 08-28):

   | Criterion | Value | Verdict |
   |---|---|---|
   | ≥95% Allow with zero overrides | Allow 78.3% (18/23) | Below the floor — **not** rubber-stamping |
   | ≥50% override rate on Revise/Block | 5 Revise/Block rows carry `Overridden`; **0 overridden → 0%** | **Measurable for the first time** — far below the kill threshold, n=5 still small |
   | Median time-to-judgment >300s | 121 s | Pass |
   | Zero marginal catches | — | User judgment, not row-derived |

   **Verdict: keep intent-judge.** The override criterion finally computes and reads healthy: every Revise stood. Escalation rate remains 0.0% against the declared 0.05 floor — still a tuning observation only.
3. **The retro lane's Linear dependency needs a durable answer** (#974): either the Linear connector gets authenticated for the desktop-app scheduled-session context, or the lane gets an API-key route. Until then every weekly retro silently degrades to report-only fallback for Step 0 and Step 9. This is a Christian action (see below), not a repo change.

## Patterns to Watch

- **Unfiled T-1** — the UL-parser ticket is drafted above; next retro's Rule-5 pass verifies it was filed and either files it (if Linear works by then) or escalates the auth gap a second time.
- **Resume catch-up bunching** — on host resume, three lanes fired within one second of each other, which is exactly the Linear-MCP-contention shape #359's consequence column describes. If contention rows appear after the next pause/resume, the harness's catch-up behavior (not the crons) is the producer.
- **Ticket-premise class at its worst-yet cost** (#943, L) — standing trigger unchanged (first landed wrong remedy ⇒ gate proposal), but the class is trending costlier while remaining pre-damage.
- **`check:generated-freshness` job-budget headroom** (#951) — the freshness gate's headless child died at the job timeout once; a second occurrence makes it a dated-budget ticket per #941's rule.
- **Guidance audit monthly window opens ~2026-09-28** — eight doctrines come due together; that retro should budget for it or split the partitions across two runs.

## Needs Christian

- **Authenticate the Linear connector for scheduled desktop-app sessions** (or provide `LINEAR_API_KEY` to the retro lane): this run could neither read the drift scan's Linear issues nor file its one backlog ticket (impediment #974). Everything else routed around it; this is the only piece an agent cannot fix.
