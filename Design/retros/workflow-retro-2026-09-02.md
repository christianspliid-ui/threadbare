# Workflow Retro — 2026-09-02

Covers 2026-08-26T09:00Z → 2026-09-02T09:13Z. Compares against [`workflow-retro-2026-08-26.md`](https://github.com/christianspliid-ui/threadbare/blob/main/Design/retros/workflow-retro-2026-08-26.md).

## Needs Christian

**Nothing new, and one thing you can stop worrying about.**

The one ask that matters is already the lead item on [your briefing](https://github.com/christianspliid-ui/threadbare/blob/ops/Design/briefing.md) and I am not going to restate it in worse words: two design tickets sit unworked at 14 and 18 days, and either sitting one or saying *"drop them back to the queue"* clears the jam.

What I will add is this: **I have now filed the agent-side half of that problem, so it stops being solely yours.** [THR-1382](https://linear.app/threadbare/issue/THR-1382/a-dead-in-design-item-consumes-the-design-staging-budget-forever) makes the design-staging limit count *live* work instead of *occupied slots*, so a ticket nobody is working stops blocking the ones behind it on its own. That does not make your two tickets go away — they are still real design work someone has to do — but it means the machine no longer grinds to a halt waiting for you to notice.

**And the two silent days were not a failure.** The machine was off from Sunday morning to Tuesday evening; Windows' own power log says so and every lane agrees. Under your 8 August ruling that overnight quiet is normal, this needs nothing from you. I checked because two day-long silences in two weeks is the shape a dying lane makes, and this is not that.

## Throughput

- **92 issues reached Done**, against 73 / 111 / 86 the three prior weeks. **122 pull requests merged, 334 commits** on `main`.
- **Completions per day (UTC):** `08-26: 16 · 08-27: 6 · 08-28: 27 · 08-29: 38 · 08-30: 3 · 08-31: 0 · 09-01: 0 · 09-02: 2`.
- **A ~58-hour machine-off gap sits inside the window, and it is benign.** Last commit before it: `main` 2026-08-30T07:03Z, `ops` 06:56Z. First after: `ops` 2026-09-01T17:09Z. **2026-08-31 — a Monday — produced zero commits on both branches, from every lane including the hourly briefing.** Unlike last week's equivalent, this one is *explained*: the machine was asleep 08-30 09:03 → 09-01 19:24 local, established independently by two lanes from Windows' power log and from every scheduled lane's last-run time clustering in the minutes after the wake. **This also retro-answers last week's open question about the 08-21 blackout** — same shape, same cause class, and it is covered by Christian's 2026-08-08 ruling that overnight quiet is normal. Not a defect, and deliberately not filed as one.
- **Corrected for the gap, the rate rose again.** 92 completions over ~4.6 active days is **≈20/day**, against last week's ≈17.8/day and the week before's 15.9/day. Three consecutive weeks of rising active-day throughput.
- **Run outcomes: 63 orchestrator runs, 39 promotions.** Distribution: 39 runs promoted 0, 16 promoted 1, 5 promoted 2, 1 promoted 3, 2 promoted 5. Every run published a report; no run exited silently. Zero orchestrator runs on 08-31 and 09-01 (machine off).
- **Queue depth: reached zero on 08-30 and stayed there through 09-01.** This is the second consecutive week the shelf has bottomed out, and this time it did not recover on its own — [backlog grooming](https://github.com/christianspliid-ui/threadbare/blob/ops/Docs/ops/backlog-grooming-2026-09-01.md) had to release a dead claim ~24 hours ahead of the stale-claim sweep's own deadline purely to put *one* item back, naming the alternative as *"~24 further hourly pickup slots that find an empty board."* Depth at the time of writing: **1** (plus THR-1382, filed by this retro).
- **Composition: ~66% product.** Of the 92, 42 landed in Continuous Improvement, 30 in Thematic Pressure & Living World, 8 in Content Architecture, 1 in UI Visual Overhaul, 11 unprojected. The Continuous Improvement share is high but is *not* the throttle failing — 33 of those 42 are the five context-cleanup rounds (THR-1353…THR-1375), a Christian-approved program that closed on 08-29, not lanes filing process tickets. No scheduled lane filed a process ticket this week.

## Findings filed

**[THR-1382](https://linear.app/threadbare/issue/THR-1382/a-dead-in-design-item-consumes-the-design-staging-budget-forever)** — *A dead In-Design item consumes the design-staging budget forever — nothing times out the column the way the stale-claim sweep times out In Dev.* High, `Ready for Dev`, Continuous Improvement.

`ORCH_MAX_IN_DESIGN` is `1`, and `In Design` has held two motionless items for the entire window — [THR-1002](https://linear.app/threadbare/issue/THR-1002) (unassigned, 14 days) and [THR-790](https://linear.app/threadbare/issue/THR-790) (assigned to Christian, 18 days). The bound counts **occupancy, not liveness**, and the stale-claim sweep is hard-scoped to `In Dev` (`scripts/stale-claim-sweep/index.ts:115`), so nothing in the machine watches `In Design` at all. Two things made this filable where last week it was not:

1. **Last week's retro set its own expiry and it triggered.** It declined to file on the grounds that the lane was correctly restraining itself, but wrote: *"If the shelf is still starved next week with the slot still held, that reasoning expires and it should be filed."* The shelf hit zero, the slot is still held, and it is now 14 days rather than 7.
2. **A second-order cost surfaced that is purely agent-side.** [THR-1349](https://linear.app/threadbare/issue/THR-1349) has now consumed **three** executor passes (PRs [#1724](https://github.com/christianspliid-ui/threadbare/pull/1724), [#1758](https://github.com/christianspliid-ui/threadbare/pull/1758), [#1760](https://github.com/christianspliid-ui/threadbare/pull/1760)). Each pass correctly found that the remaining work is a design fork and moved it to `Todo` *"so tb-orchestrator T2 can re-scope"*. T2 is barred, so it never gets re-scoped; the shelf then empties, grooming re-promotes it for want of anything else, and the next executor rediscovers the same thing. Checkpoint #3 states it *"should not be re-promoted as written"* — and starvation pressure re-promoted it anyway. **That is a closed loop the machine cannot exit on its own**, and it is what moved this from an impediment row to a ticket.

The lanes' own numbers, quoted rather than derived: *"Eleventh consecutive run barred at that bound"* (T2, 08-29), then *"jammed for twenty-one runs straight"* ([orchestrator 08-30](https://github.com/christianspliid-ui/threadbare/blob/ops/Docs/ops/orchestrator-2026-08-30.md)), and still barred at [09-02 run b](https://github.com/christianspliid-ui/threadbare/blob/ops/Docs/ops/orchestrator-2026-09-02b.md) with THR-1300 and THR-1381 both stacked behind it.

The ticket is deliberately scoped to **counting, not mutation**: exclude `Parked` and stale-unassigned items from the bound, and add a *warn-only* `In Design` staleness comment. Its coordination block names the trap that would otherwise sink it — in `In Dev` a null assignee means "deliberate park, leave alone" (THR-1283), while in `In Design` it means "dead stage, stop counting". The two states invert the meaning of the same field, so reusing the `In Dev` release path would destroy exactly the items the bound should be ignoring.

## Clean checks

- **Ship mechanics: PASS, and perfectly clean.** **80 distinct line-anchored `Fixes|Closes|Resolves THR-NNN` closers across 334 commits, and every single one landed on an issue that is Done.** Zero false-closes; no THR-540 pattern. The 12 Done issues with no closing commit were audited individually: **11 are `wayfinder:*` decision tickets** (THR-1276–1282, 1288–1291) that resolve by recorded verdict rather than code — the pattern last week established as legitimate — and the 12th is THR-1369, the context-cleanup round-5 umbrella, whose `completedAt` (22:31:49Z) matches its final child's PR [#1754](https://github.com/christianspliid-ui/threadbare/pull/1754) merge to the second. An umbrella closing on its last child is bookkeeping, not an executor hand-closing shipped code.
- **Autoclose: PASS.** 200 `linear-autoclose` runs sampled (100 `push`, 100 `pull_request`), **100% success, zero failures.** Combined with last week's 160, that is 360 consecutive green runs; impediment #692's push-path failures remain settled rather than merely quiet.
- **CI: PASS.** 250 `ci.yml` runs sampled (the fetch cap; the true window count is higher) — 174 success, 18 cancelled, 8 failure. **All 8 failures are on feature branches, none on a `main` push**, and two pairs are the same branch failing twice then landing (`thr-1344-genome-reach-pass`, `priceless-elgamal-708860`) — ordinary red-then-fix iteration, not a broken gate. The 08-29 timeout-budget raise (20 → 35 min) appears to have held.
- **Open-PR backlog: zero.** Third consecutive week with no open PR at all. The distinguish-conflict-from-red-from-hold check is vacuous again and is reported as such, not as a pass.
- **WIP and claim discipline: PASS.** `In Dev` holds four: one live claim ([THR-1298](https://linear.app/threadbare/issue/THR-1298), claimed 09-01T17:30Z and actively shipping slices 1–5 through this morning) and three correctly-shaped parks (THR-1130, THR-1133, THR-1168) each carrying `Parked` with a null assignee, which frees the WIP slot rather than consuming it. No double-assignment, and **no executor set `state:"Done"` by hand.** The one dead claim (THR-1349, stranded by the machine sleep) was caught by the stale-claim sweep, warned at 09-01T16:27Z — about three hours after the machine woke — and released by grooming with its full reasoning. The mechanism worked.
- **Handoff quality: PASS, with an unusually good worked example.** [THR-1299's handoff](https://linear.app/threadbare/issue/THR-1299) carries the plan-doc path *and* its merged PR link *and* a liveness verification, the gate verdicts (intent-judge Allow after an initial Revise; three forked audits), full three-pillar action items across Engine/Content/UI/Wiring, two explicit veto invitations for Christian, and all three coordination lines with reasons per THR-688 rule B. **No handoff was bounced by `/pull-work` for a missing block.** The one pickup hold this week was the *correct* behaviour, not a bounce: the lane claimed THR-1299, found its stated mutex with THR-1298 verifiably still applicable, released it back to the queue unclaimed, and wrote down why — refusing the THR-688 rule B reversal because the reason was live rather than stale.
- **Checkpoint hygiene: PASS.** Every unfinished pass posted a checkpoint naming what shipped, what remains, the branch, and the resume point. THR-1349 is the one issue with 3+ checkpoints and no ship — and **the split has already been recommended and its seams named**, by checkpoint #3 itself: (1) throughput recovery under a live board, a design/balance question and the actual blocker; (2) the cutover, one commit, blocked on 1; (3) test-guard fragility, routed to impediment #959 rather than a ticket. I have no split to add. That the recommendation cannot be *acted on* is not a hygiene failure — it is the THR-1382 finding above.

## Handoffs to the Friday retro

- **~160 impediment rows this week** (ids ~800 → 959, against 800 at last retro). Pattern analysis across that volume is Friday's; I read only the rows touching lane mechanics.
- **Impediment #959 is pre-batched for you by name.** THR-1349's checkpoint #3 logged it deliberately rather than filing it — *"a guard that fails on noise is the vacuity guard's mirror — both are one defect, a population too small to carry a verdict"* — and routed it here. Two acceptance tests (`edgeIntegrity`, `lairClearing`) are vacuity guards on organically produced world content whose populations are 1–3 items.
- **The drift-scan lane missed a second consecutive Friday.** Last Friday's retro set the escalation rule explicitly: *"if next Friday's scan is also absent, the retro should read the lane's own run reports/logs before anything else."* 08-28 was silent, and I saw no `drift-scan` issue touched this window either. That is now two misses and the diagnosis is owed. Note the confound before concluding lane death: the machine was off for 58 hours mid-window, though **not** on either Friday.
- **The stale-claim sweep counts machine-off hours as staleness.** It warned THR-1349 on the basis of "no activity since 2026-08-30T06:40Z" when ~58 of those hours were a powered-down machine. The verdict happened to be right (that session really was dead) so I did not file it, but the sweep is measuring wall-clock rather than lane-uptime, and a future case could release a genuinely live claim that simply spanned a sleep.
- **The lane-silence probe's pause marker holds an expired window from 3 August**, so it reads red against every benign sleep. The briefing itself names it as outstanding agent-side tidying. Below the materiality bar alone; worth a row if it is still there next month.
- **Worktree accumulation is now re-measured and it has roughly doubled: `git worktree list` reports 200 registrations** (199 before this retro added its own) against **211 directories** on disk in `.claude/worktrees/` — so ~12 directories are unregistered orphans. Compare the 102 flagged on 08-19 and left unre-measured on 08-26. That is roughly +97 in two weeks, and it still carries the THR-797 retargeting hazard.

## Notes

- **Window boundary checked, not assumed.** Last week's retro declared its window end as 08-26T09:00Z but did not land on `main` until 09:26Z (`0890092f`). Two issues completed inside that 26-minute shadow — THR-1278 at 09:16Z and THR-1277 at 09:20Z — and because last week counted to 09:00Z and this window starts there, both are counted exactly once, here. Had I taken the *publication* time as the boundary instead of the declared one, both would have been dropped by both retros. I also passed an explicit `Z` suffix to every `git log --since` after last week's note that git reads bare timestamps in local time (+0200) while Linear's `completedAt` is UTC — the trap that produced two phantom false-closes last week did not recur.
- **A measurement trap I hit: `gh run list` caps at 250 and the window is bigger than that.** The CI numbers above are a floor, not a census; the autoclose numbers hit the same cap. I report them as sampled rather than presenting a truncated count as a total.
- **Judgment call — I filed the design jam this week having declined it last week, and the reversal is deliberate.** Last week's reasoning was sound: the lane was restraining itself correctly, filing against correct restraint is the accretion failure CLAUDE.md warns about, and the binding constraint was Christian's hours rather than the bound. What changed is that a purely agent-side cost appeared — THR-1349's re-scoping loop — which no amount of Christian's attention fixes and which the machine cannot exit on its own. The ticket is scoped to *that*, and says in its own text that it asks Christian for nothing.
- **Judgment call — I did not file the 58-hour gap, and this one was easy.** Two independent lanes reached the same reading from Windows' power log, it is covered by a standing director ruling, and filing a ticket against a machine being switched off would be exactly the "hardening against failures that have not happened" the throttle excludes. I verified the claim rather than accepting it, because a lane asserting "not a defect" about its own silence is the reading I would most want to be wrong about.
- **Judgment call — the 66% product share.** A naive read of "42 of 92 completions landed in Continuous Improvement" says the process-work throttle failed. It did not: 33 of those are the five context-cleanup rounds, a program Christian approved and which closed on 08-29. Counting an approved program as process-work bloat would be the metric eating the rule.
- **No queue mutation performed.** This retro claimed nothing, moved nothing, and closed nothing. THR-1382 was created in `Ready for Dev`, verified by `get_issue` re-query (impediment #48), confirmed **born unassigned** — THR-845's create-path fix still holds — with `blockedBy` empty, and its coordination block posted as the first comment per THR-836.
