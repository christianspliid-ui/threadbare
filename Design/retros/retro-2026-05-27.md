# Workflow Retrospective — 2026-05-27

**Period covered:** 2026-05-21 → 2026-05-27 (7 days; clean weekly cadence from the 2026-05-20 retro)
**Days since last retro:** 7 (previous retro `Design/retros/retro-2026-05-20.md`, items through #138 + #137-5th-recurrence)
**Scope:** Status check on the three consented experiments and five Christian-asks from the 2026-05-20 retro, plus genuinely-new impediment activity, plus a design-council pass on next-week actions, plus a workflow-velocity scan against the conspicuous 10-day quiet stretch in the impediment log.

> **Sources scanned.** `Docs/impediments.md` (current to #138, last new entry 2026-05-15, last #137 recurrence 2026-05-16), `Docs/project-status.md` (Updated 2026-05-25), `Docs/project-history.md` (most-recent code ship 2026-05-15 THR-75), `Docs/changelog.md` (most-recent row 2026-05-25 docs hygiene), `Design/user-actions.md` (Last updated 2026-04-27 — now 30 days stale), `Design/retros/retro-2026-05-20.md`, `Design/impediment-dashboard.html` (still `DATA` ends at #78, `LAST_RETRO.date` still `2026-04-23`).

> **Linear-access caveat (carried from 2026-05-20).** This `weekly-workflow-retro` run cannot reach Linear MCP from the scheduled context — `plugin:productivity:linear` exposes only `authenticate` / `complete_authentication`, no list/read tools. **Shipping and state status below is reconstructed from repo artifacts only.** Where Linear is the only authority (THR-434, THR-425, THR-442 current state), the report says "not visible in repo artifacts" rather than asserting. This was Christian-Ask #5 from the 2026-05-20 retro and has not been resolved — see Ask #4 below.

> **Data-quality note (carried forward, SEVENTH retro running).** The `Design/impediment-dashboard.html` `DATA` array still ends at #78 — now ~60 entries behind `Docs/impediments.md`. THR-434 (Ready for Codex per the 2026-05-14 retro) has been the dashboard regen ticket for six retros; the 2026-05-20 retro called for a priority bump or explicit accept-and-note. No evidence either landed. The retro continues to read `Docs/impediments.md` as source of truth and treats the dashboard as a stale visual.

---

## Top 3 impediments this week

1. **Twelve-day silence in shipping and impediment streams — interpretation uncertain.** No code ship has hit `project-history.md` since THR-75 on 2026-05-15. No new impediment entry has been logged since #138 (2026-05-15) and no #137 recurrence since 2026-05-16. The only `changelog.md` row in the period is a 2026-05-25 docs-hygiene correction. Three readings are live: (a) genuine quiet — Christian on vacation / week off, executor queues drained, no friction to log; (b) silent automation failure — `keep-work-flowing` and the CC hourly poller stopped firing and we are not seeing the gaps; (c) work is in flight on long branches not yet merged. From repo artifacts alone these are indistinguishable. The 2026-05-20 retro's recurring slip pattern was "outside-repo / interactive-only items don't move on scheduled time" — twelve days of nothing visible is consistent with that pattern, but also consistent with (b). *Next step:* see Consented Experiment #1 — a 60-second automation-status check the next interactive sitting must run, plus a methodology note added to this SKILL.md so future retros bias toward verifying (b) before claiming (a).

2. **Three 2026-05-20 consented experiments — no shipping evidence; three Christian-asks — no resolution evidence.** Experiment #1 (promote THR-442 + re-purge outside-repo brief), Experiment #2 (path-guardrail + pre-commit-sequence note in `pull-work` SKILL.md), and Experiment #3 (bump THR-434 priority / own-or-accept THR-425) all should have shipped as small Cowork-or-CC edits in the past 7 days. None appear in `project-history.md` or `changelog.md`. Christian-asks #1–#5 from the 2026-05-20 retro are status-unknown from repo artifacts. Pattern is identical to the 2026-05-14→2026-05-20 carryover the last retro flagged: *"the same ask as last retro and the retro before."* This is now the third consecutive retro carrying the same set of small unblockers. *Next step:* see Consented Experiment #2 — collapse the carryovers into a single dated user-actions refresh + bias toward filing every experiment as a Linear issue rather than scoping any as "Cowork inline" (the failure mode the 2026-05-14 retro corrected).

3. **`Design/user-actions.md` is 30 days stale and disagrees with `impediments.md`.** The file's "Last updated" is 2026-04-27. The 2026-05-20 retro added five new Christian-asks; user-actions.md still shows the original April triage. Item #1 (Obsidian MCP decision) is the only consistent carryover; items #3 (orphan uncommitted changes, 9 days at last update — now 39) and the rest are stale. The 2026-05-20 retro noted "user-actions.md (which is itself ~24 days stale and worth a rebuild on retro day)" — the rebuild did not happen. *Next step:* see Consented Experiment #3 — rebuild user-actions.md from the current impediments.md + 2026-05-20 + 2026-05-27 retro asks in this retro's run, since it is a docs-only Cowork edit that does not require Linear access.

---

## Status of active impediments

New activity since the 2026-05-20 retro plus carryovers whose status may have changed. Ages relative to 2026-05-27. Stale (>7 days, no movement) marked ⚠.

| # | Title | Age (days) | Owner | Status | Next action |
|---|-------|-----------:|-------|--------|-------------|
| — | **Zero new impediments logged 2026-05-17 → 2026-05-27 (10 days)** | 0 | Cowork | New methodology gap — period of unusual silence | Verify executor automations still firing (Consented Experiment #1). |
| **135–137 cluster** | `keep-work-flowing` session-handoff / Slack stale brief — 5 recurrences thru 2026-05-16, then quiet | 11 ⚠ | Christian | Open — last logged recurrence 2026-05-16; 10-day silence may indicate Ask #1 from 2026-05-20 landed, OR `keep-work-flowing` simply hasn't fired | Verify in next interactive sitting: is the brief edited? Is `keep-work-flowing` still firing? |
| **137 (worktree path variant)** | Scheduled-task Edit/Write paths land in wrong worktree | 12 ⚠ | Cowork | Open — Consented Experiment #2 from 2026-05-20 retro called for SKILL.md edit; no shipping evidence | Folded into Consented Experiment #2 below (same SKILL edit; renew the ask). |
| **138** | Pre-commit hook drops M files after defensive failure → needs `--amend` | 12 ⚠ | Cowork | Open — Consented Experiment #2 from 2026-05-20 retro called for SKILL.md edit; no shipping evidence | Same as above — bundled into Consented Experiment #2. |
| 132 | Skill-sync hook false positive *after merge* on Windows | 16 ⚠ | Cowork | THR-420 covers root cause but #132 verification still pending | One fresh-worktree verification pass on a post-merge `check:skill-sync` run; file follow-up only if it still false-positives. |
| 133 | `create_scheduled_task` blocked inside scheduled-task sessions | 15 ⚠ | Christian | THR-417 deferral filed; status unknown from repo artifacts | Batch with #137 cluster — register `monthly-rulebook-review` + `weekly-retro` in one interactive sitting. |
| 131 / 134 / 135 / 137 | `session-handoff` skill missing | 11–15 ⚠ | Christian | In-repo half closed (THR-422/443/444); outside-repo brief status unknown | Folded into #137 cluster row above. |
| 125 | Skill-sync drift root-cause `core.autocrlf` | 16 ⚠ | — | THR-420 shipped 2026-05-12 (`.gitattributes`); fresh-worktree confirmation still pending per #132 | Same as #132 — one verification pass. |
| 114 / 121 / 122 / 127 | Cross-executor collisions (5 in two weeks) | 19–22 ⚠ | — | THR-421 shipped 2026-05-12 (resume-path included). 21 days quiet since #127 — close-cluster window opens. | Confirm no 6th recurrence in the next retro; close formally. |
| 112 | `flush-plan-docs` branch-fallback didn't push / open PR | 23 ⚠ | — | THR-423 shipped 2026-05-12, PR #270 | Close in dashboard regen (THR-434, still stale). |
| 126 | Stranded closeout commits (zombie branches) | 19 ⚠ | — | THR-424 shipped 2026-05-12, PR #273 | Close in dashboard regen (THR-434, still stale). |
| 108 | Linear MCP rate-limit (HTTP 429) | 27 ⚠ | Cowork | THR-425 filed `Ready for Dev` Low priority 2026-05-14; no shipping evidence since (now 13 days) | Bump priority to Medium (Consented Experiment #4) or explicitly accept slow-track. |
| 94 | Required status check hung indefinitely; required checks removed | 30 ⚠ | TBD | Branch protection rolled back; structural carryover | Own as test-suite stabilization project, not a small experiment. |
| 111 | `weekly-retro` task never registered (interactive-only) | 23 ⚠ | Christian | Open — interactive constraint | Batch with #133 + #137 cluster (one interactive sitting). |
| 106 | Linear MCP exposes no `create-comment` method | 20 ⚠ | Cowork | Workaround in place | Platform-level — file with Linear MCP maintainer. |
| 75 | Obsidian MCP unreachable | 33 ⚠ | Christian | Partial — filesystem fallback in vault skills | Decide: auto-start plugin, or formally accept fallback and close (Ask #3 below). |
| 59 | 8+ orphan uncommitted changes in working trees | 33 ⚠ | Christian | Open — mitigated by THR-277 worktree isolation | Triage in one sitting; attribute each change group or discard. |
| — (this retro) | `Design/user-actions.md` is 30 days stale; out-of-sync with current impediments and asks | 30 ⚠ | Cowork | New — rebuild in this retro's run (Consented Experiment #3) | Rebuild from current impediments + 2026-05-20 + 2026-05-27 asks. Docs-only, no Linear access needed. |
| — (this retro) | `impediment-dashboard.html` still ends at #78; `LAST_RETRO.date` still `2026-04-23` | 34 ⚠ | Codex | THR-434 carries this; status unknown from repo artifacts | Consented Experiment #4 — escalate THR-434 or accept-and-note in the dashboard footer. |

---

## What shipped since last retro

Per `project-history.md` + `changelog.md`:

- **Docs hygiene (2026-05-25)** — `project-status.md` updated to correct stale THR-360 status; THR-360 was listed both as queued and as shipped in the same file. Internal contradiction resolved. *This is the only `changelog.md` row in the 7-day period.*

**Not visible in repo artifacts (would need Linear MCP to confirm):**
- **THR-434** (dashboard regen, filed 2026-05-14 Ready for Codex with full handoff). Still no shipping evidence in repo artifacts; sixth retro carrying the carryover.
- **THR-425** (Linear poller stagger, filed 2026-05-14 Low priority). Still no shipping evidence; now 13 days at Low.
- **THR-442** (session-handoff skill missing + Slack MCP read-only). Idea-state at the 2026-05-20 retro; called for promotion to Ready for Dev. Status unknown.
- **Consented Experiments #1–#3 from 2026-05-20 retro.** None visible in `project-history.md` or `changelog.md`.

**Net (with explicit hedge):** the period has **one** docs-hygiene row, **zero confirmed** feature ships, and **zero confirmed** workflow-improvement ships. By repo artifacts alone, this is the slowest 7-day window since the 2026-05-04 retro period — and the 2026-05-04 retro at least had explanations (`weekly-retro` scheduled task blocked, no executor pickup). This period has no logged explanation because nothing was logged. That is the new finding.

---

## What slipped

1. **All three 2026-05-20 Consented Experiments** — no repo-artifact evidence any landed:
   - *Experiment #1* (promote THR-442 + re-purge outside-repo `keep-work-flowing` brief): both parts are user-facing. Part A is Cowork-pullable; if it ran the Linear state transition would not show in repo artifacts (impediment #106 — no create-comment). Part B is Christian-owned outside-repo.
   - *Experiment #2* (path-guardrail + pre-commit-sequence note in `pull-work` SKILL.md): single-file Cowork edit. Should be visible as a `.claude/skills/pull-work/SKILL.md` change in `changelog.md` — it is not.
   - *Experiment #3* (THR-434 priority bump or accept-and-note): Linear-side change; status unknown.
2. **All five Christian-asks from 2026-05-20** — no resolution evidence:
   - *Ask #1* (edit outside-repo `keep-work-flowing` brief): the 10-day #137 silence is consistent with this having landed, but also consistent with `keep-work-flowing` having stopped firing. Cannot disambiguate.
   - *Ask #2* (confirm 2026-05-14 Experiment #0 shipped to outside-repo `weekly-workflow-retro` SKILL): this run still cannot run a Linear all-states cross-check from the scheduled context, so Experiment #0 either didn't land or didn't help here. The check that motivated it remains absent from this retro.
   - *Ask #3* (register `weekly-retro` + `monthly-rulebook-review` scheduled tasks): blocked by impediment #133; requires interactive CC session.
   - *Ask #4* (decide Obsidian MCP path #75): 33 days stale now.
   - *Ask #5* (decide Linear MCP access from scheduled retro context): unresolved — this run is the proof.
3. **`Design/user-actions.md` rebuild** that the 2026-05-20 retro explicitly called for ("user-actions.md (which is itself ~24 days stale and worth a rebuild on retro day)"). Didn't happen. Now 30 days stale. *This retro will fix it inline — see Consented Experiment #3.*

**Slip pattern (carried from 2026-05-14 → 2026-05-20 → here, now three retros).** The 2026-05-14 retro corrected the slip narrative from "Cowork's mechanical-edit follow-through is not high" to "experiments scoped as 'inline' or living outside the repo don't ship." The 2026-05-20 retro carried the same pattern. This retro confirms it for a third consecutive week. **The fix is structural, not motivational** — every retro experiment must become a filed Linear issue with an executor handoff, and outside-repo asks need a non-retro surface (the `Design/user-actions.md` rebuild is the operationalization of this). See Consented Experiment #3.

---

## Workflow observations

- **The 10-day impediment silence is the single most surprising signal of the period** and the report's biggest interpretive risk. The previous longest streak in 2026 was ~5 days. Three causes are plausible; the retro cannot distinguish them from repo artifacts. The methodology gap is not "we don't have Linear MCP" — it is "we don't have an executor heartbeat." Any future scheduled retro needs to check whether `keep-work-flowing` and the hourly CC pickup automation are still firing before concluding "quiet period." Right now this retro is one log file away from confidently mis-telling the story.
- **Zero confirmed ships in 12 days vs. 50+ in the early-May window.** This is a velocity inflection. Either the executor lanes have nothing to pull (queue exhaustion) — which would be a *good* problem implying the recent Now-state projects have closed out — or the lanes have stopped pulling. Both are worth checking. `project-status.md` still names active phases under Encounter Experience (F-phase integration + Phase H), Social Systems Expansion (Phase 2 of THR-75 filed as Deferral), and Continuous Improvement — so queue exhaustion is unlikely. That makes pull-side silence the more likely story, and the executor-status check the more important next step.
- **The 2026-05-20 retro's design council had three consented experiments queued; none visibly shipped.** This is the most direct measurement of the "retro→issue→ship" pipeline since the 2026-05-12 batch's 5/6 same-day success. The pattern reversed: 5/6 → 0/3 over two cycles. The reversal needs naming, even if the cause is "everyone took the week off." Especially if the cause is "everyone took the week off" — a retro that doesn't notice a week-off and keeps demanding action is the wrong retro.
- **The retro's own observability is degrading retro-over-retro.** 2026-05-14 ran a mid-run Linear scan and self-corrected. 2026-05-20 lost Linear access and explicitly hedged. 2026-05-27 (this run) cannot even disambiguate "experiments slipped" from "everyone was OOO" without Linear. Without Linear MCP or a separate executor heartbeat, the scheduled retro will keep losing fidelity.
- **`user-actions.md` being 30 days stale is a self-inflicted wound.** The file was designed (2026-04-27 seed) to be the non-retro surface for Christian asks specifically because retro prose buries asks. Letting it go 30 days stale defeats its own design intent. Fixing it inline this run is cheap and the right operationalization of the "outside-repo asks need a non-retro surface" finding from the last two retros.
- **The dashboard carryover is now a *symbol* not a problem — sixth retro running.** The 2026-05-20 retro proposed two paths (escalate THR-434 or accept-and-note). Neither happened. This retro stops apologizing for the dashboard and joins the 2026-05-20 retro in recommending an explicit decision next interactive sitting.

---

## Design-council pass — consented experiments (small, reversible)

Four voices: **Ops**, **Engineer**, **PM**, **Tech Lead**. Council ran against this period's signals.

**Council discussion (condensed).**
*Ops:* "We're at a fork. Either everyone's on vacation and this retro is screaming into a quiet room, or `keep-work-flowing` and CC pickup stopped firing and we've been blind for ten days. A 60-second status check in the next interactive sitting tells us which. That has to be experiment #1."
*Engineer:* "Three SKILL-edit experiments from last retro didn't ship. The pattern is the same one the 2026-05-14 retro already named — anything not filed as a Linear issue with an executor handoff doesn't move. So this retro files them. Not 'Cowork inline,' actually filed."
*PM:* "We are also losing the *retro's own utility* by letting asks stack in retro prose. The 2026-04-27 seed of `user-actions.md` was supposed to be the non-retro surface — it's been ignored for 30 days. Cowork can rebuild it inline this run from the impediment log and the 2026-05-20 retro asks. That's a docs-only edit, doesn't need Linear, and operationalizes the last two retros' findings."
*Tech Lead:* "Last thing — the methodology gap matters more every retro. Without Linear access or an executor heartbeat, the next retro will be even further from ground truth. Add an automation-status probe step to the retro SKILL.md outside-repo (asking Christian to apply it) AND a one-line 'last-fired-at' marker the executor automations write somewhere we can read. The marker is the cheaper half."
*Consensus:* one verification (E1), one user-actions rebuild done in-line this run (E3 — Cowork does it now), and two small filings (E2, E4). All small, all reversible, all batched into a single Christian sitting where Christian-action is needed.

**Consented actions:**

**1. Verify executor automation status in the next interactive sitting (~60 seconds).** Single command-pass. Christian or an interactive CC session runs (a) `list_scheduled_tasks` to confirm `keep-work-flowing` and `weekly-workflow-retro` and any other relevant tasks are still scheduled; (b) inspects each task's `nextRunAt` to confirm it hasn't fallen out of schedule; (c) checks the most recent task-run log for a successful exit. *Outcome:* either the 10-day silence is "everyone was OOO" (close gracefully) or "automation stopped firing" (restart and investigate). *Reversibility:* n/a (verification only). *Owner:* Christian. *Closes:* the interpretive ambiguity that dominates this retro.

**2. File the two 2026-05-20 carryover Cowork SKILL.md edits as actual Linear issues this run.** Stop scoping them as "Cowork inline." Concretely: (a) draft a Linear issue for the `pull-work` SKILL.md two-part edit (path-guardrail under "before Edit/Write" + pre-commit-sequence note under "pre-commit sequence" — text already exact in the 2026-05-20 retro Consented Experiment #2); apply the `plan-pending-commit` label and move to Ready for Dev with a coordination block. (b) Same for the `.agents/skills/session-handoff/SKILL.md` stdout-fallback ship (Experiment #1 Part A from 2026-05-20). Both are mechanical edits an executor can pull and ship in <30 minutes each. *Reversibility:* delete the Linear issue. *Owner:* Cowork (would have filed in this run, but the scheduled context cannot reach Linear MCP — see Christian-Ask #1). *Closes:* #137 (worktree-path variant), #138, in-repo half of #137 cluster.

**3. Rebuild `Design/user-actions.md` inline in this retro's run.** Docs-only Cowork edit, no Linear access needed, can land now. Replace the 2026-04-27 seed with current top-of-mind items derived from this retro + 2026-05-20 asks + open impediments with Christian-owned next actions. Keep the file's existing structure (top = highest blast radius; "Fix" / "What breaks" / "Source" per item). Mark the file `Last updated: 2026-05-27`. *Reversibility:* revert the file. *Owner:* Cowork — done in this same run (see the rebuild as a separate write below this retro). *Closes:* the 30-day staleness on the non-retro Christian-ask surface; operationalizes the "outside-repo asks need a non-retro surface" finding from the prior two retros.

**4. File THR-434 priority + THR-425 priority as one batched docs/Linear pass.** Pick one path for each: THR-434 either gets a High priority + assignee bump, or the impediment-dashboard.html footer adds a one-line "regen tracked by THR-434; running against impediments.md as source of truth." Same shape for THR-425 (bump to Medium or explicitly accept the slow-track and stop noting it every retro). *Reversibility:* re-set priority / revert the footer edit. *Owner:* Cowork in the next interactive Linear-reachable session, OR Christian if Cowork doesn't get Linear access by next retro. *Closes:* the long-tail bookkeeping debt; stops the retro from re-quoting the same two items every cycle.

**Not picked as experiments.**
- *CI test-suite stabilization (#94):* still a project, not a small experiment.
- *Linear MCP `create-comment` (#106):* platform-level.
- *Obsidian MCP path decision (#75):* Christian-owned binary decision; lives on user-actions.md (Ask #3).

---

## Ask of the user (Christian)

Ranked by blast radius. **Items 1–4 batch into one ~10-minute interactive (non-scheduled) sitting.** Item 5 is a yes/no decision.

1. **Run the 60-second executor-automation status check** (Consented Experiment #1 above) — `list_scheduled_tasks` + inspect `nextRunAt` for `keep-work-flowing`, `weekly-workflow-retro`, the hourly CC pickup automation, and any others. *This is the single most important action this week* — without it the next retro is flying blind on whether the 10-day silence is healthy or alarming.

2. **Authorize Cowork to file the two 2026-05-20 carryover Linear issues** (Consented Experiment #2 above) — or do it yourself in the same interactive sitting. The text for both is already in the 2026-05-20 retro; this run cannot reach Linear from the scheduled context to file them directly.

3. **Decide the THR-434 + THR-425 priority pair** (Consented Experiment #4). THR-434 = dashboard regen (now seventh retro carrying it). THR-425 = Linear poller stagger (now 13 days at Low priority). Both are bookkeeping debt; both will keep being quoted in every retro until decided.

4. **Confirm the Obsidian MCP path** (#75, 33 days stale). Auto-start plugin OR accept filesystem fallback formally. Carried unchanged from 2026-05-20 (and 2026-05-14 before it).

5. **Decide the Linear-MCP-from-scheduled-context question** (carried from 2026-05-20 Ask #5). Without it the retro's degradation curve continues. Either authorize a token-based Linear read from the scheduled context, or commit to the explicit "needs-Linear-verification" hedge pattern as the new normal. Either is workable; the silent gap is not.

> **Lower-urgency carryovers, workarounds in place:** triage 8+ orphan uncommitted changes (#59, 33 days), GitHub Pro upgrade decision for branch-protection enforcement (#56-derived). Both restated on the rebuilt `Design/user-actions.md` written in this same run.

---

## Summary — three things to act on this week

1. **Run the 60-second executor-automation status check.** The 10-day impediment silence cannot be interpreted from repo artifacts alone. The next retro's confidence depends on this. Without it, "we had a quiet week" and "automation died" look identical.
2. **File the two 2026-05-20 carryover SKILL.md edits as actual Linear issues**, not as "Cowork inline" work. The retro→issue→ship pipeline works fast when used; the failure mode is items that bypass it. Three retros running have called for this same shape — this is the cycle to actually do it.
3. **Refresh `Design/user-actions.md` (done inline in this retro's run).** Operationalizes the "outside-repo asks need a non-retro surface" finding from the last two retros. The retro should not be the only channel for Christian asks; the user-actions doc was the design answer and letting it go 30 days stale defeats its own purpose.

---

*Retro run 2026-05-27 via the `weekly-workflow-retro` scheduled task. Autonomous run — no user present. Linear MCP not reachable from this scheduled context; prior-experiment and current-state status reconstructed from `project-history.md` + `changelog.md` + `project-status.md` and hedged explicitly where Linear is the only authority. No Linear issues filed this run (the scheduled context cannot reach the MCP) — the four consented experiments above are queued for Cowork or Christian action in the next Linear-reachable session. `Design/user-actions.md` rebuild done inline in the same run as a docs-only Cowork edit. Inputs as listed in the opening Sources block.*
