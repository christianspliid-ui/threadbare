# Retrospective — 2026-08-28

## Period
From: 2026-08-22 (exclusive; last retro)
To: 2026-08-28

**Cycle note:** the implementation lanes resumed after the 08-19 pause and ran at full density all week — this is the highest-volume cycle on record (209 entries in 7 days), driven by the Proactive Agent Actions wave (THR-1276 map → THR-1292/1296/1297 plan docs → THR-1305–1323 deferral drain) and the Doctrine v2 content campaign (THR-1221/1223/1224).

## Summary
- Drift-scan issues consumed: **none this cycle.** The scan last fired 2026-08-21 (~14:39 UTC) and its five issues were consumed by the 08-22 retro; as of this retro's start (15:14 UTC today) the expected ~14:39 UTC Friday run had not fired and no `drift-scan` issue was touched. First missed week — watch item, not yet a dead-lane verdict.
- Impediments logged this cycle (entries dated 08-22 → 08-28): **209 entries / 260 recorded occurrences, ~675 min (~11.3 h) heuristic loss.** Caveat: the `node_modules`-stub rows log *cumulative running tallies* (…"×8", "×9", "×10"), so the 260 occurrence sum overcounts that class; the true stub count is ~31 arrivals (see § Cross-Signal Patterns). Deterministic draft via `npm run retro-draft`, re-binned by date; draft deleted after commit per THR-1056.
- Top categories by entries: tooling 49, verification 46, sandbox 43, process 37, environment 14 (but 59 occurrences — the stub tallies). Impact: 6 × L, 43 × M, 160 × S, 0 × Blocked.
- **Guidance audit ran (Step 5d, first triggered run) and caught real drift**: 10 non-CURRENT surfaces across skills/Docs/vault three days after the Doctrine v2 conversion sweep, including 4 of 5 `prose@2` stamps over-claiming. See § Guidance Audit.
- Improvements implemented this session: **4 quick wins** (CLAUDE.md tree-diffing rule, wiring-guide `spawn_artifact` fix, pull-work DIRTY-discharge predicate, guidance-manifest correction).
- Improvements backlogged: _Filed in Step 9._
- **Previous retro's backlog: 2 of 2 Done same day** (THR-1191 and THR-1192 both filed and merged 2026-08-22), and neither failure shape (#619 wiki two-dot, #689 CRLF-vs-blob) recurred this cycle. The mid-week workflow-retro additionally filed and landed THR-1283 (park-safe stale-claim sweep, Done 08-26), which covers four of this cycle's process rows (#703, #706, #752, #755).

## Drift-Scan Signals This Cycle

No scan output. The lane fired on schedule 08-21 and was consumed last retro; today's expected run had not fired by 15:14 UTC (last week's observed fire: ~14:39 UTC). One missed week after a healthy run is lane-latency territory, not signal death — scheduled-lane silence can also be a deliberate token-limit pause. **Signal health: indeterminate this week; if next Friday's scan is also absent, the retro should read the lane's own run reports/logs before anything else.** The standing counts (THR-911/912/913/915/747) are therefore one week stale; no basis to claim movement or stability.

## Impediment Analytics

Re-binned to entries dated 2026-08-22 → 08-28: 209 entries / 260 recorded occurrences / ~675 min.

### By Category
| Category | Entries | Occurrences |
|---|---:|---:|
| tooling | 49 | 53 |
| verification | 46 | 46 |
| sandbox | 43 | 43 |
| process | 37 | 37 |
| environment | 14 | 59* |
| testing | 6 | 7 |
| design | 5 | 5 |
| content | 4 | 5 |
| skill-gap / engine | 2+2 | 2+2 |
| documentation | 1 | 1 |

*Inflated by cumulative tally rows — see the Summary caveat.

### By Impact
| Impact | Entries |
|---|---:|
| L | 6 (#697, #706, #722, #763, #770, #832) |
| M | 43 |
| S | 160 |

### Root-cause clusters
| Cluster | Volume | Exemplars | Disposition |
|---|---|---|---|
| Fresh-worktree `node_modules` stub (`.vite`-only or plain-missing) | **~31 arrivals in 7 days** — the watch item from 08-22 fired at 10× its ≥3/week trigger | #694, #705, #712 ×4, #734, #745, #750, #753, #766, #773, #775, #778, #783, #790–#901 tally family; **plus this retro's own worktree, which arrived with no `node_modules` at all** | THR-1111/1115 were read per the 08-22 instruction: they scoped the **home-tree donor wiper**, not this producer — the stub class was never in their scope. **Backlog T-3** (producer forensics + precheck coherence, #790's concrete fix) |
| Claim-integrity chain: armed PR ends attention, nothing inherits it | 4 entries, 1 × L | #743 (lane self-overlap near-miss), #755 (live claim stripped twice), #763 (**THR-1245 implemented twice concurrently — ~1 full duplicated session**), #765 (DIRTY armed PR silently stalled 4.5 h) | Park half fixed by THR-1283 (Done 08-26). DIRTY-discharge predicate → **quick win 3**. Remainder (resume liveness, In-Dev-created-ticket hole) → **Backlog T-2** |
| Vacuous guards / fixture-invents-both-sides / controlled-arm self-checks | ~35 verification entries | #714, #715, #821, #832 (L), #845, #854, #872, #876, #878, #890, #897, #903 | Discipline holding — catches at 100%, several arms now catching their *own* vacuousness. No gate proposal clears the bar; the memory/doc corpus on this class is doing its job |
| Ticket/plan premise defects caught by executor falsification | ~14 entries, 1 × L | #707, #720, #731, #744, #770 (L — wrong composition point), #782 (scope 4× low), #792, #806, #829, #835, #842, #848, #881 | Watch (standing trigger unchanged: first *landed* wrong remedy converts this to a gate proposal) |
| Shell-mediated write corruption + python probe hangs | ~19 entries | #698, #709, #721, #772, #788, #791, #815, #836, #838, #855, #861, #874, #884, #886, #889, #892 | `cant-fix` class, documented; the one new fact — **python availability varies per session and a bare probe hangs 2 min** (#838/#847/#874 settle the contradiction) — recorded in session memory. #791/#796: five corrupted rows survive in the log on `main`; repair deferred (both prior programmatic repair attempts corrupted other rows, #668) |
| `preview_start` refused in unattended runs | ~9 entries | #713, #717, #749, #771, #802, #882, #887 | Mapped `cant-fix`; jsdom substitution route holding. No new information this cycle |
| Closeout full-suite false reds — three *named* recurring tests | 6 entries | #779, #819, #869 (impediment-id-allocation 5 s timeout, 4th+ occurrence), #871 (coastline 500 ms perf, 41 min blocked armed PR), #756 (worktree-write-guard 9/9 red on every local Windows run) | Triage protocol absorbing at ~3.5 min each, but recurrence now clears the bar → **Backlog T-5** |
| Gate defects (single-occurrence each) | ~12 entries | #728, #751, #777 (L-adjacent, 4.5 h stranded PR), #785, #786, #787, #810, #813, #858, #859, #877, #904 | #777 → **quick win 1**; #858 → **Backlog T-4**; rest below bar individually, logged |

Unresolved blockers (Workaround Found = No): #858 (recorded, not worked around — the page still generates) and the #791/#796 corrupted log rows. Nothing session-blocking.

## Cross-Signal Patterns

1. **The stub class industrialized without being understood.** ~31 stub/missing `node_modules` arrivals in 7 days — including this retro's own worktree. The repair is rote (~2 min, documented, works first try), lanes have converged on one-line tally rows, and the class is thereby *contained* — but nobody knows the producer. The 08-22 watch item asked this retro to read THR-1111's diff before filing: done — THR-1111/1115 named and repaired the **home-tree donor wiper**; the fresh-worktree stub producer was explicitly not in scope. Sharper edge: the session-precheck's `test:` line reports green against a stub tree (#694, #712, #790), so the one signal a session reads first actively misleads it.
2. **Arming a PR ends the session's attention, and nothing inherits it.** Four rows, one L: a DIRTY armed PR reads as shipped from every surface (#765), a hand-created `In Dev` ticket skips the claim that is the mutual-exclusion primitive and two lanes build it simultaneously (#763), an hourly lane can outlast its own interval and its successor "resumes" the live branch (#743). THR-1283 fixed the park-shaped half mid-week; the discharge predicate is quick-won here; the rest is T-2. The common fix shape across all of them: **re-read the shared state at the point of commitment, never act on the orientation snapshot.**
3. **A conversion sweep is bounded by its list, and the list was short.** The guidance audit's headline: THR-1252's sweep held on the surfaces it named, missed identical guidance three bullets up in the same file (Tonal Bible P1–P3), and never saw the unlisted surfaces at all — `Vision/taste-profile.md` still mandates retired picturable-anchor prose to every design session, and `template-context-rewrite` still teaches the retired scene-first workflow as its entire Pass 2. The manifest is now corrected (quick win 4) so the next sweep's list is honest; the remediation itself is T-1.
4. **The verification-discipline corpus is compounding.** 46 verification entries and almost every one cites the memory/doc pattern it instantiates — controlled arms now catch their own non-application (#872, #878), their own fixture vacuousness (#897), and their own revert leaks (#711, #876). The cost per catch is minutes, the catches are pre-damage, and no landed wrong remedy occurred this cycle. This is the system working; no new machinery wanted.
5. **Previous retro's fixes held.** THR-1191 and THR-1192 shipped same-day and their failure shapes produced zero new rows. THR-1283's park fix landed mid-week with the same result. The three-retro streak of backlogged-tickets-Done-within-a-day is worth naming as a healthy baseline.

## Guidance Audit

**Doctrine versions this retro: `prose@2`** (versionNote: narrator mode, landed 2026-08-25 via THR-1250/1251/1252). Previous retro (08-22) predates the manifest — no recorded baseline, and the doctrine version landed after it.

**Ran: yes** — version moved since the last retro (the Step 5d trigger's first firing). Method per the `guidance-audit` skill: authorities read live, three partitions (skills / Docs / vault) audited in parallel by independent subagents, quotes mandatory on every non-CURRENT verdict. `check:guidance-freshness` itself reads OK (advisory burn-in until 2026-09-08, THR-1256) — which is exactly the mechanical-vs-semantic split the skill predicts: no authority was edited without its sweep, *and* ten surfaces still teach the retired mode.

**Ranked pollution list (blast radius, worst first):**

1. **`vault:Vision/taste-profile.md`** — DRIFTED. Loaded at the start of every design pass by its own contract; its load-bearing "quality bar" entry mandates *"Every sentence carries a picturable anchor. If the reader cannot see it, rewrite it"* — retired by name in Doctrine v2. The THR-1247 vision audit amended a different entry on this page the same day and walked past this one.
2. **`.claude/skills/encounter-pipeline/SKILL.md`** — INTERNALLY-CONTRADICTORY. The "communication pivot" orientation paragraph still teaches *"one short flavor quote"* as card-face anatomy; the same file's REVISE trigger 16 bans it. Every factory run reads the pivot paragraph first. One-phrase fix.
3. **`.claude/skills/template-encounter-rewrite/SKILL.md`** — INTERNALLY-CONTRADICTORY twice: the What-You-Author field table instructs authors to write the retired `fiction` field (its own gate 0i bans it 150 lines later), and editorial checklist Q3 teaches show-don't-tell as live PASS/FAIL — with a model answer (*"Something shifts in the way {target} holds {their} shoulders…"*) that opens on the vagueness lexicon's flagship word and fails the file's own detector.
4. **`.claude/skills/prose-content-systems/SKILL.md`** — INTERNALLY-CONTRADICTORY. READ-FIRST quality-check item 3 teaches the retired "moment, not label" bar with an in-situ camera-work exemplar (*"Their eyes met."*), three lines above the paragraph retiring the workflow that produced it. Also its foreshadowing-clause exemplar (*"Something about it…"* marked ✅) vs. the evasive-vagueness-at-zero rule — and the foreshadowing surface's written-from-inside-the-head framing has no recorded narrator-mode exemption: needs a ruling, not a silent assumption.
5. **`Docs/plans/2026-07-30-nudge-card-repertoire.md`** — DRIFTED, linked without caveat from live canon (`Docs/canon/encounters.md` → "Repertoire progression") with its engine ticket unshipped, so the next executor reads *"Flavor quote | One short generic line … the card's only prose"* and the retired 14-question checklist as the contract.
6. **`Docs/plans/2026-07-04-voice-register-plainspoken-malazan.md`** — DRIFTED with `status: current`: its peak-surface list still grants *"encounter climax steps … major aftermath beats"* — the exact grants Doctrine v2 revoked.
7. **`vault:Systems/Tonal Bible.md`** — INTERNALLY-CONTRADICTORY. THR-1252's banner and P4/P5 amendments landed; the identical prose-tone bullets on Blend Principles 1–3 are un-bannered and P1 teaches outright in-situ interiority (*"a moment of exhaustion, a flicker of doubt, a memory of mortality surfacing unbidden"*); the header still says "Read this first before creating any content."
8. **`.claude/skills/template-context-rewrite/SKILL.md`** — DRIFTED, structural, and outside the manifest until today: its entire Pass 2 is titled and built on *"Scene-first fragment drafting — write the paragraph first, then extract the fragment"*, the named mechanism by which in-situ prose got in; plus the retired one-earned-concrete-detail anchor rule.
9. **`Docs/plans/2026-05-04-encounter-experience-design-plan.md`** — INTERNALLY-CONTRADICTORY: carries v2 supersession markers on some sections while §3.2/§4.1/§5.3 hand an unmarked, fully-specified retired choice-card + authored-factor contract to anyone landing mid-file under a header still reading "Implementation contract for the executor"; the canon page's claim that §3.4 is marked in-file is false as of this read.
10. Tail: `Systems/Content Creator Cheat Sheet.md` line 366 (stale routing line directly under its own correct tombstone), `prose-pipeline/SKILL.md` aesthetic bullets ("irony over sentimentality", "one vivid detail"), `nudge-model-encounter-system.md` settled ruling 3 (per-encounter card prose — reversed by the THR-883 pivot, no marker).

**Stamps:** 4 of the 5 `prose@2`-stamped skill files carry the contradictions above — the stamp is over-claiming on everything except `prose-vignettes-and-enrichment`. Clean and stampable per the auditors' full reads: `attachment-pipeline/SKILL.md` (after a ruling on its peak carve-out for artifact lore, which is outside the doctrine's enumerated peak list), plus the three agent prompt files (no frontmatter surface). The doctrine-cleanest files in the whole tree are the two factory agent prompts — the surfaces that do the actual writing — which is why shipped batches have been clean while the orientation/checklist layers around them drifted.

**Manifest gaps — corrected this run (audit Step 6, quick win 4):** added as `dependents`: `template-context-rewrite/SKILL.md`, `attachment-pipeline/SKILL.md` + its editorial prompt, `game-design-direction.md`, `voice-register-plainspoken-malazan.md`, `nudge-model-encounter-system.md`, `nudge-card-repertoire.md`, `2026-05-04-encounter-experience-design-plan.md`; added as `manualDependents`: `vault:Vision/taste-profile.md`, `vault:Systems/Content Strategy.md`. `Docs/authoring-brief.md` is deliberately *not* added: it is generated, and its sources (now all in the manifest) cover it transitively via `check:authoring-brief`. Verified: `check:guidance-freshness` runs green over the widened manifest.

**Remediation is Backlog T-1** — the audit reports, the retro promotes, and editing ten surfaces is beyond quick-win scope. **Sunset renewal:** the guidance-audit skill, the manifest, and the gate are renewed this cycle by these catches (named drift found before further batches shipped against it). Kill-criteria check: "two consecutive audits find nothing" — inapplicable; this one found ten.

## Previous Retro's Backlog (Rule 5)

| Issue | Status |
|---|---|
| [THR-1191](https://linear.app/threadbare/issue/THR-1191) wiki-freshness two-dot diff | Done (2026-08-22, same day, PR #1570) — zero #619-shaped rows since |
| [THR-1192](https://linear.app/threadbare/issue/THR-1192) generated-freshness EOL normalization | Done (2026-08-22, same day, PR #1575) — zero #689-shaped rows since |

Watch items from 08-22: stub arrivals post-THR-1111 → trigger fired at ~31/week, THR-1111 diff read, producer confirmed out of its scope → T-3 filed ✓. Judge `Overridden` adoption → 4 new rows carry the column ✓ (see Tuning). T-1/T-2 gate fixes → both merged, no recurrence ✓.

## Improvements Made This Session

1. **CLAUDE.md § Testing — the tree-diffing general rule now names its fourth source and closes the merge loophole** (#777, ~4.5 h stranded PR): `Docs/plans/2026-04-16-systemic-wiring-guide.md` ← "Update systemic wiring guide" (feeds gate 3b through `build-authoring-brief`'s sha1-pinned provenance), and the explicit clause that **a `git merge origin/main` is itself a tree-changing edit** — "last action before push" is literal. ROI 4×3/1 = **12**.
2. **`Docs/plans/2026-04-16-systemic-wiring-guide.md` § Spawn Artifact — dead field names replaced with the live type's** (#739; Step 5b): `artifactName`/`artifactSubtype`/`possessedByAgentId`/`chronicleEntry` → `templateId`/`category`/`tier`/`nameOverride`/`targetAgentId`/`messageOverride`/`tags`/`when`, with a pointer naming `src/types/unifiedAction.ts` as the authority. `Docs/authoring-brief.md` regenerated in the same commit. ROI 3×3/1 = **9**.
3. **`.claude/skills/pull-work/SKILL.md` Step 1.5 — a `DIRTY` PR no longer discharges a claim** (#765): the WIP gate now reads `mergeStateStatus` when resolving claims to PRs and routes a DIRTY-carried claim to Step 1.7 resume, converting a silent indefinite stall into a normal pickup. `last_validated_against` bumped. ROI 3×3/1 = **9**.
4. **`Docs/guidance-manifest.json` corrected** per the audit's manifest-gap findings (8 new dependents, 2 new manualDependents — details in § Guidance Audit). Effort 1; this is the write that makes the change-time gate stop lying by omission.

Also session-local: the agent memory note on python availability rewritten (#838/#847/#874 — availability varies per session; a bare `python` probe hangs the full 2-min timeout; never probe, use Node `.mjs`).

## Improvements Backlogged

- [THR-1324](https://linear.app/threadbare/issue/THR-1324/prose-doctrine-v2-remediation-sweep-10-operative-surfaces-still-teach) — Prose Doctrine v2 remediation sweep: the 10 drifted surfaces from § Guidance Audit, with stamp corrections (impediment #910; ROI 4×3/1 = 12). Todo, Medium, coordination block as first comment; recommended to land before Retrofit Batch 2 (THR-1222).
- [THR-1325](https://linear.app/threadbare/issue/THR-1325/pull-works-claim-predicates-miss-two-live-states-a-lane-resumes-a-live) — pull-work resume-liveness + the hand-created-In-Dev hole (impediments #743, #763 L; ROI 3×4/1 = 12). Todo, High. Companion to THR-1283; the DIRTY-discharge half was quick-won in this retro's own PR.
- [THR-1326](https://linear.app/threadbare/issue/THR-1326/fresh-worktree-node-modules-stub-find-the-producer-31-arrivalsweek-and) — stub producer forensics + precheck `test:`-line abstention (~31 arrivals/week, #909 family; ROI 5×2/2 = 5). Todo, Medium. Fulfils the 08-22 watch-item instruction.
- [THR-1327](https://linear.app/threadbare/issue/THR-1327/generate-project-status-renders-1-of-281-fragments-the-60-line-cap-is) — project-status page renders 1 of 281 fragments; cap smaller than any entry (#858; ROI 3×2/1 = 6). Todo, Low.
- [THR-1328](https://linear.app/threadbare/issue/THR-1328/de-flake-the-three-named-closeout-tests-impediment-id-allocation-5s) — de-flake the three named closeout tests (#779/#819/#869, #871, #756; ROI 3×2/1 = 6). Todo, Low.

Deliberately **not** filed: the heredoc/python shell-corruption class (harness `cant-fix`; the new fact — availability varies, bare probe hangs — recorded in session memory), the `preview_start` unattended refusals (mapped `cant-fix`, no new information), the #791/#796 corrupted log rows on `main` (both prior programmatic repair attempts corrupted other rows, #668 — leave for a hand-verified pass), every single-occurrence gate quirk (#728, #751, #785, #786, #787, #810, #813, #859, #877, #904 — below the bar, logged), and any new gate for the ticket-premise-defect class (catch rate still 100%; the standing landed-wrong-remedy trigger unchanged).

## Step 5b — Systemic Wiring Guide Audit

The `spawn_artifact` drift (#739) is fixed above. One flag, not yet a gap: the Proactive Agent Actions wave landed new strategic operations this week (`create_location` in THR-1308, `grantHolding`/`transferHolding` in THR-1297, `create_group` pending in THR-1309) that the guide's Part 2 Capability 5 does not mention — deliberately unflagged as a defect for now, because the wave's content-facing surface (undertaking `motivations`, THR-1301) is still pending and the ops are not yet reachable from authored content. When THR-1301 cuts over, the guide owes that chapter; the systemic-wiring-guide closeout step on that ticket should catch it. Worth restating #739's structural lesson: the guide is hand-maintained and nothing detects its drift from the types — when next touched substantively, diff its documented effect shapes against `src/types/unifiedAction.ts` wholesale.

## Tuning Recommendations

1. **No drift-scan threshold changes.** The lane's silence today is the observation to carry, not a tuning input: one missed Friday after a healthy 08-21 run. If next Friday is also silent, diagnose the lane (its own run logs first) before reading anything into flat signals.
2. **Step 5c — intent-judge kill-criteria aggregation** (`Docs/judge-metrics/`, header-keyed parse; **22 rows**, up 6 from 08-22):

   | Criterion | Value | Verdict |
   |---|---|---|
   | ≥95% Allow with zero overrides | Allow 77.3% (17/22) | Below the floor — **not** rubber-stamping |
   | ≥50% override rate on Revise/Block | 4 rows now carry `Overridden` | **Prospectively measurable at last** — too few rows for a rate; compute next cycle |
   | Median time-to-judgment >300s | 120 s | Pass |
   | Zero marginal catches | — | User judgment, not row-derived |

   **Verdict: keep intent-judge.** The 08-22 quick win (mandatory `Overridden` column) took: new rows carry it. Escalation rate remains 0.0% against the skill's declared 0.05 floor — still a tuning observation, not a kill criterion.
3. **Guidance audit cadence stands** (version-bump or monthly). The one calibration note from its first run: the *stamps* were applied during the conversion sweep to files whose contradictions the sweep itself missed — a stamp should only ever come from a full-file read, which is what the audit skill already says. No mechanism change; the audit's existence is the check on stamp honesty.

## Patterns to Watch

- **Drift-scan lane silence** — first missed Friday. Next retro: if absent again, read the lane's run reports before the impediment log.
- **Stub producer forensics (T-3)** — until it lands, expect the tally rows to continue; they are contained cost (~2 min each), not decay.
- **Judge override rate** — 4 rows carry the column; next cycle it becomes a real rate on Revise/Block rows.
- **Retrofit Batch 2 (THR-1222, High, Todo) vs. T-1 remediation** — the factory's draft/editorial prompts are doctrine-clean, so batches are not blocked; but the orientation/checklist drift T-1 fixes sits exactly where a batch's critic loop reads. Recommended ordering (not a hard block): T-1 before the next factory batch.
- **Coastline 500 ms perf assertion** (#871) — first occurrence was deliberately left unwidened; T-5 carries it now that the class recurred across three named tests.
- **Tally-row logging** — lanes now log known classes as cumulative tallies ("ninth occurrence…"). Good hygiene; retro re-binning must treat those counts as cumulative, not additive (this cycle's occurrence sum was inflated by exactly this).

## Needs Christian

Nothing new requiring a decision this week. Standing (4th week): the two rulebook drift-scan signals ([THR-912](https://linear.app/threadbare/issue/THR-912), [THR-913](https://linear.app/threadbare/issue/THR-913)) remain at No priority and structurally unpickable; counts one week stale because the scan did not fire today.
