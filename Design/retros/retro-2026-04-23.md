# Workflow Retrospective — 2026-04-23

**Period covered:** 2026-04-18 → 2026-04-23 (impediments #55c–#78, ~23 new occurrences)
**Days since last retro:** 5 (previous retro 2026-04-18, items #40–#55)
**Scope:** Status check on prior open items + fresh impediments logged since last retro, with a design-council pass on proposed actions.

> ## Correction — posted 2026-04-23 (after Linear cross-check)
>
> **The sections below marked ~~strikethrough~~ were wrong.** This retro was written from the impediment dashboard's 5-day-stale `DATA` array (stops at #55) without cross-checking Linear, and it repeated the dashboard's "TB-120 didn't land" framing. That framing is false. A Linear query for the Repo Health project shows:
>
> - **THR-160 (TB-120 — Test Suite Repair Sprint):** Done 2026-04-18, same day the last retro was filed.
> - **THR-161 (TB-121 — CI/CD Pipeline Setup):** Done 2026-04-18.
> - **THR-162 (Browser stall at tick 72 / impediment #42):** Done 2026-04-19, with four perf-deferrals (THR-185 / THR-186 / THR-187 / THR-188) all Done as well.
> - **THR-165 (delete remaining hardcoded count assertion):** Done 2026-04-19.
> - **THR-237 (contentInvariants helper):** Done 2026-04-23 at 08:12.
> - **THR-213 (verification-before-completion hard rule):** Done 2026-04-23 at 12:12.
> - **THR-245 (v2 contentInvariants sweep):** in Ready for Codex since 2026-04-23 08:14 — correctly routed to Codex's queue.
>
> **Net effect on the retro's conclusions:** "TB-120 is now the retrospective's unanswered question" (Summary bullet #1) is wrong — it shipped. "What slipped" is wrong about TB-120 and TB-121. "Ask of the user" #1 (authorize TB-120) and #5 (assign owner for #42) are moot — both closed. The "Top 3 impediments" #3 (TB-120 compounding) is overstated: THR-29's trace-buffer overflow (#57) is real, but it lives against a repaired baseline, not a chronically-red one.
>
> **What remains valid:** everything about automation shell setup (`$CODEX_HOME`, Obsidian MCP, `rg.exe`), the Cowork duplicate-draft near-miss (#69), the handoff-template completeness gap (#70), the `git stash` data-loss lesson (#62), and the dashboard-drift observation. The four consented experiments stand — especially #1 ($CODEX_HOME fix) and #2 (mechanical dashboard regen, which this correction is motivating directly).
>
> **Root cause of this retro's error:** trusted the dashboard without verifying against Linear. The dashboard being stale was flagged in the retro body itself, but I still used its framing for the top-level narrative instead of running `list_issues project:"Repo Health"` first. Lesson: next weekly retro starts with Linear queries, not the dashboard. The dashboard is a visualization, not a source.
>
> Strikethroughs below mark the specific passages contradicted by the Linear state. The original text is preserved for audit — do not rewrite, annotate.

> **Critical data-quality note.** The impediment dashboard's `DATA` array still ends at #55. The canonical `Docs/impediments.md` log runs through #78 — roughly 23 new occurrences (5 days of signal) are invisible to the dashboard. This retro uses the markdown log as source of truth; the dashboard-regen experiment from last retro slipped and needs to be the first action this week.

---

## Top 3 impediments this week

1. **`$CODEX_HOME` unset — 12 occurrences in one week (#76), plus #64/#67.** A trivially-fixable environment-variable gap is now the single highest-count impediment. Every Threadbearer-coding automation run pays the rediscovery tax at session start. At ~1 min per occurrence this is ~15 min/week lost to a one-line profile edit.
2. **Cowork near-miss: 7 duplicated shipped issues (#69).** A prior Cowork session drafted THR-199–205 as a "new" UI Visual Overhaul series without checking the Done column, where the identical THR-168–174 series had already shipped. Had CC picked up THR-199 before cleanup, we would have burned days either producing no-op commits or merge-conflicting with shipped code. Workaround is documented; the structural fix (hard pre-draft Done-column check) is still missing.
3. ~~**TB-120 still not landed — now with a new compounding failure (#57).** Last retro's #1 action item did not land. Worse, THR-29 (`phaseFactionActions`) shipping overflowed the trace-buffer and created a *new* unrelated failure in `traceBuffer-integration.test.ts`. The red baseline is no longer stable — new real failures are now landing on top of pre-existing ones, making the "baseline noise" story harder to defend.~~ — **Corrected 2026-04-23:** TB-120 (THR-160) shipped 2026-04-18; the #57 trace-buffer overflow is a discrete new issue against a repaired baseline, not a compounding failure on a red one. See correction block at top.

---

## Status of active impediments

Prior-open items carried from 2026-04-18 retro plus all unresolved/partial entries since.

| # | Title | Age (days) | Owner | Status | Next action |
|---|-------|-----------:|-------|--------|-------------|
| ~~22~~ | ~~`npm test` red baseline (TB-120)~~ | — | — | **Closed 2026-04-18** — TB-120 shipped as THR-160. Dashboard still shows `workaroundFound: false`; flip in regen. | Close in dashboard. |
| 36 | `ThreadLineMesh` → `ActivityIconMesh` coupling | 14 | TBD | Opportunistic — still not extracted | Fold into next HexMapV2 touch, no standalone issue. Owner: whoever next edits either file |
| ~~42~~ | ~~Browser stall at tick 72 with 1010+ agents~~ | — | — | **Closed 2026-04-19** — shipped as THR-162 with four perf-deferrals (THR-185/186/187/188, all Done). Dashboard still shows `workaroundFound: false`; flip in regen. | Close in dashboard. |
| 47 | `omenState.activeOmens` / `OmenTrackTemplate` missing | 6 | Cowork | Deferred to THR-128 (acceptable) | Leave as-is; sequence with Thematic Pressure omen project. No further action. |
| 56 | GitHub branch protection needs Pro | 5 | Christian | Blocked on account upgrade | **Ask of user.** Upgrade to Pro or make repo public so CI can gate merges. |
| 57 | `traceBuffer-integration.test.ts` overflow from THR-29 | 4 | TBD | Discrete failure against the now-green baseline (not a TB-120 compounding failure — TB-120 shipped). | File a dedicated Linear issue in Repo Health project — owner Codex (pattern-following); pick (a) bump `TRACE_BUFFER_SIZE`, (b) reduce `phaseFactionActions` trace verbosity, or (c) tolerate circular-buffer wrap in the sequential-IDs test. By 2026-04-26. |
| 58 | `session-handoff` skill files deleted | 5 | Cowork | Files gone, Linear auto-close covers most use | Restore from git history or confirm deletion intentional (THR-236 retired Slack closeout); update any remaining SKILL references. Owner: Cowork |
| 59 | 8+ orphan uncommitted changes | 5 | Christian | Open, no workaround | Human review pass to attribute each file to a Linear issue, then commit or discard. Owner: Christian |
| 61 | Scheduled-task sonnet cannot pick up `model:opus` issues | 4 | Cowork | Blocked, needs manual opus session | Document in scheduled-task skill: if only Ready-for-Dev item is `model:opus` and caller is sonnet, post a "needs opus pickup" comment and no-op cleanly. Owner: Cowork |
| 62 | `git stash` data-loss pattern (~40 min re-authoring) | 4 | Cowork | Workaround found | **Add to CLAUDE.md Known Sandbox Limitations:** "Never `rm -f` untracked files for diagnostic A/B. Use `git worktree add` or `git stash -u`." Owner: Cowork, by 2026-04-25 |
| 63 | Linear `save_issue(id)` silently overwrites existing | 4 | Cowork | Workaround found, but destructive | Add to `Docs/plans/2026-04-13-linear-coordination-protocol.md`: explicit "never pass `id` when creating new". Consider wrapping in a guard helper. Owner: Cowork, by 2026-04-25 |
| 64/67/76 | `$CODEX_HOME` unset (× ~14 occurrences) | 4 | Christian / Codex operator | Workaround found, but recurring | **Set `$CODEX_HOME` globally** in Windows PowerShell profile so automation shells inherit it. Owner: Christian (one-line fix), by 2026-04-24 |
| 66/71/75 | Obsidian MCP unreachable (× ~7 occurrences) | 4 | Christian | Partial workarounds, vault log entries queued | Either (a) keep Obsidian Local REST API plugin running continuously, or (b) switch vault-log appends to filesystem-mount fallback. Owner: Christian to decide path |
| 69 | Cowork duplicated 7 shipped issues | 3 | Cowork | Cleanup done; structural fix pending | **Hard gate in Cowork issue-creation workflow:** query `list_issues project:"<proj>" state:"Done"` before drafting any issue in an existing project; abort draft if title-keyword match ≥0.6. Owner: Cowork, by 2026-04-28 |
| 70 | THR-222 handoff missing Files-to-touch / Done-when | 3 | Cowork | Paused; refreshed handoff requested | Add handoff-template completeness check to `cowork-plugin-customizer` or plan template. Owner: Cowork |
| 72 | `git add <files>` staged extra untracked files | 3 | Codex | Partial — committed as-is, flagged | Add `git diff --cached --name-only` pre-commit check to automation commit step. Owner: Codex session template |
| 73/74/78 | `rg.exe` blocked in Codex sandbox (× many) | 4 | n/a | Documented in CLAUDE.md Known Sandbox Limitations | No new action — already documented; agents reliably falling back. |
| 77 | Wrong npm script name in handoff comment | 2 | Cowork | Workaround found | Add `npm run` verification pass to handoff-template generation (verify listed scripts exist in package.json). Owner: Cowork |

Dashboard update backlog (one-line: flip `workaroundFound: true` / add closing SHA in the `DATA` array): #40, #41, #43, #44, #45, #46, #50, #51, #52, #53 carry from last retro — still not reflected. Plus new closeable items: #48, #49, #55, #58, #60, #65, #68, #73/74, #77.

---

## What shipped since last retro

- **`Known Sandbox Limitations` section added to CLAUDE.md** — carried action from 2026-04-18 retro. `rg.exe`, PowerShell scoping, git perms, npm-test timeout, esbuild EPERM, PDF/`yt-dlp` absence, web.open ref expiry, Linear `save_issue` silent-write, Linear `orderBy: 'priority'` rejection. Agents are now hitting these and self-serving the workaround. Shipped.
- **THR-183 / UI Visual Overhaul project** — 7-issue series (THR-168–174) all in Done. This week's duplicate-creation (#69) was the failure mode of success: the project itself shipped cleanly.
- **Codex-review wrapper + PR-gated Action** (`.github/workflows/claude-review.yml`, `Docs/plans/2026-04-19-cc-review-replacement.md`). Runs in advisory mode pending GitHub Pro.
- **CI workflow running `npm test`, `npx tsc --noEmit`, `npx vite build` on every push/PR** — works, but cannot hard-block merge until #56 clears.
- **Obsidian MCP error-queueing pattern established** — sessions log intent locally and replay when the connector is reachable. Acceptable partial until the root fix lands.
- **THR-236 retired Slack closeout** — "Linear state transition IS the handoff" is now canonical per CLAUDE.md. Scheduled-task SKILL.md is stale against this (#67).

---

## What slipped

- ~~**TB-120 test suite repair** — still the #1 action item, now for the third retro running. Likely reason: no forcing function. Every session continues to pull its own project's top Linear issue; "Finish Before You Start" covers projects but not infrastructure. Until TB-120 is its own project or gets an explicit user-authored override, this will keep slipping.~~ — **Corrected 2026-04-23:** TB-120 (THR-160) and TB-121 (THR-161) both shipped 2026-04-18 into the new Repo Health project. THR-237 / THR-213 shipped today (2026-04-23). The retro's "slipped" framing was dashboard-induced false signal.
- **Dashboard auto-regen stub** — last retro proposed "next agent editing `Docs/impediments.md` updates `DATA` array in the same commit." 23 new entries landed without the dashboard moving. The promise-driven approach didn't stick; needs a mechanical generator.
- **Retroactive dashboard closes for #14, #25** — called out in last retro's Ask of the User, still not done. The "Unresolved" stat on the dashboard remains wrong.
- **Silent-typing typed-helper / lint rule plan (#40/#45/#46/#50)** — last retro flagged as "not urgent enough for a separate experiment." No plan drafted. Fine to defer again, but note for next month if silent-typing recurs.

---

## Workflow observations

- **Recurring themes cluster around automation shell setup.** `$CODEX_HOME` unset (#64/#67/#76), Obsidian MCP connector missing (#66/#71/#75), `rg.exe` blocked (#73/#74/#78). Total: ~20+ occurrences in 5 days for issues with one-time fixes. Automation cold-starts are where we're bleeding the most time.
- **Cowork's design authority has failure modes worth naming.** Two distinct ones this week: (a) drafting duplicates of already-shipped work because the Done column wasn't checked (#69), (b) handoff comments that reference artifacts outside the repo (#70) or that use stale paths (#65) or wrong script names (#77). The handoff artifact is the contract — it needs a mechanical completeness check, not a human-attentiveness appeal.
- **The impediment log and the dashboard have diverged.** 23 new entries not reflected, plus the carryover closeouts still pending. The dashboard is now actively misleading — it advertises "5 unresolved" when the real count is ~10 (plus partial-workaround items). If the dashboard isn't the source of truth, it shouldn't be the artifact this retro reads from. Either regenerate it or retire it.
- **New red-test failures are compounding on the old baseline.** #57 (`traceBuffer-integration.test.ts` overflowing from THR-29) is a *new* failure, not one of the pre-existing set. The longer TB-120 slips, the more of these stack up and the harder the eventual repair gets.
- **The `/session-handoff` instruction in uploaded scheduled-task SKILL.md is now stale (#67).** CLAUDE.md says Linear transition IS the handoff. The scheduled-task file sits outside the repo, so Cowork can't update it in-session. This retro's scheduled task is an example — the input task file itself is the governance artifact that needs re-authoring.
- **Codex review replacement (`.github/workflows/claude-review.yml`) is quietly paying off.** No direct catches this week, but advisory mode is now running on PRs, and the `Docs/plans/2026-04-19-cc-review-replacement.md` gate is lined up for when #56 clears. Worth preserving.

---

## Design-council pass — consented experiments (small, reversible)

Four voices weighed in internally (engineer, ops, PM, tech lead). Consensus actions, each scoped so a revert is one PR:

1. **Fix `$CODEX_HOME` once.** Add a single line to the Windows PowerShell profile so every automation shell inherits it: `$env:CODEX_HOME = "$env:USERPROFILE\.codex"`. Expected impact: ~12 occurrences/week → 0. Reversibility: delete the line. Owner: Christian, ETA 2026-04-24. *(Engineer: "5-min fix, stop paying the tax." PM: "Biggest count, smallest fix — do it first.")*

2. **Mechanical dashboard regen (replacing the promise-based approach).** Add a `scripts/generate-impediment-dashboard.ts` that parses `Docs/impediments.md` and rewrites the `DATA = [...]` array in `Design/impediment-dashboard.html`. Wire into a pre-commit or `npm run` step. Expected impact: dashboard stops drifting. Reversibility: delete script, no harm done. Owner: Cowork to design, CC to implement, ETA end of week. *(Ops: "The promise didn't stick. Mechanize it.")*

3. **Cowork issue-creation hard gate.** Before drafting any issue in an existing project, Cowork must run `list_issues project:"<proj>" state:"Done"` and diff titles. If any Done issue's title shares ≥3 non-trivial words with the proposed draft, abort and either extend the shipped issue or ask the user to confirm a fresh pass. Expected impact: no more near-miss duplications. Reversibility: remove the gate, accept the risk. Owner: Cowork (update `cowork-plugin-customizer` or add to `product-management:write-spec` checklist), ETA 2026-04-28. *(PM: "This almost cost a week of CC work. It's the least negotiable item this week.")*

4. **Add "Never `rm -f` untracked files for A/B diagnostics" to CLAUDE.md Known Sandbox Limitations.** Codify the #62 lesson (40 min of re-authoring). Prescribe `git worktree add` or `git stash -u` as the only correct patterns. Expected impact: next time someone reaches for the destructive version, they see the warning. Reversibility: delete the paragraph. Owner: Cowork, ETA 2026-04-25. *(Tech lead: "The lesson cost 40 min once. Make sure it costs 0 min next time.")*

Two items explicitly *not* picked as experiments this week — noted so they aren't forgotten:

- **Typed-helper / lint rule for silent-typing (#40/#45/#46/#50).** Deferred a second time. Re-evaluate if any new silent-typing occurrences show up next week.
- **Dashboard retirement.** If mechanical regen (#2) turns out harder than expected, retire the dashboard and treat `Docs/impediments.md` as canonical. Fall-back option only.

---

## Ask of the user (Christian)

1. ~~**Authorize TB-120 as CC's next pick, overriding whatever is top of Ready for Dev.** Same ask as last retro — the baseline is now compounding (#57). Without your override, the "Finish Before You Start" rule will keep deferring this.~~ — **Moot 2026-04-23:** TB-120 shipped 2026-04-18. No action needed.
2. **Set `$CODEX_HOME` in your PowerShell profile** (one line: `$env:CODEX_HOME = "$env:USERPROFILE\.codex"`). This is a you-only fix — automations inherit the shell. Biggest-count impediment this week and takes 30 seconds.
3. **Decide Obsidian MCP hard-dependency vs. graceful-degrade.** Options: (a) auto-start the Local REST API plugin on boot so it's always up; (b) add a filesystem-mount fallback to the vault-log-append skill so sessions don't queue indefinitely. Either is fine; pick one.
4. **GitHub Pro upgrade decision for #56.** Branch protection / required CI gates depend on this. Not urgent this week (CI is green and advisory), but it blocks the final hardening of `.github/workflows/claude-review.yml`.
5. ~~**Assign an owner for #42 (browser stall at tick 72).** 11 days old, no Linear issue, no workaround. Worth a dedicated issue in the engine project.~~ — **Moot 2026-04-23:** shipped as THR-162 on 2026-04-19 with four perf-deferrals all Done. No action needed.
6. **Confirm deletion of the `session-handoff` skill files (#58) was intentional** per THR-236. If yes, close #58 and drop it from the dashboard. If no, restore from git.
7. **Re-author the `keep-codex-flowing` / weekly-retro scheduled-task SKILL.md files** (via the `schedule` skill) to drop the `/session-handoff` step. The files sit outside the repo so agents can't self-heal this (#67).

---

## Summary

- ~~**TB-120 is now the retrospective's unanswered question.** Three retros have named it as #1; three retros have watched it slip. The baseline is no longer stable (#57 is a new failure), so the cost of continued deferral is rising.~~ — **Corrected 2026-04-23:** TB-120 shipped 2026-04-18, TB-121 shipped 2026-04-18, THR-237 + THR-213 shipped today. The *real* retro-level observation is that **the dashboard diverged so badly from Linear that I wrote a retro claiming the opposite of what happened.** Mechanical dashboard regen (Experiment #2) needs to ship this week, not next.
- **Automation shell setup is bleeding ~20+ small impediments/week.** `$CODEX_HOME`, Obsidian MCP, `rg.exe` — each individually S-impact, together a 30+ min/week drag. The $CODEX_HOME fix alone would retire ~12 occurrences.
- **Cowork's design authority needs mechanical gates, not human-attentiveness appeals.** #69 (7 duplicates) and #70 (incomplete handoff) are both "a careful designer would catch this" failures. Replace with pre-draft Done-column check and handoff-completeness lint.
- **The dashboard is now actively misleading** — 23 impediments behind the log. Mechanize regen or retire.

---

*Retro run 2026-04-23 via the `weekly-workflow-retro` scheduled task. Autonomous run — no user present. Input: `Design/impediment-dashboard.html` (stale at #55) cross-checked against `Docs/impediments.md` (current to #78).*
