# Retrospective — 2026-08-07

## Period
From: 2026-07-31 (exclusive; last retro)
To: 2026-08-07

## Summary
- Drift-scan issues consumed: THR-911, THR-912, THR-913, THR-915 — **updated in place by today's 14:22 UTC scan run**, the first cycle of THR-756's dedupe-filing behavior working as designed (no duplicate tickets filed; the open issues were retitled/re-counted at 14:29 UTC). THR-914 (quick-ref vs rulebook) did **not** re-fire — the scanner bug the 07-31 retro fixed (phantom `Culmination)` stage) stayed fixed. THR-747 (skill freshness, 07-24) was untouched by today's run — see Tuning.
- Impediments logged this cycle: **104 entries / 128 occurrences, ~6.3h estimated loss** (deterministic draft, `Design/retros/retro-2026-08-07-draft.md` reproducible via `npm run retro-draft`).
- Top category: tooling (59 entries / 70 occurrences), then process (24/30), testing (17/17).
- Improvements implemented this session: **5 quick wins** (all effort-1 doc/skill edits).
- Improvements backlogged: 2 — see § Improvements Backlogged.
- **Previous retro's backlog: 4 of 4 shipped Done within the week** (THR-908 in 2 days, THR-921, THR-756, THR-757) — the improvement loop's best week on record, and THR-756's fix was directly observable working in this run's own Step 0.

## Drift-Scan Signals This Cycle

Today's scheduled scan succeeded (run 31187281212, 14:22 UTC, 6m25s) and fired **before** this retro — last cycle's tuning recommendation #1 (restore scan-before-retro ordering) is satisfied; no further change needed.

| Issue | Signal | This week | Last consumption (07-31) | Direction |
|---|---|---|---|---|
| THR-911 | UL drift | 0 canonical-unused, 15 used-uncanonical | 6 / 15 | Improved on one axis |
| THR-912 | Rulebook → UL | 10 broken references | 8 | **Worsened** |
| THR-913 | Rulebook IMPL tags | 8 broken code references | 6 | **Worsened** |
| THR-915 | Interface-map coverage | Culture; 8 subsystems unaudited | 11 | Improved |
| (THR-914) | Quick-ref vs rulebook | did not re-fire | 1 divergence (scanner bug) | Structurally closed 07-31 |

**Signal health: working, dedupe shipped, still triage-starved.** All four live signals sit in `Idea`, untouched since the 07-22 era, and the two rulebook-drift counts are growing. The structural reason they never get picked: Rule 0 correctly excludes doc drift (no demonstrated loss), so they sort at rule 3 by priority — and they carry **No priority**. That is a standing decision nobody has actually made. See § Needs Christian.

## Impediment Analytics

Deterministic tables in `Design/retros/retro-2026-08-07-draft.md`. This week's real clusters (regrouped by root cause, not category):

| Cluster | Entries / occurrences | Exemplars | Disposition |
|---|---|---|---|
| `preview_start` reports a port Vite is not on | 6 entries / ~10 occ | #368, #376 ×2, #384 ×2, #396 ×2, #403 ×3, #450 | Quick win 2 (CLAUDE.md bullet); tool-level cause is `cant-fix` from the repo |
| Worktree `node_modules` runbook itself broken | 7 entries / ~9 occ | #370, #394 ×2, #412 ×3, #431 ×2, #458, #342 ×8 | Quick win 1 (repair sequence rewritten) |
| Game interrupt modals defeat browser-verify | 7 entries | #385, #427, #445, #446, #447, #453, #455 | Backlog ticket (testing lever); game-side half is THR-1005 (Todo, in flight) |
| `__DEBUG` async / aliasing pitfalls | 5 entries | #405, #435, #436, #459, #463 | Quick win 4 (Debug Bridge await note) |
| Shell-mediated file corruption | 5 entries | #399 (Write tool NUL), #426, #449, #457, #464 | Known-limitation bullet already exists; #399 is a new *Write-tool* shape — watch |
| Closeout / merge-state friction | 6 entries / ~9 occ | #460 ×3, #461, #466, #467, #423, #402 | THR-1016 shipped 08-07, THR-1018 In Dev; #466 → backlog ticket |
| Vacuous gates / probes | 6 entries | #409, #417, #421, #429, #432, #452 | Quick win 3 (#409); the rest covered by 07-31's testing-patterns section — falsification is being *run* now, which is why these were caught |
| Promotion premise integrity | 3 entries | #416, #420, #434 | THR-990 shipped (T1 fifth decline reason); #434 → quick win 5 |

Unresolved blockers (Workaround Found = No/Partial): #453 (partial — DOM-assert workaround; a genuinely clean frame is still untried via `browser_press_key`). Nothing fully blocked.

## Cross-Signal Patterns

1. **The improvement loop closed its own loop this week.** All four of last retro's tickets shipped within days; the dedupe fix (THR-756) was verified working by this run's own Step 0; the scanner bug fix (THR-914) held. The machinery this skill exists to feed is demonstrably consuming what it files.
2. **The game's own interrupt design now fights the DoD's evidence path.** Ascendant beats auto-open (by design — THR-934, and THR-1005 will extend interrupts to aftermaths), and 7 impediments this week were verification runs spending 5–15 minutes fighting modals: pointer-interception timeouts that read as broken selectors (#445), captures dimmed by full-viewport dialogs that synthetic Escape cannot close (#453), surfaces reachable only through a dismiss-loop → badge-click → dismiss-loop dance (#447). Every fix that makes the game *more* interruptive (correct for players) makes scripted verification *more* expensive. The two needs diverge and need a sanctioned lever — backlogged below.
3. **Gate-shaped holes keep moving down the stack.** CI-level gate theater was closed (THR-686/755); this week's finds are a prescribed local gate that lints zero files (#409 — the command CLAUDE.md itself named), a monitoring probe blind to half the failure it reports on (#466), and an inert `.gitattributes` merge driver (#467). The pattern: a gate's *invocation shape* silently deciding it inspects nothing. Same falsification discipline applies — quick wins 3 and backlog T-B.
4. **Closeout-docs conflict friction peaked and (probably) died mid-week.** #460 ×3 / #461 / #467 all trace to every closeout writing the same `project-status.md` anchors; THR-1016 (one-file-per-entry fragments) merged **today**. Next retro should see this cluster vanish — if it doesn't, the fix missed.
5. **Scan-vs-impediments coverage gap:** none new this cycle. The #465 GitHub webhook-delivery outage (×2) is platform-side and self-resolved; not a signal candidate.

## Previous Retro's Backlog (Rule 5)

| Issue | Status |
|---|---|
| THR-908 pull-work Step 3 provenance + mutex liveness | Done (2026-08-02) |
| THR-921 promotion-time plan-doc liveness gate | Done (2026-08-06) |
| THR-756 drift-scan dedupe filing | Done (2026-08-02) — verified working on today's scan |
| THR-757 wiring-guide gap-fill (ambitions + Divine Receipt) | Done (2026-08-02) |

## Improvements Made This Session

1. **CLAUDE.md § Known Sandbox Limitations — worktree `node_modules` repair sequence rewritten** (#370, #394 ×2, #412 ×3, #431 ×2, #458): the old prescription (`cmd /c rmdir` the stub first) is guaranteed to no-op on the exact stub shape it was written for — the stub holds *hidden* Vite caches, so `rmdir` refuses and the error points at the subsequent `mklink`. New 7-step sequence: probe `.bin/vitest` in worktree **and donor**, inspect with `ls -a`/`Get-ChildItem -Force`, `Remove-Item -Recurse -Force`, junction, verify by binary, remove junction at closeout. ROI 5×2/1 = **10**.
2. **CLAUDE.md § Known Sandbox Limitations — new `preview_start` port bullet** (#368, #376, #384, #396, #403, #450 — ~10 occ): the reported port is a hint; Vite's `Local:` banner in `preview_logs` is the authoritative binding — read it before the first navigation. ROI 5×2/1 = **10**.
3. **CLAUDE.md § Testing step 5 — `lint:plan-doc` explicit-mode caveat** (#409, THR-984 still open in Idea): the bare documented command lints zero files and always prints a pass; invoke with `-- --staged` / `-- --all` / a path. ROI 2×3/1 = **6**.
4. **CLAUDE.md § Debug Bridge — Promise/await note** (#405, #436, #459, #463): most `__DEBUG` accessors return Promises; a forgotten `await` reads as a wrong-shape result, and `getEventsSince` is a 100-entry rolling buffer. ROI 4×1.5/1 = **6**.
5. **`.claude/skills/pull-work/SKILL.md` Step 5 — comment-skim rule** (#434): on any ticket with >1 comment, skim every comment's first line, not only the latest — the `Reopened` label is the wrong trigger for scope corrections filed by sibling implementations. `last_validated_against` bumped to 2026-08-07. ROI 2×3/1 = **6**.

## Improvements Backlogged

- [THR-1019](https://linear.app/threadbare/issue/THR-1019) — Browser-verify testing lever: deterministically clear ascendant-beat interrupts via a `__DEBUG` helper wired to the interrupt registry (impediments #385, #427, #445, #446, #447, #453, #455; ROI 4×2/2 = 4). Filed to Todo with coordination block as first comment; preferred after THR-1005 lands.
- [THR-1020](https://linear.app/threadbare/issue/THR-1020) — `check:armed-prs` reads `statusCheckRollup` and distinguishes "conflicted" from "conflicted and red" (impediments #466, #402; ROI 2×3/1 = 6). Filed to Todo with coordination block as first comment.

Deliberately **not** filed: impediment-id collision repair (#460 ×3 — THR-1018 already In Dev), closeout-anchor conflicts (#461/#467 — THR-1016 merged today; watch, don't duplicate), `lint:plan-doc` no-op (#409 — THR-984 already filed, still Idea; quick win 3 mitigates meanwhile), the Playwright/preview_start port behavior (`cant-fix` at tool level — absorbed into the CLAUDE.md bullet), and the GitHub webhook delivery outage (#465 — platform-side, self-resolved).

## Tuning Recommendations

1. **No signal thresholds need tuning.** The four live signals are real drift, correctly measured (the counts moved in both directions this week, which is what a live measurement looks like).
2. **Skill-freshness signal (THR-747) was not refreshed by today's scan** — its title still reads `[2026-07-24]` and its `updatedAt` predates today, while the other four signals were updated in place. Either the signal no longer fires or THR-756's dedupe missed it. Not actionable on one observation; **next retro's Step 0 should check whether the 08-14 scan touches it**, and file a scanner ticket if not.
3. **Step 5c — intent-judge kill-criteria aggregation** (`Docs/judge-metrics/`, header-keyed parse):

   | Criterion | Value | Verdict |
   |---|---|---|
   | ≥95% Allow with zero overrides | Allow 81.8% (9/11) | Below the floor — **not** rubber-stamping |
   | ≥50% override rate on Revise/Block | — | **NOT MEASURABLE** (0 rows carry an override column) |
   | Median time-to-judgment >300s | 92.5s (n=10) | Pass |
   | Zero marginal catches | — | User judgment, not row-derived |

   **Verdict: keep intent-judge — unchanged from 07-31, because the data is unchanged: 11 rows, zero new judgments logged this week.** Plan docs were authored this week (design sessions ran), so either no doc passed through the judge or judgments ran without writing metrics rows. One check next cycle: if new plan docs merged but `Docs/judge-metrics/` gained no rows, the metrics half of the judge is silently dark and the kill criteria are un-evaluable going forward.

## Step 5b — Systemic Wiring Guide Audit

The 07-31 carry-forward is satisfied: the nudge-card `grants` vocabulary (THR-885) now has its own guide section (`## Capability: Nudge cards that cost, grant, and filter (THR-885)`, plus Capability 14 for the nudge hand). THR-757 closed the two known gaps (world-minted ambitions, Divine Receipt) on 08-02, and the guide took further updates through 08-05 (army supply / Flow Web P2). No new undocumented capabilities flagged this week — the guide is being maintained in-stride, which is the healthy state.

## Needs Christian

**The four drift-scan signals have been open since 2026-07-22 and two are worsening** (rulebook → UL broken refs 8 → 10; IMPL tags with broken code refs 6 → 8). They are structurally unpickable: Rule 0 correctly excludes doc drift from the fast lane, and at No-priority they sort below every feature ticket forever. One decision clears it: **either give THR-912/THR-913 (the two worsening rulebook signals) a Low/Medium priority so the queue can reach them, or explicitly accept rulebook drift as a cost until the next rulebook-review cycle.** THR-911/THR-915 are stable-or-improving and can wait either way.

## Patterns to Watch

- **THR-1016 fragment closeouts** — the same-anchor conflict class (#460/#461/#467) should disappear from next week's log; if any closeout PR goes `DIRTY` on `Docs/status/` fragments, the fix missed. THR-1018 (union-merge id collisions) is In Dev and covers the sibling class.
- **Write-tool NUL corruption (#399)** — a single occurrence of the *Write tool itself* (not a heredoc) landing a U+0000 byte. Harness-level if it recurs; one more occurrence justifies an upstream report alongside claude-code#79713.
- **GitHub `pull_request` event delivery outage (#465 ×2)** — platform-side, self-resolved, `cant-fix`; worth a row only if it becomes a pattern.
- **Judge-metrics silence** — see Tuning 3; the check is one `ls` next cycle.
- **Beat-vs-verification divergence** — after the backlogged testing lever lands, watch whether the modal-fighting cluster actually drops; if entries persist, the lever's coverage is wrong, not the diagnosis.
