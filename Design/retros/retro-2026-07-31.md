# Retrospective — 2026-07-31

## Period
From: 2026-07-24 (exclusive; last retro, reconstructed under THR-798)
To: 2026-07-31

> The deterministic draft (`npm run retro-draft`) reports "From: 2026-05-04" because it keys
> on the last `Retrospective conducted:` footer in `Docs/impediments.md`, and neither the
> 2026-07-24 run (which never wrote its report) nor its THR-798 reconstruction wrote one.
> This report's narrative analysis covers the 07-24 → 07-31 week; the draft's cumulative
> tables since 05-04 are reproducible via `npm run retro-draft`.

## Summary
- Drift-scan issues consumed: THR-746, THR-747, THR-748, THR-749, THR-750, THR-751 (all from the 2026-07-24 scan, all still untriaged in `Idea` a week later), plus THR-764 (Done — its fix is why this draft sees 338 entries).
- **This run fired at ~12:40 UTC — before today's 14:00 UTC drift scan**, so no 2026-07-31 scan data existed to consume. Logged as impediment #353; see Tuning Recommendations.
- Impediments logged this cycle (07-25 → 07-31 dates): ~130 entries. Cumulative log: 338 entries / 1277 occurrences / ~45.9h estimated loss (vs 109 / 1021 / ~32.5h at the 07-24 draft — most of the entry-count jump is THR-764 making 65 pre-existing paragraph-form entries visible to the parser, not new friction).
- Top new categories this cycle: tooling (dominated by browser-evidence tooling), verification (16 of 17 all-time entries are from this week), process.
- Improvements implemented: 4 (three doc/skill quick wins + a dashboard-generator regex fix found in-flight)
- Improvements backlogged: 1

## Drift-Scan Signals This Cycle

Consumed from the 2026-07-24 scan (created by the last successful scheduled run; today's scan had not fired when this retro ran):

| Issue | Signal | Summary | First-time or repeat |
|---|---|---|---|
| THR-747 | Skill freshness | 23 stale skills (>60 days unvalidated) | Repeat (dup of THR-707, 07-22) |
| THR-746 | UL drift | 6 canonical-unused, 15 used-uncanonical | Repeat (dup of THR-706) |
| THR-748 | Rulebook → UL | 8 broken UL references in rulebook Definitions | Repeat (dup of THR-708) |
| THR-749 | Rulebook IMPL tags | 6 IMPL tags with broken code references | Repeat (dup of THR-709) |
| THR-750 | Quick-ref vs rulebook | 1 factual divergence (Doom stage name) | Repeat (dup of THR-710) |
| THR-751 | Interface-map coverage | 11 subsystems unaudited; clear Culture | First-time as ticket |

**Signal health: working, but triage-starved and duplicated.** Every 07-24 signal is a repeat of the 07-22 set and all sit untriaged in `Idea`. The repeat-without-triage pattern is exactly what THR-756 (dedupe filing — update open signal issues instead of filing weekly duplicates) exists to fix; it was promoted to Ready for Dev on 07-28 and should ship before the next scan doubles the set again. No auto-kill assessment needed — the scan produced signals both of the last two runs.

Scan pipeline health note: the three scheduled runs before 07-22 (07-03, 07-10, 07-17) all failed in under a minute; 07-22 (manual) and 07-24 (scheduled) succeeded, confirming the P43 repair held.

## Impediment Analytics

Deterministic tables: `npm run retro-draft` (cumulative since 05-04). This cycle's shape, from entries dated 07-25 → 07-31:

| Cluster (this week) | Entries / occurrences | Exemplars |
|---|---|---|
| Playwright MCP browser-evidence failures | ~10 entries, ~13 occ | #257 ×3, #318 ×2, #319, #327, #228 ×2, #244, #264, #265, #308 |
| Vacuous verification / gates that cannot fail | 16 entries | #241, #282, #288, #292, #295, #296, #237, #242 |
| Worktree/home `node_modules` unreliable | ~6 entries, ~10 occ | #283 ×2, #287 ×2, #294 ×3, P95 |
| Coordination-block gaps in `/pull-work` Step 3 | 2 entries, 7 occ | #280 ×4 (orchestrator promotion), #224 ×3 (mutex partner in Todo) |
| GitHub Actions billing outages failing open | 4 entries | P85, #278, #279, #290 — all ticketed (THR-842, THR-768, both Done) |
| Scheduled-task prompt drift/mirroring | 3 entries | #248, #332, #333 — ticketed (THR-850 Done, THR-823/851 Ready for Dev) |
| Bash-tool heredoc/quoting corruption | 4 occ | #211 ×2, #329, #352 |

## Cross-Signal Patterns

1. **The evidence path is now the biggest friction source.** The DoD's browser-verify requirement is binary, but its sanctioned Playwright route failed ~13 times this week in four distinct silent modes (stale allowed-roots, wrong-port attachment, missing `.playwright-mcp` dir, viewport reverting across navigation). The dangerous direction is uniform: **the capture succeeds and looks like proof of the wrong thing.** Every workaround is known and logged; they were scattered across six impediment entries. → Quick win 1 consolidates them into a single pre-capture ritual in CLAUDE.md's sanctioned-routes list. Root cause (MCP server outlives sessions, roots pinned at launch) is not fixable from the repo: `cant-fix` at tool level.
2. **Verification entries are a new genre, and they rhyme.** 16 of the log's 17 all-time `verification` entries were written this week — probes that pass on empty populations, tests where both sides are fixtures, guards that filter out their own population. This is the same "gate theater" family THR-686/THR-755 attacked at the CI level, recurring at the test-authoring level. → Quick win 2 adds a "vacuous verification" anti-pattern to the `testing-patterns` skill.
3. **The impediment log's own prescriptions are going unfiled.** #280 states "the durable fix remains unticketed and is now 4 occurrences old"; #351 documents a defect that sat in retro action items and `Design/` notes for four months and grew 5× because it never became a ticket — the executor lane reads only Linear. → This retro files the #280/#224 fix as a ticket (see Improvements Backlogged) rather than leaving it as prose, which is Rule 8's spirit applied to the queue.
4. **Scan-vs-impediments coverage gap:** the drift scan has no signal for scheduled-task-prompt ↔ mirror drift (#332/#333 family) — the weekly-project-hygiene task covers part of it (and was itself wrong once, #332). Candidate for a future signal once THR-823/851 land; not proposed this cycle.

## Previous Retro's Backlog (Rule 5)

| Issue | Status |
|---|---|
| THR-753 junction-safe reaping | Done (within 2 days) |
| THR-754 sanctioned headless evidence paths | Done |
| THR-755 gate-credibility sweep | Done |
| THR-756 drift-scan dedupe filing | Ready for Dev (promoted 07-28, not yet picked) |
| THR-757 wiring-guide gap-fill | Ready for Dev (promoted 07-28, not yet picked) |

## Improvements Made This Session

1. **CLAUDE.md § Definition of Done, sanctioned evidence routes:** added the Playwright pre-capture ritual (mkdir → navigate-to-own-port → probe change-only symbol → re-assert viewport in the capture pass) consolidating impediments #257/#318/#319/#327. ROI 5×3/1 = 15.
2. **`.claude/skills/testing-patterns/SKILL.md`:** new anti-pattern section "Probes and guards that cannot fail (vacuous verification)" with the falsify-the-probe rule; `last_validated_against` bumped to 2026-07-31. ROI 4×3/1 = 12.
3. **CLAUDE.md § Known Sandbox Limitations:** two new bullets — Bash-tool heredoc corruption (#211/#329/#352; use Write/Edit tools) and worktree `node_modules` unreliability (probe `node_modules/.bin/vitest`, not the directory). ROI ~3×2/1 = 6.
4. **`scripts/generate-impediment-dashboard.ts`:** found in-flight — the retro-filename regex only matched the retired `Docs/retrospectives/YYYY-MM-DD-retro.md` naming, so the dashboard's "last retro" block was stuck at 2026-05-04 despite ten newer retros in `Design/retros/retro-YYYY-MM-DD.md`. Regex now accepts both eras; dashboard regenerated (per #351's lesson, fixed in the same pass rather than noted for later).
5. **Process:** this run follows the THR-798 ordering (report committed before any ticket cites it); impediment #353 filed for the scan/retro scheduling inversion.

## Improvements Backlogged

_Filed in Step 9._

## Tuning Recommendations

1. **Task ordering → restore scan-before-retro.** The registry documents drift scan Fridays 14:00 UTC and weekly-retro ~15:00 UTC; this retro fired at ~12:40 UTC, so Step 0 could only consume week-old scan data. Proposed: nudge the `weekly-retro` scheduled task to fire ≥15:00 UTC (after scan + jitter), or have Step 0 check `gh run list --workflow drift-scan.yml` for a same-day run and note staleness (this run did the latter manually). Owner: scheduled-task registry (Christian-owned live task; record in `Docs/ops/scheduled-tasks-registry.md` when changed).
2. **Drift-scan filing → ship THR-756 before the next scan.** Two more runs at current behavior means a third duplicate set of the same five signals, and the triage queue is already ignoring the second set.
3. No signal thresholds need tuning this cycle — the signals that fired are real drift, just unactioned.

## Step 5b — Systemic Wiring Guide Audit

`Docs/plans/2026-04-16-systemic-wiring-guide.md` has ~54 references across the key capability nouns and was updated during the week's content work. The known gaps (world-minted ambitions, Divine Receipt) are already ticketed as THR-757 (Ready for Dev) — not re-filed. One forward-looking note rather than a ticket: the nudge card system (THR-885, merged 07-30) introduces card `grants` as a content-facing vocabulary, but the nudge model is mid-migration (WS5 batches pending, prose contract paused behind THR-883) — a wiring-guide section written now would rot; it should ride the WS5/THR-883 settlement, and the next retro's 5b should check it happened.

## Patterns to Watch

- **Typecheck-ratchet ergonomics:** #322 ×2 (Omit-over-union collapse forces casts; suggested `emitTrace` overload is unticketed), #334 (red-baseline files as fixture copy sources certify stale shapes). Not yet frequent enough post-fix to ticket; the memory layer carries both.
- **Armed-PR lifecycle gaps:** THR-897 (DIRTY armed PRs idle forever) filed 07-31 — watch whether the sweep extension closes the class.
- **Untriaged drift-scan output:** if the 07-24 signal set is still in `Idea` at the next retro, triage capacity — not filing behavior — is the binding constraint, and the retro should say so to Christian via the briefing.
