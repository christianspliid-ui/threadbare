# Workflow Retro — 2026-08-19

Covers 2026-08-12 → 2026-08-19. Compares against [`workflow-retro-2026-08-12.md`](https://github.com/christianspliid-ui/threadbare/blob/main/Design/retros/workflow-retro-2026-08-12.md).

## Needs Christian

**Nothing new from this retro.** Everything I found that needs you is already on your page and phrased better there than I would phrase it — [`Design/user-actions.md`](https://github.com/christianspliid-ui/threadbare/blob/ops/Design/user-actions.md) carries all six standing asks, and this morning's [briefing](https://github.com/christianspliid-ui/threadbare/blob/ops/Design/briefing.md) leads with the smallest one. I checked specifically so I would not add a seventh item saying the same thing.

One item this retro filed is *eventually* yours, but not yet: [THR-1190](https://linear.app/threadbare/issue/THR-1190/the-park-decay-remedy-is-a-christian-owned-linear-setting-with-no) ends in a two-minute Linear settings toggle only you can flip. It is deliberately **not** on your list today, because nobody has yet written down which control to click. The ticket's own first step is to find that out. It reaches you as a proper ask once it can tell you exactly what to do — adding it now would just be a puzzle with your name on it.

Context worth having, no action needed: **the lanes you paused this morning were the two that were working.** The week they were on shipped 111 tickets and 162 merges with zero open pull requests left behind. The pause is recorded and correctly reported everywhere; nothing is decaying because of it. The one thing it changes is that the four remaining queued jobs will sit until you resume — which the briefing already tells you.

## Throughput

- **111 issues reached Done** (`completedAt` 2026-08-12 → 2026-08-19), up from 86 last week and 119 the week before. The highest week on record without the lane freeze that depressed last week's tail.
- **162 pull requests merged**, **380 commits** on `main`. Daily merge counts: 14 / 20 / 24 / 35 / 17 / 22 / 23 / 7 (the 7 being this morning, pre-pause).
- **Open-PR backlog: zero.** Not "one old one" — none at all. There is no PR to distinguish between conflict, red check and deliberate hold, because there is no PR.
- **Run outcomes.** The hourly lanes ran continuously through 2026-08-19 ~04:30Z, then both were switched off at your request (~06:00Z). `tb-orchestrator` alone produced 16 run reports on 08-17 and 11 on 08-18. Reading its own logs, the steady state was: promote 1, decline 5–7 with evidence per decline, file 0. Not a single run in the sampled set exited "no ready work" without saying why.
- **Queue depth: 4, and that is the finding of the week — but it is not a new one and it is not a lane defect.** Ready for Dev holds [THR-1183](https://linear.app/threadbare/issue/THR-1183/sublocations-are-minted-in-two-incompatible-node-shapes), [THR-1184](https://linear.app/threadbare/issue/THR-1184/sacred-route-is-registered-but-has-zero-consumers-an-eight-tick), [THR-857](https://linear.app/threadbare/issue/THR-857/possession-subcategory-vocabulary-has-3-off-union-strays-intelligence) and [THR-1133](https://linear.app/threadbare/issue/THR-1133/attended-pixel-pass-sweep-five-owed-19201080-captures-one-dev-server) — every one `Deferral`-labelled, every one `Low`. Zero non-`Deferral` program work, for the third consecutive orchestrator run before the pause.

  **What is different from last week is that the diagnosis now carries a measurement.** The 08-19 run b report records: THR-1170 promoted at 02:30Z, claimed at 03:02Z, merged Done at 03:32Z — **62 minutes promotion-to-shipped**. The executor is draining the shelf faster than promotion can fill it, and promotion can only pass along what design has already produced. That is a supply constraint at the top of the funnel, stated with a number instead of an assertion, and the lane surfaced it under `## Needs Christian` on its own without this retro's help.

  **Composition is healthy where last week's was not.** Last week's retro flagged "zero fresh feature or content work." That reversed: this week shipped [THR-733](https://linear.app/threadbare/issue/THR-733)'s four authored encounters (*The Gate Held*, *Two Roads Named*, *The Third Watch*, *The Quiet Offer*), the nudge card mechanics ([THR-1178](https://linear.app/threadbare/issue/THR-1178) / [THR-1179](https://linear.app/threadbare/issue/THR-1179) / [THR-1180](https://linear.app/threadbare/issue/THR-1180)), and the backstory strata expansion 459 → 687 variants ([THR-625](https://linear.app/threadbare/issue/THR-625)). The orchestrator's own independent split for the same window is **~48 product against ~4 process-infrastructure and ~5 wayfinder decisions**. Roughly 48% of Done still carries `Deferral`, consistent with prior weeks — but `Deferral` here mostly labels follow-on defect work spun out of shipped features, not process tidying, and the process-work throttle held: no lane filed a process ticket this week, and no process ticket was promoted into the starved shelf.

## Findings filed

**[THR-1190](https://linear.app/threadbare/issue/THR-1190/the-park-decay-remedy-is-a-christian-owned-linear-setting-with-no)** — *The park-decay remedy is a Christian-owned Linear setting with no ticket and no route to him.* Medium, `Todo`, Continuous Improvement.

A **park** (`assignee: null` ∧ `In Dev`) is the only shape `keep-work-flowing-cc` matches to surface finished-but-blocked work under `## Needs Christian`. Parks keep decaying — impediments #306, #380, #508, #607 and #657 (×2), six logged occurrences. The cause was settled on 2026-08-15: Linear's native GitHub integration re-assigns on PR **open**, and again on **merge**, whenever the PR names the issue id. CLAUDE.md now carries the authorial workaround, and impediment #657 is proof that a workaround requiring every future session to remember it does not always hold.

The gap is that the *structural* fix is a Linear settings toggle no agent can apply, and it has **never** appeared on `Design/user-actions.md` — verified with `git log -S"auto-assign" -- Design/user-actions.md` on `origin/ops`, which returns zero commits. It lives only in impediment rows and one comment thread on a parked issue, both invisible to the hourly briefing. Measured cost of the decay: THR-875 cleared by hand ~35 min late; THR-1168's question invisible ~47 min; #657 a full re-derivation of settled work by a run that then re-broke the park with the PR that logged it.

Filed Medium rather than High deliberately: the failure is real and recurring, but the fix is two minutes and everything else competing for Christian's attention this week is worth more of it.

## Clean checks

- **Ship mechanics: PASS.** Cross-referenced all 111 Done issues against line-anchored `Fixes|Closes|Resolves THR-NNN` commits on `main`. 102 matched directly; the 9 without were individually audited and are all legitimate — seven are `wayfinder:*` decision tickets (THR-974, 986, 1158, 1159, 1160, 1161, 1176) that resolve by recorded verdict rather than code, THR-1093 was last week's PR-#1114 cleanup whose whole scope was closing a PR and dropping a branch, and THR-1066 landed real code in `ccd40543`. **The inverse check is also clean:** zero closing keywords fired for an issue that is not Done, so no false-close, no THR-540 pattern.
- **Autoclose and CI: PASS, with one caveat now logged.** `linear-autoclose` ran 250 times in the window: 125 `pull_request` (0 failures) and 125 `push` (3 failures). The three push-path failures on 08-17 are benign and are impediment #692 below — every issue closed correctly. `ci.yml` over the same window: 229 success, 18 cancelled, 3 failure across 250 runs.
- **WIP and claim discipline: PASS.** Two issues sit `In Dev` — [THR-1130](https://linear.app/threadbare/issue/THR-1130/encounter-factory-pilot-volume-retrofit-the-15-nudge-era-encounters-to) and [THR-1168](https://linear.app/threadbare/issue/THR-1168/two-authored-encounter-audio-moments-have-no-live-caller-wire-or) — and both are correctly **parked** (`assignee: null`), which frees the WIP slot rather than consuming it. No double-assignment, no claim outliving its session, and no executor setting `state:"Done"` by hand.
- **Handoff quality: PASS, and notably good.** Every orchestrator T1 promotion posts a coordination block as the latest comment with all three lines and a reason per THR-688 rule B, plus its promotion evidence (`blockedBy` re-read live, plan-doc liveness, THR-990 standing-verdict check, shelf depth at scan). THR-1168's promotion comment goes further and names the boundary the executor must not cross — *"do not wire a 1.6s beat into the live commit path on your own judgement"* — which is exactly the level-system review gate working before the fact rather than after. No handoff was bounced by `/pull-work` for a missing block.
- **Checkpoint hygiene: PASS, with the best worked example the log has produced.** THR-1130 carries ten comments across four days and has not shipped its full scope — which the check flags as a split candidate. Reading it, it is not a stall: it ships in slices under sibling ids (batch 1 as THR-1131, the re-pass as PR #1528), each checkpoint states what shipped and what remains, and its one genuine process failure was caught and repaired *by the lane itself* — a park whose stated reason went stale was released 2.5 hours later by an attended session that wrote down why. It also contains a textbook THR-688 rule B mutex reversal, claimed past a mutex on evidence that the stated reason was verifiably inapplicable. No split recommended; the seams are already being cut the right way.
- **Deliberate-hold discipline: PASS, and worth naming.** `keep-work-flowing-cc` is currently *withholding* THR-1130's sample verdict from Christian — 37 hours and counting — because The Grateful Kin's payoff opens onto an unwritten room, and judging it today would be judging half a system. Today's grooming report lists the same item as an ask waiting on him. The briefing names the disagreement explicitly and states which call it is making. That is two lanes disagreeing in the open with a stated reason, which is the level-system gate (THR-1007 rule 5) behaving correctly, not a coordination defect.

## Handoffs to the Friday retro

- The Encounter Factory retrofit is 6 of 15 encounters through the line, with batches 2–3 gated behind a Christian verdict that is being deliberately withheld pending THR-1182. Whether that sequencing is right is a content-pipeline question, not a lane question.
- Two wayfinder maps ([THR-1157](https://linear.app/threadbare/issue/THR-1157), and the second on THR-902) have burned down every agent-doable ticket and now hold only human-in-the-loop frontier items. The wayfinder mechanism appears to work; whether the maps are converging on useful decisions is a product-health read.
- `check:typecheck` ratchet moved 3529 → 3178 over recent weeks (per THR-1130's evidence block). Direction is good; the Friday retro owns whether the remaining debt is being worked or just not grown.
- **`git worktree list` reports 102 registered worktrees**, the oldest (`kwf-2026-07-31-1610`) 19 days old, with 5 sitting on detached HEAD. The reaper's liveness guards were deliberately widened after THR-797, so under-reaping is the intended failure direction rather than a regression — and nothing lost work this week, so by the materiality bar this is explicitly *not* a ticket. Flagged for the Friday lens because THR-797's hazard is real and specific: `.claude/worktrees/` sits inside the home tree's working copy, so a worktree whose `.git` file is removed while a session still holds it silently retargets that session's git at `main`. 102 registrations is a large surface for that, and the accumulation rate (roughly one per hourly briefing run) means it grows on its own.

## Notes

- **Judgment call — the three autoclose failures got an impediment row (#692), not a ticket.** They meet the letter of the materiality bar (same failure 3× in a week) and fail its spirit: zero work was lost, the redundancy that absorbed them is deliberate and documented in the workflow's own header, and there has been no recurrence in the two days since against 122 push-path successes in the same window. Filing it would have put a fourth process ticket onto a shelf the orchestrator is correctly refusing to feed process work into. The row records the durable fix (wrap `compareCommits` in a try/catch falling back to `context.payload.commits`) for whoever wants it.
- **A methodology correction I nearly shipped.** My first ship-mechanics sample was the last 40 autoclose runs — 40/40 green, and I almost reported "PASS, 40/40." At 162 merges a week, 40 runs is about one day. The three failures were outside it. Recorded in impediment #692: sample the whole window explicitly, or a high-volume workflow's failures fall off the end of the default page.
- **Impediment #691 closes the open half of #195.** #195 (2026-07-21) recorded that a state-filtered `list_issues(state:"Done")` overflows the response budget and named the fix it wanted — a `fields` projection. That projection now exists and this run proved it: the default field set overflowed at 85,849 characters and went to disk, while the identical call with `fields:["completedAt","labels"]` returned every issue inline in one call. #195's disk-parsing workaround is no longer the standing advice. One trap recorded with it: the enum rejects `identifier`, which is the natural guess since the returned objects show `"THR-1183"` — the value lives on `id`.
- **Grey zone — the window boundary.** Last week's retro published at ~17:15 on 08-12 and counted through 08-11, explicitly excluding that day's activity. I counted from 08-12T00:00, so the six issues completed on 08-12 are in this window and in neither retro twice. The 111 is a hand-tally from the projected `completedAt` response, cross-checked by `comm` against the 102 line-anchored closers.
- **No queue mutation performed.** This retro claimed nothing, moved nothing, and closed nothing. THR-1190 was created in `Todo`, verified by `get_issue` re-query (impediment #48), and confirmed to carry no defaulted assignee (impediment #306 — THR-845's fix holds).
