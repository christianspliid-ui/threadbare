# Retrospective — 2026-08-14

## Period
From: 2026-08-07 (exclusive; last retro)
To: 2026-08-14

## Summary
- Drift-scan issues consumed: THR-911, THR-912, THR-913, THR-915, **and THR-747** — all five updated in place by today's scan (~14:55 UTC, before this retro; scan-before-retro ordering held). Second consecutive cycle of THR-756's dedupe-filing working: zero duplicate tickets, standing issues retitled/re-counted. **Last cycle's open tuning question is answered:** THR-747 (skill freshness) *was* refreshed this run — the scanner covers it again; no scanner ticket needed.
- Impediments logged this cycle: **102 entries / 128 occurrences, ~9.2h estimated loss.** (Deterministic draft via `npm run retro-draft`; the draft's own period header spans from 07-31 because its parser keys on the retrospective ledger, so this report's numbers were re-binned to entries dated 2026-08-08 → 08-14. Drafts are deliberately untracked; tables are cited by command, not path.)
- Top categories: tooling (45 entries / 51 occ), **false-signal (19/24 — new #2, almost all from one content campaign)**, testing (10/12), process (10/11), sandbox-limitation (2/11, dominated by one ×10 entry).
- Improvements implemented this session: **4 quick wins** (all effort-1 doc/skill edits).
- Improvements backlogged: _Filed in Step 9._
- **Previous retro's backlog: 2 of 2 shipped Done the same day they were filed** (THR-1019 at 19:12, THR-1020 at 16:29 on 2026-08-07). Every watch item from that retro also resolved favorably — see § Previous Retro's Backlog.

## Drift-Scan Signals This Cycle

| Issue | Signal | This week | Last week (08-07) | Direction |
|---|---|---|---|---|
| THR-911 | UL drift | 0 canonical-unused, 15 used-uncanonical | 0 / 15 | Stable |
| THR-912 | Rulebook → UL | 10 broken references | 10 | **Stable — the worsening halted** |
| THR-913 | Rulebook IMPL tags | 8 broken code references | 8 | **Stable — the worsening halted** |
| THR-915 | Interface-map coverage | Culture; 8 subsystems unaudited | 8 | Stable |
| THR-747 | Skill freshness | 2 stale, 0 archive-candidate | not refreshed since 07-24 | **Scanner refreshes it again ✓** |
| (THR-914) | Quick-ref vs rulebook | did not re-fire | closed 07-31 | Held |

**Signal health: working.** All five signals live and measured; the two rulebook-drift counts that grew for two consecutive cycles held flat this week. All five still sit in `Idea` at No priority — the standing decision surfaced to Christian on 08-07 (§ Needs Christian below) remains open, but with the counts stable the urgency is lower than last week's framing.

## Impediment Analytics

Re-binned to this cycle (entries dated 2026-08-08 → 08-14): 102 entries / 128 occurrences / ~551 min (~9.2h heuristic). Clusters by root cause:

| Cluster | Entries / occ | Exemplars | Disposition |
|---|---|---|---|
| `node_modules` install decay — both trees dead at once, installs not durable | ~14 / 19 | #431 ×3, #501–#503, #506, #510, #520 ×2, #536 ×3, #538, #540, #566 | **Backlog ticket T-A** — runbook fixes are exhausted; the wiper needs forensics |
| False-signal verification probes (THR-1101 prose campaign) | 17 / 20 | #552, #559 ×2, #560 ×2, #563 ×2, #565, #568, #569, #571–#573, #575, #577 | Quick win 4 (canon probe rules) |
| Unattended browser-verify structurally unreachable — `preview_start` refused outright | 2 / 11 | #546 ×10, #574 (+#570 attended-variant) | Quick win 2 (sanctioned jsdom substitution route); machine-check gap is THR-882 (Idea) |
| Full-suite false reds at the closeout decision | ~8 / 13 | #483, #516, #522, #527, #554 ×3, #562 ×3, #576 | Quick win 3 (triage protocol); the actual leak fixed (THR-1106 Done, sibling THR-1108 in Ready for Dev) |
| Linear MCP dead in scheduled runs | 3 / 5 | #141 ×3 (**Blocked**), #537, #544 | `cant-fix` repo-side; recovered by 08-13 — § Needs Christian |
| Mutex re-offer churn — honoured mutex released back to the queue | 1 / 2 | #551 ×2 | Quick win 1 (pull-work routing rule); the instance ticket (THR-1096) shipped Done today |
| Ticket-premise inversions caught by executor falsification | 5 / 5 | #500, #523, #524 (**L** — remedy would have destroyed 30 templates' endings), #555, #556 | Watch — the catching discipline is working; no gate proposal survives the materiality bar yet |
| CLI `aftermath pick` double-applies auto-resolved reactions | 2 / 2 (+#514 adjacent) | #553, #577 | **Backlog ticket T-B** |
| `preview_start` port class (continuing) | 2 / 2 | #490 (~11th of class), #495 | Absorbed by the 08-07 CLAUDE.md bullet; tool-level `cant-fix` |

Unresolved blockers (Workaround Found = No): **#141** — Linear MCP unauthenticated in scheduled runs has no in-run workaround at all; the 08-11 grooming lane no-opped entirely (×3). Auth had recovered by 08-13 (this run's board access is proof).

## Cross-Signal Patterns

1. **Every watch item from 08-07 resolved in the right direction.** The closeout-docs conflict cluster (#460/#461/#467) produced zero new entries after THR-1016's fragment closeouts merged — the fix landed. Judge-metrics resumed (3 new rows after a two-week silence). The modal-fighting cluster dropped from 7 entries to ~2 after THR-1019's `dismissBeats`/`suppressBeats` shipped, and the remaining entries (#519) document the lever's *deliberate* encounter-veil scope rather than a gap. No Write-tool NUL recurrence. A retro whose fixes all verify one week later is the loop working.
2. **The evidence machine is now the top friction source.** False-signal is the #2 category (19/24) and nearly all of it is one campaign's sweep probes measuring the wrong corpus shape, the wrong field names, or the wrong token semantics. The falsification discipline caught every one — but each probe paid tuition individually. Canonizing the probe rules where content authors start (quick win 4, `Docs/canon/prose.md`) is the cheapest transfer of that tuition.
3. **Environment decay escalated from "runbook broken" to "nothing to repair from."** Last cycle's fix rewrote the junction repair sequence; this cycle both donor and worktree were dead *simultaneously* four days out of seven, and #540 shows a successful `npm install` wiped back to zero packages mid-session. Documentation cannot fix this — something is deleting installs, and nothing yet identifies it. That is ticket T-A.
4. **The unattended lane's capability envelope shrinks one step earlier each week** (#493 pane capture → #499 pane navigation → #546 server start), and each discovery surfaced at the *end* of otherwise-finished work, ~10 min each. The route decision now sits at claim time (quick wins 1–2), which is as early as it can move.

## Previous Retro's Backlog (Rule 5)

| Issue | Status |
|---|---|
| [THR-1019](https://linear.app/threadbare/issue/THR-1019) browser-verify beat-clearing lever | Done (2026-08-07, same day) — modal-fighting cluster dropped 7 → ~2 entries this week |
| [THR-1020](https://linear.app/threadbare/issue/THR-1020) `check:armed-prs` reads the check rollup | Done (2026-08-07, same day, PR #1336) |

## Improvements Made This Session

1. **`.claude/skills/pull-work/SKILL.md` — honouring a mutex now routes the candidate to `Todo`** (#551 ×2): the mutex-liveness table's "serialize, bounce, continue" never said where the candidate goes, and the natural release back to `Ready for Dev` re-offered it hourly at ~4 min/run of claim-verify-release churn with no path to progress. New rule: verify the `blockedBy` relation, park in `Todo`, and let `tb-orchestrator` T1 promote when the partner clears. `last_validated_against` bumped. ROI 3×2/1 = **6**.
2. **CLAUDE.md § Browser-verify sanctioned routes + § Known Sandbox Limitations — the `preview_start`-refused case** (#546 ×10, #574): scheduled runs can be denied dev-server starts outright, which shuts the Playwright route too (it presumes a server). Sanctioned substitution: jsdom render assertions on the real component via `@testing-library/react`, recorded as `Browser-verify substitution: jsdom-render — unattended run, no startable dev server`; decide the route before claiming, not at the capture. ROI 4×2/1 = **8**.
3. **CLAUDE.md § Known Sandbox Limitations — full-suite false-red triage** (#483, #516, #522, #527, #554 ×3, #562 ×3, #576): the three false-red shapes (worker-contention flake on an unreachable file; worker-fork crash whose tell is a missing test name and an off-by-one file total; piped exit codes) plus the protocol — isolate-rerun and full-rerun before treating a closeout red as real, never pipe the gate run. ROI 4×2/1 = **8**.
4. **`Docs/canon/prose.md` — corpus verification-probe rules** (17 false-signal entries): probe the converted corpus (`ENCOUNTER_TEMPLATES`), never authored field names; pronoun tokens are actor-bound and `{es}` is not a token; removal passes need baseline set-diffs; CLI `aftermath pick` double-applies. Placed on the page every prose session loads at Step 0. ROI 5×1.5/1 = **7.5**.

Also verified rather than changed: THR-1096 — the concrete ticket churning in the #551 loop — shipped Done today on its own, so no board repair was needed beyond the skill rule.

## Improvements Backlogged

- [THR-1111](https://linear.app/threadbare/issue/THR-1111) — something is deleting `node_modules` out from under live sessions: instrument via session-precheck, identify the wiper (reaper / autosync / npm-across-junctions / OS / harness), ship one containment (impediments #431 ×3, #501–#503, #506, #510, #520 ×2, #536 ×3, #538, #540, #566; ROI 5×2/3 ≈ 3.3). Filed to Todo, Medium, coordination block as first comment.
- [THR-1112](https://linear.app/threadbare/issue/THR-1112) — CLI `aftermath pick` re-applies reactions that already auto-resolved, duplicating every effect kind (impediments #553, #577; ROI 3×2/1 = 6). Filed to Todo, Low, coordination block as first comment.

Deliberately **not** filed: the unattended browser-verify gap (#546 ×10, #574 — absorbed into the CLAUDE.md sanctioned-route quick win; the machine-check half is already THR-882, Idea), the leaked-timer required-check failures (#554 ×3 — THR-1106 already Done, sibling THR-1108 already in Ready for Dev), Linear MCP scheduled-run auth (#141 ×3 — `cant-fix` repo-side, recovered, surfaced in § Needs Christian), the `preview_start` port class (#490, #495 — tool-level `cant-fix`, documented 08-07), and the mutex re-offer churn (#551 ×2 — closed by the pull-work quick win; its instance ticket THR-1096 shipped Done today).

## Tuning Recommendations

1. **No signal-threshold changes.** All five signals are live, refreshed, and moving plausibly; the THR-747 refresh gap from last cycle self-resolved.
2. **Step 5c — intent-judge kill-criteria aggregation** (`Docs/judge-metrics/`, header-keyed parse; **14 rows**, up 3 from last cycle — the metrics half of the judge is writing again, closing last retro's "silently dark" concern):

   | Criterion | Value | Verdict |
   |---|---|---|
   | ≥95% Allow with zero overrides | Allow 78.6% (11/14) | Below the floor — **not** rubber-stamping |
   | ≥50% override rate on Revise/Block | — | **NOT MEASURABLE** (0 of 14 rows carry an override column) |
   | Median time-to-judgment >300s | 95s | Pass |
   | Zero marginal catches | — | User judgment, not row-derived |

   **Verdict: keep intent-judge.** One follow-through: the three new rows still lack the optional `Overridden` column the skill specifies, so the two override-keyed criteria remain unfalsifiable prospectively. If the next cycle's new rows still lack it, the judge's metrics-writing step should make the column mandatory (skill-update, effort 1) — noting it here as the trigger.
   Escalation rate 0.0% remains below the skill's own 0.05 floor — a tuning observation, not a kill criterion.

## Step 5b — Systemic Wiring Guide Audit

Healthy, maintained in-stride: THR-1110's new `attachment_grant` effect (merged this week) already has its own guide section (`### Granting any attachment: attachment_grant (THR-1110)`) plus a Part-5 inventory row. No undocumented capabilities found this cycle.

## Needs Christian

- **Standing from 08-07, still open:** the drift-scan signals ([THR-912](https://linear.app/threadbare/issue/THR-912), [THR-913](https://linear.app/threadbare/issue/THR-913)) remain at No priority and structurally unpickable. The pressure eased — both counts held flat this week — so this is a decision that can wait, but it is still a decision nobody has made: give the two rulebook signals a Low/Medium priority, or explicitly accept rulebook drift until the next rulebook-review cycle.
- **Linear MCP auth died in scheduled runs on 08-11** (impediments #141 ×3 — the grooming lane no-opped entirely; only Blocked-impact entries this cycle) and recovered by 08-13. Nothing to do now; if scheduled lanes go board-silent again, the fix is reconnecting the Linear connector on your side, and the lanes cannot do it themselves.

## Patterns to Watch

- **T-A forensics** — whether the `node_modules` wiper gets identified; until then every lane keeps paying ~4 min installs. If next week's log shows the class continuing at ≥3/week, escalate the ticket's priority.
- **THR-1108** (sibling unmount-timer leaks, Ready for Dev) — once merged, the required-check flake class from leaked timers should go quiet; #554-shaped entries after that mean more leaks, not the same one.
- **Judge `Overridden` column** — three new rows without it; next cycle's check is mechanical (see Tuning 2).
- **Ticket-premise inversions** (#500, #523, #524, #555, #556) — five catches in one week, all pre-damage. If one *lands* (a wrong remedy actually executed), that is the trigger for a design-session-side gate proposal; until then the executor-falsification discipline is the control.
- **`?spawn=` / `?outcome=` verification etiquette** — #486, #494, #519, #521 are all small frictions of the new encounter-review tooling; if they cluster again next week, a consolidated "encounter-review runbook" section in `Docs/canon/encounters.md` is the cheap fix.
