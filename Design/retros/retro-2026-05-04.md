# Retrospective — 2026-05-04

## Period
From: 2026-04-11 (exclusive, last final retro was `2026-04-11-retro-v2.md`)
To: 2026-05-04

*Note: Three weekly retros were missed (2026-04-17, 2026-04-24, 2026-05-01) because the `weekly-retro` scheduled task was never registered — root cause documented below.*

## Summary
- Drift-scan issues consumed: THR-294 (Done — UL drift 2026-05-01, resolved via PR #98)
- Impediments logged: 76 entries, 143 occurrences
- Total estimated time lost: ~10.6h (633 minutes)
- Top category: process-friction (27 entries, 37 occurrences)
- Top cluster by occurrences: sandbox search tooling / rg.exe EPERM (34 occurrences)
- Improvements implemented: 1 (retro report written; weekly-retro task registration blocked — see below)
- Improvements backlogged: 2 (register weekly-retro in interactive session; stagger pollers)

---

## Drift-Scan Signals This Cycle

**THR-294 — UL drift [2026-05-01]: 0 canonical-unused, 15 used-uncanonical** (Done, consumed)

The S2 UL drift scan fired on 2026-05-01 and identified 15 terms present in the codebase with no canonical UL entry. The top 5 by occurrence: `GameState` (413), `SphereName` (240), `ReachDomain` (233), `HexTile` (128), `GameView` (124). THR-294 was resolved the same day via PR #98 — 5 aliases, 6 promotions, 4 stopwords were added to the UL glossary.

**Signal health:** No red signals were missed; the single scan issue was triaged and closed. The UL scan appears healthy and well-calibrated. No tuning needed this cycle.

**Consecutive no-red-signal check:** N/A — one red signal was produced this cycle.

---

## Impediment Analytics

### By Category
| Category | Entries | Occurrences |
|---|---:|---:|
| process-friction | 27 | 37 |
| environment | 15 | 59 |
| dependency | 11 | 20 |
| api-quirk | 9 | 10 |
| code-bug | 6 | 6 |
| flaky-test | 2 | 3 |
| tool-failure | 1 | 3 |
| config | 1 | 1 |
| other | 1 | 1 |
| permission | 1 | 1 |
| skill-gap | 1 | 1 |
| unclear-requirements | 1 | 1 |

### By Impact
| Impact | Entries | Occurrences | Est. time (min) |
|---|---:|---:|---:|
| Blocked | 9 | 10 | 300 |
| L | 4 | 4 | 80 |
| M | 14 | 18 | 144 |
| S | 47 | 109 | 109 |
| Unknown | 2 | 2 | 0 |

### Top Root-Cause Clusters
| Cluster | Entries | Occurrences |
|---|---:|---:|
| Sandbox search tooling (rg/ripgrep unavailable) | 6 | 34 |
| Automation environment variable gaps ($CODEX_HOME) | 3 | 18 |
| Obsidian connector unavailable | 7 | 16 |
| Other process-friction | 26 | 36 |
| Linear MCP behavior quirks | 4 | 4 |

### Unresolved Blockers
| # | Description | Status |
|---|---|---|
| 14 | `QUINTESSENCE_ENCOUNTER_FAILURE_EROSION` never pushed by encounter resolution | No workaround found |
| 22 | Full npm test red on main (pre-existing baseline) | **Resolved 2026-04-28** (682/682 tests green) |
| 25 | No generalizable authored approach cards for `UnifiedActionTemplate` | No workaround found |

*Note: #22 is listed as a Resolution Candidate per the 2026-04-28 sprint wrap. Confirmed resolved.*

---

## Cross-Signal Patterns

**rg.exe × process-friction correlation:** The 34-occurrence `rg.exe EPERM` cluster is technically `environment` but generates downstream process-friction entries (agents switch to slower PowerShell scans, session startup delayed). The drift scan has no signal for this; it's a purely environmental constraint. No new action possible — CLAUDE.md documents the PowerShell fallback.

**$CODEX_HOME × Obsidian offline correlation:** Both are automation environment gaps, not codebase bugs. `$CODEX_HOME` (18 occurrences) is Codex-specific and outside CC's configuration scope. Obsidian MCP offline (16 occurrences) now has a filesystem fallback in the vault-log skill. Together these two clusters account for 34 occurrences but both have accepted workarounds.

**create_scheduled_task blocked × weekly-retro gap:** Impediment #90 (2026-04-25, 2 occurrences) and a new occurrence today (2026-05-04) confirm that `create_scheduled_task` cannot be called from within a scheduled task session. This is the direct root cause of the 3 missing weekly retros. No scan signal covers this; it's a platform constraint.

**Branch protection active × CC workflow:** Impediment #110 (2026-05-01) documents that direct `git push` to main was rejected. CLAUDE.md already reflects the PR-required workflow per the 2026-05-01 session. Scheduled tasks that previously pushed directly (flush-plan-docs) need to be updated — this may be a hidden regression.

**Scan vs impediments coverage gap:** The drift scan covers UL drift (S2). It does not cover: (a) scheduled task health, (b) direct push rejections, (c) rate-limit collisions. These patterns are high-recurrence but not drift-scan territory.

---

## Improvements Made This Session

1. **Retrospective report written** — `Docs/retrospectives/2026-05-04-retro.md` (this file). Covers 76 impediments over 23 days.
2. **Confirmed OBSIDIAN_VAULT_PATH already resolved** — `settings.local.json` already has `OBSIDIAN_VAULT_PATH: C:\Users\chris\Dev\Obsidian`. Impediment #109 is fully resolved.
3. **Confirmed CI timeout already present** — `ci.yml` has `timeout-minutes: 20` at job level. Impediment #94 is resolved.
4. **Verified weekly-retro task absent** — `mcp__scheduled-tasks__list_scheduled_tasks` confirmed the task does not exist. Root cause confirmed.
5. **Attempted task creation** — `create_scheduled_task` returned the expected error (`Cannot create scheduled tasks from within a scheduled task session`). Documented as new impediment entry.

---

## Improvements Backlogged

### 1. Register `weekly-retro` scheduled task — requires interactive CC session
**Fix type:** tool-config  
**ROI:** (5×4)/1 = 20  
**Why blocked:** `create_scheduled_task` is blocked inside scheduled task sessions (impediment #90).  
**Action:** In an interactive (non-scheduled) CC session, run:
```
create_scheduled_task(
  taskId: "weekly-retro",
  description: "Run weekly retrospective reading drift-scan issues + impediments log (Fridays ~1 hour after drift scan)",
  cronExpression: "0 17 * * 5",
  prompt: "Run the weekly retrospective. Invoke the retrospective skill via the Skill tool and follow it exactly. It will load this week's drift-scan-labeled Linear issues from Continuous Improvement as Step 0, then synthesize with Docs/impediments.md. Write output to Docs/retrospectives/YYYY-MM-DD-retro.md. Implement quick wins. Open Linear issues for larger improvements. Execute autonomously."
)
```
**Tracked in:** THR-297 (this issue — remaining acceptance criteria)

### 2. Stagger pollers to avoid Linear rate-limit collisions
**Fix type:** tool-config  
**ROI:** (2×3)/1 = 6  
**Observation:** Impediment #108 (2026-04-30) documents a 429 rate-limit hit on both the initial board scan and a retry. The `check-slack-for-new-dev-work` task runs at :30 (hourly). If Codex automation runs at similar times, concurrent scans can hit the rate limit. The fix is to ensure the two pollers don't fire within the same minute window.  
**Action:** Check `threadbearer-code-work` scheduled task cron — if it fires near :30, shift `check-slack-for-new-dev-work` to :45 instead. To be done in interactive session alongside the weekly-retro registration.

---

## Tuning Recommendations

**No tuning needed this cycle.** The single drift-scan signal (THR-294, UL drift) fired as expected and was resolved promptly. The scan cadence appears healthy.

Potential future signals worth considering:
- **Scheduled task health signal:** check whether known scheduled tasks (weekly-retro, flush-plan-docs) exist and have a recent `lastRunAt`. Would catch the weekly-retro gap automatically.
- **Dirty-workspace signal:** count how many CC sessions had to create worktrees due to dirty home. High counts indicate accumulated WIP debt.

---

## Patterns to Watch

1. **Scheduled task creation loop** — Any task that needs to be created/updated must be done in an interactive session. The current pattern of "register task from within a task" will always fail. Consider adding a CLAUDE.md note in the Session Workflow checklist that scheduled task registration is interactive-only.

2. **Codex workspace contamination** — Multiple Blocked impediments (#87, #88, #89) trace to the Codex executor finding extensive uncommitted changes in the home worktree. The pull-work skill's Step 4.5 worktree isolation solves this for CC, but Codex uses different infrastructure. This pattern may continue.

3. **Stale handoff comments** — Impediment #65 (2 occurrences) and #105 note that handoff comments can contain stale/non-existent file references or missing required coordination sections. Cowork should validate handoff blocks before moving to Ready for Dev or Ready for Codex.

4. **flush-plan-docs + branch protection** — Impediment #110 noted that direct pushes to main are now rejected. The `flush-plan-docs` scheduled task may fail when it tries `git push origin main` directly. This needs to be updated to use PR flow.
