# Workflow Retro — 2026-08-12

Covers 2026-08-05 → 2026-08-12. Compares against `Design/retros/workflow-retro-2026-08-06.md`.

## Needs Christian

**Nothing new — the one live event this week already reached you through the normal channel and appears resolved.** `tb-opus-pickup` (executor) and `tb-orchestrator` were both switched off from ~2026-08-11 21:26/22:01 through this morning — roughly 15-16 hours, correlating with your manual Ready-for-Dev purge that evening (9 canceled, 7 batched into THR-1089). `daily-backlog-grooming`'s 2026-08-12 report correctly read this as deliberate, not a fault, and the hourly briefing asked you directly whether to re-enable. Both lanes now show `enabled: true` and have already shipped two PRs (#1403, #1404) since resuming. No action needed from this retro; flagging only so the week's throughput dip has its cause on record.

## Throughput

- **86 issues reached Done** in the 7 days before this report (`completedAt` 2026-08-05 → 2026-08-11; today's activity isn't in this count). Down from last week's ~119, entirely explained by the lane freeze above eating the tail end of the window (5 Done on 08-11 vs. 13-18/day 08-06 through 08-10).
- **Composition:** `Deferral` 44/86 (51%), `Improvement` 30, `Infrastructure` 22, `Engine` 24, `UI` 33, `Content` 21 — still heavily process-weighted, consistent with the last two retros. See Handoffs below.
- **A major, deliberate pivot landed mid-week:** the entire Nudge Model WS5 Batch 1 family (epic THR-772, tracker THR-838, and 8 sub-batch children including THR-860/861/863/864/848/855/856/859) was Canceled/Done on 2026-08-10, superseded by the Encounter Factory pipeline (THR-1047, shipped 2026-08-09). This is a content-direction decision, not a process defect — each cancellation carries its reason, and the disposal is well-documented (see Clean checks).
- **Queue-depth trend: refilling, but 100% process.** Ready for Dev sat at 21 items pre-this-retro (now 22 after filing THR-1093 below) — every one is `Infrastructure`/`Improvement`/`Deferral`, zero fresh feature or content work. This is already surfacing correctly: today's briefing names it explicitly ("21 cleanup items with zero feature or content work") and points at THR-907/THR-974 (Christian's own encounter-slice verdicts) as the actual refill mechanism. Not a new finding — the detection is working, the fix is upstream (design capacity), same shape CLAUDE.md's Rule 0 anticipates.
- **Open-PR backlog: 1 open PR, now ticketed for cleanup** (#1114 — see Findings filed).

## Findings filed

**THR-1093** — "PR #1114 is orphaned — its issue was canceled 2026-08-10 with an explicit 'close it' disposition, and nothing tracks the cleanup." Low priority, Ready for Dev, Continuous Improvement project. THR-860 (the PR's issue) was Canceled with two separate comments instructing "close PR #1114, drop the branch" and explicitly noting this "stays executor work" — but the instruction lives only in a Canceled issue's comment thread, invisible to any pickup query. Two days on, still open. Filed as a single, fully-specified, five-minute action so it becomes claimable.

**Impediment #544 logged** (not a ticket — below the materiality bar as a one-off). The 2026-08-11 `daily-backlog-grooming` run found the Linear MCP connector unauthenticated, no-opped the whole lane, and drafted a row for `Docs/impediments.md` but couldn't land it (that file is `main`-only; the lane can't commit to `main` from the home tree). Its allocated id (#535) was claimed by a different impediment before a session with write access ran. Re-allocated and landed as **#544** this run, per the original report's own note ("carried here verbatim for the next run or the weekly retro to land").

## Clean checks

- **Ship mechanics: PASS.** `linear-autoclose.yml`: 30/30 recent runs succeeded. CI (`Test · Typecheck · Build`): 20/20 succeeded. Cross-checked all 86 Done issues against line-anchored `Fixes/Closes/Resolves THR-XX` commits: 81 matched directly. The 5 that didn't were individually audited and are all legitimate: THR-772 and THR-838's family closed as a program-epic/tracker disposal (bulk cancellation event, not code), THR-1039 closed on its own stated Done-when ("a findings comment," not code), and THR-1005/THR-1064 are "technical verdict" shell/not-a-defect closes — explicitly authorized in `daily-backlog-grooming`'s own skill prompt ("Technical verdicts (not-a-defect closes, CI assessments, stale no-ops) are agent calls... move the issue to the correct terminal state"). No false-close pattern found.
- **Checkpoint hygiene: PASS, and a clean worked example.** THR-1005 (aftermath auto-pop) ran its full checkpoint 1→2→3 arc exactly per protocol: two genuine defects found and shipped without closing keywords (deliberately, to avoid a premature auto-close), a 3rd-checkpoint split recommendation, a same-day orchestrator re-scope into THR-1017, both seams landing on `main`, and a same-week grooming close of the resulting shell — each step cited its evidence. This is the `MAX_CHECKPOINTS_BEFORE_SPLIT` mechanism working as designed, not a stall.
- **WIP/claim discipline: PASS** (with context). In Dev sat at 0 for most of the observed window — first as normal churn, then as the deliberate lane freeze above. No evidence of double-assignment or a claim outliving its session.
- **Handoff quality: PASS.** Sampled this week's Ready-for-Dev entrants (THR-1085, THR-1090, several WS5-successor-adjacent tickets): self-scoped process/Deferral tickets correctly omit a coordination block per THR-836 (claimable without one, `pull-work` Step 3 derives it at claim time); orchestrator-promoted tickets carry full blocks with reasons.
- **Last week's filed ticket shipped cleanly.** THR-1001 (pause-vs-outage marker) went Ready for Dev → In Dev → Done in under 3 hours on 2026-08-06, evidenced with two dry-run cases per its Done-when. Good corroboration the retro→ticket→ship loop works when a finding is real and well-scoped.

## Handoffs to the Friday retro

- The Nudge Model WS5 family cancellation (10 issues in one day) and its replacement by the Encounter Factory pipeline is a large content-pipeline pivot — worth the product-health/drift-scan lens, not re-litigated here.
- Composition of Done work remains ~51% `Deferral`-labeled this week, matching the pattern the 2026-08-10 materiality throttle was written to address. THR-1090 (retroactive materiality sweep for the already-queued shelf) is itself sitting in Ready for Dev, unclaimed — worth checking whether it ships before the pattern repeats a third week running.
- THR-1064's reversal (a "flip the label" verdict that turned out to rest on a backwards premise, corrected by re-deriving from code rather than trusting the brief — impediment #523) is a clean specimen for a "verify before implementing" pattern note, if the Friday retro is tracking that class.

## Notes

- **Grey zone — the lane freeze resolved while this retro was running.** `tb-opus-pickup` and `tb-orchestrator` showed `enabled: false` in the sources this retro read first (the 2026-08-12 grooming report, the current briefing) but `enabled: true` with runs already completed by the time this report was written — re-enabled and resumed within the same window. Reported as resolved as of now; the "Needs Christian" section above reflects that, not the earlier snapshot.
- **Judgment call — no ticket filed for the lane freeze itself.** It was correctly diagnosed as deliberate (not a scheduler fault), correctly escalated via the hourly briefing per THR-608, and self-resolved before this report published. The 2026-08-10 process throttle reserves ticket-filing for the weekly retro on a materiality bar; a deliberate, correctly-handled, already-resolved pause doesn't clear it the way THR-1001's genuinely-unexplained 62-hour silence did last week. If it recurs without a stated reason, that's the trigger.
- **Judgment call — investigated and dropped a suspected Rule-3 violation.** THR-1005/THR-1039/THR-1064/THR-772/THR-875 were Done without a line-anchored closing commit, which looked at first like a CC lane manually setting `state: "Done"` against the documented rule. Full audit (see Ship mechanics) found all five legitimate — two are program-container/epic disposals, one closed on a non-code Done-when, two are `daily-backlog-grooming`'s documented "technical verdict" closes. Noting the investigation here so it isn't invisible, since no finding survived it.
