# Workflow Retrospective — 2026-07-20

**Period covered:** 2026-07-04 → 2026-07-20 (17 days — the Monday runs of 07-06 and 07-13 did not fire; see Observations)
**Previous retro:** `Design/retros/retro-2026-07-03.md`
**Scope:** Scheduled autonomous run. Report-only — no Linear writes, no doc rewrites beyond this file.

> **Sources scanned.** `Docs/impediments.md` (current to #193, 2026-07-17 — used as source of truth), `Design/impediment-dashboard.html` (**file mtime 2026-06-12 — stale, 4th consecutive retro; fallback rule applied**), `Docs/project-status.md` (updated 2026-07-18), `Design/briefing.md` (generated 2026-07-18 19:28), live Linear via the main connector (Ready for Dev ×5, In Dev ×3), `mcp__scheduled-tasks__list_scheduled_tasks` heartbeats.
>
> **Data-quality note.** The main Linear connector authenticated and worked this run; the `plugin:productivity:linear` plugin remains unauthenticated (per-session noise + it blocked scheduled runs this window — see Top 3 #1).

---

## Top 3 impediments this week

1. **Linear OAuth outage in scheduled sessions stalled the whole autonomous pipeline for ~12 days (#143 count 12, #192, #193).** Shipping and impediment logging both go dark 2026-07-05 → 2026-07-17; #143 records the hourly CC pickups unable to board-scan, #192/#193 record daily grooming and an interactive design handoff blocked the same way. This is the compounding blocker of the window — one credential gated pickups, grooming, PM briefs, and handoffs simultaneously. It is working again (this run queried Linear live), but the duplicate unauthenticated plugin connector is still installed and was last retro's "optional cleanup" — no longer optional. *Next step:* Ask #1 — remove or reauthenticate `plugin:productivity:linear`; add a first-action Linear ping to scheduled task briefs so an auth outage is surfaced as a one-line finding instead of a silent 12-day stall.

2. **`check:skill-sync` false-positive family reached its 54th recurrence (#159/#165/#169–#189).** Every commit in the window — engine, content, docs-only alike — was blocked by the same pre-existing gitignored `image-manipulation-workspace/**/outputs/*.png` mirror drift. Executors now routinely reach for `--no-verify` (documented each time, but normalizing hook bypass is how a real sync regression eventually ships). The root fix (referenced repeatedly as THR-540) remains unshipped. *Next step:* Experiment #1 — scope the hook to staged skill files; it is a one-line guard and ends the largest single noise source in the log.

3. **DoD browser-verify is impossible in headless auto-pickup runs for WebGL-adjacent UI (#185, count 2).** The browser-pane screenshot hangs on the live WebGL render loop and Claude-in-Chrome has no connected browser in scheduled runs, so a DOM overlay on the game view cannot get its contractual 1920×1080 screenshot. Executors either stall or self-exempt ad hoc. *Next step:* Experiment #4 — codify a deferred-screenshot evidence path so the DoD stays binary without blocking headless lanes.

---

## Status of active impediments

Ages relative to 2026-07-20. Stale (>7 days, no movement) marked ⚠.

| # / ref | Title | Age (days) | Owner | Status | Next action |
|---------|-------|-----------:|-------|--------|-------------|
| 143/192/193 | Linear OAuth blocks scheduled sessions (12+ recurrences) | 16 | Christian | Working this run; plugin still unauthenticated | Ask #1: remove/reauth the plugin; add auth ping to task briefs. |
| 169–189 | `check:skill-sync` false positive ×54 | 22+ ⚠ | CC (THR-540) | Open, worked around every commit | Experiment #1: staged-files guard; ship THR-540. |
| 185 | Headless DoD screenshot impossible on WebGL surfaces | 3 | CC / process | Open, 2 hits | Experiment #4: deferred-screenshot evidence path. |
| 184 | `lint:plan-doc` flags non-dated reference docs in `Docs/plans/` | 3 | CC | Open | Experiment #3: gate lint on `YYYY-MM-DD-` filename pattern. |
| 188 | Sandbox clock ~8h behind host — wrong timestamps in stamped docs | 2 | CC / process | Open, workaround known (cross-check scheduler clock) | Note in Known Sandbox Limitations at next CLAUDE.md touch. |
| 190 | Preview-harness proxy port denied; underlying Vite port works | 2 | CC | Workaround found | Log-only; close if quiet next retro. |
| — | Impediment dashboard stale (mtime 06-12) | 38 ⚠ | Christian decision | **Slipped 4th consecutive retro** | Ask #2: formally retire it (recommended). |
| — | Duplicate impediment row numbers (#180 ×2, #184 ×2, #185 ×2) | 15 ⚠ | Cowork/CC grooming | Slipped from last retro (E5) | Experiment #2: one renumber pass + "next free #" header line. |
| — | THR-616 In Dev with stale "blocked" text (blocker THR-615 shipped 07-05) | 15 ⚠ | CC | Detected by briefing | Free to pull; fix the description line at pickup. |
| — | `Design/briefing.md` hourly refresh silent since 07-18 19:28 | 2 | CC lane / Christian | Machine likely off over weekend | Experiment #5: liveness wording for THR-652's gate. |
| 75 | Obsidian MCP decision (auto-start vs bless fallback) | ~87 ⚠ | Christian | 4th retro carrying it | Ask #3 — or auto-close: THR-651 already routes CC vault work via filesystem; recommend blessing the fallback and dropping this line forever. |

---

## What shipped since last retro

Despite the 12-day scheduled-lane stall, the interactive + CC lanes delivered a structural milestone: **the Pure Claude Code Migration (Phases 0–1)** — THR-648 (Cowork memory → `Docs/ways-of-working.md`), THR-649 (`design-session` skill: CC commits its own plan docs, no flush pipeline), THR-650 (`keep-work-flowing-cc` — hourly committed `Design/briefing.md` replaces the Cowork chat brief, **includes the freshness ping = last retro's Experiment #1, landed**), THR-651 (skill-tree audit: 2 skills ported, 6 marked for retirement). THR-652 (parallel-run go/no-go gate, Urgent) is In Dev.

Also shipped: THR-575 (CLAUDE.md diet, PR #588 — the parked human-gate deadlock from last retro resolved via chat review), THR-636 (encounter context UX), THR-637 (Entity Visual Header), THR-639/640 (action unlock reveal, foreshadowing Phase C), THR-611 slices 1–5 (Divine Economy essence sources), THR-613 slices (player action progression), THR-615 (Mortal Economy P1), THR-609 content-eval waves. Full suite is green at **12,463 tests**.

From last retro's remediation list: **worktree pruning is now automated** — the `weekly-worktree-hygiene` scheduled task exists and ran 07-19 (fulfills THR-410, attacks the root cause of the sync-corruption family). `daily-backlog-grooming` is re-enabled and ran today.

---

## What slipped

1. **Dashboard file-or-demote (E2) — fourth consecutive slip.** The retro task brief gained a fallback clause (partial credit), but the artifact itself is 38 days stale and no Linear issue was filed. Same root cause as the last three slips: it never entered the Linear loop.
2. **Design-close guard (E3) did not land.** No trace of "a design issue may not close without a named implementation issue" in CLAUDE.md or the coordination protocol — it was folded into THR-575's scope on paper, but the shipped diet doesn't contain it. The THR-570-style hole is still open.
3. **Config-integrity check in `session-precheck.ts` (E4) did not land.** No `CONFIG CORRUPT` marker in scripts. Mitigated indirectly by worktree pruning (the root-cause load on the sync layer), so downgraded, not dropped.
4. **Duplicate impediment numbering (E5) not fixed** — and the collision family grew (#180, #184, #185 each used twice).
5. **Two retro cycles missed** (07-06, 07-13) — the task is enabled and cron'd Mondays 09:04, but nothing fired between 07-03 and today; catch-up burst timestamps this morning suggest the machine was off/asleep at the scheduled hour on those Mondays.

**Slip pattern, refined.** Last retro's law — *what isn't in Linear doesn't happen* — held exactly: E1 landed because it rode a Linear issue (THR-650); E2–E5 slipped because they were consented in a report and never filed. This retro files its experiments as explicit next actions with named owners, and the "consented ⇒ filed before run-end or it isn't consented" rule from last retro must actually be honored — this run cannot write to Linear (auth for filing writes is available but the task is report-only), so the experiments below are written as ready-to-file issue one-liners for the next grooming pass to create verbatim.

---

## Workflow observations

- **The pipeline has a single point of failure: one OAuth token.** The 07-05→07-17 gap shows queue depth, briefs, grooming, and pickups all share the Linear credential. Detection took days because no scheduled task treats "Linear unreachable" as a first-class reportable outcome — each run just failed silently. Cheap fix: every scheduled brief opens with a one-line Linear reachability verdict.
- **Hook noise is teaching agents to bypass hooks.** 54 skill-sync false positives, plus `lint:plan-doc` (#184) and `check:wiki-freshness` (#180 first entry) false positives, mean nearly every commit in the window carried either a sync-workaround or `--no-verify`. The hooks' credibility is the casualty; a real failure will hide in the wolf-cries.
- **The briefing system worked, then went quiet.** `Design/briefing.md` correctly caught a stale-blocked issue (THR-616), home-tree drift, and clock skew — real PM value — but its heartbeat depends on the machine being awake, which also explains the missed retros. Scheduled-lane liveness ≡ machine uptime is now the operative constraint for THR-652's "hourly for 2 consecutive days" evidence.
- **WIP is 3 In Dev vs the limit of 1** (THR-665 assigned to Christian, THR-652 gate, THR-616 stale-parked). Likely interactive/parked overlaps rather than protocol regression — same shape as last retro's WIP=2 — but the limit is now more honored in the breach; either the limit or the parking convention should be restated.
- **Queue health is good:** 5 Ready for Dev (2 High from Christian's 07-19 playtest — modal auto-pause THR-668, toast threading gate THR-666), nothing starved. Christian's playtest→issue loop is producing well-specified, executor-ready tickets — the best-shaped intake the queue has had.

---

## Design-council pass — consented experiments (small, reversible)

Four voices: **Ops**, **Engineer**, **PM**, **Tech Lead**.

*Ops:* "Twelve days of silence because a token expired and nothing was contracted to say so. I don't want a monitoring platform — I want the first line of every scheduled run to be 'Linear reachable: yes/no', and 'no' to land in the briefing's Needs-Christian block. One line per task brief."

*Engineer:* "The skill-sync hook diffs the whole tree instead of the staged set — that's the entire bug. Guard it with `git diff --cached --name-only | grep skills/` and 54 recurrences become zero. Same disease in `lint:plan-doc`: gate on the dated-filename pattern. Both are one-liners with instant rollback."

*PM:* "Four retros, one dashboard, zero regenerations — the verdict is in: retire it. The retro already reads the log directly; keeping a lying artifact costs a paragraph of every report and a moment of every reader's trust. And the council rule from last time only worked when the experiment rode a Linear issue — so this time every experiment below IS a ready-to-file issue line."

*Tech Lead:* "Two things guard the migration. First, THR-652's evidence says 'hourly for 2 consecutive days' but the lane sleeps with the machine — restate it as 2 consecutive machine-awake days or the gate can never pass honestly. Second, the design-close guard we consented last time silently fell out of THR-575's scope; re-file it as its own 2-line issue so it can't be scope-dieted away twice."

*Consensus:* six experiments, each a one-liner or single filing, all reversible.

**Consented actions** (each line is the issue to file at next grooming — Continuous Improvement project unless noted):

1. **Scope `check:skill-sync` to staged files.** Pre-commit hook exits 0 unless `git diff --cached --name-only` touches `.claude/skills/` or `.agents/skills/`. Supersedes/absorbs THR-540. *Owner:* CC. *Reverts:* remove the guard.
2. **Impediment-log renumber pass + header convention.** Fix #180/#184/#185 collisions; add a "next free #: NNN" line under the log header so appends stop colliding. *Owner:* CC (docs-only). *Reverts:* n/a, append-only log.
3. **`lint:plan-doc` dated-filename gate.** Lint only files matching `Docs/plans/YYYY-MM-DD-*.md`; reference docs (systemic-wiring-guide, wiring-checklist, `_template`) pass untouched. *Owner:* CC. *Reverts:* remove the filename test.
4. **Headless browser-verify fallback in the DoD.** When no browser is reachable in an automated run: unit-render coverage + `__DEBUG` state assertion + explicit `Browser-verify deferred: headless lane` in the commit body, and the next interactive session owes the screenshot (tracked on the issue). *Owner:* CC via CLAUDE.md §Definition of Done edit. *Reverts:* delete the clause.
5. **THR-652 evidence wording.** Change "hourly for 2 consecutive days" to "hourly across 2 consecutive machine-awake days (gaps while the machine is off do not reset the clock)". *Owner:* whoever holds THR-652. *Reverts:* trivial.
6. **Re-file the design-close guard** (fell out of THR-575): "A design-deliverable issue may not move to Done unless its implementation issue exists and is linked" — one line in the coordination protocol + CLAUDE.md. *Owner:* CC. *Reverts:* delete the line.

**Not picked.** *Auto-reauth for Linear plugin* — not technically possible headless; the fix is removal (Ask #1). *Machine-uptime automation* (wake timers etc.) — Christian's hardware, his call (Ask #3). *Config-integrity precheck (old E4)* — deprioritized: worktree pruning removed the load that caused the corruption; revive only if truncation recurs post-pruning.

---

## Ask of the user (Christian)

Plain-language decisions, each with a recommendation:

1. **Remove the duplicate Linear connection?** You have two Linear hookups; the extra one (`plugin:productivity:linear`) has been asking to log in for weeks and was part of why the automated helpers went quiet for ~12 days in early July. The main one works. **Recommendation: remove the extra plugin** (Settings → connectors). If you'd rather keep it, log in once interactively and the nagging stops.
2. **Retire the impediment dashboard?** The HTML dashboard hasn't been regenerated since mid-June and the retro now reads the log directly. **Recommendation: yes, retire it** — say the word and the next session marks it deprecated and stops reporting on it. This is its 4th consecutive stale retro.
3. **Is the weekend pause OK?** The hourly automation and these Monday retros only run while your PC is awake — two retro Mondays and a weekend of briefings were skipped because it wasn't. **Recommendation: accept it** (it's a solo project; weekend silence is fine) and we bake that into the migration gate's wording (Experiment #5). If you'd rather have true always-on automation, that's a hardware/hosting conversation.
4. **Is THR-665 yours on purpose?** The thread-tug rework is sitting In Dev assigned to you. If you meant to hand it to the executor, it should be unassigned and put back in Ready for Dev; if you're mid-testing it yourself, all good — just say so, so the WIP count reads clean.
5. **(Carried, last time we'll ask) Obsidian MCP:** the executor now writes to the vault via the filesystem path and it works. **Recommendation: bless the fallback permanently** and this item disappears from retros for good.

---

## Summary — three things to act on this week

1. **Silence the false-positive hooks** (Experiments #1 + #3): two one-line guards end the 54-recurrence skill-sync family and the plan-doc lint noise — the biggest per-commit time tax and a growing `--no-verify` habit that will eventually mask a real failure.
2. **Make auth outages loud** (Ask #1 + the Ops one-liner): remove the duplicate Linear plugin and put a "Linear reachable: yes/no" first line in every scheduled brief, so the next credential expiry costs one hour, not twelve days.
3. **Protect the migration gate** (Experiments #5 + #6): restate THR-652's evidence in machine-awake terms and re-file the design-close guard — both guard the Pure-CC migration, which is this month's structural win and deserves an honest go/no-go.

---

*Retro run 2026-07-20 via the `weekly-workflow-retro` scheduled task (first firing since 07-03 — two Monday slots missed while the machine was off). Autonomous run — no user present. Impediment dashboard stale (fallback rule applied; log read directly). Main Linear connector reachable this run; `plugin:productivity:linear` unauthenticated. Report-only: no Linear writes; consented experiments are written as ready-to-file issue lines for the next grooming pass.*
