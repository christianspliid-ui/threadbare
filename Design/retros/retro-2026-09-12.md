# Retrospective — 2026-09-12

## Period
From: 2026-09-06 (exclusive; last retro)
To: 2026-09-12

**Draft-period note:** `npm run retro-draft` reported "From: 2026-07-31" (657 entries) because its period marker was the retired impediment-log footer (THR-825) — the last footer it can see predates six committed retros. Re-binned by hand to entries #975–#1020 (dated after the 09-06 retro's cut). The draft's parsing was otherwise sound and was used as the entry index; deleted after commit per THR-1056.

## Summary
- Drift-scan issues consumed: **THR-911, THR-912, THR-913, THR-915** (standing signals, re-fired by update 2026-09-11). The scan lane is healthy — six consecutive weekly green runs.
- **The retro lane's Linear access is restored** — last cycle's blocking gap (#974: unauthenticated MCP, could neither read the scan nor file its ticket) is gone; this run read the board and files its tickets directly. The 09-06 "Needs Christian" item is discharged.
- Impediments logged this cycle: **46 entries / 55 recorded occurrences, ~125 min (~2.1 h) heuristic loss.** Impact: 10 × M, 45 × S (occurrence-weighted), zero L, zero Blocked — the first cycle in five with no L-impact entry.
- Top categories by entries: tooling 16, testing 7, verification 5, plan-accuracy 5, engine 3, process 3, seven singletons.
- Improvements implemented this session: **4 quick wins** (pull-work line-anchored upstream-shipped confirm; pull-work `blockedBy` liveness dispositions; protocol-doc Rule C reachability corollary + a seven-week-stale Rule C constraint corrected; sandbox-limitations Playwright artifact-path bullet) plus one memory update.
- Improvements backlogged: _Filed in Step 9._
- **Previous retro's backlog: the one drafted ticket (UL dashboard status parser) was NEVER FILED** — no Linear-capable session picked it up in six days, confirmed by board search. Filed by this run in Step 9 verbatim. Rule-5 lesson: "any Linear-capable session files it" is a hope, not a mechanism; the retro that drafts an unfileable ticket should also be the retro that checks and files it, which is what happened here.
- **Previous retro's quick win 2 did not hold:** the claim-time browser-verify route rule (added to pull-work Step 3 on 09-06 after #946's third recurrence) was bypassed again on 09-10 — #1011 ×4, route decided at the capture, implementation already complete. Two prose fixes have now failed; this cycle's response is a mechanical trigger (ticket T-2).

## Drift-Scan Signals This Cycle

All four standing signals re-fired 2026-09-11 (the scan updates its standing Idea-state issues in place). Trend against their last-read values (2026-07-31 titles):

| Signal | Issue | Was | Now | Trend |
|---|---|---|---|---|
| UL drift | [THR-911](https://linear.app/threadbare/issue/THR-911/drift-scan-2026-09-11-ul-drift-0-canonical-unused-15-used-uncanonical) | 6 canonical-unused / 15 used-uncanonical | **0** / 15 | Canonical-unused cleared; used-uncanonical stable (all 15 are code identifiers — `ActionStep`, `TraceEntry` etc. — a known-shape candidate list, not drift) |
| Rulebook → UL | [THR-912](https://linear.app/threadbare/issue/THR-912/drift-scan-2026-09-11-rulebook-ul-9-ul-references-broken-in-rulebook) | 8 broken refs | **9** | Creeping up |
| Rulebook IMPL tags | [THR-913](https://linear.app/threadbare/issue/THR-913/drift-scan-2026-09-11-rulebook-impl-tags-8-impl-tags-with-broken-code) | 6 broken | **8** | Creeping up — identifiers drift as engine files move |
| Interface-map coverage | [THR-915](https://linear.app/threadbare/issue/THR-915/drift-scan-2026-09-11-interface-map-coverage-audit-cross-system) | 11 subsystems unaudited | **5** | Halved — audits are landing |

None promotes: no signal records a demonstrated loss (the materiality bar), and all four sit in `Idea` by the 2026-08-10 throttle's design. The two creeping counts (THR-912/913) are cheap mechanical sweeps whenever a session is already in the rulebook — noted as a Patterns-to-Watch line, not tickets.

## Impediment Analytics

Entries #975–#1020, dated 2026-09-06 (post-retro) → 2026-09-11.

### By Category
| Category | Entries | Note |
|---|---:|---|
| tooling | 16 | Shell-escaping/heredoc corruption (#982, #987, #1012), python-hang tallies (#995 ×2, #1018), Playwright home-tree artifacts (#1000, #1020 ×2), Linear `#N`-mangling (#1017), classifier refusals (#1016), one shared-worktree git incident (#989 M) |
| testing | 7 | The vacuity discipline's long tail — wrong-layer pins, portal `not.toMatch`, contention flakes |
| verification | 5 | #1011 ×4 (route-at-capture again), #1005 (unreachable organic Done-when) |
| plan-accuracy | 5 | #983/#984 (THR-1430's Reach/Sphere axis + unreachable threshold), #990 (DELETE list vs later adopter), #996, #1002 |
| engine / process | 3 + 3 | API-semantics traps; #1015 (upstream-shipped grep false positive), #1019 ×2 |
| singletons | 7 | ticket-authoring #978, process-friction #977, linear #991, wiring #980, plan-quality #981, git #1008, engine-api #1013 |

### By Impact
| Impact | Occurrences | Ids |
|---|---:|---|
| M | 10 | #980, #981, #983, #984, #989, #990, #994, #998, #1005, #1015 |
| S | 45 | rest (multi-count: #992, #995, #1007, #1009, #1019, #1020 ×2 each; #1011 ×4) |
| L / Blocked | 0 | — |

Unresolved blockers (Workaround Found = No): none.

### Root-cause clusters

| Cluster | Volume | Exemplars | Disposition |
|---|---|---|---|
| **Plan/ticket-premise defects caught by executor falsification** — the cycle's headline by weight | 9 entries, 4 × M | #983 (plan said "Shadow" meaning the Reach, code read the Sphere — orthogonal axes, both `Record<string,number>`, empty result not error); #984 (0.35 capability gate unreachable for trait-less NPCs — the sigmoid is near-binary across the population); #990 (a DELETE list authored 08-26 named machinery the undertaking grid adopted 09-04 — executing verbatim deletes a live system); #981 (plans keep targeting `AgentDetailPanel.tsx`, mounted nowhere since the modal cutover); #977, #978, #986, #996, #1002 | Catch rate still 100% — every one was caught pre-damage by the executor's falsification habit, at S/M cost. Standing trigger unchanged: first *landed* wrong remedy converts to a gate proposal. The authoring-side share (Done-when reachability, grep-vs-predicate) is now written into the protocol doc — **quick win 3** |
| **Verification-route rules bypassed at the moment they bind** | #1011 ×4, #1019 ×2 | The claim-time browser-verify rule exists in verification-gates.md *and* (since 09-06) pull-work Step 3, and a run still implemented first and discovered the `preview_start` refusal at the capture; THR-1463's Done-when required `__DEBUG` on the prod bundle where it is tree-shaken out | Two failed prose fixes ⇒ mechanical trigger: **backlog T-2** (`classify:diff` prints the route reminder for UI-pillar diffs). The Done-when half is **quick win 3** |
| **Upstream-shipped grep false positive** | #1015 (M) | `--grep "Fixes THR-1155"` matches slice commits whose body *disclaims* the keyword (`` `Fixes THR-1155` rides slice 3, not this commit ``) — a slice-shipping ticket reads as already-Done at pickup | **Quick win 1**: pull-work now requires a line-anchored confirm per candidate SHA (THR-738's own predicate) at all three grep sites |
| **Board-shape deadlocks via relations** | #991 | T1 promoted THR-1303 while its `blockedBy` partner THR-1301 sat parked in `Todo` — THR-908's deadlock shape through the relation rather than a mutex line | **Quick win 2**: pull-work's liveness dispositions now apply to bare `blockedBy` relations, including promote-or-claim-the-partner on a `Todo` partner |
| **Test pins at the wrong layer / vacuous arms** | 7 entries (testing) + #997, #999 | #998 (M — forecast pinned against "the single source of truth" that two downstream floors modify); #1009 ×2 (`not.toMatch` on a portalled component asserts nothing); #1014 (falsification arm at wrong nesting); #992, #1007 ×2, #1010 | The discipline's long tail — each documented in the log with its discriminator; memory corpus already carries the family. No new rule clears the bar this cycle |
| **Shared-worktree git incident** | #989 (M) | A subagent ran `git stash`/`pop` in a shared multi-agent worktree despite a "no git" instruction; the aborted pop reverted four agents' tracked edits | Single occurrence. The fix (tell subagents *why*, give a read-only alternative) is orchestration-prompt-side; watch for recurrence before writing a rule |
| **Multi-seed single-process contamination** | #994 (M) | Module-level state survives the reset helpers, so seed 99's census reported seed 42's world leaking through — reads exactly like the engine hole under investigation | Documented in the log with the one-seed-per-process rule; candidate for testing-patterns if it recurs |
| **Mapped `cant-fix` tallies** | ~9 entries | python heredoc hangs (#995 ×2, #1018 — 6th/7th recorded), heredoc/escaping corruption (#982, #987, #1012), Linear `#N` auto-linking (#1017), attended classifier vs `.claude/skills/` (#1016) | Tallies; no new information. #1020's Playwright path quirk was the one unmapped member — **quick win 4** adds it to the catalog |

## Cross-Signal Patterns

1. **The premise-defect class has moved upstream of the executor and stalled there.** Nine entries this cycle are variations of "the ticket/plan asserted something the codebase no longer (or never) satisfied", and all nine were caught by executor falsification at low cost. The catch machinery works; what does not exist is anything that makes *authoring* cheaper to get right. This cycle's contribution is Rule C's corollary (constructed arms, surface-matched levers, predicate-over-grep) — the first authoring-side rule the class has produced.
2. **Rules keep failing at the moment they bind; printed triggers are the next rung.** #1011 is the third impediment in three cycles about the same written rule not being read at the right moment, *after* the rule was moved to where the session already is (claim step). The remaining escalation is output the session cannot not-see — a line printed by a gate it already runs. T-2 tests that theory on the cheapest possible surface (`classify:diff`).
3. **Volume collapsed and flattened.** 46 entries / ~2.1 h against last cycle's 47 / ~3.6 h over a shorter window, with zero L-impact and zero Blocked. The heavy classes of the last two cycles (CI budget decay, claim integrity, stub arrivals) are all quiet — their fixes are holding. What remains is a wide, flat field of S-impact tool semantics, which is what the log looks like when the delivery machine is basically healthy.
4. **Two-sided staleness in the governance docs themselves.** The protocol doc's Rule C constraint said "until THR-689 ships" seven weeks after it shipped — the exact drift shape the guidance program exists to catch, sitting in the coordination protocol. Fixed in-line this run. A reminder that the retro's own reading pass is part of the audit surface.

## Guidance Audit

Doctrine versions this retro (from `Docs/guidance-manifest.json`): **prose@2**, **encounter-authoring@1**, **identity-vision@1**, **cosmology@1**, **rules-of-play@1**, **ui-laws@1**, **architecture@1**, **coordination-process@1**.

Ran: **no — no version movement since 2026-09-06; last audited baselines unchanged** (prose audited 2026-08-28; the seven others registered 08-28 → 08-30, registration = baseline). The monthly window opens ~2026-09-28: the next retro (~09-18/19) is still inside the month for all eight, so barring a version bump the ~09-25/26 retro runs the audit — and should budget for eight doctrines or split them across two runs, as flagged last cycle.

## Improvements Made This Session

1. **[`.claude/skills/pull-work/SKILL.md`](../../.claude/skills/pull-work/SKILL.md) — upstream-shipped grep hits must be confirmed line-anchored** (#1015, M): Step 3.5 now applies THR-738's own predicate (`^(Fixes|Closes|Resolves) THR-N\s*$` on the commit body) to every `--grep` hit before ruling a ticket shipped, with pointer sentences at the Step 1.7-resume and Step 4.4 sites. Prose that names the keyword to *disclaim* it (slice-shipped tickets do this deliberately) no longer parks a live ticket. ROI 3×4/1 = **12**.
2. **Same file — the mutex-liveness dispositions now cover bare `blockedBy` relations** (#991): partner Done → spent; partner In Dev/RFD → bounce to Todo; partner Todo/Backlog → promote or claim the *partner* and park the candidate per THR-846. Kills the THR-908 deadlock shape arriving through the relation. ROI 2×3/1 = **6**.
3. **[`Docs/plans/2026-04-13-linear-coordination-protocol.md`](../../Docs/plans/2026-04-13-linear-coordination-protocol.md) § Ticket-authoring rules — Rule C corollary: Done-whens are reachable by construction, on the surface they name** (#1005 M, #1019 ×2, #978): organic observations need a known-supplied precondition or a constructed arm; `__DEBUG` levers imply a dev build, never prod; a supplied grep is a snapshot, the predicate is the membership. Also corrected the Rule C standing constraint, which still said "until THR-689 ships" seven weeks after it shipped. ROI 3×3/1 = **9**.
4. **[`Docs/ops/sandbox-limitations.md`](../../Docs/ops/sandbox-limitations.md) — Playwright MCP artifacts land in the home tree root** (#1000, #1020 ×2): relative `filename`s ignore the session cwd; pass absolute paths, sweep debris at closeout. ROI 3×2/1 = **6**.

Session-local: memory `reference_browser_mcp_from_cc_debug` extended with the Playwright artifact-path fact.

## Improvements Backlogged

_Filed in Step 9._

## Tuning Recommendations

1. **No drift-scan threshold changes.** All four signals were consumed for the first time in three cycles; the two creeping counts (THR-912/913) are trend, not threshold, problems.
2. **`retro-draft`'s period detection is broken by design since THR-825** — it keys off the retired impediment-log footer, so every draft since 07-31 has reported a six-week period and every retro has re-binned by hand (~10 min/run). Fix: derive the period from the newest committed `Design/retros/retro-*.md` filename instead. Below the materiality bar as a ticket on cost, but it is a one-line change in `scripts/retro-draft.ts`; noted here so any session already in that file takes it. Three consecutive retros have now paid the re-bin cost — if a fourth does, it clears the ≥3-recurrence bar and the next retro files it.
3. **Step 5c — intent-judge kill-criteria aggregation** (`Docs/judge-metrics/`, header-keyed parse; **23 rows — unchanged since 09-06**, no new judgments this cycle):

   | Criterion | Value | Verdict |
   |---|---|---|
   | ≥95% Allow with zero overrides | Allow 78.3% (18/23) | Below the floor — not rubber-stamping |
   | ≥50% override rate on Revise/Block | 5 Revise/Block rows, 0 overridden | Far below the kill threshold |
   | Median time-to-judgment >300s | 121 s (n=22) | Pass |
   | Zero marginal catches | — | User judgment, not row-derived |

   **Verdict: keep intent-judge.** No new data this cycle (no plan docs judged — consistent with a week of executor-heavy work); numbers identical to 09-06.

## Patterns to Watch

- **`classify:diff` route trigger (T-2) is a theory test** — if #1011's class recurs *after* the printed reminder ships, the failure is not discoverability at any layer and the next step is a hard gate, which needs Christian-level cost/benefit discussion first.
- **Premise-defect class**: standing trigger unchanged — the first *landed* wrong remedy converts the falsification habit into a gate proposal. Nine catches this cycle, all pre-damage.
- **Rulebook drift counts creeping** (THR-912: 8→9, THR-913: 6→8): cheap to sweep whenever a session is in the rulebook; if either crosses ~12 the sweep stops being incidental-sized.
- **Shared-worktree subagent git** (#989): one M-impact occurrence; a second converts to an orchestration-prompt rule ("state *why* git is forbidden + sanctioned read-only alternative").
- **`retro-draft` period detection** (see Tuning 2): fourth hand re-bin files the ticket.
- **Guidance audit ~09-25/26**: eight doctrines come due together; budget or split.
