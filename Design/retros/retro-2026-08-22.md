# Retrospective — 2026-08-22

## Period
From: 2026-08-14 (exclusive; last retro)
To: 2026-08-22

**Cycle note:** the implementation lanes (`tb-opus-pickup`, `tb-orchestrator`) were paused on 2026-08-19 at Christian's request, and the impediment log's last entry is dated 2026-08-19. This cycle's data is therefore ~5 days of running lanes (08-15 → 08-19), not a full week.

## Summary
- Drift-scan issues consumed: THR-911, THR-912, THR-913, THR-915, THR-747 — all five updated in place by the 2026-08-21 scan (dedupe-filing working, fourth consecutive cycle: zero duplicate tickets, standing issues retitled/re-counted). All five counts **stable** vs 08-14.
- Impediments logged this cycle: **110 entries / 169 occurrences, ~12.3h estimated loss** in 5 lane-days. (Deterministic draft via `npm run retro-draft`; the draft's own period header spans from 07-31 because its parser keys on the retrospective ledger, so this report's numbers were re-binned to entries dated 2026-08-15 → 08-19. Draft deleted after commit per THR-1056.)
- Top categories by occurrence: environment (20 entries / 50 occ — dominated by two aggregate rows: `preview_start` refused ×12 and ×13), tooling (26/34), false-signal (21/23), process (13/17), testing (11/13).
- Improvements implemented this session: **3 quick wins** (all effort-1 doc/skill edits).
- Improvements backlogged: _Filed in Step 9._
- **Previous retro's backlog: 2 of 2 Done within a day** (THR-1111 same evening 08-14, THR-1112 on 08-15). Watch item THR-1108 also Done (08-15). One watch item did *not* resolve: worktree `node_modules` stubs kept arriving after THR-1111 shipped — see § Patterns to Watch.

## Drift-Scan Signals This Cycle

| Issue | Signal | This week (08-21 scan) | Last week (08-14) | Direction |
|---|---|---|---|---|
| THR-911 | UL drift | 0 canonical-unused, 15 used-uncanonical | 0 / 15 | Stable |
| THR-912 | Rulebook → UL | 10 broken references | 10 | Stable (3rd flat week) |
| THR-913 | Rulebook IMPL tags | 8 broken code references | 8 | Stable (3rd flat week) |
| THR-915 | Interface-map coverage | Culture; 8 subsystems unaudited | 8 | Stable |
| THR-747 | Skill freshness | 2 stale, 0 archive-candidate | 2 | Stable |

**Signal health: working.** All five signals live, refreshed on schedule (scan fired 08-21 ~14:39 UTC), and their stability is plausible: with the implementation lanes paused since 08-19 and the week's merges dominated by content/fix work, no drift movement is the expected reading, not a dead-signal tell. The standing decision on THR-912/THR-913's priority (No priority, structurally unpickable) remains with Christian — third consecutive week; counts flat, so no urgency escalation.

## Impediment Analytics

Re-binned to this cycle (entries dated 2026-08-15 → 08-19): 110 entries / 169 occurrences / ~740 min (~12.3h heuristic).

### By Category
| Category | Entries | Occurrences |
|---|---:|---:|
| environment | 20 | 50 |
| tooling | 26 | 34 |
| false-signal | 21 | 23 |
| process | 13 | 17 |
| testing | 11 | 13 |
| sandbox-limitation | 7 | 13 |
| process-friction | 2 | 7 |
| content-authoring | 1 | 3 |
| others (api-quirk, ci-gate, verification, linear, ci, content, engine-behaviour) | 9 | 9 |

### By Impact
| Impact | Entries | Occurrences |
|---|---:|---:|
| L | 3 | 5 |
| M | 45 | 68 |
| S | 62 | 96 |

### Root-cause clusters
| Cluster | Entries / occ | Exemplars | Disposition |
|---|---|---|---|
| Harness path guard misfires on `cmd /c` in the worktree `node_modules` repair — chaining any statement with `cmd /c` mis-binds `/c` as the preceding cmdlet's path and refuses the whole call | ~10 / 19 | #636 ×3, #654 ×2, #659 ×2 (controlled arm), #663 ×3, #665 ×4, #674 ×2, #680, #686 ×5, #637 (counterexample) | **Quick win 1** — the trigger question the rows left open is settled (#659: chaining is sufficient; #637: creation verbs pass alone) and CLAUDE.md's repair sequence now carries the one-call-per-step rule + guard-safe routes |
| `preview_start` refused outright in unattended runs (continuing class, now aggregated) | ~12 / 40+ | #638 ×12, #683 ×13, #586/#593/#596–#599, #614, #620, #632, #642, #648, #652, #655 | Known `cant-fix`; jsdom substitution route shipped 08-14. The *new* consequence is board-level — see next row |
| Ready-for-Dev queue consumed by attended-only tickets the unattended lane must refuse | 4 / 13 | #604 ×6, #611 ×4, #660, #652 | Moot while lanes are paused; surfaced in § Needs Christian for re-enable |
| Python text-mode writes CRLF-convert repo files; git hides it; freshness gates misdiagnose it | 5 / 5 | #630, #662, #671, #677, #689 (**L**) | **Quick win 2** (prevention bullet) + **Backlog T-2** (gate-side normalization) |
| `check:wiki-freshness:blocking` two-dot diff attributes `main`'s advance to the PR — self-renewing race on an hourly lane | 1 / 3 | #619 ×3 (**L**) | **Backlog T-1** — the entry itself deferred the gate fix to this retro per the process-work throttle |
| Worktree `node_modules` stubs still arriving (post-THR-1111) | 4 / 4 | #627, #646, #653, #678 | Watch — see § Patterns to Watch |
| Full-suite false reds at closeout (continuing) | ~4 / 6 | #644 ×3, #622, #684, #588 | Absorbed by the 08-14 triage protocol; entries now cite the class rather than rediscovering it |
| Ticket-premise defects caught by executor falsification | ~8 / 9 | #606, #609, #643, #645, #647, #656, #658, #675, #690 | Watch (discipline holding; no landed wrong remedy yet) |
| Linear platform quirks | 5 / 6 | #605 (parent Done cascades), #607 (PR body repopulates parked assignee), #624 (503), #657 ×2 (park re-broken by its own PR) | `cant-fix` platform-side; #607/#657 writing rule already in CLAUDE.md 08-15 |
| CLI / debug-bridge verification gaps | 5 / 5 | #613 (**L** — CLI seeds zero `thread` edges), #618, #621, #650, #547 | #650's crashing command fixed in-session; #613 documented in memory; below bar individually |

Unresolved blockers (Workaround Found = No): none this cycle — every entry records a workaround or an in-session fix.

## Cross-Signal Patterns

1. **The delivery machine spent the week fighting its own safety rail.** The dominant new cluster (19 occurrences) is the harness path guard refusing the *documented* worktree repair — five sessions in three days paid the same ~1–2 min setup cost, each following CLAUDE.md's sequence as written, because the sequence itself chained the two steps that trigger the misfire. The lanes did exemplary forensics (controlled arm #659 varied exactly one token and settled a trigger question four prior rows left inconsistent) and repeatedly said "the fix is one sentence in the CLAUDE.md bullet, not in this row." That sentence is now there (quick win 1).
2. **CRLF is the quiet sibling of the heredoc-corruption family.** Five entries where a Python text-mode write silently rewrote line endings, git's own normalization hid it, and a byte-comparing gate then failed with a message naming the generator — the one suspect that is innocent. Two halves to the fix: prevention where the mistake is made (quick win 2) and robustness where the false red is raised (backlog T-2). Notably #689 is Windows-local and CI *cannot* reproduce it, so it can never be diagnosed by pushing.
3. **The unattended-lane capability gap stopped producing discoveries and started producing arithmetic.** `preview_start` refusals are now logged as aggregate rows (×12, ×13) rather than fresh surprises — the class is fully mapped, the jsdom substitution route works, and the remaining damage is upstream: the Ready-for-Dev shelf filled with attended-only pixel passes that no running lane could claim (#604 ×6, #611 ×4, #660 — at one point the entire ready queue). With the lanes paused this is dormant, but it is the first thing that will bite on re-enable.
4. **Previous retro's fixes held.** Both backlogged tickets Done within a day; the mutex re-offer churn and modal-fighting clusters produced zero new entries; the full-suite false-red class now cites the documented triage protocol instead of re-deriving it. One exception: stub `node_modules` arrivals continued after THR-1111 — four this cycle — so either the containment doesn't cover pool-worktree reuse or the stub shape has a second producer.
5. **Falsification discipline is still the load-bearing control for ticket quality.** ~9 occurrences of tickets with wrong premises, phantom functions, vacuous evidence, or half-shipped scopes — all caught before damage, none landed. Same verdict as last retro: no gate proposal survives the materiality bar while the catch rate is 100%.

## Previous Retro's Backlog (Rule 5)

| Issue | Status |
|---|---|
| [THR-1111](https://linear.app/threadbare/issue/THR-1111) `node_modules` wiper forensics + containment | Done (2026-08-14, same evening) — but stub arrivals continued this cycle (#627, #646, #653, #678); see § Patterns to Watch |
| [THR-1112](https://linear.app/threadbare/issue/THR-1112) CLI `aftermath pick` double-apply | Done (2026-08-15) — zero recurrences in the log after the fix |

Watch items from 08-14: THR-1108 (sibling unmount timers) Done 08-15 with no #554-shaped entries since ✓; judge `Overridden` column — trigger fired, see Tuning 2; `?spawn=`/`?outcome=` etiquette frictions did not re-cluster ✓.

## Improvements Made This Session

1. **CLAUDE.md § Known Sandbox Limitations — worktree repair sequence rewritten for the guard misfire** (#659 ×2, #663 ×3, #686 ×5, #636 ×3, #665 ×4, #674 ×2, #637): steps 4 and 5 are now explicitly one-call-per-step; step 5 prefers the native `New-Item -ItemType Junction` (no `cmd` for the guard to mis-parse) with `cmd /c mklink /J` sanctioned only as the sole statement in a call; step 7's junction strip names the two working teardown routes (`[System.IO.Directory]::Delete($path, $false)` from PowerShell, `cmd //c rmdir` from Bash) since `cmd /c rmdir` is refused from PowerShell however spelled. Five sessions in three days paid this at setup; the fix is read before the mistake now. ROI 5×2/1 = **10**.
2. **CLAUDE.md § Known Sandbox Limitations — new bullet: Python text-mode writes CRLF-convert repo files** (#630, #662, #671, #677, #689): the mechanism (git normalization hides it, byte-comparing gates fail on it, regenerating cannot help), the prevention (`newline=''` or Write/Edit tools), the diagnosis (gate red + git clean = line endings), and the repair (binary rewrite; `git checkout --` structurally cannot help). Placed beside the heredoc bullet it is the quieter sibling of. ROI 4×2.5/1 = **10**.
3. **`.claude/skills/intent-judge/SKILL.md` — `Overridden` column now mandatory on new rows** (trigger set by the 08-14 retro, pulled here: two more rows landed without it, keeping both override-keyed kill criteria unfalsifiable). New rows write `yes`/`no` (default `no`, edited when an override is learned); historical rows stay blank. `last_validated_against` bumped. ROI 2×2/1 = **4**.

## Improvements Backlogged

- [THR-1191](https://linear.app/threadbare/issue/THR-1191/checkwiki-freshnessblocking-diffs-two-dot-so-mains-own-advance-fails) — `check:wiki-freshness:blocking` two-dot diff attributes `main`'s advance to the PR; scope the changed set to the PR's own commits (impediment #619 ×3, L; ROI 3×4/1 = 12). Filed to Todo, Medium, coordination block as first comment.
- [THR-1192](https://linear.app/threadbare/issue/THR-1192/checkgenerated-freshness-byte-compares-disk-vs-blob-with-no-eol) — `check:generated-freshness` EOL-normalize its byte comparison so a CRLF working copy stops failing forever with a generator-blaming message (impediments #630, #662, #671, #689 L; ROI 4×3/1 = 12). Filed to Todo, Low, coordination block as first comment.

Deliberately **not** filed: the `cmd /c` path-guard misfire class (harness-level `cant-fix`; the repo-side fix is the CLAUDE.md sequence rewrite, quick win 1), the CRLF *producing* mistake (prevention bullet, quick win 2), the unattended `preview_start` refusals (mapped `cant-fix`, jsdom route shipped 08-14, machine-check half is THR-882 in Idea), the attended-only queue starvation (moot while lanes are paused — § Needs Christian), stub `node_modules` arrivals (watch item pending a read of THR-1111's actual scope), and every single-occurrence gate/probe false signal (below the materiality bar; logged rows only).

## Tuning Recommendations

1. **No signal-threshold changes.** All five signals live and refreshed; three flat weeks on the rulebook counts is consistent with the lane pause, not with signal death.
2. **Step 5c — intent-judge kill-criteria aggregation** (`Docs/judge-metrics/`, header-keyed parse; **16 rows**, up 2 from last cycle):

   | Criterion | Value | Verdict |
   |---|---|---|
   | ≥95% Allow with zero overrides | Allow 75.0% (12/16) | Below the floor — **not** rubber-stamping |
   | ≥50% override rate on Revise/Block | — | **NOT MEASURABLE** (0 of 16 rows carry an override column) |
   | Median time-to-judgment >300s | 95s | Pass |
   | Zero marginal catches | — | User judgment, not row-derived |

   **Verdict: keep intent-judge.** The override gap is now closed prospectively — quick win 3 makes the column mandatory on new rows, so next cycle's check is whether new rows actually carry it. Escalation rate 0.0% remains below the skill's declared 0.05 floor — still a tuning observation, not a kill criterion.

## Step 5b — Systemic Wiring Guide Audit

No new content-facing engine capabilities since 08-14: the only merges touching the watched surfaces (`src/types/effects.ts` et al.) were validation and threshold fixes (THR-1177 chokepoint edge validation, THR-1151 faction rank scale), neither introducing a capability a content author could reach for. The guide's Part 5 inventory remains current (spot-checked `attachment_grant` coverage from last cycle — present with worked section). Nothing to update.

## Needs Christian

- **The implementation lanes are paused (your 2026-08-19 request) and the impediment log is silent since then — this is expected, not decay.** When you re-enable them, the first thing they will hit is the Ready-for-Dev shelf: at last reading it held attended-gated pixel-pass tickets the unattended lanes must refuse (impediments #604 ×6, #611 ×4). Draining those wants an attended session (or a re-scope of their Done-whens); until then re-enabled lanes will report a starved shelf.
- **Standing from 08-07, third week open:** the two rulebook drift-scan signals ([THR-912](https://linear.app/threadbare/issue/THR-912/drift-scan-2026-08-21-rulebook-ul-10-ul-references-broken-in-rulebook), [THR-913](https://linear.app/threadbare/issue/THR-913/drift-scan-2026-08-21-rulebook-impl-tags-8-impl-tags-with-broken-code)) sit at No priority and are structurally unpickable. Counts flat for three weeks, so it can keep waiting — but it stays a decision only you can make: give them a priority, or explicitly accept rulebook drift until the next rulebook-review cycle.

## Patterns to Watch

- **Stub `node_modules` arrivals post-THR-1111** (#627, #646, #653, #678 — four this cycle). The wiper-forensics ticket shipped Done the same day it was filed, yet the stub shape keeps arriving on fresh/pool worktrees. If the class persists at ≥3/week once lanes resume, the next retro should read THR-1111's actual diff and check whether pool-worktree reuse was in its scope before filing anything new.
- **Judge `Overridden` adoption** — mechanical check next cycle: do the new rows carry the now-mandatory column?
- **T-1 / T-2 gate fixes** — both target false reds on required or blocking checks; once merged, #619-shaped and #689-shaped entries should stop. Recurrence after merge means the fix missed a direction.
- **Ticket-premise defects** (~9 caught this cycle, 5 last cycle) — the executor-falsification catch rate is still 100%. The standing trigger holds: the first *landed* wrong remedy converts this from a watch item to a design-session gate proposal.
- **Aggregate-row logging** (#638 ×12, #683 ×13, #686 ×5) — lanes have started logging known classes as single rows with occurrence counts instead of fresh entries. Good for log hygiene; the retro's re-binning already handles it, but the draft parser's per-entry heuristic undercounts time lost for aggregated rows whose per-occurrence cost varies.
