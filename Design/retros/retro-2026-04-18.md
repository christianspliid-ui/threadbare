# Workflow Retrospective — 2026-04-18

**Period covered:** 2026-04-12 → 2026-04-18 (impediments #40–#55)
**Days since last retro:** 7 (previous retro 2026-04-11, items #13–#39)
**Scope:** 16 new impediments logged since last retro, plus status check on open items from last retro.

---

## Top 3 impediments this week

1. **TB-120 test suite repair is still not landed (8+ days → 15+ days red).** Impediment #54 (2026-04-17) is the latest occurrence of "full `npm test` red on main for pre-existing reasons." The fix was the #1 action from last retro. Until this lands, every slice ships with scoped-only verification and real regressions keep hiding inside baseline noise.
2. **Browser simulation stalls at tick 72 with 1010+ agents (#42, open, no workaround).** The `?seeded` URL creates a much larger world than CLI seed 42 (1010+ vs 414 agents). This is a real scaling bug that prevents full browser verification of any engine work, and it has been open since 2026-04-12 with no owner.
3. **Silent-typing cluster: four code bugs in one week (#40, #45, #46, #50) where TypeScript compiled clean but runtime failed.** Wrong property names on actor nodes, scope leaks out of `for…of`, optional `id` field missing, and structural subtype collisions. Same failure mode repeating — we need a typed-helper / lint rule, not more case-by-case fixes.

---

## Status of active impediments

| # | Title | Age (days) | Owner | Status | Next action |
|---|-------|-----------:|-------|--------|-------------|
| 14 | `QUINTESSENCE_ENCOUNTER_FAILURE_EROSION` unused | 16 | TBD | Dashboard says unresolved; last retro verified it was wired into orchestrator | Close in dashboard: flip `workaroundFound` to true and cite the verification from 2026-04-11 retro, by 2026-04-19, owner: Cowork |
| 22 | `npm test` red baseline | 15 | TBD (TB-120) | Still open — #54 re-confirmed 2026-04-17 | Pick up TB-120 as the next Claude Code issue before any new feature work, by 2026-04-21, owner: CC |
| 25 | No authored approach-card field on `UnifiedActionTemplate` | 14 | TBD | Dashboard still says unresolved; last retro verified `authoredChoices` shipped | Close in dashboard with commit reference, by 2026-04-19, owner: Cowork |
| 36 | `ThreadLineMesh` → `ActivityIconMesh` cross-module coupling | 9 | TBD | Opportunistic — not yet extracted | Extract `REACH_ICON_COLORS` into a shared palette module during next HexMapV2 touch, owner: TBD |
| 42 | Browser stalls at tick 72 with 1010+ agents | 6 | **TBD — needs owner** | Open, no workaround | Reproduce under `?seeded`, profile tick loop, compare world-gen between `?seeded` and CLI seed 42, by 2026-04-25, owner: TBD |
| 47 | `omenState.activeOmens` / `OmenTrackTemplate` vocabulary tables don't exist | 1 | TBD | Wired but cannot inject vocabulary | Sequence after the Thematic Pressure omen-template work lands (Linear project); until then, leave `omenState` as a no-op pass-through, owner: Cowork |
| 48 | Linear `save_issue` returns 200 but does not change state | 1 | TBD | Workaround: re-query | Add "verify-after-write" guard to the CC session-handoff skill and to the Linear coordination protocol doc, by 2026-04-22, owner: Cowork |
| 49 | Linear `list_issues orderBy: 'priority'` rejected at runtime | 1 | TBD | Workaround: sort in memory | Add a known-limitations line to `Docs/plans/2026-04-13-linear-coordination-protocol.md`, by 2026-04-22, owner: Cowork |
| 54 | `unifiedActionPhases.test.ts` pre-existing failure | 1 | TBD (TB-120) | Symptom of #22 | Rolled into TB-120 triage, owner: CC |
| 55 | RemoteTrigger `trig_012H3CEdTnrAqY4w81T4rLXz` returns 404 | 0 | TBD | Workaround: Slack DM only | Recreate or refresh the trigger id inside the `session-handoff` skill; guard against 404 with a fallback log-only path, by 2026-04-22, owner: Cowork |
| 15/33/37 | `rg.exe` blocked in Codex sandbox | 33 | TBD | Documented workaround | Pending: add "Known Sandbox Limitations" section to CLAUDE.md (carried from last retro), by 2026-04-21, owner: Cowork |

Resolved-and-can-close items (one-line next action: move `workaroundFound: true` and add a commit SHA in the dashboard `DATA` array): #40, #41, #43, #44, #45, #46, #50, #51, #52, #53.

---

## What shipped since last retro

- **#40 / #41** — Strategic-action candidate generator: fixed `domainCapability` → `domainCapabilities` and persisted `strategicState` across the agent loop. Merchant proving slice unblocked.
- **#45** — `deriveLocationActivities.ts` scope leak and stray `declare const agentNode` cleaned up (THR-22 session 1).
- **#46** — Location activity map re-keyed from `LocationNode.id` (which does not exist on the HexMapV2 type) to `col,row` strings throughout. Renders correctly again.
- **#50** — THR-89 Phase 1 TG guild migration: removed pre-migrated guilds from `getFactionEncounterById` chain once they moved to `UNIFIED_ACTION_TEMPLATES`. 52 runtime test failures cleared. Established migration pattern for future guilds.
- **#51 / #52 / #53** — THR-112 hidden-mark system: trace-assertion test pattern established (`enableTracing()` in `beforeEach`), decay phase no longer short-circuits on unchanged length, same-tick mark consumption guarded. Last one was found in Codex review — the review protocol is paying off.
- **Codex review loop is active.** #53 was caught by Codex review before merge. This validates the `Codex review: yes` line in the handover template.

---

## What slipped

- **TB-120 (Test Suite Repair Sprint)** — the #1 action item from last retro. Not landed; #54 is yet another occurrence. Likely cause: no single agent owns it, and every session is focused on pulling the top-priority Linear issue from its own project rather than repairing baseline infrastructure. The "Finish Before You Start" rule in CLAUDE.md does not cover test-health repair because it isn't a project issue.
- **TB-121 (CI/CD Pipeline)** — not landed. Without this, TB-120's gains would regress the next time someone adds a brittle assertion. These two must ship together.
- **CLAUDE.md "Known Sandbox Limitations" section** — still not added. Agents keep paying the rediscovery tax on `rg.exe`, PowerShell scoping, git perms, npm-test timeout.
- **`REACH_ICON_COLORS` shared palette extraction (#36)** — opportunistic, no forcing function; still coupled.

---

## Workflow observations

- **Dashboard drift.** The dashboard's `DATA` array still shows #14 and #25 as unresolved even though last retro's code review verified they ship. The dashboard is a hand-maintained JS array inside the HTML file, and updates to the markdown log don't propagate. We're losing signal about what's actually open. (The "5 unresolved" stat in the header is inflated by ~2 stale entries.)
- **Silent-write API calls are becoming a recurring category.** Linear `save_issue` (#48), RemoteTrigger (#55), even last month's ESLint-revert (#9a) are all the same shape: tool returns success, state didn't change. We keep catching these one at a time.
- **Promoting repo-health to a project would give it an owner.** TB-120 and TB-121 live as backlog items but aren't attached to a Linear project per CLAUDE.md's "every issue must belong to a project." Either create a "Repo Health" project or attach them to Linear Setup; otherwise they'll keep being passed over when agents apply the "Finish Before You Start" rule to their own project.
- **Browser vs CLI world-gen mismatch (#42).** The fact that `?seeded` produces a 1010-agent world while CLI seed 42 produces 414 agents is its own impediment — that means browser and CLI are generating different worlds even with "the same seed," which undermines the CLI as a verification surface.
- **Codex review is working.** Explicitly worth keeping — #53 was the payoff.

---

## Experiments for next week (small, reversible)

1. **One-day test-repair blitz.** Carve out Monday (2026-04-20) for Claude Code to land TB-120 + minimal TB-121 (GitHub Actions running `npm test`, `npx tsc --noEmit`, `npx vite build`). Scope cap: if it isn't green by end of day, revert the branch and ask the user to scope down further. Reversible because it's a branch.
2. **Dashboard auto-regen stub.** Next time an agent edits `Docs/impediments.md`, have them also update the `DATA = [...]` array in the dashboard HTML in the same commit — treat dashboard as source-controlled output, not hand-curated. If that proves annoying within a week, back it out and schedule a proper generator script.

---

## Ask of the user (Christian)

- **Approve TB-120 + TB-121 as the next CC pick**, ahead of whatever is top of Ready for Dev in product projects. This is the "finish before you start" override — repo health has slipped to where it blocks every future verification.
- **Decide where TB-120 / TB-121 / repo-health work belongs in Linear Projects.** Either create a new "Repo Health" project or attach these under "Linear Setup." Without a project, agents won't pick them up under the new ordering rule.
- **Assign an owner for #42 (browser stall / world-gen divergence).** This is a real scaling bug, not infrastructure, and it'll need someone to actually reproduce and profile. Worth a dedicated Linear issue inside the relevant engine project.
- **Confirm it's OK to close #14 and #25 in the dashboard** based on last retro's code verification, so the "unresolved" stat reflects reality.

---

## Summary

- **The week's real story is that TB-120 didn't land.** Engine and content work is shipping (THR-22, THR-89, THR-112) and Codex review is catching bugs, but every session continues to ship on scoped verification over a red baseline. That is still the #1 thing to fix.
- **A silent-typing theme emerged (#40/#45/#46/#50).** Four code bugs in one week that TypeScript compiled cleanly. Worth a typed-helper + lint-rule plan; not urgent enough for a separate experiment this week.
- **Dashboard is drifting from reality.** #14 and #25 are verified-closed but still shown as unresolved. Fix in the same commit as the next impediment update.
