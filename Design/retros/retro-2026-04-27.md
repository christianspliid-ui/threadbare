# Workflow Retrospective — 2026-04-27

**Period covered:** 2026-04-23 → 2026-04-27 (impediments #79–#93, ~15 new entries; 4 days since last retro)
**Days since last retro:** 4 (previous retro 2026-04-23, items through #78)
**Scope:** Status check on last week's four consented experiments, fresh impediments since 2026-04-23, design-council pass on next-week actions.

> **Data-quality note (carried forward).** The dashboard's `DATA` array still ends at #78 — five days, 15 new entries, and three Critical-severity blockers later, the dashboard is misleading by exactly the same margin last retro flagged. The mechanical regen experiment slipped. This retro reads `Docs/impediments.md` as source of truth (entries through #93) and treats the dashboard as a stale visualization.
>
> **Drift-scan input not consulted.** THR-274 wired the retrospective skill to read this week's `drift-scan`-labeled Linear issues as Step 0. The scheduled-task SKILL.md driving this run pre-dates THR-274 and does not include that step. Logging as workflow observation; the input file itself needs re-authoring to match the new cadence.

---

## Top 3 impediments this week

1. **`LINEAR_API_KEY` GitHub Actions secret is unset — silently disabling every merge-gated auto-close (#85).** The `linear-autoclose.yml` workflow runs on every push to main, logs "LINEAR_API_KEY secret not set — skipping Linear auto-close," and exits cleanly. Every `Fixes THR-XX` commit that has merged since the secret first went missing has stayed In Dev forever. THR-234 is the documented case; an unknown number of other issues are also leaking. This breaks the canonical "merge-to-main IS the closeout" invariant that CLAUDE.md, the Linear coordination protocol, and CC's pull-work skill all depend on.
2. **Codex hourly executor consumed 4 cycles in 2 days on dirty-worktree bounces (#87/#88/#89).** Each cycle: claim THR-256 → discover unrelated tracked/untracked changes → release claim → exit. ~50% of automation slots wasted on the same blocker. The "release and bounce" pattern is safer than committing through dirty state, but it's also a doom loop — nothing on the executor side can clear the dirty worktree.
3. **Three Christian-owned paid-service blockers landed in one week — none yet actioned (#85, #91, #93).** `LINEAR_API_KEY` secret missing (auto-close broken), GitHub Actions billing failed (CI gate broken on PR #41), Linear free issue quota exceeded (new issue creation blocked, including UL proposals). These compound: PRs can't merge through CI, merges that do happen don't auto-close, and new issues can't be filed to track the resulting drift. Last retro's #1 ask ($CODEX_HOME) also slipped — the retro→user→action loop is the actual bottleneck.

---

## Status of active impediments

Prior-open items carried from 2026-04-23 retro plus all unresolved/partial entries since. Stale items (>7 days no movement) marked.

| # | Title | Age (days) | Owner | Status | Next action |
|---|-------|-----------:|-------|--------|-------------|
| 36 | `ThreadLineMesh` → `ActivityIconMesh` coupling | 18 ⚠ stale | TBD | Opportunistic | Fold into next HexMapV2 touch. No standalone issue. |
| 47 | `omenState.activeOmens` / `OmenTrackTemplate` missing | 10 | Cowork | Deferred to THR-128 (acceptable) | No action; sequenced with Thematic Pressure. |
| 56 | GitHub branch protection needs Pro | 9 ⚠ stale | Christian | Blocked on account upgrade | **Ask of user.** Upgrade or accept advisory CI. |
| 57 | `traceBuffer-integration.test.ts` overflow | 8 ⚠ stale | TBD | Discrete failure, no Linear issue yet | File in Repo Health, assign Codex. By 2026-04-30. |
| 58 | `session-handoff` skill files deleted | 9 ⚠ stale | Christian | Awaiting confirmation | Confirm deletion intentional per THR-236; close in dashboard. |
| 59 | 8+ orphan uncommitted changes | 9 ⚠ stale | Christian | Open, no workaround | **Ask of user.** Human review pass to attribute and commit. |
| 61 | Scheduled-task sonnet cannot pick up `model:opus` issues | 8 ⚠ stale | Cowork | Open | Pragmatic-override pattern landed in #92; codify in pull-work skill. |
| 62 | `git stash` data-loss pattern | 8 ⚠ stale | Cowork | Workaround found, codification deferred | Add to CLAUDE.md Known Sandbox Limitations. By 2026-04-30. |
| 63 | Linear `save_issue(id)` silently overwrites existing | 8 ⚠ stale | Cowork | Workaround found, doc deferred | Add explicit guard to coordination protocol. By 2026-04-30. |
| 64/67/76 | `$CODEX_HOME` unset | 8 ⚠ stale | Christian | Recurring (16 occurrences total now, up from 12) | **Ask of user (3rd retro running).** One-line PowerShell profile edit. |
| 66/71/75/86 | Obsidian MCP unreachable | 8 ⚠ stale | Christian | Partial workaround (queue + replay) | **Ask of user.** Decide auto-start vs filesystem-mount fallback. |
| 69 | Cowork duplicated 7 shipped issues | 7 ⚠ stale | Cowork | Cleanup done; structural fix pending | Hard pre-draft Done-column gate. By 2026-04-30. |
| 70 | THR-222 handoff missing Files-to-touch | 7 ⚠ stale | Cowork | Refreshed handoff requested | Add handoff-template completeness check to plan template. |
| 72 | `git add <files>` staged extra untracked | 7 ⚠ stale | Codex | Partial — committed as-is | Add `git diff --cached --name-only` pre-commit check. |
| 77 | Wrong npm script name in handoff | 6 | Cowork | Workaround found | Add `npm run` script-exists check to handoff template. |
| **79** | **Linear MCP rate limits** | 4 | Cowork | **RESOLVED** via THR-246 | Close in next dashboard regen. |
| **80** | **`claude-review` GitHub Action structural failure** | 3 | TBD | **RESOLVED** 2026-04-24 (PR #10) | Close in next dashboard regen. |
| **81** | **Fresh `git worktree` has no `node_modules`** | 3 | Codex | Workaround found | Add `npm install` to fresh-worktree pickup template. |
| **82** | **GitHub repo labels `codex` / `codex-automation` missing** (×5) | 2 | Christian | Workaround found (skip labels) | **Ask of user.** Create the two labels in repo settings. |
| **83/84** | **Push to main rejected (non-fast-forward / GH013)** | 3 | Codex | Workaround found (PR path) | Codify "always PR, never direct-to-main" in pickup skill. |
| **85** | **`LINEAR_API_KEY` secret unset → auto-close silently skips** | 3 | Christian | **CRITICAL — invariant broken** | **Ask of user (highest priority).** Add `LINEAR_API_KEY` to GitHub Actions secrets. Audit all merged `Fixes THR-XX` commits since secret was first absent, manually close stragglers. |
| **87/88/89** | **Codex pickup blocked by dirty worktree** (4 cycles) | 2 | Codex / Cowork | Repeat blocker | Add clean-worktree-or-bail policy to Codex pickup skill. By 2026-04-30. |
| **90** | **`create_scheduled_task` fails inside scheduled task** | 2 | Cowork | Workaround (run in non-scheduled session) | Note in `schedule` skill description. |
| **91** | **GitHub Actions CI billing failed** | 2 | Christian | Resolved same session after user funded | **Ask of user (preventive).** Set up GitHub Actions spending alerts; keep budget funded. |
| **92** | **CC opus pickup loop on missing handoff comment** | 2 | Cowork | Pragmatic override applied (THR-271 shipped) | Update pull-work to treat issue description as fallback when no comment exists. |
| **93** | **Linear free issue quota exceeded** | 2 | Christian | **CRITICAL — issue creation blocked** | **Ask of user.** Upgrade Linear plan or clear workspace usage. |

Dashboard regen backlog has grown: prior carryover (#40, #41, #43, #44, #45, #46, #50, #51, #52, #53) + last-retro's pending closes (#48, #49, #55, #58, #60, #65, #68, #73/74, #77) + this week's resolved (#79, #80) + 13 new unrepresented entries (#79–#93). Still not reflected in `DATA`.

---

## What shipped since last retro

- **THR-275** — HexSidebar location click handler. PR #47 on auto-merge (subject to #85 not breaking the close).
- **THR-211** — First `playtest-interface` run (27 PASS, 2 FAIL, 10 SURPRISE, 10 SKIP). RUBRIC.md annotated; EXPECTED-FINDINGS.md seeded; THR-275 filed from findings.
- **THR-243** — `Docs/exemplars.md` quality exemplars index. PR #34 merged.
- **THR-272** — `grill-me` skill (4 invocation triggers, conversational + async-batch modes, synthesis artifact spec). CLAUDE.md design governance Step 0 added. PR #32.
- **THR-271** — Ubiquitous Language v1 (7 sharded files, 73 canonical terms, propose-new-term + retirement flows, "UL wins on terminology disagreements" rule). PR #44.
- **THR-274** — Retro × drift-scan cadence sync. `retrospective` skill reads weekly drift-scan-labeled Linear issues as Step 0; CLAUDE.md documents Friday cadence. PR #41 still pending CI (blocked by #91 billing).
- **THR-258** — Runtime phase-predicate `edge-exists` support in `phaseComposition`. Phase-side FilterQuery evaluation for `and`/`or`/`not`/`prop-equals`/`has-edge`.
- **THR-246** — Linear MCP rate-limit fix per `Docs/plans/2026-04-23-linear-mcp-rate-limits.md`. Closes #79.
- **#80** — `claude-review` GitHub Action repaired (`persist-credentials:false` removed; Rule 8 grep replaced with exit-code check). PR #10 landed.

Net: 8 issues shipped, 2 impediments resolved (#79, #80) — strong execution week despite environmental drag.

---

## What slipped

- **Experiment #1: `$CODEX_HOME` global fix.** Owner Christian, ETA 2026-04-24. Did not land. Six new occurrences this week (THR-242, THR-259, THR-255, THR-254, THR-245, plus the no-work cycle). Total count now 16, up from 12. **Third retro running.** The retro→user→action loop is the bottleneck, not the technical fix.
- **Experiment #2: Mechanical dashboard regen.** Owner Cowork to design, CC to implement, ETA end of week. Did not land. Dashboard `DATA` array still ends at #78; 15 new entries (#79–#93) and three Critical-severity blockers are invisible to the dashboard. The retro right above this paragraph is reading the wrong source again.
- **Experiment #4: "Never `rm -f` untracked files for A/B" in CLAUDE.md.** Owner Cowork, ETA 2026-04-25. Not visible in CLAUDE.md Known Sandbox Limitations as of this session. Did not land.
- **Experiment #3: Cowork issue-creation hard gate (Done-column check).** Owner Cowork, ETA 2026-04-28 (tomorrow). Status unverified. No new duplicate-draft impediments this week, which is weak signal for absence rather than presence — could be the gate landed quietly, could be Cowork happened not to draft anything in an existing project.

**Slip pattern.** 3 of 4 last-week experiments did not land. Two of the three were Cowork-owned mechanical edits with no paid-service dependency — the failure is in Cowork's own follow-through, not external blockers. The fourth ($CODEX_HOME) is a 30-second fix but requires Christian's PowerShell profile, and Christian-owned items have now slipped across three retros.

---

## Workflow observations

- **Paid-service blockers are this week's dominant theme.** Three landed in 4 days: `LINEAR_API_KEY` secret missing (#85), GitHub Actions billing failed (#91), Linear free-issue quota exceeded (#93). Each individually surfaced via a different impediment; collectively they form a single failure mode — service capacity / authentication that the team depends on but doesn't actively monitor. Without a "user-action queue" view, each one ambushes a session, gets logged, and waits for the next retro.
- **The merge-gated auto-close has been silently broken for an unknown duration (#85).** The `linear-autoclose.yml` workflow runs cleanly and exits with no error when `LINEAR_API_KEY` is unset. From the outside it looks like the close path works. CC's pull-work and Codex's pickup both rely on "merge IS the close" — every one of those interactions has been operating against an invariant that wasn't actually enforced. THR-234 is the *known* victim. There is no audit yet for how many other issues have leaked.
- **Codex automation is bleeding cycles on dirty-worktree pickup (#87/#88/#89).** Four hourly slots in two days lost to the same blocker. The release-and-bounce safety pattern is correct in isolation but creates a doom loop because the executor cannot itself clean the worktree. Either Cowork must clean before handoff, or the executor must accept dirty state with safe-scoped commits, or pickup must run in a fresh `git worktree add` so the dirty state is invisible. Status quo wastes ~50% of relevant automation slots.
- **The retrospective→user→action loop is the actual bottleneck.** $CODEX_HOME is 8 days and 16 occurrences old. Obsidian MCP is 8 days and ~12 occurrences old. GitHub Pro / branch protection is 9 days old. Each retro restates the ask; nothing changes. The fix isn't another retro re-statement — it's surfacing the asks somewhere Christian sees them outside the retro.
- **Cowork's own deferred items also slipped.** Three last-week experiments (#1 dashboard regen, #4 CLAUDE.md `rm -f` rule, plus the carryover items #62, #63 codifications) are Cowork-owned mechanical edits with no external blocker. Cowork's design authority is high but Cowork's mechanical-edit follow-through is not high. Possible cause: design ↔ implementation handoff inside Cowork's own work isn't tracked the way executor handoffs are.
- **`drift-scan`-labeled Linear issues are now part of the canonical retrospective input (THR-274 / CLAUDE.md), but the scheduled-task SKILL.md driving this run is stale against that protocol.** Same failure mode as #67 from last retro: governance artifacts that live outside the repo can't self-heal in-session. The scheduled-task file itself needs to be re-authored.
- **Strong shipping week despite drag.** Eight issues closed, two impediments resolved. The pipeline is capable; the drag is environmental, not productivity-related. The cost of leaving environmental drag in place rises with throughput — a faster pipeline pays the $CODEX_HOME tax more times per week, not fewer.

---

## Design-council pass — consented experiments (small, reversible)

Four voices weighed in: engineer, ops, PM, tech lead. Consensus actions, scoped so any can be reverted with one PR or one shell line.

1. **Replace per-retro user-asks with a persistent "User Action Required" board.** Create `Design/user-actions.md` (or a Linear project; either works) listing every Christian-owned blocker with: title, age, count, one-line fix, "what breaks if we don't." Update mechanically on retro day. Reference it from each retro instead of restating asks. Expected impact: $CODEX_HOME / Obsidian MCP / GitHub Pro / `LINEAR_API_KEY` / Linear quota / GH labels stop being retro-by-retro re-discoveries; they become a single dashboard Christian can clear in one sitting. Reversibility: delete the file. Owner: Cowork to author this run, ETA today. *(PM: "We've nagged Christian three times about the same one-line fix. The retro isn't the right channel for that.")*

2. **Audit all merged `Fixes THR-XX` commits since `LINEAR_API_KEY` first went missing; manually close stragglers.** `git log main --grep "Fixes THR-" --since=2026-04-01 --oneline` → cross-check each against Linear state via `get_issue`. Anything still In Dev gets a manual close via Linear MCP with a comment noting the auto-close skip. Expected impact: invariant restored; stops the "phantom open" pile that's been growing silently. Reversibility: closes are reversible, comments are append-only. Owner: Cowork, by 2026-04-30. *(Tech lead: "We've been operating against a broken close-loop for an unknown duration. Find out the duration before fixing the cause.")*

3. **Codex pickup runs in a fresh `git worktree add` if the main worktree is dirty.** Update the Codex pickup skill: at session start, run `git status --porcelain` in main worktree; if non-empty, create `git worktree add ../tfws-codex-<issue>` from `origin/main` and run the pickup there. `npm install` in the fresh worktree (#81). Closeout pushes from the fresh worktree. Expected impact: #87/#88/#89 stop blocking pickup; ~50% recovered automation throughput. Reversibility: skip the worktree branch, fall back to current behavior. Owner: Cowork to update pickup skill, by 2026-04-30. *(Engineer: "Dirty worktree is a fact of life with multi-agent work. Stop fighting it; isolate from it.")*

4. **Mechanical dashboard regen — re-scope to a single-PR Cowork task this week, not a CC handoff.** Last week's "Cowork to design, CC to implement" split is exactly the kind of intra-Cowork handoff that slipped. Re-scope: Cowork writes a node script (`scripts/generate-impediment-dashboard.ts`) that parses `Docs/impediments.md` markdown table → emits the `DATA = [...]` array → splices it into the HTML between sentinel comments. One PR, no executor handoff. Run it manually for this retro, then wire to a pre-commit hook or `npm run` step. Expected impact: dashboard stops being five days behind reality; this retro has *some* chance of being read against accurate data. Reversibility: revert the PR. Owner: Cowork, by 2026-04-30. *(Ops: "The retro keeps reading the wrong source. Mechanize the source.")*

Two items explicitly *not* picked as experiments this week:

- **`drift-scan` integration into this scheduled task.** The retro skill itself reads drift-scan now (THR-274), but the scheduled-task SKILL.md upstream of it doesn't. Re-authoring the scheduled-task file is in the existing "Ask of user" set (#67 last week, still open). Don't add as a separate experiment.
- **Codify Cowork's own deferral tracking.** Three Cowork-owned items slipped this week despite being mechanical. Worth investigating, not yet worth a process change. Re-evaluate next retro if pattern repeats.

---

## Ask of the user (Christian) — consolidated, ranked by blast radius

(All of these will also be in the new `Design/user-actions.md` per Experiment #1.)

1. **Add `LINEAR_API_KEY` GitHub Actions secret (#85).** Highest priority — silently breaks every merge-gated auto-close. Settings → Secrets → Actions → New repository secret. Use a Linear personal API key.
2. **Resolve Linear free-issue quota (#93).** Blocks new issue creation including UL proposals. Upgrade plan or clear unused.
3. **Fund GitHub Actions billing + set spending alerts (#91).** Currently green again post-fix; preventive measure to avoid mid-PR ambush.
4. **Set `$CODEX_HOME` in PowerShell profile (#76, #64, #67).** One line: `$env:CODEX_HOME = "$env:USERPROFILE\.codex"`. Third retro asking; 16 occurrences and growing.
5. **Decide Obsidian MCP path (#66, #71, #75, #86).** Auto-start the Local REST API plugin on boot, OR add filesystem-mount fallback to vault-log skill. Either works; pick one.
6. **Create GitHub repo labels `codex` and `codex-automation` (#82-1).** Settings → Labels → New label, twice. Unblocks automation PR tagging.
7. **Confirm `session-handoff` skill deletion was intentional (#58).** If yes, close in dashboard. If no, restore from git.
8. **Triage 8+ orphan uncommitted changes (#59).** Each needs Linear-issue attribution then commit-or-discard. Will not self-heal.
9. **GitHub Pro upgrade decision (#56).** Not urgent (CI is green and advisory) but blocks `.github/workflows/claude-review.yml` flipping to blocking mode.
10. **Re-author the `weekly-workflow-retro` scheduled-task SKILL.md** to read `drift-scan`-labeled Linear issues per THR-274. Files sit outside the repo so agents can't self-heal. Same fix as #67 last week, still pending.

---

## Summary — three things to act on this week

1. **`LINEAR_API_KEY` secret is the single highest-leverage fix** — it silently broke our canonical close-loop. Add the secret + audit-and-manual-close stragglers (#85 + Experiment #2).
2. **Codex hourly automation needs to run in a fresh worktree** — current state wastes ~50% of slots on dirty-worktree bounces (#87-89 + Experiment #3).
3. **Stop nagging Christian via retros; build a User Action board** — six paid-service / configuration blockers are sitting at varying age waiting for one human action each. Consolidate them into one place Christian sees outside retro day (Experiment #1).

---

*Retro run 2026-04-27 via the `weekly-workflow-retro` scheduled task. Autonomous run — no user present. Input: `Design/impediment-dashboard.html` (stale at #78) cross-checked against `Docs/impediments.md` (current to #93) and `Docs/project-status.md` (current to 2026-04-27). Drift-scan input not consulted — scheduled-task SKILL.md predates THR-274.*
