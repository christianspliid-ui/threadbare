# Retrospective — 2026-09-25

## Period
From: 2026-09-18 (exclusive; last retro)
To: 2026-09-25

**Draft-period note:** the four-week hand re-bin is over. `npm run retro-draft` now reports `period start 2026-09-18 from retro-report`, so THR-1512 held on its first live run. The draft's 21 entries (#1054–#1073, plus #359's third recurrence on 09-23) were cross-checked row by row against `Docs/impediments.md` and match.

## Summary
- **Drift-scan issues read:** THR-911, THR-912, THR-913 and THR-915, as the scan updated them on 2026-09-18 17:35Z, after last week's retro had already read the board. The values match the 09-11 figures exactly (details below). The 09-25 scan had not fired when this retro started (15:05Z).
- **Impediments logged:** 21 entries, 27 occurrences. The draft's heuristic says **~6.2 h**, but that figure is inflated. Thirteen rows are tagged `L` (15+ min) while their own Consequence column states 1–10 minutes. Re-summed from the stated costs, the work lost is **~2.5 h**. Another **~16 h** was elapsed delay, where work waited but nobody was working: a PR sat red for 12 h on a false gate result (#1062), and a ticket sat 4 h behind a dead run (#1063).
- **Top category:** tooling (13 entries). Almost all of it is CLI and `__DEBUG` gaps met while gathering evidence for the Physical Conflict slices, plus gates that give false signals.
- **Improvements made this session:** 6 small edits to docs and skills (listed below).
- **Improvements backlogged:** none. No cluster clears the materiality bar this cycle (see Improvements Backlogged).
- **Last retro's backlog: all three tickets shipped.** [THR-1512](https://linear.app/threadbare/issue/THR-1512) (retro-draft period) is proven by this run's draft. [THR-1513](https://linear.app/threadbare/issue/THR-1513) (the classify:diff reminder falls back to the working tree, and the pickup lane now invokes it) and [THR-1514](https://linear.app/threadbare/issue/THR-1514) (live-proof and census read state, not the trace ring) are both Done. Also filed and Done this cycle: [THR-1517](https://linear.app/threadbare/issue/THR-1517) (orchestrator.test.ts timeouts, for #1054's five recurrences) and [THR-1529](https://linear.app/threadbare/issue/THR-1529) (WIP push before verify, for #1059). Both closed their clusters: no orchestrator timeout recurred after THR-1517, and #1063's recovery worked because of THR-1529's probe.
- **One loose end from last cycle:** the 09-18 report's Improvements Backlogged section still reads `_Filed in Step 9._` on `main`, so that run never backfilled it. The tickets it filed were THR-1512, THR-1513 and THR-1514. This report records them here instead of reopening a merged file.

## Drift-Scan Signals This Cycle

| Signal | Issue | 09-11 | 09-18 | Trend |
|---|---|---|---|---|
| UL drift | [THR-911](https://linear.app/threadbare/issue/THR-911) | 0 / 15 | 0 / 15 | Flat. All 15 are code identifiers, a known candidate list |
| Rulebook → UL | [THR-912](https://linear.app/threadbare/issue/THR-912) | 9 | 9 | Flat |
| Rulebook IMPL tags | [THR-913](https://linear.app/threadbare/issue/THR-913) | 8 | 8 | Flat. The Physical Conflict rulebook §7 edits did not move it |
| Interface-map coverage | [THR-915](https://linear.app/threadbare/issue/THR-915) | 5 unaudited | 5 (Culture) | Flat |

None is promoted: none records a loss, and all four sit in `Idea` under the 2026-08-10 throttle. This is not a silence streak, because the scan fired. The signals are flat, not dead.

## Impediment Analytics

### By Category (entries / occurrences)
| Category | Entries | Occ. |
|---|---:|---:|
| tooling | 13 | 14 |
| ci | 1 | 4 |
| environment | 1 | 3 |
| coordination | 2 | 2 |
| documentation | 2 | 2 |
| testing | 1 | 1 |
| verification | 1 | 1 |

### By Impact
| Impact | As tagged | As stated in Consequence |
|---|---:|---:|
| L | 15 entries | 2–3 (#1059 ~30 min, #1057 ~25 min; #1056 ~10 min ×2) |
| M | 4 | ~12 |
| S | 2 | ~6 |
| Blocked | 0 | 0 |

No blockers are unresolved: every row has `Workaround Found = Yes`.

### Root-cause clusters

| Cluster | Rows | Cost | Disposition |
|---|---|---|---|
| **The CLI and debug bridge lack the levers needed for fight/monster evidence** | #1066, #1069 (`@hero` in the headless CLI is unplaced: `spawn duel` refuses, `spawn fight` silently fights with the *ascendant*), #1068 (CLAUDE.md lists a `move agent` the headless CLI lacks), #1071 (the trace ring evicts `fight.ending` between reads), #1055 (spawn stacking past ~20), #1057 + #1072 (no `__DEBUG` opener for a specific location page or a hex-zoom view) | ~60 min over 7 rows; #1069 could have produced false evidence without anyone noticing | **Quick win 1** (CLAUDE.md corrected, both `@hero` traps named). The two debug-opener rows (~35 min) go on the watch list. A third starts a ticket for a generic `__DEBUG.openSurface` |
| **Gates and hooks that give false signals** | #1062 (`check:predicate-copies` reads any `grep -vE` as the docs predicate: PR red ~12 h), #1064 (`check:plan-doc-liveness` never fetches, so it reports a false STRANDED right after a merge), #1065 (the `lint:plan-doc` pre-commit hook prints `[ERROR]` and commits anyway), #1067 (`gameplay-report` writes a tracked-looking file) | ~20 min of work, ~12 h of PR delay | **Quick win 2** (fetch before liveness in the design-session skill). #1062, #1065 and #1067 each have a named durable fix in their own row. Each is one occurrence, below the bar |
| **Lanes dying mid-run or blocked by model limits** | #1059 (~22 h stranded, fixed by THR-1529), #1063 (died after its WIP push; tests and docs were stranded 4 h), #1060 (intent-judge's `fable` hit a 429 overnight, and the skill's rule would have parked six plan docs) | ~40 min of work, ~26 h of delay | **Quick wins 3 and 4** (a second WIP push after the tests are written; a named intent-judge fallback for a rate-limited frontier model) |
| **Shell quoting rewrites the payload** (a known family) | #1056 ×2 (`node -e` in double quotes stripped backticks, and four docs were written corrupted), #1070 (a `node -e` rewrite, then Vite kept serving the stale module) | ~25 min | Already in the catalog (`Docs/ops/sandbox-limitations.md` heredoc bullet) and in memory. #1056 happened in design sessions, which read the catalog less. Watch |
| **Linear and scheduler automation acts on its own** | #1061 (closing a map's last child auto-completes the map before the carve-up comment), #359 ×3 (any enable/disable can drop a cron) | small | **Quick win 5** (wayfinder close order). #359 is a platform bug (`cant-fix`); the registry already makes the diff after every toggle mandatory |
| **Test timing** | #1054 ×4 (resolved by THR-1517), #1073 (a heavy-lane 5 s timeout under full load that passes when run alone) | small | #1054 is closed. #1073 is the first tally for `peopleThingsCells` |
| **A false claim in a docblock** | #1058 (`REALM_RANK_ACCESS` said "read by nobody") | ~20 min avoided (intent-judge caught it) | Fixed in-row. The sweep suggestion (`grep "read by nobody"`) is cheap and on the watch list |
| **Impact labels misread (a finding of this retro)** | 13 of 21 rows tagged `L` on stated costs under 15 min | inflates the retro's time estimate ~2.5× | **Quick win 6** (impediment-reporter: `L` means *Large*) |

## Cross-Signal Patterns

1. **Last cycle's structural responses held.** THR-1512, 1513 and 1514 all shipped, plus two lane-filed fixes (THR-1517, THR-1529). Neither cluster those fixes target recurred in its old shape. #1063 is not a failure of THR-1529 but a gap at its edge, and the probe THR-1529 added is what found the stranded work.
2. **The evidence work moved to the Physical Conflict slices, and the harness did not keep up.** Seven of this cycle's tooling rows come from THR-1538 to THR-1556 sessions trying to *stage* a fight, duel or lair in the CLI or open the right surface in the browser. The pattern is: the engine gains a new kind of scene, but no matching staging lever exists in the CLI or debug bridge. It costs minutes each time, not hours, and one row (#1069) shows it can also produce believable wrong evidence. This is the cluster to watch: if the Monsters and Duels slices keep generating these rows, a "staging levers for new encounter kinds" rule belongs in the plan-doc template's Done-when guidance.
3. **The intent-judge record under-counts during overnight fallback runs.** #1060's night produced 24 judge verdicts across six plan docs and wrote **zero** rows to `Docs/judge-metrics/`. W39 holds one row. So the Step 5c aggregate below is missing roughly half of all judgments ever made. Quick win 4 fixes this going forward; the 24 cannot be honestly backfilled, because latency and overrides were not recorded.
4. **The drift scan's four signals have been flat for two weeks,** while the rulebook's §7 was rewritten three times (THR-1548/1549/1556). Either the rewrites add no broken references, or the scan does not look at §7. This is not yet a finding. If THR-913 stays at exactly 8 through the Monsters slices, check the scan's IMPL-tag parser against a fresh §7 tag.

## Guidance Audit

Doctrine versions this retro (from `Docs/guidance-manifest.json`): **prose@2**, **encounter-authoring@1**, **identity-vision@1**, **cosmology@1**, **rules-of-play@1**, **ui-laws@1**, **architecture@1**, **coordination-process@1**.

Ran: **no.** No doctrine version has moved since 2026-09-18 (the manifest has no commits in the period). The monthly window for all eight doctrines opens 2026-09-28 (prose was audited 08-28; the other seven were registered 08-28 to 08-30), and today is 28 days on. Last week's report called this retro "~09-25/26" as the one that owed the audit. That was a date slip: the window opens three days after this run. **The 2026-10-02 retro owes the audit for all eight doctrines.** Budget it as two runs, prose + encounter-authoring + rules-of-play + ui-laws first, since those four had the most operative-surface churn this cycle through the Physical Conflict content.

## Improvements Made This Session

1. **[CLAUDE.md](https://github.com/christianspliid-ui/threadbare/blob/main/CLAUDE.md) § Headless CLI** (#1066, #1068, #1069): removed `move agent` from the `npm run cli` command list (it exists only in the in-game debug CLI; `scripts/cli.ts` has no `move` case, checked). Added a two-trap note: to place an agent headlessly, rebind `located_at` via `eval`; `@hero` is unplaced in the headless world, so `spawn duel` refuses and `spawn fight` silently fights with the ascendant. ROI 3×3/1 = **9**.
2. **[design-session skill](https://github.com/christianspliid-ui/threadbare/blob/main/.claude/skills/design-session/SKILL.md) § handoff Step 0** (#1064): `git fetch origin main --quiet` now runs before `check:plan-doc-liveness`, and the skill says why. ROI 2×2/1 = **4**.
3. **[pull-work skill](https://github.com/christianspliid-ui/threadbare/blob/main/.claude/skills/pull-work/SKILL.md) § Mid-run WIP push** (#1063): added a second WIP push once the Done-when tests and closeout docs are written, before the full gate run. ROI 2×4/1 = **8**.
4. **[intent-judge skill](https://github.com/christianspliid-ui/threadbare/blob/main/.claude/skills/intent-judge/SKILL.md) § Anti-correlation** (#1060, Cross-Signal 3): a frontier model that is *rate-limited* (429), not absent, now falls back to `opus`. The slip is recorded in the verdict, the plan tail and the metrics row, and a fallback verdict still owes its metrics row. A plain Escalate remains the rule for a frontier model that is truly unavailable. **This is a calibration call made by the agent under the 2026-08-12 rule. Veto invited.** It weakens the anti-correlation guarantee overnight in exchange for delegated work not stalling. ROI 2×4/1 = **8**.
5. **[wayfinder skill](https://github.com/christianspliid-ui/threadbare/blob/main/.claude/skills/wayfinder/SKILL.md) § Closing the map** (#1061): post the carve-up comment *before* resolving the map's last child ticket, because Linear auto-completes the parent about 20 s later. ROI 2×2/1 = **4**.
6. **[impediment-reporter skill](https://github.com/christianspliid-ui/threadbare/blob/main/.claude/skills/impediment-reporter/SKILL.md) § Impact** (analytics finding): `L` means *Large* (15+ min), not *Low*. Rate by the time the Consequence column states. ROI 4×2/1 = **8** (it is the input to every future retro's estimate).

`last_validated_against` was bumped on intent-judge, impediment-reporter and pull-work. wayfinder and design-session already read 2026-09-25.

## Improvements Backlogged

**None this cycle. Checked against the materiality bar:**
- CLI/debug staging levers: ~60 min over 7 rows, but spread across five distinct levers, none recurring 3×. Quick win 1 covers the doc half.
- Gate false signals: each of #1062, #1065 and #1067 is a single occurrence with ~10 min of work lost. The 12 h of PR delay is waiting, not labour. Each row carries its own durable fix.
- Lane death: closed by THR-1529 plus quick win 3.
- The Rule-0 grooming demotion owed by the task prompt: nothing to demote. Continuous Improvement has zero tickets in Todo or Ready for Dev, and the only open tickets created this cycle are the four drift-scan signals (`Idea`) and THR-1577 (a UL proposal). None of those are process tickets.

## Tuning Recommendations

1. **No drift-scan threshold changes.** All four signals are flat. See Cross-Signal 4 for the one check to run if THR-913 stays at exactly 8.
2. **Step 5c: intent-judge kill-criteria aggregation** (`Docs/judge-metrics/`, header-keyed parse; **26 rows**, +1 this cycle, W39):

   | Criterion | Value | Verdict |
   |---|---|---|
   | ≥95% Allow with zero overrides | Allow 80.8% (21/26) | Below the floor: not rubber-stamping |
   | ≥50% override rate on Revise/Block | 0 overrides; 8 rows carry the column, 1 of them a Revise | Measurable but thin; far below the threshold |
   | Median time-to-judgment >300s | 122 s (n=25) | Pass |
   | Zero marginal catches | #1058 is a documented catch this cycle (a false docblock that would have dropped THR-1448's supply side) | User judgement. The row evidence says the judge catches things |

   **Verdict: keep intent-judge.** **Caveat:** the file record missed ~24 fallback judgments on 2026-09-23 (Cross-Signal 3), so these aggregates describe the judgments that were recorded, not all judgments made. Escalation rate is still 0.0%, below its own 0.05 floor, and remains a tuning watch.
3. **The retro-draft heuristic trusts the Impact tag.** Now that quick win 6 has landed, next week's draft should land closer to the stated costs. If it still reads more than 2× the stated costs, have `retro-draft` print a "stated-cost vs tag" warning for rows whose Consequence names minutes under the tag's floor.

## Patterns to Watch

- **Debug openers for specific surfaces** (#1057 location page, #1072 hex zoom): 2 so far. A third means filing a generic `__DEBUG.openSurface(kind, id)` ticket.
- **`@hero` in the headless CLI:** now documented. If a session still stages a god-vs-beast fight as mortal evidence after this note, make CLI `@hero` fall back to `selectDefaultTrackedHero` (the fix named in #1066).
- **False signals from gates:** #1062's extractor fix (accept `grep -vE` only after `git diff --name-only`) and #1065's hook (exit non-zero on `[ERROR]`, or print "advisory") are each one occurrence. A second on either one files it.
- **`node -e` backtick stripping in design sessions** (#1056 ×2): the family is catalogued, but both hits were in design sessions. If a third lands there, add the catalog pointer to the design-session skill.
- **Heavy-lane timeout under load** (#1073, `peopleThingsCells`): tally 1.
- **"Read by nobody" docblocks** (#1058): a one-grep sweep is available to any session in `src/data/`.
- **Guidance audit: the 2026-10-02 retro owes all eight doctrines.** Split it into two runs.
