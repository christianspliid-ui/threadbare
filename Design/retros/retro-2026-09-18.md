# Retrospective — 2026-09-18

## Period
From: 2026-09-12 (exclusive; last retro)
To: 2026-09-18

**Draft-period note (4th consecutive):** `npm run retro-draft` reported "From: 2026-07-31" (689 entries) — the period marker it keys on is the impediment-log footer retired by THR-825. Re-binned by hand to rows #1021–#1051 plus three old-row recurrences (#1011 5th, #172 2nd, #588 3rd). Per the 09-12 report's own standing rule, this **fourth** hand re-bin clears the ≥3-recurrence bar: the fix ticket is filed this run (see Improvements Backlogged). Draft deleted after commit per THR-1056.

## Summary
- Drift-scan issues consumed: **none this cycle.** The four standing signals (THR-911/912/913/915) were last updated 2026-09-11 and consumed by the 09-12 retro; the 09-18 scan had not updated them by retro start. Not a silence streak — last week produced signals — so no signal-health assessment is owed.
- Impediments logged this cycle: **31 entries / 40 recorded occurrences, ~101 min (~1.7 h) heuristic loss.** Impact occurrence-weighted: 1 × L, 6 × M, 33 × S, zero Blocked. The L is the cycle's headline defect (#1049 — a gate script minted a false engine-bug ticket).
- Top categories: verification 13, tooling 13, then content 2 and three singletons (process, testing, ci).
- Improvements implemented this session: **3 quick wins** (verification-gates.md § Browser-verify — the #1032 route contradiction resolved and the THR-1471 theory test voided on evidence; pull-work claim-step trigger strengthened to read the Done-when; sandbox-limitations verify-after-write extended to error outcomes) plus the #1021 occurrence fold.
- Improvements backlogged: _Filed in Step 9._
- **Previous retro's backlog: both tickets executed.** [THR-1470](https://linear.app/threadbare/issue/THR-1470/ul-dashboard-generator-status-parser-drops-annotated-status-values-10) (UL dashboard status parser) shipped and merged this cycle. [THR-1471](https://linear.app/threadbare/issue/THR-1471/classifydiff-prints-the-browser-verify-route-reminder-for-ui-pillar) (classify:diff route reminder) shipped 09-12 — but its delivery is defective: three post-ship recurrences (#1042 ×3) prove the reminder has **never rendered in the ordering it was built for** (it diffs `origin/main...HEAD`, empty pre-commit; and the pickup lane's task file never tells code-diff runs to invoke it). Its theory test is therefore void — see Cross-Signal Patterns 1.
- Previous quick wins held: no recurrence of the upstream-shipped grep false positive (#1015) or the `blockedBy` deadlock shape (#991) this cycle.

## Drift-Scan Signals This Cycle

No scan output landed in this cycle's window. The 09-11 firing of the four standing signals was consumed and trended by the 09-12 retro; between 09-12 and this retro's start the scan lane wrote nothing new (`list_issues label:"drift-scan" updatedAt:-P7D` returns only the 09-11 updates). The 09-18 Friday scan may still fire after this retro's start — if it does, its output rolls to next week's retro, which is the designed cadence, not a loss. No threshold judgment owed.

## Impediment Analytics

Entries #1021–#1051 (31 rows, dated 2026-09-12 → 2026-09-17) plus three recurrences appended to older rows: #1011 (5th, 09-17), #172 (2nd, 09-16), #588 (3rd, 09-12).

### By Category
| Category | Entries | Note |
|---|---:|---|
| verification | 13 | The browser-verify route class (#1025 ×2, #1032 ×4, #1042 ×3), absence-read-as-evidence (#1049 L, #1045, #1047), probe technique (#1037, #1040), deletion survey (#1039), flake triage (#1051) |
| tooling | 13 | CLI batch semantics (#1022, #1026), shell/string mangling (#1023, #1029, #1046), Linear mangling (#1043, #1044), generator traps (#1033, #1034), gate quirks (#1024, #1031), init cycle (#1030), retro-draft (#1021) |
| content | 2 | #1035, #1036 — authored-but-ungrantable ids across parallel catalogs |
| process / testing / ci | 3 | #1041 (`--fix` dedupe hazard), #1048 (heavy-lane flake tally = 1), #1050 (CI timeout re-run) |

### By Impact (occurrence-weighted)
| Impact | Occurrences | Ids |
|---|---:|---|
| L | 1 | #1049 (false engine-bug ticket THR-1510 minted by `check:undertaking-live`'s fail branch) |
| M | 6 | #1022, #1024, #1027, #1030, #1048, #1051 |
| S | 33 | rest (multi-count: #1025 ×2, #1032 ×4, #1042 ×3; old-row recurrences ×3) |
| Blocked | 0 | — |

Unresolved blockers (Workaround Found = No): none.

### Root-cause clusters

| Cluster | Volume | Exemplars | Disposition |
|---|---|---|---|
| **Browser-verify route decided at the capture — and every mitigation layer failed for a different reason** | 10 occurrences (#1025 ×2, #1032 ×4, #1042 ×3, #1011 5th) | #1032 surfaced the standing contradiction: two rows (#1027, #1028) confirmed Bash-started `npx vite` works unattended and yields real 1920×1080 captures, while canon sanctions jsdom-render — evidence strength decided by which document a session read. #1042 falsified THR-1471's delivery: the reminder cannot fire pre-commit, and one lane never invokes the script at all. Occurrences 8–9 showed claim-time jsdom was *stronger* evidence for structural changes; the 10th (THR-1494) owed browser evidence with zero UI-pillar paths touched — the trigger must read the Done-when, not the diff | **Quick wins 1–2** (route doctrine written in one place; claim trigger sharpened) + **backlog T-2** (fix the reminder's input and wire it into the pickup lane) |
| **Absence of evidence read as evidence of absence** | 5 entries, 1 × L | #1049: `check:undertaking-live` treats a trace evicted from the 2000-entry ring as a dropped seed — a Medium engine bug (THR-1510) was authored, promoted and claimed on that sentence; re-measurement showed 17/17 seeds consumed, 0 dropped. Same misread shape: #1047 (piped `test:heavy` exit code belongs to `tail`), #1045 (tick 200 silently stops at 175), #1040 (empty portal container reads as unrendered), #1037 (placeholder-only paragraph reads as broken enricher) | **Backlog T-3**: live-proof + census scripts decide consumption off `state.tickEvents`, never off the ring. The rest are logged with their discriminators (echo actuals; read state) |
| **Shell/string mangling — mapped `cant-fix` family, one new mechanism** | 3 entries | #1029 (heredoc collapses `\'`), #1023 (bare `python -` hangs, `\|\|` unreachable), #1046 (**new**: `String.replace` replacement string containing `$'` splices the file tail — function replacer is the fix, memory updated) | Tallies; family fully documented |
| **Linear MCP mangling family — one new mechanism** | 2 entries | #1043 (**new**: `save_issue` create returned `502 upstream_unavailable` while the issue was created — a retry files a duplicate), #1044 (bare `#N` corruption survives patch edits; only `replace_range` removes it) | **Quick win 3**: verify-after-write extended to both outcomes in the sandbox catalog |
| **Generator/emitter gate gaps** | 4 entries | #1033 (unguarded `main()` makes importing a helper a file write), #1034 (emitted test never parsed by any gate), #1038 (wiki-freshness vs generated-freshness disagree by construction on the one generated wiki page; 5 sessions have re-derived the exemption), #1031 (interface-map symbols must live where they are spelled) | Documented per-row; #1038 is one row wide and watch-listed |
| **Test flakiness, three distinct shapes** | 4 entries | #1048 (heavy-lane `controlRenewalReach` flake — tally starts at 1; the verdict arms are full-lane-clean + full-lane-repeat), #1050 (CI orchestrator timeouts at the default 5000ms on a Codex-only diff — the known ~2.2× CI variance class; re-run separated it), #1051 (Windows NTFS mtime skew makes `check-lane-silence` deterministically red in the full local suite; durable fix named in-row), #588 3rd hit | Watch-listed; no single fix clears the bar |
| **Content catalog split** | 2 entries | #1035 (`trait.condition.debt-laden` authored but grantable by nothing), #1036 (`assign_ambition` accepts one of three same-shaped ambition catalogs) | The structural fix is the in-flight content-model program (THR-1481 registry + tag vocabulary); promote only if slices land without closing the visibility gap |

## Cross-Signal Patterns

1. **The route-ordering class has produced its complete diagnosis: four mitigation layers, four different failure modes.** The canon rule was unread at the binding moment; the pull-work rule was read too late (and then, in occurrences 8–9, correctly early); the classifier reminder was keyed to an input that is empty at the moment it targets; and the pickup lane's task file never names the script that carries it. The lesson generalizes beyond this class: **a shipped mitigation owes a delivery test — proof it actually renders/fires in the ordering it was built for** — before its recurrence tally is allowed to mean anything. THR-1471's status entry drew an escalation inference ("next rung is a hard gate") from a tally that carried zero discoverability evidence.
2. **Absence-as-evidence produced this cycle's only L — and the producer was a script, not a person.** The premise-defect class the last three retros tracked was human ticket-authoring; #1049 is the first time a *gate* authored the false premise (`check:undertaking-live`'s fail branch), and it cost more than any human one this cycle (a false Medium bug promoted and claimed; the real gap THR-1511 hidden behind it). Executor falsification caught it pre-implementation — the catch machinery still holds at 100% — but a script producer is mechanically fixable, which humans are not. T-3 does that.
3. **Volume flat, severity concentrated.** 31 entries / ~1.7 h against last cycle's 46 / ~2.1 h. The wide flat field of S-impact tool semantics persists (shell mangling, Linear mangling, CLI REPL quirks — all mapped families), while the losses that matter came from exactly two shapes: route-ordering (10 occurrences) and absence-as-evidence (1 L). Both get structural responses this cycle.
4. **The evidence-strength question resolved itself with data.** Occurrences 8–9 of #1032 demonstrated jsdom-render is *stronger* than a screenshot for text/structure/absence assertions; #1027/#1028 demonstrated Bash-vite delivers real paint evidence unattended, twice. The direction call (quick win 1) is a decision rule keyed to the Done-when, not a winner: text/structure → jsdom; paint/layout → bash-vite with the tree-identity guards. Recorded as an agent-made calibration call per the 2026-08-12 rule; veto invited.

## Guidance Audit

Doctrine versions this retro (from `Docs/guidance-manifest.json`): **prose@2**, **encounter-authoring@1**, **identity-vision@1**, **cosmology@1**, **rules-of-play@1**, **ui-laws@1**, **architecture@1**, **coordination-process@1**.

Ran: **no — no version movement since 2026-09-12; baselines unchanged** (prose audited 2026-08-28; the seven others registered 08-28 → 08-30). The monthly window opens ~2026-09-28, so the **next retro (~09-25/26) runs the audit for all eight doctrines** — flagged two cycles running: budget for it or split across two runs. Version line recorded above for that retro's trigger.

## Improvements Made This Session

1. **[`Docs/canon/verification-gates.md`](../../Docs/canon/verification-gates.md) § Browser-verify — the #1032 contradiction resolved and the THR-1471 paragraph corrected** (#1032 ×4, #1027, #1028, #1042 ×3): a new sanctioned route names Bash-started worktree Vite (`npx vite --port 5191 --strictPort`) for unattended runs whose Done-when needs real paint, with the two mandatory tree-identity guards (code-symbol marker, re-probe after every edit); a decision rule keys the jsdom-vs-bash-vite choice to the Done-when's subject; and the THR-1471 paragraph now records that its theory test is void until the reminder can render pre-commit. ROI 4×3/1 = **12**.
2. **[`.claude/skills/pull-work/SKILL.md`](../../.claude/skills/pull-work/SKILL.md) — claim-step route trigger reads the Done-when, and is named the only binding enforcement point** (#1025, #1032 10th, #1042): the trigger text now leads with "Done-when and labels, not the diff" (THR-1494 owed browser evidence with no UI path touched), states that no diff classifier can carry the decision pre-commit, and lists the bash-vite route among the claim-comment options. ROI 4×3/1 = **12**.
3. **[`Docs/ops/sandbox-limitations.md`](../../Docs/ops/sandbox-limitations.md) — verify-after-write covers error outcomes too** (#1043): the #48 bullet now carries its mirror image — a `save_issue` that returns `502 upstream_unavailable` can still have landed, so the board is read back after **both** outcomes, and only what the read-back shows absent is retried. ROI 2×3/1 = **6**.

Data fold: `Docs/impediments.md` row #1021 count 3 → 4 (this run's re-bin is the fourth; the row's own rule files the ticket).

## Improvements Backlogged

_Filed in Step 9._

## Tuning Recommendations

1. **No drift-scan threshold changes** — no scan data this cycle; the four standing signals were consumed last week and trend there.
2. **Scan/retro timing:** if the Friday 14:00 UTC scan repeatedly lands after the 15:00 UTC retro reads the board, every retro consumes week-old signals. One data point so far (this run); if next week's retro also finds the scan silent at start, check the scan lane's actual fire time against the registry before assuming a lane fault.
3. **Step 5c — intent-judge kill-criteria aggregation** (`Docs/judge-metrics/`, header-keyed parse; **25 rows, +2 this cycle** — both Allow, ~142 s / ~120 s, both `Overridden: no`):

   | Criterion | Value | Verdict |
   |---|---|---|
   | ≥95% Allow with zero overrides | Allow 80.0% (20/25) | Below the floor — not rubber-stamping |
   | ≥50% override rate on Revise/Block | 0 overrides on the 1 Revise row that carries the column (4 predate it) | **Measurable at last** — the prospective `Overridden` column is now live on 7 rows (W35–W37); far below the kill threshold on the data that exists |
   | Median time-to-judgment >300s | 121 s | Pass |
   | Zero marginal catches | — | User judgment, not row-derived |

   **Verdict: keep intent-judge.** The override criterion has crossed from NOT MEASURABLE to measurable-with-thin-data, which retires the last structural gap in the kill criteria. Escalation rate remains 0.0% against its own 0.05 floor — still a tuning watch, not a kill signal.

## Patterns to Watch

- **The theory test resets when T-2 lands.** Only after the reminder provably renders pre-commit *and* the pickup lane invokes it does a recurrence of the route-ordering class say anything about discoverability. Until then, no hard-gate escalation on any tally.
- **Heavy-lane flake tallies:** `controlRenewalReach` at 1 (#1048); the full-suite worker-contention class at 3 files (#588). #1051's Windows mtime skew has a named durable fix (assert against `m.sinceMs`, not a second wall-clock read) for whoever is next in `scripts/pause-marker.ts` tests.
- **CLI twilight halt** (#1022 M, #1045): `tick N` fires its phase-change guard on every tick once the phase is terminal. Two occurrences in one cycle; a third converts to a ticket (the fix is comparing against the *previous* phase).
- **Content catalog grantability** (#1035, #1036): if the content-model registry slices (THR-1481 family) land without making grantable-vs-decorative visible at the grep, promote.
- **`check:impediment-ids --fix` dedupe hazard** (#1041): it should refuse to dedupe two rows that differ outside the Count column; until then, fold by hand on any union collision touching a multi-occurrence row.
- **Playwright artifact roots are plural** (#172 2nd occurrence, 09-16): some sessions' MCP roots to a OneDrive `.playwright-mcp` dir rather than the home tree root — the absolute-path workaround covers both, but the catalog bullet names only one root.
- **wiki-freshness vs generated pages** (#1038): one manifest row wide today; recurs for every session editing an interface-contract row.
- **Guidance audit next retro:** eight doctrines come due together ~09-28. Budget or split.
