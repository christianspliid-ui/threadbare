# User Action Required

**Last updated:** 2026-07-30 11:56 local, by the hourly `keep-work-flowing-cc` CC task.

**ZERO OPEN CHRISTIAN-OWNED ASKS — thirteenth consecutive published run at zero, eighth at a true empty (no standing asks either).** Discord inbox **genuinely empty** for an eleventh consecutive read — the `after=` cursor returned a literal `[]` (2 bytes). Home tree on `main`, **0 behind**, 0 stranded, 2 tracked-dirty (settled). Deploy **`skipped`** at `421c3e82` — correctly flat, docs-only merges since. Merge gate `healthy`, `startupFailureCount: 0`. Fleet heartbeat `ok`. **The run's substantive results: (1) `mergeStateStatus: BLOCKED` CONFLATES A CHECK THAT IS STILL RUNNING WITH A CHECK THAT HAS FAILED — #1107 and #1103 read the identical field value with opposite prognoses, and finding 29(a)'s self-heal result applies ONLY to `BEHIND`; a failed required check never self-heals, so THR-868's UI half will sit indefinitely (earned finding 43); (2) THE mtime FIX SPECIFIED LAST RUN FOR FINDING 36 HAS NO DISCRIMINATING POWER — `orchestrator-2026-07-30g/h` carry the same mtime to the second, so the proposed repair would have had nothing to choose between (finding 37 applied to a fix rather than a test); (3) last run's stated test on #1096 PAID OUT UNFAVOURABLY — it crossed the benign floor and went `DIRTY`; (4) the corrected publish witness (finding 42) confirms healthy on first application — #1104 merged 09:07:44Z.**

## This run's measurements (2026-07-30 09:56 UTC)

**The publish path is healthy, measured with the corrected witness.** `gh pr list --state all --search docs/briefing` shows **#1104 MERGED at 09:07:44Z** (the 08:58Z brief) and **#1101 MERGED at 09:02:41Z** (the 07:53Z brief, which last run had wrongly suspected of being a lost run). Deltas below span one hour of board state.

- **`mergeStateStatus: BLOCKED` CONFLATES A CHECK STILL RUNNING WITH A CHECK THAT HAS FAILED — the run's strongest result, and it lands directly on finding 29(a)'s scope.** Two open PRs read the identical status this hour with opposite prognoses:
  - **#1107** (`?forceencounters`, opened 09:55:29Z): `Test · Typecheck · Build` is **`IN_PROGRESS`**. `BLOCKED` here means *not yet*, and it will clear itself.
  - **#1103** (THR-868 UI half, opened 08:46:21Z): the same check **`FAILURE`** at 09:49:16Z. `BLOCKED` here means *never*, absent a new push.
  - **The step-level read is what separates them, and it is one command.** `gh run view <id> --json jobs`: steps 1–9 all `success` (`npm ci`, process lint, **tests**, encounter-content lint, **typecheck ratchet**), step 10 **`Generated artifact freshness` failure**, steps 11–12 (wiki freshness, production build) `skipped`. **Not a timeout** — 17m19s wall time invited that reading (memory `ci_red_may_be_timeout`), and the step conclusions refute it.
  - **The defect is the known one:** `check:generated-freshness` compares against `HEAD`, so a closeout edit to a generated-artifact *source* lands after the gate's numbered position and ships a stale dashboard (THR-755 row 6 / impediment #201 / memory `generated_not_built`). Fix is `npm run prebuild` + commit.
  - **Why this matters beyond one PR: finding 29(a) established that an armed PR self-heals — and that result is scoped to `BEHIND` only.** `BEHIND` clears when `main` moves; a failed required check clears when *someone pushes*. **Both surface as `BLOCKED`, and the operational rule inverts between them.** Applying 29(a)'s "leave it, it lands within ~2h" to #1103 would leave THR-868 parked indefinitely while reading as healthy patience. **Earned finding 43.**
  - **Scope honestly: #1103 failed 7 minutes before brief time.** Its owner may already be mid-fix. The item is the *conflation*, not an abandonment claim.
- **THE mtime FIX SPECIFIED LAST RUN FOR FINDING 36 HAS NO DISCRIMINATING POWER, AND THIS RUN IS THE COUNTER-EXAMPLE.** Last run's fix shape was "select by explicit mtime (not glob order)".
  - **`orchestrator-2026-07-30g.md` and `-30h.md` both carry mtime `08:20:23` — identical to the second.** An mtime sort has nothing to choose between them, so the proposed repair would have fallen back to whatever order the shell handed it: exactly the coupling it was meant to remove.
  - **No consequence this hour** — both sections read "Nothing needs you", so either choice was correct. **The instrument failed silently on a benign input**, which is the only way this was ever going to be discovered.
  - **This is finding 37 turned on a *fix* rather than a *test*: a repair also needs power, and "newest by mtime" has none at a tie.** The correct discriminator is the producer's own sequence marker (the `b`/`c`/…/`h` suffix *is* the ordering), or an explicit ordinal in the file. **Fix shape, corrected: read all in-window same-task reports and union their sections** — then "newest" never has to be decided at all.
  - **Correction to last run's count** (finding 39 — re-measure, do not inherit): it recorded "NINE files on disk"; `ls -1 Docs/ops/orchestrator-2026-07-30*.md | wc -l` returns **8** (`-30`, `-30b` … `-30h`), with run i still unmerged in PR #1102.
- **Queue: BACKED UP — 56 ready (9 Urgent / 3 High / 7 Medium / 37 Low), 2 In Dev.** **Flat across the hour: 56 → 56, and the priority profile is byte-identical to last run.** Nothing closed, nothing filed to the shelf. Nothing stale: oldest `updatedAt` is THR-739 at 2026-07-24T20:29:29Z, **~5.6 days**, inside `STALE_ISSUE_DAYS`=7.
  - **A flat hour is a working hour, not a stalled one** — both In Dev rows carry open PRs pushed this hour. **Recorded as flat rather than as decay** (finding 38: a count against a periodic process is phase, not health).
  - **The nine Urgent are still the WS5 Batch 1 partition** (THR-856, 848, 855, 858, 859, 860, 861, 863, 864), file-level mutex with each other on `src/data/encounter-content.ts` + the `WS5_MIGRATED` test array. **Not starvation** — WIP=1 serialises them anyway. **THR-858 reads `Blocked by: nothing`.**
  - **WIP IS 2, WHICH BREACHES THE WIP=1 RULE — and the second row was created by Christian in chat, not pulled from the shelf.** THR-878 went `Idea` → `In Dev` at 09:53:03Z, **85 minutes after being created at 09:52:28Z**, never passing through Ready for Dev. Both rows are stamped `assignee: "Christian Spliid"`, which per closed ask #5 carries no information about who holds them.
  - **THR-878** (High) — *`?forceencounters` debug flag*. Requested by Christian in chat this morning: he could not judge the WS5 content by playing, because most encounters are authored at background/shaping attention tier and resolve silently. Ships a URL flag that survives production (unlike `window.__DEBUG`, stripped in prod), widening **already-threaded** agents to full prose + full choice set + immediate interrupt, while leaving dormant agents invisible. PR #1107, checks in progress. **The permanent four-tier→binary simplification he also asked for was filed separately rather than bundled** — correct call, it touches `VISIBILITY_BY_POSITION`, the Retinue/ThreadTugBadge UI, `attentionTier.ts`'s TIER_MATRIX and a wiki page.
  - **THR-868** (High) — *Meet The First conversion*. Engine slice shipped 07:31Z; UI half is PR #1103, **now red on the generated-artifact gate** (see the finding-43 item above). Three deferrals split out rather than bolted on: THR-872, THR-874, THR-875 — **and THR-874's subject (no dev URL reaches Meet The First) is adjacent to THR-878's**, worth noting for whoever takes either.
- **THE ASSIGNEE-LEAK TEST: the stated 12:00Z deadline has still not arrived, and this run did NOT re-check the three rows.** Last run committed the next run to re-reading **the same three** (THR-863, THR-864, THR-867) past 12:00Z. It is 09:56Z. **Re-reading them now would produce a second one-interval sample and nothing else** (finding 37 — a test below its threshold licenses nothing). All three are present in this run's Ready-for-Dev payload with no `assignee` field, which is consistent but not the test. **The commitment carries to the first run after 12:00Z.**
- **Deploy (step 2.5): `skipped` — the publish point is UNCHANGED at `421c3e82`, correctly.** Verbatim: *"The live site is up to date. Commits since the last publish (421c3e82) only touched notes and docs, so the game itself did not need rebuilding."* `needsChristian: false`. **Finding 28's flat state again, and consistent with the merge log** — everything since 421c3e82 is docs/ops.
- **Merge gate (step 2.5b): `healthy`, `startupFailureCount: 0`, `standDown: false`.** **Nineteenth consecutive independent confirmation** that Christian's billing action holds. **Load-bearing this run:** it is what licenses reading #1103's red check as a real content failure rather than the billing signature (a billing block records the required check as `skipped`, which *satisfies* protection — the opposite failure).
- **Step 2.7 (heartbeat) returned `ok`.** Verbatim: `{"verdict":"ok","needsChristian":false,"checked":8,"stalled":[],"neverRun":["monthly-rulebook-review"],"summary":"All 8 enabled scheduled tasks are within 2 slots of schedule."}`. **Eighteenth consecutive `ok`** — lanes fired 09:01Z / 09:26Z / 09:53Z at **distinct** timestamps, so the wake-boundary defect (finding 30) is untriggered rather than merely quiet.
- **Reaper healthy — ran 11:40:02 local (09:40Z), 16 minutes before this brief.** `SUMMARY:` **41** worktrees / **57** branches / **0** stashes / **0** needs-disposition. **Stashes held at 0 for a fourth consecutive hour**, confirming THR-680's drain did not re-accrue (finding 31's re-accrual test, three intervals in). Worktrees 40 → 41 and branches 54 → 57 over the hour — **this lane adds one of each per run and does not clean up after itself**, which is the reaper's job by design.
- **HOME TREE CURRENT: on `main`, 0 behind, 0 stranded, 2 tracked-dirty, 1 untracked.** The tracked dirt is `.claude/settings.json` + `.claude/settings.local.json` — settled per correction (c); the untracked file is Friday's retro draft. **No reflog inference was attempted** (finding 40): the outcome — 0 behind — is the measurement.
- **Discord: the cursor returned `[]` — 2 bytes, zero messages, eleventh consecutive read.** No receipt owed, none sent; cursor left at `1532100414189867109`. `allowFrom` was read from `access.json` **before** any classification, preserving fail-closed ordering with nothing to authorize. **Silence is not evidence he is away** (finding 8) — and this run has direct proof of that: **he was active in chat at 09:52Z**, filing THR-878, while the Discord channel read empty.
- **Ping gate (step 6): no ping sent, correctly.** Stored hash `e3b0c442…b852b855` is the SHA-256 of the empty string, **verified by computing it this run rather than assuming it**; this run's Needs-Christian carries no items, so the hash matches and the doorbell stays silent.
- **Sibling reports (step 2.6): two in-window, no items folded as asks.** `orchestrator-2026-07-30h.md` reads *"Nothing needs you. One clean promotion this run…"* — **empty state, skipped.** `backlog-grooming-2026-07-30.md` (mtime 09:50:03, refreshed since last run) is the **non-empty-but-ask-free** case: *"Nothing blocking… No decision needed today; the board state is already correct."* **Folded as context, not as an ask** (finding 7). **Third consecutive observation of that third state** — the step-2.6 rule still does not name it.
  - `weekly-hygiene-2026-07-26.md` is **~4 days**, outside the 36h window.
  - **Same-day set measured at EIGHT files on disk** (`-30` … `-30h`), correcting last run's "nine"; run i is unmerged in PR #1102. **mtime is now TIED between `-30g` and `-30h`** (both 08:20:23), so lexical order is the only thing still deciding — see the finding-36 item above.
- **Zero Linear writes.** Two column reads (`Ready for Dev`, `In Dev`) and two `get_issue` calls (THR-878, THR-868). Read-only throughout.
- **This run's worktree was created outside `.claude/worktrees/`** (`Dev/Projects/kwf-wt-20260730-0956`), with `rev-parse --show-toplevel` asserted after creation, **the `.git` file's existence checked (THR-797), and the home tree's branch re-read as `main` after the `worktree add`.** **The home tree was touched by `git -C` reads, one `git worktree add`, two read-only `npm run` probes, `gh` reads, and nothing else.**

## Open agent work (not Christian's)

- **PR #1103 IS RED ON A REQUIRED CHECK AND WILL NOT SELF-HEAL — the highest-value agent-side item this run, and the one most likely to be misread as patience.** THR-868's UI half failed `Test · Typecheck · Build` at 09:49:16Z on step 10, **`Generated artifact freshness`**; steps 1–9 including tests and the typecheck ratchet all passed.
  - **The fix is one command plus a push:** `npm run prebuild`, commit the regenerated artifacts, push to the same branch. **The cause is structural, not careless** — `check:generated-freshness` compares against `HEAD`, and `Docs/impediments.md` is itself a generated-artifact *source*, so a closeout impediment logged at its documented position lands after the gate ran (THR-755 row 6, impediment #201, memory `generated_not_built`). **This is why CLAUDE.md numbers the step 3b but says run it LAST.**
  - **The trap is that `mergeStateStatus` reads `BLOCKED`, exactly like a PR whose checks are merely still running.** #1107 read `BLOCKED` at the same moment with its check `IN_PROGRESS`. **Finding 29(a) licenses leaving a `BEHIND` PR alone for ~2h; it licenses nothing about a failed check**, which clears only on a push. **Earned finding 43.**
  - **Scope honestly: 7 minutes old at brief time**, so its owner is plausibly mid-fix. **The durable item is the conflation, which wants a ticket** (finding 23): any lane that reads `mergeStateStatus` to decide whether to intervene must read the check conclusions too, or it cannot tell "wait" from "act".
- **THIS LANE'S PUBLISH GAP IS CLOSED, AND THE CORRECTED WITNESS CONFIRMED HEALTHY ON ITS FIRST APPLICATION.** `gh pr list --state all --search docs/briefing` returns **#1104 MERGED 09:07:44Z** and **#1101 MERGED 09:02:41Z** — both briefs landed, including the one last run had suspected of being lost.
  - **The detector request survives as a *step-5 self-check*, not as an investigation** (finding 23): `gh pr list --state all --search docs/briefing --limit 3`; a slot with neither a merged commit nor a PR is the real gap. Everything else is merge latency.
  - **Both prior alarms are now explained and neither recurs.** Per finding 41 the *question* stays retired only because the instrument changed, not because two quiet hours passed.
- **THIS LANE'S SELF-COLLISION RISK IS REAL BUT DID NOT FIRE THIS RUN, AND THE REASON IS THE RULE.** `Design/briefing.md` is a full-file rewrite and **deliberately not** in the `merge=union` set, so two concurrent briefing PRs cannot merge cleanly.
  - **This run branched fresh off current `origin/main` with the predecessor already merged** — #1104 landed at 09:07:44Z, 49 minutes before this run started. **No race was possible**, which is precisely what last run predicted: an armed predecessor always merges, so the conflict the clever mitigation was avoiding cannot occur.
  - **The retired mitigation stays retired, with its reason recorded:** clearing `BEHIND` on an armed PR *is* pressing merge, so you cannot piggyback onto one. **Wants a ticket** (finding 23) to encode "branch fresh, never touch the predecessor" in step 5.
  - **Note the shape:** the exposure is real but requires `main` busy *and* the predecessor unmerged at fire time. **Record as accumulating exposure, not accumulating safety** (finding 36) — two clean hours are not a fix.
- **A SECOND ALREADY-SHIPPED-UNDER-PARENT CHILD, SAME PARENT, WITHIN THREE HOURS — now a mechanism, and the remedy moves upstream.** THR-680 (05:23Z) and THR-681 (`completedAt` 07:22:55Z) are both children of **THR-674**, both promoted carrying scope that had already shipped. **Logged as impediment #310.**
  - **Finding 26 is the governing rule: the second instance with the same fingerprint changes the remedy.** A staleness re-check at *pickup* catches each instance one at a time and costs a slot every time. **The cheap detector is at the split** — when a parent is broken into children, record which child carries scope already delivered under the parent's id.
  - **What the executor did right, and should be the pattern:** stood down on discovery, **left the row In Dev and unassigned rather than consuming the WIP slot**, and the grooming lane *verified before closing* (`b55b0b22` ancestor of `origin/main`; disposal doc present; reconciled paragraph live at `.claude/skills/pull-work/SKILL.md:91`) instead of trusting the stand-down comment. **Memory `stale_ticket_gate` paid out.**
  - **Wants a ticket** (finding 23) — for the split-time detector, not for a third pickup-time check.
- **THR-871 IS FILED BUT SITS IN `Idea`, WHICH IS THE UNREACHABLE COLUMN** (finding 19 / THR-835's shape). Filed Medium by the grooming lane against CLAUDE.md's priority-1 deferral bucket being unexecutable — `pull-work` sorts on priority alone, all 25 deferrals are `Low`, and the WS5 partition keeps `Medium` work continuously in queue.
  - **The ticket about the unreachable bucket is itself parked in the column the promotion lane must reach into.** Not a defect in the filing — `Idea` is where a decision ticket with four options belongs — but worth stating plainly so it is not assumed to be queued.
  - **Belongs to the orchestrator's promotion lane.** No Christian input needed: this is a workflow-ordering call, not a design-vision one.
- **The assignee-leak instrumentation is TICKETED — THR-867** (*"Instrument the orchestrator's assignee-clear write sequence…"*). **Priority correctly `Low`: the leak is cosmetic while the reader stays indifferent** (THR-845 removed the filter; THR-681 was claimed while stamped).
  - **The stated test HELD one interval in as of 08:54Z**, and **this run deliberately did not re-run it.** It is 09:56Z; the stated deadline is 12:00Z. A second sub-threshold sample would license nothing (finding 37) and would read as accumulating evidence (finding 34).
  - **The commitment carries to the first run after 12:00Z, on the same three rows** (THR-863, THR-864, THR-867) — not whatever is stamped then. All three appear unassigned in this run's payload, which is consistent but is not the test.
  - **A positive control exists and was observed incidentally:** THR-878 and THR-868 both carry `assignee: "Christian Spliid"` on the In Dev call, so the field is genuinely being read rather than silently omitted (memory `vacuous_probe`).
- **The step-0 allowlist snippet wants a one-token path fix**: `$HOME` → `$USERPROFILE` or a Windows-resolvable literal, because Windows `python` cannot open the MSYS-style path `$HOME` expands to. **Wants a ticket** (finding 23).
- **Step 2.6's section extraction is keyed on the producer's incidental formatting AND its file naming, and neither is stable — and THE mtime FIX SPECIFIED LAST RUN IS NOW FALSIFIED.** The file-selection axis is **eight-deep on disk** in a single day (measured, correcting last run's "nine"), with run i unmerged in PR #1102.
  - **`-30g` and `-30h` carry the identical mtime `08:20:23`.** "Select by explicit mtime" therefore has **nothing to choose between them** and degrades to shell order — the exact coupling it was proposed to remove. **A fix needs power just as a test does** (finding 37, applied to a repair).
  - **Corrected fix shape: stop deciding "newest" at all.** Read *every* in-window report per producing task and union their `Needs Christian` sections; de-duplicate on item text. A lane that writes eight reports a day has no single current one, and any tie-break is a guess.
  - **The formatting axis is unchanged and still live:** `orchestrator-2026-07-30.md` uses `## Needs Christian`; `orchestrator-2026-07-29.md` uses `### Needs Christian` — same lane, one day apart, two levels. Match the *text* with a permissive level (`^#{2,6} Needs Christian`), terminate on a same-or-shallower heading. **Wants a ticket** (finding 23).
  - **A THIRD section state surfaced this run, and the rule does not name it.** `backlog-grooming-2026-07-30.md` is **non-empty but ask-free**: *"Nothing blocking… No decision needed today; the board state is already correct."* The step-2.6 rule tests for the *empty* state only, so a section like this reaches the fold-in step and must be judged by hand — and judged wrongly it becomes exactly the finding-7 failure (surfacing a settled call for ratification). **Fix shape:** treat *no-decision-required* as a first-class skip alongside empty, and fold such sections into the brief as context rather than as items.
- **`check:task-heartbeat` mis-fires at a wake boundary — carried, not re-derived this run.** The probe accepts any sibling whose `lastRunAt` falls **inside** the stall window as its liveness witness; after a host sleep the whole fleet's `lastRunAt` clusters at the window's *closing* edge. **Fix shape:** require the witness to have fired strictly inside the window with a margin, or detect the burst directly (≥2 lanes sharing a `lastRunAt` to the second ⇒ host-wake, suppress). **Wants a ticket** (finding 23).
- **Verify-after-write needs a clock — carried; THR-860's self-refuting row remains the best artefact.** Its description asserts a `get_issue` re-query confirmed the assignee absent; the field was present on read. **The claim and its refutation live in the same row.** **Fix shape:** verify after the last side-effect in your own sequence, or on the next run.
- **FIVE open PRs; #1101 and #1104 from last run's list both MERGED, so the backlog is churning rather than accreting.** All have auto-merge enabled; none needs Christian.
  - **#1103 `BLOCKED` — RED, not waiting.** The WS6 UI half; required check `FAILURE` on `Generated artifact freshness`. **The one PR here that cannot resolve itself.** See the finding-43 item at the top.
  - **#1107 `BLOCKED` — waiting, not red.** The `?forceencounters` flag, opened 09:55:29Z, check `IN_PROGRESS` at brief time. **Same status string as #1103, opposite prognosis** — this is the pair that earned finding 43.
  - **#1096 `DIRTY` — LAST RUN'S STATED TEST PAID OUT, UNFAVOURABLY.** Last run set the checkpoint explicitly: *"if it is still open next run it crosses that floor and becomes one."* It is still open at **~2h49m** (armed 07:06:52Z), past the ~2h07m benign band, **and it has gone `DIRTY` rather than merely slow.** Fix is `git merge origin/main && git push` — GitHub's server-side merge ignores `merge=union`, so the web resolver is the wrong tool (memory `dirty_pr_duplicate`: check the diff direction first; this is the impediments doc, a union-attributed file, so a regressing duplicate is unlikely but must be confirmed). **Unclaimable from this lane; wants a write-remit session.**
  - **#1102 `BEHIND`** (orchestrator run i — promote THR-872, hold THR-873), opened 08:30:56Z, **~1h25m**, inside the benign band. Its report file is the same-day `orchestrator-2026-07-30*` collision class (THR-849).
  - **#1031 `UNKNOWN`, ~62h idle** (orchestrator/THR-762, superseded report doc). **Genuinely stuck and genuinely disposable: one `gh pr close`, zero content lost** (superseded, not regressing). **Unclaimable from this lane; wants a write-remit session.**
  - **Finding 29(b) held again — `UNKNOWN` on one of five, no re-read attempted** (finding 34: a second sample of a field known to be unstable across *time* adds nothing).
- **THR-835 is unreachable in `Idea`** — carried. The required check has repeatedly run at 80–90% of its 20-minute cap, which is exactly the runner-variance hazard THR-835 describes. **A working promotion lane that never reaches a column is stronger evidence than a broken one** (finding 19). Remedy belongs to a write-remit session.
- **`rescue/2026-07-17-detached-plans` is prunable — carried, not re-derived this run.** A **local branch only**; against `origin/main` it carried **4 unique commits** as of 22:54Z. **Pruning it is `git branch -D` in the home tree, which this lane may not do** — not merely unclaimed, unclaimable from here. **Wants a write-remit session with home-tree authority.**
- **The orchestrator lane's stray-write defect is open and agent-owned.** `tb-orchestrator` wrote its 08:29Z report into the home tree as well as its own worktree, blocking a fast-forward. Durable fix is in that lane's write path (absolute paths, or `rev-parse --show-toplevel` asserted before any write — the workaround this task takes hourly). **Wants a ticket** (finding 23).
- **No gate notices live/mirror prompt divergence.** The two were proven in sync for `tb-orchestrator` on 2026-07-30 02:58Z (diff = one trailing newline), so the general defect stands without a live instance: `~/.claude/scheduled-tasks/<id>/SKILL.md` is what runs, `Docs/ops/scheduled-task-prompts/` is a mirror a merge can update while the live copy stays stale, and **nothing compares them.** THR-850 covers the missing mirror for `tb-opus-pickup`; the divergence *detector* is unticketed. **Wants a ticket** (finding 23).
- **Friday's retro draft** (`Design/retros/retro-2026-07-24-draft.md`) is untracked and wants a `git add` in any docs PR. **Not a carry** — it is THR-798's step 2, with an owner and a Done-when.
- **Impediment #267 is used twice**; **#219 and #228 are duplicate rows for one defect**. Both dispositions: fold at the next retro. **No ticket.**

## Durable findings carried forward

Rules earned by tickets that have since shipped. **Kept as rule sentences; every worked example is in `git log -p Design/user-actions.md`.**

1. **Before carrying an observation into its Nth run, re-derive it once.** **Paid out 2026-07-30 02:58Z as a correction** (three runs blamed a missing `python` interpreter; the real cause was MSYS path expansion, and the remedy changed). **Paid out 2026-07-30 03:57Z in the other direction:** the "reader no longer filters on assignee" carry was checked against `pull-work` SKILL.md lines 27/176/184 and **held** — and the check surfaced a *disposition* the carry had lost (a non-zero count is the documented handled path, not a regression). **A re-derivation that confirms is not a wasted one; it re-sources the claim and often widens it.**
2. **A subsystem with no callers is invisible to every sweep that looks for wrong behaviour, because it has none.** Corollary: grep the *assignment* side, not the read side.
3. **A plan doc's past tense is a claim, not evidence.**
4. **Advisory gates that nobody reads are indistinguishable from gates that do not exist.**
5. **A test suite that agrees with the bug stays green forever** — and a pool-scoped guard is a suite that agrees with the bug one shelf over.
6. **Read composition, not depth** — and stop re-testing the favourable dip.
7. **A gate clearing is not the same as a decision arriving.** Surfacing a settled call for ratification is worse than not surfacing it. Corollary: verify a *favourable* outcome's cause as carefully as an unfavourable one.
8. **An empty Discord inbox is not evidence Christian is away.** Silence means only that he did not write.
9. **A startup-failure cluster is indistinguishable from quota exhaustion at the moment it happens.** The disambiguating evidence — *does a fresh run execute jobs?* — costs one run. Take it before escalating to a billing page.
10. **Deploy-observability must classify by paths, not by branch name or PR title.** — SHIPPED 2026-07-27 in THR-785.
11. **Mutex lines with reasons work.**
12. **An unanswered ask is not an unheard ask — do not re-ring the doorbell.** A standing ask carries a stated safe default, so silence resolves it rather than blocking on it.
13. **Declining to build the missing half is a shippable outcome, and it needs its reasoning recorded to survive.**
14. **A signal with two meanings is not a signal — and the repair is a notification path, never a new gate.**
15. **When a ticket asks "which of these fixes?", census the pipeline before answering** — the honest answer may be "none, and here is why". Validate the *sample*, not just the stage.
16. **A deliberately-parked issue and a broken auto-close are indistinguishable from the board.** Report a falsifiable test in both directions.
17. **Before carrying an ask into another run, check whether it was answered somewhere other than the channel it was asked on.**
18. **State a falsifiable test against the outcome, never against an activity proxy.** State the test's enabling premise explicitly, or a benign world reads as its own failure case. **Now subsumes finding 37's threshold rule — a test also needs enough *power*, not just the right target.**
19. **A column-scoped queue query reports the lane's reach, not the board's contents — and from inside the query the difference is invisible.**
20. **When an ask names a *mechanism* rather than a decision, expect the answer to arrive on the channel he has, not the one the mechanism needs.** (a) Prefer asks that are pure decisions. (b) State plainly what was *not* done.
21. **Re-derive a carry even when you expect it to hold** — the check has a wider aperture than the claim. Look at what the source shows you, not only the field you came to check.
22. **An item in the lane's column is not an item the lane can take.** The cheap detector: read the comments, not just the fields. *Consequential* and *decidable-by-Christian* are independent axes.
23. **A ticket this task files is unpullable by construction, and the cost lands on someone else's run — so filing is never free.**
24. **A file's *state* is evidence; its *author* is a separate claim needing separate evidence.**
25. **A confirmed test licenses the claim it tested, not the remedy that motivated it — and the gap is widest exactly when the result is most satisfying.** Corollary: **the mirror failure is a test whose aperture is wider than the claim.**
26. **One instance locates a defect; the second instance with the same fingerprint is what turns it into a mechanism — and changes the remedy.**
27. **A timestamp is evidence only once its zone is verified, and a `Z` suffix is a claim like any other.** Prefer a machine-written field (`stateHistory`, `lastRunAt`, commit metadata) over a prose header. **The *wrong field* misdates an event as badly as the wrong zone** — THR-667's `updatedAt` sits 28 minutes after its actual `Ready for Dev`→`In Dev` transition.
28. **A classifier observed only in its stable state has not been tested.** **Satisfied in BOTH directions:** three consecutive `deployedSha` advances, then two correct **flat** hours when only docs merged. **Note what switched: the evidence, not the verdict word** — five runs reported `skipped` throughout.
29. **"Green and armed" is not "merging" — and the difference is invisible from every signal a lane normally checks.** Checks answer *is this change correct*; `mergeStateStatus` answers *can it go in now*. **The tell is throughput, not status.**

  **Two corollaries.** (a) ~~**It does not self-heal.**~~ **RETRACTED 2026-07-29 13:23Z, and the retraction GENERALISED 2026-07-30 04:58Z on a second independent data point** — #1042 merged ~2h07m after entering `BEHIND`; **#1085 merged at 04:18:37Z, 1h49m after arming, with nobody intervening.** **The operational rule inverts: do not spend a session force-updating a branch that is merely `BEHIND` — it lands within ~2h unattended.** **The retraction is itself the lesson: "still true at T" is not "true at every T"** — and it was load-bearing twice, because it also set the floor the discriminating test had to clear (finding 37). (b) **`gh pr view` returns `UNKNOWN` for `mergeable`/`mergeStateStatus` on the first read** and often the true value on the second — but **not always, and the unreliability runs in both directions.** **04:58Z: BOTH reads returned `UNKNOWN` for both #1085 and #1031**, where an hour earlier both reads on #1085 returned real values. Treat `UNKNOWN` as "not yet computed", never as evidence of mergeability — **and never assume a second read will resolve it.**

  **29(a) IS NOW SCOPED, AND BOTH BOUNDS WERE MEASURED IN ONE RUN (2026-07-30 09:56Z).** (i) **It applies to `BEHIND` only.** A PR blocked by a *failed* required check never self-heals, and surfaces under the identical `BLOCKED` label — **earned finding 43.** (ii) **Even within `BEHIND`, "lands within ~2h" is a tendency, not a guarantee: #1096 crossed the ~2h07m band and went `DIRTY` at ~2h49m.** The checkpoint last run stated in advance resolved **unfavourably**, which is what a properly-powered test is for (finding 37). **The rule survives in weakened form: do not spend a session on a merely-`BEHIND` PR *inside* the band — but check it once past the band, because the failure mode past it is `DIRTY`, which needs hands.**

  **The `gh pr list --json` "workaround" recorded 2026-07-30 06:57Z is RETRACTED 07:54Z.** It rested on one favourable sample. This run the list form returned `UNKNOWN` for #1096 and #1031, and **another lane had read #1096 as `DIRTY` 22 minutes earlier** — so the list form is not a better instrument, just another sample. **The field is unstable across time, not across commands**, and #1098 moving `BEHIND` → `BLOCKED` inside 26 minutes says the same thing. **Read it twice with a gap, or read throughput instead** (29's main clause).

30. **A liveness witness must fire strictly *inside* the window it witnesses, not merely fall within it.** **The general form: a guard that accepts an endpoint as evidence about an interval passes hardest exactly when the interval is most degenerate.** Corollary: a probe's verdict is evidence, not a verdict — when independent witnesses contradict it, override and say why.

31. **A cleanup and its cause are separate deliverables, and shipping the cleanup is the moment the cause is most likely to be dropped.** State the re-accrual rate and a dated falsifiable test against the next producing event.

  **The honest quantity is not a rate: it is `|rows the sweep handled|`, workload-dependent and unbounded above.** Meta-lesson: **when a number has been wrong once, the reflex is to move it the other way and re-assert it with equal confidence.** Prefer naming the *variable*.

  **Closed out 2026-07-30 01:58Z in a direction the finding did not anticipate.** THR-845 shipped cleanup, cause-side hardening, **and** a third thing: **it removed the consumer that made the cause consequential.** Generalisation: when a defect's cleanup and cause are both expensive, check whether the *reader* can be made indifferent. **Confirmed twice over by 03:57Z — the leak has produced two more rows (THR-863, THR-864) and produced no symptom, and the reader's own source now documents the leak as a reportable number rather than a fault.**

32. **Verify-after-write needs a clock, not just a re-read — an immediate confirmation read can be served stale and is indistinguishable from success.** **Corollary: a count is not a verification.**

  **A `get_issue` check was correct when it ran and false 131 seconds later. An immediate verify cannot distinguish "my write held" from "my write held briefly"** — so the fix is **verify after the last side-effect in your own sequence**, not after the mutation you care about. **Sharpened by a controlled pair:** 9 immediate verifies after unassigns all held an hour later; 2 after creations were both false within the hour. **The verification was never the variable — what wrote next was.**

  **Best artefact: THR-860's description asserts its own verification and the field contradicts it in the same row.**

33. **A claim widens when it is restated, and the widening is invisible because every restatement still cites the original evidence.** **The drift is toward the remedy's framing**, because a report is written to justify acting. **Detector:** state what observation would falsify *today's* wording, then check whether it would also have falsified the wording used when the evidence was gathered. **Corollary: severity claims need their own evidence, separately from mechanism claims.**

34. **N confirming sightings of an effect do not locate its cause — and each sighting makes the unexamined attribution feel better supported.** **The trap is that repetition reads as accumulating evidence.** **Detector, cheap: before reporting a recurrence, name one property the affected rows share that the unaffected rows do not — and check it.** If you cannot state the contrast class, you have an effect, not a cause.

  **Corollary earned 2026-07-30 03:57Z while *maintaining* a contrast class rather than finding one: a row that was repaired after the fact is not evidence about the write path, and counting it as a clean case silently weakens the very contrast you are testing.** THR-861 reads unassigned only because the orchestrator hand-cleared it. **Keep the repair history attached to the data point, or the contrast decays as the cleanup succeeds.**

35. **A query's filter encodes a premise about the world, and nobody audits a premise that has never produced a visible failure.** (Earned 2026-07-29: `pull-work` selected work with `assignee:null` for months, encoding *"an assignee means a person has claimed this row."* Christian, unprompted: *"I do not use linear or github at all. so any issues in linear or PRs in github with my name on them are yours."*) **Detector, cheap: for each predicate in a query you are debugging, say out loud what it assumes about who or what writes that field — then ask whether anyone has ever confirmed it with the writer.** **Corollary:** 34 says N sightings do not locate a cause; **35 says the cause may not be in the mechanism at all, but in the question.** **The reader is usually yours to change even when the writer is not.**

  **SHIPPED 2026-07-30 01:21:50Z as THR-845, OBSERVED WORKING 02:02:22Z, RE-SOURCED 03:57Z, and CLOSED ON DIRECT BEHAVIOURAL EVIDENCE 06:57:17Z** — `pull-work` claimed **THR-681 while it carried `assignee: "Christian Spliid"`**, a row the pre-fix reader could not see. **Every earlier confirmation read the source; this one watched the behaviour.** **What makes this the finding's proof rather than its epilogue: the eight prior runs each produced a correct local measurement and a wrong global conclusion, and no amount of additional write-side measurement would have converged.** The unlock was a question, asked of the one party nobody had asked. **Second-order lesson for this lane: the answer arrived because step 0 existed to hear it.**

36. **A consumer that matches on a heading's *level* rather than its *text* is a channel that silently narrows as producers reformat.** **Detector: for every "no items found" result on a shared channel, confirm the *absence* against the raw file once, not just against your pattern.** Sibling of finding 19: **a scan reports its own pattern's reach, and a narrowed pattern and an empty world are the same observation.**

  **Generalised: "formatting" was too narrow — the near-miss was *file selection*.** So the rule is **every point where a consumer infers structure from a producer's convention is a coupling the producer can change unilaterally, and each one fails silently in the same direction.** Detector: for each such inference, name the producer change that would break it, then check whether anything would tell you.

  **Sixth consecutive correct-by-luck hour, 2026-07-30 03:57Z: the same-day set is now `-30` … `-30f`.** **A coupling that has survived six hours looks progressively more like a design and less like a hazard, and that drift is finding 33's tell turned on itself.** The luck is alphabetical and finite. **Record the streak as accumulating exposure, not accumulating safety.**

  **THE PROPOSED REPAIR WAS FALSIFIED 2026-07-30 09:56Z, and by a benign input.** The fix specified at 08:58Z was "select by explicit mtime (not glob order)". This run, **`-30g` and `-30h` carry the same mtime to the second (`08:20:23`)** — so an mtime sort has nothing to discriminate on and silently falls back to shell order, the coupling it was meant to remove. **No consequence: both sections read "Nothing needs you", so either pick was right.** That is the only way an instrument's failure surfaces on a benign input, and it is why it was worth checking a fix that looked obviously correct. **Generalisation, and it is finding 37 pointed at a *repair* rather than a *test*: a fix needs discriminating power too, and "take the newest" has none when the producer emits several per minute.** The honest fix removes the decision instead of improving it — **read all in-window reports and union their sections; never pick a winner.**

37. **A falsifiable test needs enough power to discriminate, and a threshold set below a known counter-example has none — it will "resolve" and license nothing.** **Earned 2026-07-30 03:57Z, cleanly.** Last run set the #1085 checkpoint at **1h30m** armed. Finding 29(a) had been retracted precisely because #1042 self-healed at **~2h07m**. The antecedent was satisfied on schedule and the conclusion was unavailable: at 90 minutes, a stuck PR and a slow-but-self-healing PR are the same observation.

  **This is distinct from finding 18.** 18 governs *what* you measure (outcome, not activity proxy). **37 governs *where you put the line*: a threshold must clear the longest benign case you already know about, or the test cannot separate the hypotheses no matter how faithfully it is run.**

  **Detector, one question, cheap: "what is the longest this has taken while still being fine?" — and set the checkpoint past it.** If no benign case is known, say so and state the test as exploratory rather than decisive. **The failure mode is seductive because an under-powered test still *resolves on time*, and a resolved test reads as a settled question.**

  **PAID OUT ONE RUN AFTER BEING EARNED, 2026-07-30 04:58Z.** The re-powered checkpoint (>2h07m) **discriminated**: #1085 merged at 1h49m, settling finding 29(a) in the favourable direction on two data points. **Same PR, same mechanism, same cadence — only the threshold differed, and the 1h30m version would have licensed nothing.** **This is the cheapest confirmed finding in the ledger: one question asked before writing the test, one run later a settled question instead of a repeated one.**

38. **A count measured against a periodic repair process is a *phase* measurement, not a health measurement — and the two are indistinguishable from the number alone.** **Earned 2026-07-30 04:58Z:** the home tree read **10 behind**, exactly at `FRESHNESS_BEHIND_THRESHOLD`, one commit from a Christian-facing alarm — while autosync was **provably healthy** (`git reflog show main`: seven consecutive hourly fast-forwards at `:50:03` to the second). All 10 commits had landed *after* the most recent tick, so the count was this hour's merge rate wearing the costume of decay.

  **Detector: before reporting a behind/lag/backlog count as decay, read the repairing mechanism's own cadence log and ask "when did it last succeed?"** A count plus a healthy cadence is phase; a count plus a missed cadence is drift. **The threshold alone cannot tell them apart, and a busy hour is exactly when the count looks worst.** Corollary: **this is why the THR-671 escalation ran for days** — a behind-count was read as decay without ever checking whether the mechanism had run.

  **Sibling of finding 30 in structure:** both are cases where a *point* observation is offered as evidence about an *interval*, and both pass hardest when the interval is least normal.

39. **A ticket-authoring rule binds the author, and nothing re-binds it at promotion — so a long-parked ticket's numbers rot in place and arrive at the top of the queue looking authoritative.** **Earned 2026-07-30 04:58Z:** THR-680 was promoted as the intended next pickup carrying "the 38-deep home-tree stash stack"; the stack holds **2**, and drained to 0 roughly **8.7 days** earlier. THR-688 rule A (predicates, not counts) exists precisely to prevent this and applies only at authoring time.

  **Detector for any promoting lane, cheap: if a ticket's Done-when quantifies anything, re-measure that quantity during the promotion run and put the fresh number in the promotion comment.** A count that survives the re-measure costs one command; a count that does not has just saved an executor a wasted session. **The failure is invisible from the board — a rotted ticket and a fresh one are the same row.**

  **Note the near-miss in the *evidence*, not just the ticket** (memory `vacuous_probe`): had this run reported "2 stashes" without the reaper's history, the honest reading would have been ambiguous between "the ticket is stale" and "I am measuring the wrong stash stack". **The decay curve `38 → 0 → 1 → 2` is what made it definitive**, and it cost one `grep`.

  **PAID OUT ONE RUN AFTER BEING EARNED, 2026-07-30 06:57Z — and the executor reached it independently.** THR-680 shipped as `1392fa8b`, *"stash residue disposal + already-shipped-under-parent finding"*: the same stale-premise diagnosis, plus memory `split_child_ships_under_parent_id`. Stack now **0**, reaper corroborating. **Scope honestly (finding 25): the *detection* is confirmed cheap and correct. The *detector* — re-measuring a Done-when's quantities at promotion — is still unimplemented in the promoting lane.**

40. **A periodic job that succeeds with nothing to do often leaves no trace, so "no log entry" and "did not run" are the same observation — and only one of them is an alarm.** **Earned 2026-07-30 06:57Z by withdrawing the previous run's alarm.** Autosync's hourly `merge origin/main` had written `:50:03` reflog entries for seven straight hours; two hours showed none, which was reported as "8 minutes late" and escalated to a watch item with a Christian-facing test. **But a fast-forward with nothing to fetch does not move the ref, and a ref that does not move writes no reflog entry.** An idle hour and a failed hour are byte-identical in that log.

  **Detector, cheap: before treating a gap in a periodic job's output as evidence it stopped, ask "would a successful no-op have written anything?"** If the answer is no, the gap is not evidence in either direction — say so, and find a witness that logs unconditionally (a run log, a heartbeat file, an exit code) or report the *outcome* instead of the trace.

  **Sibling of findings 30 and 38, and the family is now clear enough to name: all three are cases of reading a periodic repairer's incidental artefacts as if they were its status.** 30 — a point observation offered as evidence about an interval. 38 — a count that measures phase, not health. 40 — an absence that measures workload, not liveness. **In each, the honest move was to read the *repaired thing* (is the tree current?) rather than the repairer's exhaust.**

  **Note what made this catchable: the alarm came with a falsifiable test, and running it forced a look at the mechanism.** An alarm carried without a test would have hardened into a fact over the next few runs — finding 33's drift, on this lane's own output.

41. **A non-recurrence discriminates nothing.** **Earned 2026-07-30 07:54Z on this lane's own defect.** Last run found a skipped publish with two candidate causes — a correct "no substantive change" skip, or a mid-run failure. This run observed two clean hourly publishes. **Both hypotheses predict exactly that**: a benign skip is supposed to be rare, and so is a transient failure. The observation is consistent with everything and therefore evidence for nothing.

  **The pull is strong because a quiet hour *feels* like an all-clear**, and downgrading the alarm feels like the disciplined move after finding 40 taught the cost of over-alarming. **But 40 says do not read a repairer's exhaust as its status; it does not say silence is health.** The two failure modes are symmetric and both are live.

  **Detector, cheap: before letting a non-recurrence retire an item, ask "what would the *benign* explanation have looked like this hour, and does it differ from what I saw?"** If the two are identical, keep the item and keep its detector request; downgrade the *alarm* while preserving the *question*. **Sibling of 18 and 37: a test that cannot separate the hypotheses has no power, and an unrepeated event is the lowest-powered test there is.**

  **VINDICATED AND SUPERSEDED IN ONE RUN, 2026-07-30 08:58Z.** 41 was right to keep the question open — and the question was then settled **not by waiting for more instances but by changing the witness** (finding 42). **The generalisation: when a test has no power, the move is to find a better instrument, not to run the same test again.** Two more hours of publish/no-publish observations would have discriminated nothing; one `gh pr list` call settled both instances at once.

42. **A detector inherits the blind spots of the artifact it reads — and one built at the wrong stage of a pipeline will manufacture the very pattern it was built to catch.** **Earned 2026-07-30 08:58Z, on a detector this lane had specified for itself two runs running.** The lane publishes `branch → PR → merge`. The proposed check read the **merged commit** on `main`. A run that published perfectly but whose PR had not yet merged is, to that check, indistinguishable from a run that produced nothing.

  **The cost was nearly a fabricated mechanism.** #1101 was open and healthy; the check would have reported a second missed publish; **finding 26 would then have promoted a one-off into a "mechanism" and moved the remedy upstream** — all on an artifact that was sitting in plain sight one command away. **This run's own first hypothesis was exactly that**, and only survived because the PR list was checked before the claim was written.

  **Detector, cheap, and it is one question: "what are all the states this thing passes through, and which one am I sampling?"** Sample the *earliest* stage that proves the work happened (the PR exists), not the *latest* stage that proves it landed (the commit merged) — the tail of a pipeline measures everything downstream of you as if it were you.

  **Completes the family named at finding 40.** 30 sampled a point and claimed an interval; 38 sampled phase and claimed health; 40 sampled exhaust and claimed liveness; **42 samples the wrong stage and claims the wrong actor.** In all four, the honest move is to name what the artifact is actually a record of, before reading a verdict off it.

43. **An aggregate status field that collapses "not yet" and "never" into one value inverts the operational rule between two states it cannot distinguish — and the safe-looking reading is the wrong one for exactly one of them.** **Earned 2026-07-30 09:56Z on a clean simultaneous pair.** `mergeStateStatus: BLOCKED` was returned for **#1107** (required check `IN_PROGRESS` — clears itself in minutes) and **#1103** (same check `FAILURE` — clears only on a push) **in the same query, at the same moment.**

  **The damage is specifically that finding 29(a) exists.** 29(a) established, on two data points, that an armed PR left alone merges within ~2h — and the resulting operational rule ("do not spend a session force-updating it") is *correct for `BEHIND` and wrong for a failed check*. **A red PR left alone under that rule reads as disciplined patience while its ticket sits parked indefinitely.** THR-868's UI half was 7 minutes into exactly that state when this run read it.

  **Detector, cheap, and it is one extra call: never read an aggregate merge status without reading the check *conclusions* beneath it.** `gh run view <id> --json jobs` gives per-step `conclusion`, which separates *waiting* from *failed* unambiguously — and, as a bonus this run, separated *failed* from *timed out* (17m19s wall time invited the timeout reading; steps 1–9 `success` and step 10 `failure` refuted it — memory `ci_red_may_be_timeout`).

  **Distinct from finding 29(b).** 29(b) says the field is *unreliable* — `UNKNOWN`, unstable across time. **43 says the field is *lossy even when it answers confidently*.** An unreliable field teaches you to re-read it; a lossy one teaches you to read something else. **Sibling of the 30/38/40/42 family in the same way:** a value that is a record of *whether the merge button is pressable* is being read as a record of *whether the work is progressing*.

**Owner of items below:** Christian. Everyone else's blockers go in Linear or `Docs/impediments.md`.
**Refresh cadence:** The hourly `keep-work-flowing-cc` scheduled task keeps this current (prunes resolved items, adds newly-surfaced Christian-owned ones); the `retrospective` skill still does the deep periodic rebuild.

## How this works

The retrospective stopped being the right channel for "Christian, please flip one switch." Christian-owned asks were buried in retro prose, and nothing surfaced them between retros. This file replaces that pattern.

Read order: top to bottom is blast radius. The top items break canonical workflow invariants right now; the bottom items are operational debt with workarounds in place.

For each item:
- **Fix** = the literal command, click path, or line of config that resolves it.
- **What breaks** = the named system or invariant currently degraded.
- **Source** = `Docs/impediments.md` entry numbers + occurrence count, so the cost is visible.

When an item resolves: delete it from this file, mark the corresponding impediment as resolved in the dashboard regen, note the close in the next retro under "What shipped." **No history retained here — `git log` + the retros are the audit trail.** The resolved log is pruned to the current day; anything older is recovered with `git log -p Design/user-actions.md`.

---

## Standing asks

**NONE OPEN — eighth consecutive published run at a true empty.**

**A new unexplained multi-hour pause would be a NEW ask with new evidence — never the overnight-wake question reopened** (finding 12). If he ever does want overnight throughput guaranteed: keep the machine awake and **on mains power**, because Windows Task Scheduler refuses the reaper on battery (`-2147020576`, memory `git_cleanup_automation`). Recorded as a fact, not a request.

**Seven questions are answered, closed, and must not be re-asked:**

1. **The five empty projects** — Christian, chat 2026-07-25 21:53: *leave them open; they are intake buckets that refill as the game iterates.*
2. **The GitHub Actions payment block** — cleared 2026-07-25 ~17:09Z and again 2026-07-28 17:02Z, both verified by re-run, and corroborated nineteen times since by independent mechanisms. **A recurrence is a new ask with new evidence, never this one reopened** (finding 12).
3. **THR-799 priority ordering** — Christian, quoted in THR-774/775, 2026-07-27: *the new encounter experience is still first priority.*
4. **Release of THR-821/820/777/778** — Christian, Discord 2026-07-27 14:38Z.
5. **The Linear "assign issue to PR author" switch** — Christian, Discord **2026-07-29 18:10:03Z**: *"can't find anything like that. I do not use linear or github at all. so any issues in linear or PRs in github with my name on them are yours."* **Closed by falsification, on the terms the ask itself set.** The second sentence is a **standing fact**: an assignee of "Christian Spliid" carries no information and must never be read as a human claim. **Enforced in code since 2026-07-30 01:21Z, observed working at 02:02Z, and re-sourced to `pull-work` SKILL.md lines 27/176/184 at 03:57Z.** **The highest-leverage answer in this file's history: one sentence retired eight runs of misdirected investigation and unblocked the executor lane.**
6. **Should the host machine stay awake overnight?** — **retired 2026-07-30 01:58Z by observation**, now four consecutive awake nights, never answered directly and no answer needed.
7. **The Sphere-Governed Ascendant pivot** — Christian, 2026-07-30: *"the whole area of power curve for ascendant growth is still rudimentary as we build the world and the mortals."* Project parked in `Idea`; THR-870 carries the design scope and activates only when he moves it out. **The board state already matches the verdict, so there is nothing to ratify** (finding 7). Re-surfaced by the grooming lane 2026-07-30 07:28Z with the same recommendation (leave parked) — **folded into the brief as context, not as an ask.**

**One standing switch exists but is not an ask:** the orchestrator lane can be disabled on one word from Christian (live 2026-07-27 19:49Z). **Its default state is the intended one**, so silence resolves it.

**Three durable corrections survive closed asks and are kept as rules:**

- (a) A blanket `git stash` was the wrong remedy for home-tree dirt; the single-path form is correct. Parking his tool-permission edits was never necessary.
- (b) `$REATTACH_MAX_TRACKED_DIRTY = 0` governs the detached-HEAD reattach path and never evaluates while HEAD is on `main`. The conclusion held; its stated reason did not.
- (c) **Tracked dirt alone does not stall autosync.** The stalling class is the *intersection* of locally-modified **∩** changed-by-incoming-commits — proven by fast-forwarding cleanly with `.claude/settings*.json` dirty.

---

## Resolved 2026-07-30

**Thirteenth consecutive published run at zero open Christian-owned asks, eighth at a true empty.** Everything that moved was agent-side.

- **`mergeStateStatus: BLOCKED` CONFLATES A CHECK STILL RUNNING WITH A CHECK THAT HAS FAILED — the run's strongest result, and it lands on finding 29(a)'s scope.** #1107 (`IN_PROGRESS`) and #1103 (`FAILURE`) returned the **identical status value in the same query**, with opposite prognoses: one clears itself in minutes, the other never clears without a push.
  - **THR-868's UI half is the live casualty.** PR #1103 failed on step 10, `Generated artifact freshness`; steps 1–9 including tests and the typecheck ratchet all passed. **Not a timeout** — 17m19s wall time invited that reading and the step conclusions refuted it (memory `ci_red_may_be_timeout`). Cause is the structural one: `check:generated-freshness` compares against `HEAD`, so a closeout edit to a generated-artifact source lands after the gate (THR-755 row 6 / impediment #201). Fix is `npm run prebuild` + push.
  - **Earned finding 43.** The danger is precisely that finding 29(a) exists and *sounds* applicable: "leave an armed PR alone, it lands within ~2h" is right for `BEHIND` and wrong for red, and **left alone under that rule a red PR parks its ticket while reading as patience.** Detector: never read an aggregate merge status without reading the check conclusions beneath it.
  - **Scoped honestly: 7 minutes old at brief time**, so its owner may be mid-fix. The durable item is the conflation, not an abandonment claim.
- **LAST RUN'S mtime FIX FOR FINDING 36 WAS FALSIFIED — by a benign input, which is the only way it could have surfaced.** `orchestrator-2026-07-30g.md` and `-30h.md` carry the **same mtime to the second** (`08:20:23`), so "select by explicit mtime" has nothing to discriminate on and degrades to shell order.
  - **No consequence this hour** — both read "Nothing needs you", so either pick was correct. **The instrument failed silently while producing the right answer.**
  - **This is finding 37 turned on a *fix* instead of a *test*: a repair needs discriminating power too.** Corrected shape: **stop picking a winner** — read every in-window report per task and union their sections. A lane emitting eight reports a day has no single current one.
  - **Count corrected (finding 39):** the same-day set is **8** files on disk, not last run's "nine"; run i is unmerged in #1102.
- **LAST RUN'S STATED TEST ON #1096 PAID OUT — UNFAVOURABLY, WHICH IS WHAT A POWERED TEST IS FOR.** The checkpoint was set in advance: *"if it is still open next run it crosses that floor and becomes one."* It is open at **~2h49m**, past the ~2h07m benign band, **and now `DIRTY` rather than merely slow** — so it needs `git merge origin/main && git push`, not more patience. **This weakens finding 29(a)'s second bound in the same run that finding 43 weakened its first.**
- **THE PUBLISH PATH IS CONFIRMED HEALTHY ON THE CORRECTED WITNESS'S FIRST APPLICATION** — #1104 merged 09:07:44Z, #1101 merged 09:02:41Z. Both briefs landed, including the one suspected lost. **The question stays retired because the instrument changed, not because two hours were quiet** (finding 41).
- **THE SELF-COLLISION RISK DID NOT FIRE, AND THE REASON IS THE RULE.** This run branched fresh off `origin/main` with #1104 already merged 49 minutes earlier — no race was possible, exactly as predicted. **Recorded as accumulating exposure, not a fix** (finding 36).
- **CHRISTIAN FILED WORK IN CHAT THIS HOUR AND THE DISCORD CHANNEL STILL READ EMPTY — direct evidence for finding 8.** THR-878 (`?forceencounters` debug flag) was created 09:52:28Z and taken In Dev 09:53:03Z, never passing through Ready for Dev. **He asked for it because he could not judge the WS5 encounter writing by playing** — most encounters resolve silently at background attention tier. The flag survives production, widens already-threaded agents to full prose + choices + immediate interrupt, and leaves dormant agents invisible. **The permanent four-tier→binary simplification he also asked for was filed separately rather than bundled into a same-session testing flag** — the right call.
- **WIP is 2, breaching WIP=1** — THR-868 and THR-878, both with open PRs pushed this hour. The second arrived by chat, not from the shelf.
- **Queue FLAT at 56 ready with a byte-identical priority profile** (9 Urgent / 3 High / 7 Medium / 37 Low). Nothing closed, nothing filed to the shelf. **A flat hour with two live PRs is a working hour, not decay** (finding 38). Oldest is THR-739 at ~5.6 days, inside the 7-day window.
- **Deploy correctly FLAT at `421c3e82` — verdict `skipped`**, docs-only merges since. **The merge gate held for a nineteenth consecutive check** — load-bearing this run, since it is what licenses reading #1103's red as a real content failure rather than the billing signature (which records the required check as `skipped` and *satisfies* protection). **Fleet heartbeat `ok` for an eighteenth** — lanes at 09:01Z / 09:26Z / 09:53Z, distinct, so finding 30's wake-boundary defect is untriggered rather than quiet. Reaper ran 09:40Z: 41 worktrees, 57 branches, **0 stashes** (held a fourth hour), 0 needs-disposition.
- **Discord returned a literal `[]` for an eleventh consecutive read** — 2 bytes, nothing to classify, no receipt owed, cursor unmoved at `1532100414189867109`. **The ping gate correctly stayed silent**, and the stored empty-string hash was verified by computing it rather than assumed.
- **The non-empty-but-ask-free sibling-section state recurred a third time** (`backlog-grooming-2026-07-30.md`, refreshed 09:50:03Z, same Sphere-Governed-Ascendant recommendation): folded as **context, not an ask** (finding 7). The step-2.6 rule still does not name that state.
