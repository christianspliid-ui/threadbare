# User Action Required

**Last updated:** 2026-07-30 04:58 local, by the hourly `keep-work-flowing-cc` CC task.

**ZERO OPEN CHRISTIAN-OWNED ASKS — seventh consecutive run at zero, second consecutive run at a true empty (no standing asks either).** Discord inbox **genuinely empty** for a fifth consecutive hour — the `after=` cursor returned a literal `[]` (3 bytes). Home tree healthy: on `main`, 0 stranded, **0 behind**, 2 tracked-dirty (settled). Deploy `skipped`-benign, publish point **flat** at `bcfbe645` (correct — docs-only landed). Merge gate `healthy`, `startupFailureCount: 0`. Fleet heartbeat `ok` — all 8 enabled lanes within tolerance. **The hour's event is that THR-845 PAID OUT IN OBSERVATION, not merely in merge: the 02:01Z pickup claimed THR-667 at 02:02:22Z — 76 seconds after waking — and had a green PR up by 02:48Z. Last run stated the falsifiable test in advance and it resolved in the favourable direction.** Two carried claims were re-derived this run and **one of them was wrong** (the `python` diagnosis, corrected below).

## This run's measurements (2026-07-30 02:58 UTC)

Full hour since the previous brief; the lane held its `:45` slot and the deltas below span a true hour.

- **THE STATED-IN-ADVANCE TEST RESOLVED, AND IT RESOLVED THE OTHER WAY.** Last run wrote: *"if the 02:00Z pickup claims nothing with 50 candidates and no WIP, the binding constraint is not visibility and THR-836 is confirmed as the real gate."* **It claimed.** `tb-opus-pickup` `lastRunAt` **02:01:06.037Z**; THR-667's `stateHistory` shows `Ready for Dev` → `In Dev` at **02:02:22.863Z** — **76 seconds**. PR #1085 opened, `Test · Typecheck · Build` **SUCCESS** at 02:48:13Z, auto-merge armed 02:29:27Z. **So visibility was the binding constraint, and THR-836 is NOT confirmed as the gate.**
  - **Read from `stateHistory`, not from `updatedAt`** (finding 27). THR-667's `updatedAt` is 02:30:36Z — 28 minutes after the actual claim — because the PR attachments landed later. A run reading `updatedAt` would have mis-timed the claim by half an hour and attributed it to the orchestrator's 02:26Z slot.
  - **Scoped to what it licenses** (finding 25). One pickup establishes *the picker can reach the shelf*; it does not establish a drain rate. THR-667 sat in `Ready for Dev` from 2026-07-28T13:28:44Z to 02:02:22Z — **~36.6h** across ~36 pickup wakes — then went on the first wake after the reader fix. **Consistent with it having been assignee-hidden, and it was orchestrator-promoted (attachment: PR #1015 "promote THR-667"), which per THR-845 stamped an assignee — but the assignee field carries no history, so this is an inference and is recorded as one.**
- **Queue: BACKED UP — 51 ready (0 Urgent / 0 High / 16 Medium / 35 Low), 1 In Dev.** Nothing stale: oldest `updatedAt` is THR-739 at 2026-07-24T20:29:29Z, **~5.27 days**, inside `STALE_ISSUE_DAYS`=7.
  - **Shelf 50 → 51, and it reconciles exactly** — worth stating because a shelf that grows during the first successful drain looks like the fix failing. −1 THR-667 (claimed 02:02Z), **+1 THR-862** (02:22:43Z, filed by the THR-667 executor as its deferral), **+1 THR-863** (02:31:01Z, orchestrator's nightly batch). **Net +1: execution started, and filing still outran it 2:1 this hour.**
  - **THR-862 weakens the THR-836 candidate story, and this is the check nobody had run.** Last run reasoned *"21 of 50 carry `Deferral`, and filed Deferrals have no coordination block by construction"* (memory `deferral_no_coord_block`). **THR-862 is a filed Deferral and it carries a full coordination block** — `Blocked by: nothing`, `Parallel-safe with`, `Mutex with`. So the "by construction" premise does not hold for executor-filed deferrals. **The count of shelf items that actually fail `pull-work` Step 3 is still nobody's measurement**, and it is now known to be smaller than the Deferral count. Stated as a correction to a candidate, not a finding.
- **The write-side leak is still live, AND last run's explanation for it is DISCONFIRMED.** THR-863 (`createdAt` 02:31:01.193Z) carries `assignee: "Christian Spliid"` on `get_issue`, not read off a list. Last run hypothesised the live prompt was unsynced from the repo mirror the THR-845 merge updated: **checked directly this run, and it is false.** `diff` of `~/.claude/scheduled-tasks/tb-orchestrator/SKILL.md` against `Docs/ops/scheduled-task-prompts/tb-orchestrator.md` returns **one line — a trailing newline**. The live prompt carries THR-845's step 5a verbatim (create → *separate* `save_issue(assignee:null)` → verify on `get_issue`).
  - **A contrast class exists and it points somewhere new** (finding 34's detector, applied). **THR-862, created 02:22Z by the executor lane, has NO assignee.** THR-863, created 02:31Z by the orchestrator lane, does. **Same Linear API actor, same team, 9 minutes apart, opposite outcomes.** So Linear's create path does not universally strand an assignee — the discriminator is *which lane wrote it*, which is the first evidence that narrows this to the orchestrator's own write sequence rather than to Linear's defaults. The orchestrator's THR-863 description offers a different account (the twice-daily `stale-claim-sweep` window), and it did clear THR-860/861 by hand this run — **but a sweep window does not explain why the executor's ticket came out clean 9 minutes earlier.**
  - **Consequence remains nil.** The reader no longer filters on the field (THR-845), so this is a cosmetic leak with a live cause, observable only by counting. **Recorded because the cause is now narrower, not because the symptom matters.**
- **Deploy (step 2.5): `skipped`, benign, `deployedSha` FLAT at `bcfbe6454c99ea6758426551b1c98ae0d50cb26d`** — unchanged from last run. Verbatim: *"The live site is up to date. Commits since the last publish (bcfbe645) only touched notes and docs, so the game itself did not need rebuilding."* **Correct: the only merge this hour was PR #1086, an orchestrator report.** Finding 28's watch-item is satisfied in the other direction now — three runs of advancing SHA, one run correctly flat.
- **Merge gate (step 2.5b): `healthy`, `startupFailureCount: 0`, `standDown: false`.** **Thirteenth consecutive independent confirmation** that Christian's billing action holds. Independently corroborated this hour by a real workflow completing: `Test · Typecheck · Build` ran 02:30:11Z → 02:48:13Z (**18m02s**) and concluded `SUCCESS` — **a genuine execution, not a 3-second startup failure.**
  - **That 18m02s is worth a note against THR-835** (the unreachable-in-`Idea` ticket about the required check running at 80–90% of its 20-minute cap). **This run: 18m02s against a 20m cap — 90.2%.** THR-835's premise is re-confirmed by live data, and it is still in `Idea` where no lane will reach it.
- **Step 2.7 (heartbeat) returned `ok`.** Verbatim: `{"verdict":"ok","needsChristian":false,"checked":8,"stalled":[],"neverRun":["monthly-rulebook-review"],"summary":"All 8 enabled scheduled tasks are within 2 slots of schedule."}`. **Twelfth consecutive `ok`**, third genuinely-awake overnight — lanes fired 02:01Z / 02:26Z / 02:53Z at **distinct** timestamps, so the wake-boundary defect is untriggered rather than merely quiet.
- **Reaper healthy — ran 04:40:02 local (02:40Z), 18 minutes before this brief.** `SUMMARY:` **37** worktrees / **48** branches / **2** stashes / **0** needs-disposition. **Counts moved UP (36→37, 46→48)** as sessions created worktrees and branches — a moving count in either direction is what proves the log is live rather than frozen. **217** total runs (was 216).
- **Home tree** on `main`, **0 stranded**, **0 behind**, **2 tracked-dirty** (`.claude/settings*.json` — settled property, correction (c) below), **1 untracked** (Friday's retro draft).
- **Discord: the cursor returned `[]` — 3 bytes, zero messages, for a fifth consecutive hour.** No receipt owed, none sent; cursor left at `1532100414189867109`. `allowFrom` was read from `access.json` **before** any classification, preserving fail-closed ordering with nothing to authorize. **Silence is not evidence he is away** (finding 8).
- **Ping gate (step 6): no ping sent, correctly.** Stored hash `e3b0c44298fc…b7852b855` = SHA-256 of the empty string; this run's Needs-Christian is also empty, so the hash matches and the doorbell stays silent.
- **Sibling reports (step 2.6): two in-window, no items folded.** `orchestrator-2026-07-30e.md` (written 02:50Z, 8 min before this brief) reads *"Nothing needs you. The Nudge Model WS5 content migration (THR-838) keeps moving one right-sized batch per hour…"* — **empty state, skipped per the rule**, and top-level `##` so the consumer's pattern matched. `backlog-grooming-2026-07-29.md` (~13h) leads with *"Nothing needs you"* — likewise skipped. `weekly-hygiene-2026-07-26.md` is **~90h**, outside the 36h window.
  - **Same-day collision is now FIVE files** (`-30` / `-30b` / `-30c` / `-30d` / `-30e`). mtime and lexical order still agree, **so the consumer has been correct-by-luck five consecutive hours.** Collision cause ticketed (THR-849); **consumer-side half still unticketed.**
- **Zero Linear writes.** Three column reads (`Ready for Dev`, `In Dev`, `Done`) and three `get_issue` confirmations (THR-667, THR-862, THR-863). Read-only throughout.
- **This run's worktree was created outside `.claude/worktrees/`** (`Dev/Projects/kwf-wt-20260730-0257`), with `rev-parse --show-toplevel` asserted after creation, **the `.git` file's existence checked (THR-797), and the home tree's branch + behind-count re-read as `main` / 0 in the same command.** **The home tree was touched by `git -C` reads, one `git worktree add`, three read-only `npm run` probes, `gh` reads, and nothing else.**

### Correction to a claim carried for three runs

- **The `python` diagnosis was WRONG, and the wrong diagnosis pointed at the wrong fix.** Three consecutive runs recorded that the skill's step-0 allowlist snippet *"is unusable in this sandbox (no `python` on PATH)"*. **Re-derived verbatim this run: `python` and `python3` are both PRESENT** (`python -c "print(1)"` → `1`). The snippet fails for a different reason: `$HOME` expands to the MSYS path `/c/Users/chris`, and **Windows `python` cannot open an MSYS-style path** — `FileNotFoundError: '/c/Users/chris/.claude/channels/discord/access.json'`.
  - **Why the correction matters rather than being pedantry: the remedy changes.** "No interpreter" implies *avoid python / read the file directly* (what three runs did as a workaround). "Path not translated" implies a **one-token fix in the skill** — `$USERPROFILE`, or a literal `C:/Users/chris/...`, or `cygpath -w`. The fail-closed property was never at risk either way, but the ticketable fix was mis-specified for three runs.
  - **This is finding 1 paying out on a claim nobody doubted**, and finding 34 in miniature: three sightings of a true *effect* (the snippet fails) hardened an untested *attribution* (no interpreter), and the attribution felt better supported each run.

## Open agent work (not Christian's)

- **PR #1085 (THR-667) is green, armed, and BEHIND — finding 29's exact shape, live.** `mergeable: MERGEABLE`, `mergeStateStatus: BEHIND`, auto-merge enabled 02:29:27Z by `christianspliid-ui`, every check `SUCCESS`. Under strict branch protection auto-merge does not fire at `BEHIND` (memory `automerge_behind`), **but finding 29(a) was retracted** — #1042 merged ~2h07m after entering `BEHIND`. **Falsifiable test for next run, stated in advance: if #1085 is still `OPEN`/`BEHIND` at 03:58Z (~1.5h armed), the branch needs an explicit update and "armed" is not self-healing at this cadence; if it merged, the retraction generalises.** Either way this is agent-side.
- **The orchestrator's write sequence is the narrowed suspect for the assignee leak, and it now has a contrast class.** THR-862 (executor-filed, 02:22Z) clean; THR-863 (orchestrator-filed, 02:31Z) stamped; live prompt content-identical to the repo mirror. **Fix shape: instrument the orchestrator's own `create → save_issue(null) → get_issue` sequence on its next filing and record the three responses**, rather than re-attributing to Linear defaults or to sweep windows. **Wants a ticket from a write-remit session** (finding 23).
- **The step-0 allowlist snippet wants a one-token path fix** (see correction above): `$HOME` → `$USERPROFILE` or a Windows-resolvable literal. Small, and the mis-diagnosis is now on record so the ticket can be specified correctly. **Wants a ticket** (finding 23).
- **Step 2.6's section extraction is keyed on the producer's incidental formatting AND its file naming, and neither is stable. Re-derived 02:58Z — the file-selection axis is now FIVE-deep in a single day.** mtime and lexical order still agree (`.` < `b` < `c` < `d` < `e`), **so the consumer has been correct-by-luck five consecutive hours.** Prior derivation unchanged: `orchestrator-2026-07-30.md` uses `## Needs Christian`; `orchestrator-2026-07-29.md` uses `### Needs Christian` — **same lane, two files, one day apart, two different levels.** **Fix shape:** select by explicit mtime (not glob order), match the *text* with a permissive level (`^#{2,6} Needs Christian`), terminate on a same-or-shallower heading. **Wants a ticket** (finding 23).
- **`check:task-heartbeat` mis-fires at a wake boundary — carried, not re-derived this run.** `scripts/check-scheduled-task-heartbeat.ts` accepts any sibling whose `lastRunAt` falls **inside** the stall window as its liveness witness; after a host sleep the whole fleet's `lastRunAt` clusters at the window's *closing* edge. **Fix shape:** require the witness to have fired strictly inside the window with a margin, or detect the burst directly (≥2 lanes sharing a `lastRunAt` to the second ⇒ host-wake, suppress). **Wants a ticket** (finding 23).
- **Verify-after-write needs a clock — carried; THR-860's self-refuting row remains the best artefact.** Its description asserts a `get_issue` re-query confirmed the assignee absent; the field was present on read. **The claim and its refutation live in the same row.** **Fix shape:** verify after the last side-effect in your own sequence, or on the next run.
- **A superseded report PR wants closing: #1031** (orchestrator/THR-762). **RE-DERIVED 02:58Z** (finding 1), and **the first read returned `UNKNOWN`/`UNKNOWN` exactly as finding 29(b) predicts** — the second read gives `OPEN`, `mergeStateStatus` **`DIRTY`**, `mergeable` `CONFLICTING`, `updatedAt` still `2026-07-28T19:31:25Z` — **~31.4h idle**. Consistent with a superseded report doc, not a regression (memory `dirty_pr_duplicate`). **One `gh pr close`, zero content lost.**
- **THR-835 is unreachable in `Idea`** — carried, and **re-confirmed by live data this hour**: the sole required check ran 18m02s against its 20m cap (**90.2%**), which is precisely the runner-variance hazard THR-835 describes. Both column scans confirm it is in neither `Ready for Dev` nor `In Dev`. **A working promotion lane that never reaches a column is stronger evidence than a broken one** (finding 19). Remedy belongs to a write-remit session.
- **`rescue/2026-07-17-detached-plans` is prunable — carried, not re-derived this run.** A **local branch only**; against `origin/main` it carried **4 unique commits** as of 22:54Z. **Pruning it is `git branch -D` in the home tree, which this lane may not do** — not merely unclaimed, unclaimable from here. **Wants a write-remit session with home-tree authority.**
- **The orchestrator lane's stray-write defect is open and agent-owned.** `tb-orchestrator` wrote its 08:29Z report into the home tree as well as its own worktree, blocking a fast-forward. Durable fix is in that lane's write path (absolute paths, or `rev-parse --show-toplevel` asserted before any write — the workaround this task takes hourly). **Wants a ticket** (finding 23).
- **No gate notices live/mirror prompt divergence.** **This run proved the two are currently in sync for `tb-orchestrator`** (diff = one trailing newline), so the general defect stands without a live instance: `~/.claude/scheduled-tasks/<id>/SKILL.md` is what runs, `Docs/ops/scheduled-task-prompts/` is a mirror a merge can update while the live copy stays stale, and **nothing compares them.** THR-850 covers the missing mirror for `tb-opus-pickup`; the divergence *detector* is unticketed. **Wants a ticket** (finding 23).
- **Friday's retro draft** (`Design/retros/retro-2026-07-24-draft.md`) is untracked and wants a `git add` in any docs PR. **Not a carry** — it is THR-798's step 2, with an owner and a Done-when.
- **Impediment #267 is used twice**; **#219 and #228 are duplicate rows for one defect**. Both dispositions: fold at the next retro. **No ticket.**

## Durable findings carried forward

Rules earned by tickets that have since shipped. **Kept as rule sentences; every worked example is in `git log -p Design/user-actions.md`.**

1. **Before carrying an observation into its Nth run, re-derive it once.** **Paid out hard 2026-07-30 02:58Z on a claim nobody doubted:** three runs asserted the step-0 snippet failed for want of a `python` interpreter; `python` is present and the real cause is MSYS path expansion. **The re-derivation cost one command and corrected the remedy, not just the wording.** The generalisation is finding 34's: a repeatedly-observed *effect* silently hardens an *untested attribution*.
2. **A subsystem with no callers is invisible to every sweep that looks for wrong behaviour, because it has none.** Corollary: grep the *assignment* side, not the read side.
3. **A plan doc's past tense is a claim, not evidence.**
4. **Advisory gates that nobody reads are indistinguishable from gates that do not exist.**
5. **A test suite that agrees with the bug stays green forever** — and a pool-scoped guard is a suite that agrees with the bug one shelf over.
6. **Read composition, not depth** — and stop re-testing the favourable dip.
7. **A gate clearing is not the same as a decision arriving.** Surfacing a settled call for ratification is worse than not surfacing it. Corollary: verify a *favourable* outcome's cause as carefully as an unfavourable one.
8. **An empty Discord inbox is not evidence Christian is away.** Silence means only that he did not write.
9. **A startup-failure cluster is indistinguishable from quota exhaustion at the moment it happens.** The disambiguating evidence — *does a fresh run execute jobs?* — costs one run. Take it before escalating to a billing page.
10. **Deploy-observability must classify by paths, not by branch name or PR title.** — SHIPPED 2026-07-27 in THR-785.
11. **Mutex lines with reasons work.**
12. **An unanswered ask is not an unheard ask — do not re-ring the doorbell.** A standing ask carries a stated safe default, so silence resolves it rather than blocking on it.
13. **Declining to build the missing half is a shippable outcome, and it needs its reasoning recorded to survive.**
14. **A signal with two meanings is not a signal — and the repair is a notification path, never a new gate.**
15. **When a ticket asks "which of these fixes?", census the pipeline before answering** — the honest answer may be "none, and here is why". Validate the *sample*, not just the stage.
16. **A deliberately-parked issue and a broken auto-close are indistinguishable from the board.** Report a falsifiable test in both directions.
17. **Before carrying an ask into another run, check whether it was answered somewhere other than the channel it was asked on.**
18. **State a falsifiable test against the outcome, never against an activity proxy.** State the test's enabling premise explicitly, or a benign world reads as its own failure case.
19. **A column-scoped queue query reports the lane's reach, not the board's contents — and from inside the query the difference is invisible.**
20. **When an ask names a *mechanism* rather than a decision, expect the answer to arrive on the channel he has, not the one the mechanism needs.** (a) Prefer asks that are pure decisions. (b) State plainly what was *not* done.
21. **Re-derive a carry even when you expect it to hold** — the check has a wider aperture than the claim. Look at what the source shows you, not only the field you came to check.
22. **An item in the lane's column is not an item the lane can take.** The cheap detector: read the comments, not just the fields. *Consequential* and *decidable-by-Christian* are independent axes.
23. **A ticket this task files is unpullable by construction, and the cost lands on someone else's run — so filing is never free.**
24. **A file's *state* is evidence; its *author* is a separate claim needing separate evidence.**
25. **A confirmed test licenses the claim it tested, not the remedy that motivated it — and the gap is widest exactly when the result is most satisfying.** Corollary: **the mirror failure is a test whose aperture is wider than the claim.** **Applied 2026-07-30 02:58Z:** one successful pickup licenses *"the picker can reach the shelf"*, not *"the shelf will drain"* — the shelf in fact grew that same hour.
26. **One instance locates a defect; the second instance with the same fingerprint is what turns it into a mechanism — and changes the remedy.**
27. **A timestamp is evidence only once its zone is verified, and a `Z` suffix is a claim like any other.** Prefer a machine-written field (`stateHistory`, `lastRunAt`, commit metadata) over a prose header. **Earned again 2026-07-30 02:58Z in a new direction: the *wrong field* misdates an event as badly as the wrong zone.** THR-667's `updatedAt` (02:30:36Z) sits 28 minutes after its actual `Ready for Dev`→`In Dev` transition (02:02:22Z, from `stateHistory`), because attachments landed later. **A run reading `updatedAt` would have credited the claim to the wrong lane's slot.**
28. **A classifier observed only in its stable state has not been tested.** **Now satisfied in BOTH directions:** three consecutive `deployedSha` advances (one landing on a real code merge), then a correct **flat** hour when only docs merged. **Note what switched: the evidence, not the verdict word** — four runs reported `skipped` throughout.
29. **"Green and armed" is not "merging" — and the difference is invisible from every signal a lane normally checks.** Checks answer *is this change correct*; `mergeStateStatus` answers *can it go in now*. **The tell is throughput, not status.** **Live instance as of 02:58Z: PR #1085, all checks SUCCESS, auto-merge armed, `BEHIND`.**

  **Two corollaries.** (a) ~~**It does not self-heal.**~~ **RETRACTED 2026-07-29 13:23Z** — #1042 merged ~2h07m after entering `BEHIND`. **The retraction is itself the lesson: "still true at T" is not "true at every T".** (b) **`gh pr view` returns `UNKNOWN` for `mergeable`/`mergeStateStatus` on the first read** and often the true value on the second — but **not always**. **Re-confirmed 02:58Z: #1031's first read was `UNKNOWN`/`UNKNOWN`, its second `DIRTY`/`CONFLICTING`.** Treat `UNKNOWN` as "not yet computed", never as evidence of mergeability.

30. **A liveness witness must fire strictly *inside* the window it witnesses, not merely fall within it.** **The general form: a guard that accepts an endpoint as evidence about an interval passes hardest exactly when the interval is most degenerate.** Corollary: a probe's verdict is evidence, not a verdict — when independent witnesses contradict it, override and say why.

31. **A cleanup and its cause are separate deliverables, and shipping the cleanup is the moment the cause is most likely to be dropped.** State the re-accrual rate and a dated falsifiable test against the next producing event.

  **The honest quantity is not a rate: it is `|rows the sweep handled|`, workload-dependent and unbounded above.** Meta-lesson: **when a number has been wrong once, the reflex is to move it the other way and re-assert it with equal confidence.** Prefer naming the *variable*.

  **Closed out 2026-07-30 01:58Z in a direction the finding did not anticipate.** THR-845 shipped cleanup, cause-side hardening, **and** a third thing: **it removed the consumer that made the cause consequential.** Generalisation: when a defect's cleanup and cause are both expensive, check whether the *reader* can be made indifferent. **Confirmed 02:58Z — the leak produced another row (THR-863) and produced no symptom.**

32. **Verify-after-write needs a clock, not just a re-read — an immediate confirmation read can be served stale and is indistinguishable from success.** **Corollary: a count is not a verification.**

  **Amended 2026-07-29 17:53Z — a second, distinct failure with the same shape.** A `get_issue` check was **correct when it ran** and false 131 seconds later. **An immediate verify cannot distinguish "my write held" from "my write held briefly"** — so the fix is **verify after the last side-effect in your own sequence**, not after the mutation you care about.

  **Sharpened 2026-07-29 21:54Z by a controlled pair:** 9 immediate verifies after unassigns all held an hour later; 2 after creations were both false within the hour. **The verification was never the variable — what wrote next was.**

  **Best artefact: THR-860's description asserts its own verification and the field contradicts it in the same row.** Argues for putting the verification *timestamp* in the prose whenever the claim is recorded.

33. **A claim widens when it is restated, and the widening is invisible because every restatement still cites the original evidence.** **The drift is toward the remedy's framing**, because a report is written to justify acting. **Detector:** state what observation would falsify *today's* wording, then check whether it would also have falsified the wording used when the evidence was gathered. **Corollary: severity claims need their own evidence, separately from mechanism claims.**

34. **N confirming sightings of an effect do not locate its cause — and each sighting makes the unexamined attribution feel better supported.** **The trap is that repetition reads as accumulating evidence.** **Detector, cheap: before reporting a recurrence, name one property the affected rows share that the unaffected rows do not — and check it.** If you cannot state the contrast class, you have an effect, not a cause.

  **Applied successfully twice on 2026-07-30 02:58Z, which is the first time this finding was *used* rather than *earned*.** (i) The assignee leak: THR-862 (executor-filed) came out clean while THR-863 (orchestrator-filed) came out stamped, 9 minutes apart on the same API actor — **the contrast class is the writing lane**, which narrows the suspect away from Linear's create defaults. (ii) The `python` claim: the contrast was *interpreter present* vs *path unopenable*, and checking it corrected three runs of mis-attribution. **Corollary, and the reason this outranks a tidier lesson: a remedy that fails while correctly applied is the highest-value evidence available about the cause.**

35. **A query's filter encodes a premise about the world, and nobody audits a premise that has never produced a visible failure.** (Earned 2026-07-29: `pull-work` selected work with `assignee:null` for months, encoding *"an assignee means a person has claimed this row."* Christian, unprompted: *"I do not use linear or github at all. so any issues in linear or PRs in github with my name on them are yours."*) **Detector, cheap: for each predicate in a query you are debugging, say out loud what it assumes about who or what writes that field — then ask whether anyone has ever confirmed it with the writer.** **Corollary:** 34 says N sightings do not locate a cause; **35 says the cause may not be in the mechanism at all, but in the question.** **The reader is usually yours to change even when the writer is not.**

  **SHIPPED 2026-07-30 01:21:50Z as THR-845 — and OBSERVED WORKING 2026-07-30 02:02:22Z.** The merge closed the class at writer, reader and enforcer; **41 minutes later the first pickup under the new reader claimed a job that had sat unclaimed for 36.6 hours, in 76 seconds.** **What makes this the finding's proof rather than its epilogue: the eight prior runs each produced a correct local measurement and a wrong global conclusion, and no amount of additional write-side measurement would have converged.** The unlock was a question, asked of the one party nobody had asked. **Second-order lesson for this lane: the answer arrived because step 0 existed to hear it.** A read channel is not a convenience; it is the only route by which a premise held by a human reaches a robot debugging around it.

36. **A consumer that matches on a heading's *level* rather than its *text* is a channel that silently narrows as producers reformat.** **Detector: for every "no items found" result on a shared channel, confirm the *absence* against the raw file once, not just against your pattern.** Sibling of finding 19: **a scan reports its own pattern's reach, and a narrowed pattern and an empty world are the same observation.**

  **Generalised 2026-07-29 23:54Z: "formatting" was too narrow.** The near-miss was **file selection**. So the rule is **every point where a consumer infers structure from a producer's convention is a coupling the producer can change unilaterally, and each one fails silently in the same direction.** Detector: for each such inference, name the producer change that would break it, then check whether anything would tell you.

  **Fifth consecutive correct-by-luck hour, 2026-07-30 02:58Z: the same-day set is now `-30` / `-30b` / `-30c` / `-30d` / `-30e`.** **A coupling that has survived five hours looks progressively more like a design and less like a hazard, and that drift is finding 33's tell turned on itself.** The luck is alphabetical and finite. **Record the streak as accumulating exposure, not accumulating safety.**

**Owner of items below:** Christian. Everyone else's blockers go in Linear or `Docs/impediments.md`.
**Refresh cadence:** The hourly `keep-work-flowing-cc` scheduled task keeps this current (prunes resolved items, adds newly-surfaced Christian-owned ones); the `retrospective` skill still does the deep periodic rebuild.

## How this works

The retrospective stopped being the right channel for "Christian, please flip one switch." Christian-owned asks were buried in retro prose, and nothing surfaced them between retros. This file replaces that pattern.

Read order: top to bottom is blast radius. The top items break canonical workflow invariants right now; the bottom items are operational debt with workarounds in place.

For each item:
- **Fix** = the literal command, click path, or line of config that resolves it.
- **What breaks** = the named system or invariant currently degraded.
- **Source** = `Docs/impediments.md` entry numbers + occurrence count, so the cost is visible.

When an item resolves: delete it from this file, mark the corresponding impediment as resolved in the dashboard regen, note the close in the next retro under "What shipped." **No history retained here — `git log` + the retros are the audit trail.** The resolved log is pruned to the current day; anything older is recovered with `git log -p Design/user-actions.md`.

---

## Standing asks

**NONE OPEN — second consecutive run at a true empty.**

**A new unexplained multi-hour pause would be a NEW ask with new evidence — never the overnight-wake question reopened** (finding 12). If he ever does want overnight throughput guaranteed: keep the machine awake and **on mains power**, because Windows Task Scheduler refuses the reaper on battery (`-2147020576`, memory `git_cleanup_automation`). Recorded as a fact, not a request.

**Six questions are answered, closed, and must not be re-asked:**

1. **The five empty projects** — Christian, chat 2026-07-25 21:53: *leave them open; they are intake buckets that refill as the game iterates.*
2. **The GitHub Actions payment block** — cleared 2026-07-25 ~17:09Z and again 2026-07-28 17:02Z, both verified by re-run, and corroborated thirteen times since by independent mechanisms. **A recurrence is a new ask with new evidence, never this one reopened** (finding 12).
3. **THR-799 priority ordering** — Christian, quoted in THR-774/775, 2026-07-27: *the new encounter experience is still first priority.*
4. **Release of THR-821/820/777/778** — Christian, Discord 2026-07-27 14:38Z.
5. **The Linear "assign issue to PR author" switch** — Christian, Discord **2026-07-29 18:10:03Z**: *"can't find anything like that. I do not use linear or github at all. so any issues in linear or PRs in github with my name on them are yours."* **Closed by falsification, on the terms the ask itself set.** The second sentence is a **standing fact**: an assignee of "Christian Spliid" carries no information and must never be read as a human claim. **As of 2026-07-30 01:21Z that fact is enforced in code, and as of 02:02Z it is observed working** — `pull-work` no longer filters on it (THR-845), and the first pickup under the new reader claimed in 76 seconds. **The highest-leverage answer in this file's history: one sentence retired eight runs of misdirected investigation and unblocked the executor lane.**
6. **Should the host machine stay awake overnight?** — **retired 2026-07-30 01:58Z by observation**, now three consecutive awake nights, never answered directly and no answer needed.

**One standing switch exists but is not an ask:** the orchestrator lane can be disabled on one word from Christian (live 2026-07-27 19:49Z). **Its default state is the intended one**, so silence resolves it.

**Three durable corrections survive closed asks and are kept as rules:**

- (a) A blanket `git stash` was the wrong remedy for home-tree dirt; the single-path form is correct. Parking his tool-permission edits was never necessary.
- (b) `$REATTACH_MAX_TRACKED_DIRTY = 0` governs the detached-HEAD reattach path and never evaluates while HEAD is on `main`. The conclusion held; its stated reason did not.
- (c) **Tracked dirt alone does not stall autosync.** The stalling class is the *intersection* of locally-modified **∩** changed-by-incoming-commits — proven by fast-forwarding cleanly with `.claude/settings*.json` dirty.

---

## Resolved 2026-07-30

**Seventh consecutive run at zero open Christian-owned asks, second at a true empty.** Everything that moved this hour was agent-side:

- **THR-845 paid out in observation, which is the event this file has been waiting eight runs to record.** The 02:01:06Z pickup claimed THR-667 at **02:02:22Z** (from `stateHistory`, not `updatedAt`) — **76 seconds** — on a job that had sat in `Ready for Dev` for **36.6 hours** across ~36 prior wakes. Green PR (#1085) by 02:48Z. **Last run stated the failure condition in advance and the world did not meet it.** Scoped per finding 25: this licenses *"the picker can reach the shelf"*, not *"the shelf will drain"*.
- **The shelf still grew 50 → 51, and the reconciliation is the interesting part.** −1 claimed, **+2 filed** (THR-862 by the executor at 02:22Z, THR-863 by the orchestrator at 02:31Z). **Execution started and filing still outran it 2:1.**
- **Last run's live-prompt hypothesis is DISCONFIRMED by direct check.** `diff` of the live `tb-orchestrator` prompt against its repo mirror = **one trailing newline**; the live copy carries THR-845's step 5a verbatim. So THR-863's stray assignee is not a stale-prompt symptom.
  - **A contrast class replaced it (finding 34's detector, used rather than earned):** THR-862 (executor-filed) clean, THR-863 (orchestrator-filed) stamped, 9 minutes apart, same API actor. **The discriminator is the writing lane** — the first evidence narrowing this away from Linear's create defaults, and it also does not fit the orchestrator's own "twice-daily sweep window" account.
- **A carried claim was corrected: the step-0 `python` snippet fails on MSYS path expansion, not for want of an interpreter.** `python` and `python3` are both present. **The remedy changes from "avoid python" to a one-token path fix**, and it had been mis-specified for three runs. **Finding 1's clearest payout to date.**
- **THR-836's candidate status weakened by evidence, not by argument.** THR-862 is a *filed Deferral carrying a full coordination block*, so the "filed Deferrals have none by construction" premise does not hold for executor-filed ones. **The count of shelf items that actually fail Step 3 remains unmeasured, and is now known to be smaller than the Deferral count.**
- **Finding 28 satisfied in the opposite direction.** `deployedSha` held **flat** at `bcfbe645` because only docs merged — after three consecutive advances. A classifier that moves when it should and holds when it should is now tested both ways.
- **Finding 29 acquired a live instance and finding 29(b) was re-confirmed.** #1085 is green + armed + `BEHIND`; #1031's first mergeability read returned `UNKNOWN`/`UNKNOWN` and its second `DIRTY`/`CONFLICTING`. **A falsifiable test for #1085 is stated in advance for next run.**
- **THR-835's premise re-confirmed by live data:** the sole required check ran **18m02s against a 20m cap (90.2%)**. Still unreachable in `Idea`.
- **The merge gate held for a thirteenth consecutive check**, corroborated this hour by an 18-minute workflow that genuinely executed. The fleet heartbeat returned `ok` for a twelfth across a third awake overnight — lanes at 02:01Z / 02:26Z / 02:53Z with distinct timestamps. The reaper ran 02:40Z with worktrees 36→**37** and branches 46→**48**, a moving count that independently proves the log is live.
- **Discord returned a literal `[]` for a fifth consecutive hour** — nothing to classify, no receipt owed, cursor unmoved at `1532100414189867109`. **The ping gate correctly stayed silent**: stored hash is the SHA-256 of the empty string and this run's Needs-Christian is also empty.
- **Finding 36's file-selection axis went five-deep in one day** (`-30` … `-30e`). mtime and lexical order still agree — **correct-by-luck for a fifth consecutive hour.** Recorded as **accumulating exposure, not accumulating safety.**
