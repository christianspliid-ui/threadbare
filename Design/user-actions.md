# User Action Required

**Last updated:** 2026-07-30 02:54 local, by the hourly `keep-work-flowing-cc` CC task.

**ZERO OPEN CHRISTIAN-OWNED ASKS — fifth consecutive run at zero.** Discord inbox **genuinely empty** for a third consecutive hour — the `after=` cursor returned a literal `[]` (3 bytes). Home tree healthy: on `main`, 0 stranded, **0 behind**, 2 tracked-dirty (settled). Deploy `skipped`-benign at an **advanced** publish point. Merge gate `healthy`, `startupFailureCount: 0`. Fleet heartbeat `ok` — all 8 enabled lanes within tolerance. **The hour's two real signals, and they cut in opposite directions: (1) the three-hour visible-queue freeze BROKE — picker-visible 45 → 48 against a flat shelf of 49; (2) the write-side workaround was applied correctly, verified clean at 00:32Z, and re-accrued within 22 minutes. The backlog cleared and the mechanism did not. Those are separate facts and this run keeps them separate.**

## This run's measurements (2026-07-30 00:54 UTC)

Full hour since the previous brief; the lane held its `:45` slot and the deltas below span a true hour.

- **The three-hour freeze broke, and the correct reading is "the backlog drained", not "the bug is fixed".** `Ready for Dev` reads **48 of 49 unassigned** (was 45 of 49). **THR-855, THR-856, THR-858, THR-859 all came clean this hour**; the sole named row is **THR-860, filed 00:32Z**. Shelf 49 → 49; **picker-visible 45 → 48.**
  - **Cause of the drain is NOT established, and this run does not claim one.** No lane's report says it unassigned those four. Candidates: the in-flight THR-845 work, or the `00:01Z` pickup lane. **A favourable movement wants its cause verified as carefully as an unfavourable one** (finding 7 corollary) — and it has not been. Recorded as measured, unattributed.
  - **The re-accrual mechanism is UNCHANGED and this hour is its strongest disconfirmation of the write-side remedy yet.** The orchestrator's own report states it applied the documented two-call workaround to THR-860 — `issueCreate`, then a separate `save_issue(assignee: null)`, **then a `get_issue` re-query that confirmed the field absent.** Verified clean at 00:32Z; **named by 00:54Z, ~22 minutes later.** So the workaround fails *while correctly applied and positively verified*.
  - **This is finding 34's corollary paying out a second time: a remedy that fails while correctly applied is the highest-value evidence available about the cause.** Three write-side remedies are now dead — inline `assignee: null` on create (THR-845), the Linear workspace setting (unreachable, closed ask 5), and the verified two-call workaround (this hour). **The reader-side re-scope (finding 35) is the only candidate left standing, and it is now the thing In Dev.**
  - **Still no rate quoted** (finding 31, amended). One named row this hour, four last hour — `|rows the sweep filed or touched|`, workload-dependent. **The durable statement remains the pair, and this hour the pair changed shape: intake 1, visible-delta +3.** A positive visible-delta does not retire the pair; it is the first hour the two terms moved independently.
- **Queue: BACKED UP — 49 ready (0 Urgent / 0 High / 14 Medium / 35 Low), 1 In Dev.** Nothing stale: oldest `updatedAt` is THR-739 at 2026-07-24T20:29Z, **~4.18 days**, inside `STALE_ISSUE_DAYS`=7.
  - **`In Dev` holds THR-845 — the assignee-visibility fix itself — claimed this hour at `updatedAt` 00:29:50Z, and it is the queue's former only High.** The High count in `Ready for Dev` is 0 *because* the High was claimed, not because it vanished. **Worth stating explicitly: a priority band emptying is ambiguous between "claimed" and "lost", and the two are indistinguishable from a column count alone.**
  - **THR-638 (art batch) left `In Dev`** — its PR #1074 merged at 00:01:40Z (below). Four consecutive runs had recorded it advancing-while-open; the fifth records it closing, which is what made those four correct rather than a stall.
  - **THR-856 carries no project** — carried unchanged. Disposition belongs to `daily-backlog-grooming`, whose remit this is. **No ticket from here** (finding 23).
- **PR #1074 MERGED at 00:01:40Z — the dated threshold expired unused, and that is the result worth recording.** Armed `2026-07-29T22:30:58Z`, merged ~**1h31m** later, against the ~2h07m self-heal measured on #1042. **Last hour's brief wrote the trigger down in advance — *"if the next brief finds it `BEHIND` past ~2h07m … it wants one `gh pr update-branch`"* — and the trigger never came due.** `gh pr view` now reports `state: MERGED` with `mergeable`/`mergeStateStatus` both `UNKNOWN`, which is the expected post-merge shape and **not** the first-read `UNKNOWN` of finding 29(b).
  - **This closes the watch item and confirms finding 29(a)'s retraction was right.** Two independent observations now sit inside the self-heal band (1h31m, 2h07m) and none outside it. **The value was in stating the trigger before the hour it would fire** — it kept a robot's hand off a PR that was going to land on its own, without requiring a fresh judgement call under time pressure.
- **Deploy (step 2.5): `skipped`, benign, and the `deployedSha` ADVANCED to `2eb2cf632f7d57a66f578d1544f63f3133756199`** (was `955d0919…`). Verbatim: *"The live site is up to date. Commits since the last publish (2eb2cf63) only touched notes and docs, so the game itself did not need rebuilding."* **Finding 28 pays out a second time:** the publish point moved across a real merge and the classifier still correctly skipped the docs commits behind it. Confirmation lives in the *evidence* changing, not the verdict word.
- **Merge gate (step 2.5b): `healthy`, `startupFailureCount: 0`, `standDown: false`.** Verbatim: `{"verdict":"healthy","summary":"Automated checks are running normally.","needsChristian":false,"standDown":false,"startupFailureCount":0}`. **Eleventh consecutive independent confirmation** that Christian's billing action holds.
- **Step 2.7 (heartbeat) returned `ok`.** Verbatim: `{"verdict":"ok","needsChristian":false,"checked":8,"stalled":[],"neverRun":["monthly-rulebook-review"],"summary":"All 8 enabled scheduled tasks are within 2 slots of schedule."}`. Tenth consecutive `ok`. **This run is the first to exercise the probe across a full overnight with the host awake** — three hourly lanes fired at 00:01Z / 00:26Z / 00:53Z with distinct timestamps, so the wake-boundary defect (below) is genuinely *not* being triggered rather than merely quiet.
- **Reaper healthy — ran 02:40:02 local (00:40Z), 14 minutes before this brief.** `SUMMARY:` **36** worktrees / **48** branches / **2** stashes / **0** needs-disposition. **Branches 49 → 48** (one pruned; the other three counts flat), so the log is demonstrably live rather than frozen — and the `=====` header at 02:40:02 is independent proof of the run.
- **Home tree** on `main`, **0 stranded**, **0 behind**, **2 tracked-dirty** (`.claude/settings*.json` — settled property, correction (c) under *Standing asks*), **1 untracked** (Friday's retro draft).
- **Discord: the cursor returned `[]` — 3 bytes, zero messages, for a third consecutive hour.** No receipt owed, none sent; cursor left at `1532100414189867109` (nothing newer to advance to). `allowFrom` was read from `access.json` **before** any classification, preserving fail-closed ordering with nothing to authorize. **Method note carried and confirmed:** the skill's inline `python -c` allowlist read is unusable in this sandbox (no `python` on PATH); the file was read directly again. **Fail-closed holds only because the failure is noticed each time** — a silent `||` fallback there is indistinguishable from an empty allowlist, so this is worth a skill fix rather than an hourly workaround. **Silence is not evidence he is away** (finding 8).
- **Sibling reports (step 2.6): two in-window, no items folded — and finding 36's file-selection axis is now exercised THREE-deep in one day.** `orchestrator-2026-07-30c.md` (written 02:50 local, 4 minutes before this brief) reads *"Nothing needs you. The Nudge Model WS5 content migration (THR-838) keeps moving one right-sized batch per hour…"* — **empty state, skipped per the rule**. `backlog-grooming-2026-07-29.md` (~11h) leads with *"Nothing needs you"* — likewise skipped, though its billing-cleared note is corroborated independently above. `weekly-hygiene-2026-07-26.md` is **~88h**, outside the 36h window.
  - **Same-day collision is now `-30.md` / `-30b.md` / `-30c.md` — three files, one producer, one day.** mtime and lexical order still agree (`.` < `b` < `c`), **so the consumer has now been correct-by-luck three consecutive hours.** The luck is load-bearing and finite: a `-30d` through `-30z` sequence keeps agreeing, but a producer that ever emits `-30-2.md` or reverts to an undated form breaks lexical ordering while mtime stays right. Collision cause ticketed (THR-849); **consumer-side half still unticketed.**
- **Zero Linear writes.** Three column reads (two states, one `assignee:null` re-read), nothing else. Read-only throughout.
- **This run's worktree was created outside `.claude/worktrees/`** (`Dev/Projects/kwf-wt-20260730-0054`), with `rev-parse --show-toplevel` asserted after creation **and the home tree's branch re-read as `main` in the same command**, per THR-797. **The home tree was touched by `git -C` reads, one `git worktree add`, three read-only `npm run` probes, `gh` reads, and nothing else.**

## Open agent work (not Christian's)

- **THR-845 is now In Dev (claimed 00:29:50Z) — and this hour supplied the evidence that should shape its scope.** The ticket still blames the orchestrator's T1 write path. **All three write-side remedies are now dead, the third killed this hour under the most favourable possible conditions:** inline `assignee: null` on create (original THR-845 finding); the Linear workspace setting (unreachable, closed ask 5); and — new — **the documented two-call workaround, applied deliberately, with a `get_issue` re-query confirming the field absent at 00:32Z, re-accrued by 00:54Z.** **The fix is to stop reading the field:** `pull-work`'s `assignee:null` pickup filter should treat Christian-assigned rows as available, because he has stated his name never signals a human claim. Wants: his verbatim answer recorded, the Done-when re-scoped onto the pickup query, and **a grep for every `assignee:null` / "unassigned" predicate across `.claude/skills/pull-work/SKILL.md`, the orchestrator lane, and the process linter** before calling it fixed. **Note for whoever holds it: the four-row drain this hour (picker-visible 45 → 48) must not be read as the bug easing.** Shelf stayed 49, one fresh row was named on arrival, and the drain's cause is unattributed. **A backlog clearing while the mechanism runs is exactly the shape that gets a fix descoped.** **This lane comments nothing** (finding 23).
- **Step 2.6's section extraction is keyed on the producer's incidental formatting AND its file naming, and neither is stable. Re-derived 00:54Z — the file-selection axis is now three-deep in a single day.** **New this run:** the same-day set reached **three files** (`orchestrator-2026-07-30.md`, `-30b.md`, `-30c.md`); mtime and lexical order still agree (`.` < `b` < `c`), **so the consumer has been correct-by-luck three consecutive hours.** The agreement is not robust, it is alphabetical: a `-30-2.md` or a reversion to an undated name breaks lexical order while mtime stays correct. **So the consumer has now been correct-by-luck three times running across two different axes — heading level, then file selection twice.** The same-day collision itself is ticketed (THR-849); **the consumer-side half is not.** Fix shape extends: select by explicit mtime (not glob order) *and* match heading text permissively. Prior derivation, unchanged: `orchestrator-2026-07-30.md` uses `## Needs Christian` (top level, one occurrence); `orchestrator-2026-07-29.md` uses `### Needs Christian` (nested under a per-run `##`, thirteen occurrences). **Same lane, two files, one day apart, two different levels** — so a `^## `-only consumer reads one and is blind to the other, and **this run's extraction succeeded purely because the newest file happened to be top-level.** A consumer that works this hour and fails the next with no code change on either side is the defect in its sharpest form. Both sections were empty again, so nothing has yet been lost; **the next non-empty orchestrator ask lands on a coin flip**, which is precisely the defect THR-826 existed to close (a Christian-facing channel with no consumer). **Fix shape:** match the *text* with a permissive level (`^#{2,6} Needs Christian`), terminate on a heading of the same-or-shallower level, and prefer the newest run block. **Wants a ticket from a write-remit session** (finding 23).
- **`check:task-heartbeat` mis-fires at a wake boundary — carried, not re-derived this run.** `scripts/check-scheduled-task-heartbeat.ts` accepts any sibling whose `lastRunAt` falls **inside** the stall window as its liveness witness; after a host sleep the whole fleet's `lastRunAt` clusters at the window's *closing* edge, so the clause is satisfied by the very timestamps proving a shutdown. **Fix shape:** require the witness to have fired strictly inside the window with a margin (`witness.lastRunAt < windowEnd − oneCadence`), or detect the burst directly (≥2 lanes sharing a `lastRunAt` to the second ⇒ host-wake, suppress). **Wants a ticket** (finding 23).
- **Verify-after-write needs a clock — carried, and this run adds a same-millisecond variant worth folding in.** The orchestrator verified 9 unassigns by immediate re-`get_issue` and all 9 held; it also verified THR-855 and THR-856 clean at creation and **both were named afterwards.** Same method, opposite outcomes, distinguished only by what wrote next. **Fix shape unchanged:** verify after the last side-effect in your own sequence, or on the next run.
- **A superseded report PR wants closing: #1031** (orchestrator/THR-762). **RE-DERIVED 00:54Z** (finding 1): `OPEN`, `mergeStateStatus` **`DIRTY`**, `mergeable` `CONFLICTING`, `updatedAt` still `2026-07-28T19:31:25Z` — **~29.4h idle.** Consistent with a superseded report doc, not a regression (memory `dirty_pr_duplicate`: check diff direction before assuming a conflict is content loss). **One `gh pr close`, zero content lost.**
- **`rescue/2026-07-17-detached-plans` is prunable — RE-DERIVED 22:54Z, and the re-derivation corrected the claim's scope** (finding 1). It is a **local branch only**: `git ls-remote --heads origin 'rescue/*'` returns nothing, so there is no remote ref and `origin/rescue/...` does not resolve. Against `origin/main` it carries **4 unique commits** (count confirmed; the blob-identity check from 15:56Z was *not* re-run and is not re-asserted here). **The correction matters for who can act:** pruning it is `git branch -D` **in the home tree**, which this lane may not do — so it is not merely unclaimed, it is unclaimable from here. **Wants a write-remit session with home-tree authority.**
- ~~**PR #1074 is green, armed, `BEHIND`…**~~ **CLOSED 2026-07-30 00:54Z — merged at 00:01:40Z, ~1h31m after arming, inside the ~2h07m self-heal band.** The dated trigger stated one hour in advance never came due, and no hand-intervention was taken. **Kept as a one-line record because the *method* is the durable part:** naming the expiry before the hour it would fire is what stopped a robot from `update-branch`-ing a PR that landed on its own. Second observation inside the band, none outside it.
- **THR-835 is unreachable in `Idea`** — carried. Both column scans confirm it is in neither `Ready for Dev` nor `In Dev`; `stateHistory` again not re-read. Content is a live merge hazard (the sole required check runs at 80–90% of its 20-minute cap; runner variance turns correct PRs red). **A working promotion lane that never reaches a column is stronger evidence than a broken one** (finding 19). Remedy belongs to a write-remit session.
- **The orchestrator lane's stray-write defect is open and agent-owned.** `tb-orchestrator` wrote its 08:29Z report into the home tree as well as its own worktree, blocking a fast-forward. Durable fix is in that lane's write path (absolute paths, or `rev-parse --show-toplevel` asserted before any write — the workaround this task takes hourly). **Wants a ticket** (finding 23).
- **Friday's retro draft** (`Design/retros/retro-2026-07-24-draft.md`) is untracked and wants a `git add` in any docs PR. **Not a carry** — it is THR-798's step 2, with an owner and a Done-when.
- **Impediment #267 is used twice** (a `sandbox` row and a `tooling` row, filed the same day by different lanes); **#219 and #228 are duplicate rows for one defect** (Playwright MCP writing a bare-filename screenshot outside the repo). Both dispositions: fold at the next retro. **No ticket.**

## Durable findings carried forward

Rules earned by tickets that have since shipped. **Kept as rule sentences; every worked example is in `git log -p Design/user-actions.md`.**

1. **Before carrying an observation into its Nth run, re-derive it once.**
2. **A subsystem with no callers is invisible to every sweep that looks for wrong behaviour, because it has none.** Corollary: grep the *assignment* side, not the read side.
3. **A plan doc's past tense is a claim, not evidence.**
4. **Advisory gates that nobody reads are indistinguishable from gates that do not exist.**
5. **A test suite that agrees with the bug stays green forever** — and a pool-scoped guard is a suite that agrees with the bug one shelf over.
6. **Read composition, not depth** — and stop re-testing the favourable dip.
7. **A gate clearing is not the same as a decision arriving.** Surfacing a settled call for ratification is worse than not surfacing it. Corollary: verify a *favourable* outcome's cause as carefully as an unfavourable one.
8. **An empty Discord inbox is not evidence Christian is away.** Silence means only that he did not write.
9. **A startup-failure cluster is indistinguishable from quota exhaustion at the moment it happens.** The disambiguating evidence — *does a fresh run execute jobs?* — costs one run. Take it before escalating to a billing page.
10. **Deploy-observability must classify by paths, not by branch name or PR title.** — SHIPPED 2026-07-27 in THR-785.
11. **Mutex lines with reasons work.**
12. **An unanswered ask is not an unheard ask — do not re-ring the doorbell.** A standing ask carries a stated safe default, so silence resolves it rather than blocking on it.
13. **Declining to build the missing half is a shippable outcome, and it needs its reasoning recorded to survive.**
14. **A signal with two meanings is not a signal — and the repair is a notification path, never a new gate.**
15. **When a ticket asks "which of these fixes?", census the pipeline before answering** — the honest answer may be "none, and here is why". Validate the *sample*, not just the stage: a bug report carries an unstated premise that its subjects were eligible to exhibit the behaviour.
16. **A deliberately-parked issue and a broken auto-close are indistinguishable from the board.** Report a falsifiable test in both directions.
17. **Before carrying an ask into another run, check whether it was answered somewhere other than the channel it was asked on.**
18. **State a falsifiable test against the outcome, never against an activity proxy** — a proxy-worded test can pass while the thing it guards fails. State the test's enabling premise explicitly, or a benign world reads as its own failure case.
19. **A column-scoped queue query reports the lane's reach, not the board's contents — and from inside the query the difference is invisible.**
20. **When an ask names a *mechanism* rather than a decision, expect the answer to arrive on the channel he has, not the one the mechanism needs — and say so instead of letting him assume it landed.** (a) Prefer asks that are pure decisions. (b) State plainly what was *not* done, or "fixed" reads as *all of it is handled*.
21. **Re-derive a carry even when you expect it to hold** — the check has a wider aperture than the claim, so a *true* carry can still pay. Look at what the source shows you, not only the field you came to check.
22. **An item in the lane's column is not an item the lane can take — and "unblocked" is the wrong question to keep asking.** The cheap detector: read the comments, not just the fields. *Consequential* and *decidable-by-Christian* are independent axes; conflating them turns a plain-language inbox into a firehose.
23. **A ticket this task files is unpullable by construction, and the cost lands on someone else's run — so filing is never free.**
24. **A file's *state* is evidence; its *author* is a separate claim needing separate evidence.** Naming the wrong author makes an ask bigger and less trustworthy than it needs to be.
25. **A confirmed test licenses the claim it tested, not the remedy that motivated it — and the gap is widest exactly when the result is most satisfying.** Corollary: **the mirror failure is a test whose aperture is wider than the claim, which can disconfirm something the claim never asserted.** Scope the test to the assertion — a whole-tree diff against a stale branch measures staleness, not whether its unique commits are merged.
26. **One instance locates a defect; the second instance with the same fingerprint is what turns it into a mechanism — and changes the remedy.** A one-off wants a cleanup, a recurring one wants a fix *plus* a cleanup. **Re-derivation earns its keep here** (finding 1) — the check that confirms a carry is the same check that catches it changing kind.
27. **A timestamp is evidence only once its zone is verified, and a `Z` suffix is a claim like any other.** Prefer a machine-written field (`stateHistory`, `lastRunAt`, commit metadata) over a prose header; when only prose is available, say which it was. **The failure is silent by construction** — a wrong-but-plausible hour never contradicts anything.
28. **A classifier observed only in its stable state has not been tested.** Report a constant verdict as the weaker fact it is, and treat the first correct switch as the real confirmation. **Paid out 2026-07-29 21:54Z:** four runs of `skipped` at a frozen SHA proved nothing; the fifth advanced the publish point across a real merge and correctly skipped the docs commits behind it. **Note what switched — the evidence, not the verdict word.** A classifier can confirm itself without any reported value changing, which is why "unchanged verdict" is not the thing to watch.
29. **"Green and armed" is not "merging" — and the difference is invisible from every signal a lane normally checks.** Checks answer *is this change correct*; `mergeStateStatus` answers *can it go in now*, and a lane that reads only the first concludes "shipping" about work that has stopped. **The tell is throughput, not status.**

  **Two corollaries.** (a) ~~**It does not self-heal.**~~ **RETRACTED 2026-07-29 13:23Z** — #1042 merged ~2h07m after entering `BEHIND`, falsifying a bound extrapolated from a 1h45m observation. **The retraction is itself the lesson: "still true at T" is not "true at every T".** (b) **`gh pr view` returns `UNKNOWN` for `mergeable`/`mergeStateStatus` on the first read** and often the true value on the second — but **not always**. Treat `UNKNOWN` as "not yet computed", never as evidence of mergeability.

30. **A liveness witness must fire strictly *inside* the window it witnesses, not merely fall within it.** **The general form: a guard that accepts an endpoint as evidence about an interval passes hardest exactly when the interval is most degenerate.** Corollary for consumers: a probe's verdict is evidence, not a verdict — when independent witnesses contradict it, override and say why. A genuinely stalled lane does not recover by waiting; that asymmetry is the cheapest available discriminator.

31. **A cleanup and its cause are separate deliverables, and shipping the cleanup is the moment the cause is most likely to be dropped.** **A row-level repair with the mechanism unshipped buys one sweep of relief, not a fix.** State the re-accrual rate and a dated falsifiable test against the next producing event.

  **Amended twice (2026-07-29 14:55Z, 15:56Z), in opposite directions** — "1 per sweep" under-fit, "at least 4" over-fit one batch. **The honest quantity is not a rate at all: it is `|rows the sweep handled|`, workload-dependent and unbounded above.** The meta-lesson: **when a number has been wrong once, the reflex is to move it the other way and re-assert it with equal confidence — replacing a wrong point estimate with another.** Prefer naming the *variable*. **A mechanism claim survives new data; a point estimate has to be retracted every time the data moves.**

  **Confirmed prospectively 2026-07-29 21:54Z, and this is the strongest form of support the finding can get.** The 20:31Z sweep cleared 9 named rows with each unassign individually verified; **2 re-accrued within the same hour**, on rows whose clean state had *also* been verified at creation. The producing lane wrote the correct disposition into its own report — *"a stopgap, not a resolution"* — **and then quoted a rate anyway** (*"roughly one-per-hour"*), which is the amendment's exact trap seen from the inside. **A lane can hold the right mechanism claim and still reach for a point estimate in the next clause.**

32. **Verify-after-write needs a clock, not just a re-read — an immediate confirmation read can be served stale and is indistinguishable from success.** Impediment #48 and memory `linear_save_issue_field_drops` establish *that* writes drop silently; this adds *when* to look. **Corollary: a count is not a verification.**

  **Amended 2026-07-29 17:53Z — there is a second, distinct failure with the same shape.** The orchestrator's `get_issue` check at 17:28:49Z was **correct when it ran** and false 131 seconds later. That is not a stale index: the write held, and *something else wrote afterwards*. **An immediate verify cannot distinguish "my write held" from "my write held briefly"** — so the fix is not only *verify later*, it is **verify after the last side-effect in your own sequence** (PR link, comment, attachment), not after the mutation you happen to care about.

  **Sharpened 2026-07-29 21:54Z by a controlled pair.** One hour, one method, opposite outcomes: 9 immediate verifies after unassigns all still held an hour later; 2 immediate verifies after creations were both false within the hour. **The verification was never the variable — what wrote next was.** So the diagnostic question is not "did my read confirm it" but "is anything else going to write to this row after me", and that is answerable *before* the write.

33. **A claim widens when it is restated, and the widening is invisible because every restatement still cites the original evidence.** **The drift is toward the remedy's framing**, because a report is written to justify acting, and the wider claim justifies acting harder. **The tell is a claim that has grown more emphatic across runs while its citation stayed identical.** **Detector:** state what observation would falsify *today's* wording, then check whether it would also have falsified the wording used when the evidence was gathered. If the answers differ, the claim has drifted. **Corollary: severity claims need their own evidence, separately from mechanism claims.**

34. **N confirming sightings of an effect do not locate its cause — and each sighting makes the unexamined attribution feel better supported.** (Earned 2026-07-29: eight consecutive runs recorded rows acquiring an assignee, each citing the previous run's framing that the orchestrator's `save_issue` was responsible. **Nobody had asked what else differed between a stamped row and an unstamped one.** One two-directional check — 5 rows, 3 stamped-with-PR-attachment, 2 unstamped-without, including a row a lane demonstrably wrote to that same hour and left clean — relocated the cause in a single pass, and it points at a Linear workspace setting no amount of repo-side work could have fixed.) **The trap is that repetition reads as accumulating evidence**: run 8 felt far more certain than run 2 while resting on exactly the same untested attribution. **Detector, cheap: before reporting a recurrence, name one property the affected rows share that the unaffected rows do not — and check it.** If you cannot state the contrast class, you have an effect, not a cause. **Corollary, and the reason this outranks a tidier lesson: a remedy that fails while correctly applied is the highest-value evidence available about the cause** — the orchestrator's failed workaround this run was worth more than the seven prior sightings combined, and it was free.

35. **A query's filter encodes a premise about the world, and nobody audits a premise that has never produced a visible failure.** (Earned 2026-07-29: `pull-work` selected work with `assignee:null` for months, encoding *"an assignee means a person has claimed this row."* Eight runs then investigated why rows kept acquiring an assignee — write path, then workspace setting — **and not one asked whether the field meant anything in the first place.** Christian, unprompted and in one sentence: *"I do not use linear or github at all. so any issues in linear or PRs in github with my name on them are yours."* **The filter had been reading a field that never carried the signal it was selecting on.**) **The trap is that a premise embedded in a filter is invisible in exactly the way a premise embedded in prose is not** — no line ever asserts it, so no review ever disputes it, and every downstream investigation inherits it as background. **Detector, cheap: for each predicate in a query you are debugging, say out loud what it assumes about who or what writes that field — then ask whether anyone has ever confirmed it with the writer.** **Corollary, and the reason this outranks finding 34 rather than repeating it:** 34 says N sightings do not locate a cause; **35 says the cause may not be in the mechanism at all, but in the question.** When every candidate remedy is out of reach — an unreachable setting, an unfixable write path — that is itself evidence you are attacking the wrong side of the predicate. **The reader is usually yours to change even when the writer is not.**

36. **A consumer that matches on a heading's *level* rather than its *text* is a channel that silently narrows as producers reformat.** (Earned 2026-07-29 21:54Z: step 2.6 extracts `^## Needs Christian`; `tb-orchestrator` nests its section as `### Needs Christian` under a per-run `## <Nth> run` heading. The consumer sees nothing and **reports the file as having no such section** — indistinguishable, from the reading side, from a producer that wrote nothing.) **The specific damage is that THR-826 was filed precisely to give these sections a consumer, and the consumer was reinstated in a form that cannot see one of the two producers.** **Detector: for every "no items found" result on a shared channel, confirm the *absence* against the raw file once, not just against your pattern.** The general rule is finding 19's sibling: **a scan reports its own pattern's reach, and a narrowed pattern and an empty world are the same observation.** Corollary: prefer matching the *text* with a permissive level (`^#{2,6} Needs Christian`), because producers own their document structure and will change it without telling consumers.

  **Re-derived and strengthened 2026-07-29 22:54Z, in the direction that matters most.** The same producer emits **`##`** in `orchestrator-2026-07-30.md` and **`###`** in `orchestrator-2026-07-29.md` — one day apart, no code change on either side. **So the level is not a fixed property of the producer to be accommodated once; it is a per-file coin flip.** This run's extraction *succeeded*, and that is the finding rather than a reprieve: **a passing consumer and a broken one were the same code reading two files.** Generalises past headings — **any consumer keyed on a producer's incidental formatting has a silent failure mode whose trigger is invisible from the reading side**, and intermittency makes it strictly harder to catch than a consistent break, because the successful hours look like evidence of correctness.

  **Generalised again 2026-07-29 23:54Z, and the generalisation is the payload: "formatting" was too narrow.** The near-miss this hour was not a heading at all but **file selection** — the producer wrote two reports for one day (`orchestrator-2026-07-30.md`, `-30b.md`), and the consumer's "newest per producer" step had to break a within-day tie it was never designed for; mtime and lexical order happened to agree. **Two consecutive hours, two different incidental properties, both correct by luck.** So the rule is not *match text not level* — it is **every point where a consumer infers structure from a producer's convention is a coupling the producer can change unilaterally, and each one fails silently in the same direction.** Detector, cheap and general: for each such inference, name the producer change that would break it, then check whether anything would tell you. **Two independent near-misses in two hours is the signature of a consumer with several such couplings, not one bug** — enumerate them rather than patching the one that surfaced.

**Owner of items below:** Christian. Everyone else's blockers go in Linear or `Docs/impediments.md`.
**Refresh cadence:** The hourly `keep-work-flowing-cc` scheduled task keeps this current (prunes resolved items, adds newly-surfaced Christian-owned ones); the `retrospective` skill still does the deep periodic rebuild.

## How this works

The retrospective stopped being the right channel for "Christian, please flip one switch." Christian-owned asks were buried in retro prose, and nothing surfaced them between retros. This file replaces that pattern.

Read order: top to bottom is blast radius. The top items break canonical workflow invariants right now; the bottom items are operational debt with workarounds in place.

For each item:
- **Fix** = the literal command, click path, or line of config that resolves it.
- **What breaks** = the named system or invariant currently degraded.
- **Source** = `Docs/impediments.md` entry numbers + occurrence count, so the cost is visible.

When an item resolves: delete it from this file, mark the corresponding impediment as resolved in the dashboard regen, note the close in the next retro under "What shipped." **No history retained here — `git log` + the retros are the audit trail.** The resolved log is pruned to the current day; anything older is recovered with `git log -p Design/user-actions.md`.

---

## Standing asks

**One open, standing on its safe default (not re-rung) for a tenth consecutive run — and this run supplies the observation that should close it next time.**

**Closing evidence, gathered 2026-07-30 00:54Z:** the host **stayed awake through the night.** Three hourly lanes fired at 00:01Z, 00:26Z and 00:53Z with **distinct** timestamps (not the same-second cluster that signals a wake burst), and the reaper — on a separate scheduler entirely — ran at 00:40Z. **Overnight throughput is happening without any switch being flipped**, which makes the preference question moot in practice rather than merely defaulted. Per finding 12 the ask was already resolved by silence; it is now *also* resolved by observation, from a different direction. **Retire it at the next refresh unless a second unexplained pause appears** — a new pause would be a new ask with new evidence, never this one reopened.

### 1. Should the host machine stay awake overnight? (informational ask — a safe default exists, now corroborated by observation)

- **What happened:** the machine slept ~04:40 → 15:22 local (02:40Z → 13:22Z). Every scheduled lane stopped and restarted together; roughly eleven hourly planning sweeps and eleven pickup attempts did not happen.
- **Evidence it is a host sleep, not a lane failure — and it is complete.** Four witnesses: three lanes sharing `lastRunAt` to the second at 13:22:16Z; the reaper, on a *different* scheduler entirely, stopping in the same eight minutes; zero commits in the window; and the reaper firing at 15:40:01 local exactly as predicted, so it was never refused on battery — it was simply not running. **The diagnosis is closed; only the preference question remains.**
- **Fix, if he wants overnight throughput:** keep the machine awake and **on mains power** — Windows Task Scheduler refuses the reaper on battery (`-2147020576`, memory `git_cleanup_automation`).
- **Safe default (finding 12):** if the pause was deliberate, silence closes this. **Nothing was lost or corrupted.** **Do not re-ring this doorbell**; a *second* unexplained multi-hour pause is a new ask with new evidence.
- **Not Christian's to fix, and not conflated with it:** the heartbeat probe's false `stalled` verdict on this same event is an agent-owned code defect, listed under *Open agent work*.

**Five questions are answered, closed, and must not be re-asked:**

1. **The five empty projects** — Christian, chat 2026-07-25 21:53: *leave them open; they are intake buckets that refill as the game iterates.*
2. **The GitHub Actions payment block** — cleared 2026-07-25 ~17:09Z and again 2026-07-28 17:02Z, both verified by re-run, and corroborated seven times since by independent mechanisms. **A recurrence is a new ask with new evidence, never this one reopened** (finding 12).
3. **THR-799 priority ordering** — Christian, quoted in THR-774/775, 2026-07-27: *the new encounter experience is still first priority.*
4. **Release of THR-821/820/777/778** — Christian, Discord 2026-07-27 14:38Z.
5. **The Linear "assign issue to PR author" switch** — Christian, Discord **2026-07-29 18:10:03Z**: *"can't find anything like that. I do not use linear or github at all. so any issues in linear or PRs in github with my name on them are yours."* **Closed by falsification, on the terms the ask itself set** — no substitute switch is to be sought, and **the second sentence is now a standing fact, not just an answer: an assignee of "Christian Spliid" carries no information and must never be read as a human claim.** Opened 17:53Z, closed 18:54Z — **one run, the shortest-lived ask in this file's history**, and the direct result of step 0 existing.

**One standing switch exists but is not an ask:** the orchestrator lane can be disabled on one word from Christian (live 2026-07-27 19:49Z). **Its default state is the intended one**, so silence resolves it.

**Three durable corrections survive closed asks and are kept as rules:**

- (a) A blanket `git stash` was the wrong remedy for home-tree dirt; the single-path form is correct. Parking his tool-permission edits was never necessary.
- (b) `$REATTACH_MAX_TRACKED_DIRTY = 0` governs the detached-HEAD reattach path and never evaluates while HEAD is on `main`. The conclusion held; its stated reason did not.
- (c) **Tracked dirt alone does not stall autosync.** The stalling class is the *intersection* of locally-modified **∩** changed-by-incoming-commits — proven by fast-forwarding cleanly with `.claude/settings*.json` dirty.

---

## Resolved 2026-07-30

**Fifth consecutive run at zero open Christian-owned asks; the 13:23Z ask stands on its safe default, un-rung for a tenth run — and now corroborated by observation rather than only by silence.** Everything that moved this hour was agent-side:

- **The three-hour visible-queue freeze BROKE — and the mechanism behind it did not.** Picker-visible **45 → 48** against a flat shelf of **49**; THR-855/856/858/859 all came clean. **Two facts, kept separate:** the backlog drained (cause unattributed — no lane claims it), and **the write-side workaround failed under its most favourable test yet.** The orchestrator applied the documented two-call form to THR-860, `get_issue`-verified the field absent at **00:32Z**, and the name was back by **00:54Z**. **Finding 34's corollary pays out again: a remedy that fails while correctly applied outranks any number of confirming sightings.** Three write-side remedies now dead; the reader-side re-scope is the only one standing, and it is what THR-845 is In Dev to do.
- **PR #1074 merged at 00:01:40Z — ~1h31m after arming, inside the ~2h07m band.** The trigger written down one hour in advance never came due, and nothing was touched by hand. **The method is the keeper, not the outcome:** dating the expiry beforehand removed the judgement call instead of deferring it. Second observation inside the band, none outside. THR-638 left `In Dev` with it — which is what made four runs of "advancing, correctly still open" right rather than a missed stall.
- **Finding 28 paid out a second time.** `deployedSha` **advanced** `955d0919…` → `2eb2cf63…` across a real merge, and the classifier still correctly skipped the docs commits behind it. **The confirmation is in the evidence moving, not the verdict word** — four runs of `skipped` at a frozen SHA proved nothing; two now at a moving one prove the switch works.
- **Finding 36's file-selection axis went three-deep in one day** (`-30.md` / `-30b.md` / `-30c.md`). mtime and lexical order still agree — **correct-by-luck for a third consecutive hour**, and the agreement is alphabetical rather than robust: `-30-2.md` or an undated reversion breaks it. Collision cause ticketed (THR-849); consumer-side half still not.
- **PR #1031 re-derived:** `OPEN` / `DIRTY` / `CONFLICTING`, `updatedAt` unmoved since 2026-07-28T19:31:25Z — **~29.4h idle.** One `gh pr close`.
- **The merge gate held for an eleventh consecutive independent check** (`startupFailureCount: 0`); the fleet heartbeat returned `ok` for a tenth, **this time across a genuinely awake overnight** — three lanes at distinct timestamps, so the wake-boundary defect is untriggered rather than merely quiet. The reaper ran 02:40:02 local with **branches 49 → 48**, a moving count that independently proves the log is live.
- **Discord returned a literal `[]` for a third consecutive hour** — nothing to classify, no receipt owed, cursor unmoved at `1532100414189867109`. **The `python -c` allowlist read in the skill is unusable in this sandbox for a second run running** and was again repaired by reading `access.json` directly. **Fail-closed holds only because the failure keeps getting noticed** — that is a skill fix owed, not an hourly workaround.
