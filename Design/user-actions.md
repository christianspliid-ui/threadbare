# User Action Required

**Last updated:** 2026-07-29 15:23 local, by the hourly `keep-work-flowing-cc` CC task.

**ONE OPEN CHRISTIAN-OWNED ASK — the host machine slept ~10h40m (04:40 → 15:22 local), so every lane was dark.** Home tree healthy: on `main`, 0 behind, 0 stranded. Deploy `skipped`-benign after a real publish. Merge gate healthy, with fresh auto-close corroboration. Nine of the ten scheduled lanes restarted together on wake; the tenth is one slot out and due.

## This run's measurements (2026-07-29 13:23 UTC)

**This run follows an 11h28m gap in the brief's own cadence (01:55Z → 13:23Z), so every delta below spans a night, not an hour.** Say so rather than reporting hour-shaped numbers over an eleven-hour window.

- **THE FINDING: the host machine slept from ~02:40Z to 13:22Z, and the shape of the evidence is what makes it a sleep rather than a fleet failure.** Three independent witnesses, none of which shares a mechanism with the others:
  1. **Synchronous restart.** `tb-opus-pickup`, `keep-work-flowing-cc` and `daily-backlog-grooming` all carry `lastRunAt: 2026-07-29T13:22:16.x` — **identical to the second**, across three different cron minutes (`0`, `45`, `7 9`). Three hourly/daily lanes cannot independently coincide to the second; that is a wake-time catch-up burst.
  2. **A second scheduler stopped too.** The reaper runs under **Windows Task Scheduler**, not the CC scheduler, and its log is a clean hourly march to `2026-07-29 04:40:01` local (= 02:40Z) and then nothing. Two unrelated schedulers failing in the same eight minutes is a host event.
  3. **Zero commits.** `origin/main` tip is `ef5ce719` at 02:32:34Z — no push in the window from any lane.
  - **Consistent prior:** memory `git_cleanup_automation` records Task Scheduler refusing with `-2147020576` on battery. A closed/unplugged laptop explains all three witnesses; nothing else does.
- **Step 2.7 (heartbeat) returned `stalled` and I overrode it — this is a probe false positive at the window boundary, not a stalled lane.** Verbatim: `{"verdict":"stalled","checked":8,"stalled":[{"taskId":"tb-orchestrator","cadence":"hourly","slotsBehind":10,"witness":"tb-opus-pickup"}]}`. **The witness clause is satisfied vacuously here.** Its purpose (per the skill: *"or every overnight shutdown reads as a broken fleet"*) is to prove siblings stayed alive *during* the gap — but `tb-opus-pickup`'s only qualifying `lastRunAt` is **13:22:16Z, the closing edge of the window**, one minute before this run, in the same wake burst as the subject's own siblings. The probe tests `witness.lastRunAt ∈ window`; the property it means to test is `witness fired strictly inside the window`. At a wake boundary those diverge, and every catch-up burst will now report the one lane that did not join it as stalled.
  - **`tb-orchestrator` is not behind in any actionable sense:** `nextRunAt` was `13:26:16Z` — three minutes after this run — i.e. its ordinary next slot. It simply did not join the burst.
  - **This is a genuine agent-owned defect in `scripts/check-scheduled-task-heartbeat.ts`** and is routed below, not filed (finding 23).
- **Queue: BACKED UP — 41 ready (1 Urgent / 1 High / 9 Medium / 30 Low), 1 In Dev.** Depth 40 → 41. Nothing stale: oldest `updatedAt` is 2026-07-24T07:59Z (THR-715), **~5.22 days**, inside `STALE_ISSUE_DAYS`=7.
  - **The +1 reconciles exactly against the composition** (finding 6): **THR-646** entered as a Low (Low 29 → 30) via `tb-orchestrator`'s 02:29Z T1 sweep; Urgent, High and Medium all held. One arrival, one bucket, zero departures.
  - **In Dev fell 2 → 1 and WIP=1 is satisfied again** — THR-661 completed (below), leaving only THR-838.
- **The `BEHIND` merge stall CLEARED, and the resolution amends last run's corollary.** PR **#1042** merged at **02:19:18Z** as `1a97468f` — **~2h07m** after entering `BEHIND` at 00:12:29Z, not indefinitely. Last run asserted *"it does not self-heal … the answer is indefinitely"*; that was over-stated from a 1h45m observation. **What I did not determine: whether it self-healed or a write-remit session nudged it.** The merge commit alone cannot distinguish the two, and I did not chase it. Correction recorded against finding 29(a) rather than left standing.
- **THR-661 closed itself, which is the best merge-gate evidence available.** `stateHistory` shows In Dev → **Done at 02:19:43.422Z** — **25 seconds** after the merge. That is `linear-autoclose.yml` firing end-to-end, the *exact* workflow Monday's billing block killed. **This is fresh corroboration, not last hour's carried claim** (which explicitly noted it had none).
- **Deploy (step 2.5): `skipped`, benign — but the SHA moved, and that is the news.** `{"verdict":"skipped","deployedSha":"1a97468f"}` — previously `b0798230` for three consecutive hours. `1a97468f` **is** the THR-661 code merge, so a real build ran and published between the two readings. **A second correct switch of the classifier** (finding 28): it deployed on the build-relevant commit and reverted to `skipped` for the docs commits after it.
- **Merge gate (step 2.5b): `healthy`**, `startupFailureCount: 0`, corroborated by CI history — the last six `ci.yml` runs executed steps, five `success` and one `cancelled` (the #1042 merge superseding its own head run). THR-842's Christian-owned half stays discharged.
- **The superseded report PR is unchanged and still wants closing.** PR **#1031** (orchestrator/THR-762) re-read: `DIRTY`, still exactly **1** file (`Docs/ops/orchestrator-2026-07-28.md`), untouched since 2026-07-28T19:31:25Z — now **~17.9h**. Auto-merge still armed against a conflicted branch, so it will never fire. **Zero unique content; one `gh pr close`.**
- **Assignee-invisibility: 19 named of 41.** Seventh consecutive sighting, logged as a count per the settled-carry rule. The mover is **THR-646**, promoted at 02:29Z and landing already stamped — the seventh such row in seven consecutive T1 sweeps. No row lost its name; no change in kind, mechanism, or rate. Remedy unchanged and still agent-owned, never Christian's (THR-608).
- **Sibling reports (step 2.6): none folded in, and one carry is now positively resolved.** `orchestrator-2026-07-29.md` (newest run 02:29Z) reads *"Nothing needs you this run."* — empty state, skipped per the rule. `backlog-grooming-2026-07-28.md` (**~30h**, still in-window) is now **resolved rather than merely falsified**: its one item asked whether WS5 would get per-family batch children, with the check *"if WS5 still has no children by tomorrow's grooming run"* — **THR-838 exists, is In Dev, and has merged three PRs**, so the check passes on its own terms. `weekly-hygiene-2026-07-26.md` is **~77h**, outside the 36h window.
  - **Last run predicted the 07-28 grooming report would age out before its successor was written.** It has not yet (30h of 36h) — but the successor is now overdue by its own schedule (07:15Z fire, missed in the sleep window), so the prediction lands next run rather than being wrong.
- **Discord: inbox genuinely empty.** The `after=` cursor returned **HTTP 200, 3 bytes** (`[]`) — the measured empty-case size, so a real nil, not a silent failure. `access.json` `allowFrom` read from the file first (single entry, `247984978283986954`), preserving fail-closed ordering. **Nothing from Christian across the whole eleven hours; no receipt owed; none sent.** Cursor unchanged at `1531722112186843226`.
- **Home tree** on `main`, **0 stranded**, **0 behind**, **2 tracked-dirty** (`.claude/settings*.json` — settled property, see correction (c) under *Standing asks*), **1 untracked** (Friday's retro draft).
- **Reaper silent since 04:40 local (02:40Z) — ~10.7h, past the 2h flag, and attributed rather than alarmed.** Last `SUMMARY:` **33** worktrees / **46** branches / **2** stashes / **0** needs-disposition (branches 44 → 46 across the night's merges). **The falsifiable test for next run:** the reaper fires at `:40` local, so by the next brief it must show a run at 15:40 local. If it does not, the sleep explanation is wrong for *this* lane and it becomes a real item — Task Scheduler refusing on battery (`-2147020576`) survives the machine waking.
- **This run's worktree was created outside `.claude/worktrees/`** (session scratchpad, absolute paths on every write; `rev-parse --show-toplevel` asserted after creation, and the home tree re-checked as still on `main` in the same command, per THR-797). **The home tree was touched by `git -C` reads, one `git worktree add`, three read-only `npm run` probes, and nothing else.**
- **Zero Linear writes.** Two board scans plus three `get_issue` reads, read-only throughout.

## Open agent work (not Christian's)

- **NEW, and the highest-value item this run: `check:task-heartbeat` mis-fires at a wake boundary.** `scripts/check-scheduled-task-heartbeat.ts` accepts any sibling whose `lastRunAt` falls **inside** the stall window as the liveness witness. After a host sleep, the entire fleet's `lastRunAt` clusters at the *closing edge* of that window (the catch-up burst), so the clause is satisfied by timestamps that prove the opposite of what it tests — and any lane that did not join the burst is reported `stalled`. Measured this run: `tb-orchestrator` flagged 10 slots behind with witness `tb-opus-pickup`, whose qualifying run was **13:22:16Z, 61 seconds before the probe**. **Fix shape:** require the witness to have fired strictly inside the window with a margin (e.g. `witness.lastRunAt < windowEnd − oneCadence`), or detect the burst directly (≥2 lanes sharing a `lastRunAt` to the second ⇒ host-wake, suppress). **The skill's own rationale is the spec** — the clause exists so *"every overnight shutdown"* does not read as a broken fleet, which is precisely what it did here. **Wants a ticket from a write-remit session; this read-mostly lane files nothing** (finding 23).
- **Armed PRs stalling at `BEHIND` — the instance cleared, the class stands, and last run's strongest claim is retracted.** #1042 merged at 02:19:18Z, **~2h07m** after entering `BEHIND` — so *"it does not self-heal … indefinitely"* was an over-read of a 1h45m observation and is corrected at finding 29(a). **What remains true and unweakened:** green-and-armed is not merging, and a lane reading only check status concludes "shipping" about stopped work. **What is now unknown and was previously asserted:** whether the un-nudged case resolves at all — #1042's clearing mechanism was not determined, so the un-nudged case still has **zero** clean observations. **THR-735 tracks the general form.** Immediate remedy when seen: `git merge origin/main` on the branch.
- **Assignee-invisibility: 19 of 41 rows carry a name**, so the executor's `assignee:null` query cannot see them — including the sole High (THR-655) and the sole Urgent (THR-842). **Two halves, both agent-owned:** (i) one `save_issue(assignee:null)` per existing row, and (ii) a fix in `tb-orchestrator`'s T1 promotion path, now observed stamping a row on **seven** consecutive sweeps (THR-757, THR-756, THR-681, THR-346, THR-347, THR-348, THR-646 at 02:29Z). Without (ii), (i) is re-accrued once per sweep — a rate measured across seven sweeps, not projected.
  - **THR-842's invisibility is currently cheap, and this run finally has fresh evidence for saying so.** Its Christian-owned half (the billing action) is discharged, corroborated **this run** by THR-661 auto-closing 25s after merge — the very workflow the block killed — rather than by carrying last hour's reading. An invisible row is a defect in the lane's reach whether or not this particular row still has work in it.
- **WIP=1 is satisfied again — the breach closed on its own.** THR-661 went Done at 02:19:43Z, leaving **THR-838 as the sole In Dev**. THR-838 still carries **no assignee**, which is the bookkeeping loose end, not a parked-work problem: it has merged three PRs (#1033, #1036, #1039) and reads as genuinely mid-flight. **Downgraded from a WIP breach to a naming gap.** Remedy still belongs to the pickup lane's Step 0. **No ticket** (finding 23).
- **A superseded report PR wants closing: #1031** (orchestrator/THR-762, `DIRTY`, untouched since 2026-07-28T19:31:25Z — now **~17.9h**). Its only file is `Docs/ops/orchestrator-2026-07-28.md`, rewritten by later merged runs; the promotion it reports already landed. Auto-merge is armed against a conflicted branch, so it will never fire on its own. **Verified superseded, not assumed** (memory: `dirty_pr_may_be_regressing_duplicate`). One `gh pr close`, zero content lost.
- **THR-835 is unreachable in `Idea`** — **carried, and the re-derivation this run is weaker than usual, which is worth stating:** both column scans (Ready for Dev, In Dev) confirm it is in neither; its `stateHistory` was again not re-read. **The promotion-lane evidence is also thinner this hour** — that lane ran once, not five times, because of the sleep window, so "a working lane that never reaches this column" rests on the prior seven sweeps rather than on fresh volume. Content is a live merge hazard (the sole required check runs at 80–90% of its 20-minute cap; runner variance turns correct PRs red). **A working promotion lane that never reaches a column is stronger evidence than a broken one** — `Idea` is unreachable by every automated lane (finding 19). Remedy belongs to a write-remit session. No deadline re-armed.
- **The orchestrator lane's stray-write defect is open and agent-owned.** `tb-orchestrator` wrote its 08:29Z report into the home tree as well as its own worktree, blocking a fast-forward. The durable fix is in that lane's write path (absolute paths, or `rev-parse --show-toplevel` asserted before any write — the workaround this task takes hourly). **Worth a ticket from a session with a write remit; this read-mostly lane files nothing** (finding 23).
- **`rescue/2026-07-17-detached-plans` is provably prunable — carried, NOT re-derived this run.** The correctly-scoped test (finding 25's corollary) was run at 01:55Z and held: branch at `053c867a`, `origin/main..<branch>` exactly **4**, all **five** plan-doc blobs `rev-parse`-identical to `origin/main`. **This run did not repeat it**, and finding 1 says to re-derive before carrying — so this carry is one run overdue and should be re-checked next run before being restated. One `git branch -D`, zero risk, wants a write-remit session. Branch count 44 → 46 across the night's merges is not evidence either way; the reaper deliberately never auto-deletes stale *unmerged* worktree branches, precisely this branch's class.
- **Friday's retro draft** (`Design/retros/retro-2026-07-24-draft.md`) is untracked and wants a `git add` in any docs PR. **Not a carry** — it is THR-798's step 2, with an owner and a Done-when.
- **Impediment #267 is used twice** (a `sandbox` row and a `tooling` row, filed the same day by different lanes); **#219 and #228 are duplicate rows for one defect** (Playwright MCP writing a bare-filename screenshot outside the repo). Both dispositions: fold at the next retro. **No ticket.**

## Durable findings carried forward

Rules earned by tickets that have since shipped. **Kept as rule sentences; every worked example is in `git log -p Design/user-actions.md`.**

1. **Before carrying an observation into its Nth run, re-derive it once.**
2. **A subsystem with no callers is invisible to every sweep that looks for wrong behaviour, because it has none.** Corollary: grep the *assignment* side, not the read side.
3. **A plan doc's past tense is a claim, not evidence.**
4. **Advisory gates that nobody reads are indistinguishable from gates that do not exist.**
5. **A test suite that agrees with the bug stays green forever** — and a pool-scoped guard is a suite that agrees with the bug one shelf over.
6. **Read composition, not depth** — and stop re-testing the favourable dip.
7. **A gate clearing is not the same as a decision arriving.** Surfacing a settled call for ratification is worse than not surfacing it. Corollary: verify a *favourable* outcome's cause as carefully as an unfavourable one.
8. **An empty Discord inbox is not evidence Christian is away.** Silence means only that he did not write.
9. **A startup-failure cluster is indistinguishable from quota exhaustion at the moment it happens.** The disambiguating evidence — *does a fresh run execute jobs?* — costs one run. Take it before escalating to a billing page.
10. **Deploy-observability must classify by paths, not by branch name or PR title.** — SHIPPED 2026-07-27 in THR-785.
11. **Mutex lines with reasons work.**
12. **An unanswered ask is not an unheard ask — do not re-ring the doorbell.** A standing ask carries a stated safe default, so silence resolves it rather than blocking on it.
13. **Declining to build the missing half is a shippable outcome, and it needs its reasoning recorded to survive.**
14. **A signal with two meanings is not a signal — and the repair is a notification path, never a new gate.**
15. **When a ticket asks "which of these fixes?", census the pipeline before answering** — the honest answer may be "none, and here is why". Validate the *sample*, not just the stage: a bug report carries an unstated premise that its subjects were eligible to exhibit the behaviour.
16. **A deliberately-parked issue and a broken auto-close are indistinguishable from the board.** Report a falsifiable test in both directions.
17. **Before carrying an ask into another run, check whether it was answered somewhere other than the channel it was asked on.**
18. **State a falsifiable test against the outcome, never against an activity proxy** — a proxy-worded test can pass while the thing it guards fails. State the test's enabling premise explicitly, or a benign world reads as its own failure case.
19. **A column-scoped queue query reports the lane's reach, not the board's contents — and from inside the query the difference is invisible.**
20. **When an ask names a *mechanism* rather than a decision, expect the answer to arrive on the channel he has, not the one the mechanism needs — and say so instead of letting him assume it landed.** (a) Prefer asks that are pure decisions. (b) State plainly what was *not* done, or "fixed" reads as *all of it is handled*.
21. **Re-derive a carry even when you expect it to hold** — the check has a wider aperture than the claim, so a *true* carry can still pay. Look at what the source shows you, not only the field you came to check.
22. **An item in the lane's column is not an item the lane can take — and "unblocked" is the wrong question to keep asking.** The cheap detector: read the comments, not just the fields. *Consequential* and *decidable-by-Christian* are independent axes; conflating them turns a plain-language inbox into a firehose.
23. **A ticket this task files is unpullable by construction, and the cost lands on someone else's run — so filing is never free.**
24. **A file's *state* is evidence; its *author* is a separate claim needing separate evidence.** Naming the wrong author makes an ask bigger and less trustworthy than it needs to be.
25. **A confirmed test licenses the claim it tested, not the remedy that motivated it — and the gap is widest exactly when the result is most satisfying.** Corollary (earned 2026-07-28 on the `rescue/` carry): **the mirror failure is a test whose aperture is wider than the claim, which can disconfirm something the claim never asserted.** Scope the test to the assertion — a whole-tree diff against a stale branch measures staleness, not whether its unique commits are merged.
26. **One instance locates a defect; the second instance with the same fingerprint is what turns it into a mechanism — and changes the remedy.** (Earned 2026-07-28 on assignee-invisibility: THR-757 at 20:30Z looked like one lane leaving one row stamped; THR-756 at 21:31Z, same lane, same T1 sweep, same signature, made it a per-slot behaviour.) The consequence is not rhetorical: a one-off wants a cleanup, a recurring one wants a fix *plus* a cleanup, and reporting the second sighting as merely "still true" would have carried the wrong remedy forward. **Re-derivation earns its keep here** (finding 1) — the check that confirms a carry is the same check that catches it changing kind.

27. **A timestamp is evidence only once its zone is verified, and a `Z` suffix is a claim like any other.** (Earned 2026-07-28: `tb-orchestrator` labels its report headers in *local* time with a `Z` suffix — `"First run — 00:31Z"` for a sweep Linear records at 22:30:28Z. Last run copied the header and published a two-hour error.) Prefer a machine-written field (`stateHistory`, `lastRunAt`, commit metadata) over a prose header; when only prose is available, say which it was. **The failure is silent by construction** — a wrong-but-plausible hour never contradicts anything, so nothing downstream ever flags it.

28. **A classifier observed only in its stable state has not been tested.** (Earned 2026-07-28 on the deploy probe: two hours of `skipped` across a moving tip proved stability; the hour it switched to `deployed` on a build-relevant change is the first evidence it *discriminates*.) Report a constant verdict as the weaker fact it is, and treat the first correct switch as the real confirmation.

29. **"Green and armed" is not "merging" — and the difference is invisible from every signal a lane normally checks.** (Earned 2026-07-29: PR #1042 held `MERGEABLE`, four SUCCESS checks and auto-merge armed while stuck at `mergeStateStatus: BEHIND`, waiting indefinitely.) Checks answer *is this change correct*; `mergeStateStatus` answers *can it go in now*, and a lane that reads only the first concludes "shipping" about work that has stopped. **The tell is throughput, not status** — this hour's brief noticed because a three-hour merge cadence went to zero, not because anything reported an error. Corollary: the failure rate scales with the *repo's* merge rate, so it worsens exactly when the lane is most productive.

  **Two corollaries earned 2026-07-29 by watching the same PR for a second hour.** (a) ~~**It does not self-heal.**~~ **RETRACTED 2026-07-29 13:23Z — #1042 merged at 02:19:18Z, ~2h07m after entering `BEHIND`.** The claim was extrapolated from a 1h45m observation and the very next reading falsified it. **The retraction is itself the lesson: "still true at T" is not "true at every T", and a duration claim needs an observation that ran to the end state.** What survives is narrower and still useful — the un-nudged case has *zero* clean observations, because #1042's clearing mechanism (self-heal vs. a session's `git merge`) was never determined, and the earlier instance was hand-cleared at 40 minutes. **Do not restate a bound until one instance is watched all the way to merge with the nudge ruled out.** (b) **`gh pr view` returns `UNKNOWN` for `mergeable`/`mergeStateStatus` on the first read** and the true value on the second (memory: `automerge_behind_strict_mode`). A single read would have reported a false all-clear on the exact defect being tracked — **always read merge state twice**, and treat the first `UNKNOWN` as "not yet computed", never as "nothing to see".

30. **A liveness witness must fire strictly *inside* the window it witnesses, not merely fall within it.** (Earned 2026-07-29 on `check:task-heartbeat`: after a ~10h40m host sleep the whole fleet's `lastRunAt` clustered at the window's closing edge in one wake-time catch-up burst, so the sibling clause — whose stated purpose is to stop overnight shutdowns reading as broken fleets — was satisfied by the very timestamps proving a shutdown, and reported the one lane that had not yet caught up as `stalled`.) **The general form: a guard that accepts an endpoint as evidence about an interval passes hardest exactly when the interval is most degenerate.** Corollary for consumers: a probe's verdict is evidence, not a verdict — when three independent witnesses (a second scheduler on a different mechanism, a to-the-second synchronous restart, zero commits) contradict it, override and say why.

**Owner of items below:** Christian. Everyone else's blockers go in Linear or `Docs/impediments.md`.
**Refresh cadence:** The hourly `keep-work-flowing-cc` scheduled task keeps this current (prunes resolved items, adds newly-surfaced Christian-owned ones); the `retrospective` skill still does the deep periodic rebuild.

## How this works

The retrospective stopped being the right channel for "Christian, please flip one switch." Christian-owned asks were buried in retro prose, and nothing surfaced them between retros. This file replaces that pattern.

Read order: top to bottom is blast radius. The top items break canonical workflow invariants right now; the bottom items are operational debt with workarounds in place.

For each item:
- **Fix** = the literal command, click path, or line of config that resolves it.
- **What breaks** = the named system or invariant currently degraded.
- **Source** = `Docs/impediments.md` entry numbers + occurrence count, so the cost is visible.

When an item resolves: delete it from this file, mark the corresponding impediment as resolved in the dashboard regen, note the close in the next retro under "What shipped." **No history retained here — `git log` + the retros are the audit trail.** The resolved log is pruned to the current day; anything older is recovered with `git log -p Design/user-actions.md`.

---

## Standing asks

**One open, raised 2026-07-29 13:23Z.**

### 1. Should the host machine stay awake overnight? (informational ask — a safe default exists)

- **What happened:** the machine slept ~04:40 → 15:22 local (02:40Z → 13:22Z). Every scheduled lane stopped and restarted together; roughly eleven hourly planning sweeps and eleven pickup attempts did not happen.
- **Evidence it is a host sleep, not a lane failure:** three lanes share `lastRunAt` to the second at 13:22:16Z (a wake-time catch-up burst); the reaper, which runs under a *different* scheduler entirely, stopped in the same eight minutes; zero commits landed in the window.
- **Fix, if he wants overnight throughput:** keep the machine awake and **on mains power** — Windows Task Scheduler refuses the reaper on battery (`-2147020576`, memory `git_cleanup_automation`).
- **Safe default (finding 12):** if the pause was deliberate, silence closes this. **Nothing is lost or corrupted** — the work simply did not occur, and the queue is exactly where it stood at 02:30Z. **Do not re-ring this doorbell hourly**; a *second* unexplained multi-hour pause is a new ask with new evidence, not this one repeated.
- **Not Christian's to fix, and not conflated with it:** the heartbeat probe's false `stalled` verdict on this same event is an agent-owned code defect, listed under *Open agent work*.

**Four questions are answered, closed, and must not be re-asked:**

1. **The five empty projects** — Christian, chat 2026-07-25 21:53: *leave them open; they are intake buckets that refill as the game iterates.*
2. **The GitHub Actions payment block** — cleared 2026-07-25 ~17:09Z and again 2026-07-28 17:02Z, both verified by re-run. **A recurrence is a new ask with new evidence, never this one reopened** (finding 12).
3. **THR-799 priority ordering** — Christian, quoted in THR-774/775, 2026-07-27: *the new encounter experience is still first priority.*
4. **Release of THR-821/820/777/778** — Christian, Discord 2026-07-27 14:38Z.

**One standing switch exists but is not an ask:** the orchestrator lane can be disabled on one word from Christian (live 2026-07-27 19:49Z). **Its default state is the intended one**, so silence resolves it. The lane's eleven-hour silence on 2026-07-27/28 did **not** re-open it — the switch's subject is whether he wants the lane at all, not whether it ran last night (finding 12).

**Three durable corrections survive closed asks and are kept as rules:**

- (a) A blanket `git stash` was the wrong remedy for home-tree dirt; the single-path form is correct. Parking his tool-permission edits was never necessary.
- (b) `$REATTACH_MAX_TRACKED_DIRTY = 0` governs the detached-HEAD reattach path and never evaluates while HEAD is on `main`. The conclusion held; its stated reason did not.
- (c) **Tracked dirt alone does not stall autosync.** The stalling class is the *intersection* of locally-modified **∩** changed-by-incoming-commits — proven by fast-forwarding cleanly with `.claude/settings*.json` dirty.

---

## Resolved 2026-07-29

**Nothing Christian-owned closed** — the one ask on the board was opened this run. What closed was agent-side, and two of the three are recorded because they *falsify* something this file previously asserted:

- **The `BEHIND` merge stall (PR #1042) cleared** at 02:19:18Z, ~2h07m in — **retracting** last run's "does not self-heal … indefinitely". Correction carried at finding 29(a).
- **WIP=1 breach closed** — THR-661 went Done at 02:19:43Z, leaving THR-838 as the sole In Dev. The remaining THR-838 issue is a missing assignee, not parked work.
- **The 07-28 grooming report's one carry is positively resolved** — it asked whether WS5 would get per-family batch children; THR-838 exists, is In Dev, and has merged three PRs, so its own stated check passes.

The line worth keeping in view: the GitHub billing block was cleared 2026-07-28 17:02Z, and **this run has the first direct proof since** — THR-661 auto-closed 25 seconds after its merge, which is exactly the workflow the block killed. That is what makes the closed ask checkable rather than merely asserted.
