# User Action Required

**Last updated:** 2026-07-26 15:54 local, by the hourly `keep-work-flowing-cc` CC task.

**✅ ZERO BLOCKING CHRISTIAN-OWNED ASKS.** The GitHub Actions account-payment block opened at 18:11 local is **closed** — verified by re-run, not assumed; see "Resolved this period". Both agent-side residues it left behind (the stranded THR-731 close, the vacuous merge gate) are now closed too, and the incident is recorded in `Docs/impediments.md`.

**✅ ANSWERED — the empty-projects question is closed and must not be re-asked.** See "Resolved this period". Christian's verdict, in chat 2026-07-25 21:53: leave the five projects open; they are intake buckets that refill as the game iterates.

**Open agent work (not Christian's), carried into the next executor session:**
- **Item 1 below** re-verified still stranded at the 13:54 run (`git cat-file -e origin/main:Docs/plans/2026-07-05-entity-visual-header.md` → *does not exist*; `rescue/2026-07-17-detached-plans` still present as a local-only branch).
- **Friday's weekly-retro write-up** (`Design/retros/retro-2026-07-24-draft.md`) is still untracked in the home tree and still the only copy anywhere — a docs PR lands it, same move as PR #768.
- **The Stale Claim Sweep workflow has never once succeeded — 88 runs, 88 failures, since 2026-06-13. Found at the 02:56 run, re-measured at every hourly run since (03:54 through **13:54 UTC**, twelve consecutive); wants a ticket this task must not file.** *(Confirmed unfiled again at the 13:54 run rather than carried: a Linear query for "Stale Claim Sweep" returns only THR-250 — the Done ticket that **created** the workflow — plus unrelated sweep tickets (THR-735, THR-574, THR-752), and no defect ticket for its failure. **Twelve runs of "wants a ticket" with no ticket is the finding rather than a footnote**: this task cannot file, so the ask needs an executor or retro session to pick it up from here, and each additional hour of restatement is evidence that a read-only surface cannot discharge a filing obligation no matter how often it repeats it.)* **Flat at 88 this run — newest fire still `2026-07-26T12:31:18Z`, i.e. the one the 12:54 run caught.** The prior run was the first in ten hours where the count moved (87 → 88), and that fire remains the freshest evidence; flatness here means only "has not fired since", never "recovered" — the job is twice-daily, so the next fire is due ~00:52Z. The twice-daily job (`.github/workflows/`, `scripts/stale-claim-sweep/index.ts`) is meant to release abandoned `In Dev` claims. **Measured, not inferred:** `gh run list --workflow="Stale Claim Sweep" --limit 100 --json conclusion` returns **88 × `failure` and zero successes**, oldest 2026-06-13T14:16:10Z. The job is *not* dying at startup — all steps run, and the failure is deterministic inside `Run stale-claim sweep`:
  ```
  Error [ERR_MODULE_NOT_FOUND]: Cannot find module
    '…/scripts/drift-scan/linear' imported from …/scripts/stale-claim-sweep/index.ts
  ```
  **One-line cause:** `index.ts:21` imports `"../drift-scan/linear"` with no extension. The target exists (`scripts/drift-scan/linear.ts`); Node's ESM resolver under `--experimental-strip-types` requires the explicit `.ts`. **Impact is low and that is exactly why it hid for six weeks** — with WIP=1 and a single executor lane, a genuinely stuck claim would have starved the board visibly, and none ever did; the sweep has simply been decorative. **Do not report this as an outage or a billing artifact** — it predates both, is reproducible on demand, and is unrelated to the 2026-07-25 Actions incidents that this file records twice. **Cross-check when filing:** this is a second, independent instance of the THR-755 gate-credibility class — a scheduled gate whose *red* went unread for 87 consecutive runs is the mirror of a gate whose *green* is vacuous, and the same "does anyone read this output?" question closes both. Technical verdict, agent-owned; nothing here is Christian's.

**This run's measurements (2026-07-26 13:54 UTC).** Home tree on `main`, **0 behind**, 0 stranded; the tracked `.claude/settings.local.json` permissions edit and the untracked `Design/retros/retro-2026-07-24-draft.md` both persist, both inert agent work. Queue **BACKED UP — 20 ready (1 High / 9 Medium / 10 Low), nothing stale, 0 In Dev, 0 In Design, 0 open PRs**. Fifth consecutive run above the `QUEUE_BACKED_UP_MIN` (15) threshold, and the fourth consecutive hour with an **empty pipe**. Reaper ran 15:40 local, 14 min before this one: **24** worktrees / **31** branches / 1 stash / **0 needs-disposition** — worktrees flat, branches *down* from 34. **This is the first reaper run after THR-797's guard shipped, and it is clean:** the 14:40 run emitted `WARN: worktree removal FAILED (in use?)` on `.claude/worktrees/youthful-goldwasser-fc2da8`; the 15:40 run classifies that same directory as `skipped orphan dir (recent activity)` with no WARN. That is post-fix behaviour observed on the live lane rather than inferred from the diff — the one form of evidence the pre-fix runs could not supply. Actions executing normally — CI and Linear Auto-Close `success` across the window. Production deploy checked ad hoc on the `main` tip (`32c2584a`): `state: success` but the single `Vercel` status at **13:16:35Z** reads **`"Canceled by Ignored Build Step"`** — the *absent-deploy* case, and correct here, since the merge touched `clean-stale-git.sh` plus the generated impediment dashboard and no `src/`. Last genuine build remains `11cb574b` at 12:33:53Z; the live site is current and unchanged. Discord inbox read via the step-0 cursor: `HTTP 200`, **3 bytes** (`[]`) — genuinely empty, cursor unchanged at `1530719628429623431`, no receipt owed, no ping sent (Needs-Christian list still empty, hash unchanged at the empty-state value). Last full rebuild was the 2026-06-23 retro.

**The 21 → 20 fall is a clean drain, and the three-hour sequence now spans the full composition argument rather than half of it.** Arithmetic: **−1** (THR-797 → **Done at 13:16:39Z**, PR #905), **+0 arrivals**. **21 − 1 + 0 = 20**, exact. The three most recent hours read: 22 → 21 (completion filing nothing), 21 → 21 (completion filing one deferral, exact substitution), 21 → 20 (completion filing nothing). **Three adjacent hours, three different count movements, one constant** — depth moved with what was *found*, never with how much was *done*. This is the strongest form the argument has had: it is no longer an assertion about what a count means, it is three consecutive measurements where the count and the work disagree in every combination. Composition is the reading; depth is not health information on its own.

**One High-priority item now (down from two — THR-797 shipped), and it is the biggest measured content gap on the board.** **THR-800** — `__DEBUG.validateTraitRefs()`, the sweep THR-786 shipped, finds **62 authored trait refs resolving to no trait definition** against 64 shipped static definitions. Every gate written against those 62 has been silently vacuous since authoring. It is the Done-when #2 "or each remaining ref is a filed defect" clause discharging honestly rather than being waved through, and it is **the second instance this week of the same shape as THR-779** (61 mortal-affinity templates with no draw path): authored content that the engine has no route to reach. Worth stating as a class, because two independent instances at 60-ish scale argues the authoring surface and the consuming surface have never been checked against each other systematically. **THR-797** (reaper can delete a live session's worktree) left this group by shipping — see "Resolved this period" — leaving THR-800 alone at High for the first time. **The trait story is now lopsided in a way worth naming:** its tooling half is complete (the detector exists, the predicate is unified, the template fields are declared) and its *content* half is untouched — 62 refs still resolve to nothing. Every remaining High-priority hour is being spent on a gap that measurement has already fully described. It is an agent-owned technical call; it is not Christian's.

**The sweep's own sharpest finding is worth recording because it is the week's recurring class:** **THR-798** — the 2026-07-24 `weekly-retro` run filed real, well-scoped tickets (THR-753, THR-754) **citing a report file that was never committed**. That is the THR-755/THR-754 gate-credibility class again from a third angle: not a vacuous green, not an unreachable bar, but *work that looks complete and leaves nothing behind to verify against*. Three instances in one week argues the class is structural rather than incidental.

**Live-site publishing re-checked ad hoc, and this hour it is again a genuine green rather than an absent deploy.** `commits/49d30f54/status` → `state: success`, single `Vercel` status at **11:33:20Z** reading **`"Deployment has completed"`** — 47 seconds after PR #901 merged, on a **code-bearing** merge (the trait `subcategory` fix touches `src/engine/`), so the build actually ran. **The contrast with last hour is the whole point of THR-785:** at 08:54 the same field read `state: success` while the description said `"Canceled by Ignored Build Step"` — an *absent* deploy of docs-only merges. Both surface identically at the `state` level and only the description distinguishes them, which is exactly the discrimination THR-785 exists to automate. Executor work to wire into this skill; not self-implementable from this task's two-file remit.

**Prior-run headline, retained for continuity: WS0 completed its design pass rather than stalling in it** — THR-773 went In Design → **Ready for Dev at 07:11:06Z** and THR-779 followed at 07:11:24Z, twenty-five minutes after the 06:46 pickup, with the buildable design merged as PR #890 (`docs/plan-ws0-engine-substrate`, CI `success` at 07:02) and intent-judge **Allow** on round 2. THR-773 is now the **only High-priority item in Ready for Dev** and sits at the top of the queue — the first claimable piece of the blessed rebuild, after six consecutive hours with no High-priority work anywhere in the ready lane. **The ready count rose 9 → 13 and the arithmetic is worth recording because a rising queue usually reads as a bad sign and here does not:** −1 shipped (THR-761), +2 design completions arriving as claimable work (THR-773, THR-779), +2 deferrals THR-761 filed rather than shipping over (THR-783 `onFailureEffects` has zero production readers, THR-784 the condition-duration UI reads the wrong side of the `has_trait` relationship), +1 new infrastructure ticket (THR-785). **Four of the five arrivals are the system working as designed**; none is a regression. **A production-deploy stoppage occurred and cleared inside this hour, and it is the first genuinely new failure class in days.** Production publishing from `main` had stopped; the only signal was a red **non-required** `Vercel` check, which the lane is *correctly* trained to walk past (`gh api .../branches/main/protection` → `{"contexts":["Test · Typecheck · Build"],"strict":true}` — Vercel is deliberately not the merge gate). It surfaced only because the THR-761 closeout (PR #892) read the error body. **Christian upgraded to Vercel Pro the same hour and the quota cause is gone — verified independently this run rather than taken from the ticket:** `gh api repos/.../commits/9977b196/status` returns `state: success` with `Vercel — "Deployment has completed"` at **07:39:29Z**, and Production deployment `5608309586` for sha `9977b196` was created 07:39:30Z, 45s after the merge. **The live site is current.** The remaining gap is observability, not tier, and is filed as **THR-785** (Medium, Infrastructure, Repo Health) — note its first Done-when names *this task* as the natural home for the signal ("read the latest `Production` deployment for `main` HEAD and surface a mismatch or failure as a `## Needs Christian` line"), which is executor work to wire into the skill, not something this run may self-implement. This run performed the check ad hoc and it was green. **The cost question is Christian's and is deliberately not an ask:** the automated lanes generate roughly one deployment every ~14 minutes, which on Pro bills rather than blocks; THR-785 scopes attribution out explicitly, and the briefing carries it as an FYI with a do-nothing recommendation until an invoice actually reads wrong — the same FYI-not-ask discipline the 2026-07-25 13:49 entry used for the previous cap. **The headline is the two In Design claims, not the queue depth** — THR-773 (06:46:01Z) and THR-779 (06:46:15Z) are the exact two items this file has carried as the open agent ask for five and six runs respectively, and they were picked up together. A five-hour pattern this file named as "ours to fix" is now being fixed; the correct next reading is whether WS0 reaches Ready for Dev rather than stalling in design. **Fourth consecutive substitution-free ship, but with a deliberate qualification.** THR-761 (aftermath conditions never expiring) shipped at 07:38 via PR #892 and *was* replaced — by two of its own deferrals (THR-783, THR-784), which is the THR-741 pattern of an hour ago rather than a clean drain. **The distinction that matters is not "did the count move" but "was the follow-up filed or swallowed"**: both defects were found during verification and written down instead of being patched around or ignored, which is the outcome this file has repeatedly said to reward. Composition, not depth, remains the reading. **What THR-761 fixed is worth stating plainly because it is player-visible:** applied conditions wrote their countdown to one field and `decayConditions` read another, so afflictions and blessings that should have worn off in days persisted indefinitely. Its two deferrals are the same class one layer out — THR-784 (a UI panel reads `ticksRemaining` off the trait node rather than the `has_trait` edge, so the displayed countdown never moves) and THR-783 (`onFailureEffects` has **zero** production readers, so five authored tavern step-failure consequences have never once fired). **High-priority work is back in Ready for Dev after six hours without any**, and it is THR-773 — the piece every other Nudge Model workstream depends on. Three High-priority items remain in Todo (THR-772 the Nudge Model epic, THR-764 the impediment-log parser gap, THR-655 the post-migration retro). The five-hour "housekeeping while the program with momentum offers nothing to claim" pattern is **closed**, not merely under repair: the claimable piece exists. Four Christian-facing decision gates are declared in the program (WS0 engine design incl. broken-state pacing + rulebook, the per-Reach fate metaphor table before the 40-image run, the encounter kill list, the first migrated batch for scoring); **the kill list is still not a real decision this hour** — 61 of its 65 entries remain orphan-KILLs pending THR-779's wire-or-delete verdict, and THR-779 is now *staged* rather than in design, so the gate is one execution away rather than one design away; the other three are not ready. **Surfacing it now would still be requesting ratification rather than a design call.** **No ask surfaced this run — seventeenth consecutive empty hour.** *(Per-run measurements that used to accrete at the end of this carried paragraph now live in the fresh block at the top of the file; only durable findings remain here.)* **One durable note is worth rescuing from the runs that wrote them:** Christian acted on the deploy block at ~07:39Z on 2026-07-25 without using the Discord channel, so an **empty Discord inbox is not evidence he is away** — it is evidence only that he did not write there, and a run must never read silence as absence. Stale Claim Sweep re-measured every run rather than carried: **87 runs, 87 × `failure`, zero successes**, oldest 2026-06-13T14:16:10Z, newest 2026-07-26T00:52:58Z — flat because the twice-daily job has not fired since, which is expected rather than reassuring.
**Owner of items below:** Christian. Everyone else's blockers go in Linear or `Docs/impediments.md`.
**Refresh cadence:** The hourly `keep-work-flowing-cc` scheduled task keeps this current (prunes resolved items, adds newly-surfaced Christian-owned ones); the `retrospective` skill still does the deep periodic rebuild. This is the slow-moving standing-asks list — the fresh-this-hour view is [`Design/briefing.md`](briefing.md).

## How this works

The retrospective stopped being the right channel for "Christian, please flip one switch." Christian-owned asks were buried in retro prose, and nothing surfaced them between retros. This file replaces that pattern.

Read order: top to bottom is blast radius. The top items break canonical workflow invariants right now; the bottom items are operational debt with workarounds in place.

For each item:
- **Fix** = the literal command, click path, or line of config that resolves it.
- **What breaks** = the named system or invariant currently degraded.
- **Source** = `Docs/impediments.md` entry numbers + occurrence count, so the cost is visible.

When an item resolves: delete it from this file, mark the corresponding impediment as resolved in the dashboard regen, note the close in the next retro under "What shipped." No history retained here — `git log` + the retros are the audit trail.

---

> **No Christian-owned ask is open.** The one item below is agent work that has simply not been picked up; it is listed for continuity, not as a request.

## 1. Land the last rescued plan doc onto `main` — near-resolved · WILL NOT SELF-HEAL

**Status:** Open (minor) · **largely resolved 2026-07-18 13:29.** The spec that actually mattered is now on `origin/main`: `Docs/plans/2026-07-17-pure-claude-code-migration.md` **and** its brainstorm companion are both landed, so the six migration tickets that reference it can read it from `main`. Only **one** rescued doc is still stranded on the `rescue/2026-07-17-detached-plans` branch — `Docs/plans/2026-07-05-entity-visual-header.md` — and its feature (THR-637, Entity Visual Header) already **shipped** (PR #564), so the doc is now a historical artifact, not a live dependency. Re-verified still stranded at the 2026-07-26 09:54 run (`git cat-file -e origin/main:Docs/plans/2026-07-05-entity-visual-header.md` → not present; the `rescue/2026-07-17-detached-plans` branch still exists locally); note the rescue branch exists **only as a local branch on this machine** — it was never pushed to origin, so the doc is one-machine-only (low stakes: the feature it specified already shipped). This item is a candidate to prune at the next full retro.
**Source:** 2026-06-23 retro + 2026-07-18 `keep-work-flowing-cc` freshness pings (retro E1); rescue verified complete 2026-07-18 11:25; migration spec confirmed on `origin/main` 2026-07-18 13:29

**Fix — a design session can do this; it is not a Christian-only task.**
```
git fetch
git checkout rescue/2026-07-17-detached-plans -- Docs/plans/2026-07-05-entity-visual-header.md
# commit onto a docs/* branch off current main, open a PR
```

**What breaks if not done.** Nothing live — the only remaining stranded doc backs an already-shipped feature. No data-loss risk (commits are branch-anchored), no active spec gap.

---

## Retracted this period

- **2026-07-25 15:16 — the "CI + deploy allowance exhausted" item, opened at 15:12 and retracted four minutes later. It was wrong.** The observation was sound and remains on the record: every workflow run from 12:27 UTC failed **at startup with zero jobs executed** (runs 30158014057 / 30157998373 — CI on `claude/thr-731-opposed-resolution`; run 30158008073 — Stale Claim Sweep on `main`), each `conclusion: failure` with `jobs.total_count: 0` in under a second, while CI on `main` at 12:12 UTC and a 17-minute run at 11:41 UTC had passed and the workflow files were unchanged across the boundary. Vercel reporting `Deployment rate limited — retry in 24 hours` in the same minute made account-level exhaustion look like the parsimonious explanation. **It was a transient GitHub Actions incident.** Actions resumed executing jobs by 13:15 UTC; this task's own briefing PR (#846) ran CI green and auto-merged at 13:15:47 UTC — a run that could not have happened under an exhausted quota. **No Christian action was ever required, and none was requested of him** — the retraction landed before the Discord ping fired, so the erroneous item reached `main` for roughly four minutes and reached Christian not at all. **The lesson, worth keeping:** a startup-failure cluster is indistinguishable from quota exhaustion at the moment it happens, and the disambiguating evidence (does a *fresh* run execute jobs?) costs one run to obtain. Take it before escalating to a billing page. The Vercel build cap is separate, real, self-clearing within 24 hours, and carried in the briefing as an FYI rather than an ask. **One residue is genuine agent work:** PR #845's required check was swallowed by the outage and no check run exists to satisfy branch protection — nothing re-triggers a startup-failed run on its own, so it needs a manual re-run before its armed auto-merge can fire. *Postscript 2026-07-25 16:14: that residue is closed — a CI run on `claude/thr-731-opposed-resolution` completed successfully at 13:30 UTC and the armed auto-merge fired at 13:48 UTC. Zero open PRs on the board.*

  **Postscript 2026-07-25 18:11 — the retraction still stands, and a genuinely different billing block has since been confirmed (item 1).** These are two separate events on the same day with the same signature, and conflating them would destroy the lesson. The 12:27–13:15 UTC cluster **was** a transient: Actions resumed unaided and PR #846 ran green at 13:15:47 UTC, which no exhausted quota permits. The cluster that began at **15:05 UTC** is a real account-payment block: GitHub's own annotation names it, and a fresh re-run at 16:10:02Z reproduced the failure in 6 seconds. **The lesson this entry recorded is exactly what made the second call correct** — "the disambiguating evidence (does a *fresh* run execute jobs?) costs one run to obtain; take it before escalating to a billing page." It was taken, and it escalated. Nothing here needs re-retracting.

## Resolved this period

- **2026-07-26 13:16 UTC — THR-797 shipped (PR #905): the hourly reaper can no longer delete a workspace a session is still working in, and the very next reaper run demonstrated the fix on live traffic.** Was one of two High-priority items at the 12:54 run, filed 08:42 by the THR-780 closeout and closed under five hours later. Verified rather than assumed: `status: Done`, `completedAt: 2026-07-26T13:16:39.078Z`; PR #905 merged 13:16Z with CI and both `Linear Auto-Close` runs `success` at 13:16:19–20Z. **What landed:** the liveness guard now reads *working-tree file mtimes*, not only git-admin-dir mtimes. That is the precise shape of the hazard — a session doing closeout (Linear, docs, impediments) writes files for hours without touching git, so its worktree aged past `WORKTREE_MIN_IDLE_MINUTES` while demonstrably alive. Plus a post-merge grace window and a refusal to delete a branch whose unregistered worktree corpse is still active. **Three findings worth outliving this entry.** (1) *The failure mode it closes is the nastiest kind: silent and misattributing.* `.claude/worktrees/` sits **inside** the home tree's working copy, so deleting a live worktree's `.git` file makes git's discovery walk *up* and find the home tree — after which `git checkout -b` creates the branch **in the home tree with no error**, `pwd` still reads the worktree path, and a live session can author commits onto `main`. There is no error message anywhere in that sequence; the tell is an `Edit` failing "File does not exist" on a file `grep` just matched. A fault that presents as an unrelated tool bug will be misdiagnosed every time it is not written down first. (2) *The fix was verified by the lane rather than by a test, and that is the stronger evidence here.* The 14:40 run — pre-fix — emitted `WARN: worktree removal FAILED (in use?)` on `youthful-goldwasser-fc2da8`. The 15:40 run — first post-fix — classified the same directory as `skipped orphan dir (recent activity)`, no WARN. A guard against a *timing* hazard cannot be proven by a unit test that controls the timing; one clean pass over real concurrent sessions is worth more than the test that shipped with it. (3) *The obvious fix was unavailable and the ticket said so instead of pretending otherwise.* Relocating `.claude/worktrees/` out from under the home tree would remove the hazard by construction, but the harness owns that path. Hardening the guard is the second-best fix, correctly chosen and correctly labelled as such — which is what stops a future session from "discovering" the relocation idea and burning a run on it. _Prunable at the next full retro; keep the silent-misattribution note and the harness-owns-the-path constraint._

- **2026-07-26 12:33 UTC — THR-801 shipped (PR #903): the phantom template trait-gate API is real API at last, and the ticket shipped it deliberately unused.** Was one of three High-priority items at the 11:54 run, filed 10:29 by THR-786 and closed two hours later. Verified rather than assumed: `status: Done`, `completedAt: 2026-07-26T12:33:28.114Z`, state history Ready for Dev (10:29) → In Dev (12:03) → Done (12:33) with no reopen, **30 minutes of build**; PR #903 merged 12:33Z, and the `main`-tip Vercel status reads `"Deployment has completed"` at 12:33:53Z on a code-bearing merge. **What landed:** `requiredTraits`/`blockedByTraits` declared on `UnifiedActionTemplate` — an additive type-only change to a file with **278 importers**, which is why THR-786 refused to make it in passing and why the plan's hard sequencing reserved it for a single deliberate editor. `encounterFilterPipeline.ts` had been *reading* both fields for as long as the gate has existed; the engine could evaluate a lock that no template could fit a door to. **Three findings worth outliving this entry.** (1) *The ticket exists because a plan doc asserted a change had happened and it had not.* The WS0 plan recorded these fields as "fixed via WS0 PR"; WS0 shipped without them, and the trait-unification session noticed only because declaring the field cleared pre-existing `tsc -b` baseline errors. **A plan doc's past tense is a claim, not evidence** — the disconfirming check here was a two-second grep, and nothing in the pipeline had run it for the three days the assertion sat there. (2) *It shipped the capability with zero authored uses, and said so in the ticket rather than adding a token example.* Done-when #3 explicitly permits "authoring is deferred to WS5, and the contract test remains the only exercise" — so `__DEBUG.validateTraitRefs()`'s `perSurface.template_gate` still reads **0**, correctly and by design. A ticket that reports "real and unused" beats one that manufactures a demonstration to look complete, and the WS5 content pass is where the doors actually get built. (3) *It filed its own deferral mid-flight rather than at closeout* — THR-802 (a KPI path resolving trait refs by raw node-id compare, bypassing the shared THR-786 resolver) was written down at 12:11, 22 minutes before the merge, and was itself surfaced by the same baseline-error delta. **The second production reader of this gate was invisible until a type declaration made it complain.** _Prunable at the next full retro; keep the plan-doc-past-tense-is-a-claim rule and the real-but-unused precedent._

- **2026-07-26 11:32 UTC — THR-787 shipped (PR #901): agents' earned reputations have never once appeared in the prose describing them, and the fix is the first clean drain this file has recorded.** Was one of four High-priority items at the 10:54 run. Verified rather than assumed: `status: Done`, `completedAt: 2026-07-26T11:32:53.871Z`, state history Ready for Dev (07:57) → In Dev (11:02) → Done (11:32) with no reopen; PR #901 merged with CI `success` at 11:32:34Z and `Linear Auto-Close` `success` at 11:32:35Z. **What landed:** three sites read `properties.category === 'reputation'` where every trait definition in the game stores `subcategory` — `proseEnrichment.ts:370`, `cultureGenerator.ts:498`, `trait-modifiers.ts:22`. The consequence was total and silent: **reputation-derived titles have never rendered in enriched prose, not once since the feature was authored.** Aligned on `subcategory` with a regression test proving a title actually renders through `enrichProse` for an agent carrying a reputation trait. **Three findings worth outliving this entry.** (1) *It is the same class as THR-800, one layer in, and the pair should be read together.* THR-800 is 62 authored trait refs pointing at definitions that do not exist; this is three engine reads pointing at a field name that does not exist. Both are **content and code disagreeing about a vocabulary**, both fail silently, and both were invisible until something measured the two sides against each other. Two instances in two days at opposite ends of the same pipeline is a strong argument that the trait authoring surface and the trait consuming surface have never been checked against each other systematically — which is the case THR-800 should be scoped to settle, not merely to patch. (2) *A test suite that agrees with the bug stays green forever.* The dead read survived because nothing ever asserted the observable outcome — that a title appears — only that the code ran. Regression tests on trait behaviour must anchor on **shipped definitions**, not on fixtures written alongside the code under test, or they will re-certify the next instance of this exact drift. (3) *This is the first hour in this file's window where a completion filed nothing.* Six prior hours each produced deferrals, and each time this file argued that a flat-or-rising count was composition rather than decay. This hour is the control case: nothing to file, so the count simply fell 22 → 21. _Prunable at the next full retro; keep the vocabulary-mismatch class note and the fixture-agrees-with-the-bug warning._

- **2026-07-26 10:55 UTC — THR-786 shipped (PR #899): the traits floor landed in the same hour it was designed, and it filed two defects on its way out rather than shipping over them.** Was the joint-top High item at the 10:54 run, and the item the *previous* run carried as gated. Verified rather than assumed: `status: Done`, `completedAt: 2026-07-26T10:55:17.029Z`, state history In Design (07:57) → Ready for Dev (08:45) → In Dev (10:02) → Done (10:55) with no reopen; PR #899 merged 10:54:55Z with CI `success` at 10:38:20Z and both `Linear Auto-Close` runs `success` at 10:54:58Z. **What landed:** six trait-predicate vocabularies — encounter filter pipeline, effect predicates, ambition graphConditions, ambition snapshot eligibility, spell prerequisites, item-granted keys — unified on one `TraitPredicate` + `resolveTraitPredicate` + a `SimulationRuntime`-owned `TraitRefIndex` (ref → **set** of node ids, ANY-match, preserving today's union semantics by construction), with unchanged-behavior contract suites written first as the kill criterion. **Design-to-merge inside one hour, which is the fastest full cycle in this file's window** — and worth recording *because* the previous run flagged it as gated: the gate was real, it cleared at 09:03Z, and the work then took 52 minutes. **Two findings worth outliving this entry.** (1) *The sweep it shipped is the deliverable that matters more than the refactor.* `__DEBUG.validateTraitRefs()` immediately found **62 of ~126 authored trait refs resolving to no definition at all** (→ THR-800), which means the unification did not merely tidy six call sites, it made a previously-invisible class *measurable*. A refactor that ends with a working detector is worth more than one that ends with fewer lines. (2) *It contradicted its own plan doc and said so.* The plan recorded the template gate fields (`requiredTraits`/`blockedByTraits`) as landing in the WS0 PR; WS0 shipped 92 minutes earlier without them, and the session filed THR-801 rather than quietly adding them out of scope or assuming the plan was right. **Consequence for the queue:** 20 → 22, with both arrivals being this ticket's own deferrals — the THR-741/THR-761 pattern for a third time, and the same reading: a count that rises because defects were written down beats a flat count that hides them. _Prunable at the next full retro; keep the detector-over-refactor note and the two-instances-of-60-orphans class (THR-800 + THR-779)._

- **2026-07-26 09:22 UTC — THR-773 shipped (PR #896): the Nudge Model has an engine, and the blessed rebuild is off paper for the first time.** Was the standing open-agent item at the 08:54 run, carried in `In Dev` there. Verified rather than assumed: PR #896 (`feat/thr-773-nudge-model-ws0`) merged 09:22Z, CI and both `Linear Auto-Close` runs `success` at 09:22:42–43Z, and THR-773 no longer appears in Ready for Dev or In Dev. **What landed — WS0 engine substrate:** the nudge schema, forecast band riders, the broken state, and the restore verb. **This is the entry that closes a five-run arc, and the arc is the lesson.** This file carried "a blessed program with nothing claimable in it" for five consecutive hours (03:54 → 07:54), correctly framed it as an agent scheduling gap rather than a Christian ask, and declined to escalate it every single time. The sequence once a session engaged: In Design 06:46 → Ready for Dev 07:11 (25 min) → In Dev 08:53 → **shipped 09:22** (29 min of build). **Five hours waiting, ~54 minutes of actual work end to end.** That ratio is the argument, and it now has its full shape rather than half of it: escalating a scheduling gap to the user would have converted zero decisions into one interruption. **What this does and does not unblock:** the four downstream workstreams (THR-774 WS1 skills, THR-775 WS2 interface, THR-777 WS4 images, THR-778 WS5 content) were gated on WS0 *shipping* and are now genuinely unblocked — they stay in Todo pending pickup, which is ordinary sequencing and deliberately not an item. The plan's four Christian-facing gates are untouched by this and none is ready; the encounter kill list in particular still waits on THR-779's wire-or-delete verdict, so surfacing it would still be requesting ratification rather than asking a design question. _Prunable at the next full retro; keep the five-hours-waiting / one-hour-doing ratio, which now supersedes the weaker 07:11 version of the same note._

- **2026-07-26 09:03 UTC — PR #895's armed-and-`BEHIND` stall cleared itself; the "wait one refresh" precedent is now three-for-three and the threshold is confirmed rather than proposed.** Was the newest open agent item, opened one refresh ago with an explicit re-flag condition: *"if PR #895 is still `BEHIND` at the 11:54 run, it has outlived the threshold and wants `git merge origin/main && git push` from an executor session."* **It never triggered.** `docs/plan-traits-trigger` merged at **09:03Z** (local 11:03) after `156167a1 Merge branch 'main' into docs/plan-traits-trigger` — i.e. the owning session refreshed its own branch and the armed merge fired unaided, the identical resolution to the 2026-07-25 21:16 and 2026-07-26 00:21 instances. **Why this third instance is worth its own entry rather than a postscript:** the previous two established the *rule* (an armed auto-merge at `BEHIND` is stalled, not dead) and the *threshold* (wait one refresh while a session still owns the branch). This one is the first time the threshold was written down **in advance as a falsifiable re-flag condition** and then held — which is a different quality of evidence than two retrospective observations. The rule is now safe to apply without hedging. **The stakes were also higher here and that is recorded deliberately:** unlike the prior two, this PR *gated a High-priority item* (THR-786's description said "wait for it on main before pickup"), so patience was being spent against a visibly blocked top-of-queue. It still paid: gate cleared 09:03, pickup 10:02, shipped 10:54. **What does not survive:** the caution about its `Test · Typecheck · Build` reporting **SKIPPED** stands unchanged as a general rule — a docs-only skip satisfying branch protection is the THR-755 shape and is never evidence that anything was tested — but it was correct-and-harmless in this instance. _Prunable at the next full retro; keep the three-instance threshold and the falsifiable-re-flag-condition note._

- **2026-07-26 07:11 UTC — WS0 exited design into Ready for Dev in twenty-five minutes; the staging gap this file carried for five hours is closed, not merely attended.** Was the standing open-agent item, and the *only* re-flag condition the previous run left behind ("re-flag here only if WS0 leaves In Design without reaching Ready for Dev") resolved the good way. Verified rather than assumed: THR-773 `In Design → Ready for Dev` at **07:11:06Z**, THR-779 at **07:11:24Z**, both now `assignee: null` and claimable; the buildable design merged as **PR #890** (`docs/plan-ws0-engine-substrate`, CI `success` 07:02:23Z); the ticket records intent-judge **Allow** on round 2. **THR-773 is now the only High-priority item in Ready for Dev**, after six consecutive hours with none. **What to keep — the pickup-to-handoff interval is the signal, not the handoff itself.** This file spent five runs reporting a blessed program that offered nothing claimable, correctly framed it as *ours, not Christian's*, and correctly declined to escalate it to him across all five. The gap then closed in twenty-five minutes once a session actually started. That ratio — five hours waiting, twenty-five minutes doing — is the argument for treating "nobody has picked this up" as a scheduling problem rather than a capacity problem, and against escalating scheduling problems to the user, where there is nothing for him to decide. **This entry *is* a completion of the staging ask; it is not a completion of WS0**, which is designed and staged but unshipped. The four downstream workstreams stay dependent on it shipping — ordinary sequencing, deliberately not carried as an item. _Prunable at the next full retro; keep the five-hours-waiting / twenty-five-minutes-doing ratio and the don't-escalate-a-scheduling-gap note._ **Postscript 2026-07-26 09:22 UTC — WS0 shipped; the "this is staging, not completion" caveat this entry set has been discharged. Superseded by the 09:22 entry above, which carries the complete five-hours-waiting / one-hour-doing ratio rather than this entry's partial version; prunable in full.**

- **2026-07-26 ~07:39 UTC — production publishing had silently stopped; Christian cleared it with a plan upgrade before any brief could ask him, and the residual gap is observability rather than tier.** Never reached this file as an ask, and that is the correct outcome rather than a miss. **What happened:** production deploys from `main` were failing, and the only evidence was a red **non-required** `Vercel` check — deliberately excluded from branch protection (`{"contexts":["Test · Typecheck · Build"],"strict":true}`) because Vercel is not a correctness gate. **The lane is trained to walk past exactly that signal**, so the stoppage surfaced only because the THR-761 closeout (PR #892) happened to read the error body. Christian upgraded to **Vercel Pro** the same hour. **Verified independently this run rather than taken from the ticket:** `commits/9977b196/status` → `state: success`, `Vercel — "Deployment has completed"` at **07:39:29Z**; Production deployment `5608309586` created 07:39:30Z, **45 seconds after the merge**. The live site is current and the quota cause is gone. **Three findings worth outliving this entry.** (1) *A non-required check is, by construction, a signal nobody is obliged to read* — and that is the right design for a merge gate but the wrong design for an outage alarm. The fix is a **notification path, not a new gate**; making Vercel required would couple every merge to a third-party service, and THR-785 records that prohibition explicitly. (2) *CLAUDE.md's Definition of Done currently treats a successful push as proof of deployment* ("Just ensure the push succeeded"), which was false for the whole window — `main` can advance while the deployed artifact does not, for any cause: bad env var, provider incident, a build that passes `vite build` locally and fails on their builder, or billing. The tier upgrade fixed one instance, not the class. (3) *The durable fix lands in this task, and this run could not self-implement it.* THR-785's first Done-when names `keep-work-flowing-cc` as the natural home — read the latest Production deployment for `main` HEAD and surface a mismatch as a `## Needs Christian` line — which is a skill-file change, i.e. executor work outside this task's two-file remit. **This run performed the check ad hoc and it was green**; the wiring is filed, not done. **The cost half is deliberately an FYI and not an ask:** the automated lanes generate ~1 deployment every ~14 minutes, which on Pro bills rather than blocks; THR-785 scopes attribution out, and the briefing carries a do-nothing-until-the-invoice-looks-wrong recommendation — the same discipline the 2026-07-25 13:49 cap entry used. _Prunable once THR-785 ships; keep the notification-not-a-gate rule and the push-≠-deploy correction._

- **2026-07-26 07:38 UTC — THR-761 shipped (PR #892): marks the god leaves on people now actually fade, and the ticket filed two deferrals rather than shipping over them.** Was the top-of-queue medium at the 08:54 run. **What landed:** applied conditions wrote their countdown to one field while `decayConditions` read another, so afflictions and blessings intended to wear off in days persisted for the rest of the run — player-visible, and quietly corrosive to every balance reading taken while it was live. **The part worth keeping is the pair of deferrals, both of which are the same class one layer further out.** THR-784: a UI panel reads `ticksRemaining` off the **trait node** rather than the `has_trait` **edge**, so the displayed countdown never moves even now that the underlying one does — a fix that makes the engine correct while leaving the player looking at a frozen number is only half a fix, and the session said so instead of declaring victory. THR-783: `onFailureEffects` has **zero production readers**, so five authored tavern step-failure consequences have never fired once — authored content that the engine has no path to reach, which is the same "promised consequence goes nowhere" shape the Nudge Model migration audit found at scale. **Consequence for the queue:** ready count 9 → 13, of which two arrivals are this ticket's own deferrals. As with THR-741 an hour earlier, **a count that rises because defects were written down is a healthier board than a flat count that hides them** — composition is the reading, and this file records both halves for exactly that reason. _Prunable at the next full retro; keep the half-a-fix note, since the engine-correct/UI-stale split will recur on the next duration surface._

- **2026-07-26 06:36 UTC — THR-760 shipped (PR #887): the always-loaded instruction file lost 29% of its bulk with no rule removed, and the deferral it was blocking landed nine minutes later.** Was the top-of-queue medium at the 07:54 run. Verified rather than assumed: `status: Done`, `completedAt: 2026-07-26T06:36:08.840Z`, state history Ready for Dev → In Dev (06:02) → Done (06:36) with no reopen; PR #887 merged with CI `success`. **What landed:** a deduplication/relocation pass, not a rule-deletion pass — the scheduled-task registry moved to `Docs/ops/scheduled-tasks-registry.md`; **Design Governance's authority flipped to `Docs/canon/design-governance.md`** with CLAUDE.md keeping only the two gates that decide whether a design may exist (never present a non-compliant design; the Three-Pillar Rule); the Domain Skills table collapsed to load-order policy, since the harness already lists every skill per session; the Debug Bridge section became a pointer at `src/debug-bridge.d.ts` plus the `tick(n)` block; and the global `~/.claude/CLAUDE.md` was retired (absence confirmed this run). **Measured, and the miss is recorded honestly:** 90,292 → **63,743 chars** against a stated target of ≤58,000 — a real 29% cut (~6.6k tokens off every one of ~24 automated sessions/day) that reached roughly 110% of target rather than 100%. **Two findings worth outliving this entry.** (1) *The authority flip is the load-bearing half, not the byte count.* A rule living in two places is a rule that drifts, and the failure mode is silent: an agent reads the stale copy and is confidently wrong. Relocating each rule to exactly one home and leaving a pointer is what makes the reduction safe; a pass that merely deleted 26k chars would have been strictly worse than doing nothing. **Corollary for whoever edits these files next: do not reverse the flip** — `Docs/canon/design-governance.md` is now authoritative and CLAUDE.md is the pointer. (2) *The nine-minute follow-on is the discipline paying out.* PR #888 landed the `node_modules` health-probe correction that the THR-780 session found and **deliberately declined to apply**, because CLAUDE.md was mid-rewrite and parallel edits to an always-loaded file are a known way to lose work. It logged the correction and waited; the door opened at 06:36 and it walked in at 06:45. Deferring a correct fix for a stated structural reason, and then actually landing it, is the behaviour this log exists to reinforce — the alternative (fix it now, lose it in the rewrite) looks more decisive and produces nothing. _Prunable at the next full retro; keep the authority-flip warning and the deferral-payout note._

- **2026-07-26 05:35 UTC — THR-755 shipped (PR #885): six gates stopped contradicting their own verdicts, and the gate-credibility class is now closed from both ends.** Was one of the three top-of-queue mediums at the 06:54 run, and the last survivor of the trio Friday's retro filed. Verified rather than assumed: `status: Done`, `completedAt: 2026-07-26T05:35:38Z`, state history Ready for Dev → In Dev (05:02) → Done (05:35) with no reopen; PR #885 merged with CI `success`. **What landed — six rows, each verified against the named example that motivated it** (the THR-686 lesson applied rather than restated): `lint:plan-doc` scoping moved off filename shape to the frontmatter opt-out plus a directory skip for `.intent-proposals/`; `check:wiki-freshness:blocking` now prints `PASS (exempt)` as its header instead of a `FAIL` banner above an exit-0; `check:process`'s `deferred-ref` rule stopped flagging `scripts/drift-scan/index.ts`'s own report-template literal as a permanent ERROR; the interface-map generator's failure text stopped reading as an architectural leak when the real cause is a mis-declared `mechanism.symbols` (it now says to grep the symbol at the site first); the duplicate `import type { UnifiedActionTemplate }` in `src/data/social-encounter-content.ts` — a hard PARSE_ERROR for vite-node's oxc transformer — was deleted; and CLAUDE.md's pre-commit step 3b was amended to run *last*, after the closeout doc and impediment edits it is supposed to judge. **Two findings worth outliving this entry.** (1) *Read it as a pair with THR-754, not as a sixth housekeeping ticket.* Both were filed by the 07-24 retro, both landed within ninety minutes, and they close one class from opposite ends: THR-754 fixed a gate whose **bar was unreachable**, THR-755 fixed gates whose **green was vacuous or whose red was false**. The downstream failure is identical — output nobody can trust — and the retro's own framing ("hook noise is teaching agents to bypass hooks") had two documented `--no-verify` instances behind it, each justified at the time by a false positive. A gate that cries wolf is not neutral; it actively trains the bypass. (2) *The step-3b ordering fix is the sneaky one.* It is not a wrong check, it is a correctly-implemented check placed one step too early: because it compares against `HEAD` and `Docs/impediments.md` is itself a generated-artifact source, every run that logged a closeout impediment passed the freshness gate and then shipped a stale dashboard. Ordering defects in gate sequences produce green runs and wrong artifacts, which is the hardest variant of this class to notice. _Prunable at the next full retro; keep the pair framing and the ordering-defect note._

- **2026-07-26 04:10 UTC — THR-754 shipped (PRs #882/#883): the browser-verify evidence contract is satisfiable from every lane, and the impediment log paid for itself in under an hour.** Was one of the four top-of-queue mediums at the 05:54 run, and the oldest of them — filed by Friday's weekly retro on 07-24 after the class had already recurred five times. Verified rather than assumed: `status: Done`, `completedAt: 2026-07-26T04:10:44Z`, state history Ready for Dev → In Dev (04:02) → Done (04:10) with no reopen; both PRs merged with CI `success`. **What landed:** the Definition of Done's "screenshot at 1920×1080" clause stays **binary**, but CLAUDE.md now names *which capture discharges it* per lane — attended sessions unchanged (Playwright for DOM, Claude-in-Chrome for WebGL); unattended runs use Playwright MCP with an **absolute** `filename` inside the session worktree; portal-rendered overlays are proven by accessibility-tree capture plus a closed-state screenshot, because CDP omits the late-painted portal layer entirely; and any deviation is recorded in the commit body as `Browser-verify substitution: <route> — <reason>`, mirroring the existing `Browser-verify exempt:` convention. The honest-dimensions rule is the load-bearing half: if a capture is not actually 1920×1080 you **state its real size** and evidence the viewport contract separately by DOM assertion from the Browser pane, which resizes correctly even when it cannot composite. **Two findings worth outliving this entry.** (1) *The eight-minute close is the point.* This ticket sat four days as retro-2026-07-20's never-landed Experiment #4, then closed in eight minutes — because the THR-780 session an hour earlier had hit the same wall, measured it (Claude-in-Chrome is pinned at 1430×989 and **silently ignores `resize_window`**), and written it up instead of shrugging. The diagnostic work was already banked; the ticket only had to spend it. That is the impediment log working as designed, and it is the cleanest instance of it on record. (2) *A gate that cannot be satisfied is a gate that gets quietly stepped around* — agents had been rediscovering per-lane workarounds one at a time for four days, and the honest ones were reporting a dimension they had not measured. This is the same class THR-755 exists to close, approached from the opposite side: THR-755 fixes gates whose **green is vacuous**, THR-754 fixed a gate whose **bar was unreachable**. Both produce the same failure — output nobody can trust. _Prunable at the next full retro; keep the honest-dimensions rule and the Chrome-ignores-resize measurement._

- **2026-07-26 03:38 UTC — THR-780 shipped (PR #880): the dev-build double-apply is fixed, and the *method* it was verified with is the part worth keeping.** Was one of the four top-of-queue mediums at the 04:54 run, filed 02:22 UTC as THR-741's own deferral and closed 76 minutes later. Verified rather than assumed: `status: Done`, `completedAt: 2026-07-26T03:38:59Z`, state history Ready for Dev → In Dev (03:02) → Done (03:38) with no reopen; PR #880 merged with CI `success`. **What landed:** `GameView.applyAftermathReactionForAgent` called a graph-mutating function *inside* a `setGameState` updater, and `<StrictMode>` double-invokes updaters in development — so an accumulating tally moved `+0.70` where the authored delta is `0.35`. Dev-only and unreachable in a real playthrough, but dev is where balance is played and where every `__DEBUG` measurement is taken, so every reading of a graph-mutating aftermath effect had been 2× off. **Three findings worth outliving this entry.** (1) *The A/B is what made the console claim honest*: four duplicate-key errors appeared during verification and read as fallout from the change; splitting the run into tick-only and reaction-only phases narrowed it to the reaction, and re-running the identical script against a **stashed pre-fix build** produced exactly the same 2 errors — identical either side, therefore pre-existing (root cause filed as **THR-781**). Without that stash-and-compare the session would have shipped a false self-report. (2) *Neither browser MCP's console reader could be trusted for counts* — Claude-in-Chrome's returned "no console errors" for a run that definitely produced them, and tracking did not survive a reload; patching `console.error`/`console.warn` into arrays as the first action after load, then slicing at phase boundaries, is what made the numbers comparable. The clinching evidence was the signature itself: pre-fix `delta: 2` / 2 applied-traces / 4 effect-traces vs post-fix `1` / `1` / `2`, same seed, same agent, same reaction. (3) *The 1920×1080 screenshot was unobtainable from **both** surfaces* — the Browser pane resized correctly but cannot composite frames when never displayed, and Claude-in-Chrome screenshots fine but is **pinned at 1430×989 and silently ignores `resize_window`**, which is the newer half of the finding: a screenshot taken there looks valid and is quietly off-contract. Logged as impediments; durable fix is **THR-754**, already in the queue. **One correction to the repo's own instructions came out of it and was deliberately not applied:** CLAUDE.md documents the home-tree `node_modules` health probe as checking `.bin/esbuild.exe`, a path that does not exist in this install (the `.bin` holds shell wrappers; the `.exe` lives in `@esbuild/win32-x64`), so following the doc literally prints the THR-753 **wipe signature on a healthy tree**. The reaper itself is correct — `clean-stale-git.sh:90` accepts either form — only the prose narrowed it. The executor logged the correction to `Docs/impediments.md` and left CLAUDE.md untouched because it is mid-rewrite under **THR-760**, which is the right call: parallel edits to an always-loaded file are a known hazard, and a damage detector that cries wolf is the one nobody believes on the day the wipe is real. _Prunable at the next full retro; keep the stash-and-compare attribution method, the console-patching recipe, and the Chrome-ignores-resize finding._

- **2026-07-26 02:43 UTC — THR-741 shipped (PR #878): the god's destructive workings now produce authored aftermath, and the work filed its own defect rather than shipping over it.** Was one of the five top-of-queue mediums at the 03:54 run. Verified rather than assumed: PR #878 merged 02:43:22Z, both `Linear Auto-Close` runs at 02:43:24Z report `success`, and THR-741 no longer appears in Ready for Dev. **What landed:** aftermath reactions for the player-castable destructive cards — the harsh end of the divine repertoire previously resolved with a number changing offstage, and now the touched people and places answer in authored prose. **The part worth keeping is the deferral, not the feature.** During browser verification the executor observed one reaction applying its graph mutations **twice**, and filed it as THR-780 (Medium, `Deferral`/`UI`/`Engine`) instead of shipping past it or silently guarding it. The cause is React StrictMode's deliberate double-invocation in the dev build — **development-only, unreachable in a real playthrough** — which means the tooling that exists to expose double-apply bugs did exactly its job, and the correct response was to record the finding rather than suppress the symptom. **Consequence for the queue:** the board's ready count did not move (12 → 12), because this shipped item was replaced by its own deferral in the same hour. A flat queue count concealing a full substitution is a reminder that depth alone is a weak health signal — composition is the reading that matters, which is why this file records both. _Prunable at the next full retro; keep the StrictMode-is-not-a-bug note, since the same double-apply signature will recur on the next aftermath surface._

- **2026-07-26 01:24 UTC — the Nudge Model migration audit shipped (THR-776, PR #875); the program's first workstream is done and the kill-list gate is now half-formed.** Was the sole top-of-queue item across the last four runs, and the only High-priority work on the board. Verified rather than assumed: `status: Done`, `completedAt: 2026-07-26T01:24:48Z`, state history Ready for Dev → In Dev (01:02) → Done (01:24) with no reopen; PR #875 merged 01:24:26Z with CI `success`. **Result:** `Docs/audits/2026-07-26-nudge-migration-audit.md` classifies **561 distinct mortal-drawable templates — 282 KEEP / 214 REWRITE / 65 KILL** — with a per-family table, S/M/L nudge-hand estimates, scene-image tags, and a six-batch WS5 order. **Three findings worth outliving this entry.** (1) *The corpus was over-counted at scope time*: the ticket's "531 + 28 + 30 = 589" double-counts the branching set, which already lives inside `UNIFIED_ACTION_TEMPLATES`; the true figure is 561, and the audit reproduced 531 exactly before subtracting, which is what makes the correction trustworthy rather than a different measurement. (2) *Exposure is ~10:1 concentrated*: a headless seed-42 medium run resolved 93 actions across 166 ticks and 368 agents, **91% of them `encounter.*`**, while the 166 guild templates drew **zero** — one seed is weak evidence of *never* but strong evidence of *low share*, and it is what orders WS5 by what a player actually sees rather than by template count. (3) *61 templates are unreachable by all three draw paths*, concentrated in the guild senior/elite reward tier — filed as THR-779 and **deliberately not treated as "delete"**, because several look like content whose wiring was never done. **Why the kill list still is not Christian's decision:** 61 of the 65 KILLs are that orphan set, so 94% of the list resolves to a technical wire-or-delete verdict the agents owe first. Surfacing it now would be requesting ratification, not a design call — it goes to him once THR-779 lands and the list is honest. _Prunable at the next full retro; keep the double-count correction and the exposure ratio._

- **2026-07-26 00:21 UTC — PR #871's armed-and-`BEHIND` stall cleared itself too; the "wait one refresh" precedent is now two-for-two.** Was the carried agent item from the 01:54 run, deliberately left for the owning session rather than nudged from here. It needed no nudge: PR #871 merged at **00:21Z** (local 02:21) and **THR-737 went Done at 00:21:50Z** on the line-anchored close — verified via `get_issue` (`status: Done`, `completedAt: 2026-07-26T00:21:50.455Z`, state history In Dev → Done with no reopen), not inferred from the merge. `gh pr list --state open` now returns `[]` and `list_issues state:"In Dev"` returns empty. **Why this one is worth keeping rather than folding into the 07-25 21:16 entry:** that entry proposed the rule from a single instance, and a single instance is indistinguishable from luck. This is the second occurrence in ~27 hours, same signature — all checks green, `Test · Typecheck · Build` **SUCCESS** (not SKIPPED), auto-merge armed, then an unrelated merge lands ahead and strict protection parks it at `BEHIND` — and the same resolution: **the owning session refreshed its own branch and the armed merge fired unaided.** The rule stands as written: an armed auto-merge at `BEHIND` is *stalled, not dead*; wait one refresh before escalating whenever a session still owns the branch, and reach in only after that session has ended. What the pair adds to `reference_automerge_behind_strict_mode` is the escalation *threshold* — one refresh, not zero. _Prunable at the next full retro; keep the two-instance threshold._

- **2026-07-25 21:16 UTC — PR #859's armed-and-`BEHIND` stall cleared itself; the board is at zero open PRs and zero In Dev.** Was the carried agent item opened one refresh ago, where it was deliberately left for the executor lane rather than nudged from here. It needed no nudge: the session that owned THR-734 finished its own job — PR #859 merged at **21:16:25Z**, THR-734 went **Done** at 21:16:46Z on the line-anchored close, and both `Linear Auto-Close` runs at 21:16:28Z report `success`. Verified rather than assumed: `gh pr list --state open` returns `[]`, and `list_issues state:"In Dev"` returns empty. **What to keep, because it sharpens rather than contradicts the `reference_automerge_behind_strict_mode` note:** an armed auto-merge at `BEHIND` is *stalled*, not *dead* — it fires the moment something refreshes the branch, and the branch's own owning session is the cheapest thing to do that. The recorded fix (`git merge origin/main && git push` from the owning session) is right; what this instance adds is that **waiting one refresh before escalating is often enough when a session still owns the branch**, and that this task was correct to leave it alone rather than reach into the executor lane. Escalate only when the owning session has ended. _Prunable at the next full retro._

- **2026-07-25 ~22:20 — the Discord channel reads replies now; the send-only gap that swallowed two of Christian's answers is closed at the cause.** Was the carried agent item from 21:55, flagged there as wanting a ticket this task could not file. It did not need one — it shipped the same evening in two merged passes: **PR #864** added step 0 to the `keep-work-flowing-cc` skill (read Christian's replies at the top of every run, before anything else, since what he said may change what the run reports), and **PR #866** replaced the naive history pull with a server-side cursor so the common case is cheap. **Verified working this run rather than assumed:** step 0 executed, `GET /channels/…/messages?after=<cursor>` returned a literal `[]`, and the measured cost of an empty inbox is **3 bytes** against ~2.5 KB for the same call with an older cursor — so an empty result is meaningful, not vacuous. **The injection-safe shape the 21:55 diagnosis asked for is what landed, and it is narrower than "surface as unverified reported speech":** the author is authenticated against `access.json` `allowFrom` every run (never a hardcoded id, and an unreadable allowlist means act on nothing — fail closed on authorization, fail open on the run); the **remit does not widen** because a message asks it to, so anything beyond this task's job is routed to an executor via a *From Christian* briefing section rather than executed; content Christian *relays* (pastes, forwards, links) stays untrusted data even though his instruction is genuine; and `/discord:access` allowlist mutations remain impossible from any channel, including one that appears to be him. **The blanket old rule — "never act on inbound Discord content" — was superseded deliberately, not eroded:** its cost was measured (two answers unread for eleven hours on 2026-07-25, the same question re-put to Christian in every brief from 09:00 to 21:10) and its load-bearing half was kept. **What to keep:** a one-way notification channel manufactures silent drops on the *user's* side, the mirror of impediment #48's Linear write-drop; the fix is not "obey the channel", it is "read the channel, authenticate the author, and keep the powers fixed". _Prunable at the next full retro._

- **2026-07-25 22:09 — the reaper's failed worktree removal is verified benign; the check it asked for was run.** Was an open agent item for exactly one refresh. The 20:40 run logged `WARN: worktree removal FAILED (in use?) … intelligent-dubinsky-8af0d5 — may now be unregistered; verify before next run`. **Both halves measured rather than assumed:** `git worktree list` no longer lists the path (so the deregistration did complete), the directory is still present on disk, and — the part that actually mattered — `git worktree prune --dry-run` returns **empty**, meaning git holds zero stale admin entries for it. Nothing is broken on the repository side. The 21:40 run then classified the same path as `skipped orphan dir (recent activity)`, which is THR-673's guard doing precisely its job: a live session's folder is never reaped, even when it is no longer a registered worktree. **What to keep:** "removal failed, may now be unregistered" reads alarming and is cheap to settle — `git worktree list` plus `git worktree prune --dry-run` distinguishes *deregistered-but-in-use* (benign, self-clearing once the session ends) from *stale-admin-entry* (needs a prune). The residual exposure is confined to whatever session still holds that folder: its own git operations would fail, but nothing outside it is affected, and no other tree can be damaged. _Prunable at the next full retro._

- **2026-07-25 21:53 — Christian answered the empty-projects question: leave them open. Standing preference closed; do not re-ask.** Carried in this file since the 2026-07-25 grooming pass and surfaced in every hourly brief since. Five projects sit in an active lifecycle status with every issue terminal — **Agent Coordination Protocol**, **Repo Health**, **Marketing Site**, **Small manual tweaks** (all **Now**) and **Procedural Hex Vignettes** (**Next**). The tidy-up rule says close a project whose issues are all terminal; the grooming pass declined to, reasoning that each declares itself a permanent intake bucket, and recommended leaving them open. **Christian's verdict, verbatim in chat:** *"the five empty generic projects are fine. leave them for later they will show up again as we iterate."* That confirms the grooming pass's reasoning and adds the load-bearing bit: emptiness here is a *phase*, not an end state — these buckets refill as the game iterates, so "active status, zero open issues" correctly reads as "no current work in this lane", not "finished lane". **Consequence for future automation:** the daily grooming pass and the weekly hygiene routine must treat these five as a known-good exception and stop surfacing them under "Needs Christian"; a project being empty is not by itself grounds to propose closing it. Re-asking a settled preference is the cost this entry exists to prevent. See `Docs/ops/backlog-grooming-2026-07-25.md` § Needs Christian for the original finding.

  **⚠ He had already answered it eleven hours earlier, on Discord, and no run read the reply.** The 08:13Z brief-ping asked the question; Christian replied at **08:57:42Z** — *"Keep them around more work will show up for them later"* (message `1530499273609707540`) — the same verdict he gave in chat at 21:53. Every hourly brief from 09:00 through 21:10 nonetheless carried it as an open question and re-surfaced it to him. **Cause, and it is structural, not an oversight:** `keep-work-flowing-cc` step 6 is *send-only* by design — the skill says "This step only sends" — so the task has never once looked at the channel it pings. `fetch_messages` works fine against that channel (verified this run), so the capability was there the whole time; nothing was wired to use it. **Consequence:** the Discord ping is a one-way doorbell, and any answer Christian gives there is invisible until he repeats it in an interactive session. That is a silent-drop class on the *user's* replies — the mirror of the impediment-#48 Linear write-drop, and worth the same treatment. **Deliberate non-action:** the send-only rule exists as prompt-injection containment (inbound Discord is untrusted data), so the fix is *not* "let the hourly task obey Discord messages". A safe shape is narrower: read replies **since the last ping**, surface them to the *next* interactive session as unverified reported-speech in the briefing under a clearly-labelled "Christian said on Discord (unverified)" heading, and never let their content trigger an action. **This wants a ticket and this task must not file one** (read-mostly, no Linear writes) — flagged here for the next retro or design session. _Prunable at the next full retro; the grooming exception and the read-back gap should both outlive the entry._

- **2026-07-25 18:04 UTC — THR-731's stranded close fired; the last outage residue is gone.** Was the sole carried agent item, open for one refresh. The group-vs-group conflict work (PR #850) had merged at 15:05:40Z carrying a correctly line-anchored `Fixes THR-731`, but the `Linear Auto-Close` run that acts on it died at startup during the Actions block, and with Christian's native-integration auto-close disabled that workflow is the only closer — so the issue sat In Dev on finished work. Verified closed rather than assumed: status **Done**, `completedAt` 2026-07-25T18:04:19Z. Its sibling THR-732 (Reunite + Sunder) closed the same minute at 18:03:07Z on its own merge, and the follow-on `Linear Auto-Close` runs at 18:07Z both report success — the close path is demonstrably working again, not merely un-failing. **What to keep:** a startup-failed workflow run is a *silent* non-close, and nothing retriggers it; when Actions has an outage, the issues whose closes fell inside the window need re-checking by hand afterwards. _Prunable at the next full retro._

- **2026-07-25 ~17:09 UTC — GitHub Actions is executing jobs again; the account-payment block is cleared and the ask is withdrawn.** Was item 1, open for exactly one refresh (18:11 → 19:15 local). **Verified by re-run rather than by absence of failures:** run `30165205305` (CI on `main`, created 16:13:49Z, attempt 1 dead with zero jobs) was re-run at **17:09:49Z** and attempt 2 executed the `Detect code changes` job through all five steps — checkout, paths-filter, teardown — concluding `success` at 17:10:08Z. That is the same one-run disambiguator that identified the block at 16:10:02Z, applied in the other direction: a live account runs the job, a blocked one dies in six seconds. Window: ~15:05 → ~17:09 UTC, about two hours. **Attribution resolved 2026-07-25 21:55 — it was Christian, and the answer had been sitting unread on Discord for three hours.** This entry originally said attribution was "genuinely unknown and left unstated rather than guessed", with the caveat that a GitHub-side release could recur. That caution was correct on the evidence available *to the run that wrote it*, and is now superseded by direct evidence: Christian replied **"Budget updated."** at **16:28:52Z** (message `1530612815310884865`), 14 minutes after the 16:14Z ask, and Actions resumed executing jobs at ~17:09Z — a ~40-minute lag entirely consistent with a billing clear propagating. **He fixed it.** The recurrence caveat is accordingly *withdrawn as stated* — this instance was account-side and is closed by payment, not by a transient release — though the general point survives untouched: the one-run re-test remains the correct disambiguator, and it is what identified both the block and its clearing. **Why the answer went unread is the same defect recorded under the empty-projects entry above:** the Discord ping is send-only, so this task pinged him, he answered, and no run ever looked. Two separate Christian replies (08:57Z, 16:28Z) sat unread on the same day. **The vacuous-merge-gate exposure this item carried closed with it, and the damage audit came back clean.** During the window a skipped required check counted as satisfied, so any PR *could* have reached `main` untested. Measured rather than assumed: `git diff --name-only cb725de8..origin/main` returns **three files, all documentation** — `Design/briefing.md`, `Design/user-actions.md`, `Docs/impediments.md`, 45 insertions / 19 deletions — with zero hits under `src/`, `scripts/`, `.github/` or `package*`. The last code-bearing merge, PR #850 at 15:05:40Z, landed on a genuine `Test · Typecheck · Build :: SUCCESS`. **No untested code reached `main`.** The executor posture recorded under this item ("do not claim new implementation work") is lifted. **What to keep:** the exhibit for THR-755 stands on its own merits — a required check that reports satisfied *by skipping* is a gate whose output does not match its verdict, and PR #851 merging two seconds after its own CI run failed is the dated instance. So does the interim rule, now promoted to a permanent one: confirm `Test · Typecheck · Build` reports **SUCCESS, not SKIPPED**, before merging code. **Source:** `Docs/impediments.md` 2026-07-25 (environment / Blocked) — third occurrence, after **#91** (2026-04-25) and **#136** (2026-05-12); #136 proposed a detector for this exact annotation string in May and it was never built, which is why the third occurrence cost a full diagnostic run again. Durable fix remains filed as **THR-768** (Urgent, Idea, Continuous Improvement) and is *not* closed by this — the block clearing does not build the detector. *Prunable at the next full retro; keep THR-768 open.*

- **2026-07-25 ~13:49 UTC — the site-host publishing cap cleared itself; the live site is current.** Carried in the 15:16 briefing as an FYI (never an ask). Verified rather than assumed: production deployment `5601732619` reports `state: success` at 13:49 UTC against sha `7386cfcb`, byte-identical to current `origin/main`, and a sweep of today's twelve deployments finds no error status at all. The gap it left is visible and harmless — the merges of #847 (13:19) and #848 (13:29) produced no deployment of their own, and the 13:48 merge of #845 published all of it in one build. **Nothing was owed to Christian at any point**; upgrading the plan was the only lever and waiting was free, as recommended. *Prunable at the next full retro.*

- **2026-07-25 ~06:39 — Christian flipped the native Linear auto-close setting; the phantom-Done class is closed at the cause.** Was item 3, carried since the 05:16 run. The repo half (THR-738, PR #823) had already made our own `linear-autoclose.yml` line-anchored, so a closing keyword buried in prose is inert. The remaining vectors — an issue swept to Done off its auto-generated branch name or a bare id in a PR title — came from Linear's own GitHub integration, which no agent can configure. Christian disabled it and confirmed in chat. **Correction to this file's own record, kept because it cost a wrong click path across ~20 briefs:** the toggle is **not** under Settings → Integrations → GitHub. It is **per-team**: Settings → Team **Threadbare** → **Workflows & automations** → **Pull request and commit automations** → "when PRs are merged" set to no action. PR *linking* by branch name / magic word has no toggle and remains on, which is the intended end state — links without auto-move. THR-738 returned to **Ready for Dev**; its one remaining Done-when was the live scratch-issue verification (two docs-only PRs proving the bare-id PR leaves the issue open and the standalone-line PR closes it), which is executor work, not Christian's. **Postscript 2026-07-25 ~11:08 — that verification ran and passed, so the fix is proven end-to-end rather than assumed.** THR-738 went back to In Dev at 09:02 and the executor built the test against scratch issue THR-765: PR #836 (negative control) fired all three vectors at once — bare id in the branch name, bare id in the title, and the close keyword inside a prose sentence — and on merge left THR-765 **untouched** (single `Idea` state-history entry, `completedAt` null), while still attaching the PR, which is the intended links-without-auto-move end state; PR #837 (positive control), carrying the keyword alone on its own line, closed it at 09:08:49 UTC. The contrast is what makes the test conclusive: our own workflow was provably incapable of closing THR-765 either way, so the issue surviving PR #836 isolates **Christian's native-integration switch** as genuinely disabled, not merely unexercised. Record: `Docs/ops/2026-07-25-autoclose-vector-verification.md`. **Closed out 2026-07-25 11:14 — THR-738 is Done** (closeout record PR #838, merged 11:13; state history shows In Dev → Done at 09:14 UTC with no reopen since). The ticket's four Done-whens are all satisfied: both live controls passed, the line-anchored contract is written into CLAUDE.md and the `pull-work` skill, and the Linear-side settings change is recorded in this file. Nothing about this class remains open. _Prunable at the next full retro._
- **2026-07-25 ~08:41 — the home tree is level with the server again; the four-day freeze is over.** Was item 1, carried across ~20 consecutive runs and peaking at **122 commits behind**. Measured this run rather than assumed: `HEAD` = `main` = `origin/main` = `9ef767c4`, `main..origin/main` = 0, `origin/main..HEAD` = 0. The tracked `.claude/settings.local.json` permissions edit (4 insertions / 1 deletion) is **still present** and no longer blocking — the fast-forward went through around it, since no incoming commit touches that file. One untracked file remains, `Design/retros/retro-2026-07-24-draft.md` (Friday's weekly-retro report), still the only copy anywhere and still **agent work** to land via a docs PR — same move as PR #768. The inert `home-tree-recovery` stash from the 07-23 park is still on the stack; the reaper tracks it (1 stash, 0 needs-disposition) and any cleanup pass may drop it. **The structural gap this class keeps exposing is unchanged and still unfiled:** THR-672's self-heal covers only detached-and-clean parks, so a branch park on a dirty tree still needs hands — fourth occurrence (07-21 ×2, 07-23, 07-24→25); THR-678 covers only the untracked-file variant and sits in Idea. Friday's retro filed THR-753/754/755 but not this one; the filing falls to the next retro or grooming run. _Prunable at the next full retro._

- **2026-07-24 ~15:59 — THR-74 (Party Formation) reopened to In Dev; the second phantom-close is undone.** Was item 2, carried since the 13:09 run. The ticket had been swept to Done at 12:47 by the merge of its prerequisite reachability PR (#790) — the second phantom-close in twelve hours — while the executor's 13:13 checkpoint explicitly reserved `Fixes THR-74` for the final UI PR. Verified live this run: status In Dev, assigned, completedAt null. The remaining scope (four authored party moments + engine hooks, the two player actions, the rulebook group/company subsection, the map/profile UI) is visible to the hourly pickup again, and the THR-736 mutex ("shares files with THR-74's player-action work") can sequence correctly. The hardening ticket for the auto-close vector itself (**THR-738**) remains in Ready for Dev. _Prunable at the next full retro. Postscript 2026-07-24 ~23:03: a **third** phantom-close hit the same ticket at 21:33 (merge of PR #807, clean titles — the known vectors don't obviously explain it) and was reopened to In Dev at 23:03 the same evening; THR-738's urgency stands. Postscript 2026-07-25 ~04:09: **THR-74 is now genuinely Done** — closed 01:57 by PR #821 (the final UI pillar), whose body carries `Fixes THR-74` deliberately with full verification evidence; verified this run rather than assumed. THR-738 itself was picked up into In Dev at ~04:02._
- **2026-07-24 ~09:35 — Christian switched off all four old Cowork automations; the Pure Claude Code Migration is complete.** Was item 1, the last Christian-only standing ask, carried since the 2026-07-21 cutover decision (THR-653). `keep-work-flowing`, `daily-backlog-grooming`, `weekly-workflow-retro`, and `weekly-project-hygiene` are disabled in the Cowork app; their CC replacements have been live since the 2026-07-22 attended trials (THR-677). `weekly-invoice-check` remains enabled by design — personal, not Threadbare. The double-run overlap is gone, and every Threadbare automation now runs in the single Claude Code lane. Confirmed by Christian in chat 2026-07-24 morning. _Prunable at the next full retro._
- **2026-07-23 ~22:00 — the home tree is reattached and the ten stranded retros are backed up.** Was item 2, carried since the 20:29 run. Measured this run rather than assumed: on `main`, `main..origin/main` = 0, `origin/main..HEAD` = 0, zero tracked modifications, zero untracked. The backup PR #768 (`docs/home-tree-backup-2026-07-23`) landed both halves of the exposure in one pass — the ten retro write-ups (`Design/retros/`, April–July) that existed only on this machine, **and** the tracked `.claude/settings.local.json` grants edit that was blocking the self-heal. One inert leftover: a `home-tree-recovery` stash whose entire content is a 3-line edit to a since-superseded `Design/user-actions.md` — verified against `origin/main` before calling it safe; the reaper tracks it (1 stash, 0 needs-disposition) and any cleanup pass may drop it. **The structural gap this class keeps exposing is unchanged and still unfiled:** THR-672's self-heal covers only detached-and-clean parks, so a *branch* park or a park on a dirty tree still needs hands — third occurrence (07-21 twice, 07-23 once); THR-678 covers only the untracked-file variant and sits in Idea. Friday's retro should file the general ticket. _Prunable at the next full retro._
- **2026-07-23 — the "name the next game-facing stretch" question answered itself: both candidate directions got built.** Was item 2, carried since 07-21. War got its depth pass — battle texture with turning points and commander peril (THR-628, PR #722), sieges that tighten as they drag with relief marches and regional gravity (THR-629, PR #724), and notable agendas including the Campaign war hand-off (THR-630, war phase D, PRs #726 et al.). The economy became visible — faction prosperity derived and feeding the territorial-expansion war trigger (PR #711 chain), cargo route events materializing as encounters (P2b), and economic context wired into encounter scoring/seeding (THR-725, PR #761). The queue now feeds from Christian's two 2026-07-23 chat directives: wiki freshness enforcement (THR-730) and Divine Receipt outcome feedback (THR-727). The direction call is back to being a per-directive conversation, not a standing ask. _Prunable at the next full retro._
- **2026-07-23 — the stale-PR backlog is gone: 14 → 0 open.** Was item 3, carried since 07-20. THR-702 (PR #708) triaged the armed-BEHIND set, and the board now shows **zero open PRs** (measured live this run via `gh pr list`). The auto-merge fix (THR-675) keeps new PRs self-merging on green, so the accumulation pattern is closed at both ends: old set cleared, inflow self-draining. The nine orphans of the retired `flush-plan-docs` job and the aged feature PR #553 are all settled. _Prunable at the next full retro._
- **2026-07-22 — all three Cowork-replacement trials passed with Christian's chat approval (THR-677 closed).** The attended runs produced real reports (`Docs/ops/backlog-grooming-2026-07-22.md`, `Design/retros/workflow-retro-2026-07-22.md`, `Docs/ops/weekly-hygiene-2026-07-22.md`), Christian approved in chat, and the three schedules were enabled at deconflicted slots (~09:16 daily / ~Wed 11:13 / ~Sun 10:10). The monthly rulebook review (THR-704) was registered the same day. Item 1 narrowed from "run the trials" to "flip the four Cowork switches." _Recorded here; the remaining ask stays in item 1._
- **2026-07-22 03:37 — encounter scene-integration Slice D shipped (THR-697, PR #705).** Planted story-seeds now actually spawn. An encounter that promised a follow-up used to leave an advisory note that nothing acted on — roughly half of them never fired. A seed now resolves against the family of encounters it belongs to and carries the original scene's cast and target forward into the follow-up, so a thread you started with a named person continues with that same person. Landed unattended overnight. _Prunable at the next full retro. Postscript: slices C, E and F (THR-696/698/699) all landed 2026-07-22, completing the six-slice chain._
- **2026-07-22 02:32 — encounter scene-integration Slice B shipped (THR-695, PR #703).** An encounter's *aftermath* can now point at the real person or place the scene happened with, rather than resolving to a generic referent — the sentinel binding runs across four aftermath fields. It also adds the first authored effect that forges a genuine bond between two characters, so a scene's consequences can change a relationship rather than only a number. Landed unattended overnight. _Prunable at the next full retro._
- **2026-07-21 23:35 — encounter scene-integration Slice A shipped (THR-694, PR #701).** The load-bearing engine piece the whole chain rests on: encounter prose can now name the real entity a scene is *with* — the `{target}` / `{target:they|faction}` placeholders and `{?has_target}` conditionals, with self-targeted actions correctly reading as "no referent" rather than inventing one. Slices B–F (THR-695/696/697/698/699) build on it and are ready to pick up. _Prunable at the next full retro._
- **2026-07-21 22:23 — the plan-doc lint no longer drowns real findings in intent-proposal noise (THR-692, PR #696).** `.intent-proposals/` is now skipped by `lint:plan-doc`, so a design-session handoff stops emitting ~40 spurious ERRORs about missing three-pillar sections on files that were never meant to have them. The PR itself became the exhibit for THR-702 — it sat ~10h armed-and-BEHIND before it merged. _Prunable at the next full retro._
- **2026-07-21 22:54 — the home tree is clean and re-attached; the ten-hour item is closed.** Was item 2, carried from 12:54 through 21:54 across ten consecutive runs: parked off-branch at a detached snapshot, one leftover tracked file (`Design/user-actions.md`, superseded), autosync refusing to fast-forward a dirty tree, and a behind-count that changed meaning halfway through. **Measured clean this run rather than assumed:** on `main`, `main..origin/main` = 0, `origin/main..HEAD` = 0, zero tracked modifications, zero untracked. Whether Christian ran the three commands or THR-672's self-heal guard caught a tree that had become clean, the tree is now inert and current, and the fix commands are withdrawn from the briefing. **The gap behind it is unchanged and remains agent work, still unfiled:** THR-672's self-heal requires a strictly clean tree, so a park landing on an already-dirty tree still needs hands — which is exactly what cost ten hours of Christian's attention here. The nearest ticket, THR-678, covers only the *untracked*-file variant and sits in Idea; nothing covers the tracked-modification-plus-detached combination. **Standing item for the Friday retro**, and the next design or execution session touching the freshness path should file it. **Deployment note preserved for whoever does:** `threadbare-autosync.ps1` lives *outside* the repo at `C:\Users\chris\bin\` with no version-controlled source, so merging a repo change does not deploy it. _Prunable at the next full retro. Postscript 2026-07-23: the same class recurred — branch park + tracked settings edit — and resolved the same evening; see the 22:00 entry above._
- **2026-07-21 18:08 — THR-688 shipped: tickets now get written so they still make sense at pickup.** Three rules, all codified in the coordination protocol and CLAUDE.md: a sweep ticket states the *rule* for what belongs in it rather than a snapshot count that rots before anyone picks it up; a "these two can't run together" line must carry its reason, so an executor can check whether the reason still holds instead of obeying it blindly; and a ticket's completion conditions must match the pillar it touches — browser evidence for things you can see, headless runs for engine work. **Why it mattered:** THR-674 failed its own Done-when this morning purely because the ticket had frozen a count that moved (12 stashes became 38), and it had to be re-scoped and re-run. _Prunable at the next full retro._
- **2026-07-21 15:41 — THR-687 shipped: the board no longer marks work finished that never happened.** The one-time board-integrity audit swept for issues auto-closed to Done with no landing commit behind them — the THR-540 pattern, where an unrelated pull request's *title* containing a bare "fix THR-XX" closed the wrong ticket two minutes after it was filed. Filed 09:12 in the morning's pattern assessment, done by 15:41. **Why it mattered beyond hygiene:** a board that silently marks things done is a board nobody can trust to answer "what is left?", and this task's queue counts are read straight off it. _Prunable at the next full retro._
- **2026-07-21 15:28 — THR-677 built all three Cowork replacements and stopped one step short, correctly.** Three of four Done-whens met; the fourth was Christian's trial gate — **satisfied 2026-07-22, see the trials entry above.** The port was not a transcription — verifying each claim against live state caught a daily query against a state retired in June and an audit of machinery demolished the same morning. Prompts mirrored to `Docs/ops/scheduled-task-prompts/` so they can never again live only in app state. _Prunable at the next full retro._
- **2026-07-21 13:13 — THR-674 closed properly: the 38-stash pile was triaged, not discarded.** It had been split into THR-680/681 and written off as unfinishable; instead the disposal pass landed via PR #674. **Four stashes were salvaged onto `origin/main`** — `stash@{12}` alone carried **21 plan and intent-proposal docs** from the retired `flush-plan-docs` job, plus `stash@{15}` (rulebook canon draft), `stash@{30}` (detail-page data model), `stash@{36}` (THR-17 closeout WIP) — with a written disposal record at `Docs/ops/2026-07-21-thr-674-stash-worktree-disposal.md`. The 6 `needs-disposition` worktrees were settled in the same pass. **Verified this run rather than assumed:** the reaper reporting `stashes: 0` one hour after reporting 38 is equally consistent with silent loss, so the salvage commits were confirmed present on `origin/main` before recording this as a good outcome. `refs/stash` no longer exists; nothing unexamined was dropped. **Consequence for the board:** THR-680 and THR-681 are now superseded and should be closed. _Prunable at the next full retro._
- **2026-07-21 12:28 — THR-686 shipped (PR #672): the advisory gates no longer lie about passing.** Filed at 09:12 in the morning's pattern assessment and done inside three hours — the fastest turnaround on the board that day. It retired the `tsc --noEmit` check, which exits 0 unconditionally (the root config lists no files) while CLAUDE.md and the pickup prompt both named it a real gate; scoped the plan-doc lint; fixed a self-referential side-effect in the process check; and rewrote the `/pull-work` board scan that had been hard-failing on response size. **The motivation is worth keeping:** gates that lie train agents to bypass gates — `--no-verify` had appeared twice in twelve runs, both times justified by a false positive. _Prunable at the next full retro._
- **2026-07-21 — the upstream bug report is filed; you said yes.** Was item 2, carried across four consecutive runs as the sole blocker on THR-676. Christian approved in chat and the report is public: https://github.com/anthropics/claude-code/issues/79713. It named Windows 11 / Claude Code 2.1.90, the scheduled-session setup, and ten reflog lines — no repository contents, no file paths from the work, nothing about the game, exactly as scoped. **THR-676 closed at 14:06** on that final one-liner — the CLAUDE.md cross-link to the filed issue (PR #677). The three-day investigation is finished end to end: cause identified, contained locally (THR-671/672), reported upstream. _Prunable at the next full retro._
- **2026-07-21 10:47 — the home tree was repaired; the recurring park is closed.** Carried and escalated across ten consecutive autosync refusals from 21:56 through 09:50. Re-verified at the 12:54 run: still on `main`, nothing parked, nothing stranded (`origin/main..HEAD` was 0 throughout). **Correction to the 11:54 entry:** that run landed the *content* of the uncommitted chat edits on `origin/main`, but could not clear the working-copy file itself — scheduled sessions may not run `checkout` against the home tree. The stale duplicate was cleared by the 22:54 resolution above. **The underlying cause remains harness-level** (filed upstream, above) but its containment is live (THR-671/672) and the tree is inert. **Deployment note preserved:** the autosync script lives *outside* the repo at `C:\Users\chris\bin\threadbare-autosync.ps1` and has no version-controlled source — merging a repo change does not deploy it. _Prunable at the next full retro._
- **2026-07-21 08:48 — the Cowork demolition shipped; you answered yes.** THR-654 went Todo → Ready for Dev (07:13) → In Dev (08:02) → Done (08:48) via PR #667. The `.agents/skills/` duplicate tree, `scripts/check-skill-sync.js`, the THR-192 pre-commit hook, the `flush-plan-docs` task and skill, the `plan-pending-commit` label and `flush-close-guard.yml` are all gone, and CLAUDE.md was rewritten to a single-executor world. **The `check:skill-sync` false-positive class (62 impediment mentions) is structurally closed, not worked around.** The six skills that existed only in `.agents/skills/` went with it as expected and are recoverable from git history. _Prunable at the next full retro._
- **2026-07-21 — the Obsidian MCP decision is settled; Option B was taken.** Carried ~60 days across ~12 occurrences (impediments #66, #71, #75, #86). Resolved structurally rather than by a decision in chat: THR-654's CLAUDE.md rewrite records that vault writes go through the filesystem, not the MCP, and vault skills **fail loud** when `OBSIDIAN_VAULT_PATH` is unset rather than silently dropping the entry. Verified — the variable is set in `.claude/settings.local.json` to `C:\Users\chris\Dev\Obsidian`. The silently-dropped-`log.md`-append failure mode is closed. **No action remains.** _Prunable at the next full retro._
- **2026-07-21 07:30 — the sixteen-day-old economy console warning closed (THR-644).** The duplicate-key React warning in the economy event feed, flagged during THR-631 browser verification on 2026-07-05, is fixed (PR #665). _Prunable at the next full retro._
- **2026-07-21 09:13 — THR-674 closed by splitting, not by finishing.** The one-time git surface cleanup landed the 15 untracked drafts (PRs #654/#655) but could not reach its "`git status` clean" Done-when: the stash stack was **38 entries** not 12 (→ **THR-680**) and the leftover work folders were **26, all under `.claude/worktrees/`**, a path `pull-work` forbids touching (→ **THR-681**). THR-674 was then re-scoped in predicate form (per THR-688), returned to Ready for Dev, and **finished for real at 13:13** — see the disposal entry above. _Prunable at the next full retro._
- **2026-07-21 06:15 — THR-673 shipped: the stale-branch cleanup job no longer fails silently.** The "Threadbare Git Cleanup" Windows task had been returning `-2147020576` (battery refusal) while logging nothing, so nobody could tell a skipped run from a clean one. Re-verified healthy at the 2026-07-23 20:29 run — last run 19:40, tracking 23 worktrees / 31 branches / 1 stash / 0 needs-disposition. The hourly cadence and the SUMMARY line are both doing their job. _Prunable at the next full retro._
- **2026-07-21 05:22 — THR-675 shipped (PR #657): pull requests now merge themselves on green.** Repo settings verified live (`allow_auto_merge: true`, `delete_branch_on_merge: true`), so `gh pr merge --auto --merge` is the standard closeout and executor sessions stop burning 3–8 minutes poll-waiting on CI. The 14 already-open PRs it could not retroactively rescue were cleared by THR-702 — see the top entry. _Prunable at the next full retro._
- **2026-07-21 ~02:20 — the 15 untracked design drafts are backed up (PR #654, THR-674).** The "one machine, no backup" exposure this file carried since 2026-07-20 is closed: brainstorms, exploration notes and intent-proposal records that existed only in the home tree are now committed to `origin/main`, with the cleanup findings logged to `Docs/impediments.md` alongside (PR #655). _Prunable at the next full retro. Postscript 2026-07-23: a fresh instance of the same class — ten untracked retro files — surfaced and was backed up via PR #768 the same evening; see the 22:00 entry above._
- **2026-07-21 01:22 — THR-672 shipped (PR #648): the home tree is now an inert, self-healing mirror of `main`.** The autosync re-attaches the provably loss-free park on its own (detached ∧ 0 unique commits ∧ 0 tracked modifications → `git switch main`), and scheduled sessions are barred from running checkout/commit/merge/rebase/reset with the home tree as CWD — the rule this very task follows. _Prunable at the next full retro._
- **2026-07-21 00:34 — THR-671 shipped (PR #646): the freshness signal distinguishes "parked off-branch" from "behind."** This closes the specific mis-reporting fault that made `Design/briefing.md` publish an escalating behind-count (58 → 79) for days while local `main` was in fact 0/0 with `origin/main` the whole time. _Prunable at the next full retro._
- **2026-07-20 22:54 — the load-bearing git/CI plan doc reached `origin/main`** (PR #638 merged). Six Ready-for-Dev tickets that cited a spec existing only on one machine became readable by any executor. _Prunable at the next full retro._
- **2026-07-20 — the "N commits behind and climbing" alarm was measuring the wrong thing.** **Correction to the record, retained because it is load-bearing for future diagnosis:** the escalating behind-count this file reported for days (58 → 64 → 69 → 75 → 77 → 79) measured a *frozen detached snapshot* against a moving `origin/main`. Local `main` was **0/0 with `origin/main` the entire time** — the tree was parked beside `main`, never lagging behind it. Nothing was decaying. **The 68 staged files were worse than "stale echoes"**: diffed against their base they were 221 insertions / 3,379 deletions, reversing THR-614's `quintessence`→`cohesion` rename and THR-575's CLAUDE.md trim — phantom staleness, not authored damage: the CC harness moved `refs/heads/main` forward via plumbing *while `main` was checked out* (07-18 20:01, reflog + autosync-log correlation), which leaves index and working files at the old content and makes `git status` report the delta as "staged changes" — nobody staged anything (see `Docs/plans/2026-07-20-git-cicd-clean-delivery.md` § 1.3). Committing them would have reverted shipped work. _Prunable at the next full retro._
- **2026-06-23 — `LINEAR_API_KEY` set in the Codex automation environment** (was item #1; impediment #141, 17 recurrences). _Superseded 2026-06-23 by the full Codex-lane retirement (THR-486). Safe to prune at the next full retro rebuild._
- **2026-06-23 — GitHub Pro / branch protection resolved** (was item #4 in the prior seed; impediment #56). Branch protection is active on `main` with `Test · Typecheck · Build` as a required status check (THR-282 shipped 2026-04-30). To be removed on next retro day.
- **2026-07-19 — local dependencies reinstalled; `npm run dev` / `npm test` work again on the home tree.** Confirmed resolved at the 16:28 run: `node_modules/.bin` now exists with **99 shims** (was absent entirely), 284 top-level packages, and `npm exec -- vite --version` returns `vite/7.3.1 win32-x64 node-v24.14.0`. Local dev server, test runs, production build and the pre-commit hooks are unblocked from the home tree, which restores the browser-screenshot step of the Definition of Done. The worktree-specific `.bin` shim workaround is still valid for **scratch worktrees**, which have no `node_modules` of their own — see impediment #186.
- **2026-07-18 — Linear-from-scheduled-context confirmed reliable** (was item #4; three clean data points: 2026-06-23, and two `keep-work-flowing-cc` runs on 2026-07-18). The hedge is dropped — scheduled/autonomous CC sessions can trust the Linear MCP without caveating conclusions on it.

---

*This file is refreshed hourly by the `keep-work-flowing-cc` Claude Code scheduled task from `Docs/impediments.md`, the Linear board, and any newly filed Christian-owned impediments; the `retrospective` skill does the deep periodic rebuild. Manual edits between runs are fine — they'll be preserved as long as the item is still open. To force a full rebuild, run `/retrospective`.*
