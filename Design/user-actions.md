# User Action Required

**Last updated:** 2026-07-30 08:57 local, by the hourly `keep-work-flowing-cc` CC task.

**ZERO OPEN CHRISTIAN-OWNED ASKS — tenth consecutive published run at zero, fifth at a true empty (no standing asks either).** Discord inbox **genuinely empty** for an eighth consecutive read — the `after=` cursor returned a literal `[]` (3 bytes). Home tree on `main`, **0 behind**, 0 stranded, 2 tracked-dirty (settled). Deploy `skipped`-benign at `abed3a00`. Merge gate `healthy`, `startupFailureCount: 0`. Fleet heartbeat `ok`. **The run's two substantive results both RETIRE open questions rather than opening new ones: (1) last run's autosync alarm was WITHDRAWN on mechanism — a no-op fast-forward writes no reflog entry, so "no entry" and "did not run" are the same observation, earning finding 40; (2) finding 35's fix was OBSERVED WORKING on its strongest data point yet — `pull-work` claimed THR-681 at 06:57:17Z *while it carried `assignee: "Christian Spliid"`*, which is direct evidence rather than inference.** **One process gap: the 05:53Z run of this lane published nothing, so the brief on `main` was two hours old rather than one.**

## This run's measurements (2026-07-30 06:57 UTC)

**Two hours since the last *published* brief (04:58Z), not one** — the 05:53Z run left no commit (see "Open agent work"). Deltas below span two hours; where that matters it is stated.

- **LAST RUN'S AUTOSYNC ALARM IS WITHDRAWN, AND THE WITHDRAWAL IS THE RUN'S BEST FINDING.** Last run flagged: *"the 06:50 tick had not fired as of 06:58 local — 8 minutes late, where the prior seven were exact to the second"*, and set the test: *"if 07:50 also misses and the count exceeds 10, autosync has stopped."*
  - **The count is 0 — the best possible reading — so the test resolves benign on its own stated terms.** Home tree is exactly current with `origin/main`.
  - **But the *premise* was wrong, which matters more than the verdict.** `git reflog show main` shows hourly `merge origin/main: Fast-forward` entries at `:50:03` through 05:50 local, then nothing until `pull --ff-only origin main: Fast-forward` at **08:20:23**. The inference "no entry ⇒ did not run" is **unsound**: a fast-forward with nothing to fetch does not move the ref, and **a ref that does not move writes no reflog entry.** An hour with no new commits upstream is therefore indistinguishable from an hour where the job failed.
  - **Neither explanation was falsifiable from the evidence available.** `threadbare-autosync.ps1` is not on disk at any searched path and writes no log file (consistent with THR-824 — the script has no repo mirror), so there is no independent witness. **Recorded as ambiguous, not as healthy and not as broken**; the tree being current is the outcome that actually matters, and it is confirmed.
  - **Earned finding 40.** This is the second consecutive run in which a *count measured against a periodic process* nearly produced a false Christian-facing alarm — finding 38 was the phase/health confusion, this is the trace/existence confusion. **Both are cases of reading a periodic repairer's incidental artefacts as if they were its status.**
- **FINDING 35's FIX OBSERVED WORKING ON DIRECT EVIDENCE — the strongest data point in the sequence, and it closes an eight-run investigation.** `pull-work` claimed **THR-681 at 06:57:17Z**, confirmed against the machine-written `stateHistory` (finding 27): `Ready for Dev` ended `2026-07-30T06:57:17.316Z`, `In Dev` began at the same instant.
  - **THR-681 carries `assignee: "Christian Spliid"`, `assigneeId: 35005bad…`.** Under the pre-THR-845 reader (`assignee:null`) this row was **invisible by construction**. It was claimed anyway.
  - **This upgrades the evidence class.** Every prior confirmation was *inference from source* — `pull-work` SKILL.md lines 27/176/184 no longer filter on the field. **This is the behaviour itself: an assigned row entering `In Dev`.** Finding 35's chain — question asked → cause located → fix shipped → reader indifferent — is now closed on observation, not on reading.
  - **Note the claim arrived mid-run**, between the column scan (which listed THR-681 under `Ready for Dev`) and the follow-up query 4 minutes later. **The column scan was not stale in the memory-`linear_list_issues_stale_status` sense — the world changed underneath it**, and only the `stateHistory` timestamp distinguishes those two.
- **THR-680 SHIPPED, AND LAST RUN'S STALE-PREMISE CALL WAS CORRECT — finding 39 paying out one run after being earned.** `Done`, `completedAt: 2026-07-30T05:23:21.041Z`.
  - Last run flagged its Done-when as near-vacuous: a per-stash verdict table over a **"38-deep"** stack that actually held 2. **The executor independently reached the same conclusion** — commit `1392fa8b` is titled *"stash residue disposal + already-shipped-under-parent finding"*, which is finding 39's diagnosis **and** memory `split_child_ships_under_parent_id` in one line.
  - **`git stash list` now returns 0**, and the reaper's own history confirms the drain (`… 2, 2, 2, 2, 0, 0`). **Two independent witnesses, same as last run.**
  - **What this licenses is narrow (finding 25):** the *detection* worked and cost one `grep`. It does **not** show the promoting lane now re-derives counts — nothing changed in that lane. Finding 39's detector remains unimplemented.
- **[SUPERSEDED — retained for the finding-37 record] The re-powered #1085 test discriminated at 04:58Z**: armed 02:29:27Z → merged 04:18:37Z unattended = **1h49m**, inside the >2h07m boundary.
- **Queue: BACKED UP — 52 ready (0 Urgent / 0 High / 18 Medium / 34 Low), 1 In Dev.** Nothing stale: oldest `updatedAt` is THR-739 at 2026-07-24T20:29:29Z, **~5.4 days**, inside `STALE_ISSUE_DAYS`=7.
  - **The scan read 53/0; the true state 4 minutes later was 52/1.** THR-681 was claimed at 06:57:17Z mid-run. **Reported as 52/1 — the later, machine-timestamped reading wins over the earlier column scan.**
  - **Over two hours: −1 ready (THR-680 → Done), +1 ready (THR-867 promoted 05:29Z), −1 ready (THR-681 → In Dev).** **One finished, one filed, one started.** Net shelf 53 → 52. **Second consecutive stretch where filing did not outpace finishing** — but per finding 25 this licenses "the lane keeps pace", **not** "the shelf drains": 52 items still sit there, and the two stretches are not independent (the same executor did both).
- **The assignee leak is UNCHANGED in mechanism and STILL nil in consequence.** THR-867 (filed 05:29Z — *the ticket about the leak*), THR-863 and THR-864 all read `assignee: "Christian Spliid"`. **THR-681 read the same and was claimed anyway**, which is the point: the reader's indifference is what retired this, not the leak stopping.
  - **Finding 32's "something re-populates after a verified clear" is carried, NOT re-derived this run** — no fresh clear-then-recheck was performed, so this run adds no evidence either way. Flagged so the next run does not read three stamped rows as confirmation of a mechanism nobody re-tested (finding 34).
- **Deploy (step 2.5): `skipped`, benign, publish point flat at `abed3a00`.** Verbatim: *"The live site is up to date. Commits since the last publish (abed3a00) only touched notes and docs, so the game itself did not need rebuilding."* **Correct — every commit since `abed3a00` is docs/ops.** Finding 28 unchanged (advance observed last run, flat observed here).
- **Merge gate (step 2.5b): `healthy`, `startupFailureCount: 0`, `standDown: false`.** **Sixteenth consecutive independent confirmation** that Christian's billing action holds.
- **Step 2.7 (heartbeat) returned `ok`.** Verbatim: `{"verdict":"ok","needsChristian":false,"checked":8,"stalled":[],"neverRun":["monthly-rulebook-review"],"summary":"All 8 enabled scheduled tasks are within 2 slots of schedule."}`. **Fifteenth consecutive `ok`** — lanes fired 06:01Z / 06:26Z / 06:53Z at **distinct** timestamps, so the wake-boundary defect (finding 30) is untriggered rather than merely quiet.
- **Reaper healthy — ran 08:40:02 local (06:40Z), 17 minutes before this brief.** `SUMMARY:` **39** worktrees / **52** branches / **0** stashes / **0** needs-disposition. **Stashes 2 → 0**, matching THR-680's disposal — an independent corroboration of that ticket's closeout.
- **HOME TREE CURRENT: on `main`, 0 behind, 0 stranded, 2 tracked-dirty, 6 untracked.** The tracked dirt is `.claude/settings.json` + `.claude/settings.local.json` — settled per correction (c); untracked files never block anything.
  - **Autosync's trace is ambiguous and is recorded as such** — see the finding-40 bullet above. Ticks at `:50:03` through 05:50 local, then a `pull --ff-only` at 08:20:23. **0 behind is the outcome that matters and it is confirmed.**
- **Discord: the cursor returned `[]` — 3 bytes, zero messages, eighth consecutive read.** No receipt owed, none sent; cursor left at `1532100414189867109`. `allowFrom` was read from `access.json` **before** any classification, preserving fail-closed ordering with nothing to authorize. **Silence is not evidence he is away** (finding 8).
- **Ping gate (step 6): no ping sent, correctly.** Stored hash is the SHA-256 of the empty string; this run's Needs-Christian is also empty, so the hash matches and the doorbell stays silent.
- **Sibling reports (step 2.6): two in-window, no items folded.** `orchestrator-2026-07-30h.md` (mtime 08:20 local) reads *"Nothing needs you. One clean promotion this run…"* — **empty state, skipped per the rule.** `backlog-grooming-2026-07-29.md` (mtime 07-29 15:50, ~17h) leads with *"Nothing needs you"* — likewise skipped. `weekly-hygiene-2026-07-26.md` is **~96h**, outside the 36h window.
  - **Same-day collision is now EIGHT files** (`-30` … `-30h`). mtime and lexical order still agree, **so the consumer has been correct-by-luck eight consecutive hours.** Collision cause ticketed (THR-849); **consumer-side half still unticketed.**
- **Zero Linear writes.** Two column reads (`Ready for Dev`, `In Dev`), one search, one `get_issue` (THR-681). Read-only throughout.
- **This run's worktree was created outside `.claude/worktrees/`** (`Dev/Projects/kwf-wt-20260730-0653`), with `rev-parse --show-toplevel` asserted after creation, **the `.git` file's existence checked (THR-797), and the home tree's branch re-read as `main` in the same command.** **The home tree was touched by `git -C` reads, one `git worktree add`, three read-only `npm run` probes, `gh` reads, and nothing else.**

## Open agent work (not Christian's)

- **THIS LANE SKIPPED A PUBLISH AND LEFT NO TRACE — new, and the most actionable item this run.** `lastRunAt` shows a `keep-work-flowing-cc` run at **05:53:28Z**, but `git log -- Design/briefing.md` jumps **05:04Z → (nothing) → this run**. The brief on `main` was therefore **two hours old** when Christian could have read it, and the header timestamp said 04:58Z — honest, but only if he checks it.
  - **Two explanations, not distinguished:** the run judged "no substantive change" and correctly skipped the commit (`COMMIT_ON_SUBSTANTIVE_CHANGE_ONLY`), or it failed after the freshness ping and before the commit. **In a two-hour window that saw a ticket ship, a ticket get filed and a ticket get claimed, "no substantive change" is implausible** — which points at the second.
  - **This is the failure class step 2.7 exists for, one level down.** The heartbeat probe checks whether a *task fired*; `lastRunAt` says it did. **Nothing checks whether a task that fired produced its output.** A lane that runs and silently emits nothing is exactly a stalled lane wearing a healthy status — the THR-837 shape, applied to output rather than dispatch.
  - **Fix shape:** have this lane assert its own last-published age (compare `Design/briefing.md`'s newest commit against `lastRunAt`) and surface a gap >1 slot as an agent-side item. **Wants a ticket** (finding 23).
- **The assignee-leak instrumentation is now TICKETED — THR-867, filed by the orchestrator 05:29Z.** *"Instrument the orchestrator's assignee-clear write sequence — THR-845's fix still leaks on every filing despite three enforcement layers."* **This closes the "wants a ticket" carry** that had run several sessions.
  - **Priority is correctly `Low`, and this run strengthens why:** THR-681 was claimed *while stamped*, so the reader's indifference is now observed rather than inferred. **The leak is cosmetic until something reads the field again.**
  - **One caution for whoever takes THR-867:** its title says "still leaks on every filing", which is a **create-path** framing. Last run's evidence was different and stronger — rows cleared *and verified absent* re-read as stamped ~14 minutes later, i.e. **something re-populates after a successful clear.** **That evidence was not re-derived this run**, so it is a hypothesis, not a finding. **The ticket should instrument the interval after a clear, not only the create sequence** — otherwise it will confirm a mechanism that may not be the live one.
- **The step-0 allowlist snippet wants a one-token path fix**: `$HOME` → `$USERPROFILE` or a Windows-resolvable literal, because Windows `python` cannot open the MSYS-style path `$HOME` expands to. **Wants a ticket** (finding 23).
- **Step 2.6's section extraction is keyed on the producer's incidental formatting AND its file naming, and neither is stable. The file-selection axis is now SEVEN-deep in a single day.** mtime and lexical order still agree (`.` < `b` < … < `g`), **so the consumer has been correct-by-luck seven consecutive hours.** Prior derivation unchanged: `orchestrator-2026-07-30.md` uses `## Needs Christian`; `orchestrator-2026-07-29.md` uses `### Needs Christian` — **same lane, two files, one day apart, two different levels.** **Fix shape:** select by explicit mtime (not glob order), match the *text* with a permissive level (`^#{2,6} Needs Christian`), terminate on a same-or-shallower heading. **Wants a ticket** (finding 23).
- **`check:task-heartbeat` mis-fires at a wake boundary — carried, not re-derived this run.** The probe accepts any sibling whose `lastRunAt` falls **inside** the stall window as its liveness witness; after a host sleep the whole fleet's `lastRunAt` clusters at the window's *closing* edge. **Fix shape:** require the witness to have fired strictly inside the window with a margin, or detect the burst directly (≥2 lanes sharing a `lastRunAt` to the second ⇒ host-wake, suppress). **Wants a ticket** (finding 23).
- **Verify-after-write needs a clock — carried; THR-860's self-refuting row remains the best artefact.** Its description asserts a `get_issue` re-query confirmed the assignee absent; the field was present on read. **The claim and its refutation live in the same row.** **Fix shape:** verify after the last side-effect in your own sequence, or on the next run.
- **A superseded report PR wants closing: #1031** (orchestrator/THR-762). `OPEN`, `updatedAt` still `2026-07-28T19:31:25Z` — **~35.4h idle**. **`mergeStateStatus: DIRTY` read cleanly this run** (via `gh pr list --json`, which returned a real value where `gh pr view` had returned `UNKNOWN` twice last run — **a cheap workaround for finding 29(b) worth reusing**). Consistent with a superseded report doc, not a regression (memory `dirty_pr_duplicate`). **One `gh pr close`, zero content lost.**
- **~~Autosync's `:50` tick was 8 minutes late~~ — WITHDRAWN 2026-07-30 06:57Z, on mechanism rather than on outcome.** A no-op fast-forward writes no reflog entry, so "no entry" and "did not run" are indistinguishable; the tree is 0 behind. **No action, and the *inference* is retired, not just the alarm** — see finding 40. **`threadbare-autosync.ps1` writes no log and is not on disk at any searched path, so this class cannot be settled without THR-824's mirror.**
- **THR-835 is unreachable in `Idea`** — carried. The required check has repeatedly run at 80–90% of its 20-minute cap, which is exactly the runner-variance hazard THR-835 describes. **A working promotion lane that never reaches a column is stronger evidence than a broken one** (finding 19). Remedy belongs to a write-remit session.
- **`rescue/2026-07-17-detached-plans` is prunable — carried, not re-derived this run.** A **local branch only**; against `origin/main` it carried **4 unique commits** as of 22:54Z. **Pruning it is `git branch -D` in the home tree, which this lane may not do** — not merely unclaimed, unclaimable from here. **Wants a write-remit session with home-tree authority.**
- **The orchestrator lane's stray-write defect is open and agent-owned.** `tb-orchestrator` wrote its 08:29Z report into the home tree as well as its own worktree, blocking a fast-forward. Durable fix is in that lane's write path (absolute paths, or `rev-parse --show-toplevel` asserted before any write — the workaround this task takes hourly). **Wants a ticket** (finding 23).
- **No gate notices live/mirror prompt divergence.** The two were proven in sync for `tb-orchestrator` on 2026-07-30 02:58Z (diff = one trailing newline), so the general defect stands without a live instance: `~/.claude/scheduled-tasks/<id>/SKILL.md` is what runs, `Docs/ops/scheduled-task-prompts/` is a mirror a merge can update while the live copy stays stale, and **nothing compares them.** THR-850 covers the missing mirror for `tb-opus-pickup`; the divergence *detector* is unticketed. **Wants a ticket** (finding 23).
- **Friday's retro draft** (`Design/retros/retro-2026-07-24-draft.md`) is untracked and wants a `git add` in any docs PR. **Not a carry** — it is THR-798's step 2, with an owner and a Done-when.
- **Impediment #267 is used twice**; **#219 and #228 are duplicate rows for one defect**. Both dispositions: fold at the next retro. **No ticket.**

## Durable findings carried forward

Rules earned by tickets that have since shipped. **Kept as rule sentences; every worked example is in `git log -p Design/user-actions.md`.**

1. **Before carrying an observation into its Nth run, re-derive it once.** **Paid out 2026-07-30 02:58Z as a correction** (three runs blamed a missing `python` interpreter; the real cause was MSYS path expansion, and the remedy changed). **Paid out 2026-07-30 03:57Z in the other direction:** the "reader no longer filters on assignee" carry was checked against `pull-work` SKILL.md lines 27/176/184 and **held** — and the check surfaced a *disposition* the carry had lost (a non-zero count is the documented handled path, not a regression). **A re-derivation that confirms is not a wasted one; it re-sources the claim and often widens it.**
2. **A subsystem with no callers is invisible to every sweep that looks for wrong behaviour, because it has none.** Corollary: grep the *assignment* side, not the read side.
3. **A plan doc's past tense is a claim, not evidence.**
4. **Advisory gates that nobody reads are indistinguishable from gates that do not exist.**
5. **A test suite that agrees with the bug stays green forever** — and a pool-scoped guard is a suite that agrees with the bug one shelf over.
6. **Read composition, not depth** — and stop re-testing the favourable dip.
7. **A gate clearing is not the same as a decision arriving.** Surfacing a settled call for ratification is worse than not surfacing it. Corollary: verify a *favourable* outcome's cause as carefully as an unfavourable one.
8. **An empty Discord inbox is not evidence Christian is away.** Silence means only that he did not write.
9. **A startup-failure cluster is indistinguishable from quota exhaustion at the moment it happens.** The disambiguating evidence — *does a fresh run execute jobs?* — costs one run. Take it before escalating to a billing page.
10. **Deploy-observability must classify by paths, not by branch name or PR title.** — SHIPPED 2026-07-27 in THR-785.
11. **Mutex lines with reasons work.**
12. **An unanswered ask is not an unheard ask — do not re-ring the doorbell.** A standing ask carries a stated safe default, so silence resolves it rather than blocking on it.
13. **Declining to build the missing half is a shippable outcome, and it needs its reasoning recorded to survive.**
14. **A signal with two meanings is not a signal — and the repair is a notification path, never a new gate.**
15. **When a ticket asks "which of these fixes?", census the pipeline before answering** — the honest answer may be "none, and here is why". Validate the *sample*, not just the stage.
16. **A deliberately-parked issue and a broken auto-close are indistinguishable from the board.** Report a falsifiable test in both directions.
17. **Before carrying an ask into another run, check whether it was answered somewhere other than the channel it was asked on.**
18. **State a falsifiable test against the outcome, never against an activity proxy.** State the test's enabling premise explicitly, or a benign world reads as its own failure case. **Now subsumes finding 37's threshold rule — a test also needs enough *power*, not just the right target.**
19. **A column-scoped queue query reports the lane's reach, not the board's contents — and from inside the query the difference is invisible.**
20. **When an ask names a *mechanism* rather than a decision, expect the answer to arrive on the channel he has, not the one the mechanism needs.** (a) Prefer asks that are pure decisions. (b) State plainly what was *not* done.
21. **Re-derive a carry even when you expect it to hold** — the check has a wider aperture than the claim. Look at what the source shows you, not only the field you came to check.
22. **An item in the lane's column is not an item the lane can take.** The cheap detector: read the comments, not just the fields. *Consequential* and *decidable-by-Christian* are independent axes.
23. **A ticket this task files is unpullable by construction, and the cost lands on someone else's run — so filing is never free.**
24. **A file's *state* is evidence; its *author* is a separate claim needing separate evidence.**
25. **A confirmed test licenses the claim it tested, not the remedy that motivated it — and the gap is widest exactly when the result is most satisfying.** Corollary: **the mirror failure is a test whose aperture is wider than the claim.**
26. **One instance locates a defect; the second instance with the same fingerprint is what turns it into a mechanism — and changes the remedy.**
27. **A timestamp is evidence only once its zone is verified, and a `Z` suffix is a claim like any other.** Prefer a machine-written field (`stateHistory`, `lastRunAt`, commit metadata) over a prose header. **The *wrong field* misdates an event as badly as the wrong zone** — THR-667's `updatedAt` sits 28 minutes after its actual `Ready for Dev`→`In Dev` transition.
28. **A classifier observed only in its stable state has not been tested.** **Satisfied in BOTH directions:** three consecutive `deployedSha` advances, then two correct **flat** hours when only docs merged. **Note what switched: the evidence, not the verdict word** — five runs reported `skipped` throughout.
29. **"Green and armed" is not "merging" — and the difference is invisible from every signal a lane normally checks.** Checks answer *is this change correct*; `mergeStateStatus` answers *can it go in now*. **The tell is throughput, not status.**

  **Two corollaries.** (a) ~~**It does not self-heal.**~~ **RETRACTED 2026-07-29 13:23Z, and the retraction GENERALISED 2026-07-30 04:58Z on a second independent data point** — #1042 merged ~2h07m after entering `BEHIND`; **#1085 merged at 04:18:37Z, 1h49m after arming, with nobody intervening.** **The operational rule inverts: do not spend a session force-updating a branch that is merely `BEHIND` — it lands within ~2h unattended.** **The retraction is itself the lesson: "still true at T" is not "true at every T"** — and it was load-bearing twice, because it also set the floor the discriminating test had to clear (finding 37). (b) **`gh pr view` returns `UNKNOWN` for `mergeable`/`mergeStateStatus` on the first read** and often the true value on the second — but **not always, and the unreliability runs in both directions.** **04:58Z: BOTH reads returned `UNKNOWN` for both #1085 and #1031**, where an hour earlier both reads on #1085 returned real values. Treat `UNKNOWN` as "not yet computed", never as evidence of mergeability — **and never assume a second read will resolve it.**

30. **A liveness witness must fire strictly *inside* the window it witnesses, not merely fall within it.** **The general form: a guard that accepts an endpoint as evidence about an interval passes hardest exactly when the interval is most degenerate.** Corollary: a probe's verdict is evidence, not a verdict — when independent witnesses contradict it, override and say why.

31. **A cleanup and its cause are separate deliverables, and shipping the cleanup is the moment the cause is most likely to be dropped.** State the re-accrual rate and a dated falsifiable test against the next producing event.

  **The honest quantity is not a rate: it is `|rows the sweep handled|`, workload-dependent and unbounded above.** Meta-lesson: **when a number has been wrong once, the reflex is to move it the other way and re-assert it with equal confidence.** Prefer naming the *variable*.

  **Closed out 2026-07-30 01:58Z in a direction the finding did not anticipate.** THR-845 shipped cleanup, cause-side hardening, **and** a third thing: **it removed the consumer that made the cause consequential.** Generalisation: when a defect's cleanup and cause are both expensive, check whether the *reader* can be made indifferent. **Confirmed twice over by 03:57Z — the leak has produced two more rows (THR-863, THR-864) and produced no symptom, and the reader's own source now documents the leak as a reportable number rather than a fault.**

32. **Verify-after-write needs a clock, not just a re-read — an immediate confirmation read can be served stale and is indistinguishable from success.** **Corollary: a count is not a verification.**

  **A `get_issue` check was correct when it ran and false 131 seconds later. An immediate verify cannot distinguish "my write held" from "my write held briefly"** — so the fix is **verify after the last side-effect in your own sequence**, not after the mutation you care about. **Sharpened by a controlled pair:** 9 immediate verifies after unassigns all held an hour later; 2 after creations were both false within the hour. **The verification was never the variable — what wrote next was.**

  **Best artefact: THR-860's description asserts its own verification and the field contradicts it in the same row.**

33. **A claim widens when it is restated, and the widening is invisible because every restatement still cites the original evidence.** **The drift is toward the remedy's framing**, because a report is written to justify acting. **Detector:** state what observation would falsify *today's* wording, then check whether it would also have falsified the wording used when the evidence was gathered. **Corollary: severity claims need their own evidence, separately from mechanism claims.**

34. **N confirming sightings of an effect do not locate its cause — and each sighting makes the unexamined attribution feel better supported.** **The trap is that repetition reads as accumulating evidence.** **Detector, cheap: before reporting a recurrence, name one property the affected rows share that the unaffected rows do not — and check it.** If you cannot state the contrast class, you have an effect, not a cause.

  **Corollary earned 2026-07-30 03:57Z while *maintaining* a contrast class rather than finding one: a row that was repaired after the fact is not evidence about the write path, and counting it as a clean case silently weakens the very contrast you are testing.** THR-861 reads unassigned only because the orchestrator hand-cleared it. **Keep the repair history attached to the data point, or the contrast decays as the cleanup succeeds.**

35. **A query's filter encodes a premise about the world, and nobody audits a premise that has never produced a visible failure.** (Earned 2026-07-29: `pull-work` selected work with `assignee:null` for months, encoding *"an assignee means a person has claimed this row."* Christian, unprompted: *"I do not use linear or github at all. so any issues in linear or PRs in github with my name on them are yours."*) **Detector, cheap: for each predicate in a query you are debugging, say out loud what it assumes about who or what writes that field — then ask whether anyone has ever confirmed it with the writer.** **Corollary:** 34 says N sightings do not locate a cause; **35 says the cause may not be in the mechanism at all, but in the question.** **The reader is usually yours to change even when the writer is not.**

  **SHIPPED 2026-07-30 01:21:50Z as THR-845, OBSERVED WORKING 02:02:22Z, RE-SOURCED 03:57Z, and CLOSED ON DIRECT BEHAVIOURAL EVIDENCE 06:57:17Z** — `pull-work` claimed **THR-681 while it carried `assignee: "Christian Spliid"`**, a row the pre-fix reader could not see. **Every earlier confirmation read the source; this one watched the behaviour.** **What makes this the finding's proof rather than its epilogue: the eight prior runs each produced a correct local measurement and a wrong global conclusion, and no amount of additional write-side measurement would have converged.** The unlock was a question, asked of the one party nobody had asked. **Second-order lesson for this lane: the answer arrived because step 0 existed to hear it.**

36. **A consumer that matches on a heading's *level* rather than its *text* is a channel that silently narrows as producers reformat.** **Detector: for every "no items found" result on a shared channel, confirm the *absence* against the raw file once, not just against your pattern.** Sibling of finding 19: **a scan reports its own pattern's reach, and a narrowed pattern and an empty world are the same observation.**

  **Generalised: "formatting" was too narrow — the near-miss was *file selection*.** So the rule is **every point where a consumer infers structure from a producer's convention is a coupling the producer can change unilaterally, and each one fails silently in the same direction.** Detector: for each such inference, name the producer change that would break it, then check whether anything would tell you.

  **Sixth consecutive correct-by-luck hour, 2026-07-30 03:57Z: the same-day set is now `-30` … `-30f`.** **A coupling that has survived six hours looks progressively more like a design and less like a hazard, and that drift is finding 33's tell turned on itself.** The luck is alphabetical and finite. **Record the streak as accumulating exposure, not accumulating safety.**

37. **A falsifiable test needs enough power to discriminate, and a threshold set below a known counter-example has none — it will "resolve" and license nothing.** **Earned 2026-07-30 03:57Z, cleanly.** Last run set the #1085 checkpoint at **1h30m** armed. Finding 29(a) had been retracted precisely because #1042 self-healed at **~2h07m**. The antecedent was satisfied on schedule and the conclusion was unavailable: at 90 minutes, a stuck PR and a slow-but-self-healing PR are the same observation.

  **This is distinct from finding 18.** 18 governs *what* you measure (outcome, not activity proxy). **37 governs *where you put the line*: a threshold must clear the longest benign case you already know about, or the test cannot separate the hypotheses no matter how faithfully it is run.**

  **Detector, one question, cheap: "what is the longest this has taken while still being fine?" — and set the checkpoint past it.** If no benign case is known, say so and state the test as exploratory rather than decisive. **The failure mode is seductive because an under-powered test still *resolves on time*, and a resolved test reads as a settled question.**

  **PAID OUT ONE RUN AFTER BEING EARNED, 2026-07-30 04:58Z.** The re-powered checkpoint (>2h07m) **discriminated**: #1085 merged at 1h49m, settling finding 29(a) in the favourable direction on two data points. **Same PR, same mechanism, same cadence — only the threshold differed, and the 1h30m version would have licensed nothing.** **This is the cheapest confirmed finding in the ledger: one question asked before writing the test, one run later a settled question instead of a repeated one.**

38. **A count measured against a periodic repair process is a *phase* measurement, not a health measurement — and the two are indistinguishable from the number alone.** **Earned 2026-07-30 04:58Z:** the home tree read **10 behind**, exactly at `FRESHNESS_BEHIND_THRESHOLD`, one commit from a Christian-facing alarm — while autosync was **provably healthy** (`git reflog show main`: seven consecutive hourly fast-forwards at `:50:03` to the second). All 10 commits had landed *after* the most recent tick, so the count was this hour's merge rate wearing the costume of decay.

  **Detector: before reporting a behind/lag/backlog count as decay, read the repairing mechanism's own cadence log and ask "when did it last succeed?"** A count plus a healthy cadence is phase; a count plus a missed cadence is drift. **The threshold alone cannot tell them apart, and a busy hour is exactly when the count looks worst.** Corollary: **this is why the THR-671 escalation ran for days** — a behind-count was read as decay without ever checking whether the mechanism had run.

  **Sibling of finding 30 in structure:** both are cases where a *point* observation is offered as evidence about an *interval*, and both pass hardest when the interval is least normal.

39. **A ticket-authoring rule binds the author, and nothing re-binds it at promotion — so a long-parked ticket's numbers rot in place and arrive at the top of the queue looking authoritative.** **Earned 2026-07-30 04:58Z:** THR-680 was promoted as the intended next pickup carrying "the 38-deep home-tree stash stack"; the stack holds **2**, and drained to 0 roughly **8.7 days** earlier. THR-688 rule A (predicates, not counts) exists precisely to prevent this and applies only at authoring time.

  **Detector for any promoting lane, cheap: if a ticket's Done-when quantifies anything, re-measure that quantity during the promotion run and put the fresh number in the promotion comment.** A count that survives the re-measure costs one command; a count that does not has just saved an executor a wasted session. **The failure is invisible from the board — a rotted ticket and a fresh one are the same row.**

  **Note the near-miss in the *evidence*, not just the ticket** (memory `vacuous_probe`): had this run reported "2 stashes" without the reaper's history, the honest reading would have been ambiguous between "the ticket is stale" and "I am measuring the wrong stash stack". **The decay curve `38 → 0 → 1 → 2` is what made it definitive**, and it cost one `grep`.

  **PAID OUT ONE RUN AFTER BEING EARNED, 2026-07-30 06:57Z — and the executor reached it independently.** THR-680 shipped as `1392fa8b`, *"stash residue disposal + already-shipped-under-parent finding"*: the same stale-premise diagnosis, plus memory `split_child_ships_under_parent_id`. Stack now **0**, reaper corroborating. **Scope honestly (finding 25): the *detection* is confirmed cheap and correct. The *detector* — re-measuring a Done-when's quantities at promotion — is still unimplemented in the promoting lane.**

40. **A periodic job that succeeds with nothing to do often leaves no trace, so "no log entry" and "did not run" are the same observation — and only one of them is an alarm.** **Earned 2026-07-30 06:57Z by withdrawing the previous run's alarm.** Autosync's hourly `merge origin/main` had written `:50:03` reflog entries for seven straight hours; two hours showed none, which was reported as "8 minutes late" and escalated to a watch item with a Christian-facing test. **But a fast-forward with nothing to fetch does not move the ref, and a ref that does not move writes no reflog entry.** An idle hour and a failed hour are byte-identical in that log.

  **Detector, cheap: before treating a gap in a periodic job's output as evidence it stopped, ask "would a successful no-op have written anything?"** If the answer is no, the gap is not evidence in either direction — say so, and find a witness that logs unconditionally (a run log, a heartbeat file, an exit code) or report the *outcome* instead of the trace.

  **Sibling of findings 30 and 38, and the family is now clear enough to name: all three are cases of reading a periodic repairer's incidental artefacts as if they were its status.** 30 — a point observation offered as evidence about an interval. 38 — a count that measures phase, not health. 40 — an absence that measures workload, not liveness. **In each, the honest move was to read the *repaired thing* (is the tree current?) rather than the repairer's exhaust.**

  **Note what made this catchable: the alarm came with a falsifiable test, and running it forced a look at the mechanism.** An alarm carried without a test would have hardened into a fact over the next few runs — finding 33's drift, on this lane's own output.

**Owner of items below:** Christian. Everyone else's blockers go in Linear or `Docs/impediments.md`.
**Refresh cadence:** The hourly `keep-work-flowing-cc` scheduled task keeps this current (prunes resolved items, adds newly-surfaced Christian-owned ones); the `retrospective` skill still does the deep periodic rebuild.

## How this works

The retrospective stopped being the right channel for "Christian, please flip one switch." Christian-owned asks were buried in retro prose, and nothing surfaced them between retros. This file replaces that pattern.

Read order: top to bottom is blast radius. The top items break canonical workflow invariants right now; the bottom items are operational debt with workarounds in place.

For each item:
- **Fix** = the literal command, click path, or line of config that resolves it.
- **What breaks** = the named system or invariant currently degraded.
- **Source** = `Docs/impediments.md` entry numbers + occurrence count, so the cost is visible.

When an item resolves: delete it from this file, mark the corresponding impediment as resolved in the dashboard regen, note the close in the next retro under "What shipped." **No history retained here — `git log` + the retros are the audit trail.** The resolved log is pruned to the current day; anything older is recovered with `git log -p Design/user-actions.md`.

---

## Standing asks

**NONE OPEN — fifth consecutive published run at a true empty.**

**A new unexplained multi-hour pause would be a NEW ask with new evidence — never the overnight-wake question reopened** (finding 12). If he ever does want overnight throughput guaranteed: keep the machine awake and **on mains power**, because Windows Task Scheduler refuses the reaper on battery (`-2147020576`, memory `git_cleanup_automation`). Recorded as a fact, not a request.

**Six questions are answered, closed, and must not be re-asked:**

1. **The five empty projects** — Christian, chat 2026-07-25 21:53: *leave them open; they are intake buckets that refill as the game iterates.*
2. **The GitHub Actions payment block** — cleared 2026-07-25 ~17:09Z and again 2026-07-28 17:02Z, both verified by re-run, and corroborated fifteen times since by independent mechanisms. **A recurrence is a new ask with new evidence, never this one reopened** (finding 12).
3. **THR-799 priority ordering** — Christian, quoted in THR-774/775, 2026-07-27: *the new encounter experience is still first priority.*
4. **Release of THR-821/820/777/778** — Christian, Discord 2026-07-27 14:38Z.
5. **The Linear "assign issue to PR author" switch** — Christian, Discord **2026-07-29 18:10:03Z**: *"can't find anything like that. I do not use linear or github at all. so any issues in linear or PRs in github with my name on them are yours."* **Closed by falsification, on the terms the ask itself set.** The second sentence is a **standing fact**: an assignee of "Christian Spliid" carries no information and must never be read as a human claim. **Enforced in code since 2026-07-30 01:21Z, observed working at 02:02Z, and re-sourced to `pull-work` SKILL.md lines 27/176/184 at 03:57Z.** **The highest-leverage answer in this file's history: one sentence retired eight runs of misdirected investigation and unblocked the executor lane.**
6. **Should the host machine stay awake overnight?** — **retired 2026-07-30 01:58Z by observation**, now four consecutive awake nights, never answered directly and no answer needed.

**One standing switch exists but is not an ask:** the orchestrator lane can be disabled on one word from Christian (live 2026-07-27 19:49Z). **Its default state is the intended one**, so silence resolves it.

**Three durable corrections survive closed asks and are kept as rules:**

- (a) A blanket `git stash` was the wrong remedy for home-tree dirt; the single-path form is correct. Parking his tool-permission edits was never necessary.
- (b) `$REATTACH_MAX_TRACKED_DIRTY = 0` governs the detached-HEAD reattach path and never evaluates while HEAD is on `main`. The conclusion held; its stated reason did not.
- (c) **Tracked dirt alone does not stall autosync.** The stalling class is the *intersection* of locally-modified **∩** changed-by-incoming-commits — proven by fast-forwarding cleanly with `.claude/settings*.json` dirty.

---

## Resolved 2026-07-30

**Tenth consecutive published run at zero open Christian-owned asks, fifth at a true empty.** Everything that moved was agent-side. **The run's two substantive results both RETIRE open questions, and one of them retires a claim this lane made itself:**

- **LAST RUN'S AUTOSYNC ALARM IS WITHDRAWN — on mechanism, not merely on outcome.** It reported the hourly sync "8 minutes late" because two `:50:03` reflog entries were missing after seven exact ones. **A fast-forward with nothing to fetch does not move the ref, and a ref that does not move writes no reflog entry** — so an idle hour and a failed hour are byte-identical in that log. The tree is **0 behind**, and no witness exists that would separate the two (`threadbare-autosync.ps1` writes no log and is not on disk — THR-824). **Earned finding 40**, and it completes a family with 30 and 38: *all three read a periodic repairer's exhaust as if it were its status.* **The alarm was catchable only because it shipped with a falsifiable test.**
- **FINDING 35 CLOSED ON DIRECT BEHAVIOURAL EVIDENCE — the strongest data point of the sequence.** `pull-work` claimed **THR-681 at 06:57:17Z** (machine-written `stateHistory`, finding 27) **while the row carried `assignee: "Christian Spliid"`** — invisible by construction to the pre-THR-845 reader. **Every earlier confirmation read the source; this one watched the behaviour.** The eight-run investigation that began with a misread query predicate is now closed at the behavioural level.
- **Finding 39 paid out one run after being earned, and the executor reached it independently.** THR-680 shipped (`completedAt` 05:23:21Z) as `1392fa8b` — *"stash residue disposal + already-shipped-under-parent finding"*: the same stale-premise diagnosis last run predicted, plus memory `split_child_ships_under_parent_id`. **Stash stack now 0**, reaper `SUMMARY` corroborating (2 → 0). **Scoped honestly: the detection is confirmed cheap and correct; the promoting lane's re-measure detector is still unimplemented.**
- **The assignee-leak instrumentation got its ticket — THR-867, filed 05:29Z**, closing a multi-session "wants a ticket" carry. **One caution recorded for whoever takes it:** its title frames the defect as create-path, but last run's evidence (verified-clear then re-stamped 14 minutes later) points at *re-population after a clear*. **That evidence was not re-derived this run**, so it is flagged as hypothesis, not finding — deliberately, to avoid finding 34's trap of counting three stamped rows as confirmation of an untested mechanism.
- **A NEW AGENT-SIDE DEFECT IN THIS LANE ITSELF: the 05:53Z run published nothing.** `lastRunAt` records the run; `git log -- Design/briefing.md` jumps 05:04Z straight to this run. **Christian's inbox was two hours old, not one.** "No substantive change" is implausible across a window in which a ticket shipped, one was filed and one was claimed. **This is the THR-837 shape one level down: step 2.7 checks that a task *fired*, and nothing checks that a task that fired *produced output*.** Fix shape and ticket request recorded above.
- **Queue 53 → 52 ready, 0 → 1 In Dev.** THR-680 done, THR-867 filed, THR-681 claimed. **Second consecutive stretch where filing did not outpace finishing** — scoped per finding 25 to "the lane keeps pace", not "the shelf drains", and the two stretches are **not independent** (same executor).
- **The merge gate held for a sixteenth consecutive check; the fleet heartbeat returned `ok` for a fifteenth** — lanes at 06:01Z / 06:26Z / 06:53Z with distinct timestamps, so finding 30's wake-boundary defect is untriggered rather than quiet. The reaper ran 06:40Z: 39 worktrees, 52 branches, **0 stashes**, 0 needs-disposition.
- **Deploy correctly flat at `abed3a00`** — every commit since is docs/ops. Finding 28 unchanged this run (the advance was observed last run, the flat state here).
- **Discord returned a literal `[]` for an eighth consecutive read** — nothing to classify, no receipt owed, cursor unmoved at `1532100414189867109`. **The ping gate correctly stayed silent.**
- **A cheap workaround for finding 29(b) surfaced by accident:** `gh pr list --json mergeStateStatus` returned a real `DIRTY` for #1031 where `gh pr view` had returned `UNKNOWN` on both reads last run. **Worth preferring the list form when mergeability actually matters.**
- **Finding 36's file-selection axis went eight-deep in one day** (`-30` … `-30h`). mtime and lexical order still agree — **correct-by-luck for an eighth consecutive hour.** Recorded as **accumulating exposure, not accumulating safety.**
