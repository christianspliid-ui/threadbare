# User Action Required

**Last updated:** 2026-08-10 15:58 local (2026-08-10 13:58 UTC), by the hourly `keep-work-flowing-cc` CC task.

> **FILE-HEALTH FLAG — STANDING, NOT RE-COUNTED. FOR THE RETRO, NOT FOR CHRISTIAN.** The count was retired at the fifth flag; restating the measurement each hour was itself the drift. **The standing fact:** standing asks start two-thirds of the way down their own file, everything above them is run-by-run self-narration the skill forbids accreting here, and **the structural fix has never been filed as a ticket** — every flag routed it to "a session" no lane is scheduled to run. **The retro files it or it does not happen:** asks to the top, narration to a sibling file or to `git log -p` on `ops`, hard cap on the per-run measurement block. **Runs since the fifth flag have held the line by writing no narration**, which caps growth without reversing it; this run is net-negative — it replaced its own measurement block in place and deleted a resolved ask rather than appending one.
>
> **Original flag, 2026-08-09 15:58Z:** This file was **912 lines / 237 KB**, and the `## Standing asks` section it exists to carry started at **line 620**. Everything above it is run-by-run self-narration that the skill explicitly forbids accreting here (*"Do not turn this into a retro. Deep impediment-log synthesis stays with the `retrospective` skill"*). **The asks are now the minority of their own file.** This run did **not** unilaterally delete the ~600 lines above them — that content was written deliberately by ~30 prior runs and destroying it in a scheduled pass is not a call a single unattended run should make — but the drift is real, it compounds hourly, and it wants a decision rather than another inch. **Recommended shape:** standing asks move to the top, everything else moves to a sibling file or to `git log -p`, and the per-run measurement block gets a hard cap. `Design/briefing.md` is regenerated whole each hour and does not have this problem; this file is append-mostly and does.

**THE ASK RE-RAISED LAST RUN AS "A TEN-SECOND CLICK ONLY HE CAN DO" WAS DONE IN UNDER TWO MINUTES, AND THAT IS THE MEASUREMENT WORTH KEEPING.** The 12:58Z brief re-raised `thr-1064-needs-close` after the previous run closed it into an empty route; the ping went out **13:00:57Z** and [THR-1064](https://linear.app/threadbare/issue/THR-1064) shows `completedAt` **13:02:53Z** — **116 seconds**. **The correction is what worked, not the ask**: the same item had already been "closed" once by routing it to a lane that could not perform it, and re-raising it as the literal manual act is what got it done. **In Dev is now empty for the first time this file has recorded** — the board reports nothing in progress, which is accurate rather than alarming. **Ask set falls seven → six; every one of the six is play or taste, and none of them blocks a ticket.** **The step-6 ping was DECLINED on a removal-only diff** (finding 70, below). Probes: `check:deploy` **`deployed`** at `2b714aa6` (equal to `origin/main` tip), `check:actions` **`healthy`**, `check:task-heartbeat` **`ok`** 9/9, `check:armed-prs` **`held`** (#1114 only, correctly silent under THR-985), `check:lane-silence` **`recovered`**, **declined for the twenty-fourth run** on Christian's 2026-08-08 answer *"overnight quiet is normal"*. Board: Ready for Dev **34 → 34**, In Dev **1 → 0**, park count **0**.

<details>
<summary>Previous run's lead (2026-08-10 09:58 UTC) — a decline that would have been a re-ask</summary>

**A DECLINE THAT WOULD HAVE BEEN A RE-ASK, CAUGHT BY THIS FILE RATHER THAN BY A PROBE.** `backlog-grooming-2026-08-09` (07:16Z) is inside the 36-hour fold window and its `## Needs Christian` section carries a live-sounding yes/no — *should the new cost-benefit rule demote the 13–17 pre-existing process tickets?* Step 2.6 read verbatim would have folded it in as this run's one new ask. **It is already answered:** Christian, Discord 08:45:04Z, *"its not either of these. it is from now on and existing tickets should be re-read and reprioritized in the light of a cost benefit analysis"* — recorded here as resolved item **2b** ninety minutes after the report was written. **The falsifier was this file's own `Resolved this period` section, not any probe**, and it is the seventh consecutive run to decline this same report: a dated sibling report is a snapshot, and the older it gets the more of it is already history. **Recorded as finding 70 — check a folded ask against the resolved list before the age window, because the window measures when the report was written and not when its questions died.** Ask set **holds at six**, membership and wording unchanged, so the digest is unchanged and **both gates go quiet: no publish, no ping.** THR-875 closed out its forty slot-1 tests and parked on a scope question that is an agent's call; THR-860 is parked on mechanics that are also an agent's; neither reaches him. Probes: `check:deploy` **`deployed`** at `89a03148` (equal to `origin/main` tip), `check:actions` **`healthy`**, `check:task-heartbeat` **`ok`** 9/9, `check:armed-prs` **`held`** (#1114 only, correctly silent under THR-985), `check:lane-silence` **`recovered`**, **declined for the twenty-second run** on Christian's 2026-08-06 answer. Inbox: `after=` cursor returned **literal `[]`, 3 bytes** — clean empty case, cursor unchanged, no receipt, no `## From Christian`. Board: Ready for Dev **38 → 34**, In Dev **2 → 2**, park count **2**, and **zero High-or-Urgent rows left in the ready pile** — the thin-shelf signal, reported as a queue line rather than an ask.

<details>
<summary>Previous run's lead (2026-08-09 15:58 UTC) — four asks closed by a ninety-second Discord window</summary>

**FOUR ASKS RESOLVED IN ONE RUN — THE LARGEST SINGLE-RUN CLOSE THIS FILE HAS RECORDED, AND ALL FOUR CLOSED BECAUSE CHRISTIAN ANSWERED FROM HIS PHONE IN A NINETY-SECOND WINDOW.** Three Discord messages at **15:11:14Z, 15:12:58Z and 15:13:20Z** carried four verdicts: *"go B for the four encounters."* → [THR-860](https://linear.app/threadbare/issue/THR-860) drops PR #1114 and re-authors under the locked format; *"1. loosen the rule"* → [THR-1062](https://linear.app/threadbare/issue/THR-1062) option 2, a reach trial need not name a value; *"2. flip the label"* → [THR-1064](https://linear.app/threadbare/issue/THR-1064) option 2, the label moves and the scenes stay; *"go with your recommendation a"* → [THR-886](https://linear.app/threadbare/issue/THR-886) option (a), the whisper that tilts. **Ask set falls ten → six, and for the first time since this file began, every remaining ask is play or taste — not one of them blocks another ticket.** **THE VERDICTS WERE RECORDED AS LINEAR COMMENTS, WHICH IS A DELIBERATE DEVIATION FROM THE TWO-FILE REMIT AND IS ARGUED RATHER THAN ASSUMED** — three of the four tickets carry a Done-when reading literally *"one of the three options is chosen and **recorded on this ticket**"*, so a verdict living only in a briefing file would leave them unfinishable by their own text; CLAUDE.md independently sanctions recording a human gate decision as a Linear comment; and the 2026-07-25 precedent that created step 0 is precisely two answers stranded in a channel nobody re-read. **A comment is not state** — no `save_issue`, no status write, no `Done`, and every comment says so in its own last line. **THE HOME-TREE WRITE STREAK ENDED AT 24 RUNS**: this session cut a worktree (`kwf-brief-1553`) and wrote both files there, so `Design/briefing.md`'s pointer stub on `main` was never modified and no capture-and-restore dance was needed. **Probes:** `check:deploy` **`deployed`** at `e9216ecb` (equal to `origin/main` tip), `check:actions` **`healthy`**, `check:task-heartbeat` **`ok`** 9/9, `check:armed-prs` **`held`** (#1114 only — and its hold is now decided rather than merely marked), `check:lane-silence` **`recovered`**, **declined for the twenty-first run** on Christian's 2026-08-06 answer. **Sibling folds contributed zero net items for a second consecutive run:** `orchestrator-2026-08-09g` raises only THR-907/THR-974/THR-986, all carried; `weekly-hygiene-2026-08-09` empty by its own words; `backlog-grooming-2026-08-09` (07:16Z) **declined for the sixth run and this time on hard grounds rather than judgement** — both its bullets died after it was written (THR-883 closed 08:45:02Z; `process-rule-retroactivity` was answered 08:45:04Z), so a verbatim fold would have re-raised two questions Christian had already settled. Board: Ready for Dev **37 → 38**, In Dev **3 → 2**, **park count 2, both of them un-parked by this run's answers.**

<details>
<summary>Previous run's lead (2026-08-09 14:53 UTC) — the park predicate that succeeded where it had failed an hour before</summary>

**THE PARK PREDICATE FOUND A PARK THIS RUN, ONE HOUR AFTER IT MISSED ONE — SAME PREDICATE, SAME FIELD, OPPOSITE RESULT, AND THAT IS THE POINT.** [THR-886](https://linear.app/threadbare/issue/THR-886) parked at 14:06Z with `assignee` genuinely null, so step 1's *In Dev ∧ assignee null* test selected it on the first pass. **Last run the identical test returned zero on THR-875, which was parked, said so in prose, and reported `assignee: "Christian Spliid"`** because [THR-1058](https://linear.app/threadbare/issue/THR-1058)'s reversion had rewritten the field. **Finding 68 said a predicate keyed on a field a known defect rewrites is a coin flip that fails silent toward "nothing to report"; this run is the coin landing the other way, which is confirmation rather than relief.** THR-875 reports null again this hour — the field healed on its own, which is worse than staying broken, because nothing in the payload distinguishes a healed field from one that was never touched. **Recorded as finding 69: a defect that intermittently self-corrects makes every clean read unfalsifiable, so the park comment must be read on every In-Dev row regardless of what `assignee` says.** The new ask is also this file's first sourced from **a park comment rather than a probe or a sibling report** — no probe emits it, and `tb-orchestrator` is forbidden from reading In Dev, so step 1's own scan was the only surface that could have carried it. **Ask set rises nine → ten**, the new key `thr-886-compulsion-card-shape`: a Haunt god's opening card, and Mind's signature, does nothing when played, and the two ways to wire it give the player materially different cards — **(a)** an unseen pull on the mortal's next decision, **(b)** an authored dream with a menu inside the encounter. **Recommended (a)** on the card's own printed line, *steer them, not the world*, and because a nudge card is already a paid choice. **Sibling folds contributed zero net items for the first time in five runs, by three different routes**: `orchestrator-2026-08-09f` **de-duplicated** (both its asks, THR-1062 and THR-1064, were folded last run and are carried verbatim); `weekly-hygiene-2026-08-09` **empty by its own words**; `backlog-grooming-2026-08-09` **declined for the fifth consecutive run** — its THR-883 bullet died at 08:45Z and its process-rule bullet is queue mechanics, an agent verdict under THR-608. Inbox: `after=` cursor returned **literal `[]`, 3 bytes** — the clean empty case, cursor unchanged, no receipt, no `## From Christian`. Probes: `check:deploy` **`deployed`** at `fff9c196` (equal to `origin/main` tip), `check:actions` **`healthy`**, `check:task-heartbeat` **`ok`** 9/9, `check:armed-prs` **`held`** (#1114 only, correctly silent), `check:lane-silence` **`recovered`**, **declined for the twentieth run** on Christian's direct answer. Board: Ready for Dev **40 → 37** (THR-841, THR-928 and THR-1054 completed), In Dev **2 → 3**, **park count 3 — the highest this file has recorded, and every one of them is waiting on Christian rather than stalled.**

<details>
<summary>Previous run's lead (2026-08-09 12:57 UTC) — the park detector a known defect had blinded</summary>

**A PARK ANNOUNCED ITSELF IN PROSE AND THE PARK DETECTOR COULD NOT SEE IT, BECAUSE THE FIELD THE DETECTOR KEYS ON HAD SILENTLY UNDONE ITSELF.** THR-875's closing comment at 12:29Z says, in its own words, *"Unassigning and leaving the issue In Dev — not because work remains for this lane, but because the only thing left is a scope decision that is yours."* **`list_issues` returns `assignee: "Christian Spliid"`.** Step 1's park predicate is *In Dev ∧ assignee null*, and it therefore returns **zero parks** on an issue that is parked, says so, and names the decision it is parked on. **That is THR-1058 — filed 08:48Z this morning as a hygiene finding about assignee reversion — reproducing live four hours later as a detection failure in this lane.** Recorded as **finding 68: a predicate keyed on a field that a known defect rewrites is not a detector, it is a coin flip, and it fails silent in the direction of "nothing to report."** The park was found by reading the issue's latest comment, which step 1 only instructs you to do *after* the predicate has already selected the issue. **Ask set rises seven → nine**, both new asks folded from `tb-orchestrator`'s 12:25Z report ([THR-1062](https://linear.app/threadbare/issue/THR-1062) slot-2 unconvertible, [THR-1064](https://linear.app/threadbare/issue/THR-1064) stone inverted) — **and both were filed 12:22Z by the executor working the very ticket this lane was watching**, which is the first time step 2.6's fold has carried items generated by in-flight work rather than by a sweep. **The two asks are one decision seen from two sides and were deliberately not merged**: THR-1062 is *what is a reach trial about*, THR-1064 is *what does the stone reach mean*, and collapsing them would hide a second question behind a first. **THR-875's "may I amend the Done-when" was declined as an ask and left agent-side** — scope bookkeeping is a technical verdict under THR-608, and the substance he actually has to rule on is THR-1062, which is already on the list. `ws5-batch-disposition` **carried unchanged**, its premise still expired and `check:armed-prs` still returning `held` for #1114 off a stale marker (finding 67 holds). Sibling folds: orchestrator **taken (both items)**; `backlog-grooming-2026-08-09` **declined for the fourth consecutive run** — the THR-883 bullet died at 08:45Z and the process-rule bullet is queue mechanics, an agent verdict under THR-608, not a design call; `weekly-hygiene-2026-08-09` **empty by its own words**. Inbox: `after=` cursor returned **one message, this lane's own 12:00Z ping**, bot author, logged and ignored per the finding-65 shape — no receipt, no `## From Christian`, cursor advanced. Probes: `check:deploy` **`deployed`** at `4178349c` (equal to `origin/main` tip), `check:actions` **`healthy`**, `check:task-heartbeat` **`ok`** 9/9, `check:armed-prs` **`held`** (#1114 only), `check:lane-silence` **`recovered`**, **declined for the nineteenth run** on Christian's direct answer. Board: Ready for Dev **40 → 40**, In Dev **2 → 2**, **park count 1 after manual correction, 0 as the predicate reported it.** Home tree on `main`, `main..origin/main` **0**, same two tracked settings files dirty, untracked **24**.

<details>
<summary>Previous run's lead (2026-08-09 11:56 UTC) — the disposition call whose premise had expired</summary>

**THE ASK SET GREW FOR THE FIRST TIME IN SEVEN RUNS, AND THE NEW ASK IS ONE THE PREVIOUS RUN HAD ALREADY SEEN AND DECLINED TO RAISE.** `tb-orchestrator`'s 13:31Z report asks for a **disposition call on the paused WS5 batch** now that THR-883 is Done: land [THR-860](https://linear.app/threadbare/issue/THR-860)'s four finished civic-seat templates as-is and retrofit them later, or drop them and let the Factory re-author them in the locked format. **The previous run held the same fact and routed it as a working-session tidy-up** — *"the pull request still carries the stale paused note; it needs removing by hand"* — which is finding 67: **a stale marker and a live decision look identical from the probe's side.** `check:armed-prs` returns `held` for #1114 either way, and THR-985 correctly forbids re-raising a decision already taken; what changed is that the decision's *premise* expired at 08:45Z, so it is no longer taken. **The tell was not in the probe payload but in a sibling report written by a lane that reads issue relations** — this lane reads merge state, the orchestrator reads what the merge was waiting for. **Folded verbatim in substance, re-worded into player terms** (four seats of office in a capital city, ten days finished-but-unlanded), with a stated recommendation of **(b) drop and re-author**, because the ten-day pause was bought precisely to stop gate-passing-but-wrong prose from shipping. **Ask set six → seven, so the step-6 ping fires** for the first time since 08:57Z. Sibling folds: orchestrator **taken**; `backlog-grooming-2026-08-09` **declined for the third consecutive run** on the same per-item liveness check (both bullets resolved 08:45Z, ~90min after the 07:16Z report); `weekly-hygiene-2026-08-09` **empty by its own words** and correctly not propagated. Inbox: `after=` cursor returned **literal `[]`, 3 bytes** — the clean empty case, cursor unchanged, no receipt, no `## From Christian`. Probes: `check:deploy` **`deployed`** at `0ac539fd` (equal to `origin/main` tip), `check:actions` **`healthy`**, `check:task-heartbeat` **`ok`** 9/9, `check:armed-prs` **`held`** (one open PR, down from two — #1364 cleared and merged), `check:lane-silence` **`recovered`**, **declined for the eighteenth run** on Christian's direct answer. Board: Ready for Dev **39 → 40**, In Dev **2 → 2** (THR-875 active and checkpointing hourly, THR-860 parked). Home tree on `main`, `main..origin/main` **0**, same two tracked settings files dirty, untracked **24**.

<details>
<summary>Narrative from the 2026-08-09 09:55Z run</summary>

**A QUIET RUN, AND THE ONE THING WORTH RECORDING IS THAT THE INBOX RETURNED A NON-EMPTY PAYLOAD CONTAINING NOTHING TO ACT ON.** The `after=` cursor returned **one message, 1750 bytes — this lane's own receipt from 08:57Z**, authored by the bot account and echoed back because the cursor sits behind everything including our own sends. **`non-empty` is not `actionable`**, and the author check is what separates them: `access.json` `allowFrom` lists one human id, the message's author was the bot, so it was logged and ignored exactly as an outsider's would be. **Recorded as finding 65** — the five previous runs all returned literal `[]` and made emptiness the implicit test; the first time a payload arrived, the naive reading would have been "Christian replied." **No receipt sent, no `## From Christian` section printed, cursor advanced past our own message.** Ask set **holds at six**, unchanged in membership and wording, **so the step-6 ping does not fire** — that gate hashes the Needs-Christian lines and they are identical. **The publish gate still returned `commit`, and predicting otherwise was this run's one error:** the digest is not only the ask keys, it also carries the probe verdicts, and `deploy` flipped `skipped` → `deployed` when `43471040` reached production. **Recorded as finding 66** — the two gates key on different things, an unchanged ask set implies a silent ping but never a skipped publish. **The daily-grooming fold was dropped for the second consecutive run, on the same liveness check**: both its `## Needs Christian` items — the process-rule retroactivity question and the THR-883 sitting — were resolved **~90 minutes after** the 07:16Z report was written. Probes: `check:deploy` **`deployed`** at `43471040` (equal to `origin/main` tip), `check:actions` **`healthy`**, `check:task-heartbeat` **`ok`** 9/9, `check:armed-prs` **`conflicted`** (#1364, now **94min — first crossing of the 90min escalation tier this lane has reported**, `escalated: true`, `abandoned: false`, still `needsSession` not `needsChristian`) + **`held`** (#1114, whose hold premise expired this morning), `check:lane-silence` **`recovered`**, **declined for the seventeenth run on Christian's direct answer**. Board: Ready for Dev **39 → 39**, In Dev **2 → 2**, **park count 0 for a sixteenth run**. Home tree on `main`, `main..origin/main` **0**, same two tracked settings files dirty, untracked **23**.

<details>
<summary>Previous run's lead (2026-08-09 08:57 UTC) — two asks closed in one hour, one by a reply that rejected the question</summary>

**TWO ASKS CLOSED IN ONE HOUR, ONE BY CHRISTIAN'S CHAT APPROVAL AND ONE BY HIS DISCORD REPLY — AND HIS REPLY REJECTED THE QUESTION'S FRAMING RATHER THAN PICKING A SIDE.** `thr-883-producer-gap`, this file's lead for two runs and the board's only `Urgent` row for ten days, **is resolved: THR-883 closed at 08:45:02Z** after he reviewed the amended spec and the Swollen Ford exemplar in chat and said *"looks fine."* **The producer gap closed by being filled, not by being filed** — PR #1363 merged at 08:29Z with both artifacts, which is precisely the ticket the previous five runs said nobody had written. **The lane was right that no ticket existed and wrong to infer nothing was scheduled**: the work was already in flight on a branch, and no board-shaped query this lane runs can see that. **Recorded as finding 64 — the twin of finding 60**, which said a state check cannot see a decision landing as prose; this says an absence-of-ticket check cannot see work landing as a PR. **`process-rule-retroactivity` is answered, and the answer was neither of the two options offered.** Christian, Discord 08:45:04Z: *"your choice is a false dichotomy. its not either of these. it is from now on and existing tickets should be re-read and reprioritized in the light of a cost benefit analysis"* — the rule binds forward **and** the existing pile gets re-read on merit rather than demoted on age. **Half of that is a directive beyond this lane's remit and was routed, not executed.** Ask set falls **eight → six**; digest changes, step-6 ping fires. Probes: `check:deploy` **`skipped`** (benign — nothing build-relevant since `5612b74d`), `check:actions` **`healthy`**, `check:task-heartbeat` **`ok`** 9/9, `check:armed-prs` **`conflicted`** (#1364, 34min, below the 90min tier — plus a **red required check**, the THR-1020 field this lane has never yet had to report) + **`held`** (#1114, now stale as prose), `check:lane-silence` **`recovered`**, **declined for the sixteenth run on Christian's direct answer**. Board: Ready for Dev **34 → 39**, In Dev **1 → 2**, **park count 0 for a fifteenth run**. Home tree on `main`, `main..origin/main` **0**, same two tracked settings files dirty, untracked **23**.

<details>
<summary>Previous run's lead (2026-08-09 07:56 UTC) — the sibling lane that declined a rule and routed the question</summary>

**A SIBLING LANE DECLINED TO OBEY A RULE CHRISTIAN WROTE YESTERDAY, DID THE SAFE THING, AND ROUTED THE QUESTION — AND THAT ROUTING IS THE ONLY REASON IT REACHED HIM.** `daily-backlog-grooming` ran at 07:16Z and found that CLAUDE.md's new demotion clause (*"a process ticket without [a cost/benefit line] is demoted to the log by grooming"*), applied literally, **demotes 13–17 of the 34 Ready-for-Dev items in one automated pass**, because every process ticket on the board predates the rule. **It demoted nothing**, treated the clause as binding from its 2026-08-08 ratification, left the older set to the weekly retro — which the amendment itself assigns that job — and wrote the open question onto THR-871 rather than filing a process ticket about the process-ticket rule. **This lane read that section from `origin/ops` and folded it in verbatim per step 2.6**; without step 2.6 the ruling would have sat in a dated ops file nobody opens. **`process-rule-retroactivity` enters the digest as the run's only membership change** — the eighth key — and it is deliberately framed so silence is safe: "from now on" is what already happened, so a non-answer costs nothing. **`thr-883-producer-gap` holds the lead on duration alone**, now fourteen hours and five work-picking rounds, re-verified this run by a fresh board search rather than carried. Ask set rises **seven → eight**; digest changes, step-6 ping fires. Probes: `check:deploy` **`deployed`** at `13be45f9` (sha compared against `origin/main` tip, equal), `check:actions` **`healthy`**, `check:task-heartbeat` **`ok`** 9/9, `check:armed-prs` **`held`** (#1114 only, correctly silent), `check:lane-silence` **`recovered`**, **declined for the fifteenth run on Christian's direct answer**. Board: Ready for Dev **35 → 34**, In Dev **2 → 1**, **park count 0 for a fourteenth run**. Home tree on `main`, `main..origin/main` **0**, same two tracked settings files dirty, untracked **23**.

<details>
<summary>Previous run's lead (2026-08-09 06:55 UTC) — the bulk field edit that faked eleven completions</summary>

**A BULK FIELD EDIT MADE ELEVEN TICKETS LOOK LIKE THEY SHIPPED OVERNIGHT, AND THE ONLY THING THAT CAUGHT IT WAS CHECKING ONE `completedAt` INSTEAD OF TRUSTING ELEVEN `updatedAt`s.** The Done query returned eleven rows stamped `2026-08-09T06:45:44Z` and `06:24:05Z` — identical to the second, across unrelated projects — including **THR-489, "get `npm test` green on `main`"**, which would have been the single most reportable line of the night. **`get_issue` returns `completedAt: 2026-06-30T06:07:16Z`.** The cluster is a bulk project/label sweep; nothing in it finished overnight. **`git log origin/main --since` is the falsifier and it returned exactly two non-merge commits** (THR-1044, THR-1045), which is what the brief reports. **The tell was the timestamp collision, not the content** — eleven tickets do not complete in the same second, and a "What's moving" section built from `updatedAt` would have been eleven-twelfths fiction while every row in it was individually real. **`thr-883-producer-gap` survived a third and fourth sweep and is now thirteen hours old**, promoting it from watch-item to the brief's lead ask. **Ask 1's blemish decision retired by delivery** — three of the five player-facing blemishes were promoted into `Ready for Dev` overnight without being asked for, so the play-now-or-wait trade-off he was holding no longer has two sides. Ask set falls **eight → seven**; digest changes, step-6 ping fires. Probes: `check:deploy` **`deployed`** at `2a95dad0`, `check:actions` **`healthy`**, `check:task-heartbeat` **`ok`** 9/9, `check:armed-prs` **`held`** (#1114 only, correctly silent), `check:lane-silence` **`recovered`**, **declined for the fourteenth run on Christian's direct answer**. Board: Ready for Dev **36 → 35**, In Dev **1 → 2**, **park count 0 for a thirteenth run**. Home tree on `main`, `main..origin/main` **0**, same two tracked settings files.

<details>
<summary>Previous run's lead (2026-08-08 19:58 UTC) — the 119-minute ask retired by delivery</summary>

**AN ASK RAISED TWO RUNS AGO WAS RESOLVED BY THE DELIVERY MACHINE INSIDE TWO HOURS, AND THE CORRECT RESPONSE WAS TO WITHDRAW A HOLD, NOT TO ANNOUNCE A FIX.** `thr-1042-consequence-suppression` entered the ask set at 17:59Z as a caveat on standing ask 8 — *the ending screen keeps the authored half of what happened and discards the world's half, so hold the consequence session or play it discounted.* **It was claimed at 19:03Z, shipped at 19:23Z, merged at 19:32Z and was live on the deployed sha by this run** — 119 minutes from ask to lift. **The ask is retired by delivery, and the thing carried to Christian is the withdrawal of the hold**, because the hold is the only part of it he ever had to act on. **Verified rather than taken from the merge subject:** `check:deploy` returns `deployedSha: d856090d`, equal to `origin/main`'s tip, and `d856090d` **is** the PR #1358 merge. **The ticket's own closing comment supplied the falsifier this file would otherwise have had to run** — the fix was browser-verified on `enc.letters_of_introduction` (4 → 9 consequence chips), **not** on the five slice templates, and the brief states that limit rather than implying the five were re-checked. **`thr-883-collapsed-to-review` retired on schedule** after its declared one-run life, replaced by `thr-883-producer-gap` — the same subject, but the half he can act on. Ask set falls **nine → eight**; digest changes, step-6 ping fires. Probes: `check:deploy` **`deployed`** at `d856090d`, `check:actions` **`healthy`**, `check:task-heartbeat` **`ok`** 9/9, `check:armed-prs` **`held`** (#1114 only), `check:lane-silence` **`recovered`**, **declined for the thirteenth run on Christian's direct answer**. Board: Ready for Dev **36 → 36**, In Dev **1 → 1**, **park count 0 for a twelfth run**. Home tree on `main`, `main..origin/main` **0**, same two tracked settings files dirty, untracked **20**.

<details>
<summary>Previous run's lead (2026-08-08 18:55 UTC) — the THR-883 collapse found in git log</summary>

**THE FILE'S LONGEST-RUNNING ASK WAS RESOLVED BY CHRISTIAN IN A CHANNEL THIS LANE DOES NOT READ, AND IT WAS FOUND BY READING `git log`, NOT THE BOARD.** Standing ask 2 (THR-883, the encounter-writing format sitting) has led every brief for a week and was re-pulled from Linear this run exactly as always: `In Design`, `Urgent`, `updatedAt` 17:57Z, description **unchanged** and still describing an interactive design session. **Every board-shaped check said the ask was live.** What actually happened is recorded only in commit `0124ee79` (PR #1356, merged 19:59 local): *"Christian, chat 2026-08-08: batch size 6 … THR-883 becomes a chat review of two Fable-drafted artifacts."* **Eight rulings, all settled, all in a plan-doc commit body.** The sitting collapsed ~56 minutes before this run and the Linear row does not know. **The generalisable failure is that ask 2's recheck has always been a *state* check — is the row still `Urgent`, still `In Design` — and a state check cannot see a decision that lands as prose in a docs PR.** The recheck that found it was reading the hour's merges for the What's-moving section, which is a different question asked for a different reason. **Recorded as finding 60.** Ask set falls **ten → nine** (`thr-883-fable-format-lock` retires; `thr-883-collapsed-to-review` enters as an informational one-run item). Probes: `check:deploy` **`deployed`** at `41ac12fa` (sha compared against `origin/main` tip, equal), `check:actions` **`healthy`**, `check:task-heartbeat` **`ok`** 9/9, `check:armed-prs` **`held`** (#1114 only), `check:lane-silence` **`recovered`**, **declined for the twelfth run on Christian's direct answer**. Board: Ready for Dev **34 → 36**, In Dev **2 → 1**, **park count 0 for an eleventh run**. Home tree on `main`, `main..origin/main` **3**, same two tracked settings files dirty, untracked **20**.

<details>
<summary>Previous run's lead (2026-08-08 17:59 UTC) — the THR-1042 consequence-session caveat</summary>

**A NEWLY-FILED DEFECT WAS CHECKED AGAINST THE EXACT CONTENT CHRISTIAN IS ABOUT TO PLAY, AND THAT CHECK IS WHAT MADE IT AN ASK RATHER THAN A QUEUE LINE.** THR-1042 arrived in `Ready for Dev` at 17:18Z off the THR-1039 composition audit, described in engine terms (*"aftermathConfig suppresses engine-derived changes wholesale"*). **Read as a board row it is one more `High` in a pile of them.** Read against the standing ask set it is the thing THR-974's verdict is *about*: the ticket's own text says it is *"the exact silent-consequence defect the consequence verdict exists to judge."* **Three checks turned that from a plausible-sounding worry into a statable one:** (1) `buildUnifiedEncounterStageModel.ts:435` — `authoredVariant?.changes ?? summary.changes`, so an authored variant replaces rather than merges; (2) `unifiedActionResolution.ts:2418-2420` — the **engine** builds `summary.changes` as `[...engineChanges, ...variant.changes]`, so the merge exists upstream and the UI discards half of it; (3) all five slice templates in `vertical-slice.ts` declare `fallback.changes` **and** `byOutcome.*.changes`, so the authored path is live on every encounter in the verdict session. **The falsifier was run too:** `rewardPool` appears **0 times** in `vertical-slice.ts`, so the ticket's headline symptom (*a rolled prize vanishes*) **cannot occur on these five** — the loss is growth and standing, and the brief says so rather than borrowing the ticket's more dramatic framing. Ask set rises **nine → ten** (`thr-1042-consequence-suppression`); digest changes, step-6 ping fires. Probes: `check:deploy` **`skipped`** (benign — nothing build-relevant since `e329caa2`), `check:actions` **`healthy`**, `check:task-heartbeat` **`ok`** 9/9, `check:armed-prs` **`conflicted`** (#1354, 32min, below the 90min escalation tier) + **`held`** (#1114), `check:lane-silence` **`recovered`**, **declined for the eleventh run on Christian's direct answer**. Board: Ready for Dev **32 → 34**, In Dev **1 → 2**, **park count 0 for a tenth run**. **THR-883 remains the board's only Urgent row.** Home tree on `main`, `main..origin/main` **0**, same two tracked settings files dirty, untracked **20**.

</details>

</details>

</details>

</details>

</details>

</details>

</details>

</details>

</details>

</details>

</details>

## This run's measurements (2026-08-09 19:00 UTC)

| Probe | Verdict | Note |
|---|---|---|
| `check:deploy` | `deployed` | `89a03148`, equal to `origin/main` tip |
| `check:actions` | `healthy` | 0 startup failures, 0 stalled |
| `check:task-heartbeat` | `ok` | 9/9 enabled tasks within 2 slots |
| `check:armed-prs` | `held` | #1114 only; `holdReason` present, correctly silent (THR-985) |
| `check:lane-silence` | `recovered` | worst uncovered gap 14.4h (08-06 20:49 → 08-07 11:14); declined, 22nd run |
| `check:substantive` | see below | digest unchanged → publish skipped |

Board: Ready for Dev **34** (31 `Low`, 3 `Medium`, **0 High/Urgent**), In Dev **2**, both parked. Home tree on `main`, `main..origin/main` **0**, `UNIQUE` **0**, tracked-dirty **2** (the two settings files), untracked **24**. Reaper last ran 20:40 local — 47 worktrees, 63 branches, 2 needing disposition. Inbox `after=` cursor returned literal `[]` (3 bytes); cursor unchanged at `1536042539486814389`.

### Measurements from the 2026-08-09 15:58Z run

- **THE INBOUND CHANNEL PAID FOR ITSELF AT ITS DESIGNED RATE FOR THE FIRST TIME.** Step 0 exists because two answers sat unread for eleven hours on 2026-08-25. This run the cursor returned **four messages, 2459 bytes**, three of them Christian's, carrying **four verdicts against four standing asks** — a 100% hit rate on the ask set, answered 45 minutes after the brief that posed the last of them. **The measurement worth keeping is not the count but the latency**: 15:01Z brief → 15:13Z answer → 15:58Z recorded. **No ask in this file has ever gone from raised to recorded inside one hour before**, and two of the four (THR-1062, THR-1064) were filed by an executor only three hours earlier.
- **THE REMIT QUESTION WAS FACED RATHER THAN ROUTED AROUND, AND THE ANSWER WAS TO WRITE TO LINEAR.** The step-0 table says to record a verdict in *this file*; three of the four tickets say their Done-when is met only when the choice is *recorded on the ticket*. **Following the table alone would have satisfied the letter of the skill and left three tickets unfinishable.** The deviation is bounded on purpose — comments only, no state, each one self-labelling as PM-lane provenance — and it is the same reasoning CLAUDE.md already applies to chat-approved human gates. **Recorded as finding 70: when a skill's recording surface and a ticket's completion contract disagree, the ticket's contract is the binding one**, because that is the surface the next executor reads.
- **A VERDICT WAS EXPANDED TO ITS TICKET'S OPTION NUMBERS RATHER THAN QUOTED AND LEFT AMBIGUOUS.** *"1. loosen the rule"* and *"2. flip the label"* are numbered against the **brief's** framing, not against the tickets' own three-option lists, and in both cases the brief's recommendation maps to the ticket's **option 2** — a coincidence that would read as a citation if left unexplained. **Each comment therefore quotes him verbatim, restates the brief's framing he was answering, and then names which numbered ticket option that is**, with the two rejected options listed explicitly. **The failure mode avoided is an executor reading "option 1" off a message numbered for a different list.**
- **ONE VERDICT WAS READ AS AN ACCEPTANCE, NOT A SELECTION, AND THE DIFFERENCE IS RECORDED IN THE COMMENT.** THR-1064's recommendation carried a caveat — *"unless stone was always meant to be about changing or keeping a thing"* — which is the reading the ticket's option 3 serves. **He did not take the caveat**, so option 3 is out; but the comment says the verdict settles *which side moves* (label, not content) and **not** what the pair is finally called, and routes the naming through a `UL-proposal`. **A verdict answers what it was asked and no more** — durable finding 25's shape applied to a director decision rather than to a test.
- **THE FOUR ANSWERS UN-PARKED BOTH IN-DEV ROWS, WHICH IS THE FIRST TIME THIS FILE'S ASKS AND THE BOARD'S PARKS HAVE CLEARED TOGETHER.** THR-875 was parked solely on the slot-2 question (now answered, and its residual Done-when amendment is agent-side scope bookkeeping under THR-608); THR-860 was parked on the disposition question (now answered). **Park count 2 → 0 in substance, though both rows still read In Dev** until a working session acts. **The brief says so explicitly rather than reporting "2 parked"**, because the number without the cause would read as congestion.
- **THE STALE-SIBLING DECLINE WAS FOR ONCE PROVABLE RATHER THAN JUDGED.** `backlog-grooming-2026-08-09` is 8.6 h old — comfortably inside the 36 h window, so the age check passes it. **Both its bullets were falsified by events after it was written**: THR-883 closed 08:45:02Z and `process-rule-retroactivity` was answered 08:45:04Z, 89 minutes after the report. **A fold obeying step 2.6 literally would have re-raised two settled questions**, one of which Christian had answered personally. **Sixth consecutive decline of this report and the first backed by timestamps instead of per-item liveness judgement** — the age window is necessary and demonstrably not sufficient.
- **THE LANE-SILENCE DECLINE HELD FOR A TWENTY-FIRST RUN, AND THIS RUN ADDS THE SHAPE ARGUMENT TO THE AUTHORITY ONE.** The grounds have always been Christian's 2026-08-06 answer. **This run also notes the three reported gaps are 20:49→11:13, 20:32→06:04 and 22:28→07:06 on three consecutive nights** — a nightly overnight pattern, not an outage, and the pause marker has no vocabulary for "expected every night". **The brief states the decline visibly instead of silently suppressing it**, so a reader who checks the probe payload and sees `needsChristian: true` can find out why it never reached them.
- **THE HOME-TREE WRITE WAS AVOIDED BY CUTTING A WORKTREE, ENDING A 24-RUN STREAK THE PREVIOUS RUN CALLED UNAVOIDABLE.** Prior runs wrote `Design/briefing.md` in place — a tracked pointer stub on `main`, i.e. the exact THR-937 shape — then restored the stub bytes by hand. **`EnterWorktree` was available the whole time**; last run's note said the remedy "remains unavailable to a session that cannot cut one", which was true of the *technique* it had in mind (`git worktree add` against the home tree) and not of the harness tool. **Recorded as finding 71: a constraint restated across two dozen runs is worth re-testing once against the tool list, not only against the reasoning that produced it.** Home tree ends this run exactly as it started: on `main`, 0 behind, same two tracked settings files dirty, 24 untracked.
- **THE SAME PREDICATE THAT FAILED LAST HOUR SUCCEEDED THIS HOUR, AND BOTH ROWS ARE STILL ON THE BOARD — FINDING 69.** Step 1's park test is *In Dev ∧ assignee null*. **THR-886 returned null and was selected; THR-875 returned null too, having returned `"Christian Spliid"` one hour earlier while parked.** Nothing was done to it in between by this lane. **A defect that intermittently self-corrects is worse for a detector than one that fails consistently**, because a clean read is then unfalsifiable — there is no field in the payload that separates "never corrupted" from "corrupted and healed". **The remedy is not a better predicate but a cheaper confirmation: read the latest comment on every In-Dev row, not only on rows the predicate already selected.** Three rows is one `list_comments` call apiece; the skill currently orders it the other way round. **Wants a ticket, and [THR-1058](https://linear.app/threadbare/issue/THR-1058) is the wrong home for it** — that ticket fixes the write, this is about not trusting the read.
- **THE NEW ASK CAME FROM A PARK COMMENT, WHICH IS A SOURCE THIS FILE HAS NOT USED BEFORE.** Every prior ask arrived from a probe payload, a sibling report's `## Needs Christian` section, or an inbound message. **THR-886's design fork exists only in a Linear comment on an In-Dev issue** — no probe emits it, and `tb-orchestrator` reads `Todo` and `Ready for Dev` only and is forbidden from touching In Dev. **Step 1's park scan was the sole surface that could have carried it**, which is exactly the gap THR-846 opened this slot for. **First live confirmation that the THR-846 slot catches something no other lane sees**, rather than merely restating rows the orchestrator already reported.
- **THE PARK COMMENT WAS AUTHORED BY AN AGENT AND REPORTS AS CHRISTIAN.** `list_comments` returns `author.name: "Christian Spliid"` on a comment whose own text reads *"Claimed this run, then parked without writing code"* and *"Unassigning so the WIP slot stays free."* **The API actor is shared, so authorship on this board is not evidence of who wrote something** — the same root as THR-1058 and THR-845, surfacing on a different field. **Recorded because the failure mode is inviting**: a lane that trusted `author.name` would have read this as a directive from Christian and folded it into `## From Christian` unquestioned, which is a fabricated inbound. **The channel-authentication discipline of step 0 has no equivalent on Linear, and this is the first run where that mattered.**
- **THE SIBLING FOLD CONTRIBUTED ZERO NET ITEMS BY THREE DIFFERENT ROUTES AT ONCE.** `orchestrator-2026-08-09f` (14:26Z) is **inside the window, live, and entirely duplicative** — both its asks were folded last run and are carried here verbatim, so the correct action is de-duplication, not a second copy. `weekly-hygiene-2026-08-09` **says in terms that nothing needs him**, which step 2.6 forbids propagating. `backlog-grooming-2026-08-09` (07:16Z) is **declined for the fifth consecutive run** on per-item liveness. **Three reports, three different reasons to contribute nothing, none of them derivable from the 36-hour age check alone** — the mirror image of last run, where the same three branches each fired positively.
- **TWO ENGINE FIXES SHIPPED IN THE SAME TWO HOURS AND THEY ARE THE SAME BUG TWICE.** [THR-841](https://linear.app/threadbare/issue/THR-841) — region conditions read an edge no location carries, so every authored region gate matched nothing. [THR-928](https://linear.app/threadbare/issue/THR-928) — `forecast_computed` was emitted through a double cast into a category the registry does not contain, the shape that had already hidden THR-924. **Both are "a rule that was written, reads correctly, and silently matches nothing", and both were found by a gate rather than by a player.** Reported in the brief as one family rather than two tickets, because the shared shape is the reportable part.
- **THE `held` VERDICT ON #1114 IS NOW CORRECT FOR A REASON IT WAS NOT CORRECT FOR YESTERDAY.** Finding 67 recorded the hold outliving its cause: the marker names THR-883, which closed at 08:45Z. **The underlying disposition question has since been raised as an ask in its own right** (`ws5-batch-disposition`, carried), so the PR is now genuinely held pending a decision rather than held on a dead marker. **The probe payload is unchanged across that transition** — same `holdReason`, same `needsChristian: false` — which is finding 67 restated from the other side: the JSON was right by accident yesterday and is right on purpose today, and nothing in it records the difference.
- **THE BOARD SHRANK FOR THE FIRST TIME IN FOUR RUNS AND ALL THREE DEPARTURES WERE COMPLETIONS.** Ready for Dev **40 → 37** (THR-841, THR-928, THR-1054 to Done), In Dev **2 → 3**. **Park count 3, the highest recorded here**, and the brief says explicitly that this is not a stall: each park was unassigned deliberately to free the WIP slot, and each is waiting on a decision rather than on capacity. **The number that would normally read as congestion is the queue working as designed**, which is worth stating because a future run seeing "3 parked" without that context would reasonably escalate it.
- **THE BRIEFING WAS WRITTEN IN THE HOME TREE AGAIN, BY THE SAME CONSTRAINT AS LAST RUN.** This session was dispatched with the home tree as CWD and no worktree, so `Design/briefing.md` — **tracked on `main` as a pointer stub** — was written in place, producing the THR-937 tracked-modification shape. **The stub bytes were captured before the write and restored by plain file write immediately after publishing**, not by `git restore`, so no git state op ran against the home tree (THR-672). **Twenty-fourth consecutive run without one.** The "cut a worktree from `main`" remedy remains unavailable to a session that cannot cut one.

### Measurements from the 2026-08-09 11:56Z run

- **A STALE HOLD MARKER AND A LIVE DECISION ARE INDISTINGUISHABLE FROM THE PROBE'S SIDE — FINDING 67.** `check:armed-prs` returns `held` for #1114 with `holdReason: "THR-883 — content migration is paused until the encounter-writing format is locked."` **That reason expired at 08:45:02Z and the payload is byte-identical before and after.** The previous run *recorded* the expiry — it is written into this file as "the first observed case of a hold outliving its cause" — and still routed the item as a working-session tidy-up rather than an ask, because the probe's `needsChristian: false` was taken as covering both readings. **It covers only one.** THR-985 says a decision already taken is not an open question; it says nothing about a decision whose premise has since dissolved, and those are the same JSON. **What surfaced it was a sibling lane reading a different field**: `tb-orchestrator` reads issue relations and asked the disposition question outright; this lane reads merge state and could not have derived it. **Fix shape: when `holdReason` names a ticket, re-pull that ticket's status and treat `Done` as demoting `held` → `needsSession` at minimum.** One `get_issue` per held PR. **Wants a ticket.**
- **THE INBOX RETURNED THE CLEAN EMPTY CASE, WHICH IS FINDING 65'S CONTROL ARM.** Literal `[]`, **3 bytes**, cursor `1535936320910917663` unchanged. Last run's payload was our own 08:57Z receipt echoed back, and the rule extracted there — *`non-empty` is not `actionable`, the author check is what separates them* — is unexercised this run in the interesting direction. **Recorded anyway, because the pair is what makes the byte counts a usable signal**: 3 bytes is empty, ~1750 is one message, and neither number tells you who wrote it.
- **THE SIBLING FOLD SPLIT THREE WAYS THIS RUN, WHICH IS THE FIRST TIME EACH BRANCH FIRED AT ONCE.** `orchestrator-2026-08-09e` (13:31Z) **taken** — its disposition ask is live and no other surface carries it. `backlog-grooming-2026-08-09` (07:16Z) **declined for the third consecutive run**, both bullets resolved 08:45Z, ~90 minutes after it was written. `weekly-hygiene-2026-08-09` (10:50Z) **skipped as an empty state** — it says in terms that nothing needs him and names THR-883's completion as context requiring no decision, which step 2.6 forbids propagating as an item. **Three reports inside the 36h window, three different correct answers, none of them derivable from the age check.**
- **THE FOLD WAS RE-WORDED AND THE SKILL SAYS NOT TO.** Step 2.6 says fold verbatim, on the sound reasoning that re-wording turns a specific ask into a vague one. **The orchestrator's item names THR-883, THR-1047, THR-973 and nine ticket ids in one sentence** — correct for an agent reader and a THR-608 violation for Christian, who does not read Linear. **The substance is preserved exactly (two options, ten days, four templates) and only the referents changed** (ticket ids → "four seats of office in a capital city"). **Recorded as a deliberate deviation, not an oversight**, because the two rules genuinely conflict when the raising lane's audience is agents and the consuming lane's audience is not — and THR-608 is the older and more load-bearing of the two. **Wants a ticket: step 2.6 should say "verbatim in substance, translated for the reader".**
- **A RECOMMENDATION WAS ATTACHED TO THE NEW ASK RATHER THAN PRESENTING THE FORK BARE.** The orchestrator raised it without one. **This file's own standing advice is that silence should be safe**, and neither option here is safe under silence — the templates stay unlanded either way, so "do nothing" has a cost. **(b) drop and re-author was recommended on the ticket's own history**: the ten-day pause was bought specifically to stop prose that passed every automated gate from shipping, and landing four pre-lock templates spends that purchase for four encounters. **Flagged because the previous run's Discord answer rejected a binary framing outright** (*"your choice is a false dichotomy"*), so a third reading is live here too and the wording invites one.
- **#1364 CLEARED WITHOUT ESCALATION AND THE PREVIOUS RUN'S TIER CROSSING WAS THE LAST WORD ON IT.** It crossed `ARMED_DIRTY_ESCALATE_MINUTES` at 94 minutes with both a conflict and a red gate, was routed as `needsSession` not `needsChristian`, and merged as `0ac539fd`. **Open PR count 2 → 1.** **The routing call is retrospectively confirmed rather than merely unpunished** — a session cleared it, which is what `needsSession` asserts.
- **THE BOARD MOVED FOR THE FIRST TIME IN THREE RUNS AND THE MOVEMENT IS ENTIRELY ONE TICKET.** Ready for Dev **39 → 40**, In Dev **2 → 2**. THR-875 converted **10 → 20** templates in the hour, shipping PR #1369, and is the only lane doing product work; the +1 on the shelf is filing, not throughput. **Park count 1** — THR-860, unchanged, and now the subject of the new ask rather than a silent row.
- **THE BRIEFING WAS WRITTEN IN THE HOME TREE BECAUSE THIS SESSION HAS NO WORKTREE, WHICH REVERSES LAST RUN'S REMEDY.** The previous run cut `kwf-briefing-2026-08-09-0955` on this file's own standing finding; this session was dispatched with the home tree as CWD and no worktree, so `Design/briefing.md` — **tracked on `main` as a pointer stub** — was written in place, producing exactly the THR-937 tracked-modification shape. **The stub bytes were restored from `HEAD` by plain file write immediately after publishing**, not by `git restore`, so no git state op ran against the home tree (THR-672). **Twenty-third consecutive run without one.** **The window is real and the remedy is not available to a session that cannot cut a worktree** — recorded so the "cut from `main`" finding is not read as unconditionally applicable.

<details>
<summary>Measurements from the 2026-08-09 09:55Z run</summary>

- **THE INBOX RETURNED 1750 BYTES AND ZERO ACTIONABLE MESSAGES — FINDING 65.** Five consecutive prior runs returned literal `[]`, which quietly trained the emptiness itself as the test. **This run the cursor returned one message: our own 08:57Z receipt**, author id `1530180014165856296`, `bot: true`. **`allowFrom` carries exactly one id and it is Christian's**, so the bot's message was logged and ignored by the same rule that would reject a stranger's. **The generalisable point is that the `after=` cursor does not skip your own sends**, so a lane that reads "payload non-empty → Christian replied" will fabricate an inbound roughly once per run in which it spoke. **The author check is not a formality on this channel; it is the only thing standing between a self-authored echo and a hallucinated directive.**
- **THE TWO GATES KEY ON DIFFERENT THINGS AND THIS RUN CONFLATED THEM — FINDING 66.** The ask set held at six with identical wording, and the inference drawn was *"digest unchanged, therefore the publish gate skips."* **`check:substantive` returned `commit`, reason `digest-changed`**, because its projection is `needsChristian:<keys>|deploy=…,freshness=…,queue=…,tasks=…` and `deploy` had flipped `skipped` → `deployed` when `43471040` reached production. **The step-6 ping gate is the one that hashes only the Needs-Christian lines**, and it correctly stays silent. **The error was writing the prediction into the file before running the probe** — the same shape as every finding here about trusting a derived signal over the direct one, committed by the lane that records them. **Corrected in place rather than left standing**, and the rule extracted is: an unchanged ask set predicts a silent ping and predicts nothing at all about publishing.
- **THE SIBLING FOLD WAS DROPPED FOR THE SECOND CONSECUTIVE RUN, ON THE SAME CHECK AND WITH A LARGER MARGIN.** `backlog-grooming-2026-08-09.md` (07:16Z) carries two `## Needs Christian` bullets: the process-rule retroactivity question and *"the encounter-format sitting is still the largest single blocker, tenth day."* **Both were resolved at 08:45Z**, ~90 minutes after the report was written and ~70 before this run. **Folding either verbatim would have asked Christian to decide something he had already decided** — the THR-985 defect, from a different direction than usual. **Step 2.6's age window (36h) is not sufficient on its own**: a report can be under two hours old and still entirely stale, so liveness is checked per item against the live board, never inferred from the filename.
- **#1364 CROSSED AN ESCALATION TIER AND WAS STILL NOT ROUTED TO CHRISTIAN.** `check:armed-prs` returns `ageMinutes: 94` against `ARMED_DIRTY_ESCALATE_MINUTES` (90), so `escalated: true` for the first time in this file's history; `abandoned` stays `false` against the 12-hour tier. **The payload also carries `checkConclusion: "failing"` — the THR-1020 field, read for the second run running.** Both a merge conflict and a red gate are technical verdicts THR-608 leaves with the agent, so it is one Freshness line naming the PR, not an ask. **The tier crossing changes the urgency of the sentence, not its addressee.**
- **#1114's HOLD IS STILL HONOURED BUT ITS PREMISE EXPIRED THIS MORNING.** The marker reads *"THR-883 — content migration is paused until the encounter-writing format is locked."* **THR-883 closed at 08:45Z**, so the stated reason no longer obtains, and the probe — which reads the marker, not the world — correctly still returns `held`. **The right response is not to raise it to Christian** (THR-985: a decision already taken is not an open question) but to name the stale marker as a working-session tidy-up in `## Queue`. **Recorded because this is the first observed case of a hold outliving its cause**, and the fail-soft runs the safe way: a stale marker suppresses an ask, it does not manufacture one.
- **THE BOARD DID NOT MOVE AND THE BRIEF SAYS SO IN THE FIRST SENTENCE.** Ready for Dev **39 → 39**, In Dev **2 → 2**, both owned. **The temptation on a flat hour is to find something to report**; what actually happened is that the twelve tickets released at 08:45Z are draining through the promotion lane on their own, so the brief leads on "nothing new, nothing wrong" and puts the drain under `## Queue` as progress rather than as news.
- **THE WORKTREE WAS CUT FROM `main`, ON THIS FILE'S OWN STANDING FINDING.** `Design/briefing.md` is **tracked** on `main` as a pointer stub, so writing it in the home tree produces a tracked modification — the THR-937 shape that stops autosync dead. **The previous run wrote into the home tree and restored the stub bytes afterwards**, which works but leaves a window; this run cut `kwf-briefing-2026-08-09-0955` instead. **NO GIT STATE OP RAN AGAINST THE HOME TREE** (THR-672); access was read-only `git -C` queries, file reads, and npm probes. **Twenty-second consecutive run without a home-tree state op.**

<details>
<summary>Measurements from the 2026-08-09 08:57Z run</summary>

- **AN ABSENCE THIS FILE RE-VERIFIED EVERY HOUR FOR FIVE RUNS WAS TRUE EVERY TIME AND STILL PRODUCED A WRONG CONCLUSION — FINDING 64.** `thr-883-producer-gap` asserted that no ticket existed to write the two artifacts THR-883's review consumes. **That assertion was correct at every check** — `list_issues state:"In Design"` really did return only THR-883 and THR-1043, and no filed row was the drafting job. **The inference drawn from it was that nothing was scheduled, and that was false**: the work was in flight on branch `worktree-thr-883-contract-spec-exemplar`, merged as PR #1363 at 08:29Z, ~30 minutes before the ticket closed. **The re-verification discipline worked exactly as designed and defended the wrong proposition.** Finding 60 says a state check cannot see a decision that lands as prose; **this is its twin — an absence-of-ticket check cannot see work that lands as a branch.** **Fix shape, one command, symmetrical with finding 60's:** for any ask whose evidence is *"nothing is filed for X"*, also run `git branch -r --list '*<topic>*'` or `gh pr list --search "<topic>"` before re-raising. It would have found this on the first run that raised it.
- **THE ASK WAS RETIRED BY THE THING IT ASKED FOR, AND THE FILE SAYS SO RATHER THAN CLAIMING THE ROUTE WORKED.** The tempting sentence is *"raised for five runs, then it got done"* — which implies causation this lane cannot demonstrate and probably does not have. **PR #1363's work began before the ask was first raised.** Recorded as unrelated unless someone can show otherwise, because a lane crediting itself for an outcome is exactly the claim nobody downstream will check.
- **CHRISTIAN'S REPLY WAS TREATED AS TWO ITEMS WITH DIFFERENT ROUTING, NOT AS ONE ANSWER.** *"it is from now on"* settles the standing rule and retires the ask. *"existing tickets should be re-read and reprioritized in the light of a cost benefit analysis"* is a directive to re-order the board — **outside this lane's remit by construction**, since it writes only these two files. **It was routed into the brief's `## From Christian` section for the next working session and explicitly not executed here**, and the receipt told him so. **The remit does not widen because the instruction is genuinely his** — that separation is the property that makes step 0 safe to have at all.
- **THE ANSWER REJECTED THE QUESTION, WHICH IS EVIDENCE ABOUT HOW THIS FILE FRAMES ASKS.** The ask was posed as a binary — backwards or from now on — with "from now on" named as the safe default. **He took neither: forward-binding *and* a merit re-read of the existing pile.** The framing was not wrong (the safe default was in fact what happened) but it was **narrower than the decision space**, and a reader who only answers the question asked would have lost the second half. **Recorded because this file's standard advice is to frame asks so silence is safe; this run shows that a silence-safe framing can still cost the part of the answer nobody asked for.**
- **THE RED-CHECK FIELD WAS READ FOR THE FIRST TIME SINCE IT SHIPPED.** THR-1020 added `checkConclusion` to `check:armed-prs` because merge state alone cannot see a failing gate. **This run is the first where a PR returned both** — #1364 is `DIRTY` *and* `failing`. **Reported to the brief as one working-session item rather than two**, and explicitly not routed to Christian: a red check and a merge conflict are both technical verdicts THR-608 leaves with the agent.
- **THE SIBLING FOLD CONTRIBUTED NOTHING THIS RUN, AND THAT IS THE CORRECT OUTCOME RATHER THAN A MISS.** `orchestrator-2026-08-09b.md` (08:30Z) leads on THR-883 as *"one chat message away"*; `backlog-grooming-2026-08-09.md` leads on the same ticket and on the process-rule question. **Both were true when written and both were resolved within the following fifteen minutes.** They were **dropped rather than folded**, on the live board state — folding a resolved ask verbatim is how a fold turns into fabrication. **`weekly-hygiene-2026-08-09.md` had already caught the close mid-sweep** and said explicitly that nothing was needed, which is the only sibling section that survived contact with the live check.
- **THE BOARD GREW BY FIVE AND EVERY ONE OF THEM IS PROCESS WORK.** Ready for Dev **34 → 39**; the additions are THR-1056 through THR-1061, all filed 08:48–08:49Z by the weekly hygiene sweep, all `Continuous Improvement`. **Stated in the brief as the pile his cost-benefit ruling now governs**, rather than as queue growth — the number rising is not the finding, the composition is.

<details>
<summary>Measurements from the 2026-08-09 07:56Z run</summary>

- **THE ONLY MEMBERSHIP CHANGE THIS RUN CAME FROM A SIBLING REPORT, NOT FROM THIS LANE'S OWN SCANS — FINDING 63.** Every probe returned green or correctly-silent, the board moved only in the direction of progress (35 → 34 ready, 2 → 1 in dev, two merges), and the home tree is clean apart from Christian's own two settings files. **A run whose own instruments all read healthy is exactly the run that folds nothing in if step 2.6 is skipped** — and `daily-backlog-grooming`'s 07:16Z section carried the run's single new ask. **Recorded because the twenty-nine previous entries justify the per-row recheck as drift insurance on carried asks; this one is the case for reading the siblings even when nothing of one's own has moved.**
- **THE NEW ASK WAS READ AT ITS SOURCE BEFORE BEING FOLDED, NOT TAKEN FROM THE REPORT'S SUMMARY.** The grooming report's `## Needs Christian` bullet states the 13–17 demotion figure; `list_comments(THR-871)` returns the full grooming note behind it, including the three things the lane actually did (demoted nothing; treated the clause as binding from ratification; left the retroactive set to the weekly retro). **The brief states the question as backwards-or-from-now-on and says silence is safe**, which is a claim about consequences that only the underlying comment supports — the summary bullet alone would have produced a vaguer ask.
- **`thr-883-producer-gap`'s ABSENCE WAS RE-VERIFIED RATHER THAN INHERITED, FOR THE SECOND CONSECUTIVE RUN.** `list_issues query:"authoring spec exemplar composition contract"` returns THR-883 itself, THR-1053 (a new `Idea` filed 07:05Z about `concepts` on aftermath changes — **not** the drafting job), and thirteen `Done`/`Canceled` rows; `list_issues state:"In Design"` returns exactly two rows, THR-883 and THR-1043. **Neither is the item that writes the two artifacts.** Asserting an absence is the easiest thing to get wrong, so it is re-run every hour rather than carried.
- **TWO MERGES WERE READ FROM `git log`, NOT FROM `updatedAt` — THE FALSIFIER FINDING 62 INSTALLED YESTERDAY.** `git log origin/main --since="2026-08-09T05:30Z"` returns THR-1046 (`ee344816`, merged `53df7f1f`) and THR-1040 (`b94763f7`, merged `13be45f9`). **THR-1046's `completedAt` was checked individually** (`2026-08-09T07:14:27Z`) rather than trusted from the same bulk-stamped `updatedAt` field that faked eleven completions last run.

</details>

<details>
<summary>Measurements from the 2026-08-09 06:55Z run</summary>

- **ELEVEN "DONE" ROWS CARRIED THIS MORNING'S TIMESTAMP AND NONE OF THEM COMPLETED THIS MORNING — FINDING 62.** `list_issues state:"Done" updatedAt:-P1D` returned THR-836, THR-844, THR-489, THR-969, THR-1038, THR-899, THR-1004, THR-976, THR-1039 and THR-896 all stamped `06:45:44.613Z` or `06:24:05.381Z`. **Identical to the millisecond, across four unrelated projects.** The cheapest possible check — one `get_issue` on the most reportable row — returned `completedAt: 2026-06-30T06:07:16Z` for THR-489. **The whole cluster is a bulk field sweep.** `git log origin/main --since="2026-08-08T19:58Z" --no-merges` returns **two** commits, and those two are what the brief reports.
- **THE FAILURE MODE THIS AVOIDED IS THE INVERSE OF THE USUAL ONE, WHICH IS WHY IT IS WORTH A FINDING.** Every prior finding in this file is about *under*-reporting — a fold that missed something, a probe that read healthy while broken. **This one would have been a spectacular over-report**, and every individual row in it was true: the tickets are real, the titles are real, `Done` is real. **Only the implied verb was false.** A "What's moving" built from `updatedAt` would have told Christian the test suite went green last night, eleven days after the fact and six weeks after it actually happened.
- **THE PRODUCER GAP CROSSED FROM WATCH-ITEM TO LEAD ASK ON DURATION ALONE, AND THE ESCALATION IS STATED AS SUCH.** At 18:55Z it was open agent work; at 19:58Z an ask with an explicit "nothing needed tonight"; it is now **thirteen hours and four sweeps old** with no ticket filed. **Nothing new was learned about it** — the promotion is the age, and the brief says so rather than dressing it as a discovery. **THR-883's own latest comment was re-pulled** and confirms the deliverable verbatim: *"Fable drafts (1) the amended `nudge-authoring-spec.md` … and (2) one exemplar encounter authored to the full contract."*
- **AN ASK WAS RETIRED BECAUSE EVENTS ANSWERED IT, NOT BECAUSE IT WAS RESOLVED.** `demo-fix-tickets-unqueued` asked him to choose: play now with rough edges, or say the word and get five cosmetic tickets queued. **Three of the five are now in `Ready for Dev`** (THR-1006, THR-1050, THR-1051) — promoted overnight by the normal sweep, unasked. **A two-sided trade-off with one side gone is not a decision**, so it leaves the set rather than being restated at a lower count.
- **THE LANE-SILENCE VERDICT WAS DECLINED FOR THE FOURTEENTH RUN, AND THE GROUNDS ARE HIS WORDS, NOT A THRESHOLD.** `check:lane-silence` returns `recovered` / `needsChristian: true` with a fresh 9.5h gap (`2026-08-08T20:32Z → 2026-08-09T06:04Z`). **The skill says put the summary verbatim in `## Needs Christian`.** It is declined because Christian answered this exact shape on 2026-08-06 — overnight quiet is expected — and **re-raising a decided question is the THR-985 defect**, which this file has now cited thirteen times. Reported under Freshness instead.
- **THE HELD PR WAS CORRECTLY INVISIBLE AND THAT WAS VERIFIED RATHER THAN ASSUMED.** `check:armed-prs` returns `verdict: "held"` with `holdReason` present on #1114, so THR-985's marker is doing its job — the skill says omit entirely, and it is omitted. **Note the payload also carries `checkConclusion: "skipped"` and one `conflictFiles` entry (`Docs/project-status.md`)**; neither is raised, because a PR parked on purpose is not stuck.
- **THE INBOX WAS EMPTY AND THE EMPTINESS COST THREE BYTES.** REST with `after=1535740649209331832&limit=50` returned literal `[]`. **No receipt sent, cursor untouched, no `## From Christian` section printed.** Fifth consecutive run with no actionable inbound.
- **NO GIT STATE OP RAN AGAINST THE HOME TREE** (THR-672); access was read-only `git -C` queries, file reads, and npm probes. **This run did not cut a worktree** — the two files were written into the home tree, published via `ops-publish.sh` (plumbing, no checkout), and **restored to their `main` stub bytes immediately after**, so the home tree ends the run exactly as it started. **Twenty-first consecutive run without a home-tree state op.**

<details>
<summary>Previous run's measurements (2026-08-08 19:58 UTC)</summary>

- **AN ASK WAS RETIRED BY DELIVERY, AND THE ONLY THING THAT REACHED CHRISTIAN WAS THE WITHDRAWAL OF ITS HOLD — FINDING 61.** THR-1042 was raised at 17:59Z as a caveat, not as a defect report: its whole content, from his side, was *hold the consequence session or play it discounted.* **The tempting write-up was the fix** — 4 → 9 chips, a merge, a deployed sha, all true and all of it his to not care about. **What he can act on is one bit: the hold is off.** The brief leads on that and puts the mechanism second, in the paragraph that explains why the hold existed. **The generalisable rule: when an ask is a hold, its resolution is the release, not the repair** — and the repair is context for the release, never the headline.
- **THE 119-MINUTE ASK-TO-RESOLUTION IS THE FIRST OF ITS KIND IN THIS FILE AND IS RECORDED AS EVIDENCE THAT THE CAVEAT WAS WORTH RAISING.** Raised 17:59Z → claimed 19:03Z → shipped 19:23Z → merged 19:32Z → live and withdrawn 19:58Z. **The counterfactual matters more than the speed:** had 17:59Z filed it as a queue line instead of an ask, Christian would have played the consequence session tonight against a screen that discarded half its content, and the ruling would have been spent. **Finding 25's converse — a check that costs three sentences and buys back a whole verdict session.**
- **THE FIX'S OWN CLOSING COMMENT SUPPLIED A LIMIT THIS FILE WOULD HAVE HAD TO FIND, AND IT WAS CARRIED RATHER THAN SMOOTHED.** The browser proof ran on `enc.letters_of_introduction` — deliberately, since it is the only shipped template carrying **both** an `aftermathConfig` and a rolling `rewardPool` — **not** on the five slice templates. **The brief says so.** The temptation was to let "shipped and live" imply "verified on your five", which is the same overstatement finding 25 forbids in the other direction. **It also closes the loop on the 17:59Z falsifier**: `rewardPool` appears 0 times in `vertical-slice.ts`, so the strongest proof case is by construction not one of his five, and the honest claim is *same code path, not individually re-checked*.
- **TWO NEW PLAYER-FACING DEFECTS WERE FOUND BY THE FIX ITSELF, AND WERE ROUTED TO THE QUEUE RATHER THAN TO HIM.** THR-1050 and THR-1051 were filed at 19:16–19:17Z from the surface as composed — both raw internal wording reaching the player on the ending screen. **Neither became an ask.** They are the same class as the five `Idea` blemishes already in ask 1, they are already filed, and one is already queued; **raising them separately would have split one standing trade-off into three.** Folded into ask 1 as a count with its status, which is the resolution finding 46 asks for.
- **A HOLD WAS RE-DERIVED AND FOUND TO HAVE CLEARED SIX MINUTES AFTER IT WAS SET.** `tb-orchestrator` held THR-1050 at 19:26Z because THR-1042 was `In Dev` against `buildUnifiedEncounterStageModel.ts`, making its coordination block's mutex claim false at pickup. **Correct when written.** THR-1042 merged at **19:32Z**. **Found by comparing the orchestrator report's timestamp against `git log --format=%cI` on the merge, not by re-reading the ticket** — the report is inside the 36h window, internally consistent, and silently obsolete. **Twenty-ninth run of the standing complaint that a freshness window is not a recheck; second consecutive run it changed a stated conclusion.**
- **THE INBOX HELD ONE MESSAGE AND IT WAS AGAIN THIS LANE'S OWN PING.** REST with `after=1535709863647248475&limit=50` returned one message authored by `1530180014165856296` (`dontgotoclaudebot`), not in `allowFrom` (`247984978283986954`). **Skipped, cursor advanced to `1535725836735348816`, no receipt sent, no `## From Christian` section printed.** **Fourth consecutive run where the author check did work a cursor-only read would not have.**
- **THE PRODUCER GAP SURVIVED A SECOND HOUR AND AN ORCHESTRATOR SWEEP, WHICH IS WHAT PROMOTED IT FROM A NOTE TO AN ASK.** At 18:55Z it was recorded as open agent work; the 19:26Z sweep filed nothing for it. **A gap that persists across two lanes that could each have closed it is no longer a backlog item, it is a mechanism failing to self-correct** — and the thing it blocks is the review that closes his largest ticket. **Raised with an explicit expiry and an explicit "nothing needed tonight"**, so it is a watch-item rather than a demand: finding 12's shape applied to an agent-side failure rather than a design option.
- **THE BOARD HELD FLAT IN BOTH COLUMNS AND THAT WAS STATED AS THROUGHPUT, NOT STASIS.** Ready for Dev **36 → 36** and In Dev **1 → 1** — but THR-1042 closed and THR-1051 was promoted inside the hour, so both flat counts are a claim-and-refill rather than an idle board. **Finding 46, applied to a delta of zero**, which is the reading most likely to be lazily reported as "no change".
- **THE WORKTREE WAS CUT FROM `origin/ops` THIS RUN, BREAKING A THREE-RUN STREAK, AND THE PROBE HAZARD WAS CLOSED A DIFFERENT WAY.** Cutting from `ops` gives the two files at their published state, which is what `check:substantive` compares against. **The known cost is that an `ops` worktree has no `package.json`, so an npm probe run from it walks up and silently reads the home tree.** Neutralised by running **every** probe with the home tree as CWD *deliberately*, after verifying it is on `main` at `d856090d` with `main..origin/main == 0` — so the probes read a correct `main` checkout by design rather than by accident. **Recorded as a variation with its verification, not as a lapse**, because the standing finding's purpose is that the probes read a current `main`, and that is what was proven.
- **NO GIT STATE OP RAN AGAINST THE HOME TREE** (THR-672); access was read-only `git -C` queries, file reads, and npm probes, all executed **before** the worktree was cut. **Twentieth consecutive run.**

<details>
<summary>Previous run's measurements (2026-08-08 18:55 UTC)</summary>

- **THE ASK RECHECK IS A STATE CHECK AND THE DECISION WAS NOT A STATE CHANGE — FINDING 60.** Twenty-seven runs of this file have justified the per-row re-pull as drift insurance, and it has caught drift. **It cannot catch this class at all:** THR-883's row is byte-identical to what it was this morning, and the ask it represents no longer exists. **The decision lived in a commit body** because Christian rules in chat and the ruling gets written into a plan doc by whoever was in the room. **Fix shape, cheapest first: for any ask whose subject is a design decision, grep the hour's merges for the ticket id before re-raising it** — `git log origin/main --since=<last run> --grep=THR-XXX` plus a read of changed `Docs/plans/`. That is one command per ask and it would have found this in the first minute rather than the fortieth. **Wants a ticket.**
- **THE ASK WAS RETIRED RATHER THAN REWRITTEN, AND THE DISTINCTION IS THE WHOLE POINT.** The tempting move was to keep ask 2 alive with new wording — *"review the two artifacts when they land"* — because something is still expected of him eventually. **That would be an ask for work that cannot be started**, since neither artifact exists and nothing is scheduled to write them. **An ask he cannot act on today is indistinguishable, from his side, from the ask that has been sitting there all week**, which is exactly how a week-old blocker stops reading as urgent. It is retired to an informational item, and the *producer gap* is raised in its place as the thing that actually needs attention.
- **THE PRODUCER GAP WAS FOUND BY CHECKING THE PLAN'S OWN NUMBERED LIST AGAINST THE FILED TICKETS, NOT BY READING THE TICKETS.** The factory plan lists five implementation items; four were filed (THR-1044/1045/1046/1047). **Item 3 — the spec amendment and skill expansion, which is precisely the Fable drafting job THR-883's review consumes — has no ticket.** Reading only the filed set would have shown four healthy High rows and nothing wrong. **The check that worked was reading the *source* enumeration and diffing it against the filings** — the same shape as verifying a coordination block against the files a change touches, applied to a plan doc.
- **THE SHIPPED WORK WAS VERIFIED AGAINST THE DEPLOYED SHA BEFORE BEING PROMISED TO HIM.** The brief tells Christian the cast-and-stakes change is in every slice link. `check:deploy` returns `deployedSha: 41ac12fa`, and `41ac12fa` **is the THR-1041 merge commit itself** (`Merge pull request #1357 … thr-1041/mount-cast-fallout-afterimages`). **Durable finding 10's habit, fourth consecutive run** — and it matters more than usual here, because the claim is about what he will see on screen in the next few minutes.
- **THE BLEMISH COUNT WAS RE-DERIVED AND WENT UP, WHICH A CARRIED NUMBER WOULD HAVE MISSED.** Three runs have said "four cosmetic fixes". **THR-1048 was filed at 18:31Z into the same `Idea` column** — a legacy choice card showing `+15% success` and a raw `coercive` key on a mortal-facing surface. **Five, not four**, and the brief says five. Same class as the stale-count payoff recorded at 13:55Z: **a set re-derived beats a count carried, and this is the second time this file has been paid for it in one day.**
- **THE INBOX HELD ONE MESSAGE AND IT WAS AGAIN THIS LANE'S OWN PING.** REST with `after=1535694616161030256&limit=50` returned one message authored by `1530180014165856296` (`dontgotoclaudebot`), not in `allowFrom` (`247984978283986954`). **Skipped, cursor advanced to `1535709863647248475`, no receipt sent, no `## From Christian` section printed.** **Third consecutive run where the author check did work a cursor-only read would not have.**
- **THE BOARD MOVED IN BOTH DIRECTIONS AND BOTH WERE NAMED.** Ready for Dev **34 → 36** (THR-1044/1045/1046 filed off the approved plan), In Dev **2 → 1** (THR-1041 shipped and closed). **Finding 46, applied to a delta whose two halves have the same cause** — the afternoon's design pass both emptied the in-flight slot and refilled the shelf.
- **`main..origin/main` IS 3 AND WAS REPORTED AS PHASE, NOT DECAY.** Three merges landed between the home tree's last hourly sync and this run; `FRESHNESS_BEHIND_THRESHOLD` is 10. **Reported to Christian as "catches up on the hour" rather than as a freshness flag** — the first non-zero reading in many runs, and the threshold exists precisely so a mid-hour reading is not an alarm.
- **#1114 RETURNED `held` FOR THE SEVENTEENTH CONSECUTIVE RUN**, `holdReason` carried as data. **Its hold reason is now stale in a way the probe cannot see** — the reason reads *"paused until the encounter-writing format is locked"*, and the format was locked this afternoon. **Recorded, not acted on:** re-arming is an executor call, and the amended spec the retrofit needs does not exist yet.
- **STEP 2.6 FOLDED TWO REPORTS AND BOTH WERE SUPERSEDED BY EVENTS.** `orchestrator-2026-08-08g.md` and `backlog-grooming-2026-08-08.md` both lead on THR-883 as the board's largest blocker; **both predate the 19:59 local merge that collapsed it.** Inside the 36h freshness window and wrong anyway. **The clearest instance yet of the standing complaint that a freshness window is not a recheck** — twenty-eighth run, first time the fold's headline item was flatly obsolete rather than merely incomplete.
- **THE WORKTREE WAS CUT FROM `main`, ON THIS FILE'S OWN STANDING FINDING** — third consecutive run.
- **NO GIT STATE OP RAN AGAINST THE HOME TREE** (THR-672); access was read-only `git -C` queries and file reads, all executed **before** the worktree was cut. **Nineteenth consecutive run.**

<details>
<summary>Previous run's measurements (2026-08-08 17:59 UTC)</summary>

- **A TICKET WAS READ AGAINST THE ASK SET RATHER THAN AGAINST THE BOARD, WHICH IS THE ONLY REASON IT SURFACED.** THR-1041 and THR-1042 both entered `Ready for Dev` this hour as `High` rows in a project whose ready pile is otherwise `Low`. **The default handling — and the handling twelve prior runs would have given them — is one line under `## Queue` saying two high-priority repairs were filed.** What changed the outcome was carrying the standing ask list into the queue scan: THR-974 asks *"is the change to the world visible"*, and THR-1042 is a report that part of the change is not rendered. **The board and the ask set are the same facts indexed differently, and only one of the two indexes surfaces a collision between them.**
- **THE TICKET'S OWN HEADLINE WAS THE PART THAT DID NOT SURVIVE THE CHECK.** THR-1042 leads with a rolled `rewardPool` prize vanishing. **`rewardPool` does not appear anywhere in `vertical-slice.ts`** — so on the five encounters Christian would actually play, the headline symptom is unreachable and the real loss is the quieter one (growth, standing). **Carrying the ticket's framing would have overstated the stakes to him by a wide margin**, and it was the cheapest of the four checks to run. **Durable finding 25's shape again — a confirmed defect licenses only what was confirmed *about the case in hand*.**
- **THE LIMIT OF THE CHECK IS STATED IN THE BRIEF, NOT ONLY HERE.** What was verified is that the discard happens and that the five templates take that path. What was **not** measured is how much `finalAction.aftermathChanges` actually holds for those five at runtime — that needs a live play, which this lane does not do. **The brief says "it may read as a small thinness rather than an obvious hole" rather than implying a measurement that was not taken**, because a warning that oversells is spent once.
- **THE RECOMMENDATION WAS SPLIT INSTEAD OF REVERSED.** Last run said "play now"; the honest response to THR-1042 is not to withdraw that, since the defect touches **one** of the two sittings. **The temptation was a blanket "hold off"** — simpler to write and wrong in the direction that costs the most, since it would re-park a slice session that has been ready for two days over a defect that cannot affect it. Slice → play; consequence → hold or play discounted. **A per-item verdict costs three sentences and preserves the session that was actually unblocked.**
- **THE INBOX HELD ONE MESSAGE AND IT WAS AGAIN THIS LANE'S OWN PING.** REST with `after=1535679155537051688&limit=50` returned one message authored by `1530180014165856296` (`dontgotoclaudebot`), not in `allowFrom` (`247984978283986954`). **Skipped, cursor advanced to `1535694616161030256`, no receipt sent, no `## From Christian` section printed.** **Second consecutive run where the author check did work a cursor-only read would not have.**
- **THE HOUR'S TWO STANDING ASKS WENT UNANSWERED AND WERE RE-STATED WITHOUT ESCALATION.** The play-now/queue-the-fixes choice was put to Christian at 16:55Z and again by Discord at 17:02Z; no reply. **Re-raised at the same volume, not louder** — the four fix tickets are still in `Idea`, so nothing about the trade-off changed, and inflating an unanswered ask is how a brief teaches its reader to discount it.
- **#1354 WAS CLASSIFIED BY ITS AGE TIER RATHER THAN BY ITS STATE.** `check:armed-prs` returned `conflicted` with `needsSession: false` at `ageMinutes: 32`, below `ARMED_DIRTY_ESCALATE_MINUTES` (90). **Reported under Freshness as too young to act on rather than as a stuck PR** — the tier exists precisely so a fresh conflict is not escalated in the same breath as an abandoned one. **First run this lane has seen a non-`held` entry from this probe.**
- **#1114 RETURNED `held` FOR THE SIXTEENTH CONSECUTIVE RUN**, `holdReason` carried as data. `ageMinutes: 13294` ages the PR, not the hold — **twenty-first instance, same field**, recorded as an unchanged known defect.
- **THE BOARD MOVED IN BOTH DIRECTIONS AND BOTH WERE NAMED.** Ready for Dev **32 → 34** (THR-1040 promoted by `tb-orchestrator` run g; THR-1041, THR-1042 filed directly), In Dev **1 → 2** (THR-1038 claimed, PR #1354 open). **A delta with named movements on both sides is throughput** — finding 46, applied to a moving count this time rather than a flat one.
- **STEP 2.6 FOLDED A REPORT THAT POINTED BACKWARDS, AGAIN.** `orchestrator-2026-08-08g.md` (newest, inside 36h) carries *"Nothing new — the demo-readiness update from run e still stands"*. **Followed the pointer to run e rather than reading the section as empty** — second consecutive run the skill's "skip the empty state" rule would have dropped the hour's largest carried item if applied to literal text.
- **THE WORKTREE WAS CUT FROM `main`, ON THIS FILE'S OWN STANDING FINDING** — second consecutive run. A worktree cut from `ops` has no `package.json`, so npm walks up and every probe silently reads the home tree.
- **NO GIT STATE OP RAN AGAINST THE HOME TREE** (THR-672); access was read-only `git -C` queries and file reads, all executed **before** the worktree was cut. **Eighteenth consecutive run.**

**Earlier 2026-08-08 run blocks were pruned** per this file's retention rule; recover with `git log -p Design/user-actions.md`.

</details>

</details>

</details>

</details>

</details>

</details>

## Open agent work (not Christian's)

- **RETIRED 2026-08-10 13:58Z — THR-1064's `Done` write happened, and the escalation trigger written above is what produced it.** The entry said: *if it is still `In Dev` when the ask set is next reviewed and no session has touched it, it returns to standing asks as a new ask, the evidence being that routing failed.* That is exactly what the 12:58Z run did, and the click followed 116 seconds after the ping. **The generalisable part is the trigger, not the ticket:** an item discharged to another lane needs a re-check date attached at the moment it is discharged, or "routed" and "done" are indistinguishable from this file's point of view.
- **AN ABSENCE-OF-TICKET ASK NEEDS A BRANCH CHECK, NOT ONLY A BOARD CHECK — FINDING 64, NEW THIS RUN.** `thr-883-producer-gap` re-verified across five runs that no ticket existed for the drafting job. **Every one of those checks was correct and the conclusion drawn from them was false** — the work was on `worktree-thr-883-contract-spec-exemplar` and merged as PR #1363 at 08:29Z. **The board is not the only place work lives, and this lane only ever looked at the board.** **Fix shape, one command, symmetrical with finding 60's:** for any ask whose evidence is *"nothing is filed for X"*, also run `gh pr list --search "<topic>"` or `git branch -r --list '*<topic>*'` before re-raising. It would have collapsed this ask on the first run that raised it. **Wants a ticket**, and it generalises: every surface that infers "unscheduled" from "unfiled" has the same blind spot.
- **THE FACTORY'S DRAFTING JOB — RESOLVED, AND THE ENTRY IS KEPT ONE RUN TO CARRY ITS LESSON.** It was raised here for five runs as an agent-side mechanism failure (*two lanes could each have filed it and neither did*). **The mechanism that closed it was neither lane: somebody simply did the work on a branch.** The agent-side finding survives its subject and is stated above as finding 64; the item itself is retired.
- **A new `Idea` was filed at 07:05Z that reads adjacent to the drafting gap and is not it** — [THR-1053](https://linear.app/threadbare/issue/THR-1053), on whether the Composition Contract's required `concepts` field agrees with what the aftermath type documents. **Checked rather than assumed**, because a plausible-looking row is exactly how an absence stops being re-verified.

<details>
<summary>Earlier entries for the producer gap</summary>

- **NOTHING IS FILED TO PRODUCE THE TWO ARTIFACTS THR-883'S REVIEW CONSUMES — SECOND RUN, AND IT HAS NOW BEEN PROMOTED TO A CHRISTIAN-FACING WATCH-ITEM.** Re-derived rather than carried: the `createdAt: -P1D` pull returns THR-1044/1045/1046/1047 and **no ticket for item 3**, and `tb-orchestrator`'s 19:26Z sweep filed nothing for it either. **A gap that survives two lanes each capable of closing it is not a backlog item; it is a mechanism failing to self-correct**, and what it blocks is the review that closes his largest ticket. **Raised in the brief with an explicit "nothing needed tonight" and an explicit expiry** — finding 12's open-option shape applied to an agent-side failure. **Still wants a ticket. This lane cannot file it.** Original measurement below.

<details>
<summary>Original measurement (2026-08-08 18:55Z)</summary>

- **NOTHING IS FILED TO PRODUCE THE TWO ARTIFACTS THR-883'S REVIEW CONSUMES — NEW THIS RUN, AND IT IS THE LIVE HALF OF A RESOLVED ASK.** The factory plan (`Docs/plans/2026-08-08-encounter-factory-workflow.md`) enumerates five implementation items. **Four were filed off it: THR-1044** (engine enablement — `encounter.*` default support bundles), **THR-1045** (`check:encounter` gate runner), **THR-1046** (the Package View), **THR-1047** (run harness). **Item 3 — "the Composition Contract lands in `nudge-authoring-spec.md` … and the pipeline prompts" — has no ticket**, and it is precisely the Fable drafting job whose output ruling 9 makes THR-883's review. **Found by diffing the plan's numbered list against the filings**, not by reading the filings, which showed four healthy `High` rows and nothing missing. **Cost if unfixed: Christian's collapsed ask never becomes actionable and quietly reads as handled** — the same decay shape as closed question 23's authorised-but-unfiled law. **Wants a ticket. This lane cannot file it.**
</details>

- **A TICKET FILED INTO `Idea` IS FILED INTO A COLUMN NO LANE READS — FOURTH RUN, SET FLAT AT FIVE, AND THIS RUN SUPPLIES THE FIX'S OWN PROOF.** Re-derived: THR-1033, 1034, 1035, 1036, 1048 all still `Idea`; THR-1037 and THR-1049 likewise but are verification/disposition items rather than player-facing blemishes, so the Christian-facing count is **five, not six** — corrected from last run, which folded them in. **The proposed fix was "file into `Todo` when the ticket is meant to be picked up", and that is exactly what happened this run without anyone acting on this note:** THR-1050 and THR-1051 were filed at 19:16–19:17Z straight into `Todo`, and THR-1051 was promoted to `Ready for Dev` nine minutes later. **The same filer, the same hour, the same class of defect — one route drains, the other does not.** **That is the cleanest available evidence that the column is the variable and not the tickets**, and it strengthens the fix shape from a proposal to a demonstrated one. **Wants a ticket; this lane cannot fix it**, since it may not write Linear state. Prior measurements below.

<details>
<summary>Prior measurement (2026-08-08 18:55Z)</summary>

- **A TICKET FILED INTO `Idea` IS FILED INTO A COLUMN NO LANE READS, AND NOTHING ANYWHERE SAYS SO — THIRD RUN, AND THE SET GREW.** Re-derived rather than carried: THR-1033, 1034, 1035, 1036 and 1037 all still read `status: "Idea"`, and **THR-1048 was filed into the same column at 18:31Z**, bringing the stranded set to six. `orchestrator-2026-08-08g.md`'s T1 log again states it scanned **Todo and Ready for Dev** only. **Three hours on, nothing has moved any of them.** **The set growing while nothing drains is the escalation this item did not previously have** — two runs ago it was a ~1h self-limiting stall; it is now a column that only accumulates. Original measurement below. **Wants a ticket; this lane cannot fix it**, since it may not write Linear state.

<details>
<summary>Original measurement (2026-08-08 16:55Z)</summary>

- **A TICKET FILED INTO `Idea` IS FILED INTO A COLUMN NO LANE READS, AND NOTHING ANYWHERE SAYS SO — SECOND RUN, UNCHANGED.** Re-derived this run rather than carried: THR-1033, 1034, 1035, 1036 and 1037 all still read `status: "Idea"` in a `createdAt: 2026-08-08` pull, and `orchestrator-2026-08-08g.md`'s T1 log again states it scanned **Todo (28) and Ready for Dev (32)**. **Two hours on, nothing has moved them and no lane has claimed them.** Original measurement below. Measured this run. `tb-orchestrator` run e filed THR-1033–THR-1037 off the demo-readiness FAIL and reported them as *"queued for the next session"*; run f's own T1 log then states it scanned **Todo (28) and Ready for Dev (31)** — the five sit in `Idea` and were never in scope. **Neither lane is wrong on its own terms:** the filer used `Idea` as a holding state, the promoter reads the two columns its skill names, and no surface compares the two. The cost is that a **Christian-facing hold was issued against work with no owner and no schedule**, and it took an unrelated `createdAt` pull to notice. **Fix shape, cheapest first:** have the filer file into `Todo` when the ticket is meant to be picked up, since `Idea`→`Todo` is the only hop T1 cannot make for itself. **Wants a ticket** — and it is worth one on materiality: the stall was ~1h and self-limiting here, but the same shape silently strands anything filed by a checkpoint. **Note this lane cannot fix it** — it may not write Linear state, so all it can do is name it every hour.

</details>

</details>

- **THE ASK RECHECK CANNOT SEE A DECISION THAT LANDS AS PROSE — FINDING 60, SECOND RUN.** Unexercised this run in the failure direction — no standing ask's subject moved in a plan-doc commit — but **the reverse shape did fire**: the orchestrator's THR-1050 hold was overturned by a `git log --format=%cI` read on a merge timestamp, not by a Linear field. **Same underlying gap, opposite surface**: state checks cannot see decisions or completions that land as commits. **The fix shape is unchanged and still one command per ask.** **Wants a ticket.**
- **THE ASK RECHECK CANNOT SEE A DECISION THAT LANDS AS PROSE — FINDING 60, AS FIRST RECORDED.** Twenty-seven runs have re-pulled every standing ask's Linear row before re-raising it, and that check is a **state** check: status, priority, assignee, `updatedAt`. **A design ruling made in chat and written into a plan-doc commit changes none of those fields**, which is how THR-883 stayed the brief's headline blocker for ~56 minutes after it stopped being one. **Fix shape, cheapest first: for any ask whose subject is a design decision, also run `git log origin/main --since=<last run> --grep=THR-XXX` and read any changed `Docs/plans/` path.** One command per ask; it would have found this immediately. **Wants a ticket** — and it generalises past this lane, since every surface that reads Linear state to decide what is blocked has the same blind spot.
- **CHRISTIAN'S STACKING RULE IS AUTHORISED AND STILL HAS NO TICKET — SEVENTH RUN, AND THE EXPOSURE IS NOW AT ITS WORST.** He gave the word at 2026-08-08 10:46:20Z. What remains is purely mechanical: the ruling (beats **may** stack, conditional on the stack reading in the order the world produced them) needs writing into `Docs/design-system/laws.md`, and **this lane can neither write the file nor file the ticket.** **This is the first run where the item no longer appears in `## From Christian`** — that section prints only when the inbox has new messages, and today's inbox was empty. **So one of the three surviving surfaces has now gone dark exactly as predicted**, leaving this section and closed question 23. **An authorised item reads as handled and stops being re-raised at precisely the moment nobody is re-raising it.** **Wants a ticket, and each run it does not get one the claim gets quieter rather than louder.**
- **NOTHING IN THE REPO KNOWS THAT OVERNIGHT QUIET IS APPROVED — SIXTH RUN, AND THE DECLINE IS ON ITS NINTH.** Christian answered outright on 2026-08-08 07:09:32Z; `check:lane-silence` returned `needsChristian: true` again this run, **unchanged in every field** (worst uncovered gap `2026-08-06T20:49:03Z → 2026-08-07T11:13:56Z`, 14.4h; the 8.6h `2026-08-07T22:28:57Z → 2026-08-08T07:06:08Z` — both overnight-shaped). **The answer is prose in two hourly-regenerated surfaces; the probe reads a marker file and commit history.** **Fix shape, unchanged and stated so it is not re-derived:** teach the probe a recurring quiet-hours window, or add a non-expiring marker mode — **the marker route is Christian's switch by design, so the probe-side fix is the one an agent may take.** **Wants a ticket.** The on-disk marker remains expired and self-describing as *"historical window only"*, which is what keeps the probe honest about a real daytime outage while unable to honour an answered question.
- **A WORKTREE CUT FROM `ops` MAKES EVERY `npm run` PROBE SILENTLY READ THE HOME TREE INSTEAD, AND THE PROBE REPORTS SUCCESS. — REMEDY APPLIED THIS RUN, TICKET STILL WANTED.** This run cut from `main` on this finding's own recommendation and the failure did not recur; `check:substantive` resolved this worktree's `package.json` and read the file actually written. **The finding is now load-bearing procedure with nothing written down outside this file**, which is exactly the shape that decays — the one-line skill edit is still unwritten. Original measurement follows. Measured 2026-08-08 15:57Z. The `ops` branch holds only `Design/`, `Docs/` and `README.md` — **no `package.json`, no `scripts/`**. Because `.claude/worktrees/` sits *inside* the home tree's working copy, npm walks up and resolves the home tree's `package.json`, so `check:substantive` ran against the home tree's **pointer stub** rather than the freshly written brief. It returned `verdict: "commit"`, `reason: "projection-changed"`, and a projection with **four empty section labels** — which is what a correct parse of a stub looks like, not an error. Re-run from a `main`-based worktree it returned `reason: "digest-changed"` with the real 8-key digest. **`bash scripts/ops-publish.sh` failed loudly on the same worktree** (`No such file or directory`), which is the only reason the wrong-file read was noticed at all.
  **The gate's fail-open design made the outcome correct and the diagnosis invisible** — both invocations said `commit`, and this run genuinely had a substantive change, so nothing downstream differed. **A run whose digest was unchanged would have published anyway and nobody would have known.** This is finding 49's shape (a probe answering about the snapshot it was handed) with a new cause: not call ordering, but *which repo root npm resolved*. **Fix shape: cut the session worktree from `main`, never from `ops`** — the two files can be written anywhere and `ops-publish.sh` targets `ops` by plumbing regardless of the worktree's branch. **Wants a ticket**, and it is cheap: one line in the skill's step 5 saying which ref to cut from.
- **THE ORIENTATION PROMPT AND THE SKILL DISAGREE ON WHAT STEP 2.6 IS — THIRD RUN, AND THE COST IS MEASURED RATHER THAN PREDICTED.** The scheduled-task prompt describes 2.6 as scheduled-workflow health (`check:workflows`); `SKILL.md` numbers 2.6 as the sibling-report fold and never mentions `check:workflows`. **Two runs ago the skill was followed and the probe was dropped; the last two runs followed the prompt and ran it.** **Wants a ticket: renumber the skill, or delete the orphaned step from the prompt.**
- **`check:substantive` STILL FAILS OPEN — BUT THIS RUN EXERCISED ITS REAL PATH, WHICH TWELVE PREVIOUS RUNS DID NOT.** Every failure path returns `commit`, so a broken gate is indistinguishable from a working one; that property is unchanged and **still wants a ticket**. **What is new is evidence the gate is not merely failing open:** it was invoked twice this run, and the two invocations took different branches. The first ran against the home tree's pointer stub before the real file existed and returned `reason: "projection-changed"` with `"no digest on both sides — using the projection backstop"` — the backstop, not the gate. The second, against the written brief, returned **`reason: "digest-changed"`** and printed the six-key digest it compared. **A gate that discriminates between its own two paths on the same run is doing arithmetic, not returning a constant** — the first positive evidence in twelve runs that the `commit` verdicts have been earned. **It does not close the fail-open concern**, which is about the failure branches, and it is stated that way.
- **STEP 2.6 NEEDS A PER-CLAIM RECHECK, NOT A FRESHNESS WINDOW — TWENTY-SEVENTH RUN, AND THIS RUN SHOWS THE *OTHER* HALF OF THE GAP.** Unexercised again in the failure direction: both sibling reports agreed with the live probes, so nothing had to be declined. **But this run also demonstrates what a freshness window cannot do** — both reports were comfortably inside the 36h window and both were *silent* on the THR-974 unblock, because it happened after they ran. **A fold that trusts a fresh report still under-reports; only the live re-pull found the new ask.** Recorded because 26 previous runs framed this purely as a staleness risk. **Wants a ticket.**
- **STEP 1'S PARK-ROUTING EXAMPLE STILL CONTRADICTS THE SHIPPED PROBE — TWENTIETH RUN.** Step 1 names THR-860 as the exemplar for routing a held park to `## Needs Christian`; THR-985 shipped the opposite, and the probe now returns `held` and suppresses it. **Unexercised again** — THR-860 carries an assignee, so it never enters the null-assignee scan. **Wants a ticket.**
- **THE PARK-DISPATCH GAP IS A LATENCY PROBLEM, NOT A TOTAL ONE.** THR-846 closed the detection gap; dispatch remains slow but demonstrably not broken. **Still wants a ticket**, and the claim "surfacing a park causes nobody to act" stays falsified.
- **PR #1114 (THR-860) STAYS UNARMED BY DESIGN** — probe returns `held` with the reason as data. **No sweep should re-arm it** until the format verdict lands.
- **AUTOSYNC STILL HAS NO HEARTBEAT.** `threadbare-autosync.ps1` unversioned on one disk (THR-824). **Fine again this run** (`main..origin/main` = 0), checked rather than assumed.
- **EIGHTEEN UNTRACKED FILES SIT IN THE HOME TREE, MOST UNDER `Docs/ops/`.** The THR-937 producing pattern, still harmless because THR-947 moved ops reports off `main`. **Flat this run.**
- **Impediment #267 is used twice**; **#219 and #228 are duplicate rows for one defect.** Fold at the next retro. **THR-1028 has now SHIPPED**, so the id-allocation *mechanism* that produces this class is fixed (`impediment:next-id` allocates, `check:impediment-ids -- --fix` repairs) — **the three existing duplicate rows are pre-existing data and still need folding by hand.** Recorded as a narrowing, not a closure.
- **THIS FILE STILL VIOLATES ITS OWN SKILL'S SECTION 4 STRUCTURALLY.** ~490 lines of numbered findings, synthesis the skill assigns to `retrospective`. **Fix shape: split into `Design/lane-findings.md`**, preserving the numbers, which are cited from outside this file. **Wants a ticket.**

</details>

## Resolved this period

- **2026-08-10 13:58Z — `thr-1064-needs-close` RESOLVED, BY THE ACT ITSELF RATHER THAN BY A MESSAGE.** [THR-1064](https://linear.app/threadbare/issue/THR-1064) reads `Done`, `completedAt` **2026-08-10T13:02:53Z** — **116 seconds after** the 13:00:57Z ping that carried it. No Discord reply accompanied it and none was needed: **the ask was a click, and the click is its own answer.** Verified live via `get_issue` (`status: "Done"`, `statusType: "completed"`, state history showing `In Dev → Done` at 13:02:53Z), not inferred from the empty In-Dev scan.
- **2026-08-10 13:58Z — FINDING 70: A REMOVAL-ONLY CHANGE TO THE ASK SET MUST NOT PING, EVEN THOUGH THE HASH SAYS IT SHOULD.** The step-6 gate fires when the section has genuine items **and** the hash differs. Both held this run — six real asks remain, and dropping the seventh changed the hash. **The ping was declined anyway**, because its entire content would have been six asks he has already been shown, sent on the occasion of him having *completed* the seventh. **That is precisely the harm the gate exists to prevent** (*"unchanged standing asks must not re-ping every hour"*), arriving through a case the gate's mechanics do not distinguish. **Rule, stated so the next run need not re-derive it:** when the diff against the pinged set is a **removal only**, update `kwf-last-ping.hash` and send nothing — the hash still advances, so the next genuinely new ask pings normally. An addition, or a material rewording of a live ask, still pings.
- **2026-08-10 11:58Z — `thr-1064-ruling-reversed` RESOLVED, AND IT IS THE SIXTH ASK EVER CLOSED BY AN INBOUND MESSAGE.** Christian, Discord **11:24:32Z**, verbatim: ***"go ahead"***, sent as a quote-reply to the 10:01Z ping. **Read as: close [THR-1064](https://linear.app/threadbare/issue/THR-1064) as not-a-defect; the stone reach's five trials stay exactly as authored.** This supersedes his 2026-08-09 15:12:58Z *"2. flip the label"*, which was answered on a brief this lane had got backwards.
- **2026-08-10 11:58Z — TAKEN AS UNAMBIGUOUS RATHER THAN QUERIED, AND THE REASON IS WORTH KEEPING.** *"go ahead"* names no object. **Exactly one item in the ask set was awaiting a one-word authorisation**, and the ping he quoted contained nothing else — so the referent is fixed by the reply target, not guessed from context. **Finding 7 governs the rest:** re-asking a man who has just said *go* ratifies a settled call, and it is the failure that held the WS5 shelf for eleven hours in July. **The reading was published rather than quietly applied** — stated in the brief, stated on the ticket, with a one-line reversal attached, so a misread costs one message instead of a wrong close.
- **2026-08-10 11:58Z — THE FIRST INBOUND CLOSURE WHOSE ACTION THIS LANE MAY NOT PERFORM, AND IT IS RECORDED AS OUTSTANDING RATHER THAN AS FINISHED.** The five previous inbound closures all ended in a *decision*, which a comment can hold. This one authorises a **`Done` write**, and the remit forbids issue-state writes without exception. **So the ask closes as answered while the thing it sanctions remains undone**, routed to an executor session. **That is closed question 23's rule paying out a second time — an authorisation is not a delivery, and it is more likely to be dropped than an open ask, because a granted permission reads as a handled item.** Carried in open agent work for exactly that reason.
- **2026-08-10 11:58Z — THE REMIT DID NOT WIDEN BECAUSE THE INSTRUCTION WAS AUTHENTICALLY HIS.** He told this lane to go ahead; this lane still did not write board state. **Author verification decides whether a message is acted on, never what actions become available** — the same separation recorded on 2026-08-09 08:57Z and 15:58Z, and the property that makes the inbound channel safe to keep at all.
- **2026-08-10 07:58Z — AN ASK CLOSED 24 SECONDS AFTER IT WAS ASKED, WHICH IS A DIFFERENT KIND OF EVENT FROM A FAST CLOSE.** `ws5-batch-scope-extension` entered at 06:58Z, pinged, and was answered *"gogogo"* at **07:01:50Z** — the ping went out at 07:01:26Z. **The previous record was twenty minutes; this is under half a minute.** Recorded not as a speed record but as evidence about the channel: **the ask, the ping and the answer all landed inside one hourly slot**, so the question never reached a second brief. That is the property step 0 was built for, and it has now fired three runs out of the last six.
- **2026-08-10 07:58Z — THE ANSWER WAS ONE WORD WITH NO OBJECT, AND THE READING WAS PUBLISHED RATHER THAN QUIETLY APPLIED.** *"gogogo"* affirms something; it does not say what. **The skill's own rule is that an ambiguous message must not be guessed at** — but re-asking a man who has just said "go" is the failure mode on the other side, and it is the one that had already held this shelf for eleven hours. **The resolution taken was neither: apply the reading, state it in plain words in the brief, and attach a one-line reversal** (*"just the four"*). **A misread now costs one message instead of thirty-four encounters**, which is what makes acting on it defensible rather than a guess.
- **2026-08-10 07:58Z — THE BOARD EMPTIED ITS IN-PROGRESS COLUMN FOR THE FIRST TIME IN THIS FILE'S RECORD.** THR-875 closed `Done` at 07:18:59Z (all forty meeting tests) and THR-860 returned to `Todo` at 07:19:33Z (released for re-authoring per the 08-09 verdict). **Both of the parks this file has been reporting for days cleared within 34 seconds of each other**, and nothing replaced them. **The queue is now 35 ready / 0 in progress** — no ask, no park and no PR is holding a working slot.
- **2026-08-10 07:58Z — AND THE SHELF THAT UNBLOCKED IS THE ONLY FEATURE WORK ON THE BOARD.** 31 of the 35 ready items are `Low`-priority cleanup and there is no High or Urgent row at all. **The thirty-four encounters this verdict released are not additional work on a healthy pipeline; they are the pipeline.** Stated here because the queue reading "backed up" has been true for days while meaning the opposite of busy.
- **15:58Z — FOUR ASKS CLOSED AT ONCE, ON THREE MESSAGES SENT INSIDE NINETY SECONDS.** His words, verbatim, each against the ask it retires:
  - *"go B for the four encounters."* (15:11:14Z) → **`ws5-batch-disposition` RESOLVED.** [THR-860](https://linear.app/threadbare/issue/THR-860)'s four capital-cluster templates are dropped, PR #1114 does not land, and they are re-authored under the locked format. The retrofit surface stays at 7 rather than growing to 11. **This ask deliberately broke the file's "silence is safe" framing** — neither option was safe under silence — and it is the one that had been waiting longest, since 2026-07-30.
  - *"1. loosen the rule"* (15:12:58Z) → **`thr-1062-slot2-scope` RESOLVED.** [THR-1062](https://linear.app/threadbare/issue/THR-1062) option 2: relax the contract so a reach trial need not declare a value pair. **He picked the first of the two answers the recommendation offered** (loosen, or set aside), so slot 2 is *in scope and gets written* rather than shelved.
  - *"2. flip the label"* (15:12:58Z, same message) → **`thr-1064-stone-polarity` RESOLVED.** [THR-1064](https://linear.app/threadbare/issue/THR-1064) option 2: the label moves to match the five scenes; the scenes and the shifts that drive the profile stay exactly as authored.
  - *"go with your recommendation a"* (15:13:20Z) → **`thr-886-compulsion-card-shape` RESOLVED.** [THR-886](https://linear.app/threadbare/issue/THR-886) option (a): the Compulsion becomes an unseen pull on the mortal's next decision, with no second menu inside an encounter. **Raised at 14:53Z, answered at 15:13Z — twenty minutes, the fastest close in this file's history**, beating `process-rule-retroactivity`'s 49 minutes set this morning.
- **15:58Z — THE TWO NUMBERED ANSWERS WERE NUMBERED AGAINST THE BRIEF, NOT AGAINST THE TICKETS, AND THAT WAS CAUGHT BEFORE RECORDING.** *"1."* and *"2."* index the brief's two-question message; both happen to map to their tickets' **option 2**. **A comment reading "Christian chose option 1" would have been a plausible, checkable-looking lie.** Each recorded verdict therefore quotes him, restates the framing he answered, then names the ticket option — and lists the rejected options explicitly so the next reader cannot re-derive it wrongly.
- **15:58Z — THE ASK SET IS FOR THE FIRST TIME ENTIRELY PLAY AND TASTE.** Six remain: two verdict sessions, one play-and-confirm, one card-wording call, two sound questions. **Not one of them blocks another ticket.** The morning's brief could already say the set contained no blocker; this run is the stronger version — nothing left is even *adjacent* to blocked work, because the four that were have all been answered.
- **15:58Z — HALF OF WHAT HIS ANSWERS AUTHORISE WAS DELIBERATELY NOT DONE.** Dropping PR #1114, deleting a branch, lifting a `Parked` label and re-authoring four templates are all now sanctioned by a director decision — **and this lane still does not touch branches, code or ticket state.** All four were routed through the brief's `## From Christian` section and named as routed. **The remit did not widen because the instruction was authentically his**, which is the same separation recorded on 2026-08-09 08:57Z and is the only reason the inbound channel is safe to keep.
- **08:57Z — THE FILE'S LEAD ASK CLOSED, AND IT CLOSED BY BEING FILLED RATHER THAN BY BEING FILED.** `thr-883-producer-gap` said no ticket existed to write the amended spec and the exemplar. **True at every check, and the wrong conclusion**: the work was on a branch the whole time, merged as PR #1363 at 08:29Z, and Christian approved both artifacts in chat at 08:38Z. THR-883 closed at **08:45:02Z** — ten days as the board's only `Urgent` row, and the largest blocked-work total on the board (eleven content tickets plus the factory harness) released with it. **Recorded as finding 64**: the absence check was sound and the inference from it was not, which is the same failure shape as finding 60 with a different blind spot — a branch instead of a plan doc.
- **08:57Z — THE ASK WAS NOT ALLOWED TO TAKE CREDIT FOR ITS OWN RESOLUTION.** The available sentence was *"raised for five consecutive runs, then it got done"*, and it is the sentence a reader would find most flattering to this lane. **PR #1363's branch predates the first time the gap was raised.** No causal claim is made and the file says so explicitly, because an unfalsifiable self-credit is exactly the assertion nobody downstream will bother to check.
- **08:57Z — `process-rule-retroactivity` WAS ANSWERED IN ONE RUN, WHICH IS THE FASTEST ANY ASK IN THIS FILE HAS EVER CLOSED.** Entered from a sibling report at 07:56Z, answered by Christian on Discord at **08:45:04Z** — 49 minutes, and the ping that carried it went out on the same run it entered. **The two-way channel paid for itself in the only way that was ever in question**: the answer arrived on the hour, was read on the hour, and the ask never reached a second brief. **Contrast the 2026-07-25 case that motivated step 0**, where two answers sat unread for eleven hours and the question was re-asked every run in between.
- **08:57Z — AND THE ANSWER WAS NOT ONE OF THE OPTIONS, WHICH IS THE PART WORTH KEEPING.** He wrote *"your choice is a false dichotomy"* and gave a third reading: forward-binding **plus** a merit re-read of the existing pile. **The ask was framed as a binary with a stated safe default, on this file's own standing advice that silence should be safe** — and that framing, while not wrong, was narrower than the decision. **A silence-safe framing can still cost you the half of the answer nobody asked for.** Recorded against ~30 runs of advice that has only ever pushed asks toward binaries.
- **08:57Z — HALF OF A GENUINE DIRECTIVE FROM CHRISTIAN WAS DELIBERATELY NOT EXECUTED.** *"Existing tickets should be re-read and reprioritized"* re-orders the board. **This lane writes two files and does not touch Linear state**, so it was routed to the next working session via the brief's `## From Christian` section and named as routed in the Discord receipt. **The remit did not widen because the instruction was authentically his** — authenticating the author is not the same as accepting arbitrary powers, and that separation is the only reason the inbound channel is safe to have.

<details>
<summary>Resolved on the 2026-08-09 07:56Z run</summary>

- **07:56Z — NOTHING RESOLVED THIS RUN, AND THE SET GREW BY ONE.** Stated rather than left blank: the previous run retired an ask by delivery, and a reader who sees no Resolved entry after that could reasonably infer the section stopped being maintained. **`process-rule-retroactivity` entered from a sibling report; no carried ask closed.** The two merges of the hour (THR-1046, THR-1040) touched no standing ask — **checked against the ask set rather than assumed from the fact that they were content/UI work.**

</details>

<details>
<summary>Resolved on the 2026-08-09 06:55Z run</summary>

- **06:55Z — AN ASK LEFT THE SET BECAUSE THE MACHINE ANSWERED IT WITHOUT BEING ASKED.** `demo-fix-tickets-unqueued` offered Christian a two-sided choice — play now with five rough edges, or say the word and have them queued. **Three of the five (THR-1006, THR-1050, THR-1051) were promoted into `Ready for Dev` overnight by the ordinary sweep.** The trade-off has one side left, so it is retired rather than restated at a lower count. **Recorded because the tempting move was to keep the slot warm** — "two still unqueued, still your call" — which would have carried a decision he no longer has to make into a fourth run.
- **06:55Z — AN ELEVEN-ROW OVER-REPORT WAS CAUGHT BY ONE `get_issue`, AND THE BRIEF SAYS SO IN PUBLIC.** The Done query's timestamp cluster would have produced a "What's moving" section headlined by the test suite going green. **The brief instead reports two commits and adds one plain sentence telling Christian the fresh timestamps were a bulk edit, not completions** — because a reader who checks Linear himself will see those same fresh dates and deserves to know why the brief ignored them.
- **06:55Z — THE LANE-SILENCE DECLINE HELD FOR A FOURTEENTH RUN AGAINST A PROBE THAT SAYS `needsChristian: true`.** The grounds are his 2026-08-06 answer, not a threshold or a judgement about severity. **Recorded each time it is declined**, because a standing override of a probe's own verdict is exactly the kind of decision that rots silently if it stops being written down.

<details>
<summary>Earlier entries (2026-08-08 and before)</summary>

- **19:58Z — AN ASK WAS RETIRED BY DELIVERY IN 119 MINUTES, AND WHAT REACHED CHRISTIAN WAS THE WITHDRAWAL OF ITS HOLD.** `thr-1042-consequence-suppression` entered at 17:59Z as a caveat on standing ask 8 — *hold the consequence session, or play it discounted, because the ending screen discards the world's half of what happened.* Claimed 19:03Z, shipped 19:23Z, merged 19:32Z, live by this run. **THE FIRST ASK IN THIS FILE EVER RESOLVED BY THE DELIVERY MACHINE INSIDE THE SAME EVENING IT WAS RAISED**, and the first whose resolution *unblocks another standing ask* rather than closing itself. **Verified rather than read off the merge subject:** `deployedSha: d856090d` equals `origin/main`'s tip, and that commit is the PR #1358 merge. **Recorded as finding 61: when an ask is a hold, its resolution is the release, not the repair** — the brief leads on "the hold is lifted, both sessions are playable" and puts the 4 → 9-chip mechanism second, where it belongs as explanation.
- **19:58Z — THE FIX'S OWN LIMIT WAS CARRIED INTO THE BRIEF RATHER THAN SMOOTHED AWAY.** The browser proof ran on `enc.letters_of_introduction`, not on the five slice templates — deliberately, since it is the only shipped template carrying both an `aftermathConfig` and a rolling `rewardPool`. **"Shipped and live" would have implied "verified on your five" at no cost to anyone's comfort and at real cost to the ruling**, which is the overstatement durable finding 25 forbids. The brief says *same path, not individually re-checked*, and names what a still-thin ending would mean. **It also closes the 17:59Z falsifier's loop**: `rewardPool` appears 0 times in `vertical-slice.ts`, so the strongest proof case is by construction not one of his.
- **19:58Z — THE ONE-RUN INFORMATIONAL ITEM EXPIRED ON SCHEDULE INSTEAD OF DRIFTING INTO A STANDING ASK.** `thr-883-collapsed-to-review` was entered at 18:55Z with a declared one-run life, on the reasoning that an ask he cannot act on is indistinguishable from the week-old ask it replaced. **It was dropped this run as declared.** **The temptation was to keep it — the news is good, it led the last brief, and a repeat costs nothing visible** — which is precisely how a resolved item becomes permanent furniture. **What replaced it is the half he can act on** (`thr-883-producer-gap`), which is a different ask with different evidence, not the old one renamed.

- **18:55Z — THE FILE'S LONGEST-RUNNING STANDING ASK IS RESOLVED, AND IT WAS RESOLVED BY CHRISTIAN IN A CHANNEL THIS LANE DOES NOT READ.** Ask 2 (THR-883) has led every brief since 2026-08-05 20:55Z. **He settled all eight of its open questions in a chat session this afternoon**, recorded in commit `0124ee79`. **THE FIRST ASK EVER RESOLVED WITHOUT ANY OF THIS LANE'S THREE DETECTION SURFACES SEEING IT** — not the Linear re-pull (row unchanged), not the Discord inbox (empty), not a sibling report (both fold reports predate the merge and still lead on it as the blocker). **Found by `git log` while assembling What's-moving**, which is a different question asked for a different reason. **Recorded as finding 60** and carried into open agent work with a one-command fix shape.
- **18:55Z — THE RESOLUTION WAS NOT ALLOWED TO LAUNDER ITSELF INTO A NEW ASK.** Something is still expected of Christian eventually — a review of two artifacts. **The tempting move was to rewrite ask 2 rather than retire it**, keeping continuity and a non-empty slot. **That would have been an ask he cannot act on**, since neither artifact exists, and an unactionable ask is indistinguishable from the stale one it replaced. **Retired to a one-run informational item; the producer gap raised in its place.** Finding 12's rule applied to a *resolution* rather than to a recurrence: the future review is a new ask with new evidence.
- **18:55Z — A CONFIRMED DECISION LICENSED ONLY WHAT IT DECIDED.** The rulings settle the *format*; they do not write the spec, unblock the eleven paused tickets, or re-arm #1114. **The brief says the sitting is off his plate and says in the same breath that nothing is scheduled to produce the artifacts** — durable finding 25's shape, and the temptation here was unusually strong because the good news is genuinely good and the caveat dulls it.
- **16:55Z — CHRISTIAN'S MORNING ASK "I'D LIKE TO TEST ALL THE DIFFERENT AFTERMATHS" IS ANSWERED BY SHIPPED, DEPLOYED CODE.** Filed as THR-1030 at 10:54Z off a chat message, merged as `e329caa2`, and serving live by 16:55Z — **under six hours from ask to usable link.** Recorded because this file has spent many runs on asks that sit; the counter-example is worth the same ink, and it is also the first time a `Standing asks` entry was closed by a ship rather than by an answer.
- **16:55Z — A CARRIED SENTENCE FROM A TRUSTED SIBLING WAS THE THING THAT WAS WRONG, NOT THE VERDICT IT CAME WITH.** Last run's adoption of the demo-readiness FAIL was correct and is untouched; the *"tickets are queued"* clause riding alongside it was not, and it was the clause Christian would have acted on. **The lesson is narrower than "distrust siblings" and that narrowness is the point:** a report mixes verdicts it owns with board claims it does not, and only the second kind is this lane's to re-derive. **Durable finding 25 already says a confirmed test licenses only what it tested; this run extends it from tests to reports.**
- **16:55Z — THE AUTHOR CHECK EARNED ITS KEEP FOR THE FIRST TIME.** Every previous run the inbox was either empty or genuinely Christian's, so `allowFrom` had never actually excluded anything. This run the cursor window contained exactly one message and it was **this lane's own Discord ping**, authored by the bot. A cursor-only read would have found "a new message" and tried to classify it; the id check dropped it in one step. **A guard observed only in its passing state has not been tested** — finding 28's shape, now satisfied for step 0's authorization branch.

- **15:57Z — THE FOLD PAID OUT IN THE DIRECTION 26 RUNS OF NOTES SAID IT WOULD NOT.** The standing complaint about step 2.6 is that a fold trusting a fresh report still under-reports, and that only the live re-pull finds what matters — **written most forcefully last run, when the sibling reports missed the THR-974 unblock.** This run inverts it exactly: the most consequential item of the hour existed **only** in `orchestrator-2026-08-08e.md`, because a checkpoint *verdict* is a judgment, not a board state, and no live query returns it. **Both claims are true and neither generalises**; the honest form is that the fold and the re-pull have disjoint blind spots, which is why the skill runs both. **The pull to over-learn from one run is what made last run's version too strong.**
- **15:57Z — A LANE'S OWN HEADLINE WAS OVERTURNED BY A SIBLING WITHIN TWO HOURS, AND THE OVERTURN WAS ADOPTED WITHOUT ARGUMENT.** Last run: *"the finished work is live as you read this… there is nothing to wait for."* This run: wait a few hours. **The temptation was to reconcile — to note both are true and leave the recommendation standing** — and that would have been technically defensible and practically wrong, since Christian acts on the instruction, not on the reconciliation. **Finding 48 says a lane's own sentence decays invisibly; this run is the cheap case, where somebody else did the checking.** Recorded because the cheap case is the one where the habit is actually formed.
- **13:55Z — THE ENABLING ARTEFACT WAS VERIFIED BEFORE THE ASK WAS RAISED, CLOSING ASK 1'S OLD FAILURE MODE.** THR-974 needs a deployed build carrying the finished aftermath. `deployedSha` `7e1118cb` **is the THR-973 merge commit itself**, so the ask is raised against a live artefact rather than a promised one. **Five runs ago ask 1 shipped naming a superseded build while claiming to be verified against the live one** — the sha-comparison habit that fixed it is what made this run's claim cheap to make honestly.
- **13:55Z — THE PING FIRES ON A GROWING SET, AND THE GATE WAS VERIFIED BEFORE IT WAS TRUSTED — SECOND CONSECUTIVE RUN.** Digest rises six → seven keys; last run's hash was reproduced from the derivation file (`68c649bf4601…`, byte-exact) before the new one was written. **The doorbell has now been observed firing on both a shrinking and a growing set**, which is finding 28's shape applied to the ping gate itself: a change-detector seen only in one direction has not been tested.
- **13:55Z — THE STALE-COUNT MEMBERSHIP RULE PAID OUT FOR THE FIRST TIME.** The previous run explicitly recorded THR-929 as *"INSIDE the window and not stale"* and named the risk of carrying a count instead of a set. **Three hours later the cutoff crossed it and the count is eight, not seven.** **A carried number would have been wrong this run and wrong silently** — the first concrete payoff for a discipline this file has asserted for twenty-odd runs on principle alone.
- **13:55Z — THE TWO `High` ROWS FILED THIS MORNING BOTH SHIPPED THE SAME DAY.** THR-1029 (aftermath reactions rendering a bare label) closed 12:31:58Z; THR-1028 (impediment-id allocation) closed. **Recorded against the previous run's stated worry** that five consecutive runs of an all-Low ready pile made "backed up but not starved" a weak claim. **The board absorbed two High rows in under eight hours**, which is evidence about throughput the worry could not have anticipated.

</details>

</details>

## Durable findings carried forward

Rules earned by tickets that have since shipped. **Kept as rule sentences; every worked example is in `git log -p Design/user-actions.md`.**

1. **Before carrying an observation into its Nth run, re-derive it once.** **Paid out 2026-07-30 02:58Z as a correction** (three runs blamed a missing `python` interpreter; the real cause was MSYS path expansion, and the remedy changed). **Paid out 2026-07-30 03:57Z in the other direction:** the "reader no longer filters on assignee" carry was checked against `pull-work` SKILL.md lines 27/176/184 and **held** — and the check surfaced a *disposition* the carry had lost (a non-zero count is the documented handled path, not a regression). **A re-derivation that confirms is not a wasted one; it re-sources the claim and often widens it.**
2. **A subsystem with no callers is invisible to every sweep that looks for wrong behaviour, because it has none.** Corollary: grep the *assignment* side, not the read side.
3. **A plan doc's past tense is a claim, not evidence.**
4. **Advisory gates that nobody reads are indistinguishable from gates that do not exist.**
5. **A test suite that agrees with the bug stays green forever** — and a pool-scoped guard is a suite that agrees with the bug one shelf over.
6. **Read composition, not depth** — and stop re-testing the favourable dip.
7. **A gate clearing is not the same as a decision arriving.** Surfacing a settled call for ratification is worse than not surfacing it. Corollary: verify a *favourable* outcome's cause as carefully as an unfavourable one.
8. **An empty Discord inbox is not evidence Christian is away.** Silence means only that he did not write.
9. **A startup-failure cluster is indistinguishable from quota exhaustion at the moment it happens.** The disambiguating evidence — *does a fresh run execute jobs?* — costs one run. Take it before escalating to a billing page.
10. **Deploy-observability must classify by paths, not by branch name or PR title.** — SHIPPED 2026-07-27 in THR-785.
11. **Mutex lines with reasons work.**
12. **An unanswered ask is not an unheard ask — do not re-ring the doorbell.** A standing ask carries a stated safe default, so silence resolves it rather than blocking on it.
13. **Declining to build the missing half is a shippable outcome, and it needs its reasoning recorded to survive.**
14. **A signal with two meanings is not a signal — and the repair is a notification path, never a new gate.**
15. **When a ticket asks "which of these fixes?", census the pipeline before answering** — the honest answer may be "none, and here is why". Validate the *sample*, not just the stage.
16. **A deliberately-parked issue and a broken auto-close are indistinguishable from the board.** Report a falsifiable test in both directions.
17. **Before carrying an ask into another run, check whether it was answered somewhere other than the channel it was asked on.**
18. **State a falsifiable test against the outcome, never against an activity proxy.** State the test's enabling premise explicitly, or a benign world reads as its own failure case. **Now subsumes finding 37's threshold rule — a test also needs enough *power*, not just the right target.**
19. **A column-scoped queue query reports the lane's reach, not the board's contents — and from inside the query the difference is invisible.**
20. **When an ask names a *mechanism* rather than a decision, expect the answer to arrive on the channel he has, not the one the mechanism needs.** (a) Prefer asks that are pure decisions. (b) State plainly what was *not* done.
21. **Re-derive a carry even when you expect it to hold** — the check has a wider aperture than the claim. Look at what the source shows you, not only the field you came to check.
22. **An item in the lane's column is not an item the lane can take.** The cheap detector: read the comments, not just the fields. *Consequential* and *decidable-by-Christian* are independent axes.
23. **A ticket this task files is unpullable by construction, and the cost lands on someone else's run — so filing is never free.**
24. **A file's *state* is evidence; its *author* is a separate claim needing separate evidence.**
25. **A confirmed test licenses the claim it tested, not the remedy that motivated it — and the gap is widest exactly when the result is most satisfying.** Corollary: **the mirror failure is a test whose aperture is wider than the claim.**
26. **One instance locates a defect; the second instance with the same fingerprint is what turns it into a mechanism — and changes the remedy.**
27. **A timestamp is evidence only once its zone is verified, and a `Z` suffix is a claim like any other.** Prefer a machine-written field (`stateHistory`, `lastRunAt`, commit metadata) over a prose header. **The *wrong field* misdates an event as badly as the wrong zone** — THR-667's `updatedAt` sits 28 minutes after its actual `Ready for Dev`→`In Dev` transition.
28. **A classifier observed only in its stable state has not been tested.** **Satisfied in BOTH directions:** three consecutive `deployedSha` advances, then two correct **flat** hours when only docs merged. **Note what switched: the evidence, not the verdict word** — five runs reported `skipped` throughout.
29. **"Green and armed" is not "merging" — and the difference is invisible from every signal a lane normally checks.** Checks answer *is this change correct*; `mergeStateStatus` answers *can it go in now*. **The tell is throughput, not status.**

  **Two corollaries.** (a) ~~**It does not self-heal.**~~ **RETRACTED 2026-07-29 13:23Z, and the retraction GENERALISED 2026-07-30 04:58Z on a second independent data point** — #1042 merged ~2h07m after entering `BEHIND`; **#1085 merged at 04:18:37Z, 1h49m after arming, with nobody intervening.** **The operational rule inverts: do not spend a session force-updating a branch that is merely `BEHIND` — it lands within ~2h unattended.** **The retraction is itself the lesson: "still true at T" is not "true at every T"** — and it was load-bearing twice, because it also set the floor the discriminating test had to clear (finding 37). (b) **`gh pr view` returns `UNKNOWN` for `mergeable`/`mergeStateStatus` on the first read** and often the true value on the second — but **not always, and the unreliability runs in both directions.** **04:58Z: BOTH reads returned `UNKNOWN` for both #1085 and #1031**, where an hour earlier both reads on #1085 returned real values. Treat `UNKNOWN` as "not yet computed", never as evidence of mergeability — **and never assume a second read will resolve it.**

  **29(a) IS NOW SCOPED, AND BOTH BOUNDS WERE MEASURED IN ONE RUN (2026-07-30 09:56Z).** (i) **It applies to `BEHIND` only.** A PR blocked by a *failed* required check never self-heals, and surfaces under the identical `BLOCKED` label — **earned finding 43.** (ii) **Even within `BEHIND`, "lands within ~2h" is a tendency, not a guarantee: #1096 crossed the ~2h07m band and went `DIRTY` at ~2h49m.** The checkpoint last run stated in advance resolved **unfavourably**, which is what a properly-powered test is for (finding 37). **The rule survives in weakened form: do not spend a session on a merely-`BEHIND` PR *inside* the band — but check it once past the band, because the failure mode past it is `DIRTY`, which needs hands.**

  **THE WEAKENED FORM IS ITSELF FALSIFIED 2026-07-30 13:53Z, FROM THE SIDE NOBODY WAS WATCHING.** #1114 went `BEHIND` → `DIRTY` at **≤1h32m** — *inside* the band, where "check it once past the band" prescribes doing nothing. **Every calibration point had been an upper bound** (#1042 ~2h07m, #1085 1h49m, #1096 ~2h49m): they measured how long a benign PR may take, and were silently read as also bounding how early a non-benign one may fail. **A one-sided calibration wearing a two-sided rule's clothing — and the unearned side is invisible because it never produced an observation.**

  **Final form, and it costs one call: inside the band, do not force-update a merely-`BEHIND` PR — but re-read its state each run, because what it decays into is the thing that needs hands.** The band bounds *when to intervene on `BEHIND`*; it bounds nothing about *when `BEHIND` ends*.

  **Detector, general: when a threshold was fitted on cases of one class only, say which class — then state explicitly that the other direction is unmeasured.** Finding 37 asks whether a test has power; **this asks whether the test has power against the hypothesis you did not write down.**

  **The `gh pr list --json` "workaround" recorded 2026-07-30 06:57Z is RETRACTED 07:54Z.** It rested on one favourable sample. This run the list form returned `UNKNOWN` for #1096 and #1031, and **another lane had read #1096 as `DIRTY` 22 minutes earlier** — so the list form is not a better instrument, just another sample. **The field is unstable across time, not across commands**, and #1098 moving `BEHIND` → `BLOCKED` inside 26 minutes says the same thing. **Read it twice with a gap, or read throughput instead** (29's main clause).

30. **A liveness witness must fire strictly *inside* the window it witnesses, not merely fall within it.** **The general form: a guard that accepts an endpoint as evidence about an interval passes hardest exactly when the interval is most degenerate.** Corollary: a probe's verdict is evidence, not a verdict — when independent witnesses contradict it, override and say why.

31. **A cleanup and its cause are separate deliverables, and shipping the cleanup is the moment the cause is most likely to be dropped.** State the re-accrual rate and a dated falsifiable test against the next producing event.

  **The honest quantity is not a rate: it is `|rows the sweep handled|`, workload-dependent and unbounded above.** Meta-lesson: **when a number has been wrong once, the reflex is to move it the other way and re-assert it with equal confidence.** Prefer naming the *variable*.

  **Closed out 2026-07-30 01:58Z in a direction the finding did not anticipate.** THR-845 shipped cleanup, cause-side hardening, **and** a third thing: **it removed the consumer that made the cause consequential.** Generalisation: when a defect's cleanup and cause are both expensive, check whether the *reader* can be made indifferent. **Confirmed twice over by 03:57Z — the leak has produced two more rows (THR-863, THR-864) and produced no symptom, and the reader's own source now documents the leak as a reportable number rather than a fault.**

32. **Verify-after-write needs a clock, not just a re-read — an immediate confirmation read can be served stale and is indistinguishable from success.** **Corollary: a count is not a verification.**

  **A `get_issue` check was correct when it ran and false 131 seconds later. An immediate verify cannot distinguish "my write held" from "my write held briefly"** — so the fix is **verify after the last side-effect in your own sequence**, not after the mutation you care about. **Sharpened by a controlled pair:** 9 immediate verifies after unassigns all held an hour later; 2 after creations were both false within the hour. **The verification was never the variable — what wrote next was.**

  **Best artefact: THR-860's description asserts its own verification and the field contradicts it in the same row.**

33. **A claim widens when it is restated, and the widening is invisible because every restatement still cites the original evidence.** **The drift is toward the remedy's framing**, because a report is written to justify acting. **Detector:** state what observation would falsify *today's* wording, then check whether it would also have falsified the wording used when the evidence was gathered. **Corollary: severity claims need their own evidence, separately from mechanism claims.**

34. **N confirming sightings of an effect do not locate its cause — and each sighting makes the unexamined attribution feel better supported.** **The trap is that repetition reads as accumulating evidence.** **Detector, cheap: before reporting a recurrence, name one property the affected rows share that the unaffected rows do not — and check it.** If you cannot state the contrast class, you have an effect, not a cause.

  **Corollary earned 2026-07-30 03:57Z while *maintaining* a contrast class rather than finding one: a row that was repaired after the fact is not evidence about the write path, and counting it as a clean case silently weakens the very contrast you are testing.** THR-861 reads unassigned only because the orchestrator hand-cleared it. **Keep the repair history attached to the data point, or the contrast decays as the cleanup succeeds.**

35. **A query's filter encodes a premise about the world, and nobody audits a premise that has never produced a visible failure.** (Earned 2026-07-29: `pull-work` selected work with `assignee:null` for months, encoding *"an assignee means a person has claimed this row."* Christian, unprompted: *"I do not use linear or github at all. so any issues in linear or PRs in github with my name on them are yours."*) **Detector, cheap: for each predicate in a query you are debugging, say out loud what it assumes about who or what writes that field — then ask whether anyone has ever confirmed it with the writer.** **Corollary:** 34 says N sightings do not locate a cause; **35 says the cause may not be in the mechanism at all, but in the question.** **The reader is usually yours to change even when the writer is not.**

  **SHIPPED 2026-07-30 01:21:50Z as THR-845, OBSERVED WORKING 02:02:22Z, RE-SOURCED 03:57Z, and CLOSED ON DIRECT BEHAVIOURAL EVIDENCE 06:57:17Z** — `pull-work` claimed **THR-681 while it carried `assignee: "Christian Spliid"`**, a row the pre-fix reader could not see. **Every earlier confirmation read the source; this one watched the behaviour.** **What makes this the finding's proof rather than its epilogue: the eight prior runs each produced a correct local measurement and a wrong global conclusion, and no amount of additional write-side measurement would have converged.** The unlock was a question, asked of the one party nobody had asked. **Second-order lesson for this lane: the answer arrived because step 0 existed to hear it.**

36. **A consumer that matches on a heading's *level* rather than its *text* is a channel that silently narrows as producers reformat.** **Detector: for every "no items found" result on a shared channel, confirm the *absence* against the raw file once, not just against your pattern.** Sibling of finding 19: **a scan reports its own pattern's reach, and a narrowed pattern and an empty world are the same observation.**

  **Generalised: "formatting" was too narrow — the near-miss was *file selection*.** So the rule is **every point where a consumer infers structure from a producer's convention is a coupling the producer can change unilaterally, and each one fails silently in the same direction.** Detector: for each such inference, name the producer change that would break it, then check whether anything would tell you.

  **Sixth consecutive correct-by-luck hour, 2026-07-30 03:57Z: the same-day set is now `-30` … `-30f`.** **A coupling that has survived six hours looks progressively more like a design and less like a hazard, and that drift is finding 33's tell turned on itself.** The luck is alphabetical and finite. **Record the streak as accumulating exposure, not accumulating safety.**

  **THE PROPOSED REPAIR WAS FALSIFIED 2026-07-30 09:56Z, and by a benign input.** The fix specified at 08:58Z was "select by explicit mtime (not glob order)". This run, **`-30g` and `-30h` carry the same mtime to the second (`08:20:23`)** — so an mtime sort has nothing to discriminate on and silently falls back to shell order, the coupling it was meant to remove. **No consequence: both sections read "Nothing needs you", so either pick was right.** That is the only way an instrument's failure surfaces on a benign input, and it is why it was worth checking a fix that looked obviously correct. **Generalisation, and it is finding 37 pointed at a *repair* rather than a *test*: a fix needs discriminating power too, and "take the newest" has none when the producer emits several per minute.** The honest fix removes the decision instead of improving it — **read all in-window reports and union their sections; never pick a winner.**

37. **A falsifiable test needs enough power to discriminate, and a threshold set below a known counter-example has none — it will "resolve" and license nothing.** **Earned 2026-07-30 03:57Z, cleanly.** Last run set the #1085 checkpoint at **1h30m** armed. Finding 29(a) had been retracted precisely because #1042 self-healed at **~2h07m**. The antecedent was satisfied on schedule and the conclusion was unavailable: at 90 minutes, a stuck PR and a slow-but-self-healing PR are the same observation.

  **This is distinct from finding 18.** 18 governs *what* you measure (outcome, not activity proxy). **37 governs *where you put the line*: a threshold must clear the longest benign case you already know about, or the test cannot separate the hypotheses no matter how faithfully it is run.**

  **Detector, one question, cheap: "what is the longest this has taken while still being fine?" — and set the checkpoint past it.** If no benign case is known, say so and state the test as exploratory rather than decisive. **The failure mode is seductive because an under-powered test still *resolves on time*, and a resolved test reads as a settled question.**

  **PAID OUT ONE RUN AFTER BEING EARNED, 2026-07-30 04:58Z.** The re-powered checkpoint (>2h07m) **discriminated**: #1085 merged at 1h49m, settling finding 29(a) in the favourable direction on two data points. **Same PR, same mechanism, same cadence — only the threshold differed, and the 1h30m version would have licensed nothing.** **This is the cheapest confirmed finding in the ledger: one question asked before writing the test, one run later a settled question instead of a repeated one.**

38. **A count measured against a periodic repair process is a *phase* measurement, not a health measurement — and the two are indistinguishable from the number alone.** **Earned 2026-07-30 04:58Z:** the home tree read **10 behind**, exactly at `FRESHNESS_BEHIND_THRESHOLD`, one commit from a Christian-facing alarm — while autosync was **provably healthy** (`git reflog show main`: seven consecutive hourly fast-forwards at `:50:03` to the second). All 10 commits had landed *after* the most recent tick, so the count was this hour's merge rate wearing the costume of decay.

  **Detector: before reporting a behind/lag/backlog count as decay, read the repairing mechanism's own cadence log and ask "when did it last succeed?"** A count plus a healthy cadence is phase; a count plus a missed cadence is drift. **The threshold alone cannot tell them apart, and a busy hour is exactly when the count looks worst.** Corollary: **this is why the THR-671 escalation ran for days** — a behind-count was read as decay without ever checking whether the mechanism had run.

  **Sibling of finding 30 in structure:** both are cases where a *point* observation is offered as evidence about an *interval*, and both pass hardest when the interval is least normal.

39. **A ticket-authoring rule binds the author, and nothing re-binds it at promotion — so a long-parked ticket's numbers rot in place and arrive at the top of the queue looking authoritative.** **Earned 2026-07-30 04:58Z:** THR-680 was promoted as the intended next pickup carrying "the 38-deep home-tree stash stack"; the stack holds **2**, and drained to 0 roughly **8.7 days** earlier. THR-688 rule A (predicates, not counts) exists precisely to prevent this and applies only at authoring time.

  **Detector for any promoting lane, cheap: if a ticket's Done-when quantifies anything, re-measure that quantity during the promotion run and put the fresh number in the promotion comment.** A count that survives the re-measure costs one command; a count that does not has just saved an executor a wasted session. **The failure is invisible from the board — a rotted ticket and a fresh one are the same row.**

  **Note the near-miss in the *evidence*, not just the ticket** (memory `vacuous_probe`): had this run reported "2 stashes" without the reaper's history, the honest reading would have been ambiguous between "the ticket is stale" and "I am measuring the wrong stash stack". **The decay curve `38 → 0 → 1 → 2` is what made it definitive**, and it cost one `grep`.

  **PAID OUT ONE RUN AFTER BEING EARNED, 2026-07-30 06:57Z — and the executor reached it independently.** THR-680 shipped as `1392fa8b`, *"stash residue disposal + already-shipped-under-parent finding"*: the same stale-premise diagnosis, plus memory `split_child_ships_under_parent_id`. Stack now **0**, reaper corroborating. **Scope honestly (finding 25): the *detection* is confirmed cheap and correct. The *detector* — re-measuring a Done-when's quantities at promotion — is still unimplemented in the promoting lane.**

40. **A periodic job that succeeds with nothing to do often leaves no trace, so "no log entry" and "did not run" are the same observation — and only one of them is an alarm.** **Earned 2026-07-30 06:57Z by withdrawing the previous run's alarm.** Autosync's hourly `merge origin/main` had written `:50:03` reflog entries for seven straight hours; two hours showed none, which was reported as "8 minutes late" and escalated to a watch item with a Christian-facing test. **But a fast-forward with nothing to fetch does not move the ref, and a ref that does not move writes no reflog entry.** An idle hour and a failed hour are byte-identical in that log.

  **Detector, cheap: before treating a gap in a periodic job's output as evidence it stopped, ask "would a successful no-op have written anything?"** If the answer is no, the gap is not evidence in either direction — say so, and find a witness that logs unconditionally (a run log, a heartbeat file, an exit code) or report the *outcome* instead of the trace.

  **Sibling of findings 30 and 38, and the family is now clear enough to name: all three are cases of reading a periodic repairer's incidental artefacts as if they were its status.** 30 — a point observation offered as evidence about an interval. 38 — a count that measures phase, not health. 40 — an absence that measures workload, not liveness. **In each, the honest move was to read the *repaired thing* (is the tree current?) rather than the repairer's exhaust.**

  **Note what made this catchable: the alarm came with a falsifiable test, and running it forced a look at the mechanism.** An alarm carried without a test would have hardened into a fact over the next few runs — finding 33's drift, on this lane's own output.

41. **A non-recurrence discriminates nothing.** **Earned 2026-07-30 07:54Z on this lane's own defect.** Last run found a skipped publish with two candidate causes — a correct "no substantive change" skip, or a mid-run failure. This run observed two clean hourly publishes. **Both hypotheses predict exactly that**: a benign skip is supposed to be rare, and so is a transient failure. The observation is consistent with everything and therefore evidence for nothing.

  **The pull is strong because a quiet hour *feels* like an all-clear**, and downgrading the alarm feels like the disciplined move after finding 40 taught the cost of over-alarming. **But 40 says do not read a repairer's exhaust as its status; it does not say silence is health.** The two failure modes are symmetric and both are live.

  **Detector, cheap: before letting a non-recurrence retire an item, ask "what would the *benign* explanation have looked like this hour, and does it differ from what I saw?"** If the two are identical, keep the item and keep its detector request; downgrade the *alarm* while preserving the *question*. **Sibling of 18 and 37: a test that cannot separate the hypotheses has no power, and an unrepeated event is the lowest-powered test there is.**

  **VINDICATED AND SUPERSEDED IN ONE RUN, 2026-07-30 08:58Z.** 41 was right to keep the question open — and the question was then settled **not by waiting for more instances but by changing the witness** (finding 42). **The generalisation: when a test has no power, the move is to find a better instrument, not to run the same test again.** Two more hours of publish/no-publish observations would have discriminated nothing; one `gh pr list` call settled both instances at once.

42. **A detector inherits the blind spots of the artifact it reads — and one built at the wrong stage of a pipeline will manufacture the very pattern it was built to catch.** **Earned 2026-07-30 08:58Z, on a detector this lane had specified for itself two runs running.** The lane publishes `branch → PR → merge`. The proposed check read the **merged commit** on `main`. A run that published perfectly but whose PR had not yet merged is, to that check, indistinguishable from a run that produced nothing.

  **The cost was nearly a fabricated mechanism.** #1101 was open and healthy; the check would have reported a second missed publish; **finding 26 would then have promoted a one-off into a "mechanism" and moved the remedy upstream** — all on an artifact that was sitting in plain sight one command away. **This run's own first hypothesis was exactly that**, and only survived because the PR list was checked before the claim was written.

  **Detector, cheap, and it is one question: "what are all the states this thing passes through, and which one am I sampling?"** Sample the *earliest* stage that proves the work happened (the PR exists), not the *latest* stage that proves it landed (the commit merged) — the tail of a pipeline measures everything downstream of you as if it were you.

  **Completes the family named at finding 40.** 30 sampled a point and claimed an interval; 38 sampled phase and claimed health; 40 sampled exhaust and claimed liveness; **42 samples the wrong stage and claims the wrong actor.** In all four, the honest move is to name what the artifact is actually a record of, before reading a verdict off it.

43. **An aggregate status field that collapses "not yet" and "never" into one value inverts the operational rule between two states it cannot distinguish — and the safe-looking reading is the wrong one for exactly one of them.** **Earned 2026-07-30 09:56Z on a clean simultaneous pair.** `mergeStateStatus: BLOCKED` was returned for **#1107** (required check `IN_PROGRESS` — clears itself in minutes) and **#1103** (same check `FAILURE` — clears only on a push) **in the same query, at the same moment.**

  **The damage is specifically that finding 29(a) exists.** 29(a) established, on two data points, that an armed PR left alone merges within ~2h — and the resulting operational rule ("do not spend a session force-updating it") is *correct for `BEHIND` and wrong for a failed check*. **A red PR left alone under that rule reads as disciplined patience while its ticket sits parked indefinitely.** THR-868's UI half was 7 minutes into exactly that state when this run read it.

  **Detector, cheap, and it is one extra call: never read an aggregate merge status without reading the check *conclusions* beneath it.** `gh run view <id> --json jobs` gives per-step `conclusion`, which separates *waiting* from *failed* unambiguously — and, as a bonus this run, separated *failed* from *timed out* (17m19s wall time invited the timeout reading; steps 1–9 `success` and step 10 `failure` refuted it — memory `ci_red_may_be_timeout`).

  **Distinct from finding 29(b).** 29(b) says the field is *unreliable* — `UNKNOWN`, unstable across time. **43 says the field is *lossy even when it answers confidently*.** An unreliable field teaches you to re-read it; a lossy one teaches you to read something else. **Sibling of the 30/38/40/42 family in the same way:** a value that is a record of *whether the merge button is pressable* is being read as a record of *whether the work is progressing*.

  **THE PROGNOSIS HALF WAS OVER-CLAIMED AND IS CORRECTED 2026-07-30 10:54Z.** #1103 was pushed and merged at **10:30:00Z**, closing THR-868 — 40 minutes after this finding said it "will sit indefinitely absent a new push." **The mechanism claim is intact** (a failed check clears only on a push; `BLOCKED` still collapses "not yet" and "never"). **What was wrong was treating a statement about the mechanism as a prediction about the situation** — a red PR usually has an owner already looking at it, and last run's own scope note said so before the headline overrode it. **Finding 25, on this lane's own claim: the evidence licensed "this needs a push", not "this will be abandoned."**

44. **A persistence test aimed at rows that receive no writes cannot detect a write-path defect — it measures only that nothing wrote, and it passes most convincingly when the defect is worst.** **Earned 2026-07-30 10:54Z on a test this lane designed, deferred twice, and would have "passed" on the third.** The commitment was to re-read THR-863/864/867 past 12:00Z to see whether a hand-cleared assignee "drifted back". All three read clean — **and the orchestrator had not written to any of them since clearing them.**

  **The contrast class was in the same response, unrequested.** The only two Ready-for-Dev rows *written* that hour — THR-872 (10:02:56Z) and THR-873 (10:33:50Z), both fresh promotions — **both carried the leak.** A clean read on untouched rows and a leaking write on touched rows are not in tension; they are the same fact.

  **This is finding 34 read backwards, and that is what makes it easy to miss.** 34 warns against N sightings without a contrast class. **44 is the mirror: N *non*-sightings without a contrast class, where the absence feels like exoneration.** Two runs argued about the test's *deadline* (finding 37, power) when the flaw was in its *sample* — and a threshold argument is seductive precisely because it looks like rigour.

  **Detector, cheap, one question before any "did it recur?" test: "what has to happen for this defect to be able to show up, and did that happen to these rows during the window?"** If the triggering event never occurred in the sample, the test has no power at any threshold. **Prefer watching the write path over watching a fixed row set** — and if only a row set is available, require at least one row that was written during the window.

45. **A promise to a person is discharged by fetching the artifact that person will touch — never by reading the status fields that say it shipped, and the most authoritative-looking field is the one most likely to hand them something broken.** **Earned 2026-07-30 11:55Z, discharging a promise this lane had carried for two runs.**

  **Every upstream signal was green and none of them was the promise.** THR-878 read `completedAt` 11:29:26Z; `check:deploy` returned `deployed` at `47f47542`; the merge landed. **All true, and all silent on whether a URL in Christian's inbox opens the feature.** The check that actually settled it was three commands: resolve the alias (HTTP 200, `<title>Threadbearer</title>`), read the served bundle path out of the HTML, `grep` the bundle for `forceencounters` — 1 hit.

  **The near-miss is the evidence, and it came from the *best* source.** The deployments API's own `environment_url` for that exact commit — the field whose entire purpose is "where this deployment lives" — returns **302**. The stable alias returns **200**. **Had the URL been taken from the authoritative field, the brief would have shipped a redirect, with every status check still green and nothing anywhere to contradict it.**

  **This is finding 42 pointed at a human rather than a pipeline.** 42 says sample the *earliest* stage that proves the work happened. **45 says that when the deliverable is a promise to a person, the stage that matters is the *last* one — the one they touch — because every prior stage can be green while the thing still fails in their hands.** The two are not in tension: 42 governs *did we do it*, 45 governs *did it arrive*.

  **Detector, cheap, one question: "what will they actually click, and have I run it?"** If the answer names a status field, a ticket state, or a probe verdict, it is not the artifact. **Corollary, and it is the expensive half: a URL, path, or command written into a Christian-facing document is a claim requiring its own evidence** — the surrounding facts being true does not transfer.

46. **A queue count that *falls* is not evidence of progress — a completion and a reach reduction move the number the same direction, and the reach reduction is the one that looks like good news.** **Earned 2026-07-30 12:58Z.** Ready-for-Dev went **58 → 49** and `Urgent` went **9 → 0** in a single hour. Read off the columns that is a shelf draining and a fire list clearing. **What actually happened is that Christian halted a programme and its entire head left the lane's reach** — the nine were exactly the WS5 partition, and 58 − 9 = 49 accounts for the whole delta.

  **This is finding 19 run backwards, and the reversal is what makes it dangerous.** 19 says a column query reports the lane's *reach*, not the board's contents — earned on work that was invisible because it sat in an unreachable column. **46 is the same mechanism producing a favourable-looking number instead of a puzzling one**, and nothing prompts an investigation into good news.

  **Detector, cheap, and it is one subtraction: when a queue count moves, account for the delta by name before characterising it.** If the drop equals a set of rows that left the column *without* reaching `Done`, it is a stop, not throughput. **The `Done`-window read is the discriminator and it costs one call** — nine rows closing and nine rows being blocked are indistinguishable from the ready count alone.

  **Sibling of the 30/38/40/42/43 family:** each reads an artifact as a record of something it is not. **46's artifact is a count, and what it actually records is column membership — which is a function of both completion and permission.**

47. **An *arming* is a point observation about a mutable setting, not a property of the thing armed — and carrying it forward converts "cannot land yet" into "will land eventually" with nothing to signal the switch.** **Earned 2026-07-30 14:53Z.** Last run recorded, correctly, that auto-merge was armed on all three open PRs. This run **#1114 returns `autoMergeRequest: null`** — the arming is simply gone, with no event anywhere that a lane reads. The PR had been recorded as *blocked but destined to land*; it is now *blocked and inert*, and the two are indistinguishable from every field except the one nobody re-reads.

  **The damage is that it silently changes the remedy's size.** A conflicted-but-armed PR needs one action (resolve the conflict; the merge fires itself). A conflicted-and-disarmed PR needs two, and the second is invisible from the conflict. **Last run's remedy sentence — "fix is `git merge origin/main && git push`" — was correct when written and is now incomplete**, which is exactly the failure mode a carried status produces: the sentence stays plausible while its world moves.

  **Detector, cheap, and it is a re-read rather than an inference: for any status that a *third party or an automation* can change without notifying you, re-read it every run you rely on it — never carry it across runs as an established fact.** The test for whether a field qualifies: *could this have changed while nobody on my side did anything?* If yes, it is an observation with a timestamp, not a property.

  **Distinct from finding 29(b), which is about a field being *unreliable at read time*.** 47 is about a field being **reliably read and then reliably stale** — the read was correct in both runs, and the error lives entirely in the carry. **Sibling of the 30/38/40/42/43/46 family in the same way: an artifact read as a record of something it is not — here, a *setting's current value* read as a *plan's durable intent*.**

48. **A lane's own characterisation of a situation decays exactly like a third-party field — and it decays invisibly, because the item's every field can stay unchanged while the situation underneath it changes completely.** **Earned 2026-07-30 15:54Z on this lane's headline item.** Three consecutive runs described THR-883 as *"still needs an interactive Fable session no lane can substitute for."* The session had run — three rounds, decisions approved in chat, a plan doc opened. **Every field this lane routinely checks agreed with the stale description:** status `In Design` (unchanged), priority `Urgent` (unchanged), `stateHistory` one unbroken row, **zero comments**. The only tells were `updatedAt` and an attachment list nothing was reading.

  **The catch came from an unrelated direction — a queue delta.** Two new `High` rows appeared, and asking *where they came from* surfaced the session. **Had the delta been recorded as a bare "+1", the wrong characterisation would have survived at least another run**, and by finding 33's drift it would have hardened: each restatement citing the same original evidence, sounding more settled each time.

  **This is finding 47 raised one level, and the level matters.** 47 governs a *field* someone else can change without telling you. **48 governs a *sentence you wrote* about a world that can change without telling you** — and a sentence has no `null` to return. **Detector, cheap, one question: for any item described identically across N runs, ask "what would have to be true for this description to be wrong?" — then check that thing, not the item.** Checking the item is what fails: its fields are exactly what the stale description predicts.

  **Corollary on the remedy, and it is finding 25's shape: the discovery does not license retiring the ask, only re-scoping it.** Three of four deliverables closed; one remains, plus two newly-named ones the plan doc parks explicitly. **A moved ask is not a closed ask, and the pull to declare victory is strongest exactly when the movement is most welcome.**

50. **A section rewrite that prepends new content without deleting the old is indistinguishable from a correct one at write time — and the result is a document where two runs' measurements sit under one timestamped heading, each reading as current.** **Earned 2026-07-30 16:55Z on this lane's own output.** The 15:54Z file carried its fresh bullets at lines 7-35 and the **14:53Z run's bullets at lines 37-62**, unmarked, under the single heading *"This run's measurements (2026-07-30 15:54 UTC)"*. The `Open agent work` section had the same defect in miniature: two stale PR bullets sat directly beneath the fresh ones **and contradicted them** — one asserting `gh pr list` returned `UNKNOWN` for #1096/#1031, the other, three lines above, asserting both commands agreed with no `UNKNOWN` at all.

  **Why it survives review.** Every individual line is true *of the run that wrote it*, so nothing reads as an error — the document fails only as a whole, and only against a timestamp in a heading nobody re-reads. **A reader sampling any single bullet gets a correct fact and no signal that it is an hour stale.** The next run then inherits both blocks as current state, which is how a contradiction gets carried forward with both halves cited.

  **Distinct from finding 33 (claims widening on restatement).** 33 is about a claim *changing* as it is retold. **48 is about a superseded claim not being retold at all — just left in place, unedited and undated, while a newer one is written above it.** The drift mechanism is absent; the damage is identical.

  **Detector, cheap, and it is structural rather than editorial: when a section is regenerated per run, replace it by its delimiters — find the heading and the next same-level heading, and splice between them.** Never append-and-hope. **The check afterwards costs one command: count the occurrences of a per-run marker inside the section — if a bullet naming a probe verdict appears twice, two runs are present.** Corollary for any lane that regenerates documents: **the write path is a place defects hide precisely because the content is correct.**


49. **A probe that reads shared mutable state answers about the snapshot it was given, not about the world — and running it before you synchronise makes it a confident reporter of a world that has already moved.** **Earned 2026-07-30 17:57Z, on this lane's Christian-facing deploy line.** `check:deploy` was called before this run's `git fetch`. It returned `verdict: "skipped"` with the summary *"Commits since the last publish (7d483fb0) only touched notes and docs, so the game itself did not need rebuilding."* **Every word was true of the `main` it could see.** Fifteen minutes later, after a fetch performed to create the session worktree, `origin/main` read `23b9918a` — carrying the THR-885 **engine** merge — and the identical command returned `deployed`.

  **The cost, had the ordering not been accidentally corrected: the brief would have told Christian the live site was carrying nothing but documentation at the exact moment his last blocking piece of machinery went live** — the single most consequential event of his day, reported as its opposite, with a green probe verdict backing it.

  **What makes this hard to catch is that nothing looks wrong.** There is no error, no `UNKNOWN`, no stale-data warning; the probe is working perfectly and the verdict is internally consistent. **The defect is not in the instrument or in the reading — it is in the *order of two steps*, and order leaves no artifact.** The fetch that exposed it was incidental and could as easily have come after publication.

  **Distinct from findings 45 and 47, which it superficially resembles.** 45 says fetch the artifact the person will touch rather than trusting a status field — **here the status field was the right instrument and was read correctly.** 47 says re-read what a third party can change — **here nothing changed underneath a carried value; the value was wrong the first time it was produced.** **The family it actually joins is 30/38/40/42/43/46: an artifact read as a record of something it is not — a verdict about `local main` read as a verdict about `production`.**

  **Detector, cheap, and it is a sequencing rule rather than a judgement call: `git fetch` before *any* probe whose verdict depends on repository state, and treat every pre-fetch reading as void rather than provisional.** The general form: **for any check that compares "ours" against "theirs", synchronise first — a comparison against a stale local copy fails silently and in the reassuring direction.** Corollary: **when a probe's verdict changes across a run, do not average, prefer, or reconcile the two — identify which snapshot each saw and discard the older outright.**

51. **A remedy earned against one failure mode can be followed exactly and still produce the wrong answer, because the instrument it prescribes answers a different question than the one that failed.** **Earned 2026-07-30 22:54Z, against finding 43 — this ledger's own rule.** 43 says: *never read an aggregate merge status without reading the check conclusions beneath it*, having been earned where `BLOCKED` collapsed a running check and a failed one. This run applied it to #1114 and read **four `SUCCESS` conclusions**. The PR is **`CONFLICTING`/`DIRTY`** and cannot merge.

  **The mechanism is mundane and that is the point: a check rollup records CI against the last pushed commit; mergeability records that commit against the current merge base.** A branch that passed everything and then fell behind a conflicting `main` is green and unmergeable **with no field in tension and nothing anywhere reading as an error.**

  **What makes this worse than an ordinary gap is the direction of the failure.** 43's remedy exists to stop a lane trusting a lossy aggregate — **so following it feels like the rigorous move, and it upgrades confidence in a wrong conclusion.** An unexamined `mergeStateStatus` would have said `DIRTY` and been right; **the extra call that finding 43 requires is what would have overridden it.**

  **Detector, cheap, one question before letting any sub-signal override an aggregate: "what is this sub-signal computed against, and is it the same thing the aggregate is computed against?"** If the answer is no, it is a *second* fact, not a *finer-grained* version of the first — and it cannot refute it. **State the comparison base, not just the value.**

  **Distinct from 29(b) and 43, and it completes the trio.** 29(b): the field is *unreliable* — re-read it. 43: the field is *lossy* — read beneath it. **51: the thing beneath it is about something else — check what each is measured against before letting one outrank the other.** **Sibling of the 30/38/40/42/43/46/47 family** in exactly the family's shape: an artifact read as a record of something it is not — here, *CI's verdict on a commit* read as *the repository's verdict on a merge*.

  **Note the self-referential hazard for this ledger specifically: a remedy sentence carried by number is a carried claim, and finding 47 applies to it.** A rule earned on one mechanism is an observation with a scope, not a property of the world.

52. **A claim that a missing detector "cost nothing this hour" is the detector's output, asserted in the detector's absence — and it is indistinguishable from the detector having run and found nothing.** **Earned 2026-07-31 00:54Z against this lane's own previous entry.** The 23:54Z run recorded the unbuilt comment-reading detector as *"unimplemented for a fifth run. **This hour it cost nothing** — the ask did not move."* **The ask had moved at 21:51:22Z, two hours and three minutes before that sentence was written.** Knowing whether it moved requires reading the comments; that is exactly the capability being reported as absent.

  **What makes it survive review is that it reads as discipline.** Finding 41 had just taught the lane not to let a quiet hour retire an open question — so the entry dutifully *kept* the item, downgraded the alarm, and preserved the detector request. **Every one of those moves was right.** The defect is the one clause that went further than the evidence: a no-cost finding smuggled in beside a correctly-preserved question, wearing the same measured tone.

  **Detector, cheap, one question before writing any "this gap cost nothing" line: "which instrument told me that, and is it the instrument I just said I do not have?"** If the answer is the missing one, the honest sentence is **"cost unknown — unmeasured for a fifth run"**, which preserves the question *and* the uncertainty. **The asymmetry matters: "unmeasured" decays into urgency, "cost nothing" decays into closure**, and five runs of the latter is how a live ask goes stale in a file whose whole purpose is to hold live asks.

  **Distinct from finding 41 and from finding 18, though it sits between them.** 41 governs whether a *non-recurrence* may retire an item. **52 governs a stronger and quieter claim: that the gap produced no damage *during* the window** — which requires observing the window, not merely observing that nothing announced itself. **Sibling of the 30/38/40/42/43/46/47 family** in the family's exact shape: an artifact read as a record of something it is not — here, **the absence of a noticed cost read as a record of the absence of a cost.**

53. **A change-detector that compares a hash of *derived* text is only as stable as the derivation — and when the derivation is unspecified, the detector reports "changed" for content that did not change, in the one direction that costs a person their attention.** **Earned 2026-07-31 01:54Z on this lane's own ping gate.** Step 6 says *normalise the `Needs Christian` item lines, hash them, compare against `PING_STATE_FILE`* — and never says what "normalise" means. This run's normaliser produced `939912ef…` against a stored `f39cebbe…`. **The literal rule says: send the DM.** The items were byte-identical to last run's.

  **The failure is invisible from inside the compare, because a hash mismatch looks the same whichever side is wrong.** There is no error, no `UNKNOWN`, no partial read — two hex strings differ, and the rule's whole purpose is to act on exactly that. **Worse, the mismatch is *self-perpetuating*: each run writes its own normaliser's hash, so a run whose derivation differs from its predecessor's both fires spuriously and re-arms the same trap for its successor.**

  **The check that separates the two costs one command and it is a *calibration*, not a comparison: re-derive the stored value from the previous artifact using today's derivation, and compare that against the stored hash.** Agreement means the derivation is stable and a mismatch is real content change. Disagreement means the *instrument* moved, and the content question must be settled another way — here, an item-level diff, which independently returned three-for-three `SAME`.

  **Note the near-miss inside the near-miss.** The first diff attempt reported two items differing at an em-dash — an artifact of decoding `git show` with the Windows locale codec on one side and UTF-8 on the other. **It pointed the same direction as the false hash.** Two independent-looking signals agreeing is normally the strongest evidence available, and here both were instrument defects with a common root: **comparing text without pinning its encoding or its derivation.** **Corollary: when two checks agree and the conclusion is "notify a human", verify that they are actually independent before acting.**

  **Distinct from finding 49, which it superficially resembles.** 49 is about *ordering* — a probe run before a fetch reads a stale world. **53 is about *derivation* — both sides are current, and the transformation applied to them differs.** **Sibling of the 30/38/40/42/43/46/47/49 family** in the family's exact shape: an artifact read as a record of something it is not — here, **a hash of one run's normaliser read as a record of another run's content.**

  **Generalisation for any state-file gate: store the input alongside the digest, or store a digest of something you did not compute.** A gate that persists only a derived value cannot tell a changed world from a changed derivation, and it will always resolve that ambiguity in the direction of firing.

  **PAID OUT ONE RUN AFTER BEING EARNED, 2026-07-31 02:54Z.** The disposition was to advance the state file so the *next* compare would be meaningful; it was, and it returned **identical hashes with no ping owed**. **What the payout does and does not license (finding 25): it confirms the derivation is stable between two consecutive runs of the same agent. It says nothing about the skill**, which still does not specify "normalise" — so the stability is a property of a choice, not of a rule, and the next run to choose differently re-arms the trap.

54. **On a gate that hashes prose, the *author* is a change vector indistinguishable from the world — so improving the wording of an unchanged ask is the same spurious notification as a drifting normaliser.** **Earned 2026-07-31 02:54Z, by being avoided rather than suffered.** Finding 53 had just established that a differing hash cannot tell a changed world from a changed derivation. **The mirror case is that it also cannot tell a changed world from a changed *sentence about* an unchanged world** — and that one is *invited* by ordinary care, because a section carried across runs accumulates staleness in its framing and the natural response is to tidy it.

  **The live instance: the `Needs Christian` opener has read "the thing waiting on you has changed" for three runs.** It was accurate when written (00:54Z, when the ask genuinely moved and a DM went out) and now reads as if something moved this hour. **Rewriting it was the better prose and the worse act:** the items are substantively unmoved, so the only effect of the edit would have been a ~05:00-local DM about an ask Christian was already pinged on. **The section was spliced verbatim out of `HEAD` instead of retyped, which also removes the risk of an unintended character-level drift doing the same thing accidentally.**

  **Detector, cheap, one question before editing any hashed section: "has the *ask* changed, or only my sentence about it?"** If only the sentence, leave it — **cosmetic improvements to a hashed section are deferred to a run that carries real news, where the ping is owed anyway and the rewording rides along for free.**

  **Distinct from 53 and it completes the pair.** 53: both sides current, the *transformation* differs. **54: the transformation is stable, the *input* was rewritten by the one party the gate cannot audit.** **Sibling of the 30/38/40/42/43/46/47/49/53 family** in the family's exact shape: an artifact read as a record of something it is not — here, **a diff in the rendering of an ask read as a change in the ask.** **The corollary is a design smell, not just a discipline: a gate whose input is human-authored prose will always have this hole; the durable fix is to hash a stable key (item identity) and carry the prose separately.**


55. **A workaround adopted to dodge a defect outlives the defect silently — because the fix ships against the *defect's* ticket, and nothing anywhere is watching on behalf of the workaround.** **Earned 2026-07-31 05:54Z on this lane's own publication path.** Previous runs published from a git worktree deliberately placed **outside** `.claude/worktrees/`, to dodge THR-880 (the write guard's unsound home-tree prefix test) and THR-797. That placement is also **invisible to the hourly reaper**, so each run leaked a tree nothing would clean up — a fix in one lane creating debt in another, as recorded for several runs. **THR-880 merged as `2c42b54e` and reached `Done` this run. The reason for the placement had ceased to exist, and the placement would have continued indefinitely.**

  **What makes it survive is that the workaround keeps working.** A workaround does not fail when its cause is fixed — it just becomes unnecessary, and “unnecessary” produces no error, no alarm, and no observation. **The lane that adopted it recorded the *debt* faithfully for runs on end and never re-checked the *premise***, because the debt was the thing that hurt and the premise was the thing that felt settled.

  **It was caught only incidentally**: the queue-delta rule (finding 46) required naming the row that left `Ready for Dev`, and the row happened to be the defect being worked around. **Had the delta been recorded as a bare “46 → 45, one completion”, the workaround would have survived at least another run** — and by finding 33's drift, its rationale would have hardened into a design decision.

  **Detector, cheap, one question when adopting any workaround: “what ticket, if it shipped, would make this unnecessary?” — write that id down beside the workaround, and re-check it whenever that id appears in a `Done` window.** If no such ticket exists, file one; a workaround with no named cause cannot ever be retired, only inherited.

  **Distinct from finding 47, which it superficially resembles.** 47 governs a *third-party setting* that changes under you and must be re-read. **55 governs your own decision, which does not change at all — what changes is the world that justified it, and the decision has no field to re-read.** **Sibling of the 30/38/40/42/43/46/47/49/53 family** in the family's exact shape: an artifact read as a record of something it is not — here, **a workaround's continued function read as evidence of its continued necessity.**

56. **A report's timestamp records when it was *written*, not when it was *measured* — so the freshest file on a shared channel can carry the stalest claim, and every staleness rule built on mtime admits it.** **Earned 2026-07-31 17:05Z, by rejecting a fold rather than suffering one.** `orchestrator-2026-07-31h.md` has mtime **16:50:03Z** and states under its own `## Needs Christian` that THR-905 *"has one question waiting for you... No movement on it across three runs now."* **THR-905 reached `Done` at 16:35:48Z** — the file was written **14m15s after** the ask it reports as open had closed.

  **Every guard the skill specifies passed.** `SIBLING_REPORT_MAX_AGE_HOURS` (36) admitted it easily; it was the **newest** report by mtime, so even finding 36's "read all in-window and union" would have included it; its `## Needs Christian` heading matched exactly. **The section was well-formed, current-looking, and wrong** — and folding it would have re-asked Christian, half an hour after he answered, for the decision he had just made. **That is worse than a missed item: it is evidence to him that the lane is not reading what he does.**

  **The mechanism is mundane and general: a producing run measures at its start and writes at its end, so every report carries a measurement age its filename cannot express.** A long run widens the gap silently. **An hourly consumer reading an hourly producer will hit this whenever the producer's window straddles a state change** — which is precisely when the state change is newsworthy.

  **Detector, cheap, and it is the rule this lane already applies to its own carries: re-verify a folded ask against its *source of truth*, never against the report that relays it.** A sibling's `## Needs Christian` line names an issue id; one `get_issue` settles whether the ask is still live. **In-window is a necessary condition for folding, never a sufficient one — and "newest" is not even necessary.**

  **Distinct from findings 47 and 48, and it completes that trio.** 47: a *third-party field* goes stale under a correct read. 48: a *sentence you wrote* goes stale about an unchanged item. **56: a *sentence someone else wrote* was already stale at the moment their file was created** — there is no time at which reading it would have been correct. **Sibling of the 30/38/40/42/43/46/47/49/53/55 family** in the family's exact shape: an artifact read as a record of something it is not — here, **a file's write time read as its content's observation time.**

57. **The 30/38/40 family has only ever been used to *withdraw* alarms, and a detector practised exclusively in one direction decays into a reflex — the honest form has an affirmative branch.** **Earned 2026-07-31 21:54Z on the home tree; fired affirmatively for the second consecutive run 2026-07-31 22:59Z.** `MAIN_BEHIND` on `main` is exactly the reading finding 38 exists to defuse (*a count measured against a periodic repairer is phase, not health*) and finding 40 reinforces (*a successful no-op leaves no trace, so absence is not evidence*). **Both detectors were run, and both returned the opposite of their usual answer.**

  **The reflog is the instrument, and its signature is unambiguous.** `git reflog show main --date=iso` shows eight consecutive `:50:03` `merge origin/main: Fast-forward` entries through **21:50:03**, and then nothing at 22:50, 23:50 or 00:50. **Because 41 commits were waiting, a healthy tick would necessarily have moved the ref and written an entry.** The absence is evidence *here* precisely because the window was not idle — which is the exact condition finding 40 says must hold before absence may be read at all.

  **The affirmative branch needs its own discipline, or it just relocates the reflex.** Two conditions must both be shown, never assumed: (a) the repairer's *own* trace has a regular signature you can point at, and (b) the window contained work it would have had to act on. **Neither alone suffices** — (a) without (b) is finding 40's trap, and (b) without (a) is guessing at a mechanism you have not observed.

  **Corollary earned the same night: a confirmed-dead repairer is not automatically an interruption.** The repair is still one command, and CLAUDE.md's session-start probe reports `behind:N` and carries it — so the cost of Christian learning this at 08:00 rather than 00:00 is one command, while a midnight ping is the disproportion findings 12/22/53 warn about. **Diagnosis strength and routing are independent axes**; upgrading the first does not automatically move the second. Structural fix belongs to THR-824 (the script is unversioned on one disk, so nothing in the repo can notice it stopping) and to a THR-837-pattern heartbeat probe for autosync.


58. **A ticket can be half-closed by a *different* ticket shipping, and nothing on the board records it — so the survivor keeps advertising work that is already done, at a rank set before the work was done.** **Earned 2026-08-01 17:02Z.** THR-923 documents two distinct bugs in one body: `{actor}` missing from the enricher's vocabulary, and the nudge adapter never calling the enricher at all. **THR-933 — a separate row, separately filed — shipped bug 1 at 16:25:26Z and closed itself.** THR-923 remains `Ready for Dev`, `No priority`, its description unchanged and now overstating the remaining work by half.

  **There is no field anywhere that carries this.** No relation, no shared label, no comment on either row; both rows' every field is exactly what a fully-open ticket and a fully-shipped ticket respectively look like. **The half-close exists only in the intersection of two prose bodies nobody reads together** — which is why the queue's ranking is stale in a way no sweep can detect: an unranked ticket whose real remaining scope halved is indistinguishable from an unranked ticket nobody has looked at.

  **The cost lands on the next executor, not here.** A session claiming THR-923 reads a two-part fix, re-derives the first part, and finds it already in `main` — memory `split_child_ships_under_parent_id` arriving from the opposite direction. That memory says *grep the parent id too* when checking whether your work already shipped; **54 says the reverse also happens: your ticket can be partly shipped by a row that never mentions it.**

  **Detector, cheap, and it belongs to the shipping lane rather than the claiming one: when a fix closes, grep the open queue for the symptom you just fixed, not for the ticket id you just closed.** An id search finds nothing by construction — the whole defect is that the ids are unlinked. A symptom search (`enrichProse`, `{actor}`) finds the sibling row in one call. **Then leave one comment naming the commit and narrowing the survivor's Done-when**; that is the entire remedy, and it costs less than the wasted re-derivation it prevents.

  **Distinct from finding 39, which it superficially resembles.** 39 is about a ticket's *own* numbers rotting while it sits parked — an internal staleness, fixed by re-measuring at promotion. **54 is about a ticket's scope being changed from outside by an event it has no link to** — no amount of re-reading that ticket reveals it, because the ticket is a faithful record of what its author knew. **Sibling of the 30/38/40/42/43/46/47/49/53 family** in the family's exact shape: an artifact read as a record of something it is not — here, **a ticket body read as a record of the work remaining, when it is only a record of the work once scoped.**
---

**Owner of items below:** Christian. Everyone else's blockers go in Linear or `Docs/impediments.md`.
**Refresh cadence:** The hourly `keep-work-flowing-cc` scheduled task keeps this current (prunes resolved items, adds newly-surfaced Christian-owned ones); the `retrospective` skill still does the deep periodic rebuild.

59. **A verdict word and the number offered as its evidence can measure different intervals, and the payload carrying both contains nothing that betrays the difference — so the more complete the report looks, the more confidently it is wrong.** **Earned 2026-08-02 19:55Z on this lane's own escalation path.** `check:armed-prs` returned `verdict: "abandoned"`, `needsChristian: true`, `ageMinutes: 4774`, `conflictFiles: ["Docs/project-status.md"]` for PR #1114, with a plain-language summary written to be pasted straight into `## Needs Christian`. **`abandoned` asserts that *this conflict* has gone uncleared for 79 hours. `ageMinutes` measures how long the *PR* has been open.** The two coincide only while nobody touches the branch — and an agent had resolved and pushed that exact conflict **45 minutes earlier**, after which `main`'s tip landed on the same file and re-created it.

  **The report is internally consistent and every field is individually true**, which is what defeats review. There is no `UNKNOWN`, no null, no stale-data warning, and no second number that disagrees with the first. **A reader checking the payload against itself finds a coherent story; the incoherence is only visible against the world.**

  **What settled it was refusing to interrogate the named object.** Reading #1114 harder returns `DIRTY` indefinitely, because it *is* dirty. **A conflict has two sides, and the probe reports one.** The other side is a commit on `main` — `git show --stat origin/main` names `Docs/project-status.md` and dates the merge 11 minutes after the resolving push. **The refutation lived in a place the subject's own record never mentions.**

  **Detector, cheap, one question before letting any probe's verdict reach a person: "what interval does this verdict claim, and what interval does the number underneath it actually measure?"** If the verdict is about a *condition* (stuck, stale, failing) and the number is about a *row* (created, opened, first seen), they diverge the instant anything intervenes — and intervention is exactly what a healthy lane does. **Corollary for probe authors: age the condition in its current unbroken run, and reset on any event that clears it.**

  **Distinct from findings 43 and 51, and it completes that trio's shape.** 43: an aggregate is *lossy* — read beneath it. 51: the thing beneath it is *about something else* — check the comparison base before letting it outrank the aggregate. **59: the aggregate and the thing beneath it are about different things *and agree*, because one is silently inherited from the other.** **Sibling of the 30/38/40/42/43/46/47 family in the family's exact shape:** an artifact read as a record of something it is not — here, **a row's age read as a condition's duration.**

60. **A tracker row records an item's *state*, not its *resolution* — so a decision made in conversation and written into a document leaves every state-shaped check reading exactly as it did before, and the ask stays "live" for as long as nobody happens to read a commit.** **Earned 2026-08-08 18:55Z on this file's own longest-running standing ask.** THR-883 led every brief since 2026-08-05 and was re-pulled from Linear this run as always: `In Design`, `Urgent`, assignee unchanged, description unchanged and still describing an interactive design session to be scheduled. **All four fields were correct. The ask had ceased to exist ~56 minutes earlier**, in commit `0124ee79`: eight rulings from a chat session, ending *"THR-883 becomes a chat review of two Fable-drafted artifacts."*

  **All three of this lane's detection surfaces reported nothing, and each was working correctly.** The Linear re-pull saw an unchanged row because no field changed. The Discord inbox was empty because he ruled in a Claude Code session, not on the phone channel. Both sibling reports were inside the 36-hour freshness window and **both still lead on THR-883 as the board's largest blocker**, because both ran before the merge. **A fourth surface would not have helped either, so long as it asked about state.**

  **What found it was asking a different question for a different reason** — reading the hour's merges to populate `What's moving`. **The finding is not "read git log"; it is that a decision and a state change are different events, and only one of them is what a tracker stores.** Conversation-settled projects produce this constantly: the ruling lands in a plan doc, the row is updated later or never.

  **Detector, cheap, one command per ask: for any standing ask whose subject is a design decision, run `git log origin/main --since=<last run> --grep=THR-XXX` and read any changed `Docs/plans/` path before re-raising it.** State checks stay — they catch drift, which this does not — but they must not be the only check on an ask whose resolution has no state to change.

  **Corollary on what to do with the resolution, earned in the same run:** the collapsed ask left a *successor* obligation (review two artifacts) that could not yet be acted on, and rewriting ask 2 to name it would have produced an ask indistinguishable, from the reader's side, from the week-old one. **A resolution that spawns future work is a retirement plus a new ask later — never a rewrite in place** (finding 12's rule, applied to resolutions rather than recurrences). **Second corollary, the one most likely to be skipped:** the rulings settle the format and nothing else, so the same run had to say both *"this is off your plate"* and *"nothing is scheduled to produce what your review consumes"* — **durable finding 25's licence rule, hardest to honour when the news is good.**

  **Note the timing, because it is the finding's own argument for itself.** THR-985 was filed at ~19:1xZ *for this class of defect* — a probe that cannot see a deliberate hold — and promoted at 19:29Z. **The first recurrence arrived 26 minutes later, from an angle the ticket does not cover** (age semantics, not blocked-by relations). **A ticket filed against one mechanism does not immunise the lane against the next hour's variant of it**, and treating a fresh ticket as coverage is how the second variant ships.

61. **When an ask is a *hold*, its resolution is the release — not the repair that made the release possible. Reporting the repair as the headline hands the reader a paragraph of mechanism to parse before reaching the one bit that changes what they do.** **Earned 2026-08-08 19:58Z on `thr-1042-consequence-suppression`, the first ask in this file resolved by the delivery machine inside the evening it was raised** (17:59Z raised → 19:03Z claimed → 19:23Z shipped → 19:32Z merged → 19:58Z live and withdrawn: 119 minutes).

  **The ask's entire content, from Christian's side, was one instruction:** hold the consequence session, or play it discounted, because the ending screen keeps the authored half of what happened and discards the world's half. **Its resolution therefore has exactly one bit of payload — the hold is off — and everything else is why.** The tempting write-up was the fix itself: 4 → 9 consequence chips, a merge sha, a deployed sha, all true, all verifiable, and none of it his to care about. **The brief leads on "both sessions are playable tonight" and puts the mechanism in the paragraph that explains why the hold existed.**

  **The general rule, stated so it survives this instance:** classify an ask by what it asks the reader to *do*, then report its resolution in the same currency. **A hold resolves to a release. A decision resolves to the decision being taken. A confirm-in-play resolves to "no longer needed".** An ask that asked for nothing but patience resolves to nothing at all and should simply vanish — which is the same discipline that expired `thr-883-collapsed-to-review` on schedule in this run rather than letting good news become permanent furniture.

  **The counterfactual is why the caveat was worth raising at all, and it is recorded because this file has spent many runs on asks that sit.** Had 17:59Z filed THR-1042 as a queue line — one more `High` among many, which is what it looks like read off the board — Christian would have played the consequence session tonight against a screen discarding half its content, and **the ruling would have been spent on a defect rather than on the design.** Three sentences of checking bought back a whole verdict session: **durable finding 25's converse, and the strongest available argument for reading new tickets against the standing ask set rather than only against the board.**

  **Corollary on the limit, which shipped in the same paragraph as the good news.** The fix was browser-verified on `enc.letters_of_introduction` and **not** on the five templates Christian will play — deliberately, since that one is the only shipped template carrying both an `aftermathConfig` and a rolling `rewardPool`, so it exercises the repair hardest. *"Shipped and live"* would have implied *"verified on your five"* at no cost to anyone's comfort. **The brief says "same path, not individually re-checked" and names what a still-thin ending would mean** — finding 25 again, in the position where it is hardest to honour: attached to the one item this run had unambiguously good news about.
## How this works

The retrospective stopped being the right channel for "Christian, please flip one switch." Christian-owned asks were buried in retro prose, and nothing surfaced them between retros. This file replaces that pattern.

Read order: top to bottom is blast radius. The top items break canonical workflow invariants right now; the bottom items are operational debt with workarounds in place.

For each item:
- **Fix** = the literal command, click path, or line of config that resolves it.
- **What breaks** = the named system or invariant currently degraded.
- **Source** = `Docs/impediments.md` entry numbers + occurrence count, so the cost is visible.

When an item resolves: delete it from this file, mark the corresponding impediment as resolved in the dashboard regen, note the close in the next retro under "What shipped." **No history retained here — `git log` + the retros are the audit trail.** The resolved log is pruned to the current day; anything older is recovered with `git log -p Design/user-actions.md`.

---

## Standing asks

**2026-08-10 13:58Z — SIX LIVE. THE RE-RAISED ASK WAS DONE IN 116 SECONDS, AND THE SET IS BACK TO PURE PLAY AND TASTE.** **`thr-1064-needs-close` RESOLVED** — the ticket reads `Done` at 13:02:53Z, less than two minutes after the ping that carried it, with no reply sent because the ask *was* the click. **Nothing joined.** The remaining six are unchanged: two verdict sessions, one play-and-confirm, one card-wording decision, two sound questions. **For the fourth time the set contains nothing that blocks another ticket**, and this run it also contains nothing mechanical — every item needs him to *play or judge*, not to click. Digest falls **seven → six**; **the step-6 ping was declined** on the removal-only rule recorded as finding 70 under Resolved this period. **The board's in-progress column is now empty**, which is the first accurate reading of it in three days.

<details>
<summary>Standing-ask preamble, 2026-08-10 12:58Z run</summary>

**2026-08-10 12:58Z — SEVEN LIVE. ONE RE-ENTERS, BECAUSE LAST RUN CLOSED IT BY ROUTING IT SOMEWHERE THAT DOES NOT EXIST.** **`thr-1064-needs-close` ENTERS** (ask 11). Last run retired `thr-1064-ruling-reversed` as answered and routed the `Done` write "to an executor session" — **and no executor session may make that write either.** CLAUDE.md reserves `Done` to the merge-autoclose path or to Christian, and THR-1064 has no code left to change, so no PR will carry the keyword. **The route was empty, which means the item was closed into a hole.** It is re-raised as what it actually is: a ten-second manual close only he can perform. **Nothing else joined or left**; the other six are unchanged and still entirely play and taste. Digest rises **six → seven**; the step-6 ping fires. **This is closed question 23 paying out a third time, one level up** — the previous run correctly refused to call the authorisation a delivery, then discharged it to a lane that could not deliver it, which reads as handled just as effectively.

</details>

<details>
<summary>Standing-ask preamble, 2026-08-10 11:58Z run</summary>

**2026-08-10 11:58Z — SIX LIVE. THE ASK THAT ENTERED LAST RUN WAS ANSWERED IN 86 MINUTES.** **`thr-1064-ruling-reversed` RESOLVED** — *"go ahead"* (11:24:32Z), a quote-reply to the ping carrying it, read as authorising closure as not-a-defect. **Nothing joined.** The remaining six are unchanged and entirely play and taste; for the third time the set contains nothing that blocks another ticket. Digest falls **seven → six**; the step-6 ping fires. **The closure is answered but not delivered** — marking the ticket `Done` is a state write this lane may not make, so it is routed and carried in open agent work, per closed question 23's rule that an authorisation reads as handled precisely because it was granted. Full verdict and the stated reading under Resolved this period.

**THE SIX CARRIED ASKS WERE RE-PULLED FROM THE LIVE BOARD** rather than carried on last run's word — THR-907, THR-974, THR-961, THR-962 all confirmed `Todo` and unchanged; THR-998 pulled in full with `get_issue` (`Todo`, `Medium`, `completedAt: null`, all three options intact); THR-986 pulled with relations (`Todo`, still `blockedBy` thirteen). **Tenth consecutive run declining a verbatim sibling fold on per-item liveness:** `backlog-grooming-2026-08-10` and `orchestrator-2026-08-10g` both raise THR-907/974/986, already this file's lead ask; `weekly-hygiene-2026-08-09` is empty by its own words and its one FYI (THR-883 closing) is four days stale.

</details>

**THE SIX CARRIED ASKS WERE RE-PULLED FROM THE LIVE BOARD THIS RUN TOO** — THR-907, THR-974, THR-961, THR-962, THR-998 all confirmed `Todo` and unchanged in a fresh `Todo`-state scan; THR-1064 confirmed still `In Dev`, assignee null, `completedAt: null`, which is what re-raises ask 11. **Eleventh consecutive run declining a verbatim sibling fold on per-item liveness:** `orchestrator-2026-08-10h` raises only THR-974, already this file's lead ask; `weekly-hygiene-2026-08-09` is empty by its own words and its one FYI is four days stale. **The lane-silence probe again returned `recovered`/`needsChristian` and was again declined** — he answered *"overnight quiet is normal"* on 2026-08-08 and the four gaps it reports are all overnight-shaped.

<details>
<summary>Standing-ask preamble, 2026-08-10 09:58Z run</summary>

**2026-08-10 09:58Z — SEVEN LIVE. ONE JOINED, AND IT IS A CORRECTION TO AN ASK THIS FILE ITSELF GOT WRONG.** **`thr-1064-ruling-reversed` ENTERS** (ask 10) — his 2026-08-09 15:12Z verdict *"flip the label"* was given on a brief that had the sign convention backwards, and an executor re-derived the convention from four code sites before implementing and found the stone set correct as authored. **The ask is not "decide again"; it is "close it as not-a-defect", which no CC lane may do.** **Nothing left the set** — the other six are unchanged and still entirely play and taste. Digest rises **six → seven**; the step-6 ping fires. **The real defect this uncovered (THR-1071, 37 of 40 templates inverted) is deliberately NOT an ask** — it carries its own fix decision and needs nothing from him.

**THE SIX CARRIED ASKS WERE RE-PULLED FROM THE LIVE BOARD** rather than carried on the previous run's word — THR-907, THR-974, THR-986, THR-961, THR-962 all confirmed `Todo` and unchanged; THR-998 pulled in full with `get_issue` (`Todo`, `Medium`, `completedAt: null`, all three options intact). **Ninth consecutive run declining a verbatim sibling fold on per-item liveness:** `backlog-grooming-2026-08-10` raises THR-907/974/986, already this file's lead ask; `weekly-hygiene-2026-08-09` is empty by its own words; `orchestrator-2026-08-10e` raises the THR-1064 reversal, which **was** taken — as ask 10 above, de-duplicated to one line rather than printed twice.

</details>

<details>
<summary>Standing-ask preamble, 2026-08-10 07:58Z run</summary>

**2026-08-10 07:58Z — SIX LIVE. THE ASK THAT ENTERED LAST RUN WAS ANSWERED IN TWENTY-FOUR SECONDS.** **`ws5-batch-scope-extension` RESOLVED** — *"gogogo"* (07:01:50Z), sent 24s after the ping carrying it, read as approval of the attached recommendation to extend. **This is the fastest close in this file's history by two orders of magnitude**, beating THR-886's twenty minutes, and it is the second consecutive ask to close inside the run that raised it. **Nothing joined.** The remaining six are unchanged and entirely play and taste — for the second time, the set contains nothing that blocks another ticket. Digest falls **seven → six**; the step-6 ping fires. Full verdict and the stated reading under Resolved this period.

<details>
<summary>Standing-ask preamble, 2026-08-10 06:58Z run</summary>

**2026-08-10 06:58Z — SEVEN LIVE. ONE JOINED, AND IT IS THE SECOND HALF OF A QUESTION HE ALREADY ANSWERED ONCE.** **`ws5-batch-scope-extension` ENTERS** — his 15:11:14Z verdict *"go B for the four encounters"* was scoped, in this file's own words, to *"THR-860's four capital-cluster templates … the retrofit surface stays at 7 rather than growing to 11."* `tb-orchestrator`'s 06:26Z report asks whether that call extends to the remaining batch tickets (~34 encounters). **It was checked against the recorded verdict before being raised, precisely because re-raising a settled decision is the THR-985 defect** — the verdict is scoped to four, so the remainder is genuinely unanswered rather than a re-ask. **Nothing left the set.** The other six are unchanged and still entirely play and taste. Digest rises **six → seven**; the step-6 ping fires.

**BOTH SIBLING REPORTS WERE DECLINED ON PER-ITEM LIVENESS, NOT ON JUDGEMENT.** `backlog-grooming-2026-08-09` (07:16Z) raises two bullets and **both died after it was written** — THR-883 closed 08:45:02Z, `process-rule-retroactivity` was answered 08:45:04Z — so a verbatim fold would have re-raised two questions he had already settled; **seventh consecutive run declining this report on those grounds.** `weekly-hygiene-2026-08-09` is empty by its own words. `orchestrator-2026-08-10b` says "nothing new" and defers to run a, which is where the taken item came from.

**THE SIX CARRIED ASKS WERE RE-PULLED FROM THE LIVE BOARD** rather than carried on the previous run's word — THR-907 and THR-974 (`Todo`, unchanged), THR-998 (`get_issue` in full: `Todo`, `Medium`, `completedAt: null`, all three options intact), THR-986 (`Todo`, still blocked), THR-961 and THR-962 (`Todo`, unchanged).

</details>

**THE 07:58Z RUN'S TWO SIBLING REPORTS WERE BOTH DECLINED, AND NEITHER ON JUDGEMENT.** `backlog-grooming-2026-08-10` (07:16Z) raises the three verdict sessions (THR-907, THR-974, THR-986) — **already the lead item in this file's own ask set**, so a verbatim fold would have printed one ask twice under two attributions. `orchestrator-2026-08-10c` says *"Nothing new"* in its own words and defers to run b. **Eighth consecutive run declining a fold on per-item liveness.**

**THE SIX SURVIVING ASKS WERE RE-PULLED FROM THE LIVE BOARD** rather than carried on last run's word — THR-907, THR-974, THR-998, THR-986, THR-961, THR-962 all confirmed `Todo` and unchanged this hour. **The board also cleared both of its parked items** (THR-875 → `Done` 07:18:59Z, THR-860 → `Todo` 07:19:33Z), so for the first time in this file's history **no ask and no park is holding a working slot**.

</details>

**2026-08-09 15:58Z — SIX LIVE, AND THE FOUR THAT LEFT ALL LEFT ON THE SAME NINETY SECONDS OF HIS ATTENTION.** **`ws5-batch-disposition`, `thr-1062-slot2-scope`, `thr-1064-stone-polarity` and `thr-886-compulsion-card-shape` ALL RESOLVED** — three Discord messages between 15:11:14Z and 15:13:20Z, each answer recorded verbatim on the ticket it settles (see Resolved this period). **Nothing joined.** **The remaining six are the same six the 08:57Z run described as entirely play and taste** — two verdict sessions, one play-and-confirm, one card-wording decision, two sound questions — and the set is now that *and nothing else*, because every ask that touched blocked work has been answered. **The four that left were the file's only content-blocking asks**; what remains cannot hold a ticket up even in principle. Digest falls **ten → six**; the step-6 ping fires.

<details>
<summary>Standing-ask preamble, 2026-08-09 08:57Z run</summary>

**2026-08-09 08:57Z — SIX LIVE, AND THE TWO THAT LEFT BOTH LEFT BECAUSE CHRISTIAN ACTED.** **`thr-883-producer-gap` RESOLVED** — the two artifacts it said nobody was writing merged at 08:29Z, he approved them in chat at 08:38Z, and THR-883 closed at 08:45:02Z, releasing eleven content tickets and the factory harness. **`process-rule-retroactivity` RESOLVED** — answered on Discord at 08:45:04Z, 49 minutes after it entered, and answered with a third option neither the ask nor the sibling report had offered. **Nothing joined.** **What remains is entirely play and taste**: two verdict sessions, one play-and-confirm, one card-wording decision, two sound questions. **For the first time since 2026-08-05 the ask set contains no blocker** — nothing in it is holding other work up. Digest falls **eight → six**; the step-6 ping fires.

<details>
<summary>Standing-ask preamble, 2026-08-09 07:56Z run</summary>

**2026-08-09 07:56Z — SEVEN LIVE, AND THE ONE THAT JOINED CAME FROM A SIBLING LANE REFUSING TO OBEY A RULE.** **`process-rule-retroactivity` ENTERS** — `daily-backlog-grooming` found that Christian's own 2026-08-08 demotion clause, applied literally, empties half the ready queue, so it demoted nothing and routed the backwards-or-from-now-on question to him. **Nothing left the set.** **`thr-883-producer-gap` holds the lead on duration** — fourteen hours, five work-picking rounds, absence re-verified by a fresh board search this run rather than carried. Digest rises **seven → eight**; the step-6 ping fires.

</details>

**ALL SEVEN CARRIED ASKS WERE RE-PULLED FROM THE LIVE BOARD on 2026-08-09 07:5xZ rather than carried forward on the previous run's word** — THR-883 (`get_issue` in full: `In Design`, `Urgent`, assigned Christian, `completedAt: null`, **latest comment re-read to confirm the two-artifact deliverable verbatim**), THR-907 and THR-974 (`Todo`, unchanged), THR-998 (`Todo`, `Medium`, three options intact), THR-961 and THR-962 (`Todo`, unchanged). **The new ask was read at its source** (`list_comments(THR-871)`) rather than folded from the sibling report's one-line summary. **Twenty-ninth run of applying by hand a clause step 2.6 still does not contain.**

<details>
<summary>Superseded header from the 2026-08-09 06:55Z run</summary>

**2026-08-09 06:55Z — SIX LIVE, AND THE ONE THAT LEFT WAS ANSWERED BY THE MACHINE RATHER THAN BY HIM.** **`demo-fix-tickets-unqueued` RETIRED** — three of its five blemishes were promoted into `Ready for Dev` overnight unasked, so the play-now-or-wait trade-off has one side left and is no longer a decision (finding 62's companion, recorded under Resolved). **`thr-883-producer-gap` PROMOTED TO LEAD** on age alone: thirteen hours, four sweeps, no ticket. **Nothing else changed membership.** Digest falls **eight → seven**; the step-6 ping fires.

**ALL SIX CARRIED ASKS WERE RE-PULLED FROM THE LIVE BOARD on 2026-08-09 06:5xZ rather than carried forward on the previous run's word** — THR-883 (`In Design`, assigned Christian, `updatedAt` 2026-08-08T17:57Z, **and its latest comment re-read in full** to confirm the two-artifact deliverable verbatim), THR-907 (`Todo`, `wayfinder:prototype`), THR-974 (`get_issue` in full: `Todo`, `completedAt: null`, `statusType: unstarted`), THR-998 (`Todo`, `Medium`, three options intact), THR-961 and THR-962 (`Todo`, unchanged). **Twenty-eighth run of applying by hand a clause step 2.6 still does not contain.**

</details>

<details>
<summary>Superseded header from the 2026-08-08 19:58Z run</summary>

**2026-08-08 19:58Z — SIX LIVE, AND ASK 8'S CAVEAT IS GONE RATHER THAN CARRIED.** **`thr-1042-consequence-suppression` RETIRED BY DELIVERY** — shipped, merged and deployed 119 minutes after it was raised, so standing ask 8 loses its hold and both verdict sessions are playable tonight (finding 61). **`thr-883-collapsed-to-review` RETIRED ON SCHEDULE** after its declared one-run life, **replaced by `thr-883-producer-gap`** — the same subject, the half he can act on, promoted from open agent work because it survived a second hour and an orchestrator sweep. **Ask 1's blemish set held flat at five, re-derived not carried, and corrected downward from last run's six** (THR-1037 and THR-1049 are verification/disposition items, not player-facing). **Two new defects of that class were filed this hour and deliberately did not become asks** — already filed, already routed, folded into ask 1 as a count. **Digest falls nine → eight keys, so the step-6 ping fires** — and it fires on a lifted hold, which is the news. **The delivery machine produced no ask for him for a tenth consecutive run:** one open PR, held by design, every merge-side probe green or correctly routed away from him.

<details>
<summary>Superseded header from the 2026-08-08 18:55Z run</summary>

**2026-08-08 18:55Z — SIX LIVE, PLUS ONE RETIRED-THIS-RUN INFORMATIONAL ITEM.** **Ask 2 RETIRED** — the format sitting collapsed into a review of two artifacts that do not yet exist (see the ask, and finding 60). **Nothing else changed membership**; ask 1's blemish set grew four → five (THR-1048, filed 18:31Z into `Idea`). **Digest falls ten → nine keys, so the step-6 ping fires** — and it fires on the retirement, which is the news. **The delivery machine produced no ask for him for a ninth consecutive run:** one open PR, held by design, every merge-side probe green or correctly routed away from him.

<details>
<summary>Superseded header from the 2026-08-08 13:55Z run</summary>

**SEVEN — FIVE DECISIONS, ONE OPEN OPTION, AND ONE CONFIRM-IN-PLAY.** **Ask 8 ENTERED because its last blocker closed** (THR-974, the consequence verdict; THR-973 merged 13:35:11Z), so membership rises six → seven and **the step-6 ping fires** for a second consecutive run. **Nothing left the set.** **The delivery machine produced no ask for him for an eighth consecutive run**, stated so it is not mistaken for an omission: one open PR, held by design, and every merge-side probe green or correctly routed away from him.

**ALL SIX CARRIED ASKS WERE RE-PULLED FROM THE LIVE BOARD on 2026-08-08 13:5xZ rather than carried forward on the previous run's word** — THR-883 (`In Design`, `Urgent`, still the board's only Urgent row), THR-907 (`get_issue` in full: `Todo`, assigned to Christian, `completedAt: null`), THR-998 (`Todo`, `Medium`, all three options re-confirmed against the body), THR-961 and THR-962 (`Todo`, `statusType: unstarted`). Ask 6 (the aftermath confirm-in-play) needs no board check: it is a play-and-report, not a row. **The new ask 8 was pulled with `includeRelations:true` before being raised**, so its `blockedBy` emptiness is a read field rather than an inference from a commit subject.

**THE PER-ROW RECHECK FOUND NO DRIFT AMONG THE CARRIED SIX, AND FOUND A NEW ASK THE SIBLING REPORTS COULD NOT HAVE.** Every re-pulled row matched what the previous run recorded. **Twenty-sixth run of applying by hand a clause step 2.6 still does not contain** — and the first where the re-pull's value was *additive* rather than corrective: it did not fix a stale claim, it found one that did not exist an hour ago. **Recorded because the twenty-five previous entries all justified the recheck as drift insurance**, which understates it.

<details>
<summary>Superseded verification line from the 2026-08-06 08:02Z run</summary>

**All five were verified against the live board on 2026-08-06 08:02Z rather than carried forward on the previous run's word** — THR-907 (`Todo`, assigned to Christian), THR-883 (`In Design`, `Urgent`, still the board's only Urgent row, `updatedAt` 07:30Z), THR-998 (`Todo`, `Medium`, pulled in full with `get_issue`), THR-961 and THR-962 (`Todo`, unstarted).

</details>

**THE CAVEAT RE-CHECK RULE FIRED AND CAUGHT A DE-DUPLICATION RATHER THAN A CORRECTION.** Both sibling reports read this run (`orchestrator-2026-08-06d.md`, `backlog-grooming-2026-08-06.md`) raise THR-883, and the grooming report states THR-998 at **two** options where the ticket carries **three**. **A verbatim fold would have printed THR-883 twice and THR-998 narrower than it is.** Seventeenth run of applying by hand a clause step 2.6 still does not contain — **recorded as the mechanism working only because a human-shaped step keeps standing in for a missing one.**

**Ask 1's six play links now serve sha `d8960910`, and the build DID move under a decision that is in progress — which is stated rather than glossed.** Thirteen PR merges (48 commits, 87 files) landed since the brief he last read. **Not one of the 87 changed paths matches `slice`** — `git diff --name-only 313a4354..origin/main | grep -i slice` returns empty, checked rather than assumed from commit subjects — so the thing he would be ruling on reads the same. **The distinction matters because the two previous runs said "unchanged" for a different reason** (nothing could publish at all); this run says "changed but not where it counts", which is a weaker claim and is written as one.

</details>

</details>

</details>

</details>

**The deploy verdict `deployed` was read from the payload, not inferred from the word.** `{"verdict":"deployed","needsChristian":false,"deployedSha":"2a95dad07a8d30c6c3dffdd92cc0e8fd0a177d08"}` — the sha equals `origin/main`'s tip, **and `2a95dad0` is the PR #1360 merge carrying THR-1045**, so the "one command runs every gate" line in the brief is verified against the artefact actually being served rather than against a merge subject. **Memory `vercel_skip_reports_success` warns of the converse**, which is why the sha is compared and the word is not trusted.

<details>
<summary>Superseded deploy note from the 2026-08-08 19:58Z run</summary>

**The deploy verdict `deployed` was read from the payload, not inferred from the word — and this run it carries a claim about a specific fix.** `{"verdict":"deployed","needsChristian":false,"deployedSha":"d856090dbc2f36748c477d0e2e411fc4791b1ecb"}` — the sha equals `origin/main`'s tip, **and `d856090d` is the PR #1358 merge that carries THR-1042**. So the sentence "the repair that lifted your hold is live" is a two-step read (probe sha → `git log` on that sha), not a merge-subject inference. **Memory `vercel_skip_reports_success` warns the converse failure** — a green status masking no deployment at all — which is why the sha is compared rather than the verdict word trusted.

<details>
<summary>Superseded deploy note from the 2026-08-08 18:55Z run</summary>

**The deploy verdict `deployed` was read from the payload, not inferred from the word.** `{"verdict":"deployed","needsChristian":false,"deployedSha":"d8960910fbedc392674674c6a053cb3e10f5efdb"}` — the sha matches `origin/main`'s tip exactly. **Memory `vercel_skip_reports_success` warns the converse failure** — a green status masking no deployment at all — which is why the sha is compared rather than the verdict word trusted. **Four consecutive runs reported `skipped` before this one**, so this is the first run since 2026-08-06 that can state the live site and `main` are the same commit.

</details>

</details>

### 1. The encounter verdict — FOUR rulings, one play session (THR-907)

- **2026-08-09 06:55Z — THE BLEMISH SET SHRANK WITHOUT ANYONE ASKING IT TO, AND THAT RETIRED THE TRADE-OFF THIS ASK WAS CARRYING.** Three of the five (THR-1006 raw float, THR-1050 `{cast:*}` in reaction labels, THR-1051 `content_grant` named to the player) are now in `Ready for Dev`, promoted overnight by the ordinary sweep. **`demo-fix-tickets-unqueued` leaves the digest as a result** — it existed to ask whether he wanted them queued, and they are. **The remaining rough edges are not re-counted here**, because a count that falls by attrition invites a fourth restatement; what matters is that nothing now blocks the session.
- **2026-08-08 19:58Z — THE BLEMISH SET WAS RE-DERIVED, HELD FLAT AT FIVE, AND CORRECTED DOWNWARD FROM LAST RUN.** Last run reported six by folding in THR-1037 (a verification task) and THR-1049 (a prune-vs-mount disposition); neither is a player-facing blemish. **The five that are: THR-1033** (inert result labels), **THR-1034** (raw float), **THR-1035** (raw outcome key), **THR-1036** (leaked `{adj}`/`{verb}`), **THR-1048** (`+15% success` and a raw `coercive`). All five still `Idea`, so the trade-off is unchanged: play now and discount, or say the word and they get queued. **A count corrected downward is worth the same ink as one corrected up** — the discipline is re-derivation, not direction.
- **2026-08-08 19:58Z — TWO NEW DEFECTS OF THE SAME CLASS WERE FOLDED IN AS A COUNT RATHER THAN RAISED AS ASKS.** THR-1050 and THR-1051 were filed at 19:16–19:17Z off the THR-1042 fix — both raw internal wording reaching the player on the ending screen. **They are already filed and already routed** (one queued, one held on a mutex that has since cleared), so nothing about them needs him. **Raising them separately would have split one standing trade-off into three and made the ask set look like it was growing when it was shrinking.**

<details>
<summary>Earlier entries for ask 1</summary>

- **2026-08-08 16:55Z — THE ONE-HOUR HOLD ON THIS ASK IS WITHDRAWN AS A HOLD AND RESTATED AS A TRADE-OFF.** Last run relayed `tb-orchestrator`'s demo-readiness FAIL with a wait-a-few-hours recommendation. **The four blemishes it names are real and still stand** (inert result labels, a raw float, a raw outcome key, a leaked `{adj}`/`{verb}` template in the story log). **What does not stand is the timeline:** THR-1033–THR-1037 sit in `Idea`, which T1 does not scan, so no session is scheduled to fix them. **Restated to Christian as: play now and discount four cosmetic edges, or say the word and they get promoted.** Recommendation is play — the four are surface and none of them touches what the four rulings are about.
- **2026-08-08 16:55Z — A NEW LEVER MAKES THIS SESSION CHEAPER AND IT SHIPPED AFTER THE ASK WAS WRITTEN.** THR-1030 landed `?outcome=<band>`, so any of the six endings of a given encounter is reachable from a link instead of by replaying until the dice cooperate. **The rare bands are the ones that most need a verdict and were previously the hardest to reach.** Supplied in the briefing with its `unauthored_band` caveat stated, since a band nobody wrote renders the base ending rather than an error.

- **Unblocked since 2026-08-01 17:34:34Z and unmoved on every published run since.** Nothing of ours has gated it for five days; the six links were supplied 2026-08-02 15:01Z and re-confirmed against the deployed sha this run.
- **It is FOUR rulings, not five.** Christian split the consequence verdict into its own later session in chat on 2026-08-02, because the aftermath work is agreed unfinished and ruling on it now would waste the ruling. **The four are: prose, firing rhythm, interface, and whether deciding is fun.**
- **The ask is a session, not a decision to make cold:** play the five-encounter slice and rule on the four. **"Needs another iteration" is a valid ruling** on any of them — it closes the ticket and charters follow-up.
- **The enabling artefact exists and is verified.** Six links (five direct-spawn routes, one free-play route for the firing verdict) against `https://threadbare.vercel.app`. They live in the briefing; **an ask whose first physical action is unnamed is incomplete** (finding 45).
- **THE ASK CARRIES NO CAVEATS — RE-CHECKED, NOT ASSUMED.** THR-923 and THR-979 are both `Done` and live on the currently-deployed sha **`50659dd7`**. **The sha is re-derived from the deploy probe every run**, which is the fix for the defect this line carried five runs ago: it named a superseded build while claiming to be verified against the live one.
- **Ordering note, stated as a suggestion and not a gate:** this session produces the concrete prose examples that ask 2 needs, so playing first is likely cheaper than deciding format cold. **Recorded as advice, never as a dependency** — ask 2 is not blocked on it.

</details>

### 2. RESOLVED 2026-08-09 08:45:02Z — the encounter-writing format is locked (THR-883)

- **THE ARTIFACTS THIS ASK SAID NOBODY WAS WRITING WERE WRITTEN, REVIEWED AND APPROVED INSIDE ONE HOUR.** PR [#1363](https://github.com/christianspliid-ui/threadbare/pull/1363) merged at 08:29Z carrying the amended [`nudge-authoring-spec.md`](https://github.com/christianspliid-ui/threadbare/blob/main/.claude/skills/encounter-pipeline/reference/nudge-authoring-spec.md) and the full-contract [Swollen Ford exemplar](https://github.com/christianspliid-ui/threadbare/blob/main/src/data/__fixtures__/nudge-exemplar/swollen-ford-exemplar.ts). **Christian reviewed both in chat with direct links and approved: *"looks fine."*** The gate was recorded on the ticket as `human gate satisfied via chat review 2026-08-09`, the closeout PR merged at 08:44Z, and THR-883 went `Done` at **08:45:02Z** — ten days as the board's only `Urgent` row.
- **THE ASK'S PREMISE WAS VERIFIABLY TRUE AND ITS CONCLUSION WAS WRONG — FINDING 64.** No ticket existed for the drafting job at any point; five consecutive runs re-verified that and were right every time. **The work was on branch `worktree-thr-883-contract-spec-exemplar` regardless.** An absence-of-ticket check cannot see work that lands as a branch, the same way finding 60's state check could not see a decision that lands as a plan-doc commit. **Fix shape carried into open agent work: pair every "nothing is filed for X" ask with one `gh pr list --search` or `git branch -r --list` before re-raising.**
- **NO CAUSAL CLAIM IS MADE FOR THE FIVE RUNS OF RAISING IT.** The branch predates the ask. **Recorded as coincident, not causal**, because the opposite reading is both flattering and unfalsifiable.
- **WHAT IT RELEASES, TAKEN FROM THE TICKET RATHER THAN COMPUTED HERE:** the eleven paused content tickets plus [THR-1047](https://linear.app/threadbare/issue/THR-1047) unblock for the orchestrator's next promotion sweep, and per ruling 8 the retrofit of all fifteen nudge-era templates rides THR-1047/THR-973 as the factory's pilot volume rather than becoming its own cleanup job. **PR [#1114](https://github.com/christianspliid-ui/threadbare/pull/1114)'s hold is lifted; its `holdReason` is now stale as prose and the next sweep owns it.** **None of that needs Christian**, and the brief says so rather than converting a resolution into a fresh ask.

<details>
<summary>Ask 2 as the producer-gap lead ask (2026-08-08 19:58Z → 2026-08-09 07:56Z, resolved)</summary>

- **2026-08-09 07:56Z — FOURTEEN HOURS, FIVE SWEEPS; NOTHING NEW WAS LEARNED AND THE ENTRY SAYS SO INSTEAD OF RESTATING THE CASE.** The gap is unchanged in substance. **What changed is one hour of age and one more work-picking round.** The absence was nonetheless re-verified rather than carried — `list_issues state:"In Design"` returns exactly THR-883 and THR-1043, and the morning's new `Idea` (THR-1053, on `concepts` in the Composition Contract) was checked individually and **is not the drafting job**. **A plausible-looking adjacent row is exactly how a verified absence stops being re-verified**, which is why the check costs one query every run.
- **2026-08-09 06:55Z — THIRTEEN HOURS, FOUR SWEEPS, STILL NO TICKET; THE ASK IS PROMOTED ON DURATION AND SAYS SO.** Nothing new was learned about the gap this run. **The change is that it survived two more work-picking rounds and an orchestrator sweep**, which is the same predicate that made it an ask at 19:58Z, applied again. **The brief drops the "nothing needed tonight" framing** it carried last night: that framing was correct while the gap was two hours old and is a way of not asking once it is thirteen.
- **2026-08-09 06:55Z — THE DELIVERABLE WAS RE-READ FROM THE TICKET RATHER THAN CARRIED.** THR-883's latest comment (2026-08-08T17:57Z, Christian's own): *"Fable drafts (1) the amended `nudge-authoring-spec.md` incorporating the Composition Contract, and (2) one exemplar encounter authored to the full contract … Christian's chat review of those two artifacts closes this ticket and unblocks the eleven content tickets paused behind it plus the factory harness (THR-1047)."* **The eleven-plus-harness figure in the brief is his, not this file's arithmetic.**
- **2026-08-09 06:55Z — A TICKET SEARCH WAS RUN BEFORE THE GAP WAS RE-ASSERTED, BECAUSE ASSERTING AN ABSENCE IS THE EASIEST THING TO GET WRONG.** `list_issues query:"authoring format writing rules worked example Fable"` returns THR-883 itself and fourteen unrelated rows; the four filed factory tickets (THR-1044/1045/1046/1047) were checked individually and none is the drafting job. **The absence is verified this run, not inherited from last run's verification.**
- **THE INFORMATIONAL ITEM EXPIRED AS DECLARED; WHAT REMAINS IS THE PRODUCER GAP, AND IT IS NOW AN ASK.** The format is settled and off his plate. **What closes THR-883 is his chat review of two artifacts — the amended `nudge-authoring-spec.md` and one full-contract exemplar — and neither exists.** Item 3 of the factory plan's five is the job that writes them, and it is **the one of the five with no ticket**, second run running, through one orchestrator sweep.
- **RAISED AS A WATCH-ITEM WITH AN EXPLICIT EXPIRY, NOT AS A DEMAND.** The brief says *nothing needed tonight* and *if it's still unscheduled when you next look, say the word*. **This is deliberately weaker than an ask and stronger than a queue line:** he cannot fix it (it wants a working session, not a decision), but he is the only reader who will notice if it never gets one, and the thing it blocks is the review that closes his largest ticket.
- **WHY IT IS HIS AT ALL, STATED SO THE ROUTING IS AUDITABLE.** Two lanes could each have closed it and neither did. **A gap that survives the mechanism designed to catch it stops being backlog and becomes a standing exposure** — the same shape as closed question 23's authorised-but-unfiled law, which has now gone seven runs.
- **Downstream, unchanged:** the eleven paused content tickets and PR #1114 stay paused, correctly — the format is settled but the written spec they would be authored against does not exist yet. **#1114's `holdReason` is stale as prose while remaining correct as a disposition.**

<details>
<summary>Ask 2 as the one-run informational item (2026-08-08 18:55Z, expired as declared)</summary>

### 2. RETIRED 2026-08-08 18:55Z — the encounter-writing format session collapsed into a short review (THR-883)

- **CHRISTIAN SETTLED IT IN CHAT THIS AFTERNOON AND THE LINEAR ROW DOES NOT KNOW.** Commit `0124ee79` (PR #1356, merged 2026-08-08 19:59 local) records eight rulings and the sentence *"THR-883 becomes a chat review of two Fable-drafted artifacts: the amended nudge-authoring-spec and one full-contract exemplar."* **The row still reads `In Design`, `Urgent`, with a description describing an interactive design session** — every state-shaped check this file has run for a week would still call the ask live. **See finding 60.**
- **The eight rulings, recorded here because the ticket does not carry them:** batch size 6 with variance visible in the batch report; briefs agent-drafted and chat-approved; **no exemptions ever** (the Composition Contract's exemption mechanism is deleted — a shape that cannot carry a block becomes its own encounter type); park-for-salvage on a double critic failure, never kill; contract expressed inline on the template; cast prose named-inline with **mandatory** binding, `{cast:*}` tokens only where the generated name earns its place; `byOutcome` floor of 3 bands; retrofit **all 15** nudge-era encounters.
- **What replaces it is NOT an ask, and that is deliberate.** The review consumes two artifacts that do not exist. **An ask he cannot act on today reads exactly like the week-old ask it replaces**, so it is carried as an informational item for one run and then dropped. **When the two artifacts land, that is a NEW ask with new evidence** (finding 12) — never this one reopened.
- **THE PRODUCER GAP IS THE LIVE ITEM AND IT IS AGENT-SIDE.** The factory plan enumerates five implementation items; four are filed (THR-1044/1045/1046 `Ready for Dev` `High`, THR-1047 `Todo`). **Item 3 — the spec amendment plus skill expansion, which IS the Fable drafting job — has no ticket.** Found by diffing the plan's own numbered list against the filings, not by reading the filings. **Carried in open agent work; wants a ticket.**
- **Downstream, unchanged for now:** the eleven paused content tickets and PR #1114 stay paused, correctly — the format is settled but the written spec they would be authored against does not exist yet. **#1114's `holdReason` is now stale as prose** (*"paused until the encounter-writing format is locked"*) while remaining correct as a disposition.

<details>
<summary>Ask 2 as it stood 2026-08-05 → 2026-08-08 (superseded)</summary>

- **PROMOTED to a standing ask 2026-08-05 20:55Z.** Not a new ask and not new evidence: it has been named inside ask 1 since 2026-08-01 and raised by `tb-orchestrator` in report after report. **Promoted because a bottleneck that appears only as another ask's subordinate clause reads as context rather than as an action.**
- **What it holds, measured rather than asserted:** eleven content tickets are paused behind it (WS5 batches 1a–1e, shell_proof, apotheosis gate, and Meeting Batch A / THR-875), plus PR #1114's four finished capital-cluster templates, which `check:armed-prs` reports as `held` with that reason as data.
- **The ask is Christian's own stated method,** chat 2026-07-30: prototype one encounter end-to-end with Fable, iterate until the exact format is locked, then have that same agent author the corpus. **This explicitly supersedes the "Fable plans only, executors implement" boundary for prose content.**
- **Two findings of his are still unanswered and are the session's actual agenda:** (a) the register spec does not produce the register — cards pass every automated detector and still ship euphemistic mood instead of stated mechanism, against his bar of *take the space to explain the reasoning behind the nudge*; (b) the factor-line strip is a QA tool shipped as player UI, the key:value-bolted-onto-prose shape he has rejected before.
- **Nothing decays while he waits, and the paused tickets are correctly paused** — landing content under an unlocked format is the rework this pause exists to prevent. **Surfaced as the largest blocked-work total on the board, never as a demand.**

</details>

</details>
</details>


### 2b. RESOLVED 2026-08-09 08:45:04Z — the process-ticket rule binds forward, and the old pile gets re-read (THR-871)

- **ANSWERED ON DISCORD 49 MINUTES AFTER IT ENTERED THE SET — the fastest close in this file's history.** Christian, verbatim: *"your choice is a false dichotomy. its not either of these. it is from now on and existing tickets should be re-read and reprioritized in the light of a cost benefit analysis"*.
- **HE REJECTED THE BINARY RATHER THAN PICKING A SIDE, AND THE THIRD OPTION IS THE SUBSTANTIVE ONE.** Forward-binding was the safe default already in force, so that half changes nothing. **The half nobody asked for is the instruction to re-read the existing pile on cost versus benefit** — merit, not age, and not demotion. **Roughly 13–17 of the now-39 ready items are in scope.**
- **THE ASK'S FRAMING WAS NARROWER THAN THE DECISION, AND THAT IS THE FINDING.** This file's standing advice is to frame asks so silence is safe; that advice produced a binary, and the binary would have lost the second half of his answer had he simply said "from now on". **Recorded against ~30 runs of advice that has only ever pushed toward binaries.**
- **THE RE-READ WAS ROUTED, NOT EXECUTED.** It re-orders the board; this lane writes two files and never Linear state. **It is in the brief's `## From Christian` section for the next working session, and the Discord receipt told him it was routed rather than done.** **The remit does not widen because the instruction is authentically his.**

<details>
<summary>Ask 2b as raised (2026-08-09 07:56Z, resolved same day)</summary>

- **RAISED BECAUSE A LANE DECLINED TO OBEY IT AND SAID SO, NOT BECAUSE ANYTHING BROKE.** Christian's 2026-08-08 amendment reads *"a process ticket without [a cost/benefit line] is demoted to the log by grooming"*, and the `daily-backlog-grooming` prompt restates it as a demotion to `Idea`. **Applied literally this morning it demotes 13–17 of the 34 Ready-for-Dev items in one pass**, because every process ticket on the board predates the rule and none can carry a line that was not required at filing.
- **THE LANE DEMOTED NOTHING AND RECORDED WHY.** It treated the clause as binding from ratification; checked the three tickets filed since (THR-1054, THR-1051, THR-1050) and found all three are content/UI defects rather than process tickets, so **zero items qualify either way**; and left the retroactive set to the weekly retro, which the amendment itself assigns the job of batching sub-bar rows. **It deliberately filed no ticket** — *"a process ticket about the process-ticket rule is the recursion the amendment exists to stop."*
- **WHY IT IS HIS AND NOT A TECHNICAL VERDICT.** THR-608 leaves CI state, merge mechanics and not-a-defect calls with the agent. **This is none of those: it is the scope of a directive he wrote**, and only its author can say which reading he meant. **Framed so silence is safe** — "from now on" is already the operating assumption, so a non-answer changes nothing and costs nothing.
- **THE SHAPE IS THR-968's, NAMED BY THE GROOMING LANE ITSELF:** a bullet that, obeyed literally, empties most of the queue while each individual move looks justified. **That precedent is what earned this a route to him rather than a queue line** — the previous instance had to be reversed after the fact.
- **FOLDED VIA STEP 2.6, AND THAT IS THE RUN'S FINDING.** Every probe this lane owns returned green; the board moved only forward. **Without reading the sibling's `## Needs Christian` section this run would have carried nothing new**, and the ruling would have sat in a dated ops file nobody opens.

</details>

</details>

### 3. A Tenacious-style trait — open option, explicitly NOT urgent, safe default is "stays parked"

- Fourth-wave decision 4 of THR-883: Christian would prefer a **Tenacious-style trait** over True for a river crossing. The Core registry holds only **5 continua**, so minting a sixth is **a content decision, recorded and not actioned**.
- **Surfaced as an open option with a stated safe default, never as a demand** — finding 12's shape. **Unchanged this run; recorded as unmoved, not as stalled.**

### 4. Two sound questions — he has to hear the game (THR-962, THR-961)

- **Promoted into this file 2026-08-02 15:54Z**, same reasoning that promoted ask 2: raised by `tb-orchestrator` run after run and reachable only through the hourly brief.
- **THR-962 — should the nudge stage carry the THR-346 cue sounds at all?** A yes/no on whether that beat wants audio. Most of the ticket is executor work — the sounds are wired to `Game/Encounter/`, whose only importer is the style guide, while `GameView` mounts `Game/encounter-stage/` instead — and the Christian half is one explicitly-marked paragraph: *"whether the nudge stage wants this cue design at all … that is design vision."*
- **THR-961 — how do the new encounter sounds actually feel?** Requires listening, not reading. Three of the nine per-reach tones are spec-exact; the other six were extrapolated. **Both are `Todo` and no executor can move them** until he has heard them.
- **Neither is urgent and neither decays.** Recorded as open options with a stated safe default of "unchanged", per finding 12's shape.

### 5. The action cards promise a danger that isn't real — (a) make the word true, or (b) print something else (THR-998)

- **NEW THIS RUN, 2026-08-06 08:02Z.** Raised by `daily-backlog-grooming` at ~07:16Z as a deferral from THR-766, which measured its two tunables, kept both, and exposed this as the defect underneath. **It is a redesign, not a tuning change**, which is why it was filed separately rather than folded into the tuning ticket.
- **The finding, measured rather than asserted:** every action card prints *steady / uncertain / perilous*, computed from the template's highest authored step difficulty. That difficulty is clamped from above before the roll, so at the two scales covering **85% of castable slots** (`local` 79%, `personal` 6%) the resolved probability is **flat across the entire difficulty range**. A "perilous" card and a "steady" card resolve identically — 64.8% success-at-cost, 28.0% success, 4.3% near-miss, 3.0% critical. **400 seeded streams per cell, n=519 templates.** It does not resolve with progression either.
- **The decision is which of two honest fixes, and it is Christian's because it is about what a card should *say*, not about a number.** **(a)** Compute the hint from the resolved probability — truthful by construction and self-maintaining as capability grows, but the same card then reads differently for different gods. **(b)** Stop printing a risk word where the danger doesn't vary, and print something else — the scale the working reaches, or what it costs. **The ticket's own recommendation, and this file's, is (b).**
- **A third direction exists and is listed to be RULED OUT, not pursued:** lowering `MIN_PROBABILITY_BY_SCALE` so authored difficulty bites again. It governs mortal resolution too, so it is not a player-cast-local change. **Named explicitly because an unlisted third option reads as an oversight when someone later finds it.**
- **Nothing decays while he waits and nothing is blocked on it** — the cards work, they just over-promise. **Surfaced as a decision with a stated recommendation, never as a demand** (finding 12's shape).

### 6. Does the encounter ending pop by itself now? — a play-and-confirm, not a decision (THR-1005, closed)

- **ENTERED 2026-08-08 07:58Z, folded verbatim from `daily-backlog-grooming`'s 07:16Z report** per step 2.6. **It is the successor to ask 7, not ask 7 restated** — ask 7 asked him to *close* a finished ticket; this asks him to *confirm the fix in play*. Filed as a new ask with new evidence (finding 12's rule).
- **The history in one line:** he reported on 2026-08-06 that the aftermath did not appear on its own and he had to click the thread badge. Both halves of the cause shipped (PRs #1321, #1322), were browser-verified against his exact repro — other modals stacked, multi-step encounter — and `daily-backlog-grooming` closed THR-1005 on that evidence at **07:20:01Z**.
- **What is being asked, and it is deliberately small:** next time he plays a pause-tier encounter, notice whether the ending appears unprompted. **No reply is needed if it works.**
- **If it still does not, that is a NEW ticket, not a reopen** — the old one's four investigations all describe defects that are now genuinely fixed, so a recurrence would be a different mechanism. **Stated in the ask so he does not have to make that call.**
- **Nothing decays while he waits and nothing is blocked on it.** The close already happened; this is confirmation after the fact. **Escalation trigger: if he reports a recurrence, it wants a fresh ticket the same day, because the evidence is freshest then.**
- **He will now pass through this naturally during ask 8**, which is a play-the-aftermath session. **Stated so the two are not scheduled separately** — but they stay distinct asks, because one needs no reply if it works and the other always wants a ruling.

### 8. The consequence verdict — the fifth ruling, unblocked 2026-08-08 13:35Z, hold lifted 19:58Z (THR-974)

- **2026-08-08 19:58Z — THE CAVEAT THIS ASK CARRIED FOR TWO RUNS IS GONE, AND THE ASK IS NOW UNQUALIFIED.** THR-1042 — the ending screen keeping the authored half of what happened and discarding the world's half — **shipped, merged at 19:32Z, and is live on the deployed sha.** The recommendation flips from *hold, or play discounted* to **play it**. **The withdrawal is what the brief leads on**, not the fix: a hold is resolved by its release, and the mechanism is explanation (finding 61).
- **2026-08-08 19:58Z — THE LIMIT OF THE FIX'S PROOF IS CARRIED INTO THE ASK, NOT LEFT IN THE TICKET.** It was browser-verified on `enc.letters_of_introduction` (4 → 9 consequence chips, the recovered four reading as sentences), **not on the five slice templates** — deliberately, since that template is the only shipped one carrying both an `aftermathConfig` and a rolling `rewardPool`. **His five take the same code path and were not individually re-checked.** Stated to him plainly, with what a still-thin ending would mean: not this defect recurring, but a second one.

- **ENTERED 2026-08-08 13:55Z, and it entered because work landed rather than because something broke.** THR-973 (*re-author the five slice aftermaths to the April migration bar*) merged at **13:35:11Z** and was the last open member of THR-974's `blockedBy` set. **First ask in this file's history added for that reason.**
- **This is Christian's own deferral coming due, not a new demand.** He split the consequence verdict out of the five-verdict slice session in chat on 2026-08-02, on the explicit reasoning that the 2026-08-02 director review had found the aftermath unfinished — one ending regardless of the roll, consequences not surfaced — and **ruling on consequence against that would waste the ruling.** The three things he was waiting for are now all `Done`: THR-969 (outcome-keyed variants, 08-02), THR-971 (consequence chips, 08-02), THR-973 (slice aftermaths re-authored, today).
- **The gate was read from the relation graph, not inferred from a commit subject.** `get_issue(THR-974, includeRelations:true)` → `blockedBy: [THR-971, THR-973]`, both `Done`; the body's third named gate THR-969 is `Done` too. **The two surfaces agree**, which matters because a gate recorded in prose but not as a relation (or the reverse) is exactly how a "cleared" dependency stays quietly uncleared.
- **The enabling artefact is verified live, not promised.** `check:deploy` returns `deployedSha: 7e1118cb`, equal to `origin/main`'s tip — **and that commit is the THR-973 merge itself.** So the finished aftermath is on the deployed build at the moment the ask is raised. **This is the check ask 1 failed five runs ago**, when it named a superseded sha while claiming live verification.
- **The ruling, in one sentence:** play a hand through to its ending and say whether **the change to the world is visible, and whether it feels like it happened in the world** rather than being announced at the player. **"Needs another iteration" is a valid ruling** and closes the ticket, chartering follow-up.
- **It shares its encounters with ask 1**, so both sittings can be one session if he prefers — **stated as convenience, never as a dependency.** The wayfinder map (THR-902) closes only when both are resolved.
- **Nothing decays while he waits and nothing is blocked on it** — but note this is the ask most likely to be *cheap right now* and expensive later, because the build he would rule on is deployed today and the board is actively landing more aftermath work (THR-1029 this afternoon, THR-989 and THR-1030 still queued). **Surfaced as a decision with a stated recommendation to take it soon, never as a demand** (finding 12's shape).

### 10. RESOLVED 2026-08-10 11:24:32Z — the stone ticket closes as not-a-defect (THR-1064)

- **ANSWERED IN 86 MINUTES BY ONE INBOUND MESSAGE.** Christian, Discord, verbatim: ***"go ahead"***, quote-replying the ping whose only ask was *"close THR-1064 as 'not a defect.' One word."* **The reading — close it as not-a-defect, the stone set stays as authored — is recorded ON THE TICKET** with a one-line reversal attached, because `## From Christian` is regenerated hourly and would erase it. **No state was set and nothing was claimed.**
- **THE ACTION IS SANCTIONED AND STILL UNDONE, WHICH IS THE PART THAT MATTERS.** Every previous inbound closure ended in a decision this lane could record and stop; this one authorises a `Done` write, which the remit forbids. **The ask is closed as answered while the write is routed to an executor.** This is closed question 23's shape — *an authorisation is not a delivery, and is MORE likely to be dropped than an open ask because it reads as handled.* Carried in open agent work until a session records the close.
- **A recurrence of the underlying question is a NEW ask with new evidence, never this one reopened** (finding 12).

<details>
<summary>The ask as it stood, 2026-08-10 09:58Z</summary>

- **ENTERED 2026-08-10 09:58Z, and it entered as a correction to this file's own output.** On 2026-08-09 15:12:xxZ he answered *"2. flip the label"* to a question this lane put to him. The framing it carried — *"one scene literally says pride preserved is a wall that keeps the world out — filed under transformation"* — **had the two poles the wrong way round.** Pride-preserved is filed under **preservation**, which is correct. The error came from the ticket body and was passed through without being re-derived.
- **The reversal is a technical verdict and was made by the executor, correctly, not routed to him.** `AxiologicalProfile` is signed `+1 = first-named pole`, stated at four independent sites (`src/types/agent.ts:5-6`, `ARCHETYPE_NAMES`, `poleLean.ts:19-22`, `meetingEncounter.ts:596-598`), with no negation in between. The executor claimed the ticket to implement the ruling, checked the convention first, and stopped. **That is the behaviour the debugging protocol asks for and it should not be eroded by the fact that it contradicted a director call.**
- **What is genuinely his:** the ticket sits `In Dev` recommending closure as not-a-defect, and **no CC lane may write `Done`.** One word clears it. It holds a working slot until then.
- **What is deliberately NOT his:** the real defect the investigation found — [THR-1071](https://linear.app/threadbare/issue/THR-1071), 37 of 40 converted templates writing the axiological profile backwards, so the merciful choice moves an agent toward Conqueror — is `High`, `Todo`, and **carries its own remedy decision in the ticket.** It proceeds without him. Raising it as an ask would be the mislabelling THR-608 forbids.
- **Why stone reads right and is not the bug:** the `humility_pride → preservation_transformation` rename inverted the pole *names* and defect 1 inverted the *signs*; the two cancel on stone alone. **Recorded because "it looked inverted" was true and still supported the opposite conclusion** — the same shape as finding 1 (re-derive before carrying), paid out here against a decision rather than an observation.
- **The lesson is logged as impediment #523** by the executor: when a ticket's evidence turns on a sign or ordering convention, re-derive the convention from code before implementing — *especially* when the ticket is specific and carries an approved decision, because a verdict inherits its brief's errors and then launders them.

</details>

---

**Ask 11 (the stone ticket's manual close, THR-1064) was resolved 2026-08-10 13:02:53Z and its section is deleted rather than archived** — the decision it carried is on the ticket, and the durable lesson is recorded as finding 70 and in the retired open-agent-work entry.

---

**One thing is agent work and is deliberately NOT an ask:**

- **PR #1114 (THR-860) stays unarmed by design.** It is a held branch, not a neglected one. **No sweep should re-arm it** until the format verdict lands. **As of 2026-08-02 21:55Z the probe knows this** — `check:armed-prs` returns `verdict: "held"` with the reason as data, so the disposition no longer depends on a reader re-deriving it.

**A new unexplained multi-hour pause would be a NEW ask with new evidence — never the overnight-wake question reopened** (finding 12). If he ever does want overnight throughput guaranteed: keep the machine awake and **on mains power**, because Windows Task Scheduler refuses the reaper on battery (`-2147020576`, memory `git_cleanup_automation`). Recorded as a fact, not a request.

**Twenty-three questions are answered, closed, and must not be re-asked:**

1. **The five empty projects** — Christian, chat 2026-07-25 21:53: *leave them open; they are intake buckets that refill as the game iterates.*
2. **The GitHub Actions payment block** — cleared 2026-07-25 ~17:09Z and again 2026-07-28 17:02Z, both verified by re-run, and corroborated many times since by independent mechanisms. **A recurrence is a new ask with new evidence, never this one reopened** (finding 12).
3. **THR-799 priority ordering** — Christian, quoted in THR-774/775, 2026-07-27: *the new encounter experience is still first priority.*
4. **Release of THR-821/820/777/778** — Christian, Discord 2026-07-27 14:38Z.
5. **The Linear "assign issue to PR author" switch** — Christian, Discord **2026-07-29 18:10:03Z**: *"can't find anything like that. I do not use linear or github at all. so any issues in linear or PRs in github with my name on them are yours."* **Closed by falsification, on the terms the ask itself set.** The second sentence is a **standing fact**: an assignee of "Christian Spliid" carries no information and must never be read as a human claim.
6. **Should the host machine stay awake overnight?** — **retired 2026-07-30 01:58Z by observation**, now fourteen consecutive awake nights, never answered directly and no answer needed.
7. **The Sphere-Governed Ascendant pivot** — Christian, 2026-07-30: *"the whole area of power curve for ascendant growth is still rudimentary as we build the world and the mortals."* Project parked in `Idea`; THR-870 carries the design scope and activates only when he moves it out.
8. **"Does the pip odds vocabulary match what you agreed?"** — closed 2026-07-30 20:54Z **on participation, not on an answer**: he has since sat three further sessions and renamed the pip row himself ("Nudge", never "Odds").
9. **"Seven or eleven?" (retrofit scope)** — closed by the ticket's own scoping: the fourth-wave comment folds the retrofit of the 7 into agent work.
10. **CLOSED 2026-07-31 00:54Z — "the five category drafts."** Christian read them; the fifth-wave checkpoint (21:33:12Z) records the review and encodes his guidance into the authoring contract. **Closed on evidence, four runs after it should have been** — see finding 52.
11. **CLOSED 2026-07-31 14:16Z — "the five design-first prototypes."** **The successor ask (THR-905, the slice roster) was a new ask with its own evidence, not this one restated — and it too is now closed.**
12. **CLOSED 2026-07-31 16:35:48Z — "which five encounters make up the vertical slice?"** Christian ruled in live chat: **Shrine Offering, the Unsafe Bridge, Snow on the Pass, the Crossroads bargain -> Full Moon Collection, the Swindled Family -> Swindler Found / Grateful Kin.** He kept Shrine Offering — the only pick already live in the production converter — over a freshly authored combat encounter, choosing **full pipeline coverage now, combat later.**
13. **CLOSED 2026-08-01 11:16:40Z, CONDITION MET 17:34:34Z, RE-OPENED AS STANDING ASK 1 — "the format verdict on the five prototype encounters" (THR-883 / THR-907), and with it "do the four capital-cluster templates land?" (THR-860).** Christian, Discord: *"The content cannot be approved until some defect tickets for the encounter interface lands."* **One sentence closing both halves of one sitting.** **The re-opening condition was precise and mechanical: THR-932, THR-933 and THR-935 reach `Done`. All three are now `Done` and the last is deployed.** *(This entry stays in the closed list as the **record of the closure and its terms** — the live ask is standing ask 1, filed as a NEW ask with new evidence exactly as this item specified. Do not re-ask the closed form.)*
14. **CLOSED 2026-08-01 — "switch the Docs gates check on in branch protection" (THR-931).** **He did it.** Verified independently rather than taken on report: ruleset `15479914` ("Main", `enforcement: active`) requires `Docs gates`. **Note the instrument** — `branches/main/protection` now returns `404 Branch not protected` since the classic rule was deleted, so the obvious endpoint cannot see the setting at all. **The ticket itself closed 2026-08-02 16:52:58Z; the ask was already finished.**
15. **CLOSED 2026-08-02 16:52:38Z–16:53:16Z — "three finished rows are stuck in a column no lane can move them out of" (THR-947, THR-931, THR-792).** **He closed all three inside 38 seconds.** The ask existed because no CC lane may write `Done`, which is the gap THR-846 opened the In-Dev scan to cover. **The ask carried an explicit escalation trigger (*"a fourth before any is cleared"*) that held flat at three for four runs and never fired.** **The In-Dev scan is what detected the clearing**, returning 2 rows where six prior runs returned 5. Verified per-row against `status`, not inferred from the count.
16. **CLOSED 2026-08-02 17:06:54Z — "one settings visit, two clicks" (THR-946).** **He made the visit ~17:00–17:05Z, and it resolved differently from how the ask was written — which is the durable part.** *Half the ask was never possible:* merge queue is offered only on **organisation-owned** repositories, and `christianspliid-ui` is a personal account, so "unlocked by the repo going public" was necessary but not sufficient. Falsified by Christian reading the ruleset editor's rule list with no merge-queue entry — **not** by any API call, because `Repository.mergeQueue` → `null` cannot distinguish *unconfigured* from *unavailable*. *The other half was done, plus a substitution:* he added `Test · Typecheck · Build` to ruleset `15479914` and **deleted the classic branch-protection rule**, THR-735's named fallback. **Verified independently with a differently-shaped instrument**: `GET /branches/main/protection` → **`404 Branch not protected`**, ruleset `active` with `strict_required_status_checks_policy: false` and both checks required. **The `BEHIND` livelock is structurally dead.** Docs sweep: THR-983. **A real queue would require transferring the repo to an organisation — its own designed ticket if ever wanted, never a side effect.**
17. **CLOSED 2026-08-03 06:27Z — "two finished tickets need a close" (THR-953, THR-910).** Both reached `Done` 16 seconds apart, during the shutdown window and ~32 minutes after the brief that last raised them. **The closing agent is not recorded and is deliberately not inferred** — neither ticket carries a closing comment, and neither had a PR whose merge could have auto-closed it, while four tickets closing in the same 06:14–06:27Z cluster *do* have merges behind them. **Same class as item 15, not item 15 reopened.** **The escalation trigger the ask carried (*a third park before either clears*) expired without firing**, the park count going 2 → 0 instead; recorded as a correct non-firing rather than dropped.
18. **CLOSED 2026-08-06 05:07:27Z — "should items ever get stronger over time?" (THR-996).** Christian, Discord, verbatim: ***"Turn the enchantment system on"***. **Answers option 1 of three, near-verbatim — the ticket's option 1 is titled "Turn it on."** while option 2 is the explicitly *narrow* variant and option 3 is deletion. **Deliberately not treated as ambiguous** (findings 7 and 12): an unqualified instruction matching an option's own title is an answer, and asking him to disambiguate between "on" and "on, narrowly" would be ratifying a settled call. **The reading was stated back in the receipt instead**, making a correction cost one word. **THE FIRST ASK EVER CLOSED BY AN INBOUND MESSAGE**, and the first raised-surfaced-answered-recorded inside a single night: 02:32Z → 02:55Z → 05:07Z → 05:57Z. **The verdict is recorded ON THE TICKET, not only here**, quoting him and stamping `human gate satisfied via chat review 2026-08-06`, because `## From Christian` is regenerated hourly and would have erased it. **No state was set and nothing was promoted** — THR-996 stays `Todo`; promotion is `tb-orchestrator`'s T1 call.

19. **CLOSED 2026-08-06 16:04:23Z — "what were you doing when the aftermath didn't appear?" (THR-1005).** Christian, Discord, verbatim: ***"There were other modals waiting in the screen. The start modals where you get your first action cards came through. The second step came automatically but not the aftermath. I had to click the notification"***. **Answers all three limbs of the question as asked** — other cards stacked (yes, the opening action-card modals), hand-driven or automatic (automatic: "the second step came automatically"), and whether the badge worked (yes: "I had to click the notification"). **THE SECOND ASK EVER CLOSED BY AN INBOUND MESSAGE, and the first where the channel demonstrably saved work rather than merely functioning** — checkpoint 2 had written that this answer "would probably collapse this", so the alternative was a third session repeating two failed reproductions. **The verdict is recorded ON THE TICKET**, quoting him and reading it against both checkpoints, because `## From Christian` is regenerated hourly. **No state was set and nothing was claimed** — THR-1005 stays `In Dev` and unassigned; picking it up is the executor lane's call. **A recurrence of the symptom is a NEW ask with new evidence, never this one reopened** (finding 12).

20. **CLOSED 2026-08-08 06:57:04Z — "is the modal stacking itself wrong?" (THR-1005).** Christian, Discord, verbatim: ***"stacking is fine and should be part of our ux law library. as long as the stacking is chronological to the game state, so the player can induce the sequence of happenings"***. **Answers the ask's one open limb in its first four words**, matching the expected answer the ask itself named (*"a one-word 'close it'"*). **THE THIRD ASK EVER CLOSED BY AN INBOUND MESSAGE AND THE FASTEST — 11 hours raise-to-record.** **Taken as unambiguous rather than queried** (finding 7): the trailing clause is additive and constrains a future law, not the shipped fix, so querying it would have ratified a settled call; the reading was stated back in the receipt so a correction costs one word. **THE SECOND HALF IS A NEW DIRECTIVE, NOT PART OF THIS CLOSURE** — *"should be part of our ux law library … chronological to the game state"* is a UI Law to author in `Docs/design-system/laws.md`, **outside this lane's remit, routed and not executed**, first exercise of the step-0 table's outside-the-remit row. **It has no ticket as of this run and wants one**; it is carried in open agent work above, because `## From Christian` is regenerated hourly and cannot hold it. **No state was set and nothing was claimed** — THR-1005 stays `Todo`, the close is Christian's. **A stacking complaint about a specific screen later is a NEW ask with new evidence, never this one reopened** (finding 12).

21. **CLOSED 2026-08-08 07:09:32Z — "is the overnight quiet a fault, or is it just the machine being off?"** Christian, Discord, verbatim: ***"overnight quiet is normal"***. **The ask had supplied that exact sentence as its own suggested answer** (*"one line from you — 'overnight quiet is normal' — and I'll stop asking"*) **and he returned it verbatim, with nothing else.** **THE FOURTH ASK CLOSED BY AN INBOUND MESSAGE; 19 hours raise-to-record.** **The lesson is about ask design, not about the answer:** an ask that writes the reply for him gets answered at the lowest possible cost, and this is the cheapest closure in the file. **The standing verdict is now: hourly lanes going quiet between roughly midnight and morning is EXPECTED and must not be raised.** **A multi-hour stoppage DURING THE DAY is a different shape and remains a new ask with new evidence** (finding 12). **`check:lane-silence` does not know any of this** — it will keep returning `needsChristian: true` on this pattern nightly, so the decline is a standing judgement carried in prose until the probe learns the window; that gap is tracked in open agent work.
22. **CLOSED 2026-08-08 07:20:01Z — "THR-1005 is finished and approved and needs the close."** **Not closed by Christian and not by this lane: `daily-backlog-grooming` set it `Done`** on evidence that shipped overnight (PRs #1321, #1322, browser-verified against his exact repro). Verified with `get_issue`, not inferred: `completedAt: 2026-08-08T07:20:01.097Z`, `statusType: completed`. **THE FIRST ASK IN THIS FILE EVER RESOLVED BY A THIRD-PARTY LANE.** **The ask's framing was wrong in an instructive way** — it said *only Christian can close this*, which is true of the **rule** (no CC lane may write `Done` on its own work) and false of the **outcome** (a sibling lane holding independent evidence made a defensible technical verdict). **Finding 17 generalises: before carrying an ask forward, check not only whether it was ANSWERED elsewhere but whether it was RESOLVED by someone else.** **The escalation trigger it carried — a second finished-but-open row before this one cleared — expired without firing**, recorded as a correct non-firing rather than dropped. **The successor is standing ask 6, a play-and-confirm raised by the same lane in the same report; it is a new ask, not this one reopened.**

23. **CLOSED 2026-08-08 10:46:20Z — "may a working session write your beat-stacking ruling into the Law Book?"** Christian, Discord, verbatim: ***"go ahead and write the law"***. **Answers standing ask 7 in five words, matching the answer shape the ask itself named** (*"One word from you and a working session picks it up"*). **THE FIFTH ASK CLOSED BY AN INBOUND MESSAGE AND THE FASTEST — 3h58m raise-to-record**, beating closed question 20's 11 hours. **Taken as unambiguous rather than queried** (finding 7): the message names no ticket and no law, but **exactly one item in the ask set was a law awaiting authorisation**, and querying a settled call would be the ratification finding 7 forbids; the reading was stated back in the receipt so a correction costs one word. **THE AUTHORISED WORK IS OUTSIDE THIS LANE'S REMIT AND WAS ROUTED, NOT EXECUTED** — writing `Docs/design-system/laws.md` is a repo edit and filing the ticket is a Linear write. **Second exercise ever of the step-0 table's outside-the-remit row, and the first where the authorisation ITSELF is the unactionable thing.** **The rule to carry: a closure that authorises work is not a closure that delivers it, and it is MORE likely to be dropped than an open ask, because an authorised item reads as handled.** The exposure is carried in open agent work. **The law is not written and has no ticket as of this run.** **A later complaint about beat ordering is a NEW ask with new evidence, never this one reopened** (finding 12).

**One standing switch exists but is not an ask:** the orchestrator lane can be disabled on one word from Christian (live 2026-07-27 19:49Z). **Its default state is the intended one**, so silence resolves it.

**A second standing switch, likewise not an ask:** `C:\Users\chris\.claude\threadbare-pause.json` declares a deliberate pause (presence is the declaration; delete to re-arm). **Closed question 21 removed the nightly need for it**, but it remains the correct instrument for a *daytime* pause on token limits. **The marker on disk covers 3–5 August and has expired** — that is the design working, not a defect.

**Three durable corrections survive closed asks and are kept as rules:**

- (a) A blanket `git stash` was the wrong remedy for home-tree dirt; the single-path form is correct. Parking his tool-permission edits was never necessary.
- (b) `$REATTACH_MAX_TRACKED_DIRTY = 0` governs the detached-HEAD reattach path and never evaluates while HEAD is on `main`. The conclusion held; its stated reason did not.
- (c) **Tracked dirt alone does not stall autosync.** The stalling class is the *intersection* of locally-modified **∩** changed-by-incoming-commits — proven negatively by many clean fast-forwards with `.claude/settings*.json` dirty, **proven positively 2026-07-31 14:16Z** when `Design/briefing.md` joined the dirt and the next incoming commit touched it, and **proven negatively again 2026-08-01 09:11** when an 88-commit fast-forward completed with both settings files still dirty. **Extension earned 2026-08-01 (THR-937): the same intersection rule governs *untracked* files**, which is what the whole 11-hour stall actually was — autosync's `skip:` line says "uncommitted changes that would be overwritten" for both cases, so **the message does not distinguish them and must not be read as naming tracked dirt.** **The guard shipped for this fired in production 2026-08-01 14:50** and behaved exactly as specified: hash-matched, deleted, synced.

---

---

---

### 9. RESOLVED 2026-08-10 07:01:50Z — "drop and re-author" extends to the whole shelf (THR-848/855/856/858/859/861/863/864)

- **HIS ANSWER, VERBATIM AND ENTIRE:** *"gogogo"* — sent **24 seconds** after the ping that carried the question, in reply to a message whose only question was *"Does 'drop and re-author' cover the rest of the shelf, or just the four?"* with the recommendation *"extend it"* attached.
- **THE READING IS STATED RATHER THAN ASSUMED, BECAUSE THE WORD IS NOT SELF-INTERPRETING.** *"gogogo"* is an affirmative with no object. It is read as **approval of the attached recommendation** — extend the call to the remaining eight batch tickets (~34 encounters) — on three grounds: the message asked exactly one question, it named a recommendation, and it said a line either way would close it. **The reading is carried in the brief with a one-line correction path** (*"just the four"* reverts it), so a misread costs one message rather than thirty-four encounters.
- **WHAT IT SETTLES.** The eight remaining WS5 batch tickets are dropped and re-authored under the locked format rather than retrofitted: [THR-848](https://linear.app/threadbare/issue/THR-848), [THR-855](https://linear.app/threadbare/issue/THR-855), [THR-856](https://linear.app/threadbare/issue/THR-856), [THR-858](https://linear.app/threadbare/issue/THR-858), [THR-859](https://linear.app/threadbare/issue/THR-859), [THR-861](https://linear.app/threadbare/issue/THR-861), [THR-863](https://linear.app/threadbare/issue/THR-863), [THR-864](https://linear.app/threadbare/issue/THR-864). **The retrofit surface goes to zero** — it was 7 after the 08-09 verdict, and the pause that produced it is now fully spent.
- **NOTHING WAS EXECUTED HERE.** Re-opening eight tickets, dropping branches and closing PR [#1114](https://github.com/christianspliid-ui/threadbare/pull/1114) are all now sanctioned by a director decision, **and this lane still does not touch ticket state, branches or code.** Routed through the brief's `## From Christian` section and named as routed in the Discord receipt — the same separation recorded on 08-09 15:58Z, and the reason the inbound channel is safe to keep.
- **Superseded framing, kept because it is what the answer was given against:**

- **THE ASK EXISTED BECAUSE HIS EARLIER ANSWER WAS SCOPED AND THE SHELF WAS NOT.** *"go B for the four encounters"* (15:11:14Z) settled [THR-860](https://linear.app/threadbare/issue/THR-860)'s four capital-cluster templates — this file recorded at the time that *"the retrofit surface stays at 7 rather than growing to 11."* **Those remaining tickets are in the identical condition**: ~34 encounters written to authoring instructions that predate the format lock, held rather than guessed at ([THR-848](https://linear.app/threadbare/issue/THR-848), [THR-855](https://linear.app/threadbare/issue/THR-855), [THR-856](https://linear.app/threadbare/issue/THR-856), [THR-858](https://linear.app/threadbare/issue/THR-858), [THR-859](https://linear.app/threadbare/issue/THR-859), [THR-861](https://linear.app/threadbare/issue/THR-861), [THR-863](https://linear.app/threadbare/issue/THR-863), [THR-864](https://linear.app/threadbare/issue/THR-864)).
- **IT WAS CHECKED AGAINST THE RECORDED VERDICT BEFORE BEING RAISED.** THR-985 forbids re-raising a decision already taken, and the failure mode here is specifically a *partially* answered question looking answered. **The verbatim answer names four encounters and this file's own contemporaneous note names the remainder as untouched**, so the remainder is unanswered rather than re-asked. Recorded so the distinction is auditable rather than asserted.
- **A RECOMMENDATION IS ATTACHED: extend it.** The same reasoning that produced *"go B"* applies unchanged — the pause was bought to stop pre-lock prose shipping, and re-authoring under the new pipeline costs less than retrofitting prose that was never right. **Silence is not safe here either:** the batches stay unlanded either way, so "do nothing" has a running cost.
- **ONE LINE EITHER WAY CLOSES IT**, and it is the only ask currently in the set that unblocks other tickets — the other six are play and taste. **Source:** `tb-orchestrator` 06:26Z report, folded per step 2.6.

## Resolved 2026-08-08

- **07:58Z — TWO ASKS CLOSED INSIDE ONE HOUR BY TWO DIFFERENT MECHANISMS, WHICH HAS NOT HAPPENED BEFORE.** Ask 6 by inbound message (Christian, 07:09:32Z); ask 7 by a sibling lane acting on the board (`daily-backlog-grooming`, 07:20:01Z). **Both verified at source rather than inferred from the ask set shrinking** — the message off the REST cursor, the closure off `get_issue`. **The ask set falls 7 → 6 net**, one new ask arriving from the same sibling report that closed the other.
- **07:58Z — AN ASK THAT WROTE ITS OWN ANSWER GOT ANSWERED VERBATIM.** The overnight-quiet ask ended *"one line from you — 'overnight quiet is normal' — and I'll stop asking."* **He sent exactly that string and nothing else.** Recorded as evidence about ask design rather than about the answer: **supplying the reply text is the cheapest possible closure**, and this is now the file's cheapest.
- **07:58Z — A CLOSED ASK LEFT A LIVE GAP BEHIND IT, AND THE GAP WAS STATED RATHER THAN LET SLIDE.** Christian's verdict lives in prose; `check:lane-silence` reads a marker file and commit history and knows nothing of it, so it will re-raise the same pattern nightly. **The decline is therefore a standing judgement this lane must re-make every run** — the decay shape of finding 48. **Carried into open agent work with a stated fix shape** (quiet-hours window, or a non-expiring marker mode).
- **07:58Z — A PROBE'S `needsChristian: true` WAS DECLINED FOR A FOURTH CONSECUTIVE RUN, ON STRONGER GROUND THAN THE PREVIOUS THREE.** Those rested on finding 12 (silence resolves an ask with a safe default) and could in principle have been wrong. **This one rests on a direct answer and cannot be.** Stated explicitly so the streak is not read as four equivalent judgement calls.
- **07:58Z — LAST RUN'S SANDBOX FINDING WAS NARROWED BY RE-DERIVATION, NOT WITHDRAWN.** The worktree-isolation guard refuses `git -C <home tree>` **from inside a worktree-isolated session**; the identical commands ran clean this hour because they ran **before** the worktree was cut. **The remedy shrinks from "change the guard" to "note the ordering in step 2."** Finding 21's shape — the check confirmed the observation and shrank the claim.
- **07:58Z — A CONFLICTED PR WAS OBSERVED INSIDE ITS GRACE WINDOW FOR THE FIRST TIME.** #1344 at `ageMinutes: 21` against `ARMED_DIRTY_ESCALATE_MINUTES` 90 → `needsSession: false`. **Every prior observation of this class was already past the threshold**, so this is the first evidence the tiering discriminates rather than merely labels.
- **07:01Z — AN ASK CLOSED BY INBOUND MESSAGE FOR THE THIRD TIME, AND FOR THE FIRST TIME THE SAME MESSAGE CREATED WORK THIS LANE MUST REFUSE.** *"stacking is fine"* closed ask 7's open half; *"should be part of our ux law library"* is a build instruction outside the remit. **Both halves were handled in the same pass and differently on purpose** — recorded, versus routed-and-declined. **First exercise of the step-0 table's outside-the-remit row**, which until now had never fired.
- **07:01Z — THE ROUTING SURFACE FOR THAT DIRECTIVE IS KNOWN-WEAK, SO THE VERDICT WAS WRITTEN SOMEWHERE DURABLE INSTEAD.** `## From Christian` is regenerated hourly and no lane reads it — the skill names this defect in its own step 2.6 preamble. **The directive is therefore recorded as closed question 20 and as open agent work**, both of which survive the rewrite. **The exposure is stated rather than resolved:** nothing files tickets from this file either, and if no session picks it up, a director directive still evaporates.
- **07:01Z — A PROBE'S `needsChristian: true` WAS DECLINED FOR THE THIRD CONSECUTIVE RUN, ON A DIFFERENT PROBE THAN THE PREVIOUS TWO.** `check:lane-silence` returned `silent`; the underlying question is ask 6, which carries a stated safe default and is now three nights consistent. **Passing it through would have re-rung a doorbell for a question silence already resolves** (finding 12). **The pattern across three runs is worth naming: the probes are correctly conservative and the lane is correctly the place that judges them.**
- **07:01Z — TWO PROBES DISAGREED ON LIVE DATA EXACTLY AS THR-1001 PREDICTED THEY WOULD.** `check:task-heartbeat` → `ok` (all lanes within 2 slots) while `check:lane-silence` → `silent` (8.5h gap). **The scheduler's catch-up burst clustered three lanes' `lastRunAt` inside eight seconds**, which is precisely why the silence probe reads commit history instead. **First observation of the designed divergence in production**; recorded because a future run seeing `ok` beside `silent` might otherwise read one of them as broken.
- **07:01Z — THE SKILL'S DOCUMENTED STEP-2 COMMAND IS NO LONGER RUNNABLE FROM A WORKTREE-ISOLATED BASH SESSION.** The isolation guard refuses `git -C "$HOME_TREE" …` outright. **PowerShell ran the identical read-only queries**, so the freshness ping completed — but by an undocumented route. **Recorded as a sandbox fact, not an outage**, and carried into open agent work as wanting a ticket.
- **EARLIER 2026-08-07 ENTRIES WERE PRUNED** per this file's own retention rule; recover with `git log -p Design/user-actions.md`.
