# User Action Required

**Last updated:** 2026-07-30 03:58 local, by the hourly `keep-work-flowing-cc` CC task.

**ZERO OPEN CHRISTIAN-OWNED ASKS — sixth consecutive run at zero, and this run RETIRES the last standing ask, taking the file to a true empty.** Discord inbox **genuinely empty** for a fourth consecutive hour — the `after=` cursor returned a literal `[]` (3 bytes). Home tree healthy: on `main`, 0 stranded, **0 behind**, 2 tracked-dirty (settled). Deploy `skipped`-benign at a **third consecutively advanced** publish point. Merge gate `healthy`, `startupFailureCount: 0`. Fleet heartbeat `ok` — all 8 enabled lanes within tolerance. **The hour's event is singular and it is the largest this file has recorded: THR-845 SHIPPED at 01:21:50Z, closing the invisible-queue class at reader, writer and enforcer. Finding 35 paid out in full — the fix was to stop reading the field, and the reader-side re-scope is what landed. The `shelf` vs `picker-visible` pair that this file tracked for eight runs is now retired by construction, because the filter that created the gap no longer exists.**

## This run's measurements (2026-07-30 01:58 UTC)

Full hour since the previous brief; the lane held its `:45` slot and the deltas below span a true hour.

- **THR-845 is Done — `completedAt` 2026-07-30T01:21:50.374Z — and it shipped all three sides, not just the one this file predicted.** `git show --stat bcfbe645` names 10 files: **reader** (`.claude/skills/pull-work/SKILL.md`, +16), **writer** (`.claude/skills/orchestrator/SKILL.md` +12, `Docs/ops/scheduled-task-prompts/tb-orchestrator.md` +9), **enforcer** (`scripts/stale-claim-sweep/index.ts` +119, `constants.ts` +13), plus docs. Merge commit subject: *"close the invisible-queue class at writer, reader and enforcer."*
  - **The reader-side change is quoted verbatim because it is the load-bearing one.** `pull-work` Step 1 now reads: *"Pick the top candidate — **every** queue item, not only the unassigned ones (THR-845: an assignee on `Ready for Dev` is noise, not a claim, and filtering on it hid the board's two highest-priority issues)."* And line 176: *"The second call deliberately does **not** filter on `assignee:null`… An assignee on `Ready for Dev` is not a claim — claims are `In Dev`, which Step 1.5 gates on."*
  - **The metric this file has reported hourly since 2026-07-29 is now undefined, and that is the correct outcome rather than a measurement gap.** There is no `picker-visible` count to report because there is no filter to be invisible to: shelf **50**, candidates **50**. **A pair of numbers converging because one of them stopped existing is not the same as them agreeing** — worth stating so a future run does not read "50/50" as the old gap having closed on its own.
  - **The write-side leak is still live, and it is now cosmetically-only live.** THR-860 (`createdAt` 00:29:50Z) and THR-861 (`createdAt` 01:30:08Z) both carry `assignee: "Christian Spliid"`, confirmed on `get_issue`, not read off a list. **THR-861 was created 9 minutes AFTER the writer-side fix merged** — but the live prompt is `~/.claude/scheduled-tasks/tb-orchestrator/SKILL.md`, a file outside the repo that the merge cannot touch, so this is expected rather than a disconfirmation. **The honest status: unfalsifiable from throughput from here on.** The reader ignores the field, so a named row costs nothing and produces no visible symptom — the leak can now only be observed by counting, never by consequence.
- **THR-860's description contains a false verification claim, and it is worth recording as evidence rather than as a fault.** It states verbatim: *"**Assignee deliberately set to null on creation, then verified with a separate** `save_issue` **+** `get_issue` **re-query**"* — and `get_issue` at 01:56Z returns `assignee: "Christian Spliid"`, `assigneeId: 35005bad…`. **This is finding 32's third and cleanest instance: the verify was almost certainly true when it ran and false later.** The row's own prose is the artefact that proves an immediate verify cannot distinguish "my write held" from "my write held briefly".
- **Queue: BACKED UP — 50 ready (0 Urgent / 0 High / 15 Medium / 35 Low), 0 In Dev.** Nothing stale: oldest `updatedAt` is THR-739 at 2026-07-24T20:29:29Z, **~5.22 days**, inside `STALE_ISSUE_DAYS`=7. Shelf 49 → 50 (THR-861 filed 01:30Z).
  - **`In Dev` is EMPTY, and this is the first hour where that is unambiguously good news rather than ambiguous.** THR-845 vacated it by shipping. The pickup lane last fired 01:01:05Z — **20 minutes before the reader fix merged** — so it ran under the old filter and is not evidence about the new one. **The 02:00Z run is the first observation that will be, and this run states the test in advance (finding 29's method): if the 02:00Z pickup claims nothing with 50 candidates and no WIP, the binding constraint is not visibility and THR-836 is confirmed as the real gate.**
  - **THR-836 is now the leading candidate for binding constraint, and it is already on the shelf at Medium.** *"Nothing authors a coordination block for directly-filed Ready-for-Dev issues, so pull-work Step 3 refuses most of the queue."* Consistent with the shelf's composition: **21 of 50 carry the `Deferral` label**, and memory `deferral_no_coord_block` records that filed Deferrals have no coordination block by construction. **Stated as a candidate, not a finding** — nobody has yet counted how many of the 50 actually fail Step 3, and that count is the missing evidence. **No ticket from here** (finding 23).
  - **THR-856 carries no project** — carried unchanged, a fourth run. Disposition belongs to `daily-backlog-grooming`, whose remit this is. **No ticket from here** (finding 23).
- **Deploy (step 2.5): `skipped`, benign, `deployedSha` ADVANCED a third consecutive time to `bcfbe6454c99ea6758426551b1c98ae0d50cb26d`** (was `2eb2cf63…`, before that `955d0919…`). Verbatim: *"The live site is up to date. Commits since the last publish (bcfbe645) only touched notes and docs, so the game itself did not need rebuilding."* **The publish point is now the THR-845 merge itself** — so the classifier tracked a real code merge and then correctly skipped the docs commits behind it. Finding 28 satisfied three times over; **the evidence moved again while the verdict word did not**, which remains the thing to watch.
- **Merge gate (step 2.5b): `healthy`, `startupFailureCount: 0`, `standDown: false`.** Verbatim: `{"verdict":"healthy","summary":"Automated checks are running normally.","needsChristian":false,"standDown":false,"startupFailureCount":0}`. **Twelfth consecutive independent confirmation** that Christian's billing action holds.
- **Step 2.7 (heartbeat) returned `ok`.** Verbatim: `{"verdict":"ok","needsChristian":false,"checked":8,"stalled":[],"neverRun":["monthly-rulebook-review"],"summary":"All 8 enabled scheduled tasks are within 2 slots of schedule."}`. **Eleventh consecutive `ok`, and a second consecutive genuinely-awake overnight** — three hourly lanes fired at 01:01Z / 01:26Z / 01:53Z with **distinct** timestamps, so the wake-boundary defect is untriggered rather than merely quiet.
- **Reaper healthy — ran 03:40:02 local (01:40Z), 18 minutes before this brief.** `SUMMARY:` **36** worktrees / **46** branches / **2** stashes / **0** needs-disposition. **Branches 48 → 46** (two pruned; other three counts flat), so the log is demonstrably live rather than frozen — and the `=====` header at 03:40:02 is independent proof of the run. 216 total runs logged.
- **Home tree** on `main`, **0 stranded**, **0 behind**, **2 tracked-dirty** (`.claude/settings*.json` — settled property, correction (c) below), **1 untracked** (Friday's retro draft).
- **Discord: the cursor returned `[]` — 3 bytes, zero messages, for a fourth consecutive hour.** No receipt owed, none sent; cursor left at `1532100414189867109`. `allowFrom` was read from `access.json` **before** any classification, preserving fail-closed ordering with nothing to authorize. **Method note carried, third run running:** the skill's inline `python -c` allowlist read is unusable in this sandbox (no `python` on PATH); the file was read directly again. **Fail-closed holds only because the failure is noticed each time** — a silent `||` fallback there is indistinguishable from an empty allowlist, so this is a skill fix owed, not an hourly workaround. **Silence is not evidence he is away** (finding 8).
- **Ping gate (step 6): no ping sent, correctly.** The stored hash is `e3b0c44298fc…b7852b855` — the SHA-256 of the empty string, i.e. last run's empty state. This run's Needs-Christian is also empty, so the hash matches and the doorbell stays silent. **The empty-state hash matching is the gate working, not the gate failing to fire.**
- **Sibling reports (step 2.6): two in-window, no items folded — and finding 36's file-selection axis is now FOUR-deep in one day.** `orchestrator-2026-07-30d.md` (written 01:50Z, 8 minutes before this brief) reads *"Nothing needs you. The Nudge Model WS5 content migration (THR-838) keeps moving one right-sized batch per hour…"* — **empty state, skipped per the rule**, and top-level `##` so the consumer's pattern matched. `backlog-grooming-2026-07-29.md` (~12h) leads with *"Nothing needs you"* — likewise skipped; its billing-cleared note is corroborated independently above. `weekly-hygiene-2026-07-26.md` is **~112h**, outside the 36h window.
  - **Same-day collision is now `-30.md` / `-30b.md` / `-30c.md` / `-30d.md` — four files, one producer, one day.** mtime and lexical order still agree (`.` < `b` < `c` < `d`), **so the consumer has been correct-by-luck four consecutive hours.** Collision cause ticketed (THR-849); **consumer-side half still unticketed.**
- **Zero Linear writes.** Three column reads (`Ready for Dev`, `In Dev`, `Done`) and two `get_issue` confirmations. Read-only throughout.
- **This run's worktree was created outside `.claude/worktrees/`** (`Dev/Projects/kwf-wt-20260730-0154`), with `rev-parse --show-toplevel` asserted after creation, **the `.git` file's existence checked (THR-797), and the home tree's branch re-read as `main` in the same command.** **The home tree was touched by `git -C` reads, one `git worktree add`, three read-only `npm run` probes, `gh` reads, and nothing else.**

## Open agent work (not Christian's)

- ~~**THR-845 is now In Dev…**~~ **SHIPPED 2026-07-30 01:21:50Z at all three sites (reader / writer / enforcer).** Kept as a one-line record because **the causal chain is the durable part**: eight runs of write-side investigation produced three dead remedies; one sentence from Christian relocated the defect to the reader in a single pass. **Finding 35 is the generalisation and it is now evidence-backed rather than predictive.** The re-scope this file recommended — *"the fix is to stop reading the field"* — is what shipped, and the grep it also asked for (every `assignee:null` predicate across `pull-work`, the orchestrator lane, the process linter) appears to have been done, since the enforcer half exists.
- **Step 2.6's section extraction is keyed on the producer's incidental formatting AND its file naming, and neither is stable. Re-derived 01:58Z — the file-selection axis is now FOUR-deep in a single day.** The same-day set reached **four files** (`-30.md`, `-30b.md`, `-30c.md`, `-30d.md`); mtime and lexical order still agree, **so the consumer has been correct-by-luck four consecutive hours.** The agreement is alphabetical rather than robust: a `-30-2.md` or a reversion to an undated name breaks lexical order while mtime stays correct. Prior derivation, unchanged: `orchestrator-2026-07-30.md` uses `## Needs Christian`; `orchestrator-2026-07-29.md` uses `### Needs Christian` (nested, thirteen occurrences) — **same lane, two files, one day apart, two different levels**, so a `^## `-only consumer reads one and is blind to the other. Both sections were empty again, so nothing has yet been lost; **the next non-empty orchestrator ask lands on a coin flip**, which is precisely the defect THR-826 existed to close. **Fix shape:** select by explicit mtime (not glob order), match the *text* with a permissive level (`^#{2,6} Needs Christian`), terminate on a same-or-shallower heading. **Wants a ticket from a write-remit session** (finding 23).
- **`check:task-heartbeat` mis-fires at a wake boundary — carried, not re-derived this run.** `scripts/check-scheduled-task-heartbeat.ts` accepts any sibling whose `lastRunAt` falls **inside** the stall window as its liveness witness; after a host sleep the whole fleet's `lastRunAt` clusters at the window's *closing* edge, so the clause is satisfied by the very timestamps proving a shutdown. **Fix shape:** require the witness to have fired strictly inside the window with a margin (`witness.lastRunAt < windowEnd − oneCadence`), or detect the burst directly (≥2 lanes sharing a `lastRunAt` to the second ⇒ host-wake, suppress). **Wants a ticket** (finding 23).
- **Verify-after-write needs a clock — carried, and this run supplies its cleanest artefact yet.** THR-860's own description asserts a `get_issue` re-query confirmed the assignee absent; the field is present now. **The claim and its refutation live in the same row**, which is a better teaching case than any pair of log lines. **Fix shape unchanged:** verify after the last side-effect in your own sequence, or on the next run.
- **A superseded report PR wants closing: #1031** (orchestrator/THR-762). **RE-DERIVED 01:58Z** (finding 1): `OPEN`, `mergeStateStatus` **`DIRTY`**, `mergeable` `CONFLICTING`, `updatedAt` still `2026-07-28T19:31:25Z` — **~30.4h idle**, and it is now the **only** open PR in the repo. Consistent with a superseded report doc, not a regression (memory `dirty_pr_duplicate`: check diff direction before assuming a conflict is content loss). **One `gh pr close`, zero content lost.**
- **`rescue/2026-07-17-detached-plans` is prunable — carried, not re-derived this run.** A **local branch only** (`git ls-remote --heads origin 'rescue/*'` returns nothing). Against `origin/main` it carried **4 unique commits** as of 22:54Z. **Pruning it is `git branch -D` in the home tree, which this lane may not do** — so it is not merely unclaimed, it is unclaimable from here. **Wants a write-remit session with home-tree authority.**
- **THR-835 is unreachable in `Idea`** — carried. Both column scans confirm it is in neither `Ready for Dev` nor `In Dev`. Content is a live merge hazard (the sole required check runs at 80–90% of its 20-minute cap; runner variance turns correct PRs red). **A working promotion lane that never reaches a column is stronger evidence than a broken one** (finding 19). Remedy belongs to a write-remit session.
- **The orchestrator lane's stray-write defect is open and agent-owned.** `tb-orchestrator` wrote its 08:29Z report into the home tree as well as its own worktree, blocking a fast-forward. Durable fix is in that lane's write path (absolute paths, or `rev-parse --show-toplevel` asserted before any write — the workaround this task takes hourly). **Wants a ticket** (finding 23).
- **The live scheduled-task prompts are outside the repo, and THR-845 just demonstrated why that matters.** `~/.claude/scheduled-tasks/<id>/SKILL.md` is what actually runs; `Docs/ops/scheduled-task-prompts/` is a mirror a merge can update while the live copy stays stale. THR-861 was filed 9 minutes after the writer-side fix merged and still acquired an assignee — **consistent with an unsynced live prompt, and indistinguishable from a failed fix without checking the live file.** THR-850 already covers the missing mirror for `tb-opus-pickup`; **the general defect — no gate notices mirror/live divergence — is unticketed.** **Wants a ticket** (finding 23).
- **Friday's retro draft** (`Design/retros/retro-2026-07-24-draft.md`) is untracked and wants a `git add` in any docs PR. **Not a carry** — it is THR-798's step 2, with an owner and a Done-when.
- **Impediment #267 is used twice** (a `sandbox` row and a `tooling` row, filed the same day by different lanes); **#219 and #228 are duplicate rows for one defect** (Playwright MCP writing a bare-filename screenshot outside the repo). Both dispositions: fold at the next retro. **No ticket.**

## Durable findings carried forward

Rules earned by tickets that have since shipped. **Kept as rule sentences; every worked example is in `git log -p Design/user-actions.md`.**

1. **Before carrying an observation into its Nth run, re-derive it once.**
2. **A subsystem with no callers is invisible to every sweep that looks for wrong behaviour, because it has none.** Corollary: grep the *assignment* side, not the read side.
3. **A plan doc's past tense is a claim, not evidence.**
4. **Advisory gates that nobody reads are indistinguishable from gates that do not exist.**
5. **A test suite that agrees with the bug stays green forever** — and a pool-scoped guard is a suite that agrees with the bug one shelf over.
6. **Read composition, not depth** — and stop re-testing the favourable dip.
7. **A gate clearing is not the same as a decision arriving.** Surfacing a settled call for ratification is worse than not surfacing it. Corollary: verify a *favourable* outcome's cause as carefully as an unfavourable one.
8. **An empty Discord inbox is not evidence Christian is away.** Silence means only that he did not write.
9. **A startup-failure cluster is indistinguishable from quota exhaustion at the moment it happens.** The disambiguating evidence — *does a fresh run execute jobs?* — costs one run. Take it before escalating to a billing page.
10. **Deploy-observability must classify by paths, not by branch name or PR title.** — SHIPPED 2026-07-27 in THR-785.
11. **Mutex lines with reasons work.**
12. **An unanswered ask is not an unheard ask — do not re-ring the doorbell.** A standing ask carries a stated safe default, so silence resolves it rather than blocking on it.
13. **Declining to build the missing half is a shippable outcome, and it needs its reasoning recorded to survive.**
14. **A signal with two meanings is not a signal — and the repair is a notification path, never a new gate.**
15. **When a ticket asks "which of these fixes?", census the pipeline before answering** — the honest answer may be "none, and here is why". Validate the *sample*, not just the stage: a bug report carries an unstated premise that its subjects were eligible to exhibit the behaviour.
16. **A deliberately-parked issue and a broken auto-close are indistinguishable from the board.** Report a falsifiable test in both directions.
17. **Before carrying an ask into another run, check whether it was answered somewhere other than the channel it was asked on.**
18. **State a falsifiable test against the outcome, never against an activity proxy** — a proxy-worded test can pass while the thing it guards fails. State the test's enabling premise explicitly, or a benign world reads as its own failure case.
19. **A column-scoped queue query reports the lane's reach, not the board's contents — and from inside the query the difference is invisible.**
20. **When an ask names a *mechanism* rather than a decision, expect the answer to arrive on the channel he has, not the one the mechanism needs — and say so instead of letting him assume it landed.** (a) Prefer asks that are pure decisions. (b) State plainly what was *not* done, or "fixed" reads as *all of it is handled*.
21. **Re-derive a carry even when you expect it to hold** — the check has a wider aperture than the claim, so a *true* carry can still pay. Look at what the source shows you, not only the field you came to check.
22. **An item in the lane's column is not an item the lane can take — and "unblocked" is the wrong question to keep asking.** The cheap detector: read the comments, not just the fields. *Consequential* and *decidable-by-Christian* are independent axes; conflating them turns a plain-language inbox into a firehose.
23. **A ticket this task files is unpullable by construction, and the cost lands on someone else's run — so filing is never free.**
24. **A file's *state* is evidence; its *author* is a separate claim needing separate evidence.** Naming the wrong author makes an ask bigger and less trustworthy than it needs to be.
25. **A confirmed test licenses the claim it tested, not the remedy that motivated it — and the gap is widest exactly when the result is most satisfying.** Corollary: **the mirror failure is a test whose aperture is wider than the claim, which can disconfirm something the claim never asserted.** Scope the test to the assertion — a whole-tree diff against a stale branch measures staleness, not whether its unique commits are merged.
26. **One instance locates a defect; the second instance with the same fingerprint is what turns it into a mechanism — and changes the remedy.** A one-off wants a cleanup, a recurring one wants a fix *plus* a cleanup. **Re-derivation earns its keep here** (finding 1) — the check that confirms a carry is the same check that catches it changing kind.
27. **A timestamp is evidence only once its zone is verified, and a `Z` suffix is a claim like any other.** Prefer a machine-written field (`stateHistory`, `lastRunAt`, commit metadata) over a prose header; when only prose is available, say which it was. **The failure is silent by construction** — a wrong-but-plausible hour never contradicts anything.
28. **A classifier observed only in its stable state has not been tested.** Report a constant verdict as the weaker fact it is, and treat the first correct switch as the real confirmation. **Satisfied three times over as of 2026-07-30 01:58Z:** four runs of `skipped` at a frozen SHA proved nothing; three consecutive advances since — the third landing *on a real code merge* — prove the switch works. **Note what switched: the evidence, not the verdict word.** A classifier can confirm itself without any reported value changing, which is why "unchanged verdict" is not the thing to watch.
29. **"Green and armed" is not "merging" — and the difference is invisible from every signal a lane normally checks.** Checks answer *is this change correct*; `mergeStateStatus` answers *can it go in now*, and a lane that reads only the first concludes "shipping" about work that has stopped. **The tell is throughput, not status.**

  **Two corollaries.** (a) ~~**It does not self-heal.**~~ **RETRACTED 2026-07-29 13:23Z** — #1042 merged ~2h07m after entering `BEHIND`, falsifying a bound extrapolated from a 1h45m observation. **The retraction is itself the lesson: "still true at T" is not "true at every T".** (b) **`gh pr view` returns `UNKNOWN` for `mergeable`/`mergeStateStatus` on the first read** and often the true value on the second — but **not always**. Treat `UNKNOWN` as "not yet computed", never as evidence of mergeability.

30. **A liveness witness must fire strictly *inside* the window it witnesses, not merely fall within it.** **The general form: a guard that accepts an endpoint as evidence about an interval passes hardest exactly when the interval is most degenerate.** Corollary for consumers: a probe's verdict is evidence, not a verdict — when independent witnesses contradict it, override and say why. A genuinely stalled lane does not recover by waiting; that asymmetry is the cheapest available discriminator.

31. **A cleanup and its cause are separate deliverables, and shipping the cleanup is the moment the cause is most likely to be dropped.** **A row-level repair with the mechanism unshipped buys one sweep of relief, not a fix.** State the re-accrual rate and a dated falsifiable test against the next producing event.

  **Amended twice (2026-07-29 14:55Z, 15:56Z), in opposite directions** — "1 per sweep" under-fit, "at least 4" over-fit one batch. **The honest quantity is not a rate at all: it is `|rows the sweep handled|`, workload-dependent and unbounded above.** The meta-lesson: **when a number has been wrong once, the reflex is to move it the other way and re-assert it with equal confidence — replacing a wrong point estimate with another.** Prefer naming the *variable*. **A mechanism claim survives new data; a point estimate has to be retracted every time the data moves.**

  **Closed out 2026-07-30 01:58Z, and the closure is instructive in a direction the finding did not anticipate.** THR-845 shipped the cleanup (a sweep), the cause-side hardening (the writer), **and** a third thing the finding had no category for: **it removed the consumer that made the cause consequential.** The leak still runs — two rows acquired an assignee this hour — but nothing downstream reads the field, so re-accrual has no symptom. **The finding's frame was "cleanup vs cause"; the actual best remedy was neither.** Generalisation: when a defect's cleanup and cause are both expensive, check whether the *reader* can be made indifferent — that retires the symptom without winning either fight.

32. **Verify-after-write needs a clock, not just a re-read — an immediate confirmation read can be served stale and is indistinguishable from success.** Impediment #48 and memory `linear_save_issue_field_drops` establish *that* writes drop silently; this adds *when* to look. **Corollary: a count is not a verification.**

  **Amended 2026-07-29 17:53Z — there is a second, distinct failure with the same shape.** The orchestrator's `get_issue` check at 17:28:49Z was **correct when it ran** and false 131 seconds later. That is not a stale index: the write held, and *something else wrote afterwards*. **An immediate verify cannot distinguish "my write held" from "my write held briefly"** — so the fix is not only *verify later*, it is **verify after the last side-effect in your own sequence** (PR link, comment, attachment), not after the mutation you happen to care about.

  **Sharpened 2026-07-29 21:54Z by a controlled pair.** One hour, one method, opposite outcomes: 9 immediate verifies after unassigns all still held an hour later; 2 immediate verifies after creations were both false within the hour. **The verification was never the variable — what wrote next was.** So the diagnostic question is not "did my read confirm it" but "is anything else going to write to this row after me", and that is answerable *before* the write.

  **Best artefact found 2026-07-30 01:58Z: THR-860's description asserts its own verification and the field contradicts it in the same row.** *"Assignee deliberately set to null on creation, then verified with a separate `save_issue` + `get_issue` re-query"* — `assignee: "Christian Spliid"` on read. **A row that documents its own refutation is the cheapest possible teaching case**, and it argues for putting the verification *timestamp* in the prose whenever the claim is recorded at all.

33. **A claim widens when it is restated, and the widening is invisible because every restatement still cites the original evidence.** **The drift is toward the remedy's framing**, because a report is written to justify acting, and the wider claim justifies acting harder. **The tell is a claim that has grown more emphatic across runs while its citation stayed identical.** **Detector:** state what observation would falsify *today's* wording, then check whether it would also have falsified the wording used when the evidence was gathered. If the answers differ, the claim has drifted. **Corollary: severity claims need their own evidence, separately from mechanism claims.**

34. **N confirming sightings of an effect do not locate its cause — and each sighting makes the unexamined attribution feel better supported.** (Earned 2026-07-29: eight consecutive runs recorded rows acquiring an assignee, each citing the previous run's framing that the orchestrator's `save_issue` was responsible. **Nobody had asked what else differed between a stamped row and an unstamped one.**) **The trap is that repetition reads as accumulating evidence**: run 8 felt far more certain than run 2 while resting on exactly the same untested attribution. **Detector, cheap: before reporting a recurrence, name one property the affected rows share that the unaffected rows do not — and check it.** If you cannot state the contrast class, you have an effect, not a cause. **Corollary, and the reason this outranks a tidier lesson: a remedy that fails while correctly applied is the highest-value evidence available about the cause.**

35. **A query's filter encodes a premise about the world, and nobody audits a premise that has never produced a visible failure.** (Earned 2026-07-29: `pull-work` selected work with `assignee:null` for months, encoding *"an assignee means a person has claimed this row."* Eight runs then investigated why rows kept acquiring an assignee — write path, then workspace setting — **and not one asked whether the field meant anything in the first place.** Christian, unprompted and in one sentence: *"I do not use linear or github at all. so any issues in linear or PRs in github with my name on them are yours."* **The filter had been reading a field that never carried the signal it was selecting on.**) **The trap is that a premise embedded in a filter is invisible in exactly the way a premise embedded in prose is not** — no line ever asserts it, so no review ever disputes it, and every downstream investigation inherits it as background. **Detector, cheap: for each predicate in a query you are debugging, say out loud what it assumes about who or what writes that field — then ask whether anyone has ever confirmed it with the writer.** **Corollary:** 34 says N sightings do not locate a cause; **35 says the cause may not be in the mechanism at all, but in the question.** When every candidate remedy is out of reach — an unreachable setting, an unfixable write path — that is itself evidence you are attacking the wrong side of the predicate. **The reader is usually yours to change even when the writer is not.**

  **SHIPPED 2026-07-30 01:21:50Z as THR-845, and the shape of the fix confirms the finding rather than merely resolving it.** The merge subject — *"close the invisible-queue class at writer, reader and enforcer"* — reads reader-side as: *"an assignee on `Ready for Dev` is noise, not a claim, and filtering on it hid the board's two highest-priority issues."* **What makes this the finding's proof rather than its epilogue: the eight prior runs each produced a correct local measurement and a wrong global conclusion, and no amount of additional measurement on the write side would have converged.** The unlock was a question, asked of the one party nobody had asked. **Second-order lesson for this lane specifically: the answer arrived because step 0 existed to hear it.** A read channel is not a convenience; it is the only route by which a premise held by a human reaches a robot that has been debugging around it.

36. **A consumer that matches on a heading's *level* rather than its *text* is a channel that silently narrows as producers reformat.** (Earned 2026-07-29 21:54Z: step 2.6 extracts `^## Needs Christian`; `tb-orchestrator` nests its section as `### Needs Christian` under a per-run `## <Nth> run` heading. The consumer sees nothing and **reports the file as having no such section** — indistinguishable, from the reading side, from a producer that wrote nothing.) **Detector: for every "no items found" result on a shared channel, confirm the *absence* against the raw file once, not just against your pattern.** The general rule is finding 19's sibling: **a scan reports its own pattern's reach, and a narrowed pattern and an empty world are the same observation.**

  **Re-derived and strengthened 2026-07-29 22:54Z.** The same producer emits **`##`** in `orchestrator-2026-07-30.md` and **`###`** in `orchestrator-2026-07-29.md` — one day apart, no code change on either side. **So the level is not a fixed property of the producer to be accommodated once; it is a per-file coin flip.** **A passing consumer and a broken one were the same code reading two files.**

  **Generalised 2026-07-29 23:54Z, and the generalisation is the payload: "formatting" was too narrow.** The near-miss was **file selection** — the producer wrote two reports for one day, and the consumer's "newest per producer" step had to break a within-day tie it was never designed for. So the rule is not *match text not level* — it is **every point where a consumer infers structure from a producer's convention is a coupling the producer can change unilaterally, and each one fails silently in the same direction.** Detector: for each such inference, name the producer change that would break it, then check whether anything would tell you.

  **Fourth consecutive correct-by-luck hour, 2026-07-30 01:58Z: the same-day set is now `-30` / `-30b` / `-30c` / `-30d`.** **A coupling that has survived four hours looks progressively more like a design and less like a hazard, and that drift is the finding's own tell (finding 33) turned on itself.** The luck is alphabetical and finite: `-30-2.md`, `-30-final.md`, or a reversion to an undated name each break lexical order while mtime stays correct. **Record the streak as accumulating exposure, not accumulating safety.**

**Owner of items below:** Christian. Everyone else's blockers go in Linear or `Docs/impediments.md`.
**Refresh cadence:** The hourly `keep-work-flowing-cc` scheduled task keeps this current (prunes resolved items, adds newly-surfaced Christian-owned ones); the `retrospective` skill still does the deep periodic rebuild.

## How this works

The retrospective stopped being the right channel for "Christian, please flip one switch." Christian-owned asks were buried in retro prose, and nothing surfaced them between retros. This file replaces that pattern.

Read order: top to bottom is blast radius. The top items break canonical workflow invariants right now; the bottom items are operational debt with workarounds in place.

For each item:
- **Fix** = the literal command, click path, or line of config that resolves it.
- **What breaks** = the named system or invariant currently degraded.
- **Source** = `Docs/impediments.md` entry numbers + occurrence count, so the cost is visible.

When an item resolves: delete it from this file, mark the corresponding impediment as resolved in the dashboard regen, note the close in the next retro under "What shipped." **No history retained here — `git log` + the retros are the audit trail.** The resolved log is pruned to the current day; anything older is recovered with `git log -p Design/user-actions.md`.

---

## Standing asks

**NONE OPEN. The file is at a true empty for the first time since it was created.**

The last standing ask — *should the host machine stay awake overnight?* — is **RETIRED this run**, on the terms the previous run set for it. It had stood on its safe default, un-rung, for ten consecutive runs.

**Retirement evidence, gathered 2026-07-30 01:58Z:** the host has now stayed awake through a **second** consecutive full night. Three hourly lanes fired at 01:01Z, 01:26Z and 01:53Z with **distinct** timestamps (not the same-second cluster that signals a wake burst), and the reaper — on a separate scheduler entirely — ran at 01:40Z. **Overnight throughput is happening without any switch being flipped, twice over, so the preference question is moot in practice rather than merely defaulted.** Per finding 12 the ask was already resolved by silence; it is now resolved by observation on two independent nights.

**A new unexplained multi-hour pause would be a NEW ask with new evidence — never this one reopened** (finding 12). If he ever does want overnight throughput guaranteed: keep the machine awake and **on mains power**, because Windows Task Scheduler refuses the reaper on battery (`-2147020576`, memory `git_cleanup_automation`). That is recorded here as a fact, not as a request.

**Six questions are answered, closed, and must not be re-asked:**

1. **The five empty projects** — Christian, chat 2026-07-25 21:53: *leave them open; they are intake buckets that refill as the game iterates.*
2. **The GitHub Actions payment block** — cleared 2026-07-25 ~17:09Z and again 2026-07-28 17:02Z, both verified by re-run, and corroborated twelve times since by independent mechanisms. **A recurrence is a new ask with new evidence, never this one reopened** (finding 12).
3. **THR-799 priority ordering** — Christian, quoted in THR-774/775, 2026-07-27: *the new encounter experience is still first priority.*
4. **Release of THR-821/820/777/778** — Christian, Discord 2026-07-27 14:38Z.
5. **The Linear "assign issue to PR author" switch** — Christian, Discord **2026-07-29 18:10:03Z**: *"can't find anything like that. I do not use linear or github at all. so any issues in linear or PRs in github with my name on them are yours."* **Closed by falsification, on the terms the ask itself set.** The second sentence is a **standing fact**: an assignee of "Christian Spliid" carries no information and must never be read as a human claim. **As of 2026-07-30 01:21Z that fact is enforced in code** — `pull-work` no longer filters on it (THR-845). **This is the highest-leverage answer in the file's history: one sentence retired eight runs of misdirected investigation.**
6. **Should the host machine stay awake overnight?** — **retired 2026-07-30 01:58Z by observation**, two consecutive awake nights, never answered directly and no answer needed.

**One standing switch exists but is not an ask:** the orchestrator lane can be disabled on one word from Christian (live 2026-07-27 19:49Z). **Its default state is the intended one**, so silence resolves it.

**Three durable corrections survive closed asks and are kept as rules:**

- (a) A blanket `git stash` was the wrong remedy for home-tree dirt; the single-path form is correct. Parking his tool-permission edits was never necessary.
- (b) `$REATTACH_MAX_TRACKED_DIRTY = 0` governs the detached-HEAD reattach path and never evaluates while HEAD is on `main`. The conclusion held; its stated reason did not.
- (c) **Tracked dirt alone does not stall autosync.** The stalling class is the *intersection* of locally-modified **∩** changed-by-incoming-commits — proven by fast-forwarding cleanly with `.claude/settings*.json` dirty.

---

## Resolved 2026-07-30

**Sixth consecutive run at zero open Christian-owned asks — and the first run at zero standing asks outright, the last one having been retired by observation rather than answered.** Everything that moved this hour was agent-side:

- **THR-845 SHIPPED at 01:21:50Z, closing the invisible-queue class at reader, writer and enforcer — the largest single event this file has recorded.** Ten files, and the load-bearing change is one sentence in `pull-work`: *"an assignee on `Ready for Dev` is noise, not a claim."* **The `shelf` vs `picker-visible` pair is retired by construction** — shelf 50, candidates 50, because the filter that created the gap is gone. **A pair converging because one term stopped existing is not the same as the terms agreeing**, and this run records it that way so no future run reads it as self-healing.
  - **Finding 35 is the finding that paid, and it paid because step 0 exists.** Eight runs of write-side investigation each produced a correct local measurement and a wrong global conclusion; one sentence from Christian on Discord relocated the defect in a single pass. **A read channel is not a convenience — it is the only route by which a premise a human holds reaches a robot debugging around it.**
  - **Last hour's unattributed 45 → 48 drain is now permanently unattributable, and that is recorded rather than quietly dropped.** The metric no longer exists, so the cause can never be established. **Finding 7's corollary is satisfied by saying so** — an unverified favourable movement does not get retired into "resolved" just because the question stopped being askable.
- **The write-side leak is still live and is now cosmetic-only.** THR-860 and THR-861 both carry an assignee (confirmed on `get_issue`, not off a list); THR-861 was created **9 minutes after** the writer-side fix merged. **Consistent with the live prompt at `~/.claude/scheduled-tasks/` being unsynced from the repo mirror the merge updated — and indistinguishable from a failed fix without reading the live file.** Newly listed under *Open agent work* as a general defect: no gate notices mirror/live prompt divergence.
- **THR-860's description asserts a `get_issue` verification that the field contradicts.** Recorded as finding 32's cleanest artefact — **a row that documents its own refutation** — and as an argument for timestamping verification claims in prose.
- **`In Dev` is empty for a good reason for the first time.** THR-845 vacated it by shipping. **The next pickup (02:00Z) is the first observation under the new filter, and the test is stated in advance** (finding 29's method): if it claims nothing with 50 candidates and no WIP, visibility was never the binding constraint and **THR-836** — coordination blocks missing from directly-filed issues — is confirmed as the real gate. **21 of 50 shelf items carry `Deferral`**, which by construction have no coordination block. Stated as a candidate, not a finding; nobody has counted how many of the 50 actually fail Step 3.
- **Finding 28 satisfied a third time.** `deployedSha` advanced `2eb2cf63…` → `bcfbe645…`, and the new publish point **is the THR-845 merge** — a real code merge tracked correctly, with the docs commits behind it correctly skipped.
- **Finding 36's file-selection axis went four-deep in one day** (`-30` / `-30b` / `-30c` / `-30d`). mtime and lexical order still agree — **correct-by-luck for a fourth consecutive hour.** Recorded as **accumulating exposure, not accumulating safety**: a coupling that survives repeatedly starts reading as a design, which is finding 33's drift turned on the findings list itself. Collision cause ticketed (THR-849); consumer-side half still not.
- **PR #1031 re-derived:** `OPEN` / `DIRTY` / `CONFLICTING`, `updatedAt` unmoved since 2026-07-28T19:31:25Z — **~30.4h idle**, and now the repo's **only** open PR. One `gh pr close`.
- **The merge gate held for a twelfth consecutive independent check** (`startupFailureCount: 0`); the fleet heartbeat returned `ok` for an eleventh, across a second genuinely-awake overnight — three lanes at distinct timestamps, so the wake-boundary defect is untriggered rather than merely quiet. The reaper ran 03:40:02 local with **branches 48 → 46**, a moving count that independently proves the log is live.
- **Discord returned a literal `[]` for a fourth consecutive hour** — nothing to classify, no receipt owed, cursor unmoved at `1532100414189867109`. **The ping gate correctly stayed silent**: the stored hash is the SHA-256 of the empty string and this run's Needs-Christian is also empty. **The `python -c` allowlist read in the skill is unusable in this sandbox for a third run running** and was again repaired by reading `access.json` directly. **Fail-closed holds only because the failure keeps getting noticed** — a skill fix owed, not an hourly workaround.
