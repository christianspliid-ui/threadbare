# User Action Required

**Last updated:** 2026-07-28 01:54 local, by the hourly `keep-work-flowing-cc` CC task.

**🟢 NO OPEN CHRISTIAN-OWNED ASKS.** Last run's orchestrator-lane notice was **delivered once, with a ping, and is discharged** — the briefing this hour states plainly that nothing needs a reply, and does not reprint the notice. **That is finding 12 executed rather than cited:** a notice whose default is the intended state resolves on silence, so re-surfacing it hourly would convert a one-time courtesy into a nag and spend the channel's credibility on the item that least needs an answer. The stop switch remains standing and reversible; it lives in *Standing asks* below as a switch, not as a question.

Four standing questions are answered and closed and **must not be re-asked**: the empty-projects question (Christian, chat 2026-07-25 21:53 — *leave the five projects open; they are intake buckets that refill as the game iterates*), the GitHub Actions payment block (cleared 2026-07-25 ~17:09 UTC, verified by re-run), **THR-799 priority ordering** (Christian, quoted in THR-774/775, 2026-07-27 — *the new encounter experience is still first priority*), and now **the release of THR-821/820/777/778** (Christian, Discord 2026-07-27 14:38Z).

> **This file was pruned at the 18:54 run** from 114 KB / 170 lines down to a compact same-day index, restoring its own stated policy (§ *How this works*): *"No history retained here — `git log` + the retros are the audit trail."* Nothing was destroyed; `git log -p Design/user-actions.md` holds every pruned word, and the durable lessons are carried forward as one-line rules below.

## This run's measurements (2026-07-27 23:54 UTC)

- **Three consecutive missed slots now — and this run supplies the discriminating evidence the previous two lacked.** `tb-orchestrator` `lastRunAt` is **still 20:27:02Z**, unchanged across a third reading, while `nextRunAt` has advanced to **00:26:16Z**. Its cron is `25 * * * *`, so **21:26, 22:26 and 23:26 all elapsed with no run recorded**. THR-618 remains `Todo`, `updatedAt` 19:49:35Z, untouched for a fourth consecutive reading.
  **The new fact is a control, and it was cheap: the scheduler is demonstrably alive.** `tb-opus-pickup` (cron `0 * * * *`) recorded `lastRunAt` **23:01:39Z** and shipped THR-827 by 23:34Z; this task itself fired at 23:53:59Z. **Three hourly lanes share one scheduler; two of them clocked in across the same three slots the third missed.** That eliminates the whole class of scheduler-wide explanations at the cost of one field already in the response.
  **The candidate set has therefore narrowed rather than widened for the first time.** Last run's four explanations were three judgement-family (errored / T3-only / nothing judged promotable) plus one liveness. Scheduler-wide failure was never on that list and is now excluded anyway; what remains is **task-specific**: the lane is failing to start, or failing so early it records nothing. The judgement family is not refuted — a lane that starts and dies before writing `lastRunAt` is indistinguishable from one that never started — but it now requires an extra assumption the liveness reading does not. **Neither is asserted, and the deadline does not move** (see *Open agent work*).
- **THR-827 shipped at 23:34:14Z — a second consecutive clean close, and the shelf went 23 → 22.** The executor claimed it in the 23:01:39Z pickup and was done in ~33 minutes; `main` carries it as PR #978. **No deferral was filed underneath it.** Two consecutive ship-with-no-filing hours is the first back-to-back break in finding 6's steady state; per that finding's own rule it is logged as **two datapoints, not a turn**, on exactly the ground that the favourable dip has been called and survived one hour on three prior occasions.
- **Queue: BACKED UP — 22 ready (0 High / 5 Medium / 17 Low)**, nothing stale (oldest `updatedAt` THR-715 at 2026-07-24T07:59Z, ~3.8 days, inside `STALE_ISSUE_DAYS`=7), nothing blocked, **0 In Dev**. Thirty-ninth consecutive run above `QUEUE_BACKED_UP_MIN` (15). **The no-High gap holds a fifth run**; the bench is empty, so the 00:00:53Z pickup takes a Medium or a Low because the board offers nothing else.
- **Top-of-queue takeability evidenced behaviourally rather than by re-querying blockers.** THR-778 still leads the priority sort carrying *"Blocked by: WS0, WS1, WS3"*, all three re-verified `Done` last run and none of which can have un-shipped since. **The stronger evidence is that the lane has claimed and shipped in each of the last two hours** — a shelf that yields work twice running is takeable by demonstration, which is a better test than re-resolving a static field. Recorded because it is the cheaper check, not merely the shorter one.
- **Needs-Christian empty for the fourth consecutive run, on the same held judgement, and the premise-check was re-run rather than assumed.** `Docs/ops/orchestrator-2026-07-27.md` still shows mtime **21:50:03** — the same post-autosync timestamp last run correctly refused to read as a rewrite — and `git diff HEAD` on that path is **empty** against the single commit `9fbef71b`. Content unchanged, so the item key `THR-826:lane-live` stands and finding 12 still retires it. `Docs/ops/backlog-grooming-2026-07-27.md` says *"Nothing needs you"* — the empty state, correctly not propagated. `Docs/ops/weekly-hygiene-2026-07-26.md` is **~39 hours old**, past `SIBLING_REPORT_MAX_AGE_HOURS` (36), and stayed out of the fold for a second run.
- **Discord: nothing new.** The `after=` cursor (`1531390902088761504`) returned a literal `[]`, 3 bytes. **The allowlist was read from `access.json` before any classification** (`allowFrom` = `247984978283986954`, single entry) even with nothing to classify, preserving the ordering the fail-closed property depends on. **No receipt owed; none sent. Cursor unchanged**, because no message was processed.
- **Home tree** on `main`, **0 stranded**, **0 behind** `origin/main`, **2 tracked-dirty** (`.claude/settings*.json`), **1 untracked** (`Design/retros/retro-2026-07-24-draft.md`). Re-derived, not carried.
- **Deploy** (step 2.5): **`deployed`**, `needsChristian: false`, live site serving **`9d5e7c31`** — the THR-827 merge itself, so tonight's work is published rather than merely merged.
- **Reaper** ran 01:40 local, 14 min before this run: **27** worktrees / **36** branches / 1 stash / **0** needs-disposition. Thirty-fifth consecutive clean post-THR-797 run.
- **No ping sent** (step 6): the section is empty and the item key unchanged, so both clauses agree. `PING_STATE_FILE` left at its existing empty-state hash rather than rewritten.
- **This run's worktree was again created outside `.claude/worktrees/`** (session scratchpad), removing this task from the THR-797 blast radius by construction. **The home tree was touched only by `git -C` reads and `git worktree add`**, never by a state op.
- **Zero Linear writes this run**; nothing claimed, no state set, nothing filed.

## Open agent work (not Christian's)

- **The orchestrator lane is live, its standing test has taken a second adverse reading, and the likeliest cause has changed from judgement to liveness.** **Test, unchanged: by the 2026-07-28 08:56Z run, the lane will have promoted THR-618** — named by its own first report as "first in line next run", and **verified this run as genuinely unblocked** (THR-611 Done 2026-07-05; THR-617 / P3 Done 2026-07-22 — free to start for five days). **Worded against the outcome, not an activity proxy** (finding 18): "did the lane run" and "did the report get written" can both pass while nothing is released. If THR-618 is still `Todo` at that run with the ceiling not binding, that is a promotion path that reads its own backlog and does not act on it — file it, do not restate it (finding 1). **Status at 23:54Z: a third slot (23:26) elapsed with `lastRunAt` still reading 20:27:02Z**, THR-618 still `Todo` at `updatedAt` 19:49:35Z, and — verified by `git diff HEAD`, not by mtime — **no unattended pass has ever written a report**. **This is the first run whose new evidence narrows the candidate set rather than widening it, and the narrowing came from a control rather than from more of the same reading.** `tb-opus-pickup` ran at 23:01:39Z and this task at 23:53:59Z, so **two of the three hourly lanes on the same scheduler clocked in across the identical three slots the third missed** — scheduler-wide failure is excluded outright. What remains is task-specific: not starting, or dying before it records anything. **The judgement family survives but now costs an extra assumption**, since a lane that starts and dies pre-write is observationally identical to one that never started. **Neither is asserted.**
  **The deadline is not moved forward on this, for the third time and on the same ground.** Finding 19's third leg binds symmetrically: a pre-committed consequence that can be pulled forward on an adverse datapoint was never pre-committed at all. **The pressure to pull it in is highest now, on the run where the evidence is both worst and sharpest — which is the case the rule exists for**, since a deadline that survives only weak evidence is not a deadline. **What the new evidence legitimately changes is the diagnosis to bring to that run, not its timing:** if the lane has not fired by 08:56Z, the question is a scheduler question, not a judgement question, and it should be filed as one.
  **One thing worth watching that the control does not settle:** the three missed slots and the two clean lanes are consistent with a fault introduced when the lane last ran, which was its own first unattended pass. That is a hypothesis with a cheap test at the deadline, not a finding.
- **The board has no High the executor lane can reach, fifth consecutive run, and the cost is being paid again.** The bench is empty and the 00:00:53Z pickup will take a Medium or a Low because the board offers nothing else. **Carried at baseline severity, deliberately not escalated for the extra hour** — finding 1's rule run against a genuinely unchanged item: the reading has not worsened, only repeated, and repetition is not evidence. **What would change it is a fresh High entering, or the promotion lane resuming** — the two are related, since T2 authoring is the mechanism that would supply one, and the lane that owns T2 is the one that has not fired in three hours.
- **THR-804 (Stale Claim Sweep, 89 runs / 89 failures / zero successes) — deliberately not re-measured, twelfth run running.** Finding 1: the count does not need re-measuring until someone claims the ticket. It sits unclaimed at Medium among seven. No escalation — a job that has never once worked is not getting worse.
- **The `rescue/2026-07-17-detached-plans` branch is provably prunable.** Four unique commits, all five of their blobs byte-identical to `origin/main`. Deleting it is a one-line `git branch -D` that this read-mostly task deliberately does not run; it wants a session with a write remit, or the next full retro. Zero risk either way: nothing is stranded on it.
- **Friday's retro draft** (`Design/retros/retro-2026-07-24-draft.md`) is still untracked and still wants a `git add` inside any docs PR — but it is **not** a carried observation here. It is a deterministic regenerable draft, and it is **THR-798's step 2**, with an owner and a Done-when.
- **The tracked `.claude/settings*.json` edits** in the home tree. Inert and non-blocking, but *tracked* modifications are the class that stalls autosync if it ever needs more than a fast-forward. Wants a commit or a discard by whoever made it.
- **Impediments #219 and #228 are duplicate rows describing one defect** (Playwright MCP writing a bare-filename screenshot outside the repo). Both carry the same working workaround; the correct disposition is to fold them into one row at the next retro. **No ticket.** Settled — kept here as a one-line disposition, not re-argued.

## Durable findings carried forward

Rules earned by tickets that have since shipped. Kept as rules; the ticket-by-ticket prose is in `git log`.

1. **Before carrying an observation into its Nth run, re-derive it once.** **Four instances inside 48 hours**: THR-804 (thirteen runs restated a one-line cause from memory; filing it found a *second* defect the restatements had missed), the retro-draft carry (twenty-two runs described a file's significance without opening it; one `head -30` falsified both halves), standing item 1 — carried for **nine days** on a filename that has never existed on any branch, describing as "stranded" a document already on `origin/main` — and the impediment #228 carry, withdrawn after one `Read` showed the item was double-logged, workaround-covered, and not the class it had been assigned to. Every carry restated the item; none opened it. **Note the failure mode is not laziness but plausibility**: each restatement was internally coherent, and coherence is exactly what stops a reader from checking. **The fourth instance adds the sharper lesson: escalation is itself a symptom.** Three runs did not merely repeat #228, they progressively *raised* it — "carried" → "occurrence 3" → "has earned its filing" — and the rising urgency came entirely from repetition, since no new evidence arrived at any step. **A carried item that is getting louder without getting newer is the strongest available signal to re-derive it**, and it is a cheaper trigger than a schedule because it fires exactly when the cost of being wrong is about to be paid. **The counter-example is also on the record:** THR-804's post-filing datapoint was *predicted* three runs running, left un-restated until it landed, and is now deliberately not re-measured until the ticket is claimed. Waiting on a named future measurement is not a carry — the distinguishing test is whether new evidence is due, not how many runs the item has survived.
2. **A subsystem with no callers is invisible to every sweep that looks for wrong behaviour, because it has none.** Eleven instances in four days: THR-779 (61 templates, no draw path), THR-787 (three engine reads, wrong field name), THR-803 (whole chain subsystem, zero production callers), THR-800 (62 refs, no definition), THR-783 (five authoring sites, no reader), THR-784 (four UI consumers, no writer), THR-811 (five gates, one lookup that could not see their operands), THR-809 (eleven of thirteen definitions absent from every world ever generated), THR-812 (the first found *prospectively*, by the habit the earlier ones produced), and now **THR-814 — ~150 authored faction templates against 227 memberships, drawn 3 times in 200 ticks and 0 times by a member.** Every one was authored or implemented capability that nothing routed to; every one failed silently; every one became visible only when something measured the producing side against the consuming side. **The class detector, not the individual repair, is the deliverable** — the repairs ran 28–82 minutes each once found. **THR-814 was the first whose repair was a design choice rather than a repoint, and it closed via THR-815 at 05:54Z on 2026-07-27** — three tickets and ~4½ hours from "the apex rank looks mis-tuned" to a measured routing change. **That the repair took a successor ticket is the finding's cost curve, not a failure**: a repoint fits inside the discovering ticket, a routing decision does not, and trying to ride one along would have shipped one of the six void candidates.

   **THR-814 sharpens the detector by showing what a partial one misses.** THR-779 measured "does this template have a draw path?" and cleared the faction pool, because a path exists — three draws prove it. What it did not measure is *who* the path delivers to, and the whole value of guild content is that it reaches guild members. **The census that found it counts draws by eligible-consumer class, not draws in total**; a reachability sweep that stops at non-zero is a sweep that agrees with the bug (finding 5's shape, one axis over). **Its repair (2026-07-27, PR #934) then found the sharpest variant yet: the content was not merely unrouted, it was unrouted at a stage nobody had thought to measure.** 817 entries cleared every eligibility filter and 0 survived the cap — because the cap walks a ~4,650-entry list from the head and the per-agent generators append at the tail. **Position in a list is an eligibility gate that no eligibility test will ever find**, and the tell was that the survivors ranked *first* on 40 of 56 boards the moment they were made visible.

   **THR-811 is the variant worth naming separately: the callers existed and ran.** What was missing was resolution — every gate read its field off `undefined` and passed. A gate that cannot find its subject is indistinguishable from a gate that approves, and neither the type system nor the suite can tell them apart.

   **THR-783 → THR-809 is the corollary's proof in two acts: wiring a dead field proves the wire, not the effect.** THR-783 shipped correct, tested and green, and the wound still never landed. Only running the world and looking for the outcome distinguishes "connected" from "working" — and here they differed by three tickets and a day. Treat a green suite on a newly-wired path as evidence the code executes, never as evidence the thing happens.

   **THR-784 adds the structural one, and it is the reason this class keeps recurring: grep the *assignment* side, not the read side.** Every instance above was found by noticing a reader that got nothing; every one could have been found faster by asking "who writes this?" first. `totalTicks` is the purest case: declared `?: number`, read by four production components, **written by nobody, ever**. No gate can catch it — `undefined` is a legal inhabitant of an optional type, the consumers guard on `!= null`, and the rendered result is visually identical to the legitimate empty case. **An optional field with no writer is invisible to every automated gate simultaneously**, which is precisely why it survives longest. **THR-812 closes the loop on this one: it was found by running the habit deliberately during another ticket, and the shipped fix confirmed the prediction to the letter.** Four consecutive tickets have now found their real defect from the writing side rather than the reading side.
3. **A plan doc's past tense is a claim, not evidence.** THR-801 existed only because the WS0 plan recorded a type change as already made; it had not been. The disconfirming check was a two-second grep that nothing in the pipeline had run for three days.
4. **Advisory gates that nobody reads are indistinguishable from gates that do not exist.** Both of THR-788's deferrals (THR-806, THR-807) surfaced from running the advisory gates at closeout and reading the output. THR-807's own framing is that the only thing noticing 61 stale plans was advisory.
5. **A test suite that agrees with the bug stays green forever — and a pool-scoped guard is a suite that agrees with the bug one shelf over.** THR-787's dead read survived because nothing asserted the observable outcome, only that the code ran. Trait regression tests must anchor on **shipped definitions**, never on fixtures written alongside the code under test (THR-809: a fixture that inserts the definition under test passes against a world where it does not exist). **THR-812 adds the scope dimension:** a guard existed for exactly this defect and was green throughout, because it enumerated one of three pools. When writing a guard over a set, assert over **every** member set and assert each is non-empty, or a rename silently empties the loop.
6. **Read composition, not depth — and stop re-testing the favourable dip.** A queue that rises after a valuable hour is not decay if the arrivals are findings rather than regressions. Eighteen consecutive hours now read 21 → 21 → 20 → 21 → 22 → 22 → 23 → 23 → 23 → 22 → 23 → 22 → 23 → 22 → 21 → 20 → 20 → 20, across which the most valuable work of the week shipped. **The wobble is the whole lesson in miniature, and it is settled rather than open**: a favourable dip has been called "one datapoint, not a trend" on three separate runs and survived **exactly one hour on all three**. The three-hour −1/0 run ended this hour at −1/+1, and that is recorded flatly — declining to call the dip a turn obliges declining to call its end a reversal, or the finding is just a mood. **The steady state is the signal**: at ~1 ship/hour and ~1–2 filings/hour the depth is stable by construction, and the filings are self-generated findings, not requests. A "backed up" verdict from `QUEUE_BACKED_UP_MIN` is therefore a threshold artefact here, not an alarm — report the composition alongside it every time, or the number lies. **The previous hour was the cleanest demonstration of that**: the depth did not move at all, while the shelf gained its first High in five hours. Depth reported alone would have said "nothing happened".

   **2026-07-27 10:54Z adds the first non-steady-state hour in eighteen: 23 → 22, a ship with no filing under it.** It is logged as one datapoint and explicitly **not** as a turn, which is this finding's own rule applied against its own preferred direction — the favourable dip has been called and survived exactly one hour on three prior occasions, and a rule that only binds when the news is bad is not a rule. The composition is what to read: a 48-minute clean close on the top-priority job, not an arrival drought.
7. **A gate clearing is not the same as a decision arriving.** Surfacing a settled call to Christian for ratification is worse than not surfacing it. This is what has kept twenty-seven consecutive empty Needs-Christian hours honest rather than merely quiet. Worked example: THR-805's guild-rank question looked like two design *feels* and turned out to be a purely mechanical selection whose answer was already determined — retired on examination rather than escalated.
8. **An empty Discord inbox is not evidence Christian is away.** He acted on the deploy block at ~07:39Z on 2026-07-25 without writing there. Silence means only that he did not write.
9. **A startup-failure cluster is indistinguishable from quota exhaustion at the moment it happens.** The disambiguating evidence — *does a fresh run execute jobs?* — costs one run. Take it before escalating to a billing page.
10. **Deploy-observability must classify by paths, not by branch name or PR title. — SHIPPED 2026-07-27 in THR-785.** THR-788 was a `docs/`-branched, docs-titled PR whose build ran anyway, because it touched `public/`. Three prior observations fit a docs-vs-code shorthand; the fourth falsified it. **The finding was carried into the ticket rather than left in this margin, and the implementation improved on it**: the probe parses the path list out of `vercel.json`'s own `ignoreCommand` instead of duplicating it, so there is no second list to drift. Kept as a rule because the generalisation outlives the ticket — *when a checker must agree with another system's decision rule, read that system's rule; do not copy it.*
11. **Mutex lines with reasons work.** THR-800 declared `Mutex with: THR-788 (settle the vocabulary before mass-rewriting refs to it)`. The pickup lane claimed it at 12:02, 14:02, 15:02 and 17:02 and released it within 30–80 seconds each time, taking other work instead — then built it in 38 minutes at 18:02, once THR-788 had merged at 17:45. **Four unprompted correct deferrals from one authored line.** This is the strongest evidence to date for THR-688 ticket-authoring rule (B); some of the earlier "0 In Dev, nothing claimed" readings in this file were partly *this*, working correctly, being read as idleness.
12. **An unanswered ask is not an unheard ask — do not re-ring the doorbell.** A standing ask carries a stated safe default, so silence resolves it rather than blocking on it. Re-pinging converts a one-question channel into an hourly nag and spends the channel's credibility on the one ask it least needs to force. **Judge the item, then let the hash follow** — the question is "is this the same ask?", never "does the string match?". The step-6 gate now enforces this mechanically as well, because the hash is taken over a **stable item key** (`THR-799:priority-ordering`) rather than over prose this task rewrites every hour; keyed on regenerated prose it fired on every rewording, which was the exact nagging it exists to prevent. **The judgement call remains primary and the hash confirms it, never the reverse** — a gate that only ever agrees has not been tested. A genuinely *new* fact about an open ask belongs in the briefing where he will see it next time, not in a second ping.
13. **Declining to build the missing half is a shippable outcome, and it needs its reasoning recorded to survive.** THR-812's ticket offered per-instance `$`-ref binding as scope option 2; the executor checked the producing side first, found `REACTIVE_AMBITION_TEMPLATES` has **no assignment path anywhere in the engine**, and refused to build a binding channel for a pool nothing mints from — naming THR-614's green-field failure as the precedent. The dead refs were repointed instead. **The general rule is finding 2 turned around: before extending a mechanism to reach a consumer, confirm the consumer has a producer at all.** A capability wired to a pool with no source is the same defect as a read with no writer, arriving one layer earlier and costing far more to build.
14. **A signal with two meanings is not a signal — and the repair is a notification path, never a new gate.** Vercel reports "published" and "build skipped, nothing needed publishing" with the identical green `success`. Measured on `a9c33078`: `success` on a commit with **no deployment record at all**. That is finding 2's pathology one layer up — a check that cannot distinguish its two cases is indistinguishable from a check that always passes — but the repair differs, and the difference is the lesson. **The tempting fix was to promote the Vercel check to required**; it was refused, correctly. It is not a correctness gate (`Test · Typecheck · Build` already proves the build compiles), and requiring it would couple every merge to a third party's availability. The right move for an ambiguous non-required signal is to **disambiguate it out of band** — ask the authoritative API which version is actually live, classify honestly, and route only the bad verdicts to a human. Generalises past deploys: when a signal is both unreliable and load-bearing, add an observer, not an obstacle.

    **Second instance, 2026-07-27, and it is our own gate rather than a vendor's — THR-819.** `npm run check:wiki-freshness` **never fetches**, so it diffs changed files against whatever `origin/main` the local repo last saw and reports **PASS on precisely the condition CI fails**. It cost THR-802 a two-hour red streak: the author ran the gate, was told they were clean, and was then blocked by the blocking copy of the same check. **The sharpening is that the ambiguity was invisible because it was asymmetric** — the local and CI runs share a name, a script, and an exit code, and differ only in a fetch nobody thought of as part of the check. A gate whose baseline is stale-by-default is not a weaker gate; it is a gate that reports the wrong answer in the reassuring direction, which is the one direction nobody re-checks.
15. **When a ticket asks "which of these fixes?", census the pipeline before answering — the honest answer may be "none, and here is why".** THR-810 proposed three levers; THR-814 proposed three routing rules; **all six were void for the identical reason, and one stage-by-stage census retired the lot in forty minutes.** Each candidate operated on a stage the content demonstrably never reached, so any of them could have been built, tested, shipped green, and changed nothing. **The general rule: a menu of candidate fixes is itself an untested assumption — that the defect lives at the stage the menu operates on.** Take the census first; it is cheap (one instrumented run of the real pipeline), it is reusable (`sweep:rank-reach` now prints eligibility every run), and it is the only thing that can *falsify* the menu rather than merely choose from it. **This is finding 13 one step earlier:** 13 says confirm the consumer has a producer before extending a mechanism to reach it; 15 says confirm the defect is where the ticket says it is before choosing between repairs. Two consecutive tickets have now been falsified by their own commissioned measurement, which is the strongest available argument for building the measurement first.

    **Third consecutive falsification, 2026-07-27 — THR-821 — and it moves the rule one layer further back: validate the *sample*, not just the stage.** THR-810 and THR-814 were wrong about *where* the defect lived. THR-821 was wrong about *who it was measured on*: seven agents all read `Doomed`, and all seven were `ambient`-tier NPCs carrying no capability data and structurally unable to reach the nudge stage at all. **Both offered causes — "the content is too brutal", "mortals are too feeble" — were analyses of a reading that a correct system had produced for a population the feature does not apply to.** Either could have been built. The content fix would have gentled an exemplar that measurement shows is fine (98.2% clearing unaided among agents who can actually be attended); the tuning fix would have moved a floor the entire resolution ladder is calibrated against, to serve agents who never reach the stage.
    **The generalisation: a bug report is a measurement, and it carries an unstated premise that its subjects were eligible to exhibit the behaviour.** That premise is invisible in the report, because an ineligible subject returns a *well-formed* answer — `p=0.050` is a real number, honestly computed, and it looks exactly like a defect. **This is finding 2's pathology inverted:** finding 2 is a probe that matches nothing and reports success; this is a probe that matches the wrong things and reports failure. Both are unfalsifiable from inside the report, and both are cheap to catch with the same question — *what population is this, and can it exhibit the thing I am measuring?*
    **The payoff is not merely avoiding the wrong fix.** Widening the measurement past the reported sample is what found the real defect: the `notable` tier, which nobody had reported, does floor and stay floored. **A census run only over the complainants would have cleared the system and shipped nothing.**
16. **A deliberately-parked issue and a broken auto-close are indistinguishable from the board.** THR-814 sat In Dev with its work merged and live; THR-810 went In Dev → Done in 20 seconds at merge. The difference was legible only in the PR body, which said the keyword was withheld on purpose because 3 of 4 Done-whens carried to a successor. **Record the reason at the park, not just the omission** — otherwise the next reader's cheapest hypothesis is the THR-738 keyword defect, and they will "repair" a correct decision. Corollary for this file: when a park is correct, **name it as correct in writing, with its successor ticket, and name the run at which it stops being correct.**

   **The named run arrived, and the prediction attached to it was wrong in the useful direction.** This file predicted that a correctly-parked issue would read to the next pickup as an occupied bench and cause a stand-down. It did not: the lane claimed the successor at 05:02Z with the park still standing, and shipped it in 52 minutes. **A stale In-Dev row is a legibility cost, not a throughput cost** — at least while the lane routes by Ready-for-Dev rather than by bench occupancy. **The transferable half is procedural, not factual: a carry that states a falsifiable test must report the result when the test fires, including "the harm I predicted did not happen".** Finding 1 catches carries that get louder without getting newer; this is its mirror — a carry that got *quieter* and would have been cheapest to simply delete. Deleting it would have preserved the file's tidiness and destroyed the only evidence that the WIP-slot worry is unfounded here.

   **Falsified a second time, which settles it.** The lane claimed THR-816 at 05:18Z and shipped it at 06:52Z with THR-814 still parked In Dev. Two consecutive claims straight through a standing park is enough to stop treating it as a live risk on this lane and start treating it as a tidiness item. The carry now costs one flat sentence per run, which is the right price for it.

   **Closed 2026-07-27 07:19:22Z, unaided.** The park resolved itself without anyone acting on this note, which retires the item and completes the record: correct park → named as correct → predicted harm falsified twice → closed on its own. **The transferable half is the pricing, not the outcome:** a carry whose stated cost is one sentence and whose payoff is a falsifiable prediction is worth keeping to term; a carry that restates a diagnosis from memory (finding 1) is not. The difference is whether new evidence is due, and here it was — twice.

17. **Before carrying an ask into another run, check whether it was answered somewhere other than the channel it was asked on.** The THR-799 priority ask was pinged to Discord once and then carried unanswered for **nine runs**, with each run dutifully re-reading an empty Discord inbox and concluding "no answer". **The answer had been recorded in a Linear ticket description that morning** — *"User priority (Christian, 2026-07-27): the new encounter experience is still first priority"* — written by a design session that had no idea an ask was waiting on it. **Finding 8 says an empty inbox is not evidence he is away; this is its sharper corollary — an empty inbox is not evidence he has not answered.** The check is cheap and specific: an ask about *priority* is answered by any dated statement of priority, wherever it lands, so before the Nth carry, grep the surfaces where his words get recorded (ticket descriptions, plan docs, chat-session notes), not only the channel the question went out on. **This also completes finding 1 rather than merely resembling it:** finding 1 says re-derive the *claim* before carrying it; 17 says re-derive the *premise that it is still open*. Nine runs re-verified the former and never once tested the latter. **The structural cause is worth naming: this task reads Discord because that is the channel it can write to, and it mistook its own outbound channel for the boundary of his voice.**

18. **State a falsifiable test against the outcome, never against an activity proxy — a proxy-worded test can pass while the thing it guards fails.** Both tests standing at the 08:53Z run fired, and **both were satisfiable without their outcome occurring**. Test A asked whether the next pickup would *claim a Low over the Highs*; the pickup claimed nothing (WIP=1, slot already occupied), so a real condition — the top-priority work sitting unstarted 2h23m — sailed past a test written to catch exactly it. Test B asked whether the stuck PR was *still red with no new commit*; a `main`-merge commit landed, satisfying the escape clause while doing nothing about the failure, which re-failed identically 14 minutes later. **The two failures are the same failure**: "did the lane claim" and "did the branch move" are cheap and visible, while "did the priority work start" and "is anyone fixing the gate" are the questions that matter, and each proxy stood in for its question without tracking it.

    **This is finding 5 one level up.** Finding 5 says a test suite that agrees with the bug stays green forever; 18 says a *prediction* can agree with the stall the same way, and it is more dangerous, because a green suite at least invites suspicion while a passing self-set test reads as vindication. **The tell is available at authoring time and costs one question: can this test pass while the situation I am worried about persists?** For both tests here the answer was yes, and obviously so, on re-reading. **It also sharpens finding 16's procedural half** — 16 obliges reporting a test's result in both directions; 18 adds that *reporting a pass is worthless if the test could not have failed for the right reason*. Restating the test correctly is part of reporting it, and the restatement belongs in the same run, not the next one.

19. **A column-scoped queue query reports the lane's reach, not the board's contents — and from inside the query the difference is invisible.** Seven consecutive runs have reported Ready-for-Dev composition as if it were the state of the work. This run it was materially wrong in the direction that matters: *"no High anywhere on the board"* was written last hour and repeated as a headline, while **THR-821 was filed at High and sat in `Idea`**, unclaimable, one hour old. The query was accurate and the sentence it produced was false, because the query's scope had quietly become the sentence's scope.

    **The mechanism generalises past this board, and it is finding 2 wearing process clothes.** Finding 2 says a subsystem with no callers is invisible to every sweep looking for wrong behaviour, because it has none. Here, **an issue in a column no lane reads is invisible to every measurement of throughput, because it never moves** — it cannot go stale (nothing ages it), it cannot block (it declares no blockers), and it cannot starve the queue (the queue it is absent from looks healthy). Four such issues, including the top-priority one, produced a board that measured well on every axis this task reports. **The detector is to count the work, then count what can reach the lane, and treat the gap as the reading** — depth alone cannot see a gap that lives outside its own filter.

    **The sharper half is that the un-releasedness is self-perpetuating and nothing in the system is assigned to notice it.** Verified this run: no scheduled task can move an issue out of `Todo`, so authored program work waits on a human-started session while executor-filed deferrals enter directly at the shelf. **A backlog fed by one route and drained by another will drift in composition indefinitely without any single step being wrong** — which is exactly why it survived twenty-eight consecutive "backed up" readings without once being called a defect.

    **Closed 2026-07-27 13:54Z by escalation rather than by repair, and the mechanism is the transferable part.** The re-derivation obliged by finding 1 was run against the *automated* half of the diagnosis rather than the item itself, and it found the missing piece: `daily-backlog-grooming`'s pipeline-gap rule fires only when Ready-for-Dev **and** In-Dev are both empty, so **a shelf full of Low-priority deferrals is indistinguishable from a healthy shelf to the only check that could object**. That is finding 2 at the process layer once more — the check has no wrong behaviour to exhibit, because the condition it tests is unreachable while the drift is happening. **The general rule: when a metric's healthy range is wide enough to contain the failure, the metric cannot be the detector, and the fix is a second reading (composition) rather than a tighter threshold on the first (depth).**

    **The procedural half completes findings 16 and 18.** 16 obliges reporting a falsifiable test in both directions; 18 obliges wording it against the outcome rather than an activity proxy. **This adds the third leg: a test whose consequence is stated in advance must be allowed to pay it, on time, without renegotiation.** The temptation at the firing moment was to restate the diagnosis for a third hour with a fresh deadline attached — which would have been indistinguishable from finding 1's escalating carry, and would have spent the item's credibility while claiming rigour. **A deadline that slips once is a deadline that never binds**, and the whole value of pre-committing the consequence is that the decision was made at a moment when the answer was not yet known.

20. **When an ask names a *mechanism* rather than a decision, expect the answer to arrive on the channel he has, not the one the mechanism needs — and say so instead of letting him assume it landed.** The 13:54Z ask was worded *"next time you open a chat session, say X"*, because a chat session can write Linear and this task cannot. He answered 44 minutes later by saying X **on Discord**, which is exactly what a reasonable person does when handed a sentence and a phone. **The decision half discharged perfectly; the mechanism half did not move at all**, and nothing about the reply's wording would tell him that.

    **The failure this avoids is silent and expensive: a satisfied-looking ask that shipped nothing.** Both parties would have been correct — he said the sentence he was asked for, this task recorded it faithfully — and the four issues would still be sitting in `Idea` and `Todo` tomorrow, with the ask closed and no one looking. That is finding 2's shape wearing process clothes for the third time (cf. finding 19): a step with no consumer, failing silently, invisible to every check because nothing exhibits wrong behaviour.

    **Two rules follow.** (a) **Prefer asks that are pure decisions** — *"is this still first priority?"* survives arriving on any channel, while *"go and file these"* only works on one. (b) **When a mechanism-shaped ask is unavoidable, the receipt must state what did and did not happen**, in the same message, unprompted: *your sentence is recorded authority, not a filed action, and here is the shortest path to the filing.* The receipt is the only place this can be said, because it is the only surface he is already looking at. **Corollary to finding 17:** 17 says check whether an ask was answered somewhere other than where it was asked; 20 says check whether being answered was *enough*.

**Owner of items below:** Christian. Everyone else's blockers go in Linear or `Docs/impediments.md`.
**Refresh cadence:** The hourly `keep-work-flowing-cc` scheduled task keeps this current (prunes resolved items, adds newly-surfaced Christian-owned ones); the `retrospective` skill still does the deep periodic rebuild. This is the slow-moving standing-asks list — the fresh-this-hour view is [`Design/briefing.md`](briefing.md).

## How this works

The retrospective stopped being the right channel for "Christian, please flip one switch." Christian-owned asks were buried in retro prose, and nothing surfaced them between retros. This file replaces that pattern.

Read order: top to bottom is blast radius. The top items break canonical workflow invariants right now; the bottom items are operational debt with workarounds in place.

For each item:
- **Fix** = the literal command, click path, or line of config that resolves it.
- **What breaks** = the named system or invariant currently degraded.
- **Source** = `Docs/impediments.md` entry numbers + occurrence count, so the cost is visible.

When an item resolves: delete it from this file, mark the corresponding impediment as resolved in the dashboard regen, note the close in the next retro under "What shipped." **No history retained here — `git log` + the retros are the audit trail.** The resolved log is pruned to the current day; anything older is recovered with `git log -p Design/user-actions.md`.

---

## Standing asks

**None open.** The single ask of the last eight days — releasing the encounter-experience work — was answered by Christian on Discord at 14:38:50Z, 44 minutes after it was raised, and **the filing action that remained of it completed at 17:21Z**. Nothing of it is outstanding in any section.

**One standing switch exists but is not an ask:** the orchestrator lane can be disabled on one word from Christian (live 2026-07-27 19:49Z). **Its default state is the intended one**, so silence resolves it rather than blocking on it, and it must not be re-surfaced as an open question in later runs — the notice was delivered once, with a ping, and that discharges it (finding 12). A genuinely new fact about the lane belongs in the briefing where he will see it next; it does not re-open an ask.

**The 20:54Z run is the first test of that instruction, and it bound.** The source section in `Docs/ops/orchestrator-2026-07-27.md` is unchanged and still non-empty, so step 2.6 folded a real item into a run that then declined to publish it as an ask. **The mechanical gate would not have caught this alone** — the hash agreed, but only because a prior run had chosen a stable item key over the prose; keyed on the section's contents it would have matched a fresh ask exactly. **A rule that is only ever satisfied by its own automation has not been tested**, and this hour the judgement had to lead.

**Held a second time at the 21:55Z run, and the second application is the cheap one.** The source section is still unchanged and still non-empty; the same judgement was applied without re-deriving it, at a cost of one sentence. **That asymmetry is the point of writing the reasoning down rather than the verdict** — the first application cost a paragraph of argument, every subsequent one costs a check that the premise still holds. The instruction stops binding the moment that section's content genuinely changes, which is the condition to watch, not the calendar.

**Held a third time at the 22:56Z run, and this hour the premise-check earned its keep.** The source file's mtime had moved (19:50 → 21:50), which is precisely the signal that *should* re-open the question — and the cheap check said no: `git log` shows one commit on that path and the content is byte-identical, so autosync's checkout moved the timestamp and nothing wrote the file. **A premise-check that is only ever run when nothing looks different is not a check**; this is the first hour it had something to reject, and the thing it rejected was a false positive in the direction of extra work rather than less.

**Held a fourth time at the 23:54Z run, and the check was run again rather than inherited from the hour that vindicated it.** The mtime still reads 21:50:03 and `git diff HEAD` on that path is empty, so the content is confirmed unchanged a second time. **The temptation this hour was the mirror of last hour's**: having once established that the moved timestamp was benign, the cheap move is to stop looking at it — which converts a verified premise into an assumed one at exactly the point it stops being tested. The check costs one command; it was run.

---

## Retracted this period

- **2026-07-25 15:16 — the "CI + deploy allowance exhausted" item, opened at 15:12 and retracted four minutes later. It was wrong**, and it never reached Christian (the retraction landed before the Discord ping fired). A cluster of Actions runs failing at startup with zero jobs executed looked like account-level exhaustion; it was a transient GitHub incident, and Actions resumed unaided by 13:15 UTC. **The lesson is finding 9 above, and it is what made a later, genuinely different call correct**: the cluster that began at 15:05 UTC that same day *was* a real account-payment block — GitHub's own annotation named it, and a fresh re-run reproduced the failure in 6 seconds. Two events, same signature, opposite causes; conflating them would destroy the lesson.

## Resolved today (2026-07-27)

- **23:34 — THR-827 shipped (PR #978), ~33m claim to done, and it is a documented invariant that was never enforced.** `src/types/resolution.ts:26` declared `actionModifiers: number; // capped at ±0.20` and **nothing clamped it anywhere**, while the exemplar hand every author reasons from already summed past the stated bound. **This is finding 14's sixth instance and its purest form yet: not an ambiguous signal but an unbacked assertion**, which is worse in one specific way — an ambiguous check at least runs, whereas a comment that states a limit produces exactly the reassurance of a gate and none of the behaviour. Every downstream reader treats the number as settled design; the code treats it as prose. **The repair records the real ceiling and bounds hands at authoring time rather than pretending to clamp downstream**, which is the same shape as THR-821's own resolution — constrain where the value is written, not where it is consumed, because the consumer cannot tell a legal large value from an illegal one. **Second consecutive clean close, no deferral filed underneath it**, taking the shelf 23 → 22. Filed by THR-821 six hours earlier as the sixth of that streak's ship-one-find-one pairs, and closed inside the same day.
- **22:32 — THR-820 shipped (PR #976), 31m06s claim to done, and it is a clean close that moved the shelf 24 → 23.** The nudge test panel had no schema field for **purpose lines** (*what this step is for*) or **factor lines** (*what is pushing the odds*). WS1 authored both — as fixture constants only, in a test file — so the authoring surface could produce them and the encounter shape had nowhere to hold them. **Finding 2's shape one notch earlier than usual: not a read with no writer, but a writer with no field to write into**, which fails silently in the same way because a fixture that carries the value looks exactly like a schema that does. **Landed before WS5 rather than during it**, which is the whole value of the timing — THR-778 is the next job in line and would have had to mint the same concept a second time, differently, against 100+ encounters. **No deferral filed underneath it**, ending the six-run ship-one-find-one streak, and the depth movement is therefore a real drain rather than the ship-plus-filing wash of the preceding eighteen hours.
- **21:30 — THR-619 shipped (PR #973), and with it the first end-to-end pass of a chain no human touched at any link.** The rival economic scheme is live: **sour the mines → corner the grain → break the guild → starve the faithful**, riding Flow Web stock tiers rather than spawning combat, and **cutting a trade route degrades the player's intelligence picture as well as their income** — a severed route makes a region go dark, so an economic attack blinds as well as impoverishes. Counter-play runs through the same Flow Web the player already reads.
  - **The timeline is the finding:** `Todo` → `Ready for Dev` **19:34:49Z** (orchestrator, first sweep) → `In Dev` **20:02:40Z** (executor, unprompted, 28 min later) → `Done` **21:30:06Z** (1h27m build) → **published and verified live** at `a07a7294` by `check:deploy`, not inferred from a green check. **Three lanes, no messages, no shared session; the only coupling is a Linear column and the `Blocked by` field.** It had been free to start for **22 days** and nobody caught it by eye.
  - **Last run's claim time was wrong and is corrected here:** it reported 20:31:51Z where `stateHistory` records 20:02:40Z. **The correction makes the result better, which is exactly why it was worth chasing** — a figure that flatters the system is the one least likely to be re-checked, and "57 minutes to claim" would have been repeated indefinitely as folklore.
  - **Recorded as a resolution of the ticket *and* of the coupling question** that last run deliberately left open. What is still open is the *unattended* case: this sweep was hand-run during the build, and the lane's first genuinely unattended pass (20:27:02Z) released nothing. See *Open agent work*.
- **19:49 — THR-826 shipped (PR #971), 46m53s claim to done: Threadbare now has a lane that decides what happens next.** Every routine before it was downstream of a decision someone else had made; nothing promoted work, so `Blocked by` — a field every handoff has carried since THR-688 — **had four months of writers and no reader**. Finding 2 at the process layer, and the largest instance yet: the executor drains Ready-for-Dev and refills it with its own deferrals, `daily-backlog-grooming`'s starvation rule fires only when Ready-for-Dev **and** In-Dev are both empty (unreachable while that holds), so depth read healthy all week while authored program work sat in `Todo` indefinitely. **The root cause was authority, not code** — `Docs/ways-of-working.md` had no clause letting an agent begin work unprompted, and the nearest one stopped at *propose*; the mandate edit (D1–D7, settled with Christian in chat) landed first, in scope.
  - **The first sweep found three tickets unblocked for 22, 5 and 22 days** — *"not a backlog that ran dry; a backlog whose dependency field nobody read"*, which is the ticket's own thesis surviving first contact. One promoted (THR-619), one held by the promotion ceiling and **named** rather than dropped, two declined on two separately-stated grounds, and two more recorded as **unassessed** rather than as ready-and-deferred. The distinction between *declined*, *held* and *unassessed* is the part worth keeping: three different states that a lazier report would have collapsed into silence.
  - **It reported its own blind spots.** `validateTraitRefs` is browser-only and cannot run headless → trait coverage logged **unmeasured**; the redundancy judgement pass logged **not assessed**. *"An unmeasured check reported as passing is the precise failure this tier exists to catch"* — applied to itself on day one, which is the strongest available evidence the tier is not decorative.
  - **One deferral filed underneath it: THR-828 (Low)** — `check:process` prints "passed" while silently skipping three Linear-backed sub-checks when `LINEAR_API_KEY` is unset, i.e. in exactly the scheduled/CI context it is meant to guard. **Finding 14's fifth instance**, found by the sweep whose remit is checks that cannot fail. Report: `Docs/ops/orchestrator-2026-07-27.md`.
  - **Its `## Needs Christian` section is the first thing step 2.6 ever consumed**, 7 minutes after the reader shipped — see *This run's measurements*.
- **18:32 — THR-821 shipped (PR #969), 30m20s claim to done, and it is the cleanest refutation-by-measurement of the week.** The question was whether a god's nudge is inert: seven seeded mortals all read `Doomed p=0.050`, and no hand moved the word. The ticket offered two causes — brutal exemplar (content) or feeble mortals (tuning). **Measurement on seeds 42 and 99 refuted both, because the sample was ineligible rather than the analysis wrong.** All seven were `ambient`-tier `npc_*`, which `npcSeeding.ts` creates with **no `domainCapabilities` property at all** (0 of 351 and 0 of 459), and which **cannot reach the nudge stage at any capability** — it is `story_beat`-only, and `resolveEffectiveTier` returns `invisible` without a court position. For the audience that can actually be attended, the exemplar is fine: `spotlight` capability p50 **0.999**, **98.2%** of pairs clearing the floor unaided.
  - **The genuine defect surfaced only because the measurement covered the whole population rather than the reported one: the `notable` tier.** A newly-threaded NPC's non-primary reaches sit at capability **0.027–0.119**; at difficulty 0.45 that floors and **stays at 0%** unaided, with two cards, at the ±0.20 cap, and at the full 0.37 hand. **This is the moment a player is most likely to experiment**, which is what makes it worth naming rather than filing away.
  - **The floor was deliberately not moved**, on the recorded ground that it and the sigmoid carry the whole resolution ladder and THR-571's `success_at_cost` calibration rides on them. Since **zero shipped steps carry `nudges[]`**, the repair is an authoring constraint WS5 (THR-778) inherits — `NUDGE_OFF_REACH_MAX_DIFFICULTY`, written into the spec both authoring skills load, pinned by three tests over the real `computeResolutionThreshold` so it cannot rot when a constant elsewhere moves, with the measurement kept runnable as `npm run measure:nudge-headroom`. The rulebook and the always-load quick-reference card gained the honest line the surviving floor requires.
  - **Surfaced to Christian as a reversible judgement call, not as a ratification request** (finding 7). He was owed a report — the previous brief promised *"you'll hear from us when it comes back"* — and a promise to report is discharged by reporting, never by converting the report into an ask.
  - **One deferral filed underneath it: THR-827 (Medium)** — `actionModifiers` documented "capped at ±0.20" with nothing clamping it, while the exemplar's hand already sums past it. **Sixth consecutive ship-one-find-one, and the first of the six to enter at the same grade rather than a lower one.** Audit: `Docs/audits/2026-07-27-thr-821-nudge-headroom.md`.
- **17:53 — THR-826 filed at High** (orchestrator lane) with its plan doc and grill-me synthesis landing on `main` at 17:57Z. Not a resolution, recorded here because it is what kept the board's top slot continuously occupied when THR-821 left it.
- **17:21 — the four encounter-experience issues reached the shelf, closing the last thread from Christian's afternoon go-ahead.** THR-821 (High), THR-820, THR-777 and THR-778 moved into `Ready for Dev` within nine seconds of each other. **This is the falsifiable test of the last three runs paying out in the direction this file did not predict**, 17 hours before its deadline: the briefing's routing surface *did* have an effective consumer. The consequence pre-committed at 14:54Z — file a defect against this task's publication path — is **cancelled outright rather than rescheduled**, because the condition it was contingent on never occurred. **The transferable half is finding 1 catching this file a second time:** the carry was procedurally impeccable (an outcome-worded test, paid on time, per finding 18) *and* its conclusion escalated across three runs on no new evidence, which is exactly the signature finding 1 names. **A carry can satisfy one finding and be an instance of another simultaneously**, and the escalation is the tell for the second even when the first is fully met.
- **17:14 — THR-798 shipped, and the defect it closed is fabricated-looking evidence rather than missing evidence.** Friday's automated review run filed two real, well-scoped tickets — THR-753 and THR-754, both since shipped — **citing a report file that was never committed to any branch**. Anyone following the citation would have found nothing. **That is strictly worse than an absent report: from the outside a lost artefact and an invented one are indistinguishable**, and the crew's entire audit trail runs on those citations resolving. Finding 14's shape at the documentation layer — a signal that reads the reassuring way for both of its branches.
  - **One deferral filed underneath it: THR-825 (Low).** The same skill's Step 7 (append a footer to the impediment log) **has not run since 2026-05-04, across eleven reports**, and the file's structure has changed underneath it since. **Finding 2 again, with a variant worth the line: a step that silently no-ops leaves the same trace as a step that was never required** — eleven consecutive reports each looked complete. Recorded rather than repaired in passing, because it is a different defect from the one being closed.

- **18:10 — THR-796 shipped (PR #961), and the deliverable is a detector, not a repair.** The browser-verify clause requires a screenshot at the contractual 1920×1080; **`resize_window` on the Claude-in-Chrome surface silently does not resize.** It returns `Successfully resized window … to WxH` while `innerWidth`/`innerHeight` stay put — verified in *both* directions from a fresh session (asked for 1920×1080, then for 800×600; success reported twice, viewport moved neither time). The resting viewport is not a fixed number either (1430×989 on 07-26, 1499×1231 on 07-27) — it tracks the window/side-panel layout, so it is unpredictable per session — and `outerWidth`/`outerHeight` read `0×0` from the extension context, so the tool cannot even detect its own failure. **Every screenshot ever taken through that route was an arbitrary size presented as the contractual one.** The DoD ladder now forbids that presentation, requires the measured dimensions be stated, and moves the viewport-contract proof to a separate DOM assertion from the Browser pane (which resizes correctly but cannot composite unattended). **Shipped clean — no deferral filed underneath it**, ending a four-close ship-one-find-one streak, and it took the shelf 20 → 19.
  - **Finding 14's third instance, and the first where the lying signal is a tool's *success string* rather than a check's verdict.** Vercel's green `success` conflated "published" with "skipped"; `check:wiki-freshness`'s PASS conflated "clean" with "stale baseline"; `resize_window` conflates "resized" with "acknowledged". All three answer the reassuring way for both branches, and all three were repaired by **measuring the thing out of band** rather than by adding a gate. **The sharpening this one adds: the previous two were signals we could in principle have fixed; this one is a third party's, unfixable from here, so the only available repair *was* the disambiguation.** That makes it the cleanest argument for the pattern — where a gate was never an option, the observer still sufficed.
- **17:11 — THR-794 shipped (PR #959).** `Docs/ops/scheduled-tasks-registry.md` is the authoritative cross-reference against `list_scheduled_tasks` and the Windows Task Scheduler lane, and **two currently-running tasks had no row**: `website-code-work` (disabled, but live in the scheduler) and **`ThreadbareRepoAutoSync` — the containment script written for THR-671/672**, the harness behaviour that parks the home tree off-branch and manufactures phantom staged diffs. **The omission is worse than a gap in a list**: the registry is where a session goes to learn what protects the home tree, so a missing row reads as *no containment exists*, and the documented repair path for the most-escalated infrastructure incident of the month was invisible at its own index. Both lanes covered again.
  - **One deferral filed underneath it — THR-824 (Low)**, found while writing the very row that was missing: `threadbare-autosync.ps1` exists **only** as an unversioned 4,634-byte file on one disk, with no mirror in the repo. **Finding 2 at the ops layer, and a variant worth naming: the artefact has no producer of its own durability.** Every prior instance was a read with no writer or a pool with no source *inside* the codebase, where a sweep could in principle reach it; this one is invisible to every gate for a stronger reason — an unversioned file is not in the set any gate enumerates. **Ship-one-find-one holds for the fourth consecutive close, and again the found item entered a grade below the shipped one** (Medium out, Low in), which is the composition drift of finding 19 measured inside a single hour.
- **16:38 — Christian answered the release ask on Discord, 44 minutes after it was raised, and the standing-asks list is empty again.** His words, verbatim: **_"Release the remaining encounter experience to the queue"_** — a threaded reply to the 14:01Z doorbell. **Treated as settled authorization for THR-821, THR-820, THR-777 and THR-778 to enter `Ready for Dev`, and never to be re-asked.** The ask lived exactly one run, which is the right lifetime; the eleven-hour unread-answer failure that motivated step 0 did not recur, and this is the second answer the inbox has caught. **What the reply could not do is file them** — this task may not write Linear state, and no message can widen that. So the run's real deliverable was the *receipt*: it told him, unprompted, that his sentence is recorded authority rather than a filed action, and named the shortest path to the filing. Source of finding 20; the residual filing action sits in *Open agent work* with a falsifiable test on the routing surface.
- **16:30 — THR-819 shipped (PR #957).** The local `check:wiki-freshness` never fetched, so it diffed against whatever `origin/main` the repo last saw and **reported PASS on precisely the condition CI fails** — it cost THR-802 a two-hour red streak yesterday, telling the author they were clean minutes before the blocking copy of the same check stopped them. Now it fetches its diff base, and **every verdict states whether it did**, which is the half that generalises: the defect was never the missing fetch, it was that a stale-baseline PASS was indistinguishable from a real one. Second same-repo instance of finding 14, and the first where the ambiguous signal was **ours** rather than a vendor's. **Published and verified live** (`check:deploy` → `deployed`, `95feb3a7`), not inferred from a green check — which is itself finding 14's first instance being used as intended.
- **15:16 — THR-793 shipped (PR #955), ~1h claim to merge.** Sixteen orphan root-level markdown files triaged, and the hygiene check that was supposed to notice them **rewritten to enumerate** rather than spot-check — the repair being the detector, not the sweep, which is finding 2's standing shape. Six relocated pre-template docs were marked `lint_plan_doc`-exempt rather than retrofitted, i.e. disposed of with a recorded reason instead of force-fitted (the THR-808 pattern, third application). **One deferral filed underneath it: THR-823**, a stale third copy of an installed task prompt, with three plan-doc lines still naming the dead copy as the edit target. Ship-one-find-one, and the found one entered at Low while the shipped one was Medium — the composition drift of finding 19, visible inside a single hour.
- **12:38 — THR-813 shipped, ~37m claim to done.** The tracked residue of THR-808: dead trait refs that no producer could ever grant. **13 unproducible ambition selection keys were retired rather than repointed**, and the three that could not be repointed carry a written reason — the disposal-with-reasons pattern THR-808 established, applied a second time. **One deferral filed underneath it (THR-822)**: two ambition-abandonment triggers describe a bearer who has stopped and stayed, and **nothing on an agent records where they live or how long they have been there**, so the loss those triggers test for is not computable. Finding 2's shape once more — authored content whose premise has no producer — found this time by executing the ticket's own instructions rather than by a sweep.
- **11:58 — THR-775 shipped (PR #951), 56m12s claim to done, and the new encounter interface is live.** The player-facing half of the Nudge Model: the nudge hand rendered per `NudgeBlockedCode` policy, the fate reveal in words (numerals never render), the commit path that finally **writes** `activeNudges` — a field that until now had readers and no writer, finding 2's exact shape — spends essence, and emits the declared-but-never-emitted `nudge_played` trace, plus the aftermath modals and a designer view in the DebugPanel. **Legacy `authoredChoices` templates keep the old screen**, branching on data presence rather than a flag, so the rollout is per-template and nothing regressed on the way in. **Published and verified live** at 11:59:44Z (`check:deploy` → `deployed`, `442fdcae`), not inferred from a green check. **Shipped with no deferral underneath it** — and with THR-774 68 minutes earlier, WS0/WS1/WS2/WS3 are all Done, leaving the program four-fifths delivered in under 30 hours.
- **10:50 — THR-774 shipped (PR #949), 47m53s claim to done — the first half of the top-priority encounter experience, and a clean close with nothing filed underneath it.** The nudge authoring pipeline is rebuilt: `encounter-pipeline` and `template-encounter-rewrite` now run the 8-step checklist (vignette → test data → 4–8-card hand with ≥4-sphere coverage → six-band prose with band fragments → trait hooks restricted to `validateTraitRefs`-passing refs → aftermath objects → image tags with fallback chain → scorer+detector evidence), with THR-782's UL entries (Nudge / Broken mortal state / Rebuild Road / Dissolution threshold) shipping in the same PR. **The deliverable that matters against finding 2 is the golden exemplar** (`NUDGE_GOLDEN_EXEMPLAR`, one of the two declared `src/` leaf files): a spec whose only instance is its own description is authored capability with no consumer, and this one ships its first consumer in the box. **The 06:30 handoff prediction is now closed at both ends** — the design pass took 15 minutes, the plan doc gate cleared at 07:18, and the build took 48; the whole workstream ran design-to-shipped inside 4h35m. **Recorded as a pass of this file's own test** (see *This run's measurements*), which is the half of finding 16 that produces nothing to write about and therefore gets skipped.
- **09:28 — THR-802 shipped (PR #943), ~2h25m claim to done — the slowest close of the day, and the delay is the deliverable.** The fix itself was small and correct within the first half-hour: `branchingDistance.ts` resolved trait refs by raw node-id equality, so of the four ref forms THR-786's shared resolver accepts (node id, short id, display name, tag) **three were invisible to it**, and item-granted traits were ignored entirely — the KPI therefore **over-reported** how many agents a gate excludes, with a fabricated `gap`. Diagnostic-only, zero gameplay impact, and latent besides (no shipped template authors a trait gate yet), which is exactly why it was worth landing *before* WS5 starts authoring them: the first tuning readout on new gated content would otherwise have been wrong in the direction that invites over-loosening. **What consumed the two hours was the blocking wiki-freshness gate, and diagnosing it found a real second defect** — the local copy of that check never fetches, so it had told the author they were clean while CI failed on the same condition. Filed as **THR-819**; folded into finding 14 as its first same-repo instance. **The 10:02Z `main` merge that last run correctly called "activity, not progress" is now confirmed as exactly that**: it did nothing about the failure, and the actual repair was the two commits at 11:09 local that updated the interface-map page and regenerated the dashboard. **Not filed as a lane defect:** a correct blocking gate stopping a PR that had genuinely not updated its documentation is the gate working, and the ~100 minutes bought a detector for the trap that caused them.
- **07:19 — THR-814's park closed, and the "wait one refresh" precedent held at 4-of-4 in the same minute.** Two carries discharged in one hour, neither by anyone acting on this file. **THR-814** went Done at 07:19:22Z after ~2h40m parked with its work merged — see finding 16 for the closed record. **PR #940 merged at 07:18:47Z**, 36 minutes before the run that had committed to calling it a defect if it hadn't; `Docs/plans/2026-07-27-nudge-encounter-experience-ws1-ws2.md` is verified on `origin/main` by `git ls-tree`, not inferred from the PR state. **Both were stated as falsifiable tests and both are reported here having come out favourably** — which is the half of finding 16's procedural rule that is easy to skip, because a passing test produces nothing to write about. A record that only surfaces predictions when they fail overstates how often carrying an item was necessary.
- **07:12 — THR-802 claimed, and it is the first bench occupancy in five runs that is live work rather than a stale row.** PR #943 opened within the minute with auto-merge armed. Its required check went red at 07:29 on the THR-730 wiki-freshness gate — logged as a measurement with a falsifiable test, **not** as this file's item: it has an owner mid-session, and a red gate is a technical verdict, not a Christian ask (finding 7). **Explicitly not filed under the armed-and-`BEHIND` self-clear precedent** — that precedent is about a green PR waiting for the branch to catch up, and armed auto-merge never rescues a *failing* required check. Two superficially similar PR states, opposite causes; conflating them would corrupt a 4-for-4 record with a case it does not cover.
- **06:54 — the THR-799 standing ask closed, answered out of band and nine runs late.** The ask (opened 2026-07-26 20:54Z, pinged once at 21:00Z, carried unanswered through nine hourly runs) was **priority ordering**: should the ceremonial reveal surface be built ahead of crew-found work? **The answer was already written down.** THR-774 and THR-775, moved In Design → Ready for Dev at 06:30:39Z, each carry in their description: *"**User priority (Christian, 2026-07-27):** 'the new encounter experience is still first priority.'"* A dated statement naming a different item as first priority answers the ordering question directly, in the direction of the ask's own default. The board matches — THR-774/775 at High, THR-799 at Medium. **Closed, retired, not to be re-asked.** **Stated in the briefing as an inference from an attributed quote rather than a reply**, so Christian can overturn it in one line if the design session mis-recorded him. Source of finding 17.
- **06:52 — THR-816 shipped (PR #941), ~94m claim to done.** `pickFactionForNpc` took the first faction whose `factionType` matched a role's preference, and six definitions share `factionType: 'guild'` — so the same guild won every time and the **Builders' Fellowship was seeded with zero members in every run**, its 10 templates unreachable by construction. Now a scored pick: **reach-fit minus a load term**, with the merit function **composed from two tables that already existed** rather than green-fielded (THR-614's failure mode, named as the precedent avoided — second consecutive ticket to do so). **The anti-starvation test asserts over the role *mix* across guilds, not a per-guild floor**, because a per-guild floor is the single-guild assumption restated as its own check — finding 5's pool-scoped-guard lesson one domain over. **Two deferrals filed rather than absorbed, both produced by executing the ticket's own instructions:** **THR-818** (`GuildQuestPanel.tsx:82` hardcodes `adventuring_guild`, so 11 of 12 factions can never surface guild quests — found by discharging the UI-pillar line *"confirm no surface hardcodes a guild roster assumption"*) and **THR-817** (`bandOpposition` mints ids from a module-scope counter, so identical simulations serialise differently — an NFP #3 determinism break where only the id diverged, not the content, which is why it stayed invisible until guild membership first activated the path).
- **06:30 — the Nudge Model WS1/WS2 design pass completed in 15 minutes** (In Design 06:15:20Z → Ready for Dev 06:30:39Z), putting THR-774 and THR-775 on the shelf at High with coordination blocks, intent-judge Allow (round 2), and NFP/three-pillar/Vision audits integrated. **Both are pickup-gated on their plan doc (PR #940) landing**, which had not merged at this run — see *This run's measurements* for the stated falsifiable test.
- **05:54 — THR-815 shipped (PR #936), 52m claim to done — and the three-ticket guild thread closed.** THR-810 asked whether the apex guild rank was mis-tuned; it was unfed. THR-814 fixed two mechanical blockers and measured the dominant one. **This built the routing rule THR-814 chose:** since the attention model's premise is that ambient actors are not simulated individually, guild work for the 225-of-227 ambient members resolves at the tier that *is* simulated — `factionMemberWork.ts` sweeping from `phaseFactionActions` on a 6-tick gate, settling one authored job per selected off-loop member. **Measured (seed 42 / medium / 150 ticks): reputation gains 0 → 449** (+32.575 over 185 memberships), **jobs resolved 0 → 944** (462 succeeded, 135 promotions), **apex holders 0 → 33**, **rank-blocked templates 21 → 0**, `>=0.60` at tick 150 **33 → 66**. `sweep:rank-reach` **exits 0** and is now a live gate. **It extended the attended path rather than paralleling it** — `getAccessibleTemplates` exported not copied (a second copy is exactly the `merchant_consortium` drift THR-814 found), payout through the same `processFactionEncounterReputation` with one optional `rewardScale`, and **off-screen work at half rate** so the unattended path cannot out-earn a watched encounter. Cost **0.109 ms/tick**, stated on the NFP #7 axis that rejected spotlight promotion at ~14×. **Two selection rules are load-bearing:** the window rotates with the pass counter (filling from the head is THR-814's positional starvation, ruled out by construction), and is **proportional to membership** — the flat cap written first scaled per-member cadence *inversely* with guild size, leaving the 45-member Merchant Consortium at 0.765 against an 0.85 apex while the 3-member Underking's Court saturated, i.e. penalising exactly the guilds holding the most content. **UI pillar decided rather than skipped:** only rank changes surface, via the existing `faction_rank_changed` event; routine jobs stay in one aggregate trace per faction per pass. **One deferral filed rather than absorbed (THR-816)** and one metric movement proven benign by stash-A/B rather than waved through. Audit: `Docs/audits/2026-07-27-thr-814-faction-draw-path.md`.
- **04:41 — THR-814's work merged (PR #934), ~40m claim to merge — and the issue deliberately stayed open.** The ticket asked which of three routing rules should make guild work reach guild members. **All three were void, and so were THR-810's three levers before them: every one adjusts a stage the content never reaches.** Stage census on the real pipeline (seed 42 / medium): generator **1,198** → awareness **857** → visibility **857** → prerequisites **817** → **after the cap, 0 of 1,263**. Two real defects fixed. (1) **Positional starvation at the cap** — `capWithDiversity` fills 40 slots from the *head* of a ~4,650-entry list while per-agent generators append at the tail, so they were unreachable by construction rather than outscored; an opt-in `personallyOffered` flag + `PERSONAL_OFFER_CAP_RESERVE` (6) + a Phase 1c reserve modelled on `BRANCHING_CAP_RESERVE` took survival **0 → 60.6%**, and such entries rank **first on 40 of 56** boards containing one. (2) **`merchant_consortium` declared `mc_trade.*` at all four rank tiers while all 15 of its templates are `mct.*`** — zero matches at every tier, 49 memberships drawing nothing; the drift was already documented at the *read* site (the THR-805 gate cites it) and never repaired at the write site. Rank-gated reachable templates **0 → 39**, pinned by a namespace-root test across all twelve factions. **The dominant blocker was measured and left alone:** 225 of 227 memberships sit on `ambient` NPCs while `phaseAgentDecision` runs only for `spotlight`, so **2 of 227 members can reach the draw path at all**. Routing rule chosen (resolve faction-side in `phaseFactionActions`) with **five** alternatives rejected on measured cost — promotion ≈14× the per-tick decision cost in the known large-map stall phase; re-seeding cuts 227 memberships to ≤17. Filed as **THR-815** at High. **The park is correct, not a defect** (finding 16): 3 of 4 Done-whens carry to the successor and the PR body says so. Audit: `Docs/audits/2026-07-27-thr-814-faction-draw-path.md`.
- **03:45 — THR-810 shipped (PR #932), ~45m claim to done — as a verdict, not a fix.** The question was whether the elite guild rank (`minReputation` 0.85) is mis-tuned. It is not; it is **unfed**. Over the live window on seed 42 / map medium: **0 reputation increases, +0.000 total**, against **227 faction memberships** and ~150 authored faction templates, because faction content was drawn **3 times in 200 ticks and never once by a member of the owning faction**. `processFactionEncounterReputation` is correctly wired at both call sites and returns at its `if (!factionEdge)` guard every time. So `member_of.reputation` only decays — ≈0.0008/tick from 0.900, `>=0.60` gone by tick 450 — and **all 60 rank-gated templates across 12 guilds are unreachable, senior as well as elite**. A mortal can hold a guild badge for a hundred turns and never be sent anything. **The constant was not changed**: the executor shipped the verdict plus `sweep:rank-reach` (a re-runnable census, exit 1 on this condition) and re-scoped the repair to **THR-814** at High, on the stated ground that choosing how faction content reaches members is a design decision, not a tuning change — finding 13's second application, and it named THR-614 as the green-field precedent to avoid, as THR-812 did. **One deferral underneath it**, ending the three-run clean-close streak; the deferral is the point of the ticket rather than residue from it. Verdict doc: `Docs/audits/2026-07-27-thr-810-guild-rank-reachability.md`.
- **02:29 — THR-785 shipped (PR #930), ~28m claim to done.** Production deploy health is now observable. The native `Vercel` check reports "published" and "skipped, nothing needed publishing" as the **same green `success`** — measured earlier this session on `a9c33078`, `success` on a commit with no deployment record at all — and being non-required, it is the signal every lane is trained to walk past. The shipped probe (`npm run check:deploy`) asks the deployments API which SHA is actually live and classifies into `deployed`/`skipped`/`pending`/`failed`+`stale`, with a 20-minute grace so a slow build is not mis-read as a stoppage; only `failed`/`stale` reach Christian, via this brief's `## Needs Christian`. **Promoting the Vercel check to required was considered and refused** — not a correctness gate, and it would couple every merge to a third party (finding 14). **Finding 10 was carried into the ticket and the implementation beat it**: rather than duplicating the build-relevant path list, `parseBuildRelevantPaths` reads it out of `vercel.json`'s own `ignoreCommand`, so there is no second list to drift. Step 2.5 consumed it live 25 minutes later: `deployed`, serving `27203f8a`. **Shipped with no new deferral underneath it — third consecutive clean close**, and the oldest item on the shelf (filed ~19h earlier), not the newest.
- **01:31 — THR-812 shipped (PR #928), 28m25s claim to done.** Revenge vows now resolve against the death the engine actually records. `target_agent_eliminated` read `properties.eliminated` — **one reference repo-wide, its own read, zero writers** — so a genuinely dead target never satisfied the milestone, while an unresolvable `$`-ref returned `true` and auto-completed it. Both shipped authorings pass `$betrayer` / `$killer`, which nothing resolves, so the auto-complete was the *only* path either ever took; `ambition_avenge_fallen` is requires-2-of-2, so it was half the bar, free at first evaluation. **Per-instance binding was rejected, not built** — `REACTIVE_AMBITION_TEMPLATES` has no assignment path at all (finding 13). The pool-scoped guard that missed this now runs over all three pools with non-empty assertions; reverting both production changes turns 8 of 8 new assertions red. Adjacent dead read (`status === 'dead'`) folded in. **Shipped with no new deferral underneath it** — second consecutive clean close.
- **00:30 — THR-809 shipped (PR #926), 28m15s claim to done** — the one that finally makes a lost tavern brawl leave a wound. Three tickets deep: THR-783 wired the failure effect (green, correct, and it still did nothing), because the *definitions* of every condition and mastery — 11 of 13 — were never inserted into any world. Their sole inserter, `ensureTraitNodes`, hung off the legacy `state.encounterProgress` loop the unified pipeline no longer populates. Now seeded at world init, so presence is an invariant rather than a race, and the `allDefs[0]` short-circuit that let one minted node lock out the other twelve is a per-node check. **Shipped with no new deferral underneath it.**

## Resolved yesterday (2026-07-26)

Compact index. Full prose for each is in `git log -p Design/user-actions.md`; the lessons worth keeping are folded into *Durable findings* above.

- **23:37 — THR-808 shipped (PR #924), 34m43s claim to done.** The residue of THR-800: 22 authored trait refs that no producer could ever grant. **One was not a trait at all** — `living`, an aliveness check written as `agent_lacks_trait`, which therefore answered "yes" unconditionally; it is now a real `agent_deceased` condition the engine sets on death, so a dynasty ends when its founder does. The other 21 were **disposed of per class with reasons recorded** rather than force-fitted — seven are `blockingTraits`, where a wrong repoint silently excludes agents who *should* qualify and is strictly worse than a visible dead ref. Tracked forward as THR-813. **Two deferrals filed mid-flight** (~27 min before its own merge): THR-813 and THR-812.
- **22:45 — THR-811 shipped (PR #922), ~43m claim to done**, 86 minutes after being filed. The lane's only High: the encounter pipeline resolved templates through `getAnyEncounterById`, which cannot see cache-registered regional templates at all, so **five stage-3 gates read fields off `undefined` and waved every such entry through**. Repointed at `getUnifiedTemplateById`, with the `actorAffinities` opt-in polarity trap handled rather than tripped.
- **22:45 — the step-6 ping-hash defect closed, one run after being observed.** The change gate had hashed its own regenerated prose, so it moved on every rewording; it now hashes a stable item key. Folded into finding 12.
- **22:45 — three rows in `Docs/impediments.md` repaired** as a side-effect of THR-811's closeout, which a dashboard had been silently dropping. Finding 4's cheapest instance yet.
- **21:47 — THR-805 shipped (PR #920), ~46m pickup to merge.** Guild rank now gates the senior/elite tiers. The two finds underneath (THR-811, THR-810) were worth more than the gate.
- **20:36 — THR-784 shipped (PR #918), 34m04s door to door.** Condition durations were read off the shared trait *template* rather than the per-carrier edge. The larger find is `totalTicks` — four consumers, no writer, ever.
- **19:54 — standing item 1 ("land the last rescued plan doc") closed as never-real**, after nine days of being carried. The file it named exists on no branch; the real docs were already on `origin/main`. Finding 1's origin.
- **19:40 — THR-783 shipped (PR #916), 37m30s door to door.** Wired, and **proven not yet sufficient** in the same session — filed THR-809 rather than closing on a green diff.
- **18:40 — THR-800 shipped (PR #914).** 40 of 62 dead trait refs repointed; the remaining 22 filed as THR-808. Sequenced correctly behind THR-788 by its own mutex line — finding 11.
- **17:45 — THR-788 shipped (PR #912).** Traits & Marks wiki page + Traits UL shard. The durable deliverable is the `sources` glob, which converts a documentation gap into a blocking CI gate.
- **16:57 — the twenty-two-run carry of Friday's retro draft discharged**, by opening the file instead of describing it.
- **16:26 — THR-803 shipped (PR #909).** Six starter-chain stages were permanently undrawable; every multi-part arc was stuck on its opening chapter.
- **14:58 — the Stale Claim Sweep filing ask discharged**: filed as THR-804 rather than restated a fourteenth time. Filing sharpened the diagnosis.
- **14:42 — THR-779 shipped (PR #907).** 17 unreachable authored encounters wired; 44 confirmed KILL.
- **13:16 — THR-797 shipped (PR #905).** The hourly reaper can no longer delete a workspace a live session is working in.
- **12:33 — THR-801 shipped (PR #903).** `requiredTraits`/`blockedByTraits` declared on a 278-importer type; shipped deliberately unused, and said so.
- **11:32 — THR-787 shipped (PR #901).** Reputation-derived titles had never once rendered — three sites read `category` where every definition stores `subcategory`.
- **10:55 — THR-786 shipped (PR #899).** The traits floor; shipped the `validateTraitRefs` detector and filed two defects rather than shipping over them.
- **09:22 — THR-773 shipped (PR #896).** The Nudge Model has an engine.
- **09:03 / 00:21 — two armed-and-`BEHIND` PR stalls cleared themselves.** The "wait one refresh" precedent is three-for-three.
- **07:39 — production publishing had silently stopped; Christian cleared it with a plan upgrade before any brief could ask him.** The residual gap is observability — **THR-785**.
- **07:38 — THR-761 shipped (PR #892).** Marks the god leaves on people now actually fade.
- **07:11 — WS0 exited design into Ready for Dev in twenty-five minutes.**
- **06:36 — THR-760 shipped (PR #887).** The always-loaded instruction file lost 29% of its bulk with no rule removed.
- **05:35 — THR-755 shipped (PR #885).** Six gates stopped contradicting their own verdicts.
- **04:10 — THR-754 shipped (PRs #882/#883).** The browser-verify evidence contract is satisfiable from every lane.
- **03:38 — THR-780 shipped (PR #880).** Dev-build double-apply fixed; the stash+HMR A/B *method* is the keeper.
- **02:43 — THR-741 shipped (PR #878).** The god's destructive workings now produce authored aftermath.
- **01:24 — THR-776 shipped (PR #875).** The Nudge Model migration audit.

*Resolved entries from 2026-06-23 through 2026-07-25 were pruned 2026-07-26 18:54 UTC. Recover any of them with `git log -p Design/user-actions.md`. The 2026-07-26 index above prunes at the next natural boundary.*
