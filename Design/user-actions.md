# User Action Required

**Last updated:** 2026-07-30 17:54 local, by the hourly `keep-work-flowing-cc` CC task.

**THE STANDING ASK MOVED FOR THE FIRST TIME — CHRISTIAN RAN THE FABLE SESSION, AND THE DOORBELL FIRES.** THR-883 is still `Urgent` / `In Design` and still blocks eleven content tickets (**re-read from `relations.blocks[]` this run**, 11 rows), but its `updatedAt` moved 12:01:37Z → **15:15:13.481Z** and it now carries **PR #1122** — *"Records the design decisions from the THR-883 live prototype session with Christian (2026-07-30)… all decisions carry Christian's chat approval per THR-608."* **Three rounds of live iteration produced a settled format**: a 14-question scene-writer checklist, an 8-class setting-envelope system, a 21-type nudge card library, and a design-time hand-authoring doctrine. **Two executor tickets were spun out (THR-884, THR-885, both `High`, both Ready for Dev, both needing nothing from Christian).** Discord returned a literal `[]`; seventeenth consecutive read with nothing from him, cursor unmoved at `1532373739331649739`. Home tree on `main`, **0 behind**, 0 stranded, 2 tracked-dirty (settled). Deploy `skipped` (benign — docs-only since `143635f1`). Merge gate `healthy`. Fleet heartbeat `ok`. **The run's substantive results: (1) THE ASK'S SHAPE CHANGED AND THE BRIEF HAD TO CHANGE WITH IT — three runs described an unmoved ask needing "an interactive session no lane can substitute for"; the session happened, and continuing to describe it as unmoved would have been the exact failure finding 47 names, one level up: a carried *characterisation* outliving its evidence. (2) THE PING GATE'S NORMALIZATION WAS CORRECTED — last run recorded it as "concatenated, no trailing newline"; that is wrong and does not reproduce. The stored `41cd0ff9…5500b` reproduces only from non-empty lines joined with NEWLINES, no trailing newline. A prose description of a hash function is not the function. (3) THE ORACLE FOR "DID THE SESSION HAPPEN?" WAS A PR BODY, NOT ANY LINEAR FIELD — THR-883 has zero comments and its state never left `In Design`. Every field this lane routinely reads said "unmoved".**

## This run's measurements (2026-07-30 15:54 UTC)

**Predecessor merged before fire time; no open `docs/briefing` PR at fire time, so this run branched off `origin/main` (the benign arm for a fourth consecutive hour — the criss-cross arm remains untested).** Deltas below span one hour of board state.

- **THE ASK MOVED, AND NO FIELD THIS LANE READS SAID SO.** THR-883 remains `Urgent`, `In Design`, **zero comments**, `stateHistory` a single unbroken `In Design` row since 12:01:36Z. **`updatedAt` moved to 15:15:13.481Z and an attachment appeared: PR #1122**, whose body states the live prototype session ran and that every decision carries Christian's chat approval.
  - **Three runs asserted "still needs an interactive Fable session no lane can substitute for."** That was true when written and false this hour. **The detector that caught it was not a status check — it was noticing two *new* `High` rows appear and asking where they came from.** Had the queue delta been recorded as a bare count (+1), the session would have gone unnoticed for another hour at least.
  - **This is finding 47 raised one level.** 47 governs a *field* a third party can change (an arming). **The same decay applies to a lane's own characterisation of a situation** — "nothing has moved" is an observation with a timestamp, and it ages exactly like `autoMergeRequest` does. **Detector: when a carried item has been described identically for N runs, spend one call asking what would have to be true for the description to be wrong — then check that thing, not the item.**
  - **The remaining THR-883 scope was read from the plan doc, not inferred:** the golden-exemplar prose is unwritten, the factor-panel surface is **explicitly parked** (`three_pillars:` line — *"UI (N/A this plan — test-panel redesign is a separate THR-883 design question)"*), and retrofit scope is unresolved. **So the ask survives in narrowed form**, which is what the brief now says.
- **QUEUE 47 → 48, AND ALL THREE COMPONENTS ARE NAMED (finding 46).** 0 Urgent / **2 High** / 8 Medium / 38 Low.
  - **Out:** THR-763 (*ROADMAP substrate-drift*, `Medium`) `completedAt` **15:11:53.779Z** — a genuine completion, third consecutive hour of real throughput.
  - **In:** THR-884 (*setting-envelope framework*) and THR-885 (*nudge card-system engine*), both `High`, both created 15:15Z as THR-883's implementation split. **47 − 1 + 2 = 48.**
  - **`High` 0 → 2 is the operationally significant half.** Last run recorded the shelf as having no urgent or high work; that is no longer true, and both new rows are unblocked (`relatedTo` THR-883, **not** `blockedBy`) — verified from the relations payload, not assumed from the label.
  - **Oldest: THR-739, `updatedAt` 2026-07-24T20:29:29Z = 5d 19h 25m.** **CORRECTION: last run wrote "~6.8 days" and this run's brief says so explicitly.** The two figures last run printed were mutually inconsistent — 6.8 days and a 07-31 20:29Z crossing cannot both hold. The crossing date was right; the day count was not. **A Christian-facing number that is internally checkable against another number in the same paragraph should be checked against it.**
- **PING GATE: FIRES THIS RUN, and the stored hash's normalization was CORRECTED, not confirmed.** Last run recorded the method as *"SHA-256 over the concatenated non-empty lines with no trailing newline"*. Tested three variants against the stored value: bare concatenation → `3a1027f3…` (**no match**); newline-joined with trailing newline → `a3aeabb5…` (no match); **newline-joined, no trailing newline → `41cd0ff9…5500b` (match)**.
  - **Last run's description was of the right family and the wrong function**, and it read as settled because it ended in a matching hash — the match came from the command actually run, not from the sentence written about it. **Finding 42's shape again: the artifact was reproduced, then described inaccurately, and the description is what the next run would have inherited.** Recorded here as an exact command so the next run can re-run rather than re-read:
    `awk '/^## Needs Christian/{f=1;next} f&&/^## /{f=0} f' Design/briefing.md | grep -v '^$'` → `printf '%s'` → `sha256sum`.
  - Content changed substantively, so the gate fires regardless; **the correction matters for the runs where it does not.**
- **Deploy (step 2.5): `skipped`, `needsChristian: false`, `deployedSha` `143635f1` — unchanged.** Verbatim: *"The live site is up to date. Commits since the last publish (143635f1) only touched notes and docs, so the game itself did not need rebuilding."* **Finding 28's classifier held flat correctly for a third hour.**
- **THE REVIEW URL WAS RE-CHECKED ANYWAY, AND THE REASON IT WAS CHEAP IS THE REASON IT WAS SKIPPABLE.** HTTP 200, bundle `index-OMQ21yXW.js`, `forceencounters` ×1 — **byte-identical bundle path to last run's verification**, because the deploy has not advanced. **Finding 45 asks whether the artifact moved; here the probe answering "no" (`deployedSha` unchanged) is the same evidence that licenses not re-fetching.** Re-fetched regardless at a cost of two commands; **the note is that the licence to skip was available and was not relied on.**
- **Merge gate (step 2.5b): `healthy`, `startupFailureCount: 0`.** Twenty-fifth consecutive independent confirmation.
- **Step 2.7 (heartbeat): `ok`.** Verbatim: `{"verdict":"ok","needsChristian":false,"checked":8,"stalled":[],"neverRun":["monthly-rulebook-review"],"summary":"All 8 enabled scheduled tasks are within 2 slots of schedule."}`. **Twenty-fourth consecutive `ok`** — lanes fired 15:01:10Z / 15:26:33Z / 15:53:30Z, **distinct**, so finding 30's wake-boundary defect is untriggered rather than merely quiet.
- **Reaper healthy — ran 17:40:02 local (15:40Z), 14 minutes before this brief.** `SUMMARY:` **44** worktrees / **60** branches / **0** stashes / **0** needs-disposition. **Stashes held at 0 for a tenth consecutive hour.** Worktrees 43 → 44; **attribution: last hour's kwf worktree, not this one** — this run's was created ~15:57Z, after the 15:40Z read (the clock, per last run's correction).
- **HOME TREE CURRENT: on `main`, 0 behind, 0 stranded, 2 tracked-dirty, 1 untracked.** Measured on `main`, per THR-671. Tracked dirt is `.claude/settings.json` + `.claude/settings.local.json` (correction (c)); untracked is Friday's retro draft.
- **Discord: literal `[]`, 3 bytes.** Cursor unmoved — **seventeenth consecutive read with nothing from Christian**. `allowFrom` read from `access.json` before any classification (fail-closed ordering preserved). No receipt owed, none sent. **Note the asymmetry this run makes vivid: he ran a three-round design session today and the channel shows nothing.** Finding 8, sharpened — **an empty inbox is not evidence he is away; this hour it coexisted with his most active work in days.**
- **Sibling reports (step 2.6): two in-window, no items folded as asks.** `orchestrator-2026-07-30k.md` reads *"Nothing needs you"* — empty state, skipped. `backlog-grooming-2026-07-30.md` is the **non-empty-but-ask-free** case (the Sphere-Governed Ascendant recommendation, already closed as answered question 7) — **folded as context, not an ask** (finding 7). **Ninth consecutive observation of that third state; the step-2.6 rule still does not name it.**
  - `weekly-hygiene-2026-07-26.md` is **~4 days**, outside the 36h window.
  - **Same-day orchestrator set is still ELEVEN files** (`-30b` … `-30k`) — **unchanged for a third consecutive hour despite `tb-orchestrator` firing at 15:26:33Z.** This lane still cannot distinguish "wrote no report" from "report unmerged" off `main`, and did not guess. Union fix remains unimplemented; selection correct-by-luck a tenth time.
- **Zero Linear writes.** Three column reads (`Ready for Dev`, `In Dev`, `Done` windowed), two `get_issue` (THR-860, THR-883 with relations), one `list_comments`. Read-only throughout.
- **This run's worktree was created outside `.claude/worktrees/`** (`Dev/Projects/kwf-wt-20260730-1554`), with `rev-parse --show-toplevel` asserted, **the `.git` file's existence checked (THR-797), and the home tree's branch re-read as `main` after the `worktree add`.** Placing it outside `.claude/worktrees/` also sidesteps **THR-880**. **The home tree was touched by `git -C` reads, one `git worktree add`, one `git fetch` of a PR ref (branch deleted after reading), three read-only `npm run` probes, `gh` reads, and nothing else.**

- **THE OPEN ASK IS UNCHANGED FOR A THIRD RUN, AND THE ELEVEN WAS RE-MEASURED RATHER THAN CARRIED.** THR-883 remains `Urgent`, `In Design`, `updatedAt` unmoved at **12:01:37.835Z** — no comments, no state change, **2h52m** after filing.
  - **`relations.blocks[]` was read this run and contains exactly 11 rows** (THR-848, 855, 856, 858, 859, 860, 861, 863, 864, 866, 875). **This is finding 39's detector turned on this lane's own Christian-facing number**: the brief asserts "eleven content jobs are stopped behind it", and a quantity written for a human is a claim needing its own evidence each time it is restated, not a carry that ages silently.
  - **The Needs-Christian paragraphs were reused byte-identical for a second consecutive hour**, so the doorbell correctly stays silent. **Finding 12 as a writing decision, not only a gating one:** the gate hashes item text, so paraphrasing an unchanged ask fires it.
- **THE PING GATE'S HASH FUNCTION WAS REPRODUCED FROM THE STORED VALUE, NOT ASSUMED.** Two runs have now written "the hash matches" as a *conclusion about the prose* rather than a measurement. This run computed it: **SHA-256 over the `## Needs Christian` section's non-empty lines, concatenated, with no trailing newline**, recomputes `41cd0ff9c9a32acf9e67e516cf5786fde56693083addee6dfcfe92cb9ce5500b` — byte-identical to `PING_STATE_FILE`, both against the *previous* briefing and against the one this run wrote.
  - **Why this was worth one command.** "The text is unchanged, therefore the hash matches" is true only if the hash is a function of exactly that text. **Nothing had ever checked which normalization the stored value came from** — a leading-whitespace rule or an included heading would have made the stored hash unreproducible, and the gate would have been firing or suppressing on a rule nobody could state. **The suppression is now a measured property of a known function.**
  - **Finding 42's shape, applied to this lane's own gate:** the artifact being read is a hash, and until you can regenerate it you do not know what it is a record of.
- **A SECOND CONSECUTIVE HOUR OF GENUINE THROUGHPUT — and finding 46's subtraction is again the only reason that is knowable.** Queue **47 ready (0 Urgent / 0 High / 9 Medium / 38 Low), 1 In Dev.** Last run: 48 ready, 1 High.
  - **Accounted for by name:** THR-874 (*"Meeting dev-URL gap — no URL reaches Meet The First"*, `High`) `completedAt` **14:40:16.132Z**, shipped under merge `143635f1` (PR #1119). **48 − 1 = 47, and the `High` 1 → 0 delta is the same row.**
  - **One row moved `Low` → `Medium` inside the window** (Medium 8 → 9, Low 39 → 38). **Membership is unchanged**, so this is a re-prioritisation, not a queue event — recorded rather than explained, because attributing it would require a per-row diff this lane does not retain. **Say "unexplained and immaterial" rather than inventing a cause** (finding 34).
  - **The favourable answer twice running is the observation.** Finding 46 was earned on a drop that was a *stop*; it has now returned *throughput* two hours in a row. **A discriminator that only ever fires unfavourably decays into a standing pessimism** — this is the evidence it is a test.
  - **Nothing stale:** oldest `updatedAt` is THR-739 at 2026-07-24T20:29:29Z, **~6.8 days**, inside `STALE_ISSUE_DAYS`=7 — **crosses 2026-07-31 ~20:29Z if untouched**, now under 30 hours away. Still `BACKED UP` (47 > `QUEUE_BACKED_UP_MIN`=15).
- **Deploy (step 2.5): `deployed`, `needsChristian: false`, `deployedSha` `143635f1`** — advanced from `a10d8678`. Verbatim: *"The live site is serving the latest commit on main (143635f1)."* **Omitted from Christian's Freshness section per the skill** (a healthy deploy is not news).
- **THE REVIEW URL WAS RE-VERIFIED AGAINST THE LIVE SITE, NOT CARRIED.** `https://threadbare.vercel.app/?view=game&seeded&forceencounters` → **HTTP 200**, `<title>Threadbearer</title>`, served bundle `/assets/index-OMQ21yXW.js` contains **`forceencounters` ×1**. **Finding 45's detector, run against a moved target:** the URL was last verified at 11:55Z against `47f47542`, and the deploy has advanced **twice** since. A URL in a Christian-facing document is a standing claim, and the artifact under it changed twice without the claim being re-checked.
  - **Bonus confirmation from the same fetch:** the bundle also contains **`firstunmet` ×1** — direct evidence that THR-874 reached production, obtained from the artifact rather than from `completedAt`.
- **Merge gate (step 2.5b): `healthy`, `startupFailureCount: 0`, `standDown: false`.** **Twenty-fourth consecutive independent confirmation** that Christian's billing action holds.
- **Step 2.7 (heartbeat) returned `ok`.** Verbatim: `{"verdict":"ok","needsChristian":false,"checked":8,"stalled":[],"neverRun":["monthly-rulebook-review"],"summary":"All 8 enabled scheduled tasks are within 2 slots of schedule."}`. **Twenty-third consecutive `ok`** — lanes fired 14:01:14Z / 14:26:32Z / 14:53:30Z at **distinct** timestamps, so finding 30's wake-boundary defect is untriggered rather than merely quiet.
- **Reaper healthy — ran 16:40:02 local (14:40Z), 13 minutes before this brief.** `SUMMARY:` **43** worktrees / **60** branches / **0** stashes / **0** needs-disposition. **Stashes held at 0 for a ninth consecutive hour** (finding 31's re-accrual test). Worktrees 42 → 43, branches flat at 60.
  - **Attribution, corrected from last run's pattern:** the +1 is **last hour's** kwf worktree, not this one — this run's was created at ~14:56Z, *after* the 14:40Z reaper read. **A metric this lane moves must be attributed with the clock, not just flagged as self-produced**, or the correction becomes as reflexive as the error it fixed.
- **HOME TREE CURRENT: on `main`, 0 behind, 0 stranded, 2 tracked-dirty, 1 untracked.** Measured on `main` rather than `HEAD`, per the THR-671 rule. Tracked dirt is `.claude/settings.json` + `.claude/settings.local.json` — settled per correction (c); the untracked file is Friday's retro draft.
- **Discord: literal `[]`, 3 bytes.** Cursor `1532373739331649739` unmoved — **sixteenth consecutive read with nothing from Christian**, and the first `[]` since last run's own-ping case. `allowFrom` was read from `access.json` before any classification (fail-closed ordering preserved). No receipt owed, none sent.
- **Ping gate (step 6): NO PING — recomputed hash equals stored `41cd0ff9…5500b`.** Second consecutive suppression of an *unchanged non-empty* ask, and the first where the equality was **measured rather than reasoned**.
- **Sibling reports (step 2.6): three in-window, no items folded as asks.** `orchestrator-2026-07-30k.md` and `-30j.md` both read *"Nothing needs you…"* — **empty state, skipped.** `backlog-grooming-2026-07-30.md` is the **non-empty-but-ask-free** case, unchanged. **Folded as context, not as an ask** (finding 7). **Eighth consecutive observation of that third state** — the step-2.6 rule still does not name it.
  - `weekly-hygiene-2026-07-26.md` is **~4 days**, outside the 36h window.
  - **Same-day orchestrator set is still ELEVEN files** (`-30b` … `-30k`, plus the base) — **unchanged for a second consecutive hour, despite `tb-orchestrator` firing at 14:26:32Z.** Either the run wrote no report or its report has not merged; **this lane cannot distinguish those from `main` alone, and should not guess.** The union fix specified five runs ago remains unimplemented; selection was correct-by-luck a ninth time.
- **Zero Linear writes.** Three column reads (`Ready for Dev`, `In Dev`, `Done` windowed), one `get_issue` (THR-883, with relations). Read-only throughout.
- **This run's worktree was created outside `.claude/worktrees/`** (`Dev/Projects/kwf-wt-20260730-1453`), with `rev-parse --show-toplevel` asserted after creation, **the `.git` file's existence checked (THR-797), and the home tree's branch re-read as `main` after the `worktree add`.** Placing it outside `.claude/worktrees/` also sidesteps **THR-880**. **The home tree was touched by `git -C` reads, one `git worktree add`, three read-only `npm run` probes, `gh` reads, and nothing else.**

## Open agent work (not Christian's)

- **THE WIP SLOT IS STILL HELD BY A FINISHED JOB THAT CANNOT CLOSE — third hour, condition unchanged.** THR-860 sits `In Dev`, assigned, work complete (PR #1114, all gates green), `blockedBy` → THR-883.
  - **#1114 re-read this run per finding 47, NOT carried: still `DIRTY`, still `autoMergeRequest: null`, now ~3h33m old.** Two hands still required — resolve the conflict (`git merge origin/main && git push`) *and* merge manually. **The re-read is the point: last run earned finding 47 on this exact field, so confirming it rather than inheriting it is the finding being used rather than merely recorded.**
  - **Read the checks, not the label** (finding 43): all conclusions `SUCCESS`, so this is a **conflict**, not a failed gate — unchanged.
  - **The blocker's character changed even though the deadlock did not.** THR-883 is no longer "waiting on a session that has not happened" — the session happened; what remains are three narrower deliverables. **The WIP slot is still held and the remedy is still a Linear write this lane may not make** (memory `parked_wip_deadlock`: unassign to free the slot). Belongs to the executor or orchestrator lane. **If the next pickup run finds no free slot, this is why — and with two `High` rows now on the shelf, that cost is higher than it was last hour.**
  - **Whether #1114 *should* merge is still not purely mechanical.** Merging moves the retrofit pile 7 → 11, and retrofit scope is now an explicitly-named open remainder of THR-883. **This lane took no action either way** and surfaced the number rather than the decision.
- **THIS LANE'S PUBLISH PATH IS HEALTHY, AND THE SELF-COLLISION ARM STAYS UNTESTED.** No open `docs/briefing` PR at fire time — the predecessor merged as `3cc17446`'s ancestry — so this run branched off `origin/main`, the arm that was never in doubt. **The criss-cross arm (predecessor still open and armed) has not recurred since the 10:54Z instance**, and the corrected rule for it stands unexercised: *after opening the PR, re-read the merge state and merge `origin/main` in whenever it is not `MERGEABLE`.* **Wants a ticket** (finding 23).
  - **Finding 25 still governs the retired clever version:** a structural argument that a conflict is *impossible* assumes the other branch stays still, and an armed PR is never still. Prefer a cheap re-check over a proof about a moving object.
- **The already-shipped-under-parent mechanism is carried, not re-derived.** THR-680 and THR-681, both children of THR-674, were both promoted carrying scope already delivered under the parent's id (impediment #310, memory `split_child_ships_under_parent_id`). **The cheap detector is at the split, not at pickup** — when a parent is broken into children, record which child carries already-delivered scope. **Wants a ticket** (finding 23).
- **THR-871 IS FILED BUT SITS IN `Idea`, WHICH IS THE UNREACHABLE COLUMN** (finding 19 / THR-835's shape). Filed Medium against CLAUDE.md's priority-1 deferral bucket being unexecutable — `pull-work` sorts on priority alone and all deferrals are `Low`.
  - **The ticket about the unreachable bucket is itself parked in the column the promotion lane must reach into.** Not a filing defect — `Idea` is where a four-option decision ticket belongs — but worth stating so it is not assumed queued.
  - **Note the premise shifted this hour and the ticket gets *more* relevant, not less:** with the WS5 partition gone from the shelf, `Medium` work is no longer continuously in queue, so the deferral bucket's unreachability now bites sooner. **Belongs to the orchestrator's promotion lane**; no Christian input needed.
- **The assignee-leak instrumentation is TICKETED — THR-867.** **Priority correctly `Low`: the leak is cosmetic while the reader stays indifferent** (THR-845 removed the filter; THR-681 was claimed while stamped).
  - **All 49 Ready-for-Dev rows read clean this hour**, including THR-881, promoted inside this window by orchestrator run k. The three rows stamped last hour (THR-880, 873, 872) now read clean too.
  - **THAT IS NOT EVIDENCE THE LEAK IS GONE, and finding 44 is the reason.** The orchestrator's own filing procedure includes an explicit `save_issue(assignee:null)` + `get_issue` verify, so **a clean read is equally consistent with "no leak" and "leak, then successfully cleared."** The three older rows were hand-cleared, which is finding 34's corollary exactly: a repaired row is not a data point about the write path.
  - **The discriminating observation remains unavailable from a column read** — it requires watching the write sequence at promotion time, which is what THR-867 should instrument. **Do not report a clean sweep as a resolution.**
- **The step-0 allowlist snippet wants a one-token path fix**: `$HOME` → `$USERPROFILE` or a Windows-resolvable literal, because Windows `python` cannot open the MSYS-style path `$HOME` expands to. **Wants a ticket** (finding 23).
- **Step 2.6's section extraction is keyed on the producer's incidental formatting AND its file naming, and neither is stable.** The same-day orchestrator set is now **eleven files** (`-30b` … `-30k`, plus the base) — a **seventh** consecutive correct-by-luck selection.
  - **The mtime fix specified two runs ago stays falsified:** `-30g` and `-30h` carry the identical mtime `08:20:23`, so an mtime sort has nothing to discriminate on and degrades to shell order — the coupling it was meant to remove. **A fix needs power just as a test does** (finding 37, applied to a repair).
  - **Corrected fix shape: stop deciding "newest" at all.** Read *every* in-window report per producing task and union their `Needs Christian` sections; de-duplicate on item text. A lane emitting eleven reports a day has no single current one, and any tie-break is a guess.
  - **The formatting axis is unchanged and still live:** `orchestrator-2026-07-30.md` uses `## Needs Christian`; `orchestrator-2026-07-29.md` uses `### Needs Christian` — same lane, one day apart, two levels. Match the *text* with a permissive level (`^#{2,6} Needs Christian`), terminate on a same-or-shallower heading. **Wants a ticket** (finding 23).
  - **The third section state recurred a sixth time and the rule still does not name it.** `backlog-grooming-2026-07-30.md` is **non-empty but ask-free**. **Fix shape:** treat *no-decision-required* as a first-class skip alongside empty, folding such sections in as context rather than items.
- **`check:task-heartbeat` mis-fires at a wake boundary — carried, not re-derived this run.** The probe accepts any sibling whose `lastRunAt` falls **inside** the stall window as its liveness witness; after a host sleep the whole fleet's `lastRunAt` clusters at the window's *closing* edge. **Fix shape:** require the witness to have fired strictly inside the window with a margin, or detect the burst directly (≥2 lanes sharing a `lastRunAt` to the second ⇒ host-wake, suppress). **Wants a ticket** (finding 23).
- **Verify-after-write needs a clock — carried; THR-860's self-refuting row remains the best artefact.** Its description asserts a `get_issue` re-query confirmed the assignee absent; the field was present on read. **The claim and its refutation live in the same row**, and the row is this hour's WIP-holder. **Fix shape:** verify after the last side-effect in your own sequence, or on the next run.
- **FOUR open PRs now — three `DIRTY` and stuck, ONE `BEHIND` and healthy.** The healthy one is new and is the first self-healing candidate in three hours.
  - **#1122 `BEHIND`, armed 15:14:18Z, ~40m old** (the THR-883 plan doc) — **inside finding 29(a)'s ~2h band, so the rule prescribes doing nothing but re-reading it next run.** Note finding 29's final form applies precisely here: the band bounds *when to intervene on `BEHIND`*, and bounds nothing about *when `BEHIND` ends* — #1114 decayed to `DIRTY` at ≤1h32m. **Re-read next run; do not force-update.**
  - **#1114 `DIRTY`, ~3h33m, still disarmed** (content THR-860, opened 12:21:00Z) — detailed above. Needs conflict resolution **and** a merge by hand.
  - **#1096 `DIRTY`, now ~8h47m** (armed 07:06:52Z) — **seventh consecutive run past the band**, stable `DIRTY`, arming intact. Fix is `git merge origin/main && git push`; GitHub's server-side merge ignores `merge=union`, so the web resolver is the wrong tool (memory `dirty_pr_duplicate` — confirm diff direction first; this is the impediments doc, a union-attributed file, so a regressing duplicate is unlikely but must be checked). **Unclaimable from this lane; wants a write-remit session.**
  - **#1031 `DIRTY`, ~44h23m idle** (created 2026-07-28T19:31:05Z) — disposition unchanged: genuinely stuck and genuinely disposable — one `gh pr close`, zero content lost (the THR-762 promotion it carried took effect long ago; the row sits in Ready for Dev). **Unclaimable from this lane; wants a write-remit session.**
  - **The hand-needing queue held at three for a third hour while a fourth PR opened healthy.** **Read the composition, not the count** (finding 6): the total went 3 → 4 and the *stuck* set did not move. A bare count would have read as deterioration.
  - **This run used `gh pr list --json` and `gh pr view` on the same PRs and they AGREED** — no `UNKNOWN` from either. **Recorded as a sample, not a rehabilitation of the list form** (finding 29(b): the field is unstable across time, not across commands; hold no preference).
  - **FINDING 29(b) PAID OUT AGAIN, AND THE UNRELIABLE INSTRUMENT WAS THE *LIST* FORM THIS TIME.** `gh pr list --json mergeStateStatus` returned **`UNKNOWN`** for #1096 and #1031; individual `gh pr view` reads seconds later returned **`DIRTY`/`CONFLICTING`** for both. **The 06:57Z "list form is the better instrument" note was already retracted at 07:54Z; this run is the direct counter-observation** — the list form was the *worse* one here. **Read twice with a gap, and do not develop a preference between the two commands.**
  - **The three-of-three reading is the observation, not each row.** The hand-needing queue has now held at three for two hours while one member degraded further. **Two hours became three; a one-line fix per PR is becoming a session's work** — and none of it is Christian's.
- **THR-835 is unreachable in `Idea`** — carried. The required check has repeatedly run at 80–90% of its 20-minute cap, which is exactly the runner-variance hazard THR-835 describes. **A working promotion lane that never reaches a column is stronger evidence than a broken one** (finding 19). Remedy belongs to a write-remit session.
- **`rescue/2026-07-17-detached-plans` is prunable — carried, not re-derived this run.** A **local branch only**; against `origin/main` it carried **4 unique commits** as of 22:54Z. **Pruning it is `git branch -D` in the home tree, which this lane may not do** — not merely unclaimed, unclaimable from here. **Wants a write-remit session with home-tree authority.**
- **The orchestrator lane's stray-write defect is open and agent-owned.** `tb-orchestrator` wrote its 08:29Z report into the home tree as well as its own worktree, blocking a fast-forward. Durable fix is in that lane's write path (absolute paths, or `rev-parse --show-toplevel` asserted before any write — the workaround this task takes hourly). **Wants a ticket** (finding 23).
- **No gate notices live/mirror prompt divergence.** The two were proven in sync for `tb-orchestrator` on 2026-07-30 02:58Z (diff = one trailing newline), so the general defect stands without a live instance: `~/.claude/scheduled-tasks/<id>/SKILL.md` is what runs, `Docs/ops/scheduled-task-prompts/` is a mirror a merge can update while the live copy stays stale, and **nothing compares them.** THR-850 covers the missing mirror for `tb-opus-pickup`; the divergence *detector* is unticketed. **Wants a ticket** (finding 23).
- **Friday's retro draft** (`Design/retros/retro-2026-07-24-draft.md`) is untracked and wants a `git add` in any docs PR. **Not a carry** — it is THR-798's step 2, with an owner and a Done-when.
- **Impediment #267 is used twice**; **#219 and #228 are duplicate rows for one defect**. Both dispositions: fold at the next retro. **No ticket.**

## Durable findings carried forward

Rules earned by tickets that have since shipped. **Kept as rule sentences; every worked example is in `git log -p Design/user-actions.md`.**

1. **Before carrying an observation into its Nth run, re-derive it once.** **Paid out 2026-07-30 02:58Z as a correction** (three runs blamed a missing `python` interpreter; the real cause was MSYS path expansion, and the remedy changed). **Paid out 2026-07-30 03:57Z in the other direction:** the "reader no longer filters on assignee" carry was checked against `pull-work` SKILL.md lines 27/176/184 and **held** — and the check surfaced a *disposition* the carry had lost (a non-zero count is the documented handled path, not a regression). **A re-derivation that confirms is not a wasted one; it re-sources the claim and often widens it.**
2. **A subsystem with no callers is invisible to every sweep that looks for wrong behaviour, because it has none.** Corollary: grep the *assignment* side, not the read side.
3. **A plan doc's past tense is a claim, not evidence.**
4. **Advisory gates that nobody reads are indistinguishable from gates that do not exist.**
5. **A test suite that agrees with the bug stays green forever** — and a pool-scoped guard is a suite that agrees with the bug one shelf over.
6. **Read composition, not depth** — and stop re-testing the favourable dip.
7. **A gate clearing is not the same as a decision arriving.** Surfacing a settled call for ratification is worse than not surfacing it. Corollary: verify a *favourable* outcome's cause as carefully as an unfavourable one.
8. **An empty Discord inbox is not evidence Christian is away.** Silence means only that he did not write.
9. **A startup-failure cluster is indistinguishable from quota exhaustion at the moment it happens.** The disambiguating evidence — *does a fresh run execute jobs?* — costs one run. Take it before escalating to a billing page.
10. **Deploy-observability must classify by paths, not by branch name or PR title.** — SHIPPED 2026-07-27 in THR-785.
11. **Mutex lines with reasons work.**
12. **An unanswered ask is not an unheard ask — do not re-ring the doorbell.** A standing ask carries a stated safe default, so silence resolves it rather than blocking on it.
13. **Declining to build the missing half is a shippable outcome, and it needs its reasoning recorded to survive.**
14. **A signal with two meanings is not a signal — and the repair is a notification path, never a new gate.**
15. **When a ticket asks "which of these fixes?", census the pipeline before answering** — the honest answer may be "none, and here is why". Validate the *sample*, not just the stage.
16. **A deliberately-parked issue and a broken auto-close are indistinguishable from the board.** Report a falsifiable test in both directions.
17. **Before carrying an ask into another run, check whether it was answered somewhere other than the channel it was asked on.**
18. **State a falsifiable test against the outcome, never against an activity proxy.** State the test's enabling premise explicitly, or a benign world reads as its own failure case. **Now subsumes finding 37's threshold rule — a test also needs enough *power*, not just the right target.**
19. **A column-scoped queue query reports the lane's reach, not the board's contents — and from inside the query the difference is invisible.**
20. **When an ask names a *mechanism* rather than a decision, expect the answer to arrive on the channel he has, not the one the mechanism needs.** (a) Prefer asks that are pure decisions. (b) State plainly what was *not* done.
21. **Re-derive a carry even when you expect it to hold** — the check has a wider aperture than the claim. Look at what the source shows you, not only the field you came to check.
22. **An item in the lane's column is not an item the lane can take.** The cheap detector: read the comments, not just the fields. *Consequential* and *decidable-by-Christian* are independent axes.
23. **A ticket this task files is unpullable by construction, and the cost lands on someone else's run — so filing is never free.**
24. **A file's *state* is evidence; its *author* is a separate claim needing separate evidence.**
25. **A confirmed test licenses the claim it tested, not the remedy that motivated it — and the gap is widest exactly when the result is most satisfying.** Corollary: **the mirror failure is a test whose aperture is wider than the claim.**
26. **One instance locates a defect; the second instance with the same fingerprint is what turns it into a mechanism — and changes the remedy.**
27. **A timestamp is evidence only once its zone is verified, and a `Z` suffix is a claim like any other.** Prefer a machine-written field (`stateHistory`, `lastRunAt`, commit metadata) over a prose header. **The *wrong field* misdates an event as badly as the wrong zone** — THR-667's `updatedAt` sits 28 minutes after its actual `Ready for Dev`→`In Dev` transition.
28. **A classifier observed only in its stable state has not been tested.** **Satisfied in BOTH directions:** three consecutive `deployedSha` advances, then two correct **flat** hours when only docs merged. **Note what switched: the evidence, not the verdict word** — five runs reported `skipped` throughout.
29. **"Green and armed" is not "merging" — and the difference is invisible from every signal a lane normally checks.** Checks answer *is this change correct*; `mergeStateStatus` answers *can it go in now*. **The tell is throughput, not status.**

  **Two corollaries.** (a) ~~**It does not self-heal.**~~ **RETRACTED 2026-07-29 13:23Z, and the retraction GENERALISED 2026-07-30 04:58Z on a second independent data point** — #1042 merged ~2h07m after entering `BEHIND`; **#1085 merged at 04:18:37Z, 1h49m after arming, with nobody intervening.** **The operational rule inverts: do not spend a session force-updating a branch that is merely `BEHIND` — it lands within ~2h unattended.** **The retraction is itself the lesson: "still true at T" is not "true at every T"** — and it was load-bearing twice, because it also set the floor the discriminating test had to clear (finding 37). (b) **`gh pr view` returns `UNKNOWN` for `mergeable`/`mergeStateStatus` on the first read** and often the true value on the second — but **not always, and the unreliability runs in both directions.** **04:58Z: BOTH reads returned `UNKNOWN` for both #1085 and #1031**, where an hour earlier both reads on #1085 returned real values. Treat `UNKNOWN` as "not yet computed", never as evidence of mergeability — **and never assume a second read will resolve it.**

  **29(a) IS NOW SCOPED, AND BOTH BOUNDS WERE MEASURED IN ONE RUN (2026-07-30 09:56Z).** (i) **It applies to `BEHIND` only.** A PR blocked by a *failed* required check never self-heals, and surfaces under the identical `BLOCKED` label — **earned finding 43.** (ii) **Even within `BEHIND`, "lands within ~2h" is a tendency, not a guarantee: #1096 crossed the ~2h07m band and went `DIRTY` at ~2h49m.** The checkpoint last run stated in advance resolved **unfavourably**, which is what a properly-powered test is for (finding 37). **The rule survives in weakened form: do not spend a session on a merely-`BEHIND` PR *inside* the band — but check it once past the band, because the failure mode past it is `DIRTY`, which needs hands.**

  **THE WEAKENED FORM IS ITSELF FALSIFIED 2026-07-30 13:53Z, FROM THE SIDE NOBODY WAS WATCHING.** #1114 went `BEHIND` → `DIRTY` at **≤1h32m** — *inside* the band, where "check it once past the band" prescribes doing nothing. **Every calibration point had been an upper bound** (#1042 ~2h07m, #1085 1h49m, #1096 ~2h49m): they measured how long a benign PR may take, and were silently read as also bounding how early a non-benign one may fail. **A one-sided calibration wearing a two-sided rule's clothing — and the unearned side is invisible because it never produced an observation.**

  **Final form, and it costs one call: inside the band, do not force-update a merely-`BEHIND` PR — but re-read its state each run, because what it decays into is the thing that needs hands.** The band bounds *when to intervene on `BEHIND`*; it bounds nothing about *when `BEHIND` ends*.

  **Detector, general: when a threshold was fitted on cases of one class only, say which class — then state explicitly that the other direction is unmeasured.** Finding 37 asks whether a test has power; **this asks whether the test has power against the hypothesis you did not write down.**

  **The `gh pr list --json` "workaround" recorded 2026-07-30 06:57Z is RETRACTED 07:54Z.** It rested on one favourable sample. This run the list form returned `UNKNOWN` for #1096 and #1031, and **another lane had read #1096 as `DIRTY` 22 minutes earlier** — so the list form is not a better instrument, just another sample. **The field is unstable across time, not across commands**, and #1098 moving `BEHIND` → `BLOCKED` inside 26 minutes says the same thing. **Read it twice with a gap, or read throughput instead** (29's main clause).

30. **A liveness witness must fire strictly *inside* the window it witnesses, not merely fall within it.** **The general form: a guard that accepts an endpoint as evidence about an interval passes hardest exactly when the interval is most degenerate.** Corollary: a probe's verdict is evidence, not a verdict — when independent witnesses contradict it, override and say why.

31. **A cleanup and its cause are separate deliverables, and shipping the cleanup is the moment the cause is most likely to be dropped.** State the re-accrual rate and a dated falsifiable test against the next producing event.

  **The honest quantity is not a rate: it is `|rows the sweep handled|`, workload-dependent and unbounded above.** Meta-lesson: **when a number has been wrong once, the reflex is to move it the other way and re-assert it with equal confidence.** Prefer naming the *variable*.

  **Closed out 2026-07-30 01:58Z in a direction the finding did not anticipate.** THR-845 shipped cleanup, cause-side hardening, **and** a third thing: **it removed the consumer that made the cause consequential.** Generalisation: when a defect's cleanup and cause are both expensive, check whether the *reader* can be made indifferent. **Confirmed twice over by 03:57Z — the leak has produced two more rows (THR-863, THR-864) and produced no symptom, and the reader's own source now documents the leak as a reportable number rather than a fault.**

32. **Verify-after-write needs a clock, not just a re-read — an immediate confirmation read can be served stale and is indistinguishable from success.** **Corollary: a count is not a verification.**

  **A `get_issue` check was correct when it ran and false 131 seconds later. An immediate verify cannot distinguish "my write held" from "my write held briefly"** — so the fix is **verify after the last side-effect in your own sequence**, not after the mutation you care about. **Sharpened by a controlled pair:** 9 immediate verifies after unassigns all held an hour later; 2 after creations were both false within the hour. **The verification was never the variable — what wrote next was.**

  **Best artefact: THR-860's description asserts its own verification and the field contradicts it in the same row.**

33. **A claim widens when it is restated, and the widening is invisible because every restatement still cites the original evidence.** **The drift is toward the remedy's framing**, because a report is written to justify acting. **Detector:** state what observation would falsify *today's* wording, then check whether it would also have falsified the wording used when the evidence was gathered. **Corollary: severity claims need their own evidence, separately from mechanism claims.**

34. **N confirming sightings of an effect do not locate its cause — and each sighting makes the unexamined attribution feel better supported.** **The trap is that repetition reads as accumulating evidence.** **Detector, cheap: before reporting a recurrence, name one property the affected rows share that the unaffected rows do not — and check it.** If you cannot state the contrast class, you have an effect, not a cause.

  **Corollary earned 2026-07-30 03:57Z while *maintaining* a contrast class rather than finding one: a row that was repaired after the fact is not evidence about the write path, and counting it as a clean case silently weakens the very contrast you are testing.** THR-861 reads unassigned only because the orchestrator hand-cleared it. **Keep the repair history attached to the data point, or the contrast decays as the cleanup succeeds.**

35. **A query's filter encodes a premise about the world, and nobody audits a premise that has never produced a visible failure.** (Earned 2026-07-29: `pull-work` selected work with `assignee:null` for months, encoding *"an assignee means a person has claimed this row."* Christian, unprompted: *"I do not use linear or github at all. so any issues in linear or PRs in github with my name on them are yours."*) **Detector, cheap: for each predicate in a query you are debugging, say out loud what it assumes about who or what writes that field — then ask whether anyone has ever confirmed it with the writer.** **Corollary:** 34 says N sightings do not locate a cause; **35 says the cause may not be in the mechanism at all, but in the question.** **The reader is usually yours to change even when the writer is not.**

  **SHIPPED 2026-07-30 01:21:50Z as THR-845, OBSERVED WORKING 02:02:22Z, RE-SOURCED 03:57Z, and CLOSED ON DIRECT BEHAVIOURAL EVIDENCE 06:57:17Z** — `pull-work` claimed **THR-681 while it carried `assignee: "Christian Spliid"`**, a row the pre-fix reader could not see. **Every earlier confirmation read the source; this one watched the behaviour.** **What makes this the finding's proof rather than its epilogue: the eight prior runs each produced a correct local measurement and a wrong global conclusion, and no amount of additional write-side measurement would have converged.** The unlock was a question, asked of the one party nobody had asked. **Second-order lesson for this lane: the answer arrived because step 0 existed to hear it.**

36. **A consumer that matches on a heading's *level* rather than its *text* is a channel that silently narrows as producers reformat.** **Detector: for every "no items found" result on a shared channel, confirm the *absence* against the raw file once, not just against your pattern.** Sibling of finding 19: **a scan reports its own pattern's reach, and a narrowed pattern and an empty world are the same observation.**

  **Generalised: "formatting" was too narrow — the near-miss was *file selection*.** So the rule is **every point where a consumer infers structure from a producer's convention is a coupling the producer can change unilaterally, and each one fails silently in the same direction.** Detector: for each such inference, name the producer change that would break it, then check whether anything would tell you.

  **Sixth consecutive correct-by-luck hour, 2026-07-30 03:57Z: the same-day set is now `-30` … `-30f`.** **A coupling that has survived six hours looks progressively more like a design and less like a hazard, and that drift is finding 33's tell turned on itself.** The luck is alphabetical and finite. **Record the streak as accumulating exposure, not accumulating safety.**

  **THE PROPOSED REPAIR WAS FALSIFIED 2026-07-30 09:56Z, and by a benign input.** The fix specified at 08:58Z was "select by explicit mtime (not glob order)". This run, **`-30g` and `-30h` carry the same mtime to the second (`08:20:23`)** — so an mtime sort has nothing to discriminate on and silently falls back to shell order, the coupling it was meant to remove. **No consequence: both sections read "Nothing needs you", so either pick was right.** That is the only way an instrument's failure surfaces on a benign input, and it is why it was worth checking a fix that looked obviously correct. **Generalisation, and it is finding 37 pointed at a *repair* rather than a *test*: a fix needs discriminating power too, and "take the newest" has none when the producer emits several per minute.** The honest fix removes the decision instead of improving it — **read all in-window reports and union their sections; never pick a winner.**

37. **A falsifiable test needs enough power to discriminate, and a threshold set below a known counter-example has none — it will "resolve" and license nothing.** **Earned 2026-07-30 03:57Z, cleanly.** Last run set the #1085 checkpoint at **1h30m** armed. Finding 29(a) had been retracted precisely because #1042 self-healed at **~2h07m**. The antecedent was satisfied on schedule and the conclusion was unavailable: at 90 minutes, a stuck PR and a slow-but-self-healing PR are the same observation.

  **This is distinct from finding 18.** 18 governs *what* you measure (outcome, not activity proxy). **37 governs *where you put the line*: a threshold must clear the longest benign case you already know about, or the test cannot separate the hypotheses no matter how faithfully it is run.**

  **Detector, one question, cheap: "what is the longest this has taken while still being fine?" — and set the checkpoint past it.** If no benign case is known, say so and state the test as exploratory rather than decisive. **The failure mode is seductive because an under-powered test still *resolves on time*, and a resolved test reads as a settled question.**

  **PAID OUT ONE RUN AFTER BEING EARNED, 2026-07-30 04:58Z.** The re-powered checkpoint (>2h07m) **discriminated**: #1085 merged at 1h49m, settling finding 29(a) in the favourable direction on two data points. **Same PR, same mechanism, same cadence — only the threshold differed, and the 1h30m version would have licensed nothing.** **This is the cheapest confirmed finding in the ledger: one question asked before writing the test, one run later a settled question instead of a repeated one.**

38. **A count measured against a periodic repair process is a *phase* measurement, not a health measurement — and the two are indistinguishable from the number alone.** **Earned 2026-07-30 04:58Z:** the home tree read **10 behind**, exactly at `FRESHNESS_BEHIND_THRESHOLD`, one commit from a Christian-facing alarm — while autosync was **provably healthy** (`git reflog show main`: seven consecutive hourly fast-forwards at `:50:03` to the second). All 10 commits had landed *after* the most recent tick, so the count was this hour's merge rate wearing the costume of decay.

  **Detector: before reporting a behind/lag/backlog count as decay, read the repairing mechanism's own cadence log and ask "when did it last succeed?"** A count plus a healthy cadence is phase; a count plus a missed cadence is drift. **The threshold alone cannot tell them apart, and a busy hour is exactly when the count looks worst.** Corollary: **this is why the THR-671 escalation ran for days** — a behind-count was read as decay without ever checking whether the mechanism had run.

  **Sibling of finding 30 in structure:** both are cases where a *point* observation is offered as evidence about an *interval*, and both pass hardest when the interval is least normal.

39. **A ticket-authoring rule binds the author, and nothing re-binds it at promotion — so a long-parked ticket's numbers rot in place and arrive at the top of the queue looking authoritative.** **Earned 2026-07-30 04:58Z:** THR-680 was promoted as the intended next pickup carrying "the 38-deep home-tree stash stack"; the stack holds **2**, and drained to 0 roughly **8.7 days** earlier. THR-688 rule A (predicates, not counts) exists precisely to prevent this and applies only at authoring time.

  **Detector for any promoting lane, cheap: if a ticket's Done-when quantifies anything, re-measure that quantity during the promotion run and put the fresh number in the promotion comment.** A count that survives the re-measure costs one command; a count that does not has just saved an executor a wasted session. **The failure is invisible from the board — a rotted ticket and a fresh one are the same row.**

  **Note the near-miss in the *evidence*, not just the ticket** (memory `vacuous_probe`): had this run reported "2 stashes" without the reaper's history, the honest reading would have been ambiguous between "the ticket is stale" and "I am measuring the wrong stash stack". **The decay curve `38 → 0 → 1 → 2` is what made it definitive**, and it cost one `grep`.

  **PAID OUT ONE RUN AFTER BEING EARNED, 2026-07-30 06:57Z — and the executor reached it independently.** THR-680 shipped as `1392fa8b`, *"stash residue disposal + already-shipped-under-parent finding"*: the same stale-premise diagnosis, plus memory `split_child_ships_under_parent_id`. Stack now **0**, reaper corroborating. **Scope honestly (finding 25): the *detection* is confirmed cheap and correct. The *detector* — re-measuring a Done-when's quantities at promotion — is still unimplemented in the promoting lane.**

40. **A periodic job that succeeds with nothing to do often leaves no trace, so "no log entry" and "did not run" are the same observation — and only one of them is an alarm.** **Earned 2026-07-30 06:57Z by withdrawing the previous run's alarm.** Autosync's hourly `merge origin/main` had written `:50:03` reflog entries for seven straight hours; two hours showed none, which was reported as "8 minutes late" and escalated to a watch item with a Christian-facing test. **But a fast-forward with nothing to fetch does not move the ref, and a ref that does not move writes no reflog entry.** An idle hour and a failed hour are byte-identical in that log.

  **Detector, cheap: before treating a gap in a periodic job's output as evidence it stopped, ask "would a successful no-op have written anything?"** If the answer is no, the gap is not evidence in either direction — say so, and find a witness that logs unconditionally (a run log, a heartbeat file, an exit code) or report the *outcome* instead of the trace.

  **Sibling of findings 30 and 38, and the family is now clear enough to name: all three are cases of reading a periodic repairer's incidental artefacts as if they were its status.** 30 — a point observation offered as evidence about an interval. 38 — a count that measures phase, not health. 40 — an absence that measures workload, not liveness. **In each, the honest move was to read the *repaired thing* (is the tree current?) rather than the repairer's exhaust.**

  **Note what made this catchable: the alarm came with a falsifiable test, and running it forced a look at the mechanism.** An alarm carried without a test would have hardened into a fact over the next few runs — finding 33's drift, on this lane's own output.

41. **A non-recurrence discriminates nothing.** **Earned 2026-07-30 07:54Z on this lane's own defect.** Last run found a skipped publish with two candidate causes — a correct "no substantive change" skip, or a mid-run failure. This run observed two clean hourly publishes. **Both hypotheses predict exactly that**: a benign skip is supposed to be rare, and so is a transient failure. The observation is consistent with everything and therefore evidence for nothing.

  **The pull is strong because a quiet hour *feels* like an all-clear**, and downgrading the alarm feels like the disciplined move after finding 40 taught the cost of over-alarming. **But 40 says do not read a repairer's exhaust as its status; it does not say silence is health.** The two failure modes are symmetric and both are live.

  **Detector, cheap: before letting a non-recurrence retire an item, ask "what would the *benign* explanation have looked like this hour, and does it differ from what I saw?"** If the two are identical, keep the item and keep its detector request; downgrade the *alarm* while preserving the *question*. **Sibling of 18 and 37: a test that cannot separate the hypotheses has no power, and an unrepeated event is the lowest-powered test there is.**

  **VINDICATED AND SUPERSEDED IN ONE RUN, 2026-07-30 08:58Z.** 41 was right to keep the question open — and the question was then settled **not by waiting for more instances but by changing the witness** (finding 42). **The generalisation: when a test has no power, the move is to find a better instrument, not to run the same test again.** Two more hours of publish/no-publish observations would have discriminated nothing; one `gh pr list` call settled both instances at once.

42. **A detector inherits the blind spots of the artifact it reads — and one built at the wrong stage of a pipeline will manufacture the very pattern it was built to catch.** **Earned 2026-07-30 08:58Z, on a detector this lane had specified for itself two runs running.** The lane publishes `branch → PR → merge`. The proposed check read the **merged commit** on `main`. A run that published perfectly but whose PR had not yet merged is, to that check, indistinguishable from a run that produced nothing.

  **The cost was nearly a fabricated mechanism.** #1101 was open and healthy; the check would have reported a second missed publish; **finding 26 would then have promoted a one-off into a "mechanism" and moved the remedy upstream** — all on an artifact that was sitting in plain sight one command away. **This run's own first hypothesis was exactly that**, and only survived because the PR list was checked before the claim was written.

  **Detector, cheap, and it is one question: "what are all the states this thing passes through, and which one am I sampling?"** Sample the *earliest* stage that proves the work happened (the PR exists), not the *latest* stage that proves it landed (the commit merged) — the tail of a pipeline measures everything downstream of you as if it were you.

  **Completes the family named at finding 40.** 30 sampled a point and claimed an interval; 38 sampled phase and claimed health; 40 sampled exhaust and claimed liveness; **42 samples the wrong stage and claims the wrong actor.** In all four, the honest move is to name what the artifact is actually a record of, before reading a verdict off it.

43. **An aggregate status field that collapses "not yet" and "never" into one value inverts the operational rule between two states it cannot distinguish — and the safe-looking reading is the wrong one for exactly one of them.** **Earned 2026-07-30 09:56Z on a clean simultaneous pair.** `mergeStateStatus: BLOCKED` was returned for **#1107** (required check `IN_PROGRESS` — clears itself in minutes) and **#1103** (same check `FAILURE` — clears only on a push) **in the same query, at the same moment.**

  **The damage is specifically that finding 29(a) exists.** 29(a) established, on two data points, that an armed PR left alone merges within ~2h — and the resulting operational rule ("do not spend a session force-updating it") is *correct for `BEHIND` and wrong for a failed check*. **A red PR left alone under that rule reads as disciplined patience while its ticket sits parked indefinitely.** THR-868's UI half was 7 minutes into exactly that state when this run read it.

  **Detector, cheap, and it is one extra call: never read an aggregate merge status without reading the check *conclusions* beneath it.** `gh run view <id> --json jobs` gives per-step `conclusion`, which separates *waiting* from *failed* unambiguously — and, as a bonus this run, separated *failed* from *timed out* (17m19s wall time invited the timeout reading; steps 1–9 `success` and step 10 `failure` refuted it — memory `ci_red_may_be_timeout`).

  **Distinct from finding 29(b).** 29(b) says the field is *unreliable* — `UNKNOWN`, unstable across time. **43 says the field is *lossy even when it answers confidently*.** An unreliable field teaches you to re-read it; a lossy one teaches you to read something else. **Sibling of the 30/38/40/42 family in the same way:** a value that is a record of *whether the merge button is pressable* is being read as a record of *whether the work is progressing*.

  **THE PROGNOSIS HALF WAS OVER-CLAIMED AND IS CORRECTED 2026-07-30 10:54Z.** #1103 was pushed and merged at **10:30:00Z**, closing THR-868 — 40 minutes after this finding said it "will sit indefinitely absent a new push." **The mechanism claim is intact** (a failed check clears only on a push; `BLOCKED` still collapses "not yet" and "never"). **What was wrong was treating a statement about the mechanism as a prediction about the situation** — a red PR usually has an owner already looking at it, and last run's own scope note said so before the headline overrode it. **Finding 25, on this lane's own claim: the evidence licensed "this needs a push", not "this will be abandoned."**

44. **A persistence test aimed at rows that receive no writes cannot detect a write-path defect — it measures only that nothing wrote, and it passes most convincingly when the defect is worst.** **Earned 2026-07-30 10:54Z on a test this lane designed, deferred twice, and would have "passed" on the third.** The commitment was to re-read THR-863/864/867 past 12:00Z to see whether a hand-cleared assignee "drifted back". All three read clean — **and the orchestrator had not written to any of them since clearing them.**

  **The contrast class was in the same response, unrequested.** The only two Ready-for-Dev rows *written* that hour — THR-872 (10:02:56Z) and THR-873 (10:33:50Z), both fresh promotions — **both carried the leak.** A clean read on untouched rows and a leaking write on touched rows are not in tension; they are the same fact.

  **This is finding 34 read backwards, and that is what makes it easy to miss.** 34 warns against N sightings without a contrast class. **44 is the mirror: N *non*-sightings without a contrast class, where the absence feels like exoneration.** Two runs argued about the test's *deadline* (finding 37, power) when the flaw was in its *sample* — and a threshold argument is seductive precisely because it looks like rigour.

  **Detector, cheap, one question before any "did it recur?" test: "what has to happen for this defect to be able to show up, and did that happen to these rows during the window?"** If the triggering event never occurred in the sample, the test has no power at any threshold. **Prefer watching the write path over watching a fixed row set** — and if only a row set is available, require at least one row that was written during the window.

45. **A promise to a person is discharged by fetching the artifact that person will touch — never by reading the status fields that say it shipped, and the most authoritative-looking field is the one most likely to hand them something broken.** **Earned 2026-07-30 11:55Z, discharging a promise this lane had carried for two runs.**

  **Every upstream signal was green and none of them was the promise.** THR-878 read `completedAt` 11:29:26Z; `check:deploy` returned `deployed` at `47f47542`; the merge landed. **All true, and all silent on whether a URL in Christian's inbox opens the feature.** The check that actually settled it was three commands: resolve the alias (HTTP 200, `<title>Threadbearer</title>`), read the served bundle path out of the HTML, `grep` the bundle for `forceencounters` — 1 hit.

  **The near-miss is the evidence, and it came from the *best* source.** The deployments API's own `environment_url` for that exact commit — the field whose entire purpose is "where this deployment lives" — returns **302**. The stable alias returns **200**. **Had the URL been taken from the authoritative field, the brief would have shipped a redirect, with every status check still green and nothing anywhere to contradict it.**

  **This is finding 42 pointed at a human rather than a pipeline.** 42 says sample the *earliest* stage that proves the work happened. **45 says that when the deliverable is a promise to a person, the stage that matters is the *last* one — the one they touch — because every prior stage can be green while the thing still fails in their hands.** The two are not in tension: 42 governs *did we do it*, 45 governs *did it arrive*.

  **Detector, cheap, one question: "what will they actually click, and have I run it?"** If the answer names a status field, a ticket state, or a probe verdict, it is not the artifact. **Corollary, and it is the expensive half: a URL, path, or command written into a Christian-facing document is a claim requiring its own evidence** — the surrounding facts being true does not transfer.

46. **A queue count that *falls* is not evidence of progress — a completion and a reach reduction move the number the same direction, and the reach reduction is the one that looks like good news.** **Earned 2026-07-30 12:58Z.** Ready-for-Dev went **58 → 49** and `Urgent` went **9 → 0** in a single hour. Read off the columns that is a shelf draining and a fire list clearing. **What actually happened is that Christian halted a programme and its entire head left the lane's reach** — the nine were exactly the WS5 partition, and 58 − 9 = 49 accounts for the whole delta.

  **This is finding 19 run backwards, and the reversal is what makes it dangerous.** 19 says a column query reports the lane's *reach*, not the board's contents — earned on work that was invisible because it sat in an unreachable column. **46 is the same mechanism producing a favourable-looking number instead of a puzzling one**, and nothing prompts an investigation into good news.

  **Detector, cheap, and it is one subtraction: when a queue count moves, account for the delta by name before characterising it.** If the drop equals a set of rows that left the column *without* reaching `Done`, it is a stop, not throughput. **The `Done`-window read is the discriminator and it costs one call** — nine rows closing and nine rows being blocked are indistinguishable from the ready count alone.

  **Sibling of the 30/38/40/42/43 family:** each reads an artifact as a record of something it is not. **46's artifact is a count, and what it actually records is column membership — which is a function of both completion and permission.**

47. **An *arming* is a point observation about a mutable setting, not a property of the thing armed — and carrying it forward converts "cannot land yet" into "will land eventually" with nothing to signal the switch.** **Earned 2026-07-30 14:53Z.** Last run recorded, correctly, that auto-merge was armed on all three open PRs. This run **#1114 returns `autoMergeRequest: null`** — the arming is simply gone, with no event anywhere that a lane reads. The PR had been recorded as *blocked but destined to land*; it is now *blocked and inert*, and the two are indistinguishable from every field except the one nobody re-reads.

  **The damage is that it silently changes the remedy's size.** A conflicted-but-armed PR needs one action (resolve the conflict; the merge fires itself). A conflicted-and-disarmed PR needs two, and the second is invisible from the conflict. **Last run's remedy sentence — "fix is `git merge origin/main && git push`" — was correct when written and is now incomplete**, which is exactly the failure mode a carried status produces: the sentence stays plausible while its world moves.

  **Detector, cheap, and it is a re-read rather than an inference: for any status that a *third party or an automation* can change without notifying you, re-read it every run you rely on it — never carry it across runs as an established fact.** The test for whether a field qualifies: *could this have changed while nobody on my side did anything?* If yes, it is an observation with a timestamp, not a property.

  **Distinct from finding 29(b), which is about a field being *unreliable at read time*.** 47 is about a field being **reliably read and then reliably stale** — the read was correct in both runs, and the error lives entirely in the carry. **Sibling of the 30/38/40/42/43/46 family in the same way: an artifact read as a record of something it is not — here, a *setting's current value* read as a *plan's durable intent*.**

48. **A lane's own characterisation of a situation decays exactly like a third-party field — and it decays invisibly, because the item's every field can stay unchanged while the situation underneath it changes completely.** **Earned 2026-07-30 15:54Z on this lane's headline item.** Three consecutive runs described THR-883 as *"still needs an interactive Fable session no lane can substitute for."* The session had run — three rounds, decisions approved in chat, a plan doc opened. **Every field this lane routinely checks agreed with the stale description:** status `In Design` (unchanged), priority `Urgent` (unchanged), `stateHistory` one unbroken row, **zero comments**. The only tells were `updatedAt` and an attachment list nothing was reading.

  **The catch came from an unrelated direction — a queue delta.** Two new `High` rows appeared, and asking *where they came from* surfaced the session. **Had the delta been recorded as a bare "+1", the wrong characterisation would have survived at least another run**, and by finding 33's drift it would have hardened: each restatement citing the same original evidence, sounding more settled each time.

  **This is finding 47 raised one level, and the level matters.** 47 governs a *field* someone else can change without telling you. **48 governs a *sentence you wrote* about a world that can change without telling you** — and a sentence has no `null` to return. **Detector, cheap, one question: for any item described identically across N runs, ask "what would have to be true for this description to be wrong?" — then check that thing, not the item.** Checking the item is what fails: its fields are exactly what the stale description predicts.

  **Corollary on the remedy, and it is finding 25's shape: the discovery does not license retiring the ask, only re-scoping it.** Three of four deliverables closed; one remains, plus two newly-named ones the plan doc parks explicitly. **A moved ask is not a closed ask, and the pull to declare victory is strongest exactly when the movement is most welcome.**

**Owner of items below:** Christian. Everyone else's blockers go in Linear or `Docs/impediments.md`.
**Refresh cadence:** The hourly `keep-work-flowing-cc` scheduled task keeps this current (prunes resolved items, adds newly-surfaced Christian-owned ones); the `retrospective` skill still does the deep periodic rebuild.

## How this works

The retrospective stopped being the right channel for "Christian, please flip one switch." Christian-owned asks were buried in retro prose, and nothing surfaced them between retros. This file replaces that pattern.

Read order: top to bottom is blast radius. The top items break canonical workflow invariants right now; the bottom items are operational debt with workarounds in place.

For each item:
- **Fix** = the literal command, click path, or line of config that resolves it.
- **What breaks** = the named system or invariant currently degraded.
- **Source** = `Docs/impediments.md` entry numbers + occurrence count, so the cost is visible.

When an item resolves: delete it from this file, mark the corresponding impediment as resolved in the dashboard regen, note the close in the next retro under "What shipped." **No history retained here — `git log` + the retros are the audit trail.** The resolved log is pruned to the current day; anything older is recovered with `git log -p Design/user-actions.md`.

---

## Standing asks

**ONE OPEN, AND IT NARROWED SUBSTANTIALLY THIS RUN — the session ran; three of its four deliverables are no longer open.**

### 1. Finish the encounter-writing format (THR-883) — three named remainders

- **What already happened, 2026-07-30:** Christian ran the live Fable prototype — **three rounds of iteration** — and the decisions are recorded in `Docs/plans/2026-07-30-encounter-authoring-frameworks.md` (PR #1122, opened 15:14:06Z, auto-merge armed 15:14:18Z, `BEHIND` and expected to self-land per finding 29(a)). **Every decision carries his chat approval (THR-608).** Settled: the **14-question scene-writer checklist**; the **8-class setting-envelope vocabulary** with per-class opening variants and a build-time completeness test; the **21-type nudge card library**; and the **design-time hand-authoring doctrine** (runtime filters an authored hand, never shuffles a generic deck).
- **What remains, and why each still needs him specifically:**
  1. **The golden-exemplar encounter is unwritten.** The frameworks specify the shape; the one encounter demonstrating it — and against which the corpus is measured — does not exist yet.
  2. **The factor/odds player surface is explicitly parked.** The plan doc's own `three_pillars:` line records *"UI (N/A this plan — test-panel redesign is a separate THR-883 design question)"*. This is the colour-coded bullet strip he rejected (memory `keyvalue_is_unfinished_ux`); weave-into-prose vs redesign vs both is undecided.
  3. **Retrofit scope is unresolved** — see the 7-or-11 number below.
- **What no longer needs him:** the engine work was split out into **THR-884** (setting envelopes) and **THR-885** (card system), both `High`, both Ready for Dev, both **unblocked** (`relatedTo` THR-883, not `blockedBy` — verified from the relations payload). **The plan doc states the sequencing constraint plainly:** *"If the corpus is written before they land, every template gets migrated twice."* So the content pause now has a second, independent reason to hold that has nothing to do with Christian's availability.
- **What breaks** = **eleven content tickets remain blocked** (`relations.blocks[]` re-read this run: THR-875, 866, 864, 863, 861, 855, 859, 860, 856, 858, 848). Their authoring instructions must also be rewritten to the new spec before pickup.
- **Source** = THR-883, `Urgent`, `In Design`, zero comments, created 2026-07-30 12:01:36Z from his chat directive: *"lets pause all the content migration issues… i would like to run a prototype of the exact way to write encounters with fable, and then have that exact fable agent write the prose, i think this is the only way to ensure quality."*
- **One number, unchanged: the retrofit pile is 7 or 11.** THR-860's 4 capital-cluster templates completed inside the pause window with all gates green and sit in unmerged PR #1114 — `WS5_MIGRATED` is 7 on `main`, 11 in that PR. **Re-read this run (finding 47, not carried): #1114 remains `DIRTY` and remains `autoMergeRequest: null`**, so landing it is still a deliberate two-step act by a session with write access. **No decision is demanded**; the batch is parked either way and no lane has acted on it.
- **Not an ask about *whether*** — he filed it and has now largely executed it. This entry survives only for the three remainders above, each of which is a chat-session output no scheduled lane can produce.

**A new unexplained multi-hour pause would be a NEW ask with new evidence — never the overnight-wake question reopened** (finding 12). If he ever does want overnight throughput guaranteed: keep the machine awake and **on mains power**, because Windows Task Scheduler refuses the reaper on battery (`-2147020576`, memory `git_cleanup_automation`). Recorded as a fact, not a request.

**Seven questions are answered, closed, and must not be re-asked:**

1. **The five empty projects** — Christian, chat 2026-07-25 21:53: *leave them open; they are intake buckets that refill as the game iterates.*
2. **The GitHub Actions payment block** — cleared 2026-07-25 ~17:09Z and again 2026-07-28 17:02Z, both verified by re-run, and corroborated twenty times since by independent mechanisms. **A recurrence is a new ask with new evidence, never this one reopened** (finding 12).
3. **THR-799 priority ordering** — Christian, quoted in THR-774/775, 2026-07-27: *the new encounter experience is still first priority.*
4. **Release of THR-821/820/777/778** — Christian, Discord 2026-07-27 14:38Z.
5. **The Linear "assign issue to PR author" switch** — Christian, Discord **2026-07-29 18:10:03Z**: *"can't find anything like that. I do not use linear or github at all. so any issues in linear or PRs in github with my name on them are yours."* **Closed by falsification, on the terms the ask itself set.** The second sentence is a **standing fact**: an assignee of "Christian Spliid" carries no information and must never be read as a human claim. **Enforced in code since 2026-07-30 01:21Z, observed working at 02:02Z, and re-sourced to `pull-work` SKILL.md lines 27/176/184 at 03:57Z.** **The highest-leverage answer in this file's history: one sentence retired eight runs of misdirected investigation and unblocked the executor lane.**
6. **Should the host machine stay awake overnight?** — **retired 2026-07-30 01:58Z by observation**, now four consecutive awake nights, never answered directly and no answer needed.
7. **The Sphere-Governed Ascendant pivot** — Christian, 2026-07-30: *"the whole area of power curve for ascendant growth is still rudimentary as we build the world and the mortals."* Project parked in `Idea`; THR-870 carries the design scope and activates only when he moves it out. **The board state already matches the verdict, so there is nothing to ratify** (finding 7). Re-surfaced by the grooming lane 2026-07-30 07:28Z with the same recommendation (leave parked) — **folded into the brief as context, not as an ask.**

**One standing switch exists but is not an ask:** the orchestrator lane can be disabled on one word from Christian (live 2026-07-27 19:49Z). **Its default state is the intended one**, so silence resolves it.

**Three durable corrections survive closed asks and are kept as rules:**

- (a) A blanket `git stash` was the wrong remedy for home-tree dirt; the single-path form is correct. Parking his tool-permission edits was never necessary.
- (b) `$REATTACH_MAX_TRACKED_DIRTY = 0` governs the detached-HEAD reattach path and never evaluates while HEAD is on `main`. The conclusion held; its stated reason did not.
- (c) **Tracked dirt alone does not stall autosync.** The stalling class is the *intersection* of locally-modified **∩** changed-by-incoming-commits — proven by fast-forwarding cleanly with `.claude/settings*.json` dirty.

---

## Resolved 2026-07-30

**15:54Z — the ask MOVED for the first time: Christian ran the Fable session, three of its four deliverables are closed, and the lane's three-run characterisation of it was wrong by the time it was written.**

- **THE STANDING ASK IS NARROWED, NOT CLOSED — AND NO FIELD THIS LANE ROUTINELY READS SAID SO.** THR-883 stayed `Urgent` / `In Design` with **zero comments** and an unbroken single-row `stateHistory`. The only tells were `updatedAt` (12:01:37Z → **15:15:13.481Z**) and a new attachment, **PR #1122**, whose body records the live prototype session and Christian's chat approval of every decision. **Settled today: the 14-question checklist, the 8-class setting-envelope system, the 21-type card library, the design-time hand doctrine. Still open: the golden exemplar, the factor/odds player surface (explicitly parked in the plan doc), and retrofit scope.**
- **THE CATCH CAME FROM A QUEUE DELTA, NOT A STATUS CHECK.** Two new `High` rows appeared (THR-884, THR-885) and asking *where they came from* surfaced the session. **Had the delta been recorded as a bare "+1", three more runs would have repeated "the ask is unchanged".** Finding 46's subtraction is designed to catch a *count* moving for the wrong reason; here it caught a count moving for a reason nothing else reported.
- **FINDING 47 RAISED ONE LEVEL — a lane's own characterisation decays exactly like a third-party field.** 47 governs settings someone else can change (an arming). **"Nothing has moved" is the same kind of object: an observation with a timestamp, restated until something contradicts it.** Detector: **when a carried item has been described identically for N runs, spend one call on what would have to be true for the description to be wrong — and check *that*, not the item.** The item's own fields were all unchanged and all uninformative.
- **THE PING GATE'S NORMALIZATION WAS CORRECTED, NOT CONFIRMED.** Last run recorded *"concatenated… no trailing newline"*; that does not reproduce (`3a1027f3…`). The stored `41cd0ff9…5500b` reproduces only from **non-empty lines joined with newlines, no trailing newline**. **The prior run's match was real; its sentence describing the function was not** — and the sentence is what the next run inherits. Exact command now recorded in the measurements section.
- **THIRD CONSECUTIVE HOUR OF REAL THROUGHPUT.** 47 → 48 ready, fully accounted: THR-763 out (`completedAt` 15:11:53.779Z), THR-884 + THR-885 in. **`High` 0 → 2**, both unblocked, both pickable now.
- **CORRECTION carried into the brief:** last run's "~6.8 days" for the oldest queue item was wrong (**5d 19h 25m**), and internally inconsistent with the 07-31 20:29Z crossing date it printed in the same breath. Crossing date was right.
- **Deploy `skipped` at `143635f1` (benign, docs-only since); merge gate `healthy` for a twenty-fifth check; heartbeat `ok` for a twenty-fourth**, lanes at 15:01Z / 15:26Z / 15:53Z, distinct. Reaper ran 15:40Z: **44** worktrees, **60** branches, **0 stashes** (tenth hour), 0 needs-disposition.
- **A fourth PR opened and it is the first healthy one in three hours** (#1122, `BEHIND`, armed). **The stuck set held at three** — composition, not count (finding 6).
- **Discord returned a literal `[]`** — seventeenth consecutive empty read, cursor unmoved. **Finding 8 sharpened: the empty inbox coexisted with his most active design work in days.** Silence is not absence.

---

**14:53Z — the ask is unchanged for a third run; the run's yield is a gate proven by reproduction, a second hour of real throughput, and a new finding earned on a PR that quietly disarmed itself.**

- **THE ONE OPEN ASK DID NOT MOVE, AND ITS HEADLINE NUMBER WAS RE-MEASURED RATHER THAN CARRIED.** THR-883 `updatedAt` unmoved at 12:01:37.835Z — no comments, no state change, 2h52m after filing. **`relations.blocks[]` was read and contains exactly 11 rows**, so the brief's "eleven content jobs" is evidenced this run, not inherited (finding 39 turned on this lane's own Christian-facing quantity). Needs-Christian paragraphs reused byte-identical; doorbell silent (finding 12).
- **THE PING GATE IS NOW A MEASURED FUNCTION, NOT AN INFERENCE.** Two prior runs asserted "the hash matches" as a conclusion about unchanged prose. This run **recomputed it from the stored value**: SHA-256 over the section's non-empty lines, concatenated, no trailing newline → `41cd0ff9…5500b`, matching `PING_STATE_FILE` against both the old and new briefing. **Nothing had ever established which normalization produced the stored hash** — had it included the heading or trimmed differently, the gate would have been firing on a rule nobody could state. **Finding 42's shape applied to this lane's own instrument: until you can regenerate an artifact, you do not know what it records.**
- **EARNED FINDING 47 — AN ARMING IS A POINT OBSERVATION, NOT A PROPERTY.** #1114 returns **`autoMergeRequest: null`** where last run recorded it armed. No event anywhere announces this. **The carried status silently changed the size of the remedy**: last run's "fix is `git merge origin/main && git push`" was right when written and is now incomplete, because the merge no longer fires itself. **Detector: any field a third party or automation can change without telling you is an observation with a timestamp — re-read it every run you rely on it.**
- **A SECOND CONSECUTIVE HOUR OF REAL THROUGHPUT.** 48 → 47 ready, `High` 1 → 0, accounted for by name: THR-874 (`completedAt` 14:40:16.132Z, merged `143635f1` / PR #1119). **Finding 46's discriminator has now returned the favourable answer twice running** — evidence it is a test rather than a standing pessimism. One row moved `Low` → `Medium` with membership unchanged; **recorded as unexplained and immaterial rather than given an invented cause** (finding 34).
- **THE REVIEW URL WAS RE-VERIFIED AGAINST A TWICE-MOVED TARGET.** `?view=game&seeded&forceencounters` → HTTP 200, `<title>Threadbearer</title>`, bundle `index-OMQ21yXW.js` contains `forceencounters` ×1. **The deploy had advanced twice since the 11:55Z verification while the claim sat unchecked in a Christian-facing file** (finding 45). The same fetch showed `firstunmet` ×1 — THR-874 evidenced in production from the artifact, not from `completedAt`.
- **ALL THREE OPEN PRs `DIRTY`, AND NOW ONLY TWO ARE ARMED.** #1114 ~2h35m (disarmed), #1096 ~7h49m, #1031 ~43h25m. **Finding 29(b) paid out with the polarity reversed** — the *list* form returned `UNKNOWN` for #1096/#1031 while individual reads seconds later returned `DIRTY`, directly counter to the (already-retracted) "list form is better" note. **Read twice with a gap; hold no preference between the commands.** Three hours without a write-remit session, and the backlog is degrading rather than holding.
- **Deploy `deployed` at `143635f1`; merge gate `healthy` for a twenty-fourth consecutive check; fleet heartbeat `ok` for a twenty-third**, lanes at 14:01Z / 14:26Z / 14:53Z, distinct. Reaper ran 14:40Z: **43** worktrees, **60** branches, **0 stashes** (ninth hour), 0 needs-disposition. **Attribution sharpened: the +1 worktree is *last* hour's kwf worktree, not this run's** — this one was created after the reaper read. A self-produced metric needs the clock, not just a disclaimer.
- **Discord returned a literal `[]`** — sixteenth consecutive read with nothing from Christian, cursor unmoved. `allowFrom` read before classification (fail-closed ordering preserved). No receipt owed, none sent.
- **The non-empty-but-ask-free sibling state recurred an eighth time** (`backlog-grooming-2026-07-30.md`): folded as **context, not an ask** (finding 7). Both in-window orchestrator reports were the empty state and were skipped. **The same-day set is unchanged at eleven files for a second hour despite `tb-orchestrator` firing at 14:26:32Z — this lane cannot tell "wrote nothing" from "report unmerged" off `main`, and did not guess.** The union fix specified five runs ago remains unimplemented; selection was correct-by-luck a ninth time.
