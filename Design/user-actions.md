# User Action Required

**Last updated:** 2026-07-28 11:56 local, by the hourly `keep-work-flowing-cc` CC task.

**🔴 ONE OPEN CHRISTIAN-OWNED ASK — the home tree can no longer update itself — and the ask was RE-DERIVED AND NARROWED this run after Christian falsified its stated cause.** `Docs/ops/orchestrator-2026-07-28.md` is tracked-modified in the home tree **and** is touched by the incoming commits, so `git merge --ff-only` aborts every hour; the behind-count is now **10**. **It is Christian's because no other actor may touch it:** `threadbare-autosync.ps1` is the owner and is the blocked party, the reaper never touches working copies, and every agent lane is forbidden home-tree state ops by THR-672. **The fix is now a single-path stash + pull** — not the three-file stash the last two runs asked for — and the *hand-edited* framing is withdrawn entirely (see the correction below and finding 24).

**This is the standing-item prediction paying out, and it is recorded as such (findings 16/18).** The `.claude/settings*.json` dirt has been carried for days with the exact words *"inert and non-blocking, but tracked modifications are the class that stalls autosync if it ever needs more than a fast-forward."* **The class was right and the instance was wrong** — the settings files never collided with anything; a *third* file appearing in the same state, on a path the incoming commits also write, is what converted the hypothetical into a stopped process. **A carry that names a mechanism rather than a file survives the file being harmless**, which is the transferable half.

Four standing questions are answered and closed and **must not be re-asked**: the empty-projects question (Christian, chat 2026-07-25 21:53 — *leave the five projects open; they are intake buckets that refill as the game iterates*), the GitHub Actions payment block (cleared 2026-07-25 ~17:09 UTC, verified by re-run), **THR-799 priority ordering** (Christian, quoted in THR-774/775, 2026-07-27 — *the new encounter experience is still first priority*), and now **the release of THR-821/820/777/778** (Christian, Discord 2026-07-27 14:38Z).

> **This file was pruned at the 18:54 run** from 114 KB / 170 lines down to a compact same-day index, restoring its own stated policy (§ *How this works*): *"No history retained here — `git log` + the retros are the audit trail."* **Pruned again at the 02:54Z run, 230 → 176 lines**, folding *Resolved 2026-07-27* to a one-line-per-item index and dropping the 2026-07-26 index that had flagged itself for exactly this boundary. Nothing was destroyed; `git log -p Design/user-actions.md` holds every pruned word, and the durable lessons are carried forward as one-line rules below.

## This run's measurements (2026-07-28 09:56 UTC)

- **Christian replied on Discord at 09:05:43Z and falsified the ask's stated cause in one sentence: *"I did not manually change anything anywhere. Feel free to investigate and do the stuff you need to do."*** **He is right, and the check that proves it is two `git diff` reads that no prior run took.** All three tracked modifications are agent- or harness-authored: the two `.claude/settings*.json` diffs are permission-allowlist appends (`fewer-permission-prompts` entries among them), and the blocking file's mtime is **10:29 local = 08:29Z**, matching `tb-orchestrator`'s second slot exactly, with content that is that run's own report. **This is impediment #267's `sandbox` row biting for real** — a worktree `-C` invocation resolving a relative path against the home tree — which means the lane delivered its report *twice*: correctly through PR #1002, and stray into the home tree.
- **The stray copy is provably redundant, which is what shrinks the ask.** `git show origin/main:Docs/ops/orchestrator-2026-07-28.md` is 130 lines and carries the 08:29Z run's content verbatim (6 occurrences of `08:29Z`, the THR-764 promotion paragraph intact); the local copy is 51 lines and every line unique to it is a *reworded* header or table row whose substance is on `origin/main`. **Nothing is stranded, and the command was changed to say so** — `git stash push -- <path>` on the one file, leaving the settings edits untouched, instead of the blanket `git stash push` the last two runs asked for.
- **A load-bearing quote in the last two runs was the wrong guard, and re-reading the script is what caught it.** Those runs justified "no automated owner can fix this" partly with `$REATTACH_MAX_TRACKED_DIRTY = 0`. **That constant governs the *reattach* path only** (`threadbare-autosync.ps1` lines 36–52, reached only when HEAD is detached); the home tree is on `main`, so it never evaluates. The real blocker is line 73's `git merge --ff-only`, which fails on **the intersection** — exactly one file, confirmed by `comm -12` this run. **The conclusion survived and its stated reason did not**, which is the same shape as finding 21 inverted: a carry can be right for a reason that does not hold.
- **Queue: BACKED UP — 23 ready (2 High / 3 Medium / 18 Low), 1 In Dev**, nothing stale (oldest `updatedAt` THR-715, 2026-07-24T07:59Z, **4.1 days**, inside `STALE_ISSUE_DAYS`=7). Forty-ninth consecutive run above `QUEUE_BACKED_UP_MIN` (15); composition turned over again — THR-655's time gate cleared at 09:29Z and it entered as the second High.
- **NEW — THR-655 was promoted with an assignee, and `pull-work` queries `assignee:null`, so the lane cannot see it.** Every other Ready-for-Dev row has a null assignee; THR-655 alone carries `Christian Spliid`. **This is finding 22's shape at the field level** — the ticket satisfies column, priority and blocker checks, and is invisible to the only query that would claim it. **Agent-owned, not Christian's**, and reported in the briefing's Queue section rather than escalated.
- **THR-838 (the encounter batch) is unclaimed and the amended test's premise limb holds.** THR-837 (High) holds the single WIP slot, claimed 09:04:30Z. **Benign branch, reported as such** rather than as a pass.
- **Discord: one new message, author-verified before classification.** `access.json` `allowFrom` read first (single entry, `247984978283986954`), matched. The `after=` cursor returned **2646 bytes / 2 messages**, one of which was this task's own bot ping and was skipped. **Classified as *operational fact* → verified rather than believed**, per the step-0 table. **Receipt owed and sent; cursor advanced only after the send.**
- **Home tree** on `main`, **0 stranded**, **10 behind** `origin/main`, **3 tracked-dirty**, **1 untracked**. The behind-count has crossed `FRESHNESS_BEHIND_THRESHOLD` (10) for the first time, so step 2's own clause now fires — three runs after the condition it exists to catch actually started.
- **Deploy** (step 2.5): **`skipped`**, `needsChristian: false`, live site serving **`d0170608`**, gap being docs and ops reports. One Freshness line, not omitted (omit-clause covers `deployed` only).
- **Step 2.7 (scheduled-task heartbeat): `npm run check:task-heartbeat` still does not exist** — THR-837 is In Dev building it. **Performed by hand and named as a substitution**: all ten registered tasks read against their crons and the fleet is healthy (`tb-opus-pickup` 09:01:43Z, `tb-orchestrator` 09:27:05Z, `daily-backlog-grooming` 07:16:28Z, weeklies inside their windows).
- **Reaper** ran 11:40 local, 16 min before this run: **28** worktrees / **39** branches / 1 stash / **0** needs-disposition.
- **This run's worktree was created outside `.claude/worktrees/`** (session scratchpad, absolute paths on every write — impediment #267's own workaround, and this run is the reason to keep taking it). **The home tree was touched only by `git -C` reads, one `git worktree add`, and one read-only `npm run` probe**, never by a state op — including when a Discord message from Christian invited exactly that (see finding 24's second half).
- **Zero Linear writes this run**; nothing claimed, no state set, nothing filed.

## Previous run (2026-07-28 09:00 UTC)

- **The hour the ask went out, and it went out with a wrong cause attached.** The home tree's fast-forward was correctly diagnosed as blocked by the *intersection* of "locally modified" and "changed by incoming commits" — one file, measured with `comm -12`, which step 2's own recipe cannot see. **The escalation was right on both axes of finding 22 and the prose framing was not**: it told Christian a file had been *edited by hand*, which he falsified the next morning. Also in that hour: `tb-orchestrator` reached two consecutive on-time slots (the verdict that the outage was over), THR-764 was promoted into the top-priority gap seven minutes after THR-837 left it, and THR-835 was noted sitting at Medium in `Idea` as the durable fix for the 80–90% CI-timeout utilisation hazard. Full prose in `git log -p Design/user-actions.md`; the correction is folded into finding 24.
- **07:57 — the hour everything unjammed**: PR #992 merged (THR-804 auto-closed), `tb-orchestrator` returned after eleven missed slots and in one run promoted THR-618 and cut THR-838 (WS5 Batch 1, 48 templates) with full coordination blocks, and `daily-backlog-grooming` moved the THR-778 container to `Todo`. Lessons already folded into findings 18 and 22.

## Open agent work (not Christian's)

- **Three carries closed last run and are pruned to this line** (file policy: `git log -p` is the audit trail). The THR-778 unpullability carry closed by repair after twelve runs; the orchestrator standing test closed favourably one brief before its deadline; the no-High gap closed at twelve runs. **The one durable residue is already a finding** — a refusal written in the right place is only a signal if something reads it (finding 22 + finding 2), and it went unread for 14 hours because the lane that owns the remedy was the lane that was down.
- **The no-High gap's replacement pattern is worth one line, because it repeated within the hour.** THR-837 left the shelf at 08:21:59Z and THR-764 was promoted into the gap at 08:28:51Z — seven minutes. **Both Highs are infrastructure about our own lanes**; nothing Christian would recognise as game work has sat above Medium for two days. That is finding 19's composition drift continuing rather than reversing, now with a functioning promotion lane, which is the condition under which it should start to reverse if it is going to.
- **NEW CARRY — is the priority work actually started? Falsifiable test, worded against the outcome (finding 18): by the 08:53Z run, THR-838 is In Dev with a claim comment, or a lane has bounced it with a stated reason.** Not *"did the 08:00:53Z pickup run"* — it will have — and not *"did the queue depth move"*, which THR-837 being claimed instead would satisfy while the encounter work sits untouched. **Both outcomes are informative and only one is bad:** a claim means the twelve-hour jam is genuinely cleared; a bounce with a reason means Batch 1 has a defect its coordination block did not anticipate, which is worth knowing immediately. **Silence — neither claimed nor bounced — is the failure case**, and it would mean the shelf's top item is once again being stepped past for reasons nothing records.

  **Corrected within minutes of being written, and the correction is the more useful artefact.** The 08:00:53Z pickup claimed **THR-837** (In Dev 08:02:44Z) — the High — leaving THR-838 neither claimed nor bounced. **As worded, the test now reads as its own failure case, and that reading would be wrong**: WIP=1 and THR-837 outranks THR-838, so the encounter batch is waiting its turn for exactly the reason the queue is supposed to make it wait. **The test failed to state the premise it depended on** — that the slot would be free — which is finding 18's trap arriving inside a test written to honour finding 18, about ninety minutes after writing it. **Amended test, with the premise made explicit: by the 09:53Z run, either THR-838 is claimed or bounced, or a higher-priority issue holds the single WIP slot.** The failure case is now only the genuine one: the slot is free, the batch is top of the shelf, and nothing has touched or refused it.

  **Status at 09:00Z: not due, premise limb currently satisfied.** THR-837 holds the single WIP slot (claimed 08:21:59Z, still In Dev), so the benign branch obtains right now. **Recorded as not-yet-due rather than as an early pass** — the deadline is 09:53Z, and declaring a pass 53 minutes early is the same renegotiation finding 19's third leg forbids, merely in the flattering direction. The next run reports it as it falls.
- **NEW WATCH — THR-835 sits at Medium in `Idea`, and it is the durable fix for a repo-wide merge hazard.** The sole required check runs at 80–90% of its own 20-minute cap (suite 737 → 909 test files since the cap was set), so runner variance turns correct PRs red; it already cost PR #992 a red streak and an hour of misdirected diagnosis. **Deliberately not escalated and deliberately not carried as a complaint:** `Idea` is unreachable by every lane, which is finding 19's exact pathology, *but* the promotion lane that was down for eleven hours has now promoted two issues in two slots. **The owner exists and has just demonstrated it works.** The watch is cheap and the reading is binary: if THR-835 is still in `Idea` after several more orchestrator slots, the drift is structural rather than incidental, and that is the run to say so.
- **THR-837's present tense is stale and its body is sound.** Filed 4m36s after the lane it declares dead had posted work. **Deliberately not filed as a correction and not closed** — eleven missed slots are real and unexplained, the ticket has an owner, and a pickup that re-derives before acting (finding 1) will see the resumption in one call. Recorded here so the next reader of this file does not spend the diagnosis twice.
- **The `rescue/2026-07-17-detached-plans` branch is provably prunable.** Four unique commits, all five of their blobs byte-identical to `origin/main`. Deleting it is a one-line `git branch -D` that this read-mostly task deliberately does not run; it wants a session with a write remit, or the next full retro. Zero risk either way: nothing is stranded on it.
- **Friday's retro draft** (`Design/retros/retro-2026-07-24-draft.md`) is still untracked and still wants a `git add` inside any docs PR — but it is **not** a carried observation here. It is a deterministic regenerable draft, and it is **THR-798's step 2**, with an owner and a Done-when.
- **MOVED TO CHRISTIAN — the home-tree tracked-dirt item, after the mechanism it named finally fired.** For days this line read *"inert and non-blocking, but tracked modifications are the class that stalls autosync if it ever needs more than a fast-forward."* It now needs more than a fast-forward: `Docs/ops/orchestrator-2026-07-28.md` is locally modified **and** carried in the incoming commits, so `git merge --ff-only` aborts and the behind-count is now 10. **The `.claude/settings*.json` edits are still inert**, are demonstrably not part of the blockage (`comm -12` returns one path), and are now explicitly **excluded** from the repair command rather than swept along by it. See the top of this file and the briefing — it is Christian's only because every automated owner is either blocked or forbidden.

- **NEW — `tb-orchestrator` wrote its 08:29Z report into the home tree as well as into its own worktree, and that stray copy is the thing blocking autosync.** Mtime 10:29 local matches the slot exactly; the content is that run's report; the same content was delivered correctly through PR #1002. **This is impediment #267's `sandbox` row — worktree `-C` resolving relative paths against the home tree — arriving as a real outage rather than a note**, and it is a THR-672 violation by a lane, not by Christian. **Agent-owned and routed, not escalated:** the durable fix is in the orchestrator lane's write path (absolute paths, or `rev-parse --show-toplevel` asserted before any write — the workaround this task already takes every hour). Worth a ticket from a session with a write remit; this read-mostly lane files nothing (finding 23).
- **NEW — THR-655 sits in Ready for Dev with an assignee, and `pull-work` queries `assignee:null`, so the pickup lane will step past it silently.** It is the only one of 23 ready rows carrying one. **Finding 22 at the field level**: column, priority and `blockedBy` all read healthy, and the single field that decides visibility is not one this task's queue verdict looks at. One `save_issue(assignee:null)` from any session with a write remit clears it. **Reported in the briefing's Queue section as crew business, not escalated** — it is mechanical, and the two-axis test (finding 22) fails its *decidable-by-Christian* limb outright.
- **Impediment #267 is used twice** — a `sandbox` row (worktree `-C` resolving relative paths against the home tree, `daily-backlog-grooming`) and a `tooling` row (the CI timeout, THR-804 resume pass), filed the same day by different lanes. **No ticket; fold at the next retro**, same disposition as #219/#228 below. Flagged because a duplicate number silently breaks any consumer keyed on it — which is the same class of defect THR-764 exists to fix one layer up.
- **Impediments #219 and #228 are duplicate rows describing one defect** (Playwright MCP writing a bare-filename screenshot outside the repo). Both carry the same working workaround; the correct disposition is to fold them into one row at the next retro. **No ticket.** Settled — kept here as a one-line disposition, not re-argued.

## Durable findings carried forward

Rules earned by tickets that have since shipped. Kept as rules; the ticket-by-ticket prose is in `git log`.

1. **Before carrying an observation into its Nth run, re-derive it once.** **Four instances inside 48 hours**: THR-804 (thirteen runs restated a one-line cause from memory; filing it found a *second* defect the restatements had missed), the retro-draft carry (twenty-two runs described a file's significance without opening it; one `head -30` falsified both halves), standing item 1 — carried for **nine days** on a filename that has never existed on any branch, describing as "stranded" a document already on `origin/main` — and the impediment #228 carry, withdrawn after one `Read` showed the item was double-logged, workaround-covered, and not the class it had been assigned to. Every carry restated the item; none opened it. **Note the failure mode is not laziness but plausibility**: each restatement was internally coherent, and coherence is exactly what stops a reader from checking. **The fourth instance adds the sharper lesson: escalation is itself a symptom.** Three runs did not merely repeat #228, they progressively *raised* it — "carried" → "occurrence 3" → "has earned its filing" — and the rising urgency came entirely from repetition, since no new evidence arrived at any step. **A carried item that is getting louder without getting newer is the strongest available signal to re-derive it**, and it is a cheaper trigger than a schedule because it fires exactly when the cost of being wrong is about to be paid. **The counter-example is also on the record, and it has now run to completion.** THR-804's post-filing datapoint was *predicted* three runs running, left un-restated for fourteen, and **paid out on 2026-07-28 at 06:11Z when the ticket was finally claimed** — at which point it got a fresh reading rather than an inherited one. Waiting on a named future measurement is not a carry; the distinguishing test is whether new evidence is due, not how many runs the item has survived. **The full cycle is the proof:** fourteen runs of deliberate silence cost nothing, and the one run where the measurement mattered was not contaminated by fourteen restatements of a number nobody had re-checked.
2. **A subsystem with no callers is invisible to every sweep that looks for wrong behaviour, because it has none.** Eleven instances in four days: THR-779 (61 templates, no draw path), THR-787 (three engine reads, wrong field name), THR-803 (whole chain subsystem, zero production callers), THR-800 (62 refs, no definition), THR-783 (five authoring sites, no reader), THR-784 (four UI consumers, no writer), THR-811 (five gates, one lookup that could not see their operands), THR-809 (eleven of thirteen definitions absent from every world ever generated), THR-812 (the first found *prospectively*, by the habit the earlier ones produced), and now **THR-814 — ~150 authored faction templates against 227 memberships, drawn 3 times in 200 ticks and 0 times by a member.** Every one was authored or implemented capability that nothing routed to; every one failed silently; every one became visible only when something measured the producing side against the consuming side. **The class detector, not the individual repair, is the deliverable** — the repairs ran 28–82 minutes each once found. **THR-814 was the first whose repair was a design choice rather than a repoint, and it closed via THR-815 at 05:54Z on 2026-07-27** — three tickets and ~4½ hours from "the apex rank looks mis-tuned" to a measured routing change. **That the repair took a successor ticket is the finding's cost curve, not a failure**: a repoint fits inside the discovering ticket, a routing decision does not, and trying to ride one along would have shipped one of the six void candidates.

   **THR-814 sharpens the detector by showing what a partial one misses.** THR-779 measured "does this template have a draw path?" and cleared the faction pool, because a path exists — three draws prove it. What it did not measure is *who* the path delivers to, and the whole value of guild content is that it reaches guild members. **The census that found it counts draws by eligible-consumer class, not draws in total**; a reachability sweep that stops at non-zero is a sweep that agrees with the bug (finding 5's shape, one axis over). **Its repair (2026-07-27, PR #934) then found the sharpest variant yet: the content was not merely unrouted, it was unrouted at a stage nobody had thought to measure.** 817 entries cleared every eligibility filter and 0 survived the cap — because the cap walks a ~4,650-entry list from the head and the per-agent generators append at the tail. **Position in a list is an eligibility gate that no eligibility test will ever find**, and the tell was that the survivors ranked *first* on 40 of 56 boards the moment they were made visible.

   **THR-811 is the variant worth naming separately: the callers existed and ran.** What was missing was resolution — every gate read its field off `undefined` and passed. A gate that cannot find its subject is indistinguishable from a gate that approves, and neither the type system nor the suite can tell them apart.

   **THR-783 → THR-809 is the corollary's proof in two acts: wiring a dead field proves the wire, not the effect.** THR-783 shipped correct, tested and green, and the wound still never landed. Only running the world and looking for the outcome distinguishes "connected" from "working" — and here they differed by three tickets and a day. Treat a green suite on a newly-wired path as evidence the code executes, never as evidence the thing happens.

   **THR-784 adds the structural one, and it is the reason this class keeps recurring: grep the *assignment* side, not the read side.** Every instance above was found by noticing a reader that got nothing; every one could have been found faster by asking "who writes this?" first. `totalTicks` is the purest case: declared `?: number`, read by four production components, **written by nobody, ever**. No gate can catch it — `undefined` is a legal inhabitant of an optional type, the consumers guard on `!= null`, and the rendered result is visually identical to the legitimate empty case. **An optional field with no writer is invisible to every automated gate simultaneously**, which is precisely why it survives longest. **THR-812 closes the loop on this one: it was found by running the habit deliberately during another ticket, and the shipped fix confirmed the prediction to the letter.** Four consecutive tickets have now found their real defect from the writing side rather than the reading side.
3. **A plan doc's past tense is a claim, not evidence.** THR-801 existed only because the WS0 plan recorded a type change as already made; it had not been. The disconfirming check was a two-second grep that nothing in the pipeline had run for three days.
4. **Advisory gates that nobody reads are indistinguishable from gates that do not exist.** Both of THR-788's deferrals (THR-806, THR-807) surfaced from running the advisory gates at closeout and reading the output. THR-807's own framing is that the only thing noticing 61 stale plans was advisory.
5. **A test suite that agrees with the bug stays green forever — and a pool-scoped guard is a suite that agrees with the bug one shelf over.** THR-787's dead read survived because nothing asserted the observable outcome, only that the code ran. Trait regression tests must anchor on **shipped definitions**, never on fixtures written alongside the code under test (THR-809: a fixture that inserts the definition under test passes against a world where it does not exist). **THR-812 adds the scope dimension:** a guard existed for exactly this defect and was green throughout, because it enumerated one of three pools. When writing a guard over a set, assert over **every** member set and assert each is non-empty, or a rename silently empties the loop.
6. **Read composition, not depth — and stop re-testing the favourable dip.** A queue that rises after a valuable hour is not decay if the arrivals are findings rather than regressions. Eighteen consecutive hours now read 21 → 21 → 20 → 21 → 22 → 22 → 23 → 23 → 23 → 22 → 23 → 22 → 23 → 22 → 21 → 20 → 20 → 20, across which the most valuable work of the week shipped. **The wobble is the whole lesson in miniature, and it is settled rather than open**: a favourable dip has been called "one datapoint, not a trend" on three separate runs and survived **exactly one hour on all three**. The three-hour −1/0 run ended this hour at −1/+1, and that is recorded flatly — declining to call the dip a turn obliges declining to call its end a reversal, or the finding is just a mood. **The steady state is the signal**: at ~1 ship/hour and ~1–2 filings/hour the depth is stable by construction, and the filings are self-generated findings, not requests. A "backed up" verdict from `QUEUE_BACKED_UP_MIN` is therefore a threshold artefact here, not an alarm — report the composition alongside it every time, or the number lies. **The previous hour was the cleanest demonstration of that**: the depth did not move at all, while the shelf gained its first High in five hours. Depth reported alone would have said "nothing happened".

   **2026-07-27 10:54Z adds the first non-steady-state hour in eighteen: 23 → 22, a ship with no filing under it.** It is logged as one datapoint and explicitly **not** as a turn, which is this finding's own rule applied against its own preferred direction — the favourable dip has been called and survived exactly one hour on three prior occasions, and a rule that only binds when the news is bad is not a rule. The composition is what to read: a 48-minute clean close on the top-priority job, not an arrival drought.
7. **A gate clearing is not the same as a decision arriving.** Surfacing a settled call to Christian for ratification is worse than not surfacing it. This is what has kept twenty-seven consecutive empty Needs-Christian hours honest rather than merely quiet. Worked example: THR-805's guild-rank question looked like two design *feels* and turned out to be a purely mechanical selection whose answer was already determined — retired on examination rather than escalated.
8. **An empty Discord inbox is not evidence Christian is away.** He acted on the deploy block at ~07:39Z on 2026-07-25 without writing there. Silence means only that he did not write.
9. **A startup-failure cluster is indistinguishable from quota exhaustion at the moment it happens.** The disambiguating evidence — *does a fresh run execute jobs?* — costs one run. Take it before escalating to a billing page.
10. **Deploy-observability must classify by paths, not by branch name or PR title. — SHIPPED 2026-07-27 in THR-785.** THR-788 was a `docs/`-branched, docs-titled PR whose build ran anyway, because it touched `public/`. Three prior observations fit a docs-vs-code shorthand; the fourth falsified it. **The finding was carried into the ticket rather than left in this margin, and the implementation improved on it**: the probe parses the path list out of `vercel.json`'s own `ignoreCommand` instead of duplicating it, so there is no second list to drift. Kept as a rule because the generalisation outlives the ticket — *when a checker must agree with another system's decision rule, read that system's rule; do not copy it.*
11. **Mutex lines with reasons work.** THR-800 declared `Mutex with: THR-788 (settle the vocabulary before mass-rewriting refs to it)`. The pickup lane claimed it at 12:02, 14:02, 15:02 and 17:02 and released it within 30–80 seconds each time, taking other work instead — then built it in 38 minutes at 18:02, once THR-788 had merged at 17:45. **Four unprompted correct deferrals from one authored line.** This is the strongest evidence to date for THR-688 ticket-authoring rule (B); some of the earlier "0 In Dev, nothing claimed" readings in this file were partly *this*, working correctly, being read as idleness.
12. **An unanswered ask is not an unheard ask — do not re-ring the doorbell.** A standing ask carries a stated safe default, so silence resolves it rather than blocking on it. Re-pinging converts a one-question channel into an hourly nag and spends the channel's credibility on the one ask it least needs to force. **Judge the item, then let the hash follow** — the question is "is this the same ask?", never "does the string match?". The step-6 gate now enforces this mechanically as well, because the hash is taken over a **stable item key** (`THR-799:priority-ordering`) rather than over prose this task rewrites every hour; keyed on regenerated prose it fired on every rewording, which was the exact nagging it exists to prevent. **The judgement call remains primary and the hash confirms it, never the reverse** — a gate that only ever agrees has not been tested. A genuinely *new* fact about an open ask belongs in the briefing where he will see it next time, not in a second ping.
13. **Declining to build the missing half is a shippable outcome, and it needs its reasoning recorded to survive.** THR-812's ticket offered per-instance `$`-ref binding as scope option 2; the executor checked the producing side first, found `REACTIVE_AMBITION_TEMPLATES` has **no assignment path anywhere in the engine**, and refused to build a binding channel for a pool nothing mints from — naming THR-614's green-field failure as the precedent. The dead refs were repointed instead. **The general rule is finding 2 turned around: before extending a mechanism to reach a consumer, confirm the consumer has a producer at all.** A capability wired to a pool with no source is the same defect as a read with no writer, arriving one layer earlier and costing far more to build.
14. **A signal with two meanings is not a signal — and the repair is a notification path, never a new gate.** Vercel reports "published" and "build skipped, nothing needed publishing" with the identical green `success`. Measured on `a9c33078`: `success` on a commit with **no deployment record at all**. That is finding 2's pathology one layer up — a check that cannot distinguish its two cases is indistinguishable from a check that always passes — but the repair differs, and the difference is the lesson. **The tempting fix was to promote the Vercel check to required**; it was refused, correctly. It is not a correctness gate (`Test · Typecheck · Build` already proves the build compiles), and requiring it would couple every merge to a third party's availability. The right move for an ambiguous non-required signal is to **disambiguate it out of band** — ask the authoritative API which version is actually live, classify honestly, and route only the bad verdicts to a human. Generalises past deploys: when a signal is both unreliable and load-bearing, add an observer, not an obstacle.

    **Second instance, 2026-07-27, and it is our own gate rather than a vendor's — THR-819.** `npm run check:wiki-freshness` **never fetches**, so it diffs changed files against whatever `origin/main` the local repo last saw and reports **PASS on precisely the condition CI fails**. It cost THR-802 a two-hour red streak: the author ran the gate, was told they were clean, and was then blocked by the blocking copy of the same check. **The sharpening is that the ambiguity was invisible because it was asymmetric** — the local and CI runs share a name, a script, and an exit code, and differ only in a fetch nobody thought of as part of the check. A gate whose baseline is stale-by-default is not a weaker gate; it is a gate that reports the wrong answer in the reassuring direction, which is the one direction nobody re-checks.
15. **When a ticket asks "which of these fixes?", census the pipeline before answering — the honest answer may be "none, and here is why".** THR-810 proposed three levers; THR-814 proposed three routing rules; **all six were void for the identical reason, and one stage-by-stage census retired the lot in forty minutes.** Each candidate operated on a stage the content demonstrably never reached, so any of them could have been built, tested, shipped green, and changed nothing. **The general rule: a menu of candidate fixes is itself an untested assumption — that the defect lives at the stage the menu operates on.** Take the census first; it is cheap (one instrumented run of the real pipeline), it is reusable (`sweep:rank-reach` now prints eligibility every run), and it is the only thing that can *falsify* the menu rather than merely choose from it. **This is finding 13 one step earlier:** 13 says confirm the consumer has a producer before extending a mechanism to reach it; 15 says confirm the defect is where the ticket says it is before choosing between repairs. Two consecutive tickets have now been falsified by their own commissioned measurement, which is the strongest available argument for building the measurement first.

    **Third consecutive falsification, 2026-07-27 — THR-821 — and it moves the rule one layer further back: validate the *sample*, not just the stage.** THR-810 and THR-814 were wrong about *where* the defect lived. THR-821 was wrong about *who it was measured on*: seven agents all read `Doomed`, and all seven were `ambient`-tier NPCs carrying no capability data and structurally unable to reach the nudge stage at all. **Both offered causes — "the content is too brutal", "mortals are too feeble" — were analyses of a reading that a correct system had produced for a population the feature does not apply to.** Either could have been built. The content fix would have gentled an exemplar that measurement shows is fine (98.2% clearing unaided among agents who can actually be attended); the tuning fix would have moved a floor the entire resolution ladder is calibrated against, to serve agents who never reach the stage.
    **The generalisation: a bug report is a measurement, and it carries an unstated premise that its subjects were eligible to exhibit the behaviour.** That premise is invisible in the report, because an ineligible subject returns a *well-formed* answer — `p=0.050` is a real number, honestly computed, and it looks exactly like a defect. **This is finding 2's pathology inverted:** finding 2 is a probe that matches nothing and reports success; this is a probe that matches the wrong things and reports failure. Both are unfalsifiable from inside the report, and both are cheap to catch with the same question — *what population is this, and can it exhibit the thing I am measuring?*
    **The payoff is not merely avoiding the wrong fix.** Widening the measurement past the reported sample is what found the real defect: the `notable` tier, which nobody had reported, does floor and stay floored. **A census run only over the complainants would have cleared the system and shipped nothing.**
16. **A deliberately-parked issue and a broken auto-close are indistinguishable from the board.** THR-814 sat In Dev with its work merged and live; THR-810 went In Dev → Done in 20 seconds at merge. The difference was legible only in the PR body, which said the keyword was withheld on purpose because 3 of 4 Done-whens carried to a successor. **Record the reason at the park, not just the omission** — otherwise the next reader's cheapest hypothesis is the THR-738 keyword defect, and they will "repair" a correct decision. Corollary for this file: when a park is correct, **name it as correct in writing, with its successor ticket, and name the run at which it stops being correct.**

   **The named run arrived, and the prediction attached to it was wrong in the useful direction.** This file predicted that a correctly-parked issue would read to the next pickup as an occupied bench and cause a stand-down. It did not: the lane claimed the successor at 05:02Z with the park still standing, and shipped it in 52 minutes. **A stale In-Dev row is a legibility cost, not a throughput cost** — at least while the lane routes by Ready-for-Dev rather than by bench occupancy. **The transferable half is procedural, not factual: a carry that states a falsifiable test must report the result when the test fires, including "the harm I predicted did not happen".** Finding 1 catches carries that get louder without getting newer; this is its mirror — a carry that got *quieter* and would have been cheapest to simply delete. Deleting it would have preserved the file's tidiness and destroyed the only evidence that the WIP-slot worry is unfounded here.

   **Falsified a second time, which settles it.** The lane claimed THR-816 at 05:18Z and shipped it at 06:52Z with THR-814 still parked In Dev. Two consecutive claims straight through a standing park is enough to stop treating it as a live risk on this lane and start treating it as a tidiness item. The carry now costs one flat sentence per run, which is the right price for it.

   **Closed 2026-07-27 07:19:22Z, unaided.** The park resolved itself without anyone acting on this note, which retires the item and completes the record: correct park → named as correct → predicted harm falsified twice → closed on its own. **The transferable half is the pricing, not the outcome:** a carry whose stated cost is one sentence and whose payoff is a falsifiable prediction is worth keeping to term; a carry that restates a diagnosis from memory (finding 1) is not. The difference is whether new evidence is due, and here it was — twice.

17. **Before carrying an ask into another run, check whether it was answered somewhere other than the channel it was asked on.** The THR-799 priority ask was pinged to Discord once and then carried unanswered for **nine runs**, with each run dutifully re-reading an empty Discord inbox and concluding "no answer". **The answer had been recorded in a Linear ticket description that morning** — *"User priority (Christian, 2026-07-27): the new encounter experience is still first priority"* — written by a design session that had no idea an ask was waiting on it. **Finding 8 says an empty inbox is not evidence he is away; this is its sharper corollary — an empty inbox is not evidence he has not answered.** The check is cheap and specific: an ask about *priority* is answered by any dated statement of priority, wherever it lands, so before the Nth carry, grep the surfaces where his words get recorded (ticket descriptions, plan docs, chat-session notes), not only the channel the question went out on. **This also completes finding 1 rather than merely resembling it:** finding 1 says re-derive the *claim* before carrying it; 17 says re-derive the *premise that it is still open*. Nine runs re-verified the former and never once tested the latter. **The structural cause is worth naming: this task reads Discord because that is the channel it can write to, and it mistook its own outbound channel for the boundary of his voice.**

18. **State a falsifiable test against the outcome, never against an activity proxy — a proxy-worded test can pass while the thing it guards fails.** Both tests standing at the 08:53Z run fired, and **both were satisfiable without their outcome occurring**. Test A asked whether the next pickup would *claim a Low over the Highs*; the pickup claimed nothing (WIP=1, slot already occupied), so a real condition — the top-priority work sitting unstarted 2h23m — sailed past a test written to catch exactly it. Test B asked whether the stuck PR was *still red with no new commit*; a `main`-merge commit landed, satisfying the escape clause while doing nothing about the failure, which re-failed identically 14 minutes later. **The two failures are the same failure**: "did the lane claim" and "did the branch move" are cheap and visible, while "did the priority work start" and "is anyone fixing the gate" are the questions that matter, and each proxy stood in for its question without tracking it.

    **This is finding 5 one level up.** Finding 5 says a test suite that agrees with the bug stays green forever; 18 says a *prediction* can agree with the stall the same way, and it is more dangerous, because a green suite at least invites suspicion while a passing self-set test reads as vindication. **The tell is available at authoring time and costs one question: can this test pass while the situation I am worried about persists?** For both tests here the answer was yes, and obviously so, on re-reading. **It also sharpens finding 16's procedural half** — 16 obliges reporting a test's result in both directions; 18 adds that *reporting a pass is worthless if the test could not have failed for the right reason*. Restating the test correctly is part of reporting it, and the restatement belongs in the same run, not the next one.

19. **A column-scoped queue query reports the lane's reach, not the board's contents — and from inside the query the difference is invisible.** Seven consecutive runs have reported Ready-for-Dev composition as if it were the state of the work. This run it was materially wrong in the direction that matters: *"no High anywhere on the board"* was written last hour and repeated as a headline, while **THR-821 was filed at High and sat in `Idea`**, unclaimable, one hour old. The query was accurate and the sentence it produced was false, because the query's scope had quietly become the sentence's scope.

    **The mechanism generalises past this board, and it is finding 2 wearing process clothes.** Finding 2 says a subsystem with no callers is invisible to every sweep looking for wrong behaviour, because it has none. Here, **an issue in a column no lane reads is invisible to every measurement of throughput, because it never moves** — it cannot go stale (nothing ages it), it cannot block (it declares no blockers), and it cannot starve the queue (the queue it is absent from looks healthy). Four such issues, including the top-priority one, produced a board that measured well on every axis this task reports. **The detector is to count the work, then count what can reach the lane, and treat the gap as the reading** — depth alone cannot see a gap that lives outside its own filter.

    **The sharper half is that the un-releasedness is self-perpetuating and nothing in the system is assigned to notice it.** Verified this run: no scheduled task can move an issue out of `Todo`, so authored program work waits on a human-started session while executor-filed deferrals enter directly at the shelf. **A backlog fed by one route and drained by another will drift in composition indefinitely without any single step being wrong** — which is exactly why it survived twenty-eight consecutive "backed up" readings without once being called a defect.

    **Closed 2026-07-27 13:54Z by escalation rather than by repair, and the mechanism is the transferable part.** The re-derivation obliged by finding 1 was run against the *automated* half of the diagnosis rather than the item itself, and it found the missing piece: `daily-backlog-grooming`'s pipeline-gap rule fires only when Ready-for-Dev **and** In-Dev are both empty, so **a shelf full of Low-priority deferrals is indistinguishable from a healthy shelf to the only check that could object**. That is finding 2 at the process layer once more — the check has no wrong behaviour to exhibit, because the condition it tests is unreachable while the drift is happening. **The general rule: when a metric's healthy range is wide enough to contain the failure, the metric cannot be the detector, and the fix is a second reading (composition) rather than a tighter threshold on the first (depth).**

    **The procedural half completes findings 16 and 18.** 16 obliges reporting a falsifiable test in both directions; 18 obliges wording it against the outcome rather than an activity proxy. **This adds the third leg: a test whose consequence is stated in advance must be allowed to pay it, on time, without renegotiation.** The temptation at the firing moment was to restate the diagnosis for a third hour with a fresh deadline attached — which would have been indistinguishable from finding 1's escalating carry, and would have spent the item's credibility while claiming rigour. **A deadline that slips once is a deadline that never binds**, and the whole value of pre-committing the consequence is that the decision was made at a moment when the answer was not yet known.

    **The successor deadline dissolved rather than fired (2026-07-28 07:40Z), and the honest report is that this rule did not get tested.** Eleven runs refused to move an 08:53Z deadline for the orchestrator stall; the lane returned unaided at 07:40, one brief early, so the consequence was never paid and the discipline never priced. **Recorded because reporting only the deadlines that fire is how this leg would quietly become decorative** — the run that would have proved the rule is exactly the run that did not happen, and a rule whose evidence is entirely favourable outcomes it did not cause is worth less than it looks.

20. **When an ask names a *mechanism* rather than a decision, expect the answer to arrive on the channel he has, not the one the mechanism needs — and say so instead of letting him assume it landed.** The 13:54Z ask was worded *"next time you open a chat session, say X"*, because a chat session can write Linear and this task cannot. He answered 44 minutes later by saying X **on Discord**, which is exactly what a reasonable person does when handed a sentence and a phone. **The decision half discharged perfectly; the mechanism half did not move at all**, and nothing about the reply's wording would tell him that.

    **The failure this avoids is silent and expensive: a satisfied-looking ask that shipped nothing.** Both parties would have been correct — he said the sentence he was asked for, this task recorded it faithfully — and the four issues would still be sitting in `Idea` and `Todo` tomorrow, with the ask closed and no one looking. That is finding 2's shape wearing process clothes for the third time (cf. finding 19): a step with no consumer, failing silently, invisible to every check because nothing exhibits wrong behaviour.

    **Two rules follow.** (a) **Prefer asks that are pure decisions** — *"is this still first priority?"* survives arriving on any channel, while *"go and file these"* only works on one. (b) **When a mechanism-shaped ask is unavoidable, the receipt must state what did and did not happen**, in the same message, unprompted: *your sentence is recorded authority, not a filed action, and here is the shortest path to the filing.* The receipt is the only place this can be said, because it is the only surface he is already looking at. **Corollary to finding 17:** 17 says check whether an ask was answered somewhere other than where it was asked; 20 says check whether being answered was *enough*.

21. **Re-derive a carry even when you expect it to hold — the check has a wider aperture than the claim, so a *true* carry can still pay.** Finding 1 justifies re-derivation by the risk of being wrong, and every instance behind it was an item that turned out false. **This run supplies the missing half.** "Nothing blocked" had been carried for many runs; opening THR-778's `blockedBy` confirmed it exactly — all three Done — and in the same two calls surfaced something no restatement could have: the top item has been **startable for 10h35m** while the work slot runs a lower-value batch its own ticket authorises to lag. **The claim was never wrong; it was narrow.** A carried assertion answers only the question it was originally phrased to answer, and phrasing is where the loss happens — *"nothing blocked"* is a fact about edges, and the thing worth knowing was about elapsed time on the node those edges point at.

    **The operational form is cheap: when re-deriving, look at what the source shows you, not only at the field you came to check.** Both facts came from one `get_issue` call. **This is also why finding 1's cost model needed correcting** — it prices re-derivation as insurance against error, which makes a carry that keeps holding look like wasted motion and quietly trains the file to stop checking the reliable ones. Price it instead as a *fresh read of a live surface*, and the confirmations are worth as much as the falsifications. **Corollary to finding 19:** 19 says a column-scoped query reports the lane's reach rather than the board's contents; 21 says a carry-scoped restatement reports the phrasing's reach rather than the situation's.

22. **An item in the lane's column is not an item the lane can take — and "unblocked" is the wrong question to keep asking.** THR-778 sat at the head of Ready-for-Dev for **11h41m**, was reported *startable* by eleven consecutive runs with an escalating elapsed-time figure, and was **never executable at any point in that window**. It had no coordination block (zero comments, which `pull-work` Step 3 refuses on) and it was a container whose promised batch children were never created — an instruction to rebuild every encounter in the game, with no Done-when an hourly run could satisfy. **The board could not show any of this.** Column, priority, assignee and `blockedBy` were all exactly as a healthy top-of-queue item's would be.

    **This is finding 19 one layer in, and the sharper layer.** 19 says a column-scoped query reports the lane's *reach* rather than the board's contents — items in `Todo` are invisible because no lane reads that column. **22 says the same blindness exists *inside* the column the lane does read**: readiness is a property of the ticket's *content* (scope, Done-when, coordination block), and every field this task queries is metadata. **A ticket can satisfy every queryable precondition and still be un-startable**, and from the query's side that state is indistinguishable from "waiting its turn."

    **The detector is cheap and this run found it by accident: read the comments, not just the fields.** The executor's bounce comment existed 51 minutes before this run and contained the entire diagnosis. **The generalisable form is that a lane's own refusal is the highest-quality readiness signal available, and it is written down** — a claim-and-release inside a minute is a lane saying *this cannot be worked*, in a place no status query looks. Before reporting an item as startable for the Nth hour, check whether anyone has already tried and said why not. **Corollary to finding 21:** 21 says a carry-scoped restatement reports the phrasing's reach rather than the situation's; 22 supplies the worked example where the phrasing (*"is it blocked?"*) was re-derived correctly four times and was the wrong question every time.

    **The second half is about escalation discipline, and it cuts the other way.** This was the strongest pull toward the Needs-Christian button in ten runs — it stalls the programme he named first priority. **It was still correctly withheld** (finding 7): the remedy is ticket-shaping, the owner is named in the bounce comment, and a pre-committed deadline already governs. *Consequential* and *decidable by Christian* are independent axes, and conflating them is how a plain-language inbox becomes a firehose.

    **Third instance, 2026-07-28, and the first arriving from another lane rather than from this file — which is what promotes it from a habit to a rule.** `daily-backlog-grooming` filed THR-837 at 07:23:58Z declaring `tb-orchestrator` dead since 20:27Z, **4m36s after that lane had posted its THR-618 promotion comment**. The filer read `lastRunAt`; the lane's output was public, timestamped, and four minutes old. **Both halves of finding 22 fire at once:** the status field satisfied every queryable precondition for "stalled", and the disconfirming evidence lived where no status query looks. **The new half is the disposition, and it is the part worth carrying: a ticket whose *present tense* has expired is not thereby void.** Eleven missed slots are a real defect and the body documents them properly; only the headline is stale. **The wrong repairs are both tempting and both destructive** — closing it discards a genuine unexplained outage, and "correcting" it spends a second lane's run rewriting prose whose owner will re-derive it anyway. **The right move is to record the staleness where the next reader already looks and leave the ticket alone**, which costs one sentence and pre-empts the whole diagnosis being paid for twice.

23. **A ticket this task files is unpullable by construction, and the cost lands on someone else's run — so filing is never free.** THR-804 was filed by `keep-work-flowing-cc` on 2026-07-26 and claimed on 2026-07-28 **only via a documented `pull-work` Step 3 reversal**: Step 3 refuses any candidate whose latest comment carries no coordination block, and a ticket filed by this task has *zero comments* by construction — it never passes through a design session, and Cowork, which used to author handoffs, was retired 2026-07-21 (THR-654). **The executor's reasoning for reversing is the part worth keeping:** bouncing would have been a *permanent* no-op, because nothing exists that could ever author the missing block, so the lane would idle in front of the ticket once an hour forever. It reversed on Rule B's own terms — scope known, mutex vacuous at WIP=1, Done-when reachable through the pillar touched — and recorded it.

    **This is the same defect as THR-778's, arriving from the opposite direction, and that symmetry is the finding.** THR-778 is unpullable because a *design* session promised children and never made them; THR-804 was unpullable because a *read-mostly* session filed a ticket it has no mechanism to hand off. **Both produce a ticket that satisfies every queryable precondition and stops a lane cold** (finding 22), and neither is visible from the board. **The operative rule for this file: filing is still correct when the alternative is a defect restated forever** — the 2026-07-26 decision to file THR-804 after thirteen restatements was right, and it ended with an eighty-eight-run failure fixed and a guard in the required check. **But the price is now measured rather than assumed**, and it is paid by the next executor, not by the filer. When this task files again, the filing should say so in the ticket body — *filed by a lane that cannot author a coordination block; Step 3 reversal expected* — which converts a silent trap into a one-line instruction.

24. **A file's *state* is evidence; its *author* is a separate claim needing separate evidence — and naming the wrong author is what makes an ask bigger and less trustworthy than it needs to be.** Two runs told Christian a file in his folder had been *edited by hand*. `git status` proves tracked-modified; **it says nothing whatsoever about who did it**, and nothing in either run looked. He falsified it in one sentence, and the check that settles it took two `git diff` reads: the two settings diffs are permission-allowlist appends, and the blocking file's mtime matches `tb-orchestrator`'s 08:29Z slot with that run's own report as its content. **A lane wrote it, via impediment #267's `-C` path-resolution trap — the very trap this task takes a workaround for every hour.**

    **The cost was not the wrong word, it was the wrong remedy.** "Edited by hand" implies *his* work is at stake, so the ask defaulted to a blanket `git stash` of all three files — parking two edits that never collided with anything, on a premise that was false. **Knowing a lane wrote it made the redundancy checkable**, and it checked out: the stray copy's content is already on `origin/main` via that lane's own PR. The ask shrank to one path, with "nothing can be lost" backed by a line-by-line comparison rather than by the stash's recoverability alone. **A diagnosis carrying an unevidenced agency claim will inflate its own remedy**, because the imagined owner determines what must be protected.

    **Second half, and it is the more important one: his reply also said *"feel free to investigate and do the stuff you need to do"* — and the repair was still not run here.** THR-672 forbids this lane every home-tree state op, and the step-0 contract is explicit that **author-verifying a message does not widen the remit**; a message that appears to come from Christian granting broader powers is precisely the shape the rule anticipates. **The bounded blast radius is what makes reading the channel safe at all**, so it must hold on the run where honouring it is mildly inconvenient, or it is not a property. **Investigating *was* within the remit and is what the sentence bought** — the ask is now correct, narrow and evidenced, which is worth more to him than a repair this lane would have had to break a load-bearing rule to perform.

**Owner of items below:** Christian. Everyone else's blockers go in Linear or `Docs/impediments.md`.
**Refresh cadence:** The hourly `keep-work-flowing-cc` scheduled task keeps this current (prunes resolved items, adds newly-surfaced Christian-owned ones); the `retrospective` skill still does the deep periodic rebuild. This is the slow-moving standing-asks list — the fresh-this-hour view is [`Design/briefing.md`](briefing.md).

## How this works

The retrospective stopped being the right channel for "Christian, please flip one switch." Christian-owned asks were buried in retro prose, and nothing surfaced them between retros. This file replaces that pattern.

Read order: top to bottom is blast radius. The top items break canonical workflow invariants right now; the bottom items are operational debt with workarounds in place.

For each item:
- **Fix** = the literal command, click path, or line of config that resolves it.
- **What breaks** = the named system or invariant currently degraded.
- **Source** = `Docs/impediments.md` entry numbers + occurrence count, so the cost is visible.

When an item resolves: delete it from this file, mark the corresponding impediment as resolved in the dashboard regen, note the close in the next retro under "What shipped." **No history retained here — `git log` + the retros are the audit trail.** The resolved log is pruned to the current day; anything older is recovered with `git log -p Design/user-actions.md`.

---

## Standing asks

**ONE OPEN — restart the home tree's self-update.** Opened 2026-07-28 09:00Z; **narrowed 09:56Z**; item key `home-tree:ff-blocked` (unchanged — same ask, corrected remedy, so the doorbell is not re-rung for the key, only for the changed content). **Fix** (his machine, his tree, ~1 second):

```
cd C:\Users\chris\Dev\Projects\TheFantasyWorldSimulator
git stash push -m home-tree-recovery -- Docs/ops/orchestrator-2026-07-28.md
git pull --ff-only origin main
```

**What breaks:** `threadbare-autosync.ps1` **line 73** (`git merge --ff-only origin/main`) aborts every hour because `Docs/ops/orchestrator-2026-07-28.md` is tracked-modified locally *and* changed by the incoming commits; his sessions read progressively staler files, and the gap is now **10**. **Source:** the intersection is measured, not inferred — `comm -12` over `git diff --name-only HEAD` and `git diff --name-only HEAD origin/main` returns exactly that one path, so the two `.claude/settings*.json` edits are *not* part of the blockage and are deliberately left alone.

**Two corrections to the prior wording, both material.** (a) **The blanket `git stash` is withdrawn** in favour of the single-path form: parking his tool-permission edits was never necessary. (b) **`$REATTACH_MAX_TRACKED_DIRTY = 0` was the wrong citation** — it governs the detached-HEAD reattach path (script lines 36–52) and never evaluates while HEAD is on `main`. The conclusion is unchanged; its stated reason was wrong.

**Why it is his:** autosync is the owner and is the blocked party, the reaper never touches working copies, and THR-672 forbids every agent lane from home-tree state ops — **including on this run, where Christian's Discord reply invited the repair and the remit correctly did not stretch to cover it** (finding 24). **Nothing can be lost, and this is now verified rather than merely recoverable:** the stray file's content is already on `origin/main` (130 lines carrying the 08:29Z run's report verbatim, delivered by that lane's own PR #1002), the local copy is a 51-line duplicate, and `git stash pop` restores it regardless. **Not his fault, either** — a lane wrote it, through impediment #267's `-C` path-resolution trap.

**It resolves on the command, not on a reply** — this is an operational switch, not a question, so it should be pruned the moment the home tree fast-forwards, and not re-pinged in the meantime (finding 12).

**The prior ask of the last eight days is closed and must not be re-asked** — releasing the encounter-experience work, answered by Christian on Discord at 14:38:50Z on 2026-07-27, with its remaining filing action completed at 17:21Z.

**One standing switch exists but is not an ask:** the orchestrator lane can be disabled on one word from Christian (live 2026-07-27 19:49Z). **Its default state is the intended one**, so silence resolves it rather than blocking on it, and it must not be re-surfaced as an open question in later runs — the notice was delivered once, with a ping, and that discharges it (finding 12). A genuinely new fact about the lane belongs in the briefing where he will see it next; it does not re-open an ask.

**The instruction has now bound six times (20:54Z → 01:54Z), and the record is kept as a line rather than an argument.** The first application (20:54Z) cost a paragraph and established that **the judgement must lead and the hash may only confirm** — step 2.6 folded a genuinely non-empty section into a run that then correctly declined to publish it as an ask, and the mechanical gate agreed only because a prior run had keyed the hash on a stable item key rather than the prose. The third (22:56Z) is the one that earned the check its keep: the source file's mtime moved 19:50 → 21:50 and the cheap verification rejected it as a false positive — autosync's checkout touched the timestamp, `git log` shows one commit, content byte-identical. Confirmations four, five and six re-ran that check rather than inheriting it.

**The check is now retired by age rather than by argument, which is the cheapest possible ending.** `Docs/ops/orchestrator-2026-07-27.md` passed **`SIBLING_REPORT_MAX_AGE_HOURS` (36) at ~09:50Z** and is out of the step-2.6 fold for good; its successor `Docs/ops/orchestrator-2026-07-28.md` carries *"Nothing needs you this run"* — an empty state, correctly not propagated. **Seven runs at one line each, then expiry.** Full reasoning in `git log -p Design/user-actions.md`. **The rule it established outlives the file it was about**: judge the item, let the hash follow, and verify an mtime move against `git log` before believing it (finding 12).

**The lane's eleven-hour silence did not re-open the switch, and that is the rule working at its least comfortable.** A reasonable-sounding case existed for re-surfacing it — *the thing you were told about then stopped running* — and it is exactly the case finding 12 forbids: the notice's subject is whether Christian wants the lane **at all**, not whether it ran last night, and an operational fault is not a fresh reason to re-ask a settled question. **The lane resumed unaided at 07:40Z before any of this reached him**, which is the outcome the silence-resolves-it default is built for. The outage is reported in the briefing as news, where new facts belong; the switch stays a switch.

---

## Retracted this period

- **2026-07-25 15:16 — the "CI + deploy allowance exhausted" item, opened at 15:12 and retracted four minutes later. It was wrong**, and it never reached Christian (the retraction landed before the Discord ping fired). A cluster of Actions runs failing at startup with zero jobs executed looked like account-level exhaustion; it was a transient GitHub incident, and Actions resumed unaided by 13:15 UTC. **The lesson is finding 9 above, and it is what made a later, genuinely different call correct**: the cluster that began at 15:05 UTC that same day *was* a real account-payment block — GitHub's own annotation named it, and a fresh re-run reproduced the failure in 6 seconds. Two events, same signature, opposite causes; conflating them would destroy the lesson.

## Resolved 2026-07-28

- **09:05 — Christian answered on Discord and closed the *cause* half of the open ask, verbatim: *"I did not manually change anything anywhere. Feel free to investigate and do the stuff you need to do."*** **Verified rather than believed**, per step 0's operational-fact row — and he was right on every file. **The ask itself stays open** (the command still needs a human), but its diagnosis, its remedy and its blast radius all changed as a result; the *hand-edited* framing is withdrawn and must not reappear. **The second clause was honoured as written and not as an invitation to widen the remit** — investigation yes, home-tree state ops no (finding 24).

- **07:20 — THR-804 shipped and auto-closed (PR #992): the stale-claim sweep resolves its imports, and a gate now says so.** Eighty-eight consecutive failures since 13 June, caused by two extensionless relative imports; the fix is two lines and **the durable half is a lint in the required check** that walks every `--experimental-strip-types` entrypoint's reachable graph. Third instance of the identical defect; the first two were remedied with impediment notes nobody read.
- **07:19–07:44 — `tb-orchestrator` returned after eleven missed slots and discharged both stalled duties in one run**: THR-618 promoted (unblocked ~6 days, nobody had read the dependency field), and **THR-838 cut as WS5 Batch 1** — 48 `encounter.*` templates, ~91% of observed draws, born with the coordination block its parent container never had. `daily-backlog-grooming` then moved THR-778 to `Todo` with an explicit *do not re-promote as-is*.
- **08:27–08:28 — second consecutive on-time orchestrator slot, which is what makes the outage over rather than paused**, and THR-764 promoted into the top-priority gap seven minutes after THR-837 left it.

- **07:44 — `tb-orchestrator` returned after an eleven-slot silence and cleared the programme jam in one run.** T1 promoted **THR-618** (divine economic verbs + essence bridge) at 07:19:22Z, unblocked for ~6 days behind two tickets Done on 2026-07-22 and 2026-07-05. T2 cut **THR-838** at 07:44:24Z — WS5 Batch 1, the `encounter.*` REWRITE set, **48 templates covering ~91% of observed draws** — the first child of a container that had been refused by two lanes for fourteen hours.
  - **The split was done properly rather than merely done, and that is the whole repair.** THR-778 was unpullable for two reasons — zero comments, and no executor-sized Done-when — and **its first child was born with both**: coordination block naming model, parallel-safety and a mutex-with-reason against a not-yet-filed Batch 2, plus the evidence shape the Done-when needs (content-pillar, CLI/headless per THR-688 rule C, no browser-verify). **A split that reproduced the parent's defect would have looked identical on the board.**
  - **`daily-backlog-grooming` moved THR-778 itself to `Todo` at 07:20:52Z with an explicit *do not re-promote as-is*.** The container is unjammed by removal, not by promotion — the orchestrator skill's own §T1 step 4b calls leaving it on the shelf worse than leaving it in `Todo`. **Two lanes, no messages, coupled only by a Linear column and a comment thread.**
- **07:20 — THR-804 merged and auto-closed (PR #992), ending the stuck-gate carry exactly as predicted.** A fresh required check ran 07:06:41Z → 07:20:29Z **SUCCESS** where its predecessor was `cancelled` 20m17s in; auto-merge fired 3 seconds later; the issue reached Done at 07:20:56Z. **The Stale Claim Sweep — 88 runs, 88 failures, six weeks — now resolves its imports**, and ships with a lint in the required check that walks every `--experimental-strip-types` entrypoint so the third instance of this typo class cannot be followed by a fourth.
  - **The self-healing path was named in advance and is what actually fired**: THR-804 was In Dev and assigned, so `pull-work`'s resume clause put the 07:01:42Z pickup in front of its own unfinished PR. **The prediction was worded against the outcome (finding 18) and could have failed** — "the lane ran" was true at 07:01 while the PR was still blocked.
- **The no-High gap ended at twelve runs**, via THR-837 entering at High. **Recorded as composition drift rather than recovery** (finding 19): the board's only High is infrastructure about our own scheduled lanes, and nothing recognisable as game work sits above Medium.
- **05:45 — THR-799 shipped (PR #990), ~43m from the THR-778 bounce: reveals have a ceremony.** A third presentation tier between a toast and a full modal, for the moments a minor element enters a life — a trait revealed, a bond formed, a working learned. `Medallion` (layered-ring icon disc), `FlavorQuote` (inset quote well, renders `null` with no children so a missing prose field removes the zone), and `RevealCard` (the zone stack), **adopted into four surfaces in the same PR** — event popups, the ascendant beat, attachment details, and the trait section of a person's page.
  - **`RevealCard` is composed on the existing `Modal`, not forked**, and the only `Modal` change is an additive `panelClassName` so the ceremonial frame rides the existing panel. Likewise `Medallion` frames an already-resolved visual rather than introducing a second art-resolution path — **the discipline that keeps this from becoming the third parallel art map** (cf. THR-740, still open on exactly that defect).
  - **Building it exposed a missing half in its own plan, and the executor widened the rule rather than implementing it literally.** The plan said *sphere-carrying ⇒ ceremonial*; the shipped routing is sphere **and** no-choices, because an event offering a choice needs the choice surface, not a ceremony. **Finding 15's shape at implementation scale:** the ticket's stated condition was an untested assumption about where the distinction lived.
- **04:40 — THR-832 closed (PR #988, batch 4), and the encounter image library is complete: 125 rows, 0 slots.** Fifteen archetype portraits — *farmer, crafter, merchant, guard, soldier, sailor, labourer, healer, priest, scholar, noble, outlaw, beggar, elder, child* — finishing a worklist THR-777 opened across four batches (48 fate images + traveler baseline → 16 nudge concepts → 14 place plates → 15 faces). **Every encounter surface can now resolve a real image instead of the `EntityVisual` gradient.**
  - **The park ended by predicate, which is the cleanest possible vindication of recording a park's reason in the ticket description.** THR-832's Done-when was *"`ENCOUNTER_IMAGE_PLAN` is empty"*; it stayed In Dev across three merged batches and closed the moment the fourth satisfied it. **No reader ever had to guess whether the open row meant a broken auto-close** (finding 16's failure mode) — the ticket said what it was waiting for, on the surface a board reader already looks at.
  - **The batch is a generated table, not fifteen rows.** `PORTRAIT_ARCHETYPE_ROLES` maps to ids, paths and concept tags; a sixteenth archetype is a one-word edit. **Worth keeping as the cheap half of finding 2's remedy:** the class keeps recurring because extending authored capability is usually expensive enough that nobody does it, and the durable defence is not a sweep but an extension cost near zero.
  - **Same reuse discipline as batch 3, one layer in:** a portrait row serves *any* unportrayed agent reading as that role, so it must read as a farmer without reading as a *particular* farmer an encounter could contradict — the portrait analogue of batch 3's unoccupied scene plates. `portrait.traveler` remains the category generic for anything outside the fifteen.
- **03:22 — THR-832 batch 3 merged (PR #986), ~40m: encounters now have the *places* they happen in.** Fourteen built/social scene plates — guild hall, tavern, market, court, shrine, ruin, siege, road, river, settlement, underground, wilds camp, rebuild, aftermath — taking the library **96 → 110 rows** and the remainder **29 → 15 slots**, all batch 4 (portraits). The ticket stays In Dev, correctly, for the fourth consecutive batch.
  - **Two authoring decisions worth keeping.** (a) **Every plate is unoccupied** — stronger than the library's usual "agents absent or silhouetted" rule, and deliberate: a scene generic is reused across 3+ unrelated encounters, so any figure in it asserts a cast the encounter may contradict. The place is evidenced by its objects (the market by its scales and stacked goods, the court by its dais and long approach). (b) **The three `settlement`-placed rows** — settlement / rebuild / aftermath — were generated as one unit against a shared architecture vocabulary, so they read as **one place in three states** rather than three unrelated villages. That is the Rebuild Road made legible in art before WS5 authors a line of its prose.
  - **The batch is 14 against a scoped ~60, and the delta is reuse rather than descoping.** The outdoor half was already covered by `TERRAIN_SCENE_GENERICS` — shipped terrain art promoted into the library instead of redrawn. **Finding 2's favourable inverse:** the usual instance is authored capability nothing routes to; this is authored capability that already had a router, found by checking before generating.
  - **The remaining 15 are the portraits, and their fallback is already correct.** The code comment states it plainly — every unportrayed role resolves to the batch-1 hooded-traveller baseline, *"already correct, just unspecific"* — which is why the batch is last, and which is the premise under this run's finding 21 sequencing observation.
- **02:50 — THR-832 batch 2 merged (PR #984), 47 minutes from claim: nudge cards render real art.** Sixteen concept generics — one per kind of nudge a god can offer (*focus*, *light*, *dark*, *luck*, *oath*, *strength*, *blessing*, …) — moved from `ENCOUNTER_IMAGE_PLAN` into a `NUDGE_CONCEPT_ART` library block, taking the library **80 → 96 rows** and the remainder **45 → 29 slots** (batch 3: 14 scene generics, batch 4: 15 portraits). **The same PR added `nudge: 'generic.blessing'` to `ENCOUNTER_IMAGE_CATEGORY_GENERIC`**, discharging the ticket's third Done-when: until it landed, a nudge card with no exact match fell through to the `EntityVisual` gradient.
  - **The ticket stays In Dev, and this is a park by construction rather than by judgement.** Its own description specifies *one batch per PR* and a Done-when of *`ENCOUNTER_IMAGE_PLAN` is empty*, so an open row with a merged batch under it is the ticket working as authored. **Worth distinguishing from finding 16 rather than filing under it:** 16 is about a park whose reason exists only in a PR body, where the next reader's cheapest hypothesis is a broken auto-close. Here the reason is in the ticket description — the most durable surface available, and the one a board reader is already looking at.
  - **This run's first measurement of the remainder was wrong, and the mechanism is worth more than the number.** `npm run check:image-library` was run in the home tree, which lagged `origin/main` by the two batch-2 commits, and printed **"45 slots still to generate"** under a green `OK` — the pre-batch figure, i.e. *"nothing shipped this hour"*. **That is finding 14 happening to this task rather than being cited by it**, and it is THR-819's exact shape one lane over: a gate with a stale baseline does not fail, it answers the reassuring way. Re-measured against `origin/main` by content — the batch-2 slots are gone from the plan, `NUDGE_CONCEPT_ART` has 16 entries. **The generalisation for this lane: the home tree is a read-only mirror and is therefore a candidate stale baseline for *any* checker run inside it**, which makes it a standing property of step 2 rather than a one-off slip.
- **01:21 — THR-777 shipped (PR #980), 1h19m claim to done: the Nudge Model's encounter image library has a manifest and a resolver.** Every library image carries id, path, concept tags, sphere, reach, place tags and mood; the builder and UI resolve by tag query down a fallback chain (image → category generic → `EntityVisual` gradient+glyph), so a hundred-plus encounters can share composed art instead of each minting its own. **Sequenced ahead of WS5 (THR-778) deliberately** — the content migration touches 100+ encounters and should point at a library that exists, the same reasoning that put THR-820's schema fields before it last night.
  - **The ticket closed while a third of its deliverable was still unmerged, and that is worth recording as a legibility shape rather than a defect.** PR #980 carried the closing keyword; **PR #982 — batch 1, the 48 fate images plus the plain hooded-traveler portrait baseline, 57 files — opened at 01:40:16Z with auto-merge armed 8 seconds later**, required check still pending when this run's measurements were taken at 01:54Z, **merged 01:58:55Z**. **Finding 16 inverted:** 16 warns that a correctly-parked issue reads as a broken auto-close; this is a correct close reading as completed work. Both are the same error — inferring delivery state from the board column — and both are cheap to avoid by looking at the PRs. **The 37-minute Done-to-landed window is the measurement**, and it is small enough to be invisible to any hourly reader who does not check.
  - **The briefing was corrected in the same run rather than left to the next one.** It was assembled at 01:54Z saying the artwork was mid-test; #982 merged four minutes later, before the PR carrying the brief had merged. **A brief is a morning-read surface, so a claim that goes stale between assembly and publication is worth one amend** — the alternative is Christian reading "landing right now" about work that landed hours earlier, which is exactly the class of staleness finding 1 keeps catching in slower form.
  - **One deferral filed underneath it: THR-832 (Medium)** — batches 2–4, the remaining 45 image slots (scene generics, nudge-card library, the rest of the portrait set). **Ship-one-find-one, and the first of the recent pairs to enter at the *same* grade as the ticket that spawned it** rather than a grade below, which is the composition drift of finding 19 not happening for once. Plan doc: `Docs/plans/2026-07-28-thr-777-image-generation-plan.md`.

## Resolved 2026-07-27

Compact index. Full prose for each is in `git log -p Design/user-actions.md`; the lessons worth keeping are folded into *Durable findings* above.

- **23:34 — THR-827 shipped (PR #978), ~33m.** `actionModifiers` was documented "capped at ±0.20" with nothing clamping it, while the exemplar hand already summed past the stated bound. Finding 14's purest form: an unbacked assertion gives the reassurance of a gate and none of the behaviour. Repaired by bounding hands at authoring time rather than pretending to clamp downstream.
- **22:32 — THR-820 shipped (PR #976), 31m06s.** The nudge test panel had no schema field for purpose lines or factor lines; WS1 authored both as fixture constants only. Finding 2 one notch earlier — a writer with no field to write into. Landed before WS5 so THR-778 would not mint the concept a second time against 100+ encounters.
- **21:30 — THR-619 shipped (PR #973).** The rival economic scheme is live: sour the mines → corner the grain → break the guild → starve the faithful, riding Flow Web stock tiers rather than spawning combat. **First end-to-end pass of a chain no human touched at any link** — three lanes, no messages, coupled only by a Linear column and the `Blocked by` field. It had been free to start for 22 days.
- **19:49 — THR-826 shipped (PR #971), 46m53s: Threadbare gained a lane that decides what happens next.** `Blocked by` had four months of writers and no reader — finding 2 at the process layer, and the largest instance yet. Root cause was authority, not code. Its first sweep found three tickets unblocked for 22, 5 and 22 days, and it **reported its own blind spots** (trait coverage logged unmeasured, redundancy pass logged not assessed). One deferral: THR-828.
- **18:32 — THR-821 shipped (PR #969), 30m20s — the cleanest refutation-by-measurement of the week.** Both offered causes were refuted because the *sample* was ineligible: all seven "doomed" mortals were ambient-tier NPCs that cannot reach the nudge stage at all. The genuine defect (the `notable` tier flooring and staying floored) surfaced only because the census covered the whole population rather than the reported one. Source of finding 15's third leg. One deferral: THR-827.
- **18:10 — THR-796 shipped (PR #961), and the deliverable is a detector, not a repair.** Claude-in-Chrome's `resize_window` returns success while the viewport does not move — verified in both directions. Every screenshot taken through that route was an arbitrary size presented as the contractual 1920×1080. Finding 14's third instance and the first where the lying signal is a tool's *success string*.
- **17:21 — the four encounter-experience issues reached the shelf**, closing the last thread from Christian's afternoon go-ahead, 17 hours before this file's deadline for them. The pre-committed consequence was **cancelled outright rather than rescheduled**, because its condition never occurred.
- **17:14 — THR-798 shipped.** Friday's review run filed two real tickets citing a report file **never committed to any branch**. From the outside a lost artefact and an invented one are indistinguishable. One deferral: THR-825.
- **17:11 — THR-794 shipped (PR #959).** Two live scheduled tasks had no registry row, including `ThreadbareRepoAutoSync` — the containment script for the home-tree parking behaviour. A missing row reads as *no containment exists*. One deferral: THR-824.
- **16:38 — Christian answered the release ask on Discord, 44 minutes after it was raised.** Verbatim: *"Release the remaining encounter experience to the queue"*. The run's real deliverable was the **receipt** — telling him unprompted that his sentence is recorded authority rather than a filed action. Source of finding 20.
- **16:30 — THR-819 shipped (PR #957).** The local `check:wiki-freshness` never fetched, so it reported PASS on precisely the condition CI fails. Second same-repo instance of finding 14, and the first where the ambiguous signal was ours.
- **15:16 — THR-793 shipped (PR #955).** Sixteen orphan root markdown files triaged, and the hygiene check rewritten to **enumerate** rather than spot-check — the repair being the detector. One deferral: THR-823.
- **12:38 — THR-813 shipped, ~37m.** 13 unproducible ambition selection keys retired rather than repointed, with reasons recorded for the three that could not be. One deferral: THR-822.
- **11:58 — THR-775 shipped (PR #951), 56m12s: the new encounter interface is live.** The nudge hand, the fate reveal in words, and the commit path that finally **writes** `activeNudges` — a field that until then had readers and no writer.
- **10:50 — THR-774 shipped (PR #949), 47m53s.** The nudge authoring pipeline, with the golden exemplar shipping its first consumer in the box. The whole workstream ran design-to-shipped inside 4h35m.
- **09:28 — THR-802 shipped (PR #943), ~2h25m — the slowest close of the day, and the delay was the deliverable.** The fix was small; diagnosing the two-hour block found a real second defect (THR-819). A correct blocking gate stopping a PR that had genuinely not updated its documentation is the gate working.
- **07:19 — THR-814's park closed** after ~2h40m, and the "wait one refresh" precedent held at 4-of-4 in the same minute. Both carries were stated as falsifiable tests and **both are reported here having come out favourably** — the half of finding 16 that is easy to skip.
- **06:52 — THR-816 shipped (PR #941), ~94m.** Six definitions share `factionType: 'guild'`, so first-match seeding left the Builders' Fellowship with zero members in every run. The anti-starvation test asserts over the role **mix**, not a per-guild floor. Two deferrals: THR-817, THR-818.
- **05:54 — THR-815 shipped (PR #936), 52m — the three-ticket guild thread closed.** Guild work for the 225-of-227 ambient members now resolves at the tier that is actually simulated. Reputation gains 0 → 449, jobs resolved 0 → 944, rank-blocked templates 21 → 0, at 0.109 ms/tick.
- **04:41 — THR-814's work merged (PR #934), and the issue deliberately stayed open.** All three proposed routing rules were void, as were THR-810's three levers: every one adjusted a stage the content never reached. **Positional starvation at the cap** — entries appended at the tail of a ~4,650-entry list the cap fills from the head — took survival 0 → 60.6%. Source of finding 15.
- **03:45 — THR-810 shipped (PR #932), ~45m — as a verdict, not a fix.** The elite guild rank was not mis-tuned; it was **unfed**. 0 reputation increases against 227 memberships. The constant was not changed; the repair was re-scoped to THR-814 on the recorded ground that it is a design decision.
- **02:29 — THR-785 shipped (PR #930), ~28m.** Production deploy health is observable. Promoting the Vercel check to required was **considered and refused** — not a correctness gate, and it would couple every merge to a third party. Source of finding 14's repair half.
- **01:31 — THR-812 shipped (PR #928), 28m25s.** Revenge vows resolve against the death the engine records. Per-instance binding was **rejected, not built** — the pool has no assignment path at all (finding 13).
- **00:30 — THR-809 shipped (PR #926), 28m15s** — the one that finally makes a lost tavern brawl leave a wound. 11 of 13 condition definitions were never inserted into any world.

*Resolved entries from 2026-06-23 through 2026-07-26 were pruned (2026-07-26 18:54 UTC, and the 2026-07-26 index at 2026-07-28 02:54 UTC). Recover any of them with `git log -p Design/user-actions.md`. The 2026-07-27 index above prunes at the next natural boundary.*
