# Batch brief — Retrofit Batch 1 (the slice six)

**Status:** approved in chat (Christian, 2026-08-15/16) with one amendment: his 10 THR-974 rewrites are **not applied verbatim** — *"run them through the factory instead with the new rules as a validation."* They are the editorial critic's calibration reference.

## The batch (6, ruling 1)

Retrofit, not greenfield — all six ship today and sit on `RETROFIT_PENDING`; deleting their names is the batch's proof.

| # | Template | Gate baseline (2026-08-16, `check:encounter`) |
|---|---|---|
| 1 | `encounter.slice.unsafe_bridge` | concepts ×7 · dead imageTags ×3 |
| 2 | `encounter.slice.grateful_kin` | **no nudge-bearing step** · concepts ×6 |
| 3 | `encounter.slice.swindled_family` | **2 bands, floor is 3** · concepts ×8 · dead imageTags ×5 · second person ×4 |
| 4 | `encounter.slice.swindler_found` | concepts ×8 · dead imageTags ×4 |
| 5 | `encounter.slice.bargain_at_crossroads` | concepts ×8 · dead imageTags ×3 |
| 6 | `encounter.slice.full_moon_collection` | **no nudge-bearing step** · concepts ×7 · systems 2/3 |

## What every encounter gets

1. **The full Composition Contract** — the gate's fix-list above, no exemptions (ruling 3).
2. **The prose re-register** — every aftermath overview, change (`causeClause`/`detail`), and reaction intent passes the THR-974 three moves: subject-first (no fragment openings), concrete events over abstract nouns, one dry line for irony. Validation reference: Christian's 10 rewrites in THR-974's resolution comment — the critic judges output against them, the text is authored fresh.
3. **Image tags** resolve against the live library (`generic.` = blessing/dark/decay/energy/focus/light/luck/matter/memory/oath/rumor/strength/vigor/ward/warmth).
4. **Concepts** per change (Law 2, THR-1033 rule: `text` is an exact substring of `detail`, tooltip only where it resolves, no decoration).

## Reach/shape/tone variance

Fixed by the existing six — both sequel pairs whole (Swindled→Swindler, Crossroads→Full Moon), Stone/Heart/Gold reaches, single-test and consequence-chain shapes. Batch 2–3: Snow on the Pass, Riders Behind the Caravan, the camp seven + sequels.

## Card exposure

The two new hands (grateful_kin, full_moon_collection) diversify against the slice's existing compositions per REVISE trigger 21 (no identical card-type composition within a family).

## Review

Batch report with the six side by side + spawn links; Christian samples 2 (the two whose passages his rewrites targeted: unsafe_bridge, grateful_kin).
