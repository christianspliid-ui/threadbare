**Two sessions could not reproduce the reported symptom, and the probe that produced the "live lead" could not have reported anything else (THR-1005, 2026-08-06).** The prior pass hardened the auto-open scan to walk past a notification that *declines*, and honestly recorded that it changed nothing observable. The blocker it could not clear is that **the final step notification of a resolved action does not decline**: when an action resolves, `currentStep` **freezes at the final index** rather than advancing past it, so THR-664's `stepIndex === currentStepIndex` check — the only thing that retires a walked-past step — passes for it forever, and nothing ever marks a spent step notification `resolved`. The aftermath is appended last of all, directly behind that permanently-openable record. Measured live at `?spawn=tg.senior.jewel_heist` by replaying the opener's own predicates against live state: queue `[step0, step1, step2, aftermath]`, action resolved at `currentStep 2`; steps 0 and 1 declined on the mismatch, **step 2 did not**, consumed the single auto-interrupt slot, and the aftermath was never reached. `isStepNotificationSupersededByAftermath` retires it, scoped deliberately to "an aftermath *for this action* is still pending" rather than the broader "a resolved action cannot open any step", so a digest-tier record or an already-acknowledged aftermath cannot be stranded with nothing left to show. **The second find is the one that should stop the next session losing a pass:** the checkpoint's live lead was impediment #447's `inspectEncounterPipeline('@hero')` reading `{threads:[],activeEncounters:[],notifications:[]}`. That probe has no `@hero` resolution at all — every filter is a raw substring match on node id/name, and no node contains the literal `@hero` — so it returns all-zeros for **any** world state. Demonstrated at one instant: unfiltered `threads: 2, notifications: 4`, `'@hero'` `0, 0`. A vacuous probe reads as hard evidence of an empty pipeline while measuring nothing; it now resolves the alias to the ascendant's avatar (`Vara`, 1 thread / 4 notifications) or errors loudly. **What is deliberately not claimed:** the aftermath renders on this route with *and* without the supersession fix, because the veil's display adapter keys off the resolved action rather than the notification kind — so the ticket's Done-when passed before either fix and does not capture Christian's complaint. Parked for one plain-language question rather than closed.
