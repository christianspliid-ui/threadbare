# THR-1357 — the wheel finally leaves the tree

**Shipped 2026-08-29.** R3-T5 of context-cleanup round 3 (THR-1350) — the round's code
ticket. Engine N/A (a test file only); Content N/A. Audit:
https://claude.ai/code/artifact/98713042-66d0-4a4e-8304-93339d0e3096

## The rejected component and the test that vouched for its absence

`src/components/Game/AgentWheel.tsx` — the intervention wheel, a rejected approach — was
recorded as deleted in the changelog on 2026-03-08 and sat in the tree ever since, importable
by nobody but its own test. What kept the fiction alive was
`actionCardRedesign-integration.test.ts`'s block titled *"AgentWheel component is fully
removed"*, whose body checked only that the (deliberately surviving) `wheel.ts` data layer
exports a function — *"Since this test runs without error, it implies AgentWheel is gone."*
Both files are now actually deleted, and the test asserts the filesystem: `fs.existsSync` on
the component and its test both `false`, plus the data-layer check it always had. The
`@deprecated` `engine/wheel.ts` survives on purpose — `ActionDrawer` still types on
`WheelSlot`.

## Law 13 on the Harvest screen — including a violation the audit missed

The audit flagged the bare echo counts ("Echoes Preserved (3 cosmic)", "2 divine echo
slots"); the fix pass found a third, worse leak the audit did not: `Significance:
{(score*100).toFixed(0)}%` — a raw percentage, banned outright. All three now render as
words: counts through the existing `countWord` ladder, significance through a new named
`ECHO_SIGNIFICANCE_BANDS` ladder (`faint/notable/resonant/defining`) rendered as a sentence
("A defining echo of the age"), not a key:value strip (Law 16). Tests updated to assert the
words and the *absence* of `%`. One vault correction rode along: the round-4 rewrite of the
Harvest Screen vault page had over-claimed "no variants" — `harvestType`
(triumphant/bittersweet/somber, mandate/doom-derived, echo counts 5/4/3) is real; the page
now states it, sourced from `cycleEnd.ts`.

## Naming: the last player-and-designer-facing "Fantasy World Simulator" strings

`public/wiki-manifest.json` title, four static wiki pages' `<title>`/subtitle/body strings,
the generated hub paragraph (`generate-design-wiki.ts`, hub regenerated), the CLI banner
(`cli.ts`), the review-agent prompt (`review-client.ts`), and — pulled forward from the
round-5 residue list — `scripts/rebuild-index.ts:324`, whose hardcoded H1 would have
reverted the vault Index fix on every regen. `npm run rebuild-index` then ran clean:
**"# Threadbearer — Knowledge Base"**, 442 pages, round-3 banners leading their index
entries. Also: the dead EB Garamond Google-Fonts link left `index.html` (the app self-hosts
Cinzel + Alegreya Sans), and `ia-manifest.ts`'s docblock stopped pointing at a YAML deleted
months ago.
