**Tier advancement was strengthening a number nothing reads, and making it correct is not the same as making it run (THR-723, 2026-08-06).** `attachmentTierAdvancement` scaled possession-edge `modifiers {iron: …}` by 1.5 per tier, but `modifiers.ts` has exactly **one** production reader — `visibility.ts`, and it only ever asks for `los_range`. So every capability key it scaled was argument-level dead: written, never queried, invisible to a symbol grep because both sides of the contract have live hits. It now scales the artifact's `stat_contribution` effects, the substrate `collectStatContributions` → `computeRawScore` actually reads since THR-718. **The clamp is the non-obvious half:** 1.5 compounds, so a legitimately-authored 1.5 iron reaches ~5.1 by tier 4 against the `ITEM_STAT_BAND_LEGENDARY` ceiling of 2.0 that a content contract test enforces on every catalog — unclamped, advancement is a sanctioned bypass of the exact power budget THR-718 built to hold items in check. **Partition by `REACH_DOMAINS` membership, not a `los_range` allowlist**, so the next live attribute someone adds keeps working instead of being silently killed by a hardcoded name. **The scope was held deliberately, and that is the part worth remembering.** The module has **zero production callers** — its only importers are its own test and the contract registry — so none of this has ever executed in a real game. The ticket's *first* comment said so and flagged wiring as "plausibly a user-facing design call"; a later promotion comment called the ticket a "clean, scoped technical fix", and `pull-work` reads only the latest comment absent a `Reopened` label, so following the skill literally would have surfaced only the reassuring summary (impediment #434). Whether items strengthening over time is a mechanic this game has is Christian's call, not an executor's — split to **THR-996** with the three options framed in game terms; the surviving reach-keyed *seed* writes in `gameInit.ts`/`seedAttachments.ts` are **THR-997**, so both LEAKED rows now point at live tickets rather than the one this PR closes. Found in passing: the row claimed `readSites: ['src/engine/orchestrator.ts']`, which has never imported the module — a transcribed intention, precisely what this registry exists to expose. All 6 new guards confirmed failing against the pre-fix source before being accepted as evidence.
