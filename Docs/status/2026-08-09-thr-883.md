# THR-883 — the encounter-writing format is locked (chat-approved)

**Shipped 2026-08-09.** The sitting that paused all content migration on 2026-07-30
is closed: Christian approved the two ruling-9 review artifacts in chat
("looks fine", human gate satisfied via chat review 2026-08-09), which was the
sitting's whole remaining scope after the 2026-08-08 grill round settled every
open input.

The two artifacts (PR #1363):

- **The amended authoring contract**
  (`.claude/skills/encounter-pipeline/reference/nudge-authoring-spec.md`) now
  carries the Composition Contract as a first-class section — the 8-block
  table, the no-exemptions ruling (a block-less shape is a future encounter
  type, never a waiver), inline expression, `check:encounter` as a PR
  precondition, and the shrink-only ratchet — plus a Cast section encoding
  ruling 6 (role-voiced inline prose as the default, `{cast:*}` tokens only
  where the generated name earns something, never gender a bound cast member,
  THR-1044 family defaults, and a **class-honesty rule**: an explicit bundle
  must read correctly at every class its envelope declares), and a rewritten
  Aftermath step (aftermathConfig mandatory, the byOutcome floor of three
  bands, `concepts` per change, and **`rewardPool` documented for the first
  time** — the composition audit found the whole mechanism untaught despite
  522 uses in the old corpus).

- **The Swollen Ford exemplar authored to the full contract**
  (`src/data/__fixtures__/nudge-exemplar/swollen-ford-exemplar.ts`). Three
  real defects fixed on the way: the cast spec was rural-only ("the miller's
  boy") on a rural+wayside envelope and became a class-honest fellow traveler
  (`wanderer`/`pilgrim`, seeded at both classes); a header comment claimed a
  `{cast:*}` token the prose never used, and the token is now real at the one
  spot the name earns it (the critical_failure rescue reveal — a stranger
  becomes a named, clickable person at the moment they pull the agent out of
  the river); and a reaction labelled "Let the valley remember this water"
  whose effect *applied exhaustion* — a click whose label promised memory and
  delivered harm — is gone, with exhaustion now minted by step-1
  `failureMetadata` so the endings narrate only state the mechanics wrote. The
  spec's own mandatory fresh-context critique pass ran against the revision
  and returned seven findings (a "Three days of rain" triple echo across
  openings and initiation, a wayside continuity break on the stakes, a rule-7
  slip on an ungated card face, a three-instance house mannerism); all fixed
  before delivery, which is the gate working as designed.

Consequences: the eleven content tickets paused behind this sitting (WS5
batches 1a–1e, shell_proof, the apotheosis gate, Meeting Batch A) and the
factory run harness (THR-1047) unblock on this close. Per ruling 8 the
retrofit of all 15 nudge-era templates is the factory's pilot volume and rides
THR-1047/THR-973, not this ticket. Session side-product: Christian's Rule
Zero — every reference he sees carries a direct clickable URL — is now the
first section of CLAUDE.md (PR #1365).

Evidence (PR #1363): `check:encounter -- fixture.encounter.swollen_ford` → OK,
clean, [systems: cast, rewards, conditions]; 976 files / 15,420 tests green;
typecheck ratchet unchanged (3462); build green; both freshness gates run
last, green.
