**The three-tier segment rule has one implementation again.** THR-1084 extracted the link/tooltip/plain renderer into `NarrativeSegments.tsx` and wired the aftermath *reaction* labels to it, but had to leave `EncounterVeil`'s consequence-chip block rendering the same three tiers inline: that block sat inside the diff of held PR #1415 (THR-1082), and rewriting it would have conflicted with a PR that could not merge until an attended session ran its pixel pass. #1415 merged at 2026-08-14T06:59Z (`cf9c3cde`), which is the whole of what this ticket was waiting on — so the substitution is the mechanical one the ticket specified, and the interest is entirely in what the gate proved rather than in what was written. **Net −64 lines**, one rule, one component (Law 27). **The chips take the default `nested={false}`, and that is the one real decision in the diff.** `NarrativeSegments` draws a `role="link"` span rather than a `<button>` when `nested` is set, because a link inside the reaction-pick button would be a button inside a button — invalid HTML. A consequence chip sits inside no outer click target, so it keeps real `<button>` links; passing `nested` here would have been wrong in a way that is invisible to most assertions. **The pre-existing suite already pinned every tier, and passing it unchanged is the evidence that matters** — the click tier by `getByRole('button', { name: 'Vasara' })`, which would *not* match a `role="link"` span and so falsifies the wrong-`nested` mistake for free; the hover tier by the Law 17 `tabindex` case; the plain tier by the Law 21 dead-underline case. All 76 assertions green with no edits, which is what the Done-when asked for. **Two tests added, and deliberately only two.** The first draft added five, then was cut: four of them restated coverage that already held — the suite has carried `makes a resolved entity inside a chip clickable` and `FAIL-OPEN: renders an entity as plain text when no handler is wired` since THR-1004, and duplicating them would have grown the suite while proving nothing new. What is genuinely new is the per-segment `data-testid`s the shared renderer emits, so that is what is pinned, plus their **absence** on a compact chip — which still draws no sentence at all, THR-1082's held ruling that incidental drift "takes away from the encounter story". `tooltipResolves` lost its last caller in this file and its import went with it. **1015 files / 16,218 tests green**, ratchet unchanged at 3448, build clean in 10.35s, wiki freshness clean across 24 pages. No engine smoke owed — UI-pillar diff only. **Browser-verify substitution: jsdom-render**, route probed before implementing and verified refused (*"Dev servers can't be started from unattended sessions"*). Honest here on the narrowest available grounds: the rendered DOM is identical by construction apart from added test ids, and unlike THR-1106 — which had to argue that from diff inspection — here an untouched 76-assertion suite proves it. **Impediment #574 corrected and bumped to 3**: its count still read 1 while its own cell narrated a recurrence, and this is the third occurrence in three days of the same structural refusal. The durable fix it names is still owed and is not an agent's to make. (THR-1105, 2026-08-14)
