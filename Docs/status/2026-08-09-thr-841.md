**Two ambition milestones that could never fire now can, and one ambition is completable for the first time.** `agent_in_region` / `agent_not_in_region` resolved a location's region by reading `loc.properties.regionId` — a property with **no writer anywhere in `src/`**, measured at 0 distinct values across all 727 locations of a live seed-42 run. Both conditions were therefore false for every agent in every world, and eight unit tests covered them green by stamping the phantom property onto their own fixtures. The read now walks the `contains` edge from a `region` node, which is the only location→region linkage that exists and the shape the architecture calls for — a relationship between two entities is an edge, and `regionId` was the property-bag spelling of exactly that relationship, which is why it never acquired a writer. Sublocations climb `parentLocationId` to the location that carries the edge, bounded at depth 4 so a cycle introduced by a future writer cannot stall `phaseAmbitionProgress`. **Fixing the read discharges nothing on its own, which is the part the ticket did not have:** region ids are minted per world as `region_0…region_N` with names generated from historical-culture ownership, so an authored literal names nothing that will ever exist — `'homeland'` and `'cursed'` were unmatchable by construction, not merely mismatched. Both shipped authorings are repointed onto a new agent-relative pair, `agent_in_origin_region` / `agent_not_in_origin_region`, which read the origin THR-822's residence observer already records and need nothing an author has to know in advance: the region-coarse, instantaneous twins of `agent_away_from_origin`, where that one is location-exact and durational. **The sweep the ticket asked for found a second site, and it was the serious one:** `ambition_escape_cursed_land` is `completion: { requires: 2, of: 2 }` with `escape_exit` gated on `agent_not_in_region: 'cursed'`, so the ambition was **unachievable for every agent in every world** — the named site, `reclaim_return`, was the benign one at 2-of-3, where only the beat the whole ambition is about ("The old soil underfoot again") could never fire. Evidence is a live seed-42 medium run at tick 40: **344 of 382 agents with an observed origin can now be answered** (341 in their origin region, 3 away), against 0 for every agent before. A `contentInvariants`-style sweep in `ambition-templates.test.ts` now names any template authoring a region literal and fails with the site id — the reporter the ticket noted was missing, since unlike trait refs these had no `validateTraitRefs` equivalent and a new dead one could be authored tomorrow unnoticed. Worldgen's region→location pass additionally walks `getNodesByType('location')` rather than the `locationIds` array and indexes tiles by hex key, replacing an O(locations × tiles) scan (~557k comparisons on a medium map, ~2.9M on epic) with a lookup; **coverage was measured before and after and did not move** — 97 of 112 top-level locations, the unlinked 15 sitting on tiles outside every flood-filled cluster, which is a worldgen property and not this pass's oversight. 976 files / 15,442 tests green, ratchet unchanged at 3458, build clean, 30-tick engine smoke reaching 383 agents. (THR-841, 2026-08-09)
