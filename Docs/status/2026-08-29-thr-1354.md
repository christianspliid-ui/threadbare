# THR-1354 — five dead files, and a registry nobody could open

**Shipped 2026-08-29.** R3-T2 of context-cleanup round 3 (THR-1350). Docs-only; Engine and
Content N/A. Christian's call in chat: *"1 retire."* Audit:
https://claude.ai/code/artifact/98713042-66d0-4a4e-8304-93339d0e3096

## Deleted

- `Docs/TOOLTIP_CHECKLIST.md` — a build-Tooltip-from-scratch checklist for a primitive that
  shipped months ago, teaching a light-mode theme prop (Law 32), the retired raw-`title`
  fallback (Law 17), hardcoded off-brand gold (Law 30), and inline copy instead of the
  registry. Whoever loaded it once built a law violation.
- `Docs/IMPLEMENTATION_PATTERNS.md` + `Docs/PATTERNS_REFERENCE_INDEX.md` — the checklist's
  companion scaffolding from the same March planning pass, carrying the retired game name and
  teaching deleted components (RetinuePanel) as live examples. The trio referenced only
  itself; live replacements are `Docs/ui-patterns.md` and `Docs/design-system/primitives.md`.
- `Docs/design-system/components.md` — an April to-do list ("unresolved — needs design
  decision") posing as a component inventory, carrying a third modal-height value (80vh).
  Superseded three ways: `component-selection.md` (choice), `layout-zones.md` (geometry),
  `primitives.md` (API). INDEX.md and the frontend-ui reference table no longer point at it.
- `.claude/skills/qa-orchestrator/.fuse_hidden00000c5a00000002` — a git-tracked byte-stale
  orphan duplicate of test-surfaces.md, the skills partition's only "Fantasy World Simulator"
  hit.

## The style tile is retired (Christian's call, 2026-08-29)

`Design/style-tile.html` — the "Master Asset Registry" five guidance surfaces ordered agents
to maintain — was an untracked local file (`Design/*` is gitignored, no git history) and is
gone from the machine. Every reference now points at the two registries that already do its
jobs: **`src/data/hex-tile-assets.ts`** (hex assets — the asset-existence rule re-anchored
there) and **`?view=styleguide`** (components, Law 29's living contract). Repointed:
STYLE.md § Hex Tile System, design-system INDEX.md (Living Registries + Change Protocol),
typography.md, frontend-ui, art-direction (rewritten — also fixes its "the visual identity
is called Threadbare" naming drift), blender-to-hexmap, documentation-ownership.md.
