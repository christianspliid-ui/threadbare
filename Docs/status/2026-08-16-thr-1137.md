**"The God's Will" stopped arriving every tick, and stopped calling the mortal "they" in the object case.** Christian hit both playing the seeded build at tick ~49: choosing a compulsion option brought the modal straight back, "Release your hold — let them choose" did not stop it either, and the option prose read *"an uncharted place calls to they, in Wraithwood"*. **Fix A** is the re-fire suppression the compulsion path never had. The Whisper phase has always carried two gates — a pending-queue check and a consumed flag — while the compulsion check emitted a brand-new `PremonitionEvent` every tick for any eligible agent, because nothing the player did wrote state the emitter reads: `dismissPremonition` emits a trace and nothing else, and `applyCompulsionChoice` writes a target that a *different* block consumes. The new `shouldEmitCompulsion` predicate (exported from `premonitionCompulsion.ts`, so it is testable as itself rather than through the whole decision pipeline) refuses while that agent holds a live queued premonition, refuses a second emission inside one tick's agent loop, and refuses inside `COMPULSION_COOLDOWN_TICKS` (24 = two game days) of the last offer. The cooldown is stamped as `lastCompulsionTick` **at emission**, not at resolution, so one write covers all three endings — chosen, dismissed, expired — and `premonitionActions.ts` stays untouched. **Fix B** is pronoun case. `{pronoun}` resolves to the subject form, and the pools used it in object slots, so the live modal said "calls to they", "the end of they", "might still reach they". One shared `resolveAgentPronouns` now replaces three byte-identical copies (content, whisper phase, compulsion module) that shared the same missing case, and it adds `{object}`/`{Object}` → them/Them. Twelve fragments were re-cased across the retcon, whisper-vignette, ambition-imagery, nudge and compulsion-vignette pools, including a subject-verb slip (*"they needs stillness"*) and four sentence-initial lowercase pronouns. Per director instruction the composed line leads with the agent's **name** — and because the three retcon pools are combinatorial, no fragment can know whether it holds the line's first mention, so `collapseRepeatedNames` makes the *composer* decide: name once, object pronoun after. The pool sweep then caught a defect the ticket had not listed — the whole `far` band and one `medium` fragment ended in a bare *"of"* while every desire fragment is a clause, so *"From distant lands, news of they could make a difference"* was broken for the same reason the reported line was; those four now take a complementizer (`word has come that`) or a dash. Evidence: the gate predicate **falsified before trusted** (neutering it fails 6 of 10, including "refuses on the very next tick"), a 5,000-line combinatorial sweep of `composeRetconLine` across every encounter type × threat band × distance band × 40 seeds asserting no unresolved placeholder, no object-case leak, no sentence-initial lowercase pronoun and no name said twice, plus a pool-wide guard so the next author cannot re-introduce the class. The headless proof is a **controlled two-arm run**, because the CLI world seeds no `thread` edges at all and the count the Done-when asked for would otherwise have been vacuous over an empty population: with eight agents threaded, pre-fix emissions per agent over 60 ticks were 6–43; post-fix they are 1–3, every gap ≥ 24. 1017 files / 16,329 tests green, ratchet unchanged at 3182, build clean, engine smoke to tick 30 with 383 agents. UI pillar N/A — both fixes are upstream of `PremonitionModal`, which renders whatever queue and prose it is handed. (THR-1137, 2026-08-16)
