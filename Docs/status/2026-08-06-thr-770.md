**A content table was keyed to a cosmology the engine no longer speaks, and the read site's `?? []` is why nothing ever said so (THR-770, 2026-08-06).** `GROUP_NAME_SPHERE_ADJECTIVES` supplied the sphere-flavored adjective a god-founded company takes into its name, keyed by `hunger`, `mind`, `spirit`, `blood`, `stone`, `tide`, `flame`, `dusk` — of which exactly **two** are sphere ids the engine can produce. The other six matched nothing, so six of eight alignments drew no flavor at all, with no crash, no warning, and no failing test. **The silence is the finding, not the typo:** the table was internally consistent, its consumer handled a miss gracefully, and every gate the repo owns passed for the life of the feature — a table can only ever be proven to equal itself, which is why the guard here is the *generator's output*, requiring that naming the same companies under a given sphere produce a different name set than naming them with none. A dead key leaves it byte-identical, which is precisely the state that shipped; reintroducing the defect reddens it. **The retired vocabulary was salvaged rather than rewritten, and where it landed is the evidence for what went wrong:** `stone`'s words moved to `matter`, `flame`'s to `energy`, `dusk`'s to `entropy` — exactly the spheres those *reaches* map to under `REACH_TO_SPHERE`, so the pool had been authored against reaches and keyed as spheres. All 12 real ids are now authored, Foundation included, because a caster's primary genuinely can be one (`hunger-catalog.ts` carries `primary: 'light'`) — and the table is typed `Record<SphereName, …>`, so a renamed sphere now fails to compile instead of going quiet. **The second half is that the pool was dead for Draw Together no matter how it was spelled:** the verb never passed a sphere, because the company does not exist when the op fires. The caster's sphere now rides on the pulled mortals as `convergePullSphere` and the formation scan reads it back off whichever member it gathered — deliberately only from members whose pull is *still open*, the same predicate that attributed the cause, so a stale stamp cannot colour a company it did not gather. Registered as an interface contract for the reason the ticket exists: a write/read key mismatch here produces a perfectly ordinary name and no signal.
