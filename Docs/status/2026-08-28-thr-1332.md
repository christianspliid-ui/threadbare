# The dead gate stops running (THR-1332)

**Every session in this repo was still executing Cowork-detection machinery for a runtime retired five weeks ago.** `.claude/settings.json` wired `detect-cowork-session.sh` at SessionStart and `cowork-role-gate.sh` on every Write/Edit and Bash call — three hook executions per gated action, each spawning bash plus node, each concluding "not Cowork" and exiting 0, forever. The Cowork VM the gate protected against (THR-654) can never occur again, which makes this the six-week sunset presumption's cleanest possible case: a gate with zero possible future catches.

Removed: the three wirings and both scripts. `worktree-write-guard`, `pre-commit-gate`, `pre-push-gate`, and `session-stop-gate` stay untouched — `.claude/hooks/lib/` stays because they share it. A repo-wide grep confirms nothing else references the deleted scripts or their `/tmp/.cowork-session-marker`. The coordination protocol's Gate-4 paragraph, which still called this "a speculative hook — if hooks fire in Cowork", was corrected under THR-1331 in the same round.

Found by the 2026-08-28 general-context audit (context-cleanup round 1) as the strongest evidence that retirement sweeps were not finishing: not stale prose but live machinery.
