**THR-929 closes: the last 14 templates name the result, and a mutex that would have held forever was reversed on evidence rather than waited out.** Sub-batch 1 shipped 21 of the filed 30 on 2026-08-09 and deliberately left the nine `encounter.*` templates behind a mutex — THR-860's PR #1114 holds `src/data/encounter-content.ts` open, and the stated reason names that exact file. Honouring it was right then. Waiting again would have been a **deadlock**, not a wait: #1114 is unarmed, `DIRTY`, untouched since 2026-08-02, and carries a line-anchored `Hold:` marker, so nothing was going to move it and the ticket's own recorded next step ("once #1114 merges or closes") had no arrival date. Two independent facts reversed it under THR-688 rule B. **The hold's stated precondition had resolved** — its marker reads *"paused until the encounter-writing format is locked"*, and THR-883 completed 2026-08-09T08:45Z with the chat gate satisfied. **And the surface is provably disjoint**: #1114 touches base lines 1896–2183 and 4284–4472 of that file, while the nine sit at 5208–12458, so a merge is clean by construction, not by luck. Both are technical verdicts — a merge either happened or it did not — and the reversal is recorded on the issue. **The rewrites are the deliverable, not the counter.** `encounter.black_market_deal`'s failure opened *"Something goes wrong"* and then hedged the result itself with *"the goods are seized, **or** {actor} is marked as a buyer"* — the player was owed one of those, and now gets both: *"The watcher was real. The goods are seized and {actor}'s name goes onto a list."* `encounter.dead_drop` said *"Someone noticed. Nothing actionable"* and now says *"A face in a doorway held on {actor} a beat too long. No name taken, no arrest"* — the same beat with the camera actually pointed at it. `encounter.confront_standoff`, the non-lethal rung, ended on *"the whole thing is over without anyone having to be carried away from it"* and now ends *"Both companies walk off the field on their own feet."* Scene prose was untouched everywhere, per the ticket's standing prohibition. **The predicate was re-derived over the full corpus rather than trusted, and it had drifted** — as the ticket warned it would. A 682-template sweep found 23 members, none of them from the filed 30. Classifying each by *which* term set its outcome hits come from split them cleanly: five failed on natural indefinites only — THR-929's own defect class, drifted in since filing — and were fixed here (`npc_befriend`, `npc_curse`, `tg.social.rumor_trade`, `fa.bounty_hunt`, `fa.rivalry_subterfuge`), taking the batch to 14. The other **18 carry `EVASIVE_VAGUENESS_TERMS`**, a set enforced in every field class since long before THR-899; they were already failing on 2026-08-01 and would still fail if THR-899 were reverted. That is a different defect wearing the same detector name, so it is filed as THR-1067 with the per-template evasive/indefinite split, not absorbed. The closing sweep reads `THR-899-attributable: 0`, which is the Done-when. Unlike sub-batch 1, this batch reports **CLEAN** on every detector, so no impediment-#517 caveat applies to it. One pre-existing typo fixed in passing because it sat inside a rewritten string: `encounter.prisoner_interrogation` read `'The prisoner{verb}s their secrets tight'` with no space, which renders as `prisonerholds`. Impediment #518 logged — `audit:nudge-batch --ids` accepts space-separated arguments silently and audits only the first, reporting `requested 1 resolved 1` for a nine-id batch. 981 files / 15,492 tests green, ratchet unchanged at 3451, build clean, smoke reached tick 30 with 383 agents; browser-verify exempt — content-only, no UI-pillar file in the diff. (THR-929, 2026-08-10)
