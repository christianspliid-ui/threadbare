**Two dead links that looked live are pages now — and the reason the army half had been deferred was a wrong-noun query.** THR-1009 fixed `ArtifactSheet` and left its two siblings reading "Full location profile / army sheet coming in a future update", which is UI Law 21's dead-link clause exactly: the link renders live, the player clicks, the page is empty. Both now follow the artifact-representation pattern — canonical visual, identity block, player-relevant prose. **The ticket's two stated blockers both dissolved on inspection, in opposite directions.** It predicted the location half would need terrain "plumbed through" the caller, because `resolveEntityVisual` returns null for a location with no `terrain` hint; in fact every location node carries `terrain` in its own property bag (`loc_0` is `temperate_forest`, verified via CLI), so the modal reads it from the node and both call sites stay unchanged — live, the sheet resolves the real 16:9 concept-art landscape at the `art` tier, not the glyph fallback. The army half was deferred on "a seeded medium world at tick 12 holds **zero** `army` nodes (`getNodesByType('army').length === 0`)", and that query cannot return anything else: per `types/army.ts` armies are **`actor` nodes carrying an `armyState` bag**, not `army`-typed nodes. Asking the right noun, a seed-42 medium world raises its first host by tick 45 and holds two by tick 70 — "The Iron Covenant — Host", marching to take Ardenmor Keep. Armies are *rare in a short peacetime run, not unreachable* (`factionAmbitions.maybeSpawnArmy` raises one whenever a faction pursues a military ambition), so the category keeps its route and gets a real sheet rather than the "recorded verdict that armies are not a reachable subject" the ticket offered as the alternative. **Law 13 is the whole design problem on the army side**: `ArmyState` is almost entirely raw magnitudes — `headcount: 10000`, `cohesion: 94.15`, `maintenanceCost`, `supply`, `estimatedAttrition` — so the sheet reads every one through a new `army-words.ts` and renders words. Cohesion bands on a **share of the army's own ceiling**, not an absolute, because `cohesionMax` varies by size (a warband peaks at 30, a host at 100): an absolute 30 is a broken host and a perfect warband, and one scale would describe both wrongly. This extends the commitment `types/army.ts` already made for supply — "the larder scalar behind this is private and never rendered". `location-words.ts` does the Law 14 job for `locationSubtype`, whose live values are internal keys (`unexplored_poi`, `master_forge`) and include `null`, so the absent case is the common one; it is a total `Record` over the union, so adding a subtype fails the typecheck until it is named. **A verification lever was built, failed, and was reverted rather than shipped unproven** — a `?debug.openModal=army` target that waits for a force to be raised never opened the sheet even after driving 70 ticks and confirming two armies in engine state, so it is gone and the negative result is recorded in the hook's docstring so the next attempt starts informed (impediment #528). En route, one wrong diagnosis was corrected against itself: an immediate DOM sample after `__DEBUG.tick(70)` read "Tick 0" and looked like a permanent React/engine divergence; re-read 1.5 s later it was 70, so it is commit lag, not divergence — a claim worth not making. Evidence is therefore split by half and labelled as such: the location sheet captured through its live route (`?debug.openModal=location`) at 1920×1080, the army sheet by mounting the real component against the engine's own measured army shape in the same browser at the same viewport. The IA manifest needed no edit and that is itself the finding — it already declared both surfaces reading `graph`, a contract that was aspirational before today and is now true. 987 files / 15,556 tests green, ratchet unchanged at 3450, build clean; no engine file touched, so the smoke is N/A. (THR-1023, 2026-08-10)
