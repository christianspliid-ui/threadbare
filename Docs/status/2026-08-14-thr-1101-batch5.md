**The assist family stops reading as mad-libs** — THR-1101 batch 5, resumed from batch 4's checkpoint, which named `assist` (15) as the natural next single-run slice. All 15 token-carrying `encounterType: 'assist'` templates authored out of the `{adj}`/`{verb}`/`{noun}`/`{action}` shape — `healers_oath`, `aid_refugees`, `healer_aid`, `militia_aid`, `scholar_aid`, `guild_aid`, `the_loan`, `trade_caravan_escort`, `plague_outbreak`, `restless_spirits`, `tend_the_weary`, `rest_and_recover`, `defend_against_predators`, `tend_the_dead`, `mend_fishing_nets` — 107 token lines. **The family is drained**, the stateable predicate this campaign has used since batch 1 in place of a count that rots. Register is duty-weight-and-mundane-care, a Civic-Guard/Dawn blend per the `template-encounter-rewrite` voice guide — help is finite, and the prose measures what giving it costs — calibrated against `encounter.trial_by_combat`, the THR-1036 exemplar. Predicate re-derived at pickup: **103** of 168 carried tokens, matching batch 4's stated figure exactly, so the scan is now stable across three consecutive runs. After this batch: **88**.

**The two-failure-shapes split held for a third family, and the ratio is now predictive rather than descriptive.** `assist` came in at 6 full mad-libs to 9 wedged-token, against `build`'s 7/9 and `trade`'s 7/8 — three families, all clustering near half and half. A full mad-lib has every prose field assembled from tokens and wants wholesale rewriting; a wedged-token template is already authored prose with a token driven into a finished sentence and wants surgical removal. Because the ratio has now held three times, a scoping estimate can be taken from it directly instead of re-sampling each family: expect roughly half of any family's templates to be cheap removals. That is the piece batch 3 could not assert from one observation and batch 4 could only call a rule; it is now a number.

**Rendering rather than reading caught two agreement defects, the fourth consecutive run that this pass has paid for itself — and this time both were pre-existing, in lines the batch merely touched.** `{They}` resolves to a **singular** pronoun, so a bare verb after it renders wrongly: `defend_against_predators` read "The predator closes before **she are** prepared" and `tend_the_dead` read "and **she carry** none of the three". Neither was introduced here — the `{they}` was in the original prose and only the surrounding word-pool tokens were this batch's business — and neither is visible in source, because the defect exists only after substitution. Notably `{s}` could not fix either one: it appends a bare `s`, so `are → ares` and `carry → carrys` are both wrong, which is the same ceiling batch 3 met on `carry` and batch 4 met twice more. Both were rephrased to drop the pronoun. **The reusable point is that removing a token from a wedged sentence is not a safe edit** — it puts the eye on a line whose *other* half may have been broken all along, so the render pass belongs on every line the batch touches, not only on the lines it writes.

**Vagueness swept 3 → 0 on the evasive set, with a judgment recorded on what was deliberately left.** All three genuine hits were pre-existing and in `plague_outbreak`'s premonition variants, not in the step prose this batch rewrote: two `something` ("as something fearful", "has reached something {name.first} cannot allow to fall") and one `way` ("in the way that disasters do"). They were fixed because `something` is in the **evasive** set, which is enforced at zero in *every* field class rather than outcome prose only. Left untouched: `tend_the_weary`'s "**Someone** nearby is struggling", because that is a step `narrative` — **scene** prose, where natural indefinites are ordinary English and explicitly permitted; it is nearly verbatim the canon's own example of *correct* prose, and rewriting it is precisely the contortion THR-899 exists to prevent. The probe deliberately applies the strictest `outcome` scope everywhere and therefore over-reports this line; the over-report is the probe's, not the prose's. One further probe artifact worth naming: the premonition variants use a **different placeholder family** (`{name.first}`, `{pronoun.subject_capitalized}`) that `enrichProse` is not the resolver for, so an `UNRESOLVED` report against them is a probe scope error rather than a content defect. Second-person and intensifier sweeps both returned zero.

**The `>50` regression lock is unchanged, deliberately**: at 88 it holds with margin, so no lowering was owed. The figure is carried forward so whichever batch crosses 50 inherits it rather than re-deriving it — that batch owes the deliberate lowering or retirement the ticket asks for, with the reason recorded.

The probe was a scratch test, deleted before commit; it names the family by **id list** per impediment #559, since the converter drops `encounterType` rather than renaming it. One new impediment logged (#562): the full suite reported `scripts/__tests__/impediment-id-allocation.test.ts` red on the first run and green on the second, and green in isolation both with and without this batch's changes — a git-shelling test flaking under 1008-file parallel load, which reads as a real regression at exactly the moment a closeout is deciding whether its own diff broke something. **No browser evidence owed and none claimed** — prose strings in a data module fire no UI-pillar path trigger. Sibling THR-1102 remains sequenced behind this campaign; 88 templates still consume the `{adj}` ladder, so it is not yet moot. Remaining: **88 templates across 6 families** — `explore` 30 (194 lines), `lead` 14 (99), `create` 13 (92), `acquire` 13 (107), `hire` 9 (74), `steal` 9 (71). (THR-1101, 2026-08-14)
