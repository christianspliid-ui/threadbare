# THR-1300 slice 4 — the batch report and the Package View

**2026-09-03 · content tooling + UI.** The undertaking line can now be *reviewed* the way the encounter line is. `undertaking:batch-report` is a renderer over the two checks' JSON — it runs neither, so it cannot disagree with CI — and it leads with the kind × CRUD grid with the batch's additions marked, because on this line the variance the director reviews for is not tone but whether the batch fills anything but C cells. A C-only batch is flagged in the report as REVISE trigger 1. The Undertaking Package View at `?view=cms#undertaking-packages` shows one template whole: the kind row and the cell it fills with the row's other cells as links (an empty D reads *nothing undoes it*), the board as words, cast, the write set, counter-play, the prose, and the contract verdict with its warn channel; `?batch=` compares six side by side.

## Measured

The batch report on the three exemplars (seed 42) wrote 3/3 live proved, 0/3 gate green (all three sit on the retrofit ratchet, which is what the ratchet is for), and raised the C-only flag — the exemplars are all creates. The Package View model builds every registry template with no numeral on the board and a verdict that agrees with the contract; the retrofit mark shows on a ratcheted template and clears on a passing one.

## What is deliberately not LIVE

`factory-pack-registry` joins the interface map badged UNVERIFIED-OK: the join is real and the compiler round-trip is proven on a fixture, but the aggregate ships empty until the pilot compiles the first template a live game can start. A green badge has to mean traffic.

## Left for slice 5

The pilot brief, Christian-approved in chat — the one HITL gate — and the six templates it drives through the line, sequenced after THR-1388's destroy-verb measurement.
