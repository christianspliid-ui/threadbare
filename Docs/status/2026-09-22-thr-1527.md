# 2026-09-22 — THR-1527: the appointment planter writes the promise with its full required set

**Ticket:** [THR-1527](https://linear.app/threadbare/issue/THR-1527) · **Project:** Thematic Pressure & Living World · **Pillars:** Engine (Content N/A — no template changed; UI N/A — no surface changed, the sheet and badge readers already coped with the missing flags). Bug found by THR-790's closeout `npm run test:heavy`.

## What was red

`src/engine/__tests__/edgeIntegrity.test.ts › THR-1177 — 150-tick seeded smoke › writes trade routes without a single schema warning` collected two `[GraphSchema]` lines on seed 42: the `owes_favor` edge `owes_favor_appt_seed_ua_269_slice.crossroads.carry_the_promise_0` was missing `redeemed` and `broken`. `EDGE_SCHEMA.owes_favor.requiredProperties` names five properties; the appointment planter in `encounterAftermath.ts` (THR-1479) wrote three plus the appointment block. Nothing warned while the Crossroads never reached a mortal; THR-1524 made it plant on seed 42 inside 150 ticks, so the write landed inside the smoke and `main` read red in the post-merge, non-required `Heavy simulation tests` workflow.

## The fix

The planter now writes `redeemed: false, broken: false` beside `grantedTick`, `magnitude`, `context` and the appointment block — the same shape the other three favour writers (`leverageOps`, `strategicGraphOps`, `secretGeneration`) already use. The kept arm removes the edge and the missed arm flips `broken` via `breakAppointmentFavour`; every reader (`describeAppointments`, `agentDetail.favorsOwed`, the badge builder) already read the flags with `!!` / `!`, so nothing downstream changes meaning — the schema is simply satisfied.

A fast-lane copy of the heavy assertion lives in `encounterSeeding-appointment.test.ts`: it spies `console.warn`, plants through the real aftermath path, and asserts both flags are `false` and no `[GraphSchema]` line names `owes_favor`. Red on the unfixed tree (`expected undefined to be false`), green after.

## Evidence

| Gate | Result |
| -- | -- |
| new unit test, unfixed tree | 1 failed — `expected undefined to be false` |
| three appointment test files, fixed | 3 files, 44 passed |
| `npx vitest run --project heavy src/engine/__tests__/edgeIntegrity.test.ts` | 17 passed, exit 0 (23.9 s) |
| `npm test` | 1283 files, 20741 passed |
| `npm run check:typecheck` | OK — 2828, unchanged from baseline |
| `npx vite build` | built in 9.98 s |
| 30-tick CLI smoke, seed 42 medium | tick 30, 494 agents, 69 events, traces present |
| `check:encounter-live` crossroads / full_moon_collection / full_moon_reckoning | checked 3, proved 3, failed 0, vacuous 0 |
