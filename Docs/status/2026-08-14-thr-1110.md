**Aftermath consequences can grant every attachment category the palette rule names — and the agreement path had been built all along.** THR-1082's palette rule calls all seven attachment categories legitimate consequence material; the effect vocabulary could reach two of them (`condition` via `condition_attachment`, `possession` via `spawn_artifact`), so an author reaching for the other five had to fake the consequence in prose — a chip claiming state nothing wrote, the exact pathology THR-971's single-sourcing rule exists to stop — or drop it. One additive `attachment_grant` member closes it for all five. **The find is that this is a dispatcher, not a new system**: `instantiateAgreementReward` has existed in `rewardPool.ts` since the attachment-slot design with **zero callers** — a fully-built dormant path, the THR-614 shape — and `instantiateReward` already resolved blessing/curse/bestowed_power/spell from the template's own type and subcategory. Nothing about how an attachment is written changed; what was missing was an aftermath's ability to ask for one. **The design question the ticket flagged — do agreements need parties? — was already answered by the substrate**: an agreement is edge-backed (`relates_to` with `agreement: true`) precisely because it is a claim *between* two entities, so `counterpartyId` is required for that category and a grant whose counterparty does not resolve writes **nothing** and traces why, rather than leaving a dangling edge. **The crossroads promise is now a thing the bearer holds**, verified end to end in the CLI: `A Promise Given` binds `asc.archetype.chaos_0` to a materialized `The Stranger at the Crossroads` with a 132-tick term matching its own Full Moon collection, where before it rode its `encounter_seed` alone — a scheduled encounter that nothing read and nobody could inspect. **Casting the stranger closed a second latent gap**: the accept path already carried `inheritContext: true`, whose whole purpose is to make the follow-up "star the same people", and with no support bundle there were no bindings to inherit while The Full Moon Collection's prose says *"The stranger is there before the light finishes arriving, same coat, same clean boots."* He is `must-persist` because a promise whose holder is collected at scene end is not a promise. **The bundle composes rather than replaces** — `withDefaultSupportBundle` returns early on any template declaring its own, so naming only the stranger silently dropped the wayside setting-class default (THR-1044) this encounter is staged with; caught by that ticket's own test, which is the gate earning its keep. The membership predicate is re-derived as an **executable** gate rather than asserted as a count (THR-688 rule A): a test walks all seven categories and fails naming any the vocabulary cannot grant. **1015 files / 16,177 tests green**, ratchet unchanged at 3448, build clean in 10.06s, 30-tick smoke clean (383 agents, 45 events). Three of the four failures this change first produced were real and mine — a `trace-vocabulary` scanner reading `effectDetail.category` as a *trace* category (renamed `attachmentCategory`), a tooltip over the 200-char bar, and the support-bundle default above. **Impediment #577**: the CLI's `aftermath pick` re-applies a reaction that already auto-applied on `tick`, duplicating every effect — the agreement edge *and* the Full Moon seed both landed twice, which reads as a fresh idempotency defect in whatever you just built and is neither fresh nor yours. No browser evidence owed or claimed — engine/content pillar, CLI acceptance, no UI-pillar path in the diff. (THR-1110, 2026-08-14)
