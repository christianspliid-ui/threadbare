# THR-1355 — the QA machinery gets its game back

**Shipped 2026-08-29.** R3-T3 of context-cleanup round 3 (THR-1350). Docs-only (skill files);
Engine and Content N/A. Audit: https://claude.ai/code/artifact/98713042-66d0-4a4e-8304-93339d0e3096

## qa-orchestrator

The rubric line said *"55 numbered laws, ratified 2026-08-06"* — every sweep since Law 56
landed (2026-08-16) audited consequence chips against a book that didn't contain their law.
Now 56, amendments through 2026-08-17. The two pre-flight URLs disagreed (`?view=game` vs
`?view=game&seeded` — half the sweeps booted identity-less); unified on `&seeded`. The finding
schema's example `location` pointed at the deleted V1 `HexMap.tsx`. The agent prompts emitted
`notionPrefix` (retired Notion tracker) while the orchestrator read `projectHint` — every
finding produced by the verbatim prompts was unroutable; the prompts now emit the schema's own
fields, `surfaceIds` included, and their key-files list drops five phantom filenames for the
live equivalents.

`test-surfaces.md` ("read before every sweep", dated 2026-03-10): six V1/retired rows
deprecated per its own rule (V1 map cluster S-004/S-030..033, RetinuePanel S-060), live rows
added (S-107 ThreadsPanel, S-108 HexMapV2 with the WebGL-invisible-to-Playwright note),
NarrativeLog → NarrativeFeed, and S-026 no longer instructs QA to verify the **rejected Agent
Wheel button works** — its presence is now a finding, not a pass. Totals re-counted (45 + 7).

## playtest-interface

RUBRIC self-certified "54 surfaces … met above" while the manifest has 63: added the five
missing sections (chapter-ledger, chapter-view, ul.dashboard/sidebar/detail-pane), removed the
phantom `game.retinue-panel`, absorbed the SettingsPanel-name baseline correction, and the
count line now states its own staleness hazard. SKILL.md stops hardcoding port 5173 (its
sibling's #1 "common mistake") and cites verification-gates § Browser-verify as the evidence
contract.

## hexmap

`hexmap-core`'s "13 named layers" table was wrong from index 6 down (live: 26 layers +
`LAYER_Z`); replaced with the live table plus a the-file-wins rule. Props contract marked
illustrative (live interface: 25+ fields). Terrain palette 30→32 (counted).
`hexmap-layers` §4 said fog is *off* by default via `?fog` while §6 and the code say ON via
`?nofog` — fixed. `directory-structure.md` regenerated: it listed 17 of 40 scene modules and
omitted `hooks/` and `lab/` entirely. **Deleted `.claude/skills/hexmap-renderer/`** — a strict
subset of hexmap-core loading on the same triggers, describing the coastline approach
`coastline-system.md` lists under "Failed Approaches (DO NOT Retry)" as current; canon
hex-map.md and CLAUDE.md now route to the two surviving skills.
