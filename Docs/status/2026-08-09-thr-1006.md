**The essence pool reads as a whole number everywhere, and the ticket's own diagnosis was wrong in the way that matters.** The reported string — `◆ 193.60000000000005 essence`, seventeen significant figures on a mortal-facing surface — was attributed to `NudgePhaseShell.tsx` / `buildNudgePhaseModel.ts`. It is neither: the producer is `EncounterVeil.tsx`, which has **three** diamond-glyph essence readouts, and the one the nudge stage actually mounts is the second. A first sweep grepped for `` essence` `` — backtick template literals — and found five raw interpolations across the adapters, all real and all fixed; it found **none of the three that mattered**, because they are JSX text (`&#9670; {essence} essence`) and carry no backtick. The browser is what caught it: the surface still showed the float after the "fix". **Two vacuity traps nearly certified that fix as working.** The pool sampled at `190` — an integer, which renders identically with or without a formatter — so a "no float leaks anywhere" assertion passed against a value incapable of failing. The evidence that actually holds reads the raw pool off the React fiber, asserts it is genuinely fractional, and requires the rendered string to **differ** from what raw interpolation would have produced: `rawPool: 191.20000000000002` → rendered `◆ 191 essence`, `differsFromRawInterpolation: true`. **The UI Laws then corrected the fix a second time.** Rounding to two decimals removed the noise and still broke Law 13, whose ratified 2026-08-06 exception permits a resource-pool balance as a *whole-number* numeral — `191.2` is not one. So the module carries two formatters with different jobs: `formatEssencePool` floors, for pool balances in chrome, and `formatEssence` keeps two decimals, for prices — because `0.05` is an authored card cost and flooring it would promise a free card. That split makes the Done-when literally true rather than approximately: the veil and the budget line now agree on **value and format** (`191` / `191`), where before they disagreed on both. The budget line's inline `Math.floor` moved into the shared formatter so the two readouts on one screen cannot drift apart again. Seven call sites in six files now route through the module; a unit test pins the reported value `193.60000000000005`, the decimals property across seven inputs, the Law 13 whole-number rule, fail-soft on non-finite, and its own falsifiability. 978 files / 15,459 tests green, build clean, ratchet **3452 → 3451** — the ActionsBlock fix cleared a pre-existing `TS18048` alongside the one it introduced. Browser evidence at a true 1920×1080 via Playwright, the sanctioned unattended route this run documented under THR-1059 an hour earlier. (THR-1006, 2026-08-09)
