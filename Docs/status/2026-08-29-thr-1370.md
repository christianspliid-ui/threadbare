# 2026-08-29 — THR-1370 · R5-T1 skills sweep (context-cleanup round 5)

Round-5 audit (https://claude.ai/code/artifact/eb5c933d-0620-4aaf-9bd4-e9821b8d39ad) found the content-authoring skills teaching three retired modes. This sweep cleared the skills partition:

- `template-encounter-rewrite` — the THR-447 populated-variants section no longer instructs authoring `authoredChoices` (mechanism note + procedural note repointed at `byOutcome`/branching promotion); `reputation_tally` chips stripped from the copy-paste skeleton and worked example (UI Law 56 rule 0d — sentence folded into `overview`); the composed-hand model (0–2 specials + `deal`, THR-1247/1248) replaces "4–8 authored StepNudges"; prose calibration repointed from soul-ferryman/borderland to `Docs/exemplars.md` + the Swollen Ford.
- `encounter-pipeline/agents/systems-prompt.md` — `AuthoredChoiceCard` removed from the live-primitives list (it told the critic not to flag the rejected model); `ActionStepBranch.decidedBy` listed instead, with an explicit flag-it instruction.
- `encounter-pipeline/SKILL.md` — "Conversion of the existing 28 is WS5" corrected: WS5 complete (THR-1086, zero shipped encounters author it).
- `attachment-pipeline` draft + editorial prompts — the revoked marquee-lore peak carve-out removed; the editorial trigger widened from "undeclared peak" to any peak on an attachment surface.
- `prose-vignettes-and-enrichment` — narrator-mode section added (the THR-1324 repair its sibling got); the Scene row no longer teaches emotional staging; the pre-v2 backstory voice ladder named as retired.
- `agent-analyser` — 9 reaches → 8; `EncounterTemplate`-era field names (`reachPrimary`, `locationTypes`) replaced with the live `UnifiedActionTemplate` shapes; corpus counts made predicates.
- Stamps: the three false-clean `prose@2` stamps now sit on content that actually conforms; `last_validated_against` bumped where instructions changed.
