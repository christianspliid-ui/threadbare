**The merge gate reported healthy for four hours while six armed PRs could not merge, because the detector was built for a different outage (THR-1013, 2026-08-06).** From ~16:14Z GitHub accepted our jobs and gave them no machine: `Detect code changes` — normally seconds — reported as started, executed **zero steps**, and was reaped at ~15 minutes as `cancelled`, taking the required check with it. `check:actions` answered `healthy / startupFailureCount: 0` throughout, across three separate lane runs, because **every clause of the billing signature was absent** — conclusion `cancelled` not `failure`, duration 15–31 minutes not 3–5 seconds, and no annotation at all. A detector built for one outage shape had nothing to say about the other and said it confidently. **The cause is GitHub-side and now bounded, which is the verdict the ticket asked for:** a `workflow_dispatch` forced at 20:05Z ran `Detect code changes` green in 31s on the same workflow file, so neither the file nor the runner pool was ever at fault. **The fix is a second verdict, `stalled`, keyed on the signature the first one cannot see** — jobs present, zero steps across all of them, past a 600s floor that separates a hang from the two neighbours it would otherwise be confused with (a billing failure completes in 3–5s; a concurrency supersede reaps a queued run promptly). It deliberately **does not** stand the lane down: THR-768's stand-down exists because a billing block makes the gate *vacuous* — a `skipped` required check satisfies branch protection — whereas `cancelled` blocks, so armed PRs merge themselves on recovery and idling the lane would forfeit an hour of delivery against a risk that is not present. What was missing was the sentence, not the halt. **Direction 3 was implemented and labelled as not-the-fix:** `detect` was the only job with no `timeout-minutes` and now has one, with a comment stating plainly that it bounds *execution* while this hang was *queue* time, so no future session mistakes it for the remedy. **The second hole found while measuring is filed, not folded in** — PRs #1327/#1328 never had a `github-actions` check suite *created at all* (Vercel's was, so the webhooks were fine), and a probe that classifies runs it can list is structurally blind to a run that never existed; that is THR-1014, with the check-suites predicate it needs. Live proof: the widened probe reads `stalledCount: 6` on the same window it previously called healthy.
