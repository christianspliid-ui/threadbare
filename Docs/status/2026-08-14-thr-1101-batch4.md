**The trade family stops reading as mad-libs** — THR-1101 batch 4, resumed from batch 3's checkpoint, which named `trade` (15) or `assist` (15) as the natural next single-run slice. All 15 token-carrying `encounterType: 'trade'` templates authored out of the `{adj}`/`{verb}`/`{noun}`/`{action}` shape — `merchants_gambit`, `caravan_deal`, `guild_negotiation`, `smuggler_pact`, `tribute_exchange`, `barter_survival`, `mystic_trade`, `the_haggle`, `market_day_festival`, `foreign_trader`, `barter_supplies`, `assess_holdings`, `barter_with_travelers`, `dead_drop`, `negotiate_passage` — 109 token lines. **The family is drained**, the stateable predicate this campaign has used since batch 1 in place of a count that rots. Register is Merchant-Consortium per the `template-encounter-rewrite` voice guide — calculation as second nature, trust as currency, sentences that measure — calibrated against `encounter.trial_by_combat`, the THR-1036 exemplar. Predicate re-derived at pickup: **118** of 168 carried tokens, matching batch 3's stated figure exactly, so the scan is now stable across two runs after the JSDoc-continuation fix (impediment #558) landed in it. After this batch: **103**.

**Batch 3's two-failure-shapes finding replicated, and it should now be treated as the family-scoping rule rather than one family's quirk.** The split here is 7 full mad-libs to 8 wedged-token — near-identical to `build`'s 7/9 — where a full mad-lib has every prose field assembled from tokens and wants wholesale rewriting, while a wedged-token template is *already authored prose* with a token driven into a finished sentence and wants surgical removal. The wedged half again produced the worse readings, because a parasite on a good sentence is more conspicuous than uniform noise: `the_haggle` had the actor leave a stall "`{adj}`-handed" where the sentence plainly wanted *empty*-handed, `market_day_festival` rendered "works the festival `{adj}`ly", and `foreign_trader` opened a failure with "Something `{verb}`s in translation". So: grep the lines, then read a sample before committing to a slice — 109 lines here was roughly batch 2's 82 in effort, not batch 3's 123.

**Rendering rather than reading caught three agreement defects, the third run running that this pass has paid for itself.** `{They}` resolves to a **singular** pronoun, so a bare verb after it renders wrongly: one instance was **introduced this batch** (`assess_holdings` — "Knowing what she hold matters less than knowing what she need") and **two were pre-existing** (`barter_supplies` — "is worth what she carry out"; `assess_holdings` — "knows exactly what she have"). All three fixed with `{s}`, the engine's agreement token, except where `{s}` cannot reach: it appends a bare `s`, so `carry → carrys` and `have → haves` are both wrong and those two needed rephrasing to `walk{s} out with` and `hold{s}`. That is the same `{s}` ceiling batch 3 hit on `carry`, now met twice more — worth treating as a known limit rather than rediscovering per batch.

**Vagueness swept 3 → 0 at the strictest applicable scope**, with a judgment recorded on what was *not* changed. Two `way` hits (`smuggler_pact` — "made the slow way", "binds the way this trade binds") sit in `narrativeTemplate`, a **scene** field where natural indefinites are ordinary English and owe nothing; they were fixed anyway because the campaign applies the `outcome` scope everywhere. One `somewhere` was a genuine failure — `negotiate_passage`'s `failureAfterimage` is outcome prose, where natural indefinites are enforced at zero. Four remaining hits are all the **comparative conjunction `rather than`** tripping the intensifier substring match; they are warn-level by construction (THR-899: intensifiers are never a failure) and were deliberately left, because rewriting a correct comparative to dodge a warn-level substring is exactly the "dodging a banned word at the prose's expense" damage the canon's rule-zero warning names. Second-person sweep also run: one hit found and fixed (`dead_drop` — "as if you were never there" → `{actor}`), a class no prior batch happened to meet.

**The `>50` regression lock is unchanged, deliberately**: at 103 it holds with wide margin, so no lowering was owed. The figure is carried forward so whichever batch crosses 50 inherits it rather than re-deriving it — that batch owes the deliberate lowering or retirement the ticket asks for, with the reason recorded.

The probe was a scratch test, deleted before commit; it re-hit impediment #559 on first run (filtering `ENCOUNTER_TEMPLATES` on `encounterType` returns **zero**, because the converter is a field allowlist and maps it to `crudType`), so it names the family by id instead. **No browser evidence owed and none claimed** — prose strings in a data module fire no UI-pillar path trigger. Sibling THR-1102 remains sequenced behind this campaign; 103 templates still consume the `{adj}` ladder, so it is not yet moot. Remaining: **103 templates across 7 families** — `explore` 30 (194 lines), `assist` 15 (107), `lead` 14 (99), `create` 13 (92), `acquire` 13 (107), `hire` 9 (74), `steal` 9 (71). (THR-1101, 2026-08-14)
