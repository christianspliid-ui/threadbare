# THR-1403 — the flip: the board walks cells

**2026-09-08 · Engine (the model flag, the division rule wired, the calibration) + Content (the four retired, the canon) · project Thematic Pressure & Living World**

`UNDERTAKING_MODEL` is `cells`. What a mortal sets their hands to is derived — the active ambition's category picks the verbs, their two leading Reaches pick the kinds, Eye adds watching on every kind — and the authored templates are absorbed. THR-1392 slice 4b; the map's step recorded rather than surprised.

## What flipped

- **The division rule, wired** (`src/engine/divisionRule.ts` over THR-1398's tables, which THR-1434 had already shipped for the codex): `deriveDivisionCells(actor, category)` at the board, the profile's hand-listed `cells` added on top, the list rotated by tick and actor so the per-actor cap starves no cell by position. `leadingReachPair` moved here from `calling.ts` (which re-exports it) so the generator and the calling read one function without importing each other.
- **The per-ambition cap does not bind a cell** (`UNDERTAKING_MAX_CANDIDATES_PER_CELL` targets each; `STRATEGIC_MAX_CANDIDATES_PER_AMBITION` stays a template-model lever). Six cell candidates from one ambition where the cap allowed five — pinned.
- **The 64 are absorbed.** Under `cells` a profile's `templateIds` are not walked. `strategic_improve_masterwork` and `strategic_commission_quest` are gone (nothing named them). `strategic_expose_cache` and `strategic_train_apprentice` leave every profile but keep their definitions — the first as the intelligence-cache kind row's counter-play so the legacy registry gate stays sound, the second because the mentorship initiative starts it by id. **Mentorship is its own initiative, not a cell:** it rides ahead of the spread for `MENTORSHIP_AMBITION_CATEGORIES` (mastery, discovery — the two whose profiles listed it) with its apprentice gate intact. `cell.destroy.route` left the two hand lists that named a cell the grid does not have.
- **The legacy arm stays.** The per-template completion switch, the packs and the kind rows are kept for the review levers and the contract tests, which start authored templates by id; `startUndertakingForReview` walks a legacy template under `'templates'` explicitly. Unreachable from the board; deletable later, not now (deviation from the plan's "deleted when the flag is cells", recorded on the ticket).
- **Calibration folded in** (THR-1402's decisions): project verbs run 2 / 3 / 4 checkpoints by tier (12 / 18 / 24 progress — the documented 18 at T2 restored); lair elites are minted `spotlightTier: 'ambient'` and decide nothing; an item's tier falls back to its class through `ITEM_TIER_BY_CLASS` when unstamped.
- **`npm run census:cells`** — the prototype's two-seed census, ported and committed: starts, completions, refusal reasons, `undertaking_cell_unreachable`, a calibration table and the multi-tick finish rate per seed.

## Measured

Two seeds, 150 ticks, medium, model `cells`:

| | seed 42 | seed 99 |
|---|---:|---:|
| starts · completions | 163 · 136 | 74 · 49 |
| distinct cells started | 14 | 23 |
| multi-tick finish rate, durations 4 / 6 / 8 | 19.0% (4/21) | 24.4% (10/41) |
| multi-tick finish rate, durations 2 / 3 / 4 | **29.6% (8/27)** | **53.7% (29/54)** |

Pooled: 22.6% → 45.7%, against the target of about half. `npm run census:undertakings` (the plan's acceptance envelope) **fails the variety floor** — 14 · 23 distinct cells against 25 — and the starts-per-mortal floor on seed 99. The derivation is not the cause (29–42 derived cells per calling); supply is: no routes, companies, networks, agreements or standing scores exist at seeding (`no_object_in_range` on 20 cells), no quarrels license the gated verbs (`no_motive` on every destroy / lower / seize cell), and one cheap instant cell (`observe × place`, 52 of 163 starts) crowds seed 42 and pushes its undertaking share to 36.1% (envelope ≤ 35). Recorded on THR-1437 as its Done-when: the seeding is what unlocks the cells, and the census re-runs there.

## Left honest

- The census verdict is FAIL on the model the flip ships. It ships anyway because the failure is diagnosed as object supply, which no change inside this ticket can fix, and the next ticket exists to fix it; holding the flip would measure THR-1437 against the model it is not for.
- A stranded threaded mortal now watches what there is for two or three days before idling into forced travel — the balance contract's tick budget doubled to say so.
- The `destroy × Standing` two-party quarrel semantic (THR-1436's find) is reviewed and kept: the cell targets *another's* standing by ownership rule, so setting two parties at odds is the intrigue it names.

## Proof

`divisionRule.test.ts` (6 — the derivation, the rotation, derived cells walked, none under templates, mentorship beside the spread, the cap not binding), `undertakingCellWalk.test.ts`, `lairEscalation.test.ts` (the elite stamp, falsified), `undertaking-objects.test.ts` (the class fallback, the stamp winning), `strategicKindReachability.test.ts` and seven legacy-arm suites pinned to `'templates'`. `npm test` 1185 files; `test:heavy`; `vite build` 10.1s; 30-tick CLI smoke; ratchet unchanged. Browser-verify exempt: engine and data only, no component touched.
