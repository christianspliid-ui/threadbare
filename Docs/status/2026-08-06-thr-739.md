**Four surfaces hand-rolled the same player-cast sequence, and the copy nobody mirrored an edit into has been untraced its whole life (THR-739, 2026-08-06).** The agent drawer (`useAgentInteraction.handleWheelSlotClick`) and the non-agent drawer (`GameView.handleNonAgentSlotClick`) each carried their own buffs → RNG → `createUnifiedAction` → essence-clamp → `recentEvents` sequence, and the drift was already on the record rather than hypothetical: the non-agent path shipped **without a dispatch toast** until THR-727 fixed both paths *separately*. **The find is the one THR-727 did not reach.** The non-agent drawer never wrote the `ACTION_START` timeline event at all — so every cast the player has ever made at a location, hex, artifact or faction was absent from the ascendant's causal trail (NFP #2), while the identical cast aimed at an agent was recorded. No gate could see it: both paths dispatch, both resolve, both produce a Divine Receipt, and the missing half is an *absence* in a trail nobody diffs. `src/engine/playerCastDispatch.ts` now holds `preparePlayerCast` + `commitPlayerCast`, and **all four** production constructors route through it — both drawers, the intervention confirm, and `__DEBUG.fireAction` — so `source: 'player'` appears exactly once outside tests. **Two optional params rather than one rigid shape, and the reason is a real bug avoided:** `applyBuffs: false` + an explicit `essencePaid` exist because the intervention path has already priced its cast through `executeIntervention` and the debug bridge fires at list price — running the buff pass there would consume a one-shot Recede/Focus on a cast that never saw the discount, spending the player's buff on nothing. That is pinned by a test asserting the buff survives on the node, not left to a comment. **Surface-specific feedback deliberately stays with the surface** — sphere audio, particle burst, drawer close, card-play animation, and each drawer's own dispatch copy — because the alternative is an engine module owning UI; only the `buffParenthetical` string is shared, since that is the one piece the two drawers had already worded by accident rather than by decision. **The adjacent cleanup turned out to be a prop kept alive by its own tests:** `ActionCard`'s `outcomeBand` had no production caller and could not have one — `playing` is dispatch-time state and the band is unknowable until the action resolves ticks later, which is exactly why THR-727 made the Divine Receipt the band surface. Its three asserting tests were the only callers in the repo; removing the prop and the band-keyed overlay with them leaves a band-agnostic spent face and the tests that prove it. Two behaviour deltas stated rather than smuggled: the non-agent drawer and the debug bridge both **gain** the `ACTION_START` event, which is the drift closure, not a regression.
