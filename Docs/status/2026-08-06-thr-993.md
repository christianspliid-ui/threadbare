**The ticket asked for the producer to be wired; the producer had been deleted on purpose, by a later ticket, on the director's instruction (THR-993, 2026-08-06).** THR-993 was filed off THR-646's finding that `encounterCard` has zero writers and `EncounterNotificationCard.tsx` is dead at runtime — both true — and concluded *"THR-636 shipped the card component and its type, but not the producer."* That last clause is the part that does not survive checking. THR-636 **did** ship the producer, `buildEncounterCard` in `useEncounterNotifications.ts`; **THR-664 deleted it three weeks later**, and `git log -- ToastStack.tsx` orders the two unambiguously (`974c4e44` then `a5f5c34b`). THR-664 opens on a verbatim Christian directive — *"I want encounter toasts replaced with a clickable icon on all relevant entities involved in the encounter … not in a global transient queue"* — with the Done-when **"Zero encounter-related toasts appear in the global ToastStack"** and spec item 3 *"don't leave dead code."* So implementing this ticket as written would have re-toasted encounters and failed a shipped, director-set Done-when. **The remedy inverts: delete the residue, finishing THR-664's own cleanup** — the card component, the `EncounterToastCard` type, the `encounterCard` field with its `ToastStack` render branch and aria-label fork, and two zero-consumer constants. The surviving comment in `encounterBadgeModel.ts` had been naming the situation the whole time ("the **retired** encounter toast card"). **Deletion self-pins where a test could not**: the field no longer exists, so re-adding a writer cannot type-check without re-adding the type. **The real find is the guard underneath.** The invariant that actually keeps toasts from returning is that the three encounter events emit no `notification` directive — `routeNotifications` skips anything without one — and nothing tested it. What did exist was a test asserting the **pre-THR-664** contract (`notification.channel === 'toast'`), green for three weeks because every assertion sat behind an `if (retinueEvent && …)` guard that stopped matching when the directive was removed. Worse, its whole describe block was inert: the fixture filtered town encounters to those *absent* from the unified pool, which the seed-system-v2 migration reduced to **0 of 74**, and each test opened with `if (available.length === 0) return`. Three tests, zero assertions executed. All three now run against a real population, with the empty-population guard turned into an assertion so the block fails loudly instead of greening silently.
