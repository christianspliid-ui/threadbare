**Context-cleanup R2 — skills sweep (THR-1340).** The `state-of-game-design` reference shards — routed to every design and content task — stopped teaching the pre-nudge game: the cosmology shard now states the Creation Spheres as four opposed pairs (was "8 independent"), teaches the nudge model in the curated-moment beat, carries the THR-870 parked-pivot guardrail and the Quintessence-is-not-a-Reach note; the verbs-resolution shard gained the five-band ladder with success-at-cost dominance, the floor-before-cards rule, the five-name influence ladder with Aspect as the Apotheosis milestone, and the nudge surface; the deprecated shard — the guardrail file — gained the entire post-May rejection roster (authoredChoices, EncounterTemplate, verb trio, retired pole-name generations, lyrical register, static factorLines, fiction-first order, player-facing numbers, intel-gating). `encounter-actor-systems` dropped its `EncounterTemplate` rows and four-band doubles ladder for the live five-band resolver and flags its wiki page's stale sections. `action-catalog-design`'s "not yet settled" surface question updated to the 2026-07-30 format lock. The retired game name swept out of six skills' agent-facing prompts (qa-orchestrator ×3 files, design-audit-pipeline ×3 prompts, gamedocumenter, playtest-interface); the retrospective's wiring-guide audit step repointed off the removed `EncounterTemplate` type; the QA surface registry dropped its AgentWheel coverage row (the component's dead code spun off separately). Audit: https://claude.ai/code/artifact/e591d762-eef7-4baf-82fe-73f69e4c3a1d
