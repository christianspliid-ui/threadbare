# 2026-09-25 — THR-1600: "Story so far" names the mortal

**Ticket:** [THR-1600](https://linear.app/threadbare/issue/THR-1600) · **Project:** Onboarding & First-Run Experience (Cold playtest · round 1) · **Plan:** none (defect with a stated fix) · **Pillars:** Engine (the empty-thread line gets its name filled in) · Content N/A (the pool's lines are unchanged) · UI N/A (the panel renders the string it is given — THR-688 rule C, vitest evidence only).

## What shipped

- **The empty-thread line is filled in.** `composeThreadStory` (`src/engine/threadDigest.ts`) returned its `EMPTY_THREAD_LINES` pick verbatim, so every mortal with fewer than `STORY_DIGEST_MIN_BEATS` beats read *"The thread of {name}'s story…"*. A new `buildEmptyLine` applies the same `{name}` substitution the tension line already used.
- **Test.** `threadDigest.test.ts` walks 120 ticks of the empty path. It asserts no `{placeholder}` survives, all three pool variants appear, and the two name-bearing variants carry the agent's name. It fails on `main` (`'{name} has barely begun…' not to match /\{[a-z_:]+\}/`) and passes on the branch.
