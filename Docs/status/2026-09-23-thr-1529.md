# 2026-09-23 — THR-1529: a killed pickup run no longer strands its work invisibly

**Ticket:** [THR-1529](https://linear.app/threadbare/issue/THR-1529) · **Project:** Continuous Improvement · **Pillars:** process only (skill prose + one git probe); Engine / Content / UI N/A. Filed by the 2026-09-23 workflow retro (impediment row 1059).

## What shipped

**WIP push before verify.** `pull-work` gains § *Mid-run — WIP push before the verify phase*: once the implementation is written, a code-track run commits (no close keyword) and pushes its branch before the long gate run (tests, heavy lane, ratchet, build, Playwright). The closing commit comes later on top, never as an amend. The `tb-opus-pickup` prompt carries the same rule in step 4, with the live prompt and its mirror `Docs/ops/scheduled-task-prompts/tb-opus-pickup.md` byte-identical.

**Resume looks at worktrees.** `pull-work` Step 1.8 gains a *stranded-work probe* that runs before a checkpoint-less claim is re-implemented. It checks for pushed branches naming the id, then for dirty worktrees sorted largest first, with the settings/codesight debris filtered out. That debris is on nearly every worktree: 238 of this box's worktrees read dirty unfiltered. Tested against the real incident, THR-1521's stranded worktree (`quirky-knuth-1bb9b6`, 39 paths) sorts first. A hit is committed in place and pushed before anything else.

**The brief stops saying "nothing lost".** `keep-work-flowing-cc` step 2 may not call an In-Dev claim "unstarted / no code / nothing lost" without running that probe. A dirty worktree is reported with its idle age against the reaper's 180-min guard, and an unrunnable probe reads "liveness unknown".

## Why

A usage-limit death gets no chance to post the THR-632 checkpoint. THR-1521's whole slice sat uncommitted for ~19.5 h while seven hourly briefs told Christian nothing was lost.

## Evidence

Docs track (THR-917 `classify:diff` → docs-only): `check:generated-freshness`, `lint:plan-doc -- --staged`, `check:impediment-ids` — see the PR body.
