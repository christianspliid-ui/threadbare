**The file:line the ticket named had been unreachable for eighteen days, and the leak it described was live one component over (THR-1008, 2026-08-06).** The UI Laws review filed two findings against `ThreadsPanel.tsx` — the Encounter Pool modal rendering `Score 0.42 · completion 65% · travel 0.10` and a pipe-delimited meta strip. Both citations were correct about the code and wrong about the surface: **`setEncounterPoolModal` was only ever called with `null`**, so `<Modal open={state !== null}>` could never open. THR-664 removed the row button on 2026-07-19 and said so in its own commit — *"EncounterPoolModal is retained per the ticket's cleanup rule but now has no entry point"* — which is why the ticket's own first step was *verify the noun*. Doing that found the live twin: `EncounterDecisionPanel` in `ThreadDetailView.tsx`, reached by clicking any thread row, printing **`Viable now: 40`, `Template: encounter.confront_the_unknown`, `Travel cost: 0.5808826068191627`, `Best score: 0.54`, `Funnel: Cached 5544 -> Awareness 3308 -> …`** — a raw internal key and a 19-significant-figure float, worse than what was filed. Dead copy deleted (291 lines); the live diagnostics moved behind the designer view, whose own module already states the rule (*"the player surface must never grow a numbers toggle"*). **The fix that the fix forced:** rank and score were doing double duty — they leaked magnitudes *and* were the only thing telling rows apart, since the pool holds one template at many destinations (nine consecutive identical rows, measured at tick 6). Dropping them alone would have traded a numeral leak for an undifferentiated wall, so normal play now collapses the pool through `groupEncounterPoolCandidates` — the grouping the deleted modal offered as a view mode, rescued rather than deleted with it. Sustained-control rows lost `⤓ 12/tick` and a `Runway: ~6 ticks` hover for banded words; all six `title=` attributes became the shared `Tooltip`, four new `ui.*` concepts registered. Two tests that pinned the *defect* were repointed to assert both sides of the gate. Verified at 1920×1080: ten distinct named options, **zero numerals in the whole section**, 0 console errors; designer view restores every figure.
