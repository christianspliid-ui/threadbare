**The unattended Browser pane is now a documented environment fact rather than a per-session rediscovery.** Four impediment rows on four consecutive days — #448 (08-06), #472 (08-07), #493 (08-08), #499 (08-09) — recorded the same class: in a scheduled run the Browser pane's *page* is unavailable, and nothing says so until the call that needs it fails. `CLAUDE.md` § Known Sandbox Limitations now carries one bullet naming all four, placed next to the `preview_start` port-mismatch row it shares a diagnosis path with. **The promoted text states the sharpest version, not the first:** the four rows show the finding moving, and a bullet written from #448 alone would have taught the wrong workaround. #448 saw only `computer{action:"screenshot"}` fail; #472 added that `read_page` returns `(empty page)` at `Viewport: 0x0` and `find` then fails naming a cache that can never be populated in that mode; #493 established the pane as a working *inspection* surface that dies only at capture, and measured ~10 min re-running an entire encounter-driving sequence under Playwright because the two surfaces do not share a page; #499 then had `navigate` denied outright, one step earlier, so #493's "drive with the pane, capture with Playwright" split was not available either. The bullet therefore leads with #499's rule — **decide attendance before any preview setup, not at the capture** — and says explicitly not to rely on the narrower split, because a session that adopts it will invest the whole setup before finding out. What stays true is kept in the same sentence so the promotion does not over-correct: `preview_start` is still the only sanctioned way to launch the dev server, `preview_logs` is still authoritative for Vite's real port, and attended sessions are unaffected — there the pane sets the viewport exactly, which Claude-in-Chrome's `resize_window` cannot (THR-796). Every one of the four rows individually sits below the materiality bar; #499's own text calls itself sub-bar. The promotion is the retro-bundling clause working as designed — four sub-bar rows aggregating into one ticket with the accumulated cost quoted — and the cost being removed is not the ~2–10 min per occurrence but the re-diagnosis, since each of the four failure texts reads as a tool-ordering mistake or a permissions prompt rather than an environment fact. Docs-only diff (`CLAUDE.md` plus closeout docs), verified empty against the THR-917 classification predicate, so it ships on the docs track: no `npm test`, no typecheck ratchet, no `vite build`. (THR-1059, 2026-08-09)
