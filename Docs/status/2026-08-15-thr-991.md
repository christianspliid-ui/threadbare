**The glossary can now say "we considered this and refused it" without lying about the term's history.** `ULTermStatus` was the closed set `canonical | proposed | deprecated | unknown`, so a term that was *never admitted* could only borrow `deprecated` — which in this glossary means the opposite thing: was canonical, has been retired, kept so historical records resolve. THR-873 hit this shipping **Unset Weave** (the "still-settling past" framing Christian rejected 2026-07-30) and took the workaround, carrying the distinction in a prose paragraph instead. `rejected` is now a real status, admitted by the parser, carried into the generated JSON, and given its own badge. **The badge is violet, not a second red, and that is the load-bearing choice** — `rejected` and `deprecated` carry opposite histories and the same instruction, so a badge that renders correctly while being visually indistinguishable from `deprecated` would satisfy the type checker and still fail the only reader this status exists for. **The double warning was the tell that the old behaviour was worse than "unclassified"**: an unrecognised value degraded the term to `unknown`, which then tripped the *second* guard for a missing `**Status:**` line — so one entry emitted two warnings and rendered as though nobody had ever classified it, the strictly worst outcome for a term whose whole purpose is to carry a do-not-use verdict. Both warnings are gone; the generator now runs with zero `missing_status` output. **The regression test asserts the warning list is empty rather than that the status parsed** — reading `.status` alone would have gone green while the second warning still fired — and it was falsified before being trusted (parser reverted, test failed `expected 'unknown' to be 'rejected'`, parser restored). **Browser-verify took the sanctioned jsdom substitution**: `preview_start` was refused with the unattended-session string (impediments #546/#574/#580), so the evidence is a render assertion on the real dashboard proving the badge reaches the composed surface through *both* independently-declared `STATUS_COLOR` maps (table and detail pane) and that its colour differs from `deprecated`'s — a case where jsdom is genuinely sufficient rather than a weaker stand-in, since the change is a token plus a label with no layout movement. The README footer was recounted from `**Status:**` lines (123 canonical / 2 deprecated / 1 rejected / 1 proposed = 127) and its THR-873 clause corrected: it had claimed Unset Weave shipped "at the new `rejected` status", which was aspirational — it shipped as `deprecated` and was flipped here. 1017 files / 16,255 tests green, ratchet unchanged at 3448, build clean in 10.54s. (THR-991, 2026-08-15)
