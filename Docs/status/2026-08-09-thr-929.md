**Twenty-one shipped templates now name the result instead of gesturing at it — the first and largest sub-batch of THR-899's content residue.** THR-899 split the vagueness lexicon by intent and scoped it by field class, which enforced `thing` / `way` / `ways` / `nothing` / `anything` / `whatever` in **outcome prose** for the first time; `countVagueness()` had never matched them, so 30 shipped templates went from clean to failing without anyone writing a word. This pass clears 21 of them — every one now reads `vaguenessByClass.outcome === 0`, verified by re-running `audit:nudge-batch` over the full predicate before and after. **The rewrites are the point, not the counter:** `loc.awaken_spirit`'s success said *"someone answers"* when the whole action is waking the spirit of the place, so it now says the spirit does; `ac.senior.planar_probe` said *"something on the other side has taken note"* across four separate slots, now consistently *the watcher*; `tavern.overheard_rumor` failed with *"Nothing usable surfaced tonight"* and now says *"No name, no route, no date"* — the three things the encounter is actually about. Scene prose was **not** touched anywhere, per the ticket's explicit prohibition: re-tightening it is what produced the "asking the room for them by name" contortions THR-899 removed, and every remaining `s`-column hit in the after-table is a natural indefinite doing ordinary work. **The nine `encounter.*` templates in `src/data/encounter-content.ts` are deliberately not in this diff** — THR-860's PR #1114 holds that exact file open (held behind THR-883, `mergeStateStatus: DIRTY`), and the mutex's stated reason names it, so the ticket stays In Dev with a checkpoint rather than shipping a conflict into a parked PR. **Two failures cleared as collateral and are worth naming because they were not targeted:** `social.intimidate` dropped to one not-X-but-Y (the second was *"the dynamic is less favorable than expected"*, a single outcome line tripping two detectors at once), and `npc_bribe` fell to one second-person once *"You have bought nothing but suspicion"* became *"The coin goes back in the purse; the suspicion stays."* **What is deliberately not claimed:** three templates still fail — `npc_eavesdrop` and `ag.senior.deep_expedition` on abstraction **and** second-person, `social.spy_on` on abstraction — and none of it is in scope. Those detectors are untouched by THR-899, the failures predate it, and the hits are unreachable from here: both second-person counts stay ≥ 2 even with every outcome-side `you` removed because the rest sit in scene prose, and the abstract-noun proxy is firing on `expedition`, `surveillance` and `fragment` — the templates' own subject nouns, which the detector module itself documents as a known limit and "a **ranking** signal, not a verdict on any single line". Filed as THR-1066 rather than gamed. So `audit:nudge-batch` still exits 1 on this set, by design, which is logged as impediment #517: the harness gates on every detector while a scoped ticket owns one, so a correct scoped batch cannot produce a green report. 981 files / 15,492 tests green, ratchet unchanged at 3451, build clean, smoke reached tick 30 with 383 agents; browser-verify exempt — content-only, no UI-pillar file in the diff. (THR-929, 2026-08-09)
