**The aftermath asked the player to "Deepen the connection to `{cast:patron}`" — and the same defect sat one layer below it, on a second surface nobody had looked at.** Law 43 is unambiguous — *"a raw `{token}` reaching a screen is a released defect with a regression lock"* — and `buildUnifiedEncounterStageModel`'s `buildAftermath` was enriching the overview (`enrichProse(rawOverview, ctx)`) and every change detail (`enrichProse(change.detail, ctx)`) while the reaction mapper three lines below passed `label` and `intent` straight through. The choice-card mapper in the same file had been enriching both since it was written, so the gap was one mapper deep, not a missing capability. **The membership predicate held exactly where the ticket left it**: 32 `label`/`intent` lines carrying a `{placeholder:…}` token across 6 files under `src/data/encounters/`, re-measured at pickup rather than trusted — a class, not the one line that was reported.

**The ticket's instruction to check the engine's own reaction path is what made this worth more than a three-line fix.** `playerReceipts.ts` has the identical shape: line 214 enriches the overview through a private `enrichOverview` helper, and line 234 hands `reactions` to the receipt untouched, where `DivineReceiptModal` renders `reaction.label` and `reaction.intent` verbatim. Same defect, second player surface, reached by a different route (a player-cast action rather than a threaded agent's encounter). **And fixing it revealed a third layer that would have made the fix look like it worked while doing nothing useful**: `resolveSceneCastContext` returns `undefined` unless it is given the template's `supportBundle`, and the receipt path was passing only `supportBindings`. Without the bundle every `{cast:*}` token *strips* rather than resolving — fail-soft, so Law 43 would have read clean, and the player would have been shown "Keep watching ." with the name silently deleted. The attended stage adapter has threaded both since THR-696; this path had half. The bundle is one line and the template was already in scope.

The receipt-side fix is written so the context is gathered **at most once per receipt and only when some field actually carries a placeholder**, preserving the original overview-only fast path rather than paying `gatherNarrativeContext` twice per receipt on prose that has no tokens in it.

**Both halves were falsified before being trusted, and both failed in the exact shape the ticket reported.** Stashing only the adapter fix: `Received: "Watch {cast:tessaly}."`. Stashing only the receipt fix: `Received: "Keep watching {cast:celebrant}."` — the second one against a template the test *discovers* by predicate (`supportBundle?.some(s => s.kind === 'actor')`) rather than naming, so it cannot rot into a vacuous pass if that template changes. The adapter test needed one correction mid-write that is worth recording: the first draft borrowed `RIVAL_SHRINE_BETRAYAL_TEMPLATE` whole and asserted against injected reactions that never rendered, because the template carries an `aftermathConfig` whose authored variant *replaces* `summary.reactions` — the test was measuring the authored variant, not the mapper. Keeping the support bundle and dropping the `aftermathConfig` is what put the mapper under test.

**Browser-verified on the reported route** — `?view=game&seeded&size=medium&spawn=enc.letters_of_introduction`, driven with `__DEBUG.suppressBeats(true)` + `__DEBUG.tick(6)` rather than a hand-rolled dismissal loop, at a genuine 1920×1080 with a wrong-tree guard that fetched both changed modules from the dev server and confirmed the fix present in the served build. Zero `{…}` tokens anywhere in the aftermath dialog; the reported line now reads **"Deepen the connection to Councilor Maevis Drent."**, and its intent resolves the same patron twice more. `getActiveUIState()` → `openModals: ["EncounterVeil"]`. One console error, `localhost:3001/api/tts/health ERR_CONNECTION_REFUSED` — an unrelated TTS sidecar that is not running locally, present before this change.

**UI-Laws judgment.** Law 43 is the law this ticket exists to enforce and it is now clean on both surfaces, each with a regression lock. Law 14 improves for the same reason — `{cast:patron}` is the rawest form of an internal key. Law 13 holds (no magnitudes touched). Law 37 holds: the aftermath still wears the encounter's chrome — title, subject strip, `all 2 resolved` step dots. **Law 1 / Law 21 do not fully hold, and not because of this change**: the enriched name renders as plain text in the reaction option while the `SEED` line directly above it renders the *same* entity as a link. Before this fix the reaction named no entity at all, so the gap is revealed rather than introduced — and closing it is a genuine design decision, since `EncounterStageAftermathReactionModel.label` is a bare `string` with nowhere to put link annotations, and a link nested inside a reaction `<button>` needs an interaction ruling. Filed as THR-1084 rather than smuggled in here. Also visible in the capture and already owned elsewhere: `content_grant` named to the player in authored prose, which is THR-1051's exact scope — noted, not double-filed.

988 files / 15,601 tests green, ratchet unchanged at 3450 (one net-new error caught and fixed rather than baselined — `EncounterSupportBundle` is declared in `types/encounter` and only re-imported by `types/unifiedAction`, never re-exported), build clean. (THR-1050, 2026-08-10)
