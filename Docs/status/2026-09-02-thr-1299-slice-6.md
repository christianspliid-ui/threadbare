# THR-1299 slice 6 — the docs sweep, the bridge, and the close

**2026-09-02 · code + docs.** Plan doc 5/6 of Proactive Agent Actions shipped as six strangler slices across one single-executor session (PRs #1772–#1776 and this one). This slice carries the closing keyword. What it adds is small on purpose: the six slices were sized so that the last one is paperwork, not a feature.

## What this slice does

- **`'bridge'` joins `LocationSubtype`** (census new finding 1). Additive; renderers fall back to the generic location signifier until an art pass, exactly as the plan said.
- **Rulebook §10.8 — Following, Moments and the Calling**, three paragraphs each carrying its `[IMPL]` pointers, and the no-named-calendar decision as `[DESIGN]`. The quick-reference card gains *Following a Mortal*.
- **The glossary seats the three words** (THR-1380): *Calling*, *Moment* and *Follow* in `Agents.md`, each with its code anchors and its disambiguation — *Calling* against the faction verb *Kindle a Calling*, *Moment* against the retired encounter concept *Defining Moment*, *Follow* keeping *retinue* in its court sense per THR-1099 — and the See-Also from *Chronicle Entry* in `Prose.md` to both.
- **The Freeholds label is confirmed, not changed.** `SLOT_TAG_DISPLAY_NAMES.holding` already reads *Freeholds* — THR-1314's arbitration landed it before this slice arrived, so the plan's item is satisfied by inspection.
- **The bar row is dropped, on the record.** `HooksBlock` used to name this ticket as the place a freehold row on the ascendant bar might come from; the note now says the decision (Law 53's HUD budget) and that a veto turns it into a small new ticket.

## The two veto invitations, restated for Christian

Decided in the plan, reversible on his word: **no named calendar** (moment and chronicle prose says *two days past*, never an invented date — a calendar is a whole system for one flourish), and **no freehold row on the god's bottom bar** (the sheet and the arc panel carry a mortal's properties; the bar stays lean). Either reversal is its own small design ticket.

## What the whole ticket delivered

The moment stream that doc 1 left ending at the trace buffer now runs end to end: a followed mortal's long work stops the world with a card in the encounter family's chrome, unfollowed mortals badge on their thread row, the sheet carries the arc, the follow toggle sits on both surfaces, the calling names every mortal with inputs and moves only when their life does, and both interface contracts that this ticket owned read 🟢 LIVE. THR-1293 closed by reference with slice 3. The telemetry gate for the chronicle-moment claim measured inside the band on both seeds.

## What is deliberately left open

Calling changes are not yet a source for the arc strip (they need a persisted record, which the history ledger does not carry today), and ambitions do not yet name the kinds they want built — both are docs 4/6 ground. `divineInfluence.actionId` stays unstamped until a rider writer records one. None of these blocks the Done-when.
