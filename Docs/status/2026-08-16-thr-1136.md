**The aftermath stopped wearing the step screen's chrome, and started answering its own vocabulary.** Five defects from one director screenshot of the *Forage for Provisions* ending, all on one surface. **§1** deletes the top-right `{tier} · Aftermath` label — the suffix duplicated the AFTERMATH section marker the content column already opens with (THR-1003), and the thread-tier word is process vocabulary that has nothing to say about what just happened to a mortal — and moves "Return to the world" out of the viewport-pinned footer into the end of the content column, so the ending reads as one column top to bottom (Law 1). **§2** reverses THR-1003's inert dots on director instruction: `StepNavigator` no longer gates clickability on `mode`, it gates on whether the host passed `onSelectStep`, which keeps THR-1003's actual rule (never draw a control that cannot act) while letting the aftermath opt in now that it renders the same `StepReplayView` swap the live path does. Identity chrome stays put during a replay; the exit is deliberately withheld mid-replay so two return-shaped controls with different destinations never share a screen (Law 21). **§3a** gives the category word its own Law 17 hover tier — the four words were explained only by the first-contact legend, whose dismissal persists across sessions (Law 51), so after "got it" a player facing `BOON · EYE` had no way left to ask, on any ending, ever. **§3b** makes personal standing name its scope (`world standing`, the ticket's own phrase, so no UL term is coined for a word the glossary already covers): it read a bare `standing`, the same word faction standing uses, so an ending carrying both drew `BOND · STANDING` twice with nothing saying whose regard moved. **§4** sorts chips by story category (`scar → bond → boon → path`, the legend's own order) then by drawn cluster size descending, emission order surviving only as an explicit stable tiebreak — sorting on the magnitude the player can *see* rather than the underlying band, because an order nobody can read back is not an order. **§5** is the director's ruling that per-Reach reputation tallies are **system-visible only**: the `reputation_tally` push is gone from the snapshot diff, so the mutation, its traces and the Whispered/Known/Legendary threshold mints all continue untouched while the chips stop. `reputationTallySentence` lost its only production caller and was retired rather than left green-but-dead; `describeTallyKey` and `TALLY_MAGNITUDE_BANDS` were **kept** — deleting them would make the inspectability §5 explicitly preserves unimplementable — and that residue is tracked as THR-1140 rather than left invisible. The general rule went into `Docs/design-system/laws.md` beside Law 13: *a quantity the aftermath reports must be inspectable on some player surface; a system-only quantity is invisible in both places.* Evidence is jsdom render assertions on the real component for every face the change produces plus absence where it should not render — the sanctioned substitution for the 1920×1080 capture no unattended run can produce (`preview_start` refused, #546 family), with the §4 assertion built through the **real adapter** so it proves the sort survives to the DOM rather than testing a fixture, and the §5 engine assertion **falsified before trusted** (restoring the push makes it read `['growth', 'reputation_tally']` and fail). 1016 files / 16,307 tests green, ratchet unchanged at 3182, build clean, engine smoke to tick 30 with 383 agents. (THR-1136, 2026-08-16)
