**The Place of Power panel reads as sentences instead of a debug label strip.** Six `label / value` rows in a `space-between` flex pair — `Holder · Stream · Decay · Mark · Bond · Origin` — which is Law 16 in its purest form and Christian's own standing direction (THR-608: key:value labels read as unfinished). THR-1080 had already cleaned the *values* inside those rows and deliberately left the row shape as a separate defect class; this is that class. **The membership predicate was re-derived rather than trusted, and it narrowed**: the ticket's own `grep` for `justifyContent: 'space-between'` across `src/components/ruins/` returns three hits, but the other two (`DelveProgressPanel`, `EmergenceDilemmaModal`) are *header* layouts putting a title opposite a timer — not label/value pairs — so the population is this one file, and the sweep is complete rather than partial. The reading is now three short sentences whose clauses vary independently, so held/unclaimed × alive/dormant × known/unknown origin all stay grammatical without a template engine: *"Vara Stonewake holds this place. Its stream gives a steady flow of spirit, and fades before long. It rose from Hollowbarrow."* The god's two marks became **chips** rather than sentences because they are categorical standing states, not narration — the mockup chip anatomy (a kind tag *plus* a sentence) is exactly their shape, and Law 16 sanctions chips as the alternative to prose. **The entity work is where the judgment sat, and two of the three calls are refusals.** The holder gets its art tile (`EntityVisual`, Law 1's image half) and its name is linked through the shared `NarrativeSegments` renderer rather than a fourth inline-link implementation (Laws 3, 27) — **but the link is gated on `holderType === 'actor'`**, because the edge schema allows `actor | god | faction` while the only handler this panel is given opens the *agent* surface, and Law 21 is explicit that "a wrong-kind link that opens the wrong drawer is a dead link that looks live". A god or faction holder is named in plain styled text, fail-open. The origin ruin is the second refusal: it has no drawer of its own, so it carries the sanctioned zoom-to-map eye affordance (Law 22, `ui-patterns` §1) and the name stays plain — deliberately **not** a new `location` kind on `NarrativeSegments`, since widening that union would make every existing `openEntity` implementation contravariant against it under `strictFunctionTypes` for one caller's benefit. Both handlers already existed on `LocationView` (`onAgentClick`, `onNavigateToRuin`) and were simply never passed down. **A Law 14 gap in my own fallback was found and closed before shipping**: the sphere word was rendered as `sphere ?? 'spirit'`, so an unresolvable `popSphere` would print the raw key — the exact miss behaviour Law 14 names. It now falls back to `essence` (true of every stream whatever its alignment) with a warn-once, and never guesses a *specific* sphere, which would state something false rather than vague; the `SphereIcon` is suppressed on that path too, since a wrong glyph is worse than none (Law 9). **The prior test suite's layout assertion pinned the shape this ticket retires** — *"keeps every row a two-cell space-between pair"*, written by THR-1080 as a containment lock for a run that could not capture pixels — so it is replaced rather than quietly deleted, and the claim it protected (wider wording must not collapse the layout) is now carried by asserting the reading is one flowing paragraph. All five THR-1080 language locks survive untouched. **20 tests green** (5 inherited + 15 new), **1015 files / 16,201 tests** across the full suite, ratchet unchanged at 3448, build clean in 13.87s. No engine smoke owed — the diff is UI-pillar only. **Browser-verify substitution: jsdom-render** — `preview_start` is refused in unattended runs (#546/#574) and the route was decided *before* implementing, per CLAUDE.md, rather than discovered at the capture. The substitution covers every face the change produces (held/unclaimed × alive/dormant × known/unknown origin, both marks, both link guards) **plus absence where an element must not render** — no eye without a navigate handler, no mark row without a mark, no art tile without a holder — which a screenshot of one state could not have shown. **UI-Laws judgment on the composed surface**: Law 16 ✓ (the strip is gone; asserted structurally, not argued), Law 1 ✓ (holder art + linked name; sphere icon + registry tooltip), Law 14 ✓ (no raw key survives, including the new fallback path), Law 21 ✓ (linked where a page exists, plain where the kind would mis-route), Law 22 ✓ (eye affordance on the map-located ruin), Law 25 ✓ (no control renders that would do nothing), Law 13 ✓ (unchanged — every magnitude was already banded by THR-1080), Law 33 ✓ (no layout growth: the panel loses four rows and gains one 40px tile). **Impediment #580** — `preview_start` refused in an unattended run for the eleventh and twelfth recorded time; the standing note now has enough occurrences to be treated as the expected state of a scheduled run rather than a surprise. **Wiki-freshness-exempt** recorded: `LocationView.tsx` is a declared source of `livelihoods-reference`, whose single place-of-power mention is essence-income drift on untyped sources — nothing this change touches. (THR-1104, 2026-08-14)
