**The mercenary encounters can be played.** All 15 `mc.*` templates threw `TypeError: Cannot read properties of undefined (reading 'initiation')` out of React the moment their stage rendered, so the encounter could never be driven to the aftermath — a crash, not a fail-soft degradation, which is what NFP #4 exists to forbid. THR-1038 had authored real `BranchAwareAftermathConfig` fallbacks for exactly these 15 templates one ticket earlier, proven at the engine level and visible to nobody; this was the second of the two silent-death layers sitting on the same content. **The ticket's premise turned out to be inverted, and that is the thing worth knowing before touching this**: it recorded `narrativeTemplates` as *optional* on `UnifiedActionTemplate` and the readers as sloppy, but the field is declared **required** (`unifiedAction.ts:1600`, no `?`) and the `mc.*` templates simply omit it — they type-check only because of the red baseline (THR-489). So the readers were not failing to guard an optional field; they were trusting a contract the content had quietly broken, which is the same shape as THR-1054 and THR-1038 and puts the defect in the *content*, with the readers as the place it surfaces. That reframing decided the fix: direction (a), fail-soft at the readers, because a crash is never the right answer to bad data (NFP #4) and the change stays additive (NFP #6) — direction (b), making the type honest, would turn every other unguarded reader in a 278-importer type's blast radius into a fresh baseline error to pay for a content problem. `resolveInitiationProse` is one named helper with an honest fallback ladder rather than four inlined `?.`s: these templates *do* carry prose, on their steps, so the step's own `narrativeTemplate` is the right second choice and the named `MISSING_INITIATION_PROSE` constant is reached only by a template carrying no prose anywhere. The membership predicate was re-derived rather than trusted, and it had drifted in both directions — the four adapter line numbers were off by four, and `StoryBeatModal.tsx` and `useAgentInteraction.ts`, both recorded as correctly guarded, were unguarded; six UI reader sites were fixed in total. The corpus test derives its own population from `UNIFIED_ACTION_TEMPLATES` on every run, so a future faction set carrying the same omission joins the suite without anyone editing a list, and it carries a non-vacuity guard so it fails rather than passes if that population ever empties. Browser-verified at a true 1920×1080 (`innerWidth`/`innerHeight` measured in the capture pass, page scrollWidth within viewport, dialog rect exactly 0,0→1920,1080): `mc.quest.patrol` renders its stage, and via `?spawn=` reaches the full aftermath — the THR-1038 fallback prose, STANDING and MARK consequence rows with magnitudes in words, and both authored reactions ("The work was done to the line.", "The road was too quiet.") matching what `npm run cli` lists for the same template. Reported honestly: `getOutcomePinVerdict()` returned `unauthored_band`, which is the correct and expected reading here — THR-1038 authored *fallbacks*, not per-band variants, so the base ending is what the Done-when asked to see. 975 files / 15395 tests green, build clean, ratchet unchanged. (THR-1040, 2026-08-09)
