# 2026-08-30 — THR-1376 · The six seated-but-unindexed UL terms got index rows, and the coverage invariant is true again

THR-1316 measured the UL README's coverage line and found it false in both directions: the footer asserted `Index coverage: 141 of 141` while the tree held 161 shard headings and 155 index anchors. Rather than round up to a number it had not met, it recorded **155 of 161 with the six names spelled out** and filed this ticket for the rows themselves — which sat in another ticket's domain. This is that one-time data fix. The self-detecting guard stays with THR-959; nothing here makes the invariant enforce itself.

**Six rows, each written from the entry it points at.** Five Prose terms seated by THR-1372 on 2026-08-29 (`narrator-mode`, `register`, `band-fragment`, `vagueness-field-class`, `register-compliance`) and the older `Traits#tag-namespace`. Each row was drafted by reading the shard entry, not by paraphrasing the ticket's bullet list — which matters, because two of them are not what their names suggest: `Register Compliance` is **report-only by settled verdict** (THR-1250) and ranks rather than identifies defects, and `Vagueness Field Class` is the scope that decides whether a natural indefinite is a defect *or correct scene prose*. A row copied from the ticket would have flattened both.

**`Band Fragment` is shard-crossing, so 161 headings resolve to 161 anchors across 162 links.** The term has a `### ` heading in *both* `Encounters.md` (indexed since the nudge work) and `Prose.md` (seated by THR-1372, unindexed), and the Prose entry says so in its own Status line — *"shard-crossing term — the Encounters shard owns the mechanics."* The diff counts headings, so both need an anchor. The Encounters row keeps the unqualified name, because the unqualified name belongs to the owner; the new row is `Band Fragment (Prose)` and carries the authoring side — every nudge owes a failure-band fragment, and base band text must read with any subset of the hand active. This is the one place where the ticket's flat list of six would have produced a wrong answer: it reads as a single missing term.

**The diff was reproduced, not trusted.** Per the coordination block: `slugifyHeading` imported from `scripts/generate-ul-dashboard-data.ts` by absolute `file://` URL, every shard `### ` heading slugified, both directions diffed against the README's `](./<Shard>.md#<anchor>)` links. Before: 161 headings, 155 anchors, **6 unindexed, 0 dead**, matching the filed finding exactly. After: **`UNINDEXED (0)` / `DEAD ANCHORS (0)`**. Per-shard heading counts (Agents 41, Encounters 36, Process 17, Traits 16, Graph 15, Coordination 14, Prose 13, Cosmology 9) match the footer census unchanged, and the `**Status:**` recount still totals 161 — 153 canonical, 5 deprecated, 1 proposed, 1 rejected, 1 retired. No term was seated here; the version marker moves to v1.14 saying exactly that.

**The regenerated dashboard shows the mechanism that hid this.** `src/data/ul-dashboard.generated.json` changed in exactly six one-liner pairs and nothing else: each of the six now draws its curated index one-liner instead of the generator's silent first-sentence-of-body fallback. That fallback is *why* six unindexed terms cost nothing and failed nothing for a day — the dashboard rendered plausible text for all of them.

**Left standing, deliberately.** The generator emits `missing_status` warnings for seven canonical terms whose `**Status:**` lines carry a parenthetical (`canonical (added 2026-08-29, THR-1372; …)`) — its parser matches bare status values only. Pre-existing, untouched by this diff, and out of scope for an index data fix; logged as impediment #958 rather than filed, per the process-work throttle.

**Coverage line rewritten to stay honest.** It now reads `161 of 161`, but keeps saying out loud that it is an assertion rather than a guard — it was `141 of 141` while 15 terms went unrecorded, and only a measured diff distinguishes the two states. The number changed; the epistemics did not.
