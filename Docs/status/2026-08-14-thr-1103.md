**The Codex stops showing the player two spellings of the same word.** After THR-1076 cleaned the CRUD axis off the card face, `hex.bless_land` still rendered `tags: ["star","regional"]` — the raw reach key sitting beside a `Reach` detail row two lines below reading `Star`, resolved, from the same mapper. Three violations remained on the composed surface and all three are closed: the reach key now resolves in the tag chips as it always did in the detail row (Law 14), `scale` gets a display vocabulary rather than being dropped (Law 14), and `Essence Cost: "3"` becomes `Cost: 3 essence` (Law 13). **The Law 13 call the ticket asked for is made and recorded in the code**, and the answer is *keep the numeral* — but not on the clause the ticket offered. The ratified exception (THR-890) covers resource-pool balances in persistent chrome, and a Codex detail row is neither, so on a plain reading it does not reach; essence is separately exempt **as a price**, which `shared/formatEssence.ts` states outright ("a price you pay has to be countable", THR-1006). So the defect was never the numeral — it was `String(template.essenceCost)`, the exact raw interpolation THR-1006 exists to prevent, carrying no unit and seventeen digits of IEEE-754 noise for an authored `0.05` price. Routing through `formatEssenceLabel` makes the Codex quote the same pool the same way as the hand and the nudge stage; `0` reads `Free`, matching `ActionCard`. **Scale is resolved rather than deleted, and the distinction from THR-1076 is the reasoning**: `crudType` was dropped because no honest player-facing word exists for an axis where `update` covers both blessing a company and scorching a hex; scale says how far a verb reaches, which is exactly what a player weighs before paying for it. **Sphere came into scope on evidence, not appetite** — the invariant test could not be written without it. Three sphere keys (`life`, `time`, `shadow`) are *also* reach keys, so a raw sphere tag is indistinguishable on the panel from the raw reach tag this ticket exists to fix, and any sweep excluding spheres would have to whitelist precisely the tokens most likely to leak; `Star · life sphere` was the defect twice over in one subtitle. **The `VOCAB[key] ?? key` idiom is retired for a resolver that implements Law 14's stated miss behaviour** — plain-English fallback plus a warn-once — because the old idiom satisfies neither half: it renders the raw key and says nothing, so a newly added enum member reaches the player silently and looks deliberate. It earned itself immediately, firing exactly twice across 180+ entries to surface `sphereAffinity: 'shadow'` and `'void'` — reach names sitting in a sphere field, a content defect that was invisible and is now filed. **The ticket's own file attribution was wrong and is corrected**: the raw `Visibility` row is in `mapCondition`, not `mapPossession`, which has no such row. **38 Codex tests green** (11 registry invariants + 8 render assertions, both pinned across the whole catalog rather than at the three call sites, so a fourth mapper fails there instead of shipping the enum back), ratchet unchanged at 3448, build clean in 10.97s. **Browser-verify substitution: jsdom-render** — `preview_start` is refused in unattended runs (#546/#574) and the route was decided *before* implementing per CLAUDE.md, not at the capture. It is honest for this change specifically: every edit is a string substitution inside a row that already rendered, so no element is added, removed or repositioned and the failure classes only pixels can catch need a layout delta that does not exist — the one real consideration, that values got *longer*, is asserted rather than assumed. **Impediment #579**: `worktree-write-guard.test.ts` fails 9/9 with exit 127 in this sandbox, on a clean baseline as well as under the diff — the shell hook cannot be executed at all, so the "allows" cases fail identically to the "blocks" ones. Pre-existing, environmental, green on CI's ubuntu runner. Residue filed as [THR-1113](https://linear.app/threadbare/issue/THR-1113) with its law numbers rather than widened into this ticket (THR-1007). (THR-1103, 2026-08-14)
