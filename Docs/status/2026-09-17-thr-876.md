# THR-876 — the five quarantined Meet-The-First scenes are regenerated and back in the pool

**Date:** 2026-09-17 · **Pillars:** Engine-N/A (no tick or graph surface; `selectDilemmaScene` is unchanged) · Content · UI-N/A (no component edit — the beat already renders whatever the selector returns)
**Issue:** [THR-876](https://linear.app/threadbare/issue/THR-876/regenerate-the-5-quarantined-meet-the-first-scene-assets-doctrine)
**Plan doc:** none — the ticket description is the spec.

The THR-868 audit quarantined five of the 32 files in `public/assets/meeting/scenes/`: `prison-cell` had the retired choice mechanic painted in as two UI buttons, `healing-tent` and `burning-palace` carried baked-in caption and title text, and `plague-ward` and `great-hall-feast` showed individuated front-lit faces (image doctrine ruling 10 — the portrait chosen at Sensing is the only likeness in the flow). Compliance was reached then by re-pointing to substitutes; the five files kept shipping in `dist/` as dead bytes and their thematic slots had no art made for them.

## What changed

- **Five new pictures, same filenames**, each 1376×768 JPG q95, each written as an *unpeopled, uneventful* scene so the generator has no face to fill and no incident to stage: an empty cell with the door ajar, a plague ward of empty cots behind lit screens, a healer's tent with a folded blanket on the cot, a palace burning over a deserted forecourt, a feast laid with every chair empty. The files are ~60% smaller than the ones they replace (4.2 MB → 1.6 MB across the five).
- **`QUARANTINED_SCENE_ASSETS` is empty.** The record and its doc comment stay, naming what the five defects were and how the next finding is added.
- **`DILEMMA_SCENE_ART` grows 16 → 21.** The five are appended with tags drawn from the authored dilemma vocabulary, so the empty-tag path (seed picks off the head of the array) is unchanged. The substitutes (`justice-hall`, `lantern-vigil`) stay registered.
- **The doctrine test keeps biting on an empty quarantine.** The floor drops 5 → 0, a falsification arm quarantines a registered scene hypothetically and requires the same predicate to report it, and a new pin asserts each regenerated file is registered, un-quarantined, and carries at least one authored tag.

## Audit

Contact sheet and the two full-resolution strip sheets are in `Docs/evidence/thr-876/`. Top strip and bottom strip: no text on any of the five. Two mid-frame details the strips cannot see were checked at 2× from the raw plates: the healing tent's one paper vial label is blank, and the carved blocks in the palace forecourt are foliate ornament, not glyphs. The only figures anywhere are three featureless silhouettes at a distant campfire through the tent flap — silhouette is the doctrine's allowed form.

**Spend: 5 images generated, 0 retries.** Every plate passed first time. No letterbox bars on any plate (the crop ran and was a no-op).

## Evidence

- `npx vitest run src/data/__tests__/meetingSceneDoctrine.test.ts`: 11/11 green.
- `npm test`: 1269 of 1270 files, 20557 of 20558 tests green, twice. The one red is `scripts/__tests__/check-lane-silence.test.ts` › "accepts an empty file", which passes 18/18 alone, imports nothing this diff touches, and is green on `main` CI at the same base — an mtime-versus-`Date.now()` race under full-suite load on the Windows machine, logged as impediment #1051.
- `npm run check:typecheck`: OK, 2848, unchanged from baseline.
- `npx vite build`: built; `dist/assets/meeting/scenes/prison-cell.jpg` is the new 305 KB file.
- Engine smoke and `test:heavy`: not owed — nothing under `src/engine/` or the tick path in the diff.
- Browser-verify exempt: asset regeneration, no component edit (no file under `src/components/`, `src/hooks/`, `src/contexts/` or `src/index.css`).
