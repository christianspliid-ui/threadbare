**The ruins surfaces stopped speaking schema, and the ticket's own instruction to re-derive the membership found twice what it named.** THR-1080 filed three leaks in `EmergenceDilemmaModal` — a raw `consequenceRoll` enum at two sites, `Auto-fires in 6 ticks`, and the *unevaluated* cost formula `Essence: magnitude × 20` on a card the player is choosing between while the component already held the answer (`claimCost`, computed six lines above to decide affordability). But the ticket's predicate (THR-688 rule A) is "every player-facing string on a **ruins/delve surface**", with an explicit instruction to re-derive by grep "rather than trusting these three". Running it found the same three defect classes in both sibling components, and confirming they were player-facing was one `grep` for their mount sites: `DelveProgressPanel` mounts from `GameView`, `PlaceOfPowerInspector` from `LocationView`, neither gated. **The worst instance was not in the ticket**: the delve panel's emergence banner rendered `pendingEmergence.agentId.slice(0, 14)` — a truncated node id shown to the player, Law 14's most literal possible violation — beside a raw `DelveScale` chip and two more tick counts. Fixing the named three and leaving those would have been the half-fix the predicate exists to prevent.

**The countdown vocabulary was adopted, not invented, which is what the mutex was for.** THR-1080 was mutex'd with THR-1070 — *"both decide how a tick countdown reads to the player … do not invent a second phrasing"* — and THR-1070 landed `getDurationWord` (`in a moment` / `shortly` / `before long`) at 11:26Z the same day. Per THR-908's liveness table a `Done` partner with a merged PR makes the mutex reason verifiably inapplicable, so the claim proceeded past it and every countdown on all three surfaces now routes through that one scale, with the zero case keeping each caller's own verb inflection (`Auto-firing now`, `Resolving…`) exactly as THR-1068 established for the veil.

New `src/data/ruin-words.ts` holds the rest: fates in two grammatical shapes, because a header noun and a mid-sentence predicate are different problems and the old code used the bare enum for both — so it read wrong in at least one of the two every time. **`transformed` → `Remade` and `catastrophic` → `Undone` are the only two re-worded, deliberately**: the other three keys are already the right English word for their condition, and renaming them purely to differ from their key would be worse copy, not better. A three-band stream-yield scale replaces `2 spirit / tick`, its thresholds derived from `POP_ESSENCE_PER_TICK_MIN/MAX` rather than written as literals so widening the engine range cannot silently strand a band. Every lookup goes through a function with Law 14's second clause wired — unresolvable key renders Title-cased plain English and warns **once per session**, never the key.

**A guard caught the author, and a second one was caught being vacuous.** The first draft asserted every fate word differs from its key; `scarred` → `Scarred` failed it, correctly — the assertion was wrong, not the vocabulary, and it was rewritten to pin presentation (Title-case, no underscores) plus the two values that genuinely re-word. More seriously, the modal's "no raw key on the surface" test asserted `container.textContent` — and `Modal` renders through `createPortal`, so it was passing against an **empty node**. Rewritten against `document.body` with explicit `cleanup`, and narrowed to the two exact defect shapes (`· ${roll}` and `the ruin was ${roll}.`), because asserting the key is absent *entirely* would be wrong: `left scarred` legitimately contains `scarred` and is presented copy, not a leak. All four suites were falsified by restoring each defect — header 2/2 red, countdown 2/2 red, sliced ids 2/2 red.

1003 files / 15,994 tests green, ratchet unchanged at 3447, build clean; no engine, type or tick-loop file touched, so no smoke owed. **Browser-verify substituted, pixel capture owed** — impediment #546, eighth occurrence, refusal reproduced verbatim. This one does *not* get #546's easy scope test: unlike the preceding occurrences (a one-character-wider label, a pure deletion, an equal-length swap), several replacement strings are **wider** than what they replace, so paint failures are not structurally unreachable, only bounded. So the substitution asserts the bounds rather than arguing them — the delve overlay is still 320px fixed-width with `overflow-y: auto` under a 420px cap, making horizontal overflow unreachable and vertical growth scroll-bounded; every inspector row is still a two-cell `space-between` pair. Two of the three components had **no test file at all** before this, which is part of why the leaks survived. A pre-existing Law 16 defect found in passing — `PlaceOfPowerInspector` is a `key: value` label strip end to end, the shape Christian calls unfinished UX — is filed as THR-1104 rather than widened into. (THR-1080, 2026-08-13)
