**The mirror rule moved out of the rows that kept missing each other.** THR-824 was filed to mirror `threadbare-autosync.ps1` into the repo; measured at pickup, THR-937 had already done that half — the fenced body in `Docs/ops/threadbare-autosync.ps1.md` verifies **byte-identical** to the live script (8659 chars both sides, line-endings normalised), as does `clean-stale-git.sh.md` against the reaper. What had *not* shipped was the part that keeps the next host task from falling in the same gap. The registry still carried a stale bullet asserting autosync "has no repo mirror yet — tracked as THR-824" **one line below** the bullet linking the mirror, so the single operational surface for the host lane contradicted itself, and an auditor reading top-down got the wrong answer first. That is the shape the ticket's own third task named: the obligation was stated twice in ad-hoc form — once for prompts in § *Prompt sources*, once inside the reaper's bullet — and **neither statement was where someone registering a new host task would look**, which is precisely how autosync went unmirrored for the ~4 weeks between its registration (2026-07-18) and THR-937. The rule is now stated once, generalised over both classes of unversioned file (CC-lane `SKILL.md` prompts *and* host-lane script bodies), with an inventory table that makes Done-when 3 auditable instead of a judgment: six live paths, each either mirrored or carrying its reason for not being. Three are deliberate non-mirrors and say so — the autosync **log** (a rewritten run output carrying no logic), the pause **marker** (its *presence* is the declaration per THR-1001, so a tracked copy would assert a pause that is not in effect), and `website-code-work` (personal, out of scope, already the named THR-850 exception). The two row-level statements became pointers to the one rule rather than third and fourth copies of it. (THR-824, 2026-08-08)
