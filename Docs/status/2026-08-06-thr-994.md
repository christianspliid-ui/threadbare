**The strip was empty on the one path players actually reach, and the only honest capture of it needed a declared substitution (THR-994, 2026-08-06).** A THR-646 deferral. `GameView` picks `buildSimpleEncounterStageModel` whenever a notification's `UnifiedAction` no longer resolves — **the common case for any encounter the player did not open mid-flight** — and that adapter's header carried `title`, `locationLabel: ''`, `threatLabel`, `threadTier` and nothing else. `ContextStrip` gates every element it draws on precisely the missing fields, so THR-636's headline "whose encounter is this, and where" answer rendered as a zero-height container with no children, while `onShowOnMap` sat wired with nothing to call it. The fix reads every value from data the adapter **already receives**, so `GameView.tsx` is untouched — which also kept the diff clear of THR-1005's open PR on that file. **`locationLabel` returns `''` where the unified adapter returns `'Unknown Location'`, and that is a decision rather than a shortcut:** `ContextStrip` rejects the placeholder *by name*, so it would render as an invisible gap either way — the empty string is the same outcome stated honestly, and Law 4 wants a designed fallback, not a lie. **Second half:** resolved history rows now carry `outcome` / `outcomeWord` / `reachLabel` / `afterimage` / `replayNarrative`, because the veil's replay body reads `replayNarrative || afterimage || ''` and the rows supplied neither — the step navigator entered replay correctly over a blank body. There is no frozen `stepProseHistory` on this path (that record belongs to a live action), so replay text is the step's own authored prose re-enriched under its outcome band, the fallback the unified adapter already documents. **The tests render the real `EncounterVeil` rather than asserting on the returned model**, as the Done-when demands, because the defect is an *invisible element* — a shape assertion passes happily against a header whose fields nothing reads; falsified against the pre-fix tree at 7 of 8, the 8th being a guard against over-filling unresolved rows that correctly passes both ways. **The browser evidence is a declared substitution and the reason is a finding in itself:** the fallback cannot be reached on demand in a seeded run, because the auto-open scan surfaces encounters while their action is still *live* (so every naturally-opened veil takes the unified adapter) and the 20-tick `RESOLVED_ACTION_RETENTION_TICKS` window never opens before the doom clock ends the run — four runs, ~45 min, logged as impediment #455 with the adapter discriminator, since the two paths render the same component and their screenshots are visually identical. Captured by mounting the production veil over the **live** `gameState` at 1920×1080 (real graph, real resolved action `ua_34`, real actor): `Vessen · Iron · Meadowcity · Show on map` with portrait tile, `childCount: 3, height: 30px` against the ticket's measured `0` and `0`.
