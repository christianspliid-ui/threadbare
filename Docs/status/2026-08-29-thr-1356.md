# THR-1356 — the vault stops teaching the March UI

**Shipped 2026-08-29.** R3-T4 of context-cleanup round 3 (THR-1350). Vault filesystem work
(no git; all 17 touched pages backed up to the session scratchpad first) + this repo-side
closeout. Engine/Content/UI code N/A. Audit:
https://claude.ai/code/artifact/98713042-66d0-4a4e-8304-93339d0e3096

Applied Christian's round-2 1A precedent — live-surface vault pages become thin maps pointing
at repo authorities, never parallel specs:

- **Thin rewrites:** Tooltip System (was wiring three deleted components and omitting the one
  enforced rule — the ≤200-char cap), Action Drawer (was teaching the rejected wheel-slot
  model, Scry-first sorting, stone-class chrome, and "~123 lines" for a 526-line component),
  Avatar HUD (button set corrected to Move / Actions / Investiture — the body taught
  Wheel/Scry), Harvest Screen (three invented ending variants and "Resonance memories"
  removed; rewritten against `cycleEnd.ts`), Location View (agent clicks no longer route to
  the rejected Agent Wheel).
- **Refresh banners + surgical fixes:** HexMapV2 Overview (26 layers, 130+ files,
  `RenderLayers.ts` named the authority; "scry mode"/"retinue" vocabulary corrected),
  HexMapV2 Architectural Review ("findings executed" — all three 2026-03 recommendations
  shipped as `hooks/` + `LAYER_Z`), Hex Zoom View (tooltips shipped long ago; the
  bare-square-agents doctrine gap noted as logged), View Levels (world level = HexMapV2, not
  SVG polygons), Visual Style Tile (historical inspiration board; the three-way sphere-color
  divergence closed by pointing at the token sources; two dead `Design/*.html` pointers
  flagged), Narrative Log (live component is `NarrativeFeed.tsx`).
- **Banner widened:** Agent Detail Panel (its Action Row routes to Psyche Strands +
  Agent Wheel — both retired; the old banner only blamed naming). Progressive Disclosure's
  contradiction with it reconciled.
- **V1 paths fixed:** Fog of War, Agent Movement System (→ HexMapV2 scene modules).
- **Naming:** vault `Index.md` H1 → "Threadbearer — Knowledge Base"; Product Strategy's
  "Threadbare is the visual aesthetic" corrected (codename only; the design system is Dark
  Tapestry).

**Deliberately deferred:** `npm run rebuild-index` — the generator
(`scripts/rebuild-index.ts:324`) hardcodes the retired H1 and would revert the Index fix; the
string fix rides THR-1357 (pulled forward from the round-5 residue list), and the regen runs
right after it.
