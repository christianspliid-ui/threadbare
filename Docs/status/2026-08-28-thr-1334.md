# One rule, one home (THR-1334)

**The single-authority + pointers ruling (2026-08-25), applied to the general layer that was violating it.** The 2026-08-28 audit measured the copies: the design-governance checklist stated three times (two saying "8 steps" against the authority's 15), the grill-me artifact home stated three ways with the UL teaching the forbidden one (`Docs/plans/` — an agent following it would commit a file governance says must not exist), the Three-Pillar Rule in four phrasings, the NFP list in three full restatements, and three canon indexes that all disagreed with the directory and each other.

This pass makes the UL define and point instead of restate: the Process shard's Design Governance, grill-me, Three-Pillar, and NFP entries now carry definitions plus pointers to their authorities, with the drift they used to carry named in place. The UL index's footer — three screenfuls of recount archaeology in an always-load file — compressed to the operative instructions (the recount grep, the slugify verification, THR-959) with the history pointed at git history. `Docs/canon/README.md` becomes the **one full canon index**, rebuilt from 10 stale rows to all 18 pages with corrected review dates; CLAUDE.md's table and the state-of-game-design router now point at it. The rulebook's three contradictory last-reviewed dates reconciled to one; its lint pointer dropped the retired "Ready for Codex" queue; its two named exemplars now carry the "prose drifted — do not copy" qualifier `Docs/exemplars.md` already ruled. The quick-reference card's header stopped promising "no status flags" above a body that has them. gamedocumenter's half-taught Merge=Done rule gained its missing halves (PR body + line-anchoring — the impediment-#140 shape). The ubiquitous-language skill stopped claiming stub functions that exist nowhere. Frontmatter dates synced to body dates on process, design-governance, and rulebook canon.

**One scope reversal, recorded:** the audit slated removal of two stale worktrees; pull-work § Ownership (THR-674) rules the hourly reaper the *single owner* of `.claude/worktrees/` disposition — sessions never remove worktrees themselves. Both stale trees sit on merged or reapable branches; the reaper collects them. The grep-exclusion rule (impediment #918) is the standing mitigation.
