# THR-1353 — the constitution was healthy; its copies were not

**Shipped 2026-08-29.** R3-T1 of context-cleanup round 3 (THR-1350, UI/UX layer). Docs-only
correctness sweep; Engine and Content N/A — no runtime file touched. Audit:
https://claude.ai/code/artifact/98713042-66d0-4a4e-8304-93339d0e3096

## What the audit found and this ticket fixed

The round-3 audit measured the UI/UX guidance layer against the live authorities and found
`laws.md` itself current while the surfaces restating it drifted. This ticket is the
correctness half (deletions are T2, skill repair T3, vault T4, code T5):

- **Naming** — design-system `INDEX.md` still introduced Dark Tapestry as "the UI/UX design
  system for The Fantasy World Simulator"; `artifact-representation.md` (a *binding* pattern)
  carried the retired name in its identity line; the `frontend-ui` skill and `ui-patterns.md`
  taught the aesthetic itself as "Threadbare". All now state the ruling: game = Threadbearer,
  design system = Dark Tapestry, "Threadbare" = repo codename only.
- **Modal height** — `primitives.md` and `component-selection.md` asserted the shared `Modal`
  ships 85vh (twice each). `Modal.tsx:167` is 75vh, lawful under Law 33's 85vh *cap* — the
  2026-04-18 viewport audit adjudicated exactly this. Docs now state the cap/value split.
- **Browser-verify law list** — laws.md Enforcement said "1, 13, 14, 21, 33, 37" while
  CLAUDE.md and verification-gates.md said "1, 13/14, 17, 21, 37". Aligned all three on the
  superset **1, 13/14, 17, 21, 33, 37** (calibration verdict under the 2026-08-12
  agreed-outcome rule).
- **Badge font** — `primitives.md` sanctioned "monospace, 9px (exception to 16px min)";
  `IconButton.tsx` ships `var(--text-xs)` and no exception exists. The licence is removed.
- **Tokens** — `tokens.md` documented `--text-muted: #7a6e60` (the pre-WCAG-fix value;
  live `#9a8e7e`) and `--border-accent: #d4a04025` (live `#d4a04040`).
- **Z-stack** — `layout.md`'s table placed Modal at 70 *above* tooltips and still named the
  deleted "SVG hex map"; re-synced with `layout-zones.md`, which matches the code.
- **Law 17** — `ui-patterns.md` §1 still offered "a Tooltip wrapper or `title` attribute";
  `interactions.md` justified the same rule by preview-tool behaviour. Both now cite the
  2026-08-06 raw-`title` retirement. The stone-class panel-styling section converted to tokens.
