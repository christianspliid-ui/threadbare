**Context-cleanup R2 — UL Encounters shard repair (THR-1339).** The terminology authority's Encounters shard was internally contradictory: it seated `EncounterTemplate` as canonical ("the authored unit of encounter content") against the THR-108 removal, aliased Reaction as "Player Choice — the primary mechanism through which players shape the simulation" while its own nudge entries state a nudge never picks an outcome, and defined Court Position as a numeric encounter role against the code's `'the_first' | 'retinue' | 'watched' | 'dormant'` string enum. All three rewritten: EncounterTemplate is now a retired entry that resolves old wikilinks to the retirement notice; Aftermath/Reaction teach that the band is rolled before the Aftermath opens and Reactions are follow-up responses, never ending-pickers; Court Position matches `src/types/influence.ts`. Six internal backlinks and the UL README index row repointed at UnifiedActionTemplate. Round-2 audit: https://claude.ai/code/artifact/e591d762-eef7-4baf-82fe-73f69e4c3a1d
