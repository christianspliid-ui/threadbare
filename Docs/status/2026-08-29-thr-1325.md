**A resume can no longer run over a live session, and a hand-created `In Dev` ticket can no longer be built twice (THR-1325).** Two claim predicates in `pull-work` missed a live state each, and both had already cost real work. New **Step 1.6 — predecessor-liveness proof** now runs before every resume and before Step 1.7's disposition (which unassigns, and unassigning a shipping session is impediment #755's failure): two reads, branch-tip age via `git log --format=%ct` and worktree filesystem activity, reusing the reaper's `worktree_recently_touched` probe and its `WORKTREE_MIN_IDLE_MINUTES` (180) rather than minting a second definition of "alive". Recent on either ⇒ stand down **silently** — no comment, no state write, no assignee write, because the live session owns that ticket. The probe fails **closed**: an unreadable signal reads as live, inverting this skill's usual fail-soft direction because the costs are asymmetric (standing down costs one hour; resuming a live session cost ~1 full duplicated session in impediment #763). The `%ci` trap that nearly swallowed #743 is stated inline — it renders a local `+0200` offset, so a naive compare reads a two-minute-old branch as two hours idle and fails open into the exact shape the step exists to close; `%ct` is epoch seconds and has no offset to get wrong. Step 1.8 now treats `In Dev` + assigned + **no claim comment** as *unclaimed, not resumable*: assert a claim comment and re-read the thread so Linear's comment ordering becomes the arbitration the state field cannot provide, and re-run the duplicate-PR sweep immediately before `gh pr create` — the point-of-commitment re-read, THR-1283's rule for assignee writes applied to the other write that assumes exclusivity. That sweep is also written into the closeout ship section, so every run passes through it rather than only the hand-created path. Item 3's open question is now a recorded ruling in `orchestrator/SKILL.md` T3: the orchestrator **surfaces** hand-created `In Dev` tickets and never normalises them — it preserves the lane's one invariant (it never writes into the executor's slot on an inference about liveness), the race is already closed downstream where the comment timestamps and open PRs actually are, and surfacing makes the practice countable so the retro can stop it at the source instead of hiding it. Impediments #743, #763, #765 cross-referenced.
