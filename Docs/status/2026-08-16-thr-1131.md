# THR-1131 — Retrofit Batch 1: the slice six through the factory line (2026-08-16)

**The first batch through the Encounter Factory as designed** — brief approved in chat, author pass, independent critic loop, machine gates, live proof, batch report. Six nudge-era encounters (Unsafe Bridge, Grateful Kin, Swindled Family, Swindler Found, Bargain at the Crossroads, Full Moon Collection) left `RETROFIT_PENDING` with the full Composition Contract enforced: every authored change now declares `concepts` (44 chips), 15 dead image tags repointed at live library rows, two encounters gained their first nudge hands (`KIN_HAND`, `FULL_MOON_HAND` — five cards each, four spheres plus an ungated common, every band covered), the Swindled Family gained its missing third ending band (the drover's-cut critical success), and the Full Moon's "one more sentence" became a real seeded second appointment instead of prose asserting a debt nothing recorded.

**The prose re-registered to the THR-974 plainness ruling, the factory way.** Christian's directive — *"dont apply the 10 rewrites verbatim, run them through the factory instead with the new rules as a validation"* — set the process: the passages were re-authored fresh, and a fresh-context editorial critic judged the output against his 10 rewrites as the calibration register. The critic returned PASS WITH REVISIONS and a 27-item list whose dominant class was **seam echoes** — afterimage, band overview, and chip cause restating each other verbatim, which the chip surface renders side by side — plus the residual inversions/aphorisms the ruling names. All 27 applied in loop 1 (35 edits); gate stayed 6/6 clean.

**Stage 4 told the truth about itself.** Live proof reports 1 proved / 5 failed — but the baseline on unmodified `main` was **0 / 6**, and every remaining ✗ is a claims-model artifact (reaction-borne effects counted as unconditional, claims judged against one rolled band, family-default cast never binding in the harness world), filed as THR-1132 with the baseline evidence. The batch strictly improved every verdict it touched; the harness's remaining reds are its own.
