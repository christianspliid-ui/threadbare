**Law 29 became machine-checkable, and the gate's own predicate had to be corrected first.** THR-1011 asked for a sync test keyed on "every export of `src/components/shared/index.ts`". Measured at pickup, that predicate would have been vacuous in exactly the place it matters: the barrel omitted `CardKeywordChip`, `EntityVisual` and `OddsPips` — `EntityVisual` being the reference implementation Laws 1, 3 and 27 are written around, with six production consumers — while it *did* export the three modules no production file imports at all. A barrel-keyed gate would have policed the dead primitives and exempted the live ones, and any future primitive could leave the gate forever by not being re-exported. The test keys on the **directory** instead: a `.tsx` in `src/components/shared/` is a shared primitive, and there is no way to be one without being one. The barrel is still asserted, as its own check rather than as the membership rule. The ticket's measured numbers were also stale (THR-688 rule A): coverage was 27/30, not 28/30, and the third gap was `Section` — the primitive Law 36 leans on — which the ticket did not name. Those three gaps turned out not to be three unrelated omissions but exactly the detail-page cluster (`Section`, `DetailBreadcrumb`, `DetailModal`), whose members import only each other. Task 3 asked to "resolve the two limbo primitives", but THR-966 explicitly defers that mount-vs-prune call to a coordinated decision with THR-951, so it was **not** settled here: the three now render at `?view=styleguide` with sample data, which satisfies Law 29 whichever way THR-966 goes and dies with the cluster if it is pruned. Both docs were refreshed against the library rather than against the ticket's list — `component-selection.md` gained the nine missing primitives plus magnitude/entity-art/list-grouping decision rows carrying their law numbers, `primitives.md` gained a 30-row spec table (props surface, variants/states, styleguide anchor). Three of the test's five assertions guard the docs, so both files now fail CI when they fall behind the library, which is what stops the 2026-04-13-to-2026-08-06 drift recurring. The gate's negative control lives in the suite rather than in a one-off manual check, and it caught a real bug in its own matcher: `import { Ghost } from '../shared/Ghost'` contains the name twice, so the first "more than one occurrence" predicate scored an import-only file as covered — passing on precisely the shape the gate exists to catch. Stripping whole import *statements* (not lines — a multi-line brace import leaves its interior names behind) fixed it. Pre-existing and filed, not fixed: `DetailModal` forks its own overlay instead of composing `Modal`, so it sets neither `role="dialog"` nor `aria-modal` and does no focus management (Laws 23, 26, 50 — THR-1024). (THR-1011, 2026-08-08)
