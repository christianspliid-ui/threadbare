**The rejected wheel's code is gone (THR-1351).** `AgentWheel.tsx` — the intervention-wheel component on the CLAUDE.md rejected-approaches list since the ActionDrawer replaced it — sat on disk with exactly one importer: its own 310-line test file, which also carried 4 of the typecheck baseline's errors. The census that authorized the deletion is the ticket's record: GameView's `wheelSlots` feed ActionDrawer and import only the `WheelSlot` *type* from `src/engine/wheel.ts` — a name collision with the component, not a dependency — so the engine data layer (`getAgentWheelSlots`, retained per its THR-501 comment and asserted by three live test files) stays, as does `sphereIcons.ts`' `WHEEL_SLOT_GLYPHS` (live in ActionCard; its doc-comment naming AgentWheel reworded). No CSS, no snapshots, no codesight importers; the QA registry's S-070 row had already been dropped by THR-1340, which deferred this code deletion to its own ticket. One irony recorded in passing: `actionCardRedesign-integration.test.ts` has asserted "AgentWheel component is fully removed" since the redesign — as of this change the assertion is finally about the tree it describes. Baseline refreshed 2951 → 2947.
