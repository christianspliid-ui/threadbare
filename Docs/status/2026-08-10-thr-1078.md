**The first paragraph of every vertical-slice encounter was a note written for us, and the disposition the ticket asked for came out the opposite way from the one it expected.** All eight templates in `src/data/encounters/vertical-slice.ts` ended their `description` with `Vertical-slice encounter, THR-883.`; six named a `Hook #NNN`; three spoiled the follow-on the player had not yet chosen (*"The accept path seeds The Full Moon Collection"*). It reached the player as `header.subtitle` — `buildUnifiedEncounterStageModel:184` resolves `template.description ?? resolveInitiationProse(...)`, and `EncounterVeil:1659` renders it in a styled slot above the fiction. The ticket offered two fixes and asked for the reason: **(1) stop the surface rendering `description`** — one component, every template forever — or **(2) rewrite the eight strings**. It leaned toward (1); the evidence says (1) is a **regression**. `description` is player-facing *by contract* (`types/unifiedAction.ts`: "Qualitative game-mechanical description… Shown in the focused card description text box", explicitly "atmospheric prose"), the veil's slot is deliberate down to its comment, and **eleven other encounter files author the field correctly as scene prose** — `road-ambush.ts` reads *"A trade road ambush already in progress. Six former soldiers, a wounded driver, and a merchant holding her nerve."* Suppressing the field would have blanked a populated, correct subtitle on all of them to fix eight mis-authored strings. So: fix (2), and the eight now read as fiction in that same register. **Two of the ticket's own claims were checked and did not hold, which is why the fix is scoped where it is.** It warned the metadata might also reach the Codex — it does not: `codexRegistry` buckets by id prefix (`divine.`/`action.`/`hex.`/`loc.`/`sub.`/`artifact.`) and slice ids are `encounter.slice.*`, matching none, so the veil was the only leaking surface. And re-deriving the membership predicate across all of `src/data/` (as the ticket instructed, rather than trusting its list) confirmed exactly eight and surfaced one deliberate exclusion: `action-technical-effects.ts` cites `THR-605` in nine strings, but that is `technicalEffect`, **wiki-facing by contract** and legitimately ticket-citing — a guard that included it would fail on correct content, and those strings are already tracked by THR-1075. The guard is therefore pool-wide over `description` + `narrativeTemplates.*` across all 572 templates, not slice-scoped, because the defect was a habit of authoring rather than a property of one file; it was **falsified before being trusted** (reintroducing `Hook #205 … THR-883` into one description fails it on all three metadata classes, naming the matched token rather than the head of the string, since the metadata sits at the tail). A companion assertion pins that the eight still *have* a description — deleting them would have been a different regression on the same surface — and a non-empty-population check keeps the sweep from passing vacuously. **Laws, judged on the composed surface rather than the diff:** Law 14 is the one that was broken (`THR-883`, `Hook #205` are internal ids on a mortal-facing surface) and is now clean; **Law 13 was mis-cited by the ticket** — it governs *magnitudes*, and a hook number is not one, so 14 covers this; 13 independently holds live (`FAIR`, `Uncertain`, `Moderate threat` — no numerals). **Law 43 likewise is adjacent, not literal**: it forbids a raw `{token}` reaching a screen, and none did; the new prose still routes through `enrichProse`. Law 1 genuinely improves — the removed follow-on names (*The Full Moon Collection*, *The Swindler Found*, *The Grateful Kin*) were concepts in text only, carrying no image, tooltip or link; the new strings name no bare concept, and on screen Vara resolves as a link and Eye as a chip. Laws 17/21/37 untouched and non-regressed. Verified on the measured route (`?spawn=encounter.slice.bargain_at_crossroads`) at a real 1920×1080 with a wrong-tree guard probing a phrase unique to this change: the dialog matches `/Hook #|THR-\d+|Vertical-slice encounter/` **zero** times across every leaf element, the subtitle renders the new line at the same 16px slot the ticket measured, in-viewport with no horizontal scroll. Console carries one pre-existing error (a TTS sidecar on `:3001` that is not running locally) and the standing GraphSchema/THREE warnings, none attributable to a prose edit. 987 files / 15,581 tests green, ratchet unchanged at 3450, build clean; no engine file touched, so the smoke is N/A. (THR-1078, 2026-08-10)
