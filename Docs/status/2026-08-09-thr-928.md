**Every trace the engine emits is now selectable in the trace inspector.** `TraceCategory` and its runtime mirror `TRACE_CATEGORIES` were missing **38 categories** that live `emitTrace` call sites emit, and because `DebugPanel` seeds its enabled-category set from that array and filters on membership, each one was **invisible in the inspector** no matter how faithfully it was emitted — an NFP #2 defect, not a typing nicety. Both now hold the same 234-member set, pinned in both directions by a new gate. Three of the 38 (`graph_op_execution`, `choice_set_player_resolved`, `choice_set_player_dismissed`) already had wired `TraceEntry` members and already sat in the array, so their absence from the union made the `TRACE_CATEGORIES: TraceCategory[]` annotation itself fail to typecheck; the ratchet fell **3458 → 3452**, the reduction the ticket predicted. **The ticket's own premise was wrong and the correction is the finding:** `forecast_computed` has been a `TraceCategory` member since 2026-05-07 (commit `9e02bb24`), three months before the ticket was filed against its absence. The TS2367 the author hit is real but has a different cause — `getTraces()` returns `TraceEntry`, a discriminated union, so `trace.category` narrows to the literals declared by the union's *members*, which is a different set from whatever `TraceCategory` lists. `ForecastComputedTrace` exists and was never added to `TraceEntry`; **20 payload interfaces are orphaned that way**, including `TickHealthTrace` and `TickCrashTrace`, the two a crash investigation reaches for first. That half is filed as **THR-1065** with a shrinking ratchet as its predicate, not fixed here: none of the 20 extends `TraceBase`, so wiring them in as-is would make `trace.id` and `trace.summary` unreadable on the union and *raise* the baseline instead of lowering it — and the 81 `as unknown as` casts at emitTrace boundaries are held in place by a second, independent cause (`Omit` over a discriminated union collapses to the shared keys), which needs a distributive omit rather than more categories. The laundering in THR-924's test therefore stays, but its comment no longer asserts the wrong mechanism. **The gate is the deliverable, not the 38 additions:** `src/types/__tests__/trace-vocabulary.test.ts` pins union ≡ array, every emitted category registered, and the orphan ratchet, with vacuity guards on the scan's own population — it found 4 dotted categories (`chronicle.aggregated`, `chronicle.aggregate_failed`, `naming.constrained_reject`, `rival.scheme_phase_advanced`) that a first hand-written scan keyed on `/[a-z0-9_]+/` had missed, and it was falsified by removing a category to confirm it fails with the emit site named. One latent trap fixed in passing: a comment mid-union read `THR-339 wiring; emitters …`, and a scan delimiting on the first `;` stops there and reports the ~120 members below as unregistered — which is how this run's first pass produced a false 76-category finding before catching itself. 977 files / 15,449 tests green, build clean; no engine smoke and no browser evidence — the diff is one type file, one new test and two comments, and `src/types/trace.ts` is outside the engine-smoke and UI-pillar triggers. (THR-928, 2026-08-09)
