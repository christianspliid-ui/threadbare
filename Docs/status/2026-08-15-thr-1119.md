# THR-1119 — home-tree autosync stall repaired, stranded content rescued (2026-08-15)

**The stall:** autosync skipped 13 consecutive hourly runs on "3 tracked modification(s) would be overwritten", leaving the home tree 29 commits behind by repair time. The THR-937 containment correctly refuses this shape — modified tracked files differ in content, so auto-resolution *would* lose work.

**The triage, from an attended session (THR-672 bars every lane from this):** only `Docs/impediments.md` actually collided — `git diff --name-only HEAD..origin/main` showed the two `.claude/settings*.json` files untouched by the incoming commits, so the ticket's "three files block" premise was one file wide of the mark; git only refuses files the fast-forward must update. The impediments diff was an uncommitted row (#582-as-written) from a Threadbearer automation lane that could not push — the same stranded row main's impediment #582 (the *published* one) already predicted would need renumbering if rescued. Stashed (`home-tree-recovery`), fast-forwarded clean to `origin/main` (0 behind), and re-filed the row as **#590** via this PR. The settings modifications — a read-only-tools `permissions.allow` block in project settings plus accumulated attended-session grants in `settings.local.json` — were inspected and **committed** (this PR), the Done-when's preferred arm and the b0a695a9 precedent; committing them also removes the latent re-stall the next time `main` touches either file.

**Containment decision (Done-when 3):** accept the containment as-is; the detector half already exists. `keep-work-flowing-cc` surfaced this stall in `Design/user-actions.md` with per-hour failure counts, and the orchestrator filed this ticket High the same morning — detection-to-ticket was under 12 hours with zero new machinery. Auto-repair for modified tracked files stays deliberately out of `threadbare-autosync.ps1`: the content at risk was real (a stranded impediment row), which is exactly the case judgment-free automation would have discarded.
