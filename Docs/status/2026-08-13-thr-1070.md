**The last raw tick numeral left a mortal-facing surface, and the vocabulary it needed did not exist yet.** `EncounterVeil`'s light-tier strip rendered `auto-resolves in 4 ticks` — Law 13's plainest violation, and the only numeral on a surface whose aftermath already bands everything beside it (`grew steadily`, `spent something on this crossing`), which is what made it read as a leak rather than a house style. The wiring was one template literal; the work was the scale, because Threadbare had words for difficulty (`gentle/fair/steep/severe`) and forecast (`doomed/perilous/uncertain/favorable/fated`) and none at all for a **wait**. `getDurationWord` bands the live 1–8 range (`RETINUE_VIGNETTE_TIMEOUT` is 8) into three: `in a moment` / `shortly` / `before long`, lowercase and prepositional so they complete the caller's sentence, with thresholds in `DURATION_BAND_MAX_TICKS` so retuning the feel is a number change (NFP #1). **Three bands, not the five every other scale in `domain-words.ts` uses** — the others band a stat, this bands a wait, and with a 12-tick game day the whole scale fits inside one day; five bands over eight ticks would offer distinctions the player cannot act on. The ticket's straw-man `soon` was dropped as a near-synonym of `shortly` that does not read as ordered beside it. THR-1068's `auto-resolving now` zero case is deliberately **not** a band, since it is a different verb inflection owned by the caller — which keeps the scale free of an unconsumed entry. **The guard is a sweep, not a spot check**, because the ticket's own diagnosis was that no routine review URL reaches this strip (it needs `threadTier === 'light'` *and* a non-null `autoResolveTick`, which `?spawn=` never produces): every tick 0–8 renders the real component and asserts the label carries no digit, and the guard was falsified by reintroducing the numeral (1/1 red, reproducing the filed string). Structural pins keep a future word from becoming unreachable — one threshold per boundary, ascending, every band reached from some tick in the live range. 999 files / 15,965 tests green, ratchet unchanged at 3447, build clean; no engine or tick-loop file touched, so no smoke owed. **Browser-verify substituted, pixel capture owed** — impediment #546 recurs, `preview_start` refused verbatim again. Honest by #546's own scope test: the widest new wording (`auto-resolves before long`) is *one character* wider than the widest numeral wording it replaces, inside a `nowrap` label in a self-sizing flex row, so no wrap, overflow, or viewport-contract failure is structurally reachable — and that claim is pinned by a composed-strip assertion rather than asserted in a commit message. (THR-1070, 2026-08-13)
