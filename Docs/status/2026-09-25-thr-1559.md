# 2026-09-25 — THR-1559: Monster in the world-objects registry (Hunts H1)

**Ticket:** [THR-1559](https://linear.app/threadbare/issue/THR-1559) · **Project:** Physical Conflict · **Plan:** `Docs/plans/2026-09-23-hunts.md` (slice H1) · **Pillars:** Engine (registry row note) · Content (UL term + canon row) · UI N/A (`Browser-verify exempt: registry and docs only`; the codex/grid monster class cells are H2's).

## What shipped

- **The Mortal row names its Monster class** (`src/data/world-objects.ts`): a lair's beast is a class of Mortal, not a kind (THR-1268) — an `individual` actor carrying `isMonsterElite` or a `monsterState` card, read only through `isMonster` (THR-1544). Follows the Realm precedent: a note, not a `classes` map, because the discriminator is not `actorType`.
- **UL `Monster`** seated by delegation 2026-09-25 in `Docs/ubiquitous-language/Agents.md` (after Realm), indexed in the UL README.
- **Canon row amended** (`Docs/canon/world-objects.md`): "one undertaking cell acts on a Mortal and only one" becomes `destroy` per class — the plot on a person, the hunt (`destroy × monster`) on a Monster. Classes column gains *monster*.
- Regenerated `world-objects.generated.md` / `public/world-objects-reference.html` (drift verdict: 0 unregistered, 0 phantom) and the UL dashboard.

## Evidence

`npm test` 1332 files / 21610 passed; `test:heavy` 36 files / 219 passed; typecheck ratchet OK (2818, unchanged); `vite build` ok; CLI seed 42 medium reached tick 30 with 487 agents.

## Next

H2 ([THR-1560](https://linear.app/threadbare/issue/THR-1560)): the hunt itself — the `monster` undertaking object type, its reason doors and the confront appointment.
