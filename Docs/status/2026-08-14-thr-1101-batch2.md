**The duel family stops reading as mad-libs** — THR-1101 batch 2, resumed from batch 1's checkpoint. Batch 1 drained the `story_beat` *tier* and its closing note recommended taking the next slice **by encounter family instead**, so one run's output reads as a single coherent voice; that is what this batch does. All 10 token-carrying `encounterType: 'duel'` templates authored out of the `{adj}`/`{verb}`/`{noun}`/`{action}` shape — `warlords_crucible`, `tavern_brawl`, `arcane_duel`, `arena_combat`, `shadow_ambush`, `honor_duel`, `pirate_raid`, `bandit_ambush`, `confront_the_unknown`, `spar_with_a_stranger` — 28 steps, 82 prose lines. **The family is now drained**, which is a predicate the retro can re-check rather than a count that rots. Register calibrated against `encounter.trial_by_combat`, the THR-1036 exemplar, which `honor_duel` sits **directly beside in the file** — the most visible remaining quality seam, and the same argument batch 1 used for its own slice. Predicate re-derived at pickup rather than trusted: **145** templates carried tokens against the checkpoint's stated 146, the one-template gap being a definitional difference in how comment lines are excluded, not drift. After this batch: **135**. **The scan found its own false positive and it is the reusable lesson** — a per-template token scan that treats the file's *trailing* JSDoc as belonging to the last template reported `confront_standoff` as carrying 4 tokens when it is fully authored, because a `/** … */` continuation line starts with `*`, not `//`. Attributing the tail comment to the last template inflates exactly one entry per file and always the same one, so the batch was scoped on the verified 10 rather than a nominal 11. **Verification went past the gate deliberately, because the gate cannot see prose quality.** The regression lock passes at any population and the suite proves no raw token *leaks*; neither says the sentences read. So every added line was rendered through `enrichProse` — the function the game actually calls — and read back: 82 lines, zero raw tokens, names and pronouns substituting correctly. That render pass is also what caught three lines that were clean on the page and ambiguous once resolved (a `Their` that read as the actor's rather than the bandits', a `The rest of {them} is not` that resolved ungrammatically, and a `He`/`{actor}` collision in `shadow_ambush`), all three rewritten. The probe was a scratch test file, deleted before commit. A vagueness sweep over the added lines returns **zero** hits across all three sets — evasive, natural-indefinite and intensifier — applying the strictest (`outcome`) scope to every line, which is stricter than the scene-prose fields actually owe. **The `>50` regression lock is unchanged, deliberately**: at 135 it still holds with wide margin, so no lowering was owed. Recording the figure so whichever batch crosses 50 inherits it rather than re-deriving it — that batch owes the deliberate lowering or retirement the ticket asks for, with the reason recorded. 1008 files / 16,033 tests green, ratchet unchanged at 3447, build clean; engine smoke run although not owed (tick 30, 383 agents, 45 events — and `spar_with_a_stranger`, one of the rewritten templates, resolved live in that run). **No browser evidence owed and none claimed** — prose strings in a data module fire no UI-pillar path trigger. Sibling THR-1102 remains sequenced behind this campaign; 135 templates still consume the `{adj}` ladder, so it is not yet moot. Remaining: 135 templates across 9 families, `explore` (30) the largest. (THR-1101, 2026-08-14)
