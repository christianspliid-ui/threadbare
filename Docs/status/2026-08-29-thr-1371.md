# 2026-08-29 — THR-1371 · R5-T3 brief generator + card wiki + attachment reference (context-cleanup round 5)

The compiled authoring brief — the first read of every draft agent — hardcoded the pre-Doctrine-v2 peak-surface list in its generator (`scripts/build-authoring-brief.ts` L63: "the final step's band prose, the fate-reveal line, major aftermath beats"). That is the THR-1250 failure shape (polluted preamble) recurring inside the surface built to fix it. Fixed to the v2 non-encounter list (doom transitions, Twilight, World-Soul/Echo) and `Docs/authoring-brief.md` regenerated.

Also:
- `public/nudge-cards-reference.html` — "Spec authority" repointed from the drifted 2026-07-30 frameworks plan to the nudge-authoring-spec (both header comment and body); the Repertoire section's DESIGN badge corrected to IMPL (its own body says "Live as of THR-887"); the dead `.anatomy-quote` CSS class dropped (the quote element retired with THR-1225).
- `Docs/attachment-primitive-reference.md` — the six live effect kinds the card omitted are now on it (`duration`, `aura`, `reveal`, `suppress`, `alter_terrain`, `stat_contribution` — field lists verified against `src/types/effects.ts`), plus the `MAX_EFFECTS_PER_ATTACHMENT`(8) / `ACTION_TRIGGER_MAX_PER_ATTACHMENT`(2) caps and the choice_set player-surface-only rule.
