# The retirements finally leave the operative layer (THR-1331)

**Two finished rulings had never finished.** Codex retired 2026-06-23 (THR-486), Cowork retired 2026-07-21 (THR-654), and the Obsidian MCP stopped being the vault path the same day — yet the 2026-08-28 general-context audit (round 1 of the context-cleanup program, directed by Christian in chat) found every one of them still taught as live by the surfaces agents load first. The UL — the authority that wins terminology disputes — held Cowork at `Status: canonical` in present tense. intent-judge, the gate every plan doc passes before Ready for Dev, framed its whole protocol around Cowork and had been re-stamped valid a month after the retirement. CLAUDE.md instructed "via Obsidian MCP" four times while its own sandbox section said filesystem-only. The design brief's audience line named all three retired parties on the literal first page of every design session.

This sweep disposes of all of it in one docs-only PR: UL Coordination shard and index (Cowork joins Codex as properly deprecated), intent-judge + its proposal template, design-council + its coordination role prompt, session-handoff, design-session's listing description, the state-of-game-design router + two shards, CLAUDE.md's four MCP lines, the design brief, the coordination protocol's four residue lines (including the instruction to hand-edit the generated `project-status.md`, THR-1016), the user-review-interface plan's lane naming and long-shipped "remaining deliverables", ways-of-working's pre-THR-760 routing line, and a THR-1252-pattern tombstone on `cowork-ways-of-working.md` — the file that was swept as current four weeks before its whole subject retired, and so looked freshly tended while every rule in it was dead.

**The structural half:** `Docs/guidance-manifest.json` grows from one doctrine to four. `identity-vision` (authorities: the design brief + rulebook quick-reference), `cosmology` (Eight Reaches × Twelve Spheres), and `coordination-process` (process canon + design governance) are now registered with their dependents and vault manualDependents, so the next direction change in the general layer arms the sweep gate and names its restatement sites — the reason these retirements haunted the layer for five weeks is that nothing did. Companion tickets carry the rest of the round: THR-1332 (Cowork hooks out of settings.json), THR-1333 (Threadbearer naming + Eight Reaches), THR-1334 (single-authority dedup), THR-1335 (vault rebuild), THR-1336 (CLAUDE.md diet).
