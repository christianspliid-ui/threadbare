**Consequence chips can now reach the thing the ending granted (THR-1120).** Christian's THR-974 verdict was *"I am seeing no links to any reward or penalty attachments anywhere"*, and the ticket's own instruction — verify the noun before the verb — is what decided the shape of the fix. **The chip payload carried no granted-entity id, and it could not have carried the obvious one:** the grant is written by the reaction's `condition_attachment` / `attachment_grant` effects, which apply only when the player *picks*, and the veil closes on the pick (`closeAfterSelection` defaults true). At the moment the chip is on screen the granted node does not exist. What does exist is the **template** — `instantiateReward` resolves it with `graph.getNode(templateNodeId)` — so the honest link is to the thing being granted, reachable a second time from the bearer's Attachments tab once it lands. `visualKind` gained an `attachment` member: the one kind with a page and no entity-visual family, since an attachment's art lives on its own node and `AttachmentDetailView` draws it. **The `EntityVisualKind` union enforces that at compile time** — `resolveIcon`'s guard cannot be deleted without a type error — which is how the icon path stays honest rather than by a comment asking future readers to remember. The association between a chip and its grant is **declared, never matched**: Law 2 forbids reading `wounded` against `trait.condition.wounded` in English, so ten slice state-nouns name their template explicitly. The anti-rot gate is the load-bearing test and it asserts the pairing *per band* — an author retuning a band to grant `exhausted` instead of `wounded` would otherwise leave a chip linking a condition nobody receives, and the link would still open a real sheet, so nothing else on the surface would show it. Falsified before being trusted: one declaration repointed, the bridge case went red naming both ids; reverted. **Browser-verify took the jsdom substitution** — `preview_start` was refused with the unattended string — and the render pair is a controlled arm rather than a single assertion: same fixture, `onSelectEntity` present draws a real `<button>` whose click hands back `('trait.condition.wounded', 'attachment')`, absent leaves plain text, and a chip granting nothing draws no control at all. **The UI-Laws pass found one pre-existing violation and filed it** (Law 17: the tooltip registry has no `condition.*`/`trait.*` prefix, so a condition word cannot carry a registry tooltip today — the link tier now lands, the hover tier still cannot). 1010 files / 16,205 tests green, ratchet unchanged at 3192, build clean in 16.22s.
