# 2026-08-29 — THR-1365: R4-T4 wiring surfaces + tombstones + false-green comments

Context-cleanup round 4 (technical architecture), sweep ticket 4 of 6. `Docs/plans/wiring-checklist.md`: the freshness stamp no longer lies about its own body (August sections land below the stamp and it now says so); the registered-phase table completed from 15 to the full 26 `ENGINE_PHASES` descriptors with a source-of-truth pointer; both Cowork-as-live-process references excised (retired 2026-07-21, THR-654); the pre-commit block now defers to `Docs/canon/verification-gates.md`. The THR-238 phase-registry plan doc's status corrected from "Ready for Dev" to shipped-2026-04-29 with honest deltas (runTick ~1,174 lines vs the <200 target; 26 registered vs ~70 inline), and its four `npx tsc --noEmit` teachings replaced with the ratchet. The systemic wiring guide's three self-contradictions fixed: Anti-Pattern 4 no longer teaches the pre-THR-697 family-seeding stub (the live anti-pattern is the inverse — templateId distrust), both effect-kind counts (23/18) now point at `KNOWN_AFTERMATH_EFFECT_KINDS`, and the "seven capabilities" framing plus cross-Part numbering ambiguity resolved. Tombstoned `Docs/content-expansion-checklist.md` (March work queue teaching the forbidden typecheck) and `Docs/agent-spawn-assessment.md` (its locationId-removal recommendation never landed and read as current direction). Code: the false-green comment in `src/engine/phases/index.ts` (claimed CI catches boot throws via `npx tsc --noEmit`) and the stale `src/types/graph.ts` comments (hexes-as-location-nodes; the pre-THR-1183 dual-mint block in present tense) corrected; anchor-catalog preamble contradiction ("there is no sublocation NodeType") fixed and catalog regenerated. Audit: https://claude.ai/code/artifact/cba8307e-e4e5-4b4a-97ca-3ac07fa0866f
