# The brainstorm home stops teaching yesterday's game (THR-1335)

**The vault is where every brainstorm begins (THR-918) — and the audit found it the most polluted layer in the project.** Its own CLAUDE.md, self-declared "the authoritative guide for all agents working in this vault," taught the retired Obsidian MCP as the only access path, the dead `.planning/BACKLOG.md`, a "Godling" player, and a daily tick model — and never mentioned `Vision/`. The mandated entry point, `Index.md`, carried 40 dead links, a fabricated 20-page Assets section, and presented the rejected Agent Wheel with its "9 action slots" as a live system. The 739-line "canonical" Systems Overview taught the pre-nudge Nudge/Amplify/Force/Block menu with numeric odds. And the Vision reading order sent designers to the two stalest files while omitting `taste-profile.md` — the one file that knows the current game — from its index entirely.

All filesystem writes (the vault has no git; the old CLAUDE.md and Index are backed up in the session scratchpad): vault CLAUDE.md rewritten small and current (Threadbearer, Ascendant, 12-tick turn, the filesystem access rule, Vision/ in the structure table, pointers to repo canon); `Index.md` regenerated via `npm run rebuild-index` — 442 pages, and because the generator takes each page's first sentence, the deprecation banners now lead their own index entries; banners on Agent Wheel, Psyche Strands, Systems Overview (page-level plus §5 Godling/loop and the §5.5 intervention menu), and the 2026-03-04 Agent Teams Guide; Product Strategy and Build Status rerouted to Linear; the stray root-level `Brainstorms/` (one file, invisible beside the real folder's sixteen) merged in, the severed root `log.md` archived, two 0-byte strays deleted.

**The Vision refresh Christian approved ("go ahead. that menu has been out forever"):** `00-north-star`, `01-core-loop`, and `02-non-negotiables` swept onto the nudge-card vocabulary — the god bends odds, fate picks the band, mortals decide forks — with the tick length recorded as settled and the Doom/Mandate pressure named in the north star. Every edit carries a dated iteration signature per the folder's own convention; the premises themselves are unchanged, which is the point — the substance was already the nudge model before the nudge model existed. `README.md` now indexes all six files and the reading order ends at the taste profile.

**Honest residue:** `Systems Overview` is bannered, not rewritten (scoped to the content round); the Flesh pages remain regenerable from `world-model.json` (same round); the Index generator's own header still says "The Fantasy World Simulator" (a `scripts/` edit — code track — left for the content round rather than mixing tracks here).
