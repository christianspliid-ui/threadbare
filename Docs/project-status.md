# Project Status
> Updated 2026-05-08.

## Current Focus
**THR-334 complete (2026-05-08) — Encounter UI Phase D1: ThreadOverlay + useThreadReveal (Moment 1 tension reveal).** New `ThreadOverlay` SVG component animates the 5-beat 1.62s tension reveal sequence (commit → inhale → drawing → taut → resolving → settled). Sphere-coloured threads via `stroke-dashoffset` animation; dust motes on taut beat; chosen thread brightens at resolve, unchosen desaturate to 35%/35%. `useThreadReveal` hook manages phase machine with `onResolveBeat` callback for post-v1 audio. 9 tests. PR #207.
**THR-289 complete — UL Interactive Dashboard shipped (Continuous Improvement).** Added in-app browseable + searchable glossary at `?view=ul` joining the reference-surface family (`?view=cms` / `?view=codex` / `?view=styleguide`). New build-time generator (`scripts/generate-ul-dashboard-data.ts`) parses the seven UL shards into a typed JSON snapshot; auto-runs as `prebuild`. Components in `src/components/UL/` (Dashboard + Sidebar + TermTable + DetailPane + SearchBox + DriftBadge + ulMarkdown + ulSearch) deliver shard tabs, debounced cross-shard search with name/alias/body ranking, See-Also chip navigation within the dashboard, drift badges (empty `drift-scan-status.json` v1; live emit deferred), and a footer disclosure of build warnings. Drift constants extracted to browser-safe `scripts/drift-scan/constants.ts` so the dashboard and the weekly scan share one source of truth. IA manifest gains three `ul.*` surfaces; StartPage gains a "Ubiquitous Language" entry; CLAUDE.md Dev Quick-Start URLs and `Docs/plans/wiring-checklist.md` updated. Fail-soft per plan §12; viewport contract holds at 1920×1080 (no scroll). Verification: `npm test` 717 files / 10999 tests pass, `npx tsc --noEmit` clean, `npx vite build` clean (9.45s); browser verified at `?view=ul` (80 terms / 7 shards rendered, search debounced, wikilink click navigates within dashboard, no console errors). 5 deferrals filed under THR-289.
**THR-360 complete — source code flesh cleanup Bucket 0 (Ready-for-Codex).** Removed residual `flesh` usage from `MARTIAL_SPHERES`, StyleGuide sample `reachPreferences`, the legacy `domain-words` comment stub, and the stale AgentDots 9-reach test fixture (now 8 reaches). Verification gates green: `npm test`, `npx tsc --noEmit`, `npx vite build`.
**THR-361 complete — TB-075 Phase 5c mechanical fixture cleanup (Ready-for-Codex).** Removed deprecated `flesh` reach literals from the bound engine test fixtures and stale lexicon residue in `src/types/traits.ts`, preserving strict `ReachDomain` fixture shapes while leaving Bucket 2/3 production/content surfaces untouched. Verification gates green: `npm test`, `npx tsc --noEmit`, `npx vite build`.
**THR-345 complete — Encounter UI Phase G3 content lint gate (Ready-for-Codex).** Added `scripts/lint-encounter-content.ts` and wired `npm run lint:encounter-content` into CI. Lint now enforces binary content-time rules for EncounterContract authoring (R1 tooltip ID resolution, R2 `consumes_item` consistency, R3 no digits in `forecast_factors`, R4 no probability words) while keeping prose-quality drift as non-blocking R5 warnings; shipped a good/bad fixture corpus and focused test suite.
**THR-344 complete — Encounter UI Phase G2 snapshot hardening (Ready-for-Codex).** Added required 1920×1080 fixture expansions in existing snapshot suites (`EncounterChoiceCard`, `OutcomeForecastBand`, `DetailModal`) and introduced net-new snapshot suites for `EiraHeroPanel`, `CapabilityStrip`, and full `EncounterScreen` layout states (full + minimum). Added required 2560×1440 sample snapshots for `EncounterScreen` and `DetailModal`, kept scope test-only (no source component edits), and created follow-up **THR-364** for deferred C4/D1/D2 snapshot surfaces per tightened handoff spec. Verification gates green: `npm test`, `npx tsc --noEmit`, `npx vite build`.
**THR-177 complete — AgentDetailPanel v7 visual overhaul (UI Visual Overhaul Design System v1).** Migrated `src/components/Game/AgentDetailPanel.tsx` (~850 lines) and `src/components/Game/AttachmentRow.tsx` from legacy Tailwind `bg-stone-*` / `text-amber-*` / `border-amber-*` classes (66 occurrences eliminated) to v7 CSS custom properties (`--bg-deep`, `--bg-raised`, `--text-primary/secondary/tertiary/muted`, `--border-subtle`, `--accent-gold`, `--positive`, `--negative`). Capability profile refactored from horizontal bars to v7 5-dot meter rows with sphere coloring via the `data-reach` cascade (`--reach-sphere-bright` per row). Added `activeVow` prop to `AttachmentRow` (panel-gold border + `linear-gradient(90deg, rgba(170,68,221,.12), --bg-raised)` sphere-spirit wash + small "VOW · ACTIVE NOW" caplabel) and applied it to the first row in Powers & Agreements. Added `.trait-row:hover` rule (`--bg-hover`) to replace removed `hover:bg-stone-800/60`. All data surfaces, click handlers, ARIA labels, and tooltip behavior preserved verbatim — no information density reduction. Verification gates green: `npm test` (23 AgentDetailPanel tests pass; 610 Game-component tests pass); `npx tsc --noEmit` clean; `npx vite build` clean (8.99s). **Note:** `AgentDetailPanel` is no longer wired into the live UI (replaced by `AgentInfoCard` per THR-105 era; only its own test file imports it). Migration is therefore a token-correctness pass that prepares the file for future re-wiring; the "select agent in `?view=game&seeded`" visual-verification path in the original handoff was infeasible. AgentInfoCard (the live consumer) was already fully on v7 tokens.
**THR-176 complete — TopBar v7 visual overhaul (UI Visual Overhaul Design System v1).** Replaced the stellaris-style top bar with the v7 visual language: solid `--bg-deep` background (no gradient), `--border-subtle` hairline bottom border (no gold), `--border-subtle` group divider (no gold), removed the `rgba(10,10,14,0.4)` right-group "tab" container in favor of spacing-only separation. Each indicator (`SimulationControls` compact, `WorldSoulIndicator`, `AttentionPoolIndicator`, `DoomBar`, `OmenIndicator`) now follows the two-tier pattern with an ALLCAPS Cinzel `--text-tertiary` section label above the existing value tier; `RivalsButton` adds an inline "Rivals" label visible at ≥1600px via existing `topbar-compact-hide`. Added two new CSS classes (`.topbar-section-label` + `.topbar-tier`) sourced entirely from existing tokens — no new magic numbers. All data surfaces and click handlers preserved verbatim. Verification gates green: `npm test` 708 files / 10965 tests, `npx tsc --noEmit`, `npx vite build`; CSS targets confirmed via `preview_eval` at 1920×1080 (`--bg-deep` bg, `rgb(42,37,32)` hairlines, 5 section labels in `rgb(168,152,128)` Cinzel 700).
**THR-362 complete — THR-318 Stream 1 Pass 2 faction contract migration shipped.** Added shared `src/data/encounter-contract-builder.ts` (extracted Pass-1 helper API with fail-soft defaults) and migrated 11 faction modules so encounter + social template arrays emit encoded `__encounter_contract_v1` metadata through `withEncounterContract`; added `faction-encounter-contract-migration.test.ts` to round-trip all migrated templates through `parseEncounterContract` and assert coercive/non-coercive pole heuristic behavior.
**THR-340 complete — Encounter UI Phase F2 world view → encounter handoff (Encounter Experience).** Added `encounterHandoff.ts` (orchestration: emits `spotlight_changed` trace, returns spotlight + transition timing, silent audio-cue hook for post-v1 H1) and `AgentPulseOverlay.tsx` (Three.js camera-projected hex pulse with thread-color flare; rAF-driven, fail-soft on missing camera/agent). Wired through `HexMapV2` (new `spotlightedAgentId` + `spotlightThreadColor` props), `GameView.handleEncounterClick` (calls `prepareEncounterHandoff`, writes `gameState.spotlightedAgent`), and `closeEncounterModalAndResume` (clears spotlight on dismiss). Priority pip added to `ThreadsPanel.CompactThreadRow` (active-rendered surface) and `RetinuePanel` (kept in sync). 400ms `anim-encounter-handoff-fade-up` keyframe wraps `EncounterVeil` mount. 10824 tests pass; tsc/build green. PR #184.
**THR-339 complete — Encounter UI Phase F1 DebugPanel inspectors + trace-category registration shipped.** Added five new DebugPanel inspector tabs (Choices, Drift, Hand, Detection, Forecast) backed by standalone components in `src/components/Game/debug/` (EncounterChoiceInspector, DriftVisualiser, HandStateInspector, DetectionStateInspector, ForecastFactorsInspector). Wired `gameState.archetypeDrift` through DebugPanel → DebugTabContent. Registered eight encounter-experience trace categories (`choice_resolved`, `forecast_computed`, `hand_filtered`, `drift_threshold_crossed`, `detection_threshold_crossed`, `item_consumed_by_choice`, `spotlight_changed`, `callback_eligibility_computed`) in `TRACE_CATEGORIES`/`TraceCategory`. Verified all four orchestrator phases (`phaseDetectionPressure`, `phaseChoiceResolution`, `phaseAscendantHandFilter`, `phaseDriftDecay`) already wired in correct semantic order — detection reads pendingChoiceCommits before resolution consumes them. Updated `Docs/plans/wiring-checklist.md` ViewMode list and trace registry. Verification gates green (`npm test` 706 files / 10957 tests pass, `npx tsc --noEmit`, `npx vite build`, CLI `tick 30` clean) and live preview confirms all 5 tabs render with no console errors.
**THR-333 complete — Encounter UI Phase C4 scene-state panel shipped.** Added right-rail bottom surface components (`SceneStatePanel`, `ThreadStrip`, `DriftIndicator`, `DetectionThread`) with qualitative-only rendering of scene threads/factions/place/protagonist conditions, drift tier emphasis (soft/banner/becoming) with dismiss + fresh-threshold return, and detection thread escalation (notice/turn/encounter). Added 1920x1080 snapshot coverage in `SceneStatePanel.test.tsx`; full verification green (`npm test`, `npx tsc --noEmit`, `npx vite build`).
**THR-343 complete — Encounter UI Phase G1 engine test hardening shipped.** Added new contract suite src/engine/__tests__/contracts/encounter-experience.contract.test.ts (choice-resolution + drift registration, detection threshold one-shot seeding, item consumption ordering, gates_to unlock behavior, hand-filter cascade, and 30-tick medium-map smoke) and extended Phase B unit suites for choice-resolution fail-soft NaN handling, hand-filter cascade/empty hand coverage, detection-pressure decay/clamp behavior, aftermath drift-register routing, callback pin-order enforcement, and prose-safe forecast factors. Verification gates green: 
pm test 57.92s, 
px tsc --noEmit, 
px vite build.
**THR-359 complete — Phase 5b repo-side cosmological canon cleanup.** Removed duplicated Part 6/7 tail block from `state-of-game-design` skill (both mirrors). Inverted stale 9-reaches guidance in encounter-toolkit vision audit §4.4/§7. Added pre-flight verification note to cosmological drift audit. Follow-on THR-360 filed for code-side Category B residue.
**THR-332 complete — Encounter UI Phase C3 right rail shipped.** Added AscendantHand (playable/dimmed/hidden card states from B3 partition + three-card disclosure + rare pulse + direct-commit callback), CastRail grouped by attention_priority (primary/background/offstage), and CastTile with relationship-node-first "to her" fallback to edge sentiment plus E1/E2 click-gate TODO marker. Added 1920x1080 snapshot coverage for cast cardinalities (0/1/4/6) and hand states (0/3/7+, dimmed prereq).
**THR-355 complete — Phase 1 canon convention verified and closed.** Confirmed all THR-304 Phase 1 deliverables (`Docs/canon/README.md`, `encounters.md`, `cosmology.md`, CLAUDE.md Canon Pages table, documentation-ownership.md entry) are complete from prior THR-305 work. Added missing `2026-05-05-encounter-ui-implementation-phasing.md` pointer to `Docs/canon/encounters.md` Active design plans.
**THR-329 complete — Encounter UI Phase B7 callback eligibility computation shipped.** Added `src/engine/callbackEligibility.ts` as a deterministic pure scoring module that resolves “moments that could echo” with author-pinned `callback_candidates[]` priority and graph-derived slack fill up to 3 candidates. Ranking uses named tunable constants for recency decay (50+ ticks => 0.5x), beat-context relevance overlap (cast/place/faction/sphere), and structural/incidental emotional weighting (1.5x/1.0x), with fail-soft empty-history handling and deterministic tie-break ordering. Added focused tests in `src/engine/__tests__/callbackEligibility.test.ts` for pinned override, heuristic ranking, tie-breaking, and empty-history behavior.
**THR-326 complete — Encounter UI Phase B4 regional detection pressure shipped.** Added a dedicated `phaseDetectionPressure` run before `phaseChoiceResolution` to apply per-choice pressure gain by region, per-tick decay, threshold crossing traces (`detection_threshold_crossed`), and rival encounter seed enqueue at encounter threshold. Added deterministic helper module coverage and surfaced `regionalDetectionPressure` in DebugPanel Encounter Seeds tab for inspectability.
**THR-357 complete — branching encounter contract metadata migration shipped.** All 23 files in `src/data/encounters/` now emit encoded `EncounterContract` metadata from authored choice data, replacing default-inferred `moral_axis_pole` at adapter fallback. Added `encounter-contract-migration.test.ts` smoke coverage to assert every branching template loads encoded metadata and round-trips through `adaptUnifiedActionTemplateToEncounterContract` with explicit poles present on every choice.
**THR-352 complete — Encounter UI Phase A3 keyframes canonicalized in runtime CSS.** Added missing `thrum-fade` and canonical aliases (`threadDraw`, `threadPulseTaut`, `dustMoteDrift`, `cardFlipReveal`, `pageDim8pct`, `breatheBrightNode`) in `src/index.css`, retained hyphen-case compatibility for v7 references, and annotated each keyframe with Moment/beat consumer comments for inspectability.
**THR-331 complete — Encounter UI Phase C2: EncounterChoiceCard + OutcomeForecastBand + MoralAxisTilt.** Three new UI primitives in `src/components/Game/Encounter/` rendering the center-column divine-choice surface. Forecast band derives a 5-tier qualitative read (`doomed`/`perilous`/`uncertain`/`favorable`/`favored`) from a numeric `successProbability` prop and surfaces 1 factor by default, hover-expanding to up to `FORECAST_FACTORS_VISIBLE_HOVER_MAX = 4`; the numeric input is never displayed. Choice card renders the full §5.3 anatomy — sphere label · cost, god-verb, agent reaction, tilts-toward, `MoralAxisTilt` line (`↬ tilts toward CONQUEROR`-style), fail-forward (`↗ on fail — …`), optional consumes-item indicator. Sphere colour resolution uses the `data-reach` cascade in `src/index.css`; `quintessence` falls back to `--accent-gold`. Wired into `?view=styleguide` for visual verification at 1920×1080. 26 jsdom tests cover keyboard activation, selected-state border, all five tier labels, and a numeric-string regression check (`/[0-9]/`).
**THR-351 complete — Encounter UI Phase A2 scaffold finalized.** Added callback-eligibility trace interface and explicit additive GameState scaffold fields (`regionalDetectionPressure`, `encounterSpotlight`) with safe defaults in `initializeGameState`, preserving legacy aliases for compatibility.
**THR-330 complete — Encounter UI Phase C1 scaffold landed.** Added `EncounterScreen` (440px hero rail + flex center + 540px right rail + 100px bottom strip), `EiraHeroPanel`, and `CapabilityStrip` with jsdom coverage for shell zones and protagonist sections.
**THR-338 complete — Encounter UI Phase E2: five typed detail page instances (Encounter Experience).** Engine: `detailPageGenerator` (cached, fail-soft), `detailPageResolvers` (graph-walking section resolvers per `(pageKind, typeId)` row + mandatory-floor fallback resolvers), schema registry in `src/data/detailPageTemplates.ts`, fallback prose pools in `src/data/detail-page-fallback-templates.ts`, empty `SHOWCASE_AUTHORING` stub in `src/data/detail-page-showcase.ts` for THR-318 Stream 2. UI: `useOpenDetailPage(gameState)` hook returning typed `openActor/Item/Faction/Place/Event/Ref` callbacks; `DetailPageOpenerContext` lets the Section dispatcher route chip / event-card / `data-term` clicks through a single provider. Tests: 13 generator unit tests + 12 integration snapshot tests at 1920×1080 covering all 5 kinds + 4-deep stack + chip-click → opener invocation. 10,836 tests pass; tsc clean; vite build green. PR pending.

**Encounter Experience — active:** THR-331 ✅ (C2 choice card + forecast band), THR-330 ✅ (C1 shell), THR-338 ✅ (E2 five typed detail pages). Recent: THR-323 (B1 choice resolution + drift), THR-324 (B2 outcome forecast), THR-325 (B3 hand filter), THR-337 (E1 DetailModal shell), THR-351 (A2 GameState scaffold). Plan docs landing for the remaining phase suite.

## Milestone Status
- **v1.0 Foundation:** Shipped 2026-03-30 — Phases 1-18 + M2.5 (81 plans, 1533 commits)
- **v1.1 Optimization:** Shipped — Phases 19-22 (determinism, wiring, performance, hygiene)
- **Encounter Format Migration (Now):** ✅ Phases 0-9 complete. All guilds migrated.
- **Content Architecture (Now):** ✅ THR-86/88/239 shipped.
- **UI Visual Overhaul — Design System v1 (Now):** ✅ Project complete.
- **Continuous Improvement (Now):** THR-303/305/306/307/309/311/312/313/314/315/316/354/355/356/357/358/359 ✅. THR-304 Phase 5 series complete (5a vault THR-356 ✅, 5b repo THR-359 ✅). THR-360 (Category B code residue) queued.
- **Social Systems Expansion (Now):** THR-28/27/30/51/29/41/34/31/35 shipped. ✅ THR-254/253. THR-78 queued.
- **Thematic Pressure & Living World (Next):** ✅ THR-19/122/125/126/80/128/127. THR-87 blocked by THR-116.

## Archived to project-history.md
- THR- and earlier — see project-history.md
- 2026-05-06 batch: THR-311, THR-312, THR-313, THR-314, THR-315, THR-316, THR-317, THR-320, THR-321, THR-322, THR-323, THR-336
- 2026-05-07 batch: THR-324, THR-325, THR-329, THR-339, THR-340, THR-341, THR-349, THR-350, THR-354, THR-355, THR-356, THR-357, THR-358, THR-359

## Active Backlog Ideas
- **TB-105–108 Thematic Pressure & Living World Pass** (omen agendas, cool failure, doom identity, intent/activity visibility)
- **TB-095–099 Social Systems Expansion** (v1.2 — designed, ready to sequence)
- TB-071 Economy Second Pass · TB-069 Location NPCs · TB-051 Monster Encounters residual · TB-037 Onboarding

Full backlog: [Linear (Threadbare team)](https://linear.app/threadbare) · Completed work: `Docs/project-history.md` + Linear "Done" state · Pre-Linear history: `.planning/BACKLOG_HISTORY.md`
- ✅ 2026-05-08: THR-353 shipped (D3) — narration/TTS discovery report + encounter narration adapter contract + contract smoke test.
- ✅ 2026-05-08: THR-292 closed (Encounter Format Migration) — vault encounter architecture pages confirmed unified-only (Systems/Encounter System.md canonical via THR-341; UL Encounters.md describes EncounterTemplate as a conceptual term, not stale TypeScript-class guidance). Closeout entry appended to vault log.md.
- ✅ 2026-05-08: THR-265 shipped — skill freshness metadata + drift-scan S5 (all skill files now carry `last_validated_against`; weekly scan now reports stale/archive/bootstrap-needed skill freshness drift).
- ✅ 2026-05-08: THR-363 shipped — Phase 2b Canon Step-0 wiring for prose/hexmap/template skills (14 SKILL.md files mirrored); Canon-first pre-read now enforced before domain authoring.
