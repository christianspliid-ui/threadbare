# Project Status

> Updated 2026-08-02. Detailed per-ticket narratives that used to live here are archived: shipped-work one-liners in `Docs/project-history.md`, full rationale rows in `Docs/changelog.md`, and the wiring detail in `Docs/plans/wiring-checklist.md`. This file stays ≤60 lines by contract.

## Current Focus

**The coordination layer stored its state on protected `main`, so the paperwork competed with the product (THR-947, 2026-08-02).** Christian, chat review: *"I don't see a need for having all the building documents as part of the source code. It is just scaffolding."* Every hourly status artifact — the briefing, the user-action list, each lane's run report — reached `main` by branch → PR → CI → merge, and under strict protection **each such merge knocks every in-flight PR to `BEHIND`**, costing an ~18-minute gate re-run apiece. Measured 10:24–14:24Z on 2026-08-01: 8 merges, only 3 product code. Exhaust now publishes to an unprotected **`ops` branch** — no PR, no CI, no auto-merge, and `main`'s tip does not move — via one shared entry point, `scripts/ops-publish.sh`, which commits through git plumbing against a throwaway index. **Plumbing rather than a checkout is what makes it safe for these lanes:** it touches no working tree (so the THR-672 home-tree rule holds), creates no worktree for the reaper (so THR-797 cannot fire), and leaves the caller's branch and HEAD alone. **The half the ticket did not name was the read side, and it fails silently in both directions.** Four surfaces resolved sibling reports by listing the local `Docs/ops/` — which after the cutover returns only the frozen pre-cutover archive: `keep-work-flowing-cc` step 2.6 would fold months-old `## Needs Christian` items into the brief while passing every freshness check on filename alone, and the orchestrator's run-letter picker would report "no runs today" and let every run of the day overwrite the same file on a branch where a push is last-writer-wins. All four now read `origin/ops`. Likewise `check:substantive`'s baseline was hardcoded to `origin/main`; left there it would compare each hour's brief against the **pointer stub**, never match, and return `commit` every run — silently un-doing THR-920. It follows the file now (`DEFAULT_BASELINE_REF`), **verified in both directions**: real brief vs `origin/ops` → `skip`/`baselineFound: true`, missing ref → fail-soft `commit`. **History stays on `main`, frozen** — a dated archive cannot be mistaken for a current copy; only the two always-current `Design/` files get pointer stubs. Membership is a predicate, not a list (`Docs/ops/README.md`), so a new lane's report is a member the day it is written. THR-945/THR-946 remain the sequenced fallbacks pending the observation window; the substantive-change gate's now-dead premise is filed as THR-954 rather than settled in-run.

**Nobody owned test-suite health, so the first sweep found 12 test files guarding code nothing imports (THR-942, 2026-08-02).** `testing-patterns` governs authoring and no lane ever pruned or profiled, so a 931-file suite only ever grew — the V1 SVG hex map ran 8 test files on every CI cycle for months before THR-941 caught it, and that was found by hand. A **weekly** test-suite health pass now lives in the orchestrator's T3 tier (`ORCH_TESTHEALTH_DOW`, Monday): dead-coverage candidates, slowest files, duplicated coverage. Weekly rather than daily because a suite this size does not decay overnight and re-listing the same candidates each morning is the "dump" T3 already forbids; output goes to its own `Docs/ops/test-suite-health-*.md` so it shares no anchor with run reports (THR-849) and next week's pass can diff. **The guardrails are the point, not the finding count:** the duty never deletes — each candidate becomes its own ticket with import-graph evidence and an executor re-verifies — and a prune ticket must prove the *tested code* is dead, never that the test is slow. **The two lists are deliberately disjoint and were kept so:** the 12 candidates and the 15 slowest files share zero entries, and the top 7 (all >10s, all engine integration/contract tests driving real ticks) are cost inherent to what they cover. **The detector was validated by positive control**, not assumed — re-run against the tree before THR-941 it flags the V1 cluster, and against today's tree it does not. Findings filed as THR-950 (5 zero-importer engine modules), THR-951 (the SceneStatePanel cluster, filed as retired-*or*-unwired with "file a wiring ticket instead" an explicitly valid outcome), THR-952 (6-module tail, sequencing the `AgentDetailPanel` parent question first). **Three traps are now written into the duty because each yields a confidently wrong answer:** a test-only helper (`src/testing/contentInvariants.ts`) has zero *production* importers by design; a type-only import marks dead runtime code reachable (`activitySummary.ts` → the never-rendered `AgentDetailPanel.tsx`); and a test named after a dead module may not test it — of THR-941's 8 deletions, `AgentDots.test.tsx` imported only the *live* `agent-visual-content`. That last one prompted a re-verification of THR-941: the module retains 5 covering tests, so no coverage was lost — a near-miss recorded rather than a defect. **Duplicated coverage is reported as not-assessed rather than clean**, because the naive metric just returns the known hub modules (`graph.ts`, 387 importers) and does not answer the question. Sensitivity stated as a residual: the strict all-subjects-dead rule under-reports, flagging 1 of the V1 cluster's 8, and the widened variant is too noisy to file from.

**The test suite spent more time importing modules than running tests, and the fix had to be split three ways rather than two (THR-940, 2026-08-01).** Vitest ran every one of 936 files in its own isolated worker, so the heavy engine/data module graph was re-imported from scratch each time: a full run reported `import 573.68s` against `tests 341.45s`. The suite now runs as **three projects under one `vitest run`** — `npm test` is unchanged, which matters because the CI job name `Test · Typecheck · Build` is a **required** status check that must not move. **The ticket specified two pools and two is not enough.** Beyond jsdom tests, a second population cannot share a worker: files using `vi.mock`. A shared module registry defeats module mocking — whichever file imports the real module first wins — and that failure is *worse than a crash*, because in `grantedTraitConsumers.test.ts` it made one assertion **pass for the wrong reason** (the unmocked lookup returned undefined, so the template carried no `requiredTraits` and sailed through the very gate it was meant to be blocked by) while two others failed. 19 of 827 node files mock modules, so isolating them costs almost nothing and is detected mechanically rather than by convention. **Both predicates are scanned, not path-based, because the conventions do not line up with the tree:** 68 node-environment tests live under `src/components/` and 6 jsdom tests live outside it, so a path split would misroute 74 files — silently, and in the slow direction. **Measured as a controlled experiment rather than a before/after wall-clock**, since THR-919 established a 2.2× runner spread that makes the ticket's "≤60% of baseline" gate winnable by luck: same file set, same worker count, same pool, isolation the only variable. Four engine files that fail under shared workers and pass alone are **pinned with their reason and a Deferral (THR-949)** rather than quietly opted out — three carry module-scope state across file boundaries (the exact hazard CLAUDE.md names under *Load-Bearing Architectural Decisions*), the fourth asserts a 500ms wall-clock budget that shared-worker allocation pressure pushes over. **Two of the four were invisible locally and surfaced only on CI**, which is the finding worth keeping: under `isolate: false` *which* files share a worker depends on worker count **and on the order files finish**, so a 32-core dev box groups them differently than a 2-core runner and a green local run does not prove the pin list complete. That limitation is recorded in the partition module itself, and the list may still be incomplete — stated as a residual rather than papered over, since the durable fix is cache ownership, not a longer list. The partition is guarded by 8 tests with population floors, so an empty scan fails loudly instead of routing everything into shared workers and going vacuously green.

**The hex map documented as deleted three phases ago was still in the tree, and still being tested (THR-941, 2026-08-01).** CLAUDE.md's Rejected Approaches list has asserted since Phase 8 that the V1 SVG hex map "was deleted", and `.planning/phases/08-integration/08-VERIFICATION.md` recorded it **✓ VERIFIED — not tracked by git at HEAD (deleted in commit `393e801`)**. `src/components/HexMap/` was nonetheless present with 19 files — 11 components/hooks and 8 test files, 2,678 lines — so the documentation and the tree had disagreed for months with the *documentation* being the thing everyone read. **The predicate was re-run at pickup rather than inherited from the ticket, and it did not match:** the description said 9 component files, the tree held 11. **Deadness was established four ways, not one**, because a single static-import grep is exactly how a lazily-imported module survives a dead-code sweep: no `components/HexMap[^V]` reference anywhere in `src/` or `index.html`, no relative import into the tree from outside it, no dynamic `import()` reaching it, and no reference from `vite.config.ts`, `index.html`, `public/wiki-manifest.json` or `scripts/` — with the sole name-collision hit (`shouldRenderIndividualOnHexMap`) confirmed to be an unrelated function in a Game test. **The 8 deleted tests covered only the dead components**, and the shared modules they also touched keep independent coverage — `engine/coastline` has 2 dedicated test files, `hexMath` has 3, HexMapV2 has 41 — so no live behaviour lost its only test, which is the failure mode a delete-the-tests change actually risks. **The typecheck ratchet corroborated the blast radius numerically rather than being merely satisfied:** the total fell by exactly 11, matching the sum of the 7 baseline entries for the deleted files, so the deletion removed precisely those errors and nothing else; the baseline is refreshed to hold the new floor per the ratchet's own instruction. Ticket's mutex with THR-940 honoured in the sequence it specified — this ticket first, shrinking the test tree THR-940 must stabilise under `isolate: false`.

**The post-merge full-suite re-run is gone, and the reason it had to go is not the reason on the ticket (THR-939, 2026-08-01).** Branch protection runs in **strict mode** — a branch must be up to date with `main` before it merges — so the tree that lands on `main` is byte-identical to the tree `Test · Typecheck · Build` already validated on the `pull_request` event minutes earlier. The push-to-`main` run re-derived a known answer, 38 times over two weeks. **The ticket's justification was void before pickup and is rewritten rather than inherited:** it costed those runs at ~22% of the CI bill, but THR-919's audit established the repository is **public** and every job runs on `ubuntu-latest`, a standard runner GitHub bills at **$0** — there is no CI spend to cut. What the redundant run actually costs is a ~10-minute occupancy of the Actions queue immediately after every merge, delaying the *next* PR's feedback; under strict protection the merge ceiling is already one PR per advance of `main`'s tip (THR-920), so anything lengthening the post-merge window slows the lane directly. That is a **throughput** argument, and it is the one now written into the file so a future reader does not inherit the wrong one. **The clause is ANDed outside the parenthesised group deliberately**, so it also suppresses the THR-768 vacuous-gate guard on push: that guard exists to refuse a *merge* it cannot vouch for, and on push the merge has already happened with no gate listening, so firing there would be an unactionable red on `main` rather than a protection — on `pull_request`, where it is load-bearing, it is untouched. **`workflow_dispatch` is unaffected and becomes more load-bearing**, now the only way to run the full suite against a `main` commit, which is exactly the THR-842 retro-verification path. **Verified by evaluating the committed condition, not a hand-copy** — a truth table derived from `ci.yml` itself across all eight event/detect combinations: push-to-main skips (code *and* docs merges), code PR runs, docs-only PR skips while `Docs gates` runs, `detect`-died-on-PR still runs and fails loudly, dispatch runs, and a cancelled run stays cancelled. Mutex on THR-909 cleared before landing (Done, PR #1210 merged, its `docs-check` job present). **The merge gate is untouched and provably still real:** branch protection consumes the `pull_request` run's status, and PR #1210 — the same file shape as this change — recorded `Test · Typecheck · Build: SUCCESS`, confirming `.github/workflows/**` classifies as code rather than slipping through as a vacuous skip. Residual named, not silently left: `Docs gates` still runs on a docs-merge push, ~2 min of the same redundancy.

**Paperwork stopped consuming code slots, and the fix had to defuse the WIP gate to be safe (THR-938, 2026-08-01).** Roughly a dozen Ready-for-Dev tickets at any time are docs/process-only, and under one-issue-per-run each burned a whole hourly slot, so source-code tickets queued behind CLAUDE.md corrections and UL proposals. The lane now **drains** up to 3 `docs-only` tickets after its primary ticket, sequentially, with the normal claim → verify → upstream-shipped → coordination-block discipline per ticket — the drain removes the ceiling, not the gates. **A separate higher-cadence docs lane (Option A) was Christian's alternative and stays deferred:** under strict protection every merge advances `main`'s tip and re-stales in-flight code PRs at ~18 min a re-run (THR-920), so a second lane buys throughput with tip churn plus ~24–48 billed runs/day. **The merge-yield gate is what makes Option B safe where Option A was not** — the drain classifies every open armed PR's diff and skips entirely if any contains code, so docs merges can only ever re-stale each other, and a docs PR's required check is `SKIPPED` by design. **The half the spec did not name was the WIP=1 gate**, and shipping without it would have red-exited the *following* hourly run: an issue whose PR is armed sits `In Dev` until auto-merge fires after the session ends, so a 3-ticket drain leaves three such claims behind and Step 1.5 reads a cross-session leak that does not exist. Step 1.5 now subtracts claims already discharged by an open PR carrying their `Fixes` line — one `gh` call, fail-soft to the raw count, deliberately narrow; **THR-927's broader question of what WIP=1 should count is untouched.** The spec's own classification grep was also corrected against canon: it omitted the two THR-922 doc-sourced artifacts, which would have mis-classified every UL-proposal ticket as code — exactly the tickets the label exists to fast-track. Queue retro-tagged to 15 `docs-only`; THR-839 and THR-756 rejected as code-track on `scripts/` surfaces.

**A badge that counted news and destroyed it unread on click (THR-935, 2026-08-01).** Christian, slice testing: clicking the "3" on Kael's Threads row gave "nothing — the icon just disappears… that icon is useless and confusing." `handleOpenNoticeBadge` selected the thread node and then called `handleClearEntityNotices` **unconditionally**, and the surface that selection opens rendered the notices nowhere — so the only observable effect of a click was N pieces of news deleted. The data was never missing: `EntityNoticeBadgeModel` has carried the full `notices[]` array since THR-666; nothing read it past `primary`. Fix is a snapshot taken *before* the clear (`buildRevealedNotices`, pure, newest-first) rendered as a `DetailSection` at the top of `ThreadDetailView` — the surface the click already opened now keeps the badge's "word of them" promise, on faction rows too (THR-667). **The guard is structural, not a second copy of the predicate:** the detail panel's node-resolution expression was hoisted out of GameView's JSX into `resolveThreadDetailNode`, and the badge's "will this open?" check calls that same function — so a badge clearing against a surface that then declines to render is now impossible by construction rather than by two expressions agreeing. Verified in-browser at 1920×1080: count 3 → 3 lines readable, badge cleared; an unresolvable anchor keeps its badge and its news. 0 console errors from the change (the 6 present are pre-existing `amb_evt_17` duplicate keys, THR-853).

**The content corpus's most-used name token was not in the enricher's vocabulary (THR-933, 2026-08-01).** Shrine Offering's attended step prose rendered "…`{actor}` turns out what they carry…" — the placeholder shown raw to the player while `{they}` beside it resolved correctly, in the same sentence. Two prose paths, one vocabulary gap: the background/event path in `orchestrator.ts` has always substituted `{actor}` directly, and the content corpus took that as the convention (~1.3k uses in `encounter-content.ts` alone, plus ~13 more data files), but `enrichProse` — the *attended stage's* path — knew `{name}`, `{location}`, `{they}`/`{them}`/`{their}`/`{s}` and never learned `{actor}`. It stayed invisible because background-tier converted templates rarely rendered attended until the `?spawn` review route (THR-883) existed. Fix is the alias plus its sentence-initial `{Actor}` form (2 shipped uses), matching the orchestrator's semantics exactly — `{actor}` is an alias of `{name}`, not a second concept. **The regression guard went to the adapter that actually leaked:** the unified adapter has carried a no-raw-braces lock since THR-694; `buildSimpleEncounterStageModel` — the path the player saw — had none, so the lock now covers it too. **Falsified rather than asserted:** removing the two alias lines turns the adapter lock red naming the exact leaked string, plus both new enricher unit tests. Verified live at 1920×1080 on the ticket's own `?spawn=encounter.shrine_offering` URL, 0 console errors. **Residual, and not this ticket's:** the same screen still leaks `{They}`/`{they}` on the *nudge-card* step panel and action cards — a surface that never calls `enrichProse` at all, which is THR-923's scope, now with live confirming evidence.

**The armed-PR stall finally got its verdict, and the deciding fact was that `main` is protected twice (THR-735, 2026-08-01).** Under strict branch protection an armed green PR knocked `BEHIND` never merges on its own, and pull-work's Step 0.8 sweep drains one per run — a race the lane lost across six occurrences from 07-23 to 08-01, silently, while every PR read green, mergeable and armed. The ticket listed four candidate remedies and asked for a decision, not a seventh `update-branch`. **Remedy 3 (raise the sweep cap) is retired as arithmetically incapable, not merely costly:** the ceiling is one merge per advance of `main`'s tip, not N per hour, so at N updates per run N−1 are invalidated by construction — the defect is serialization, not throughput. **Remedy 4 (batch the lane's docs traffic) is retired as a remedy while proceeding as work in its own right** under THR-947, because measured 2026-07-28 `main` took only ~2 merges/hour and the sweep still lost: halving a collision probability leaves a silent failure mode intact. That left merge queue vs dropping strict mode — and the new finding is what prices the second one. **`main` carries two overlapping protection surfaces and they disagree:** the legacy classic rule sets `strict: true`, while the active ruleset (edited the same morning) sets `strict_required_status_checks_policy: false`, and GitHub applies the most restrictive version of each rule. The livelock this ticket describes is produced *entirely* by the legacy rule; the surface actually being maintained already declines to ask for strict — so drop-strict is a one-field retirement of a superseded duplicate, not the governance trade the ticket assumed. **Decision: merge queue (THR-946), with drop-strict as the named fallback.** The deciding column is not cost but the guarantee: H6 kept branch protection because the required check caught a phantom 3,379-line reversal before it reached `main`, and drop-strict spends exactly that guarantee while a merge queue *strengthens* it — GitHub tests the merged tree rather than the PR head. **Done-when #2 does not transfer to this ticket's evidence and says so**: an unattended merge demonstrated across a busy hour is a proof of the mechanism *in production*, identical to THR-946's own Done-when #2, so THR-735 owns the call and THR-946 owns the proof.

**A generated file was also a tracked file, so every merge re-conflicted every other open PR (THR-916, 2026-08-01).** `Design/impediment-dashboard.html` is ~6,400 lines of which ~6,200 render from `Docs/impediments.md`, and it was committed. The log survives concurrent appends because it is `merge=union` (THR-691); HTML has no such driver, so the moment any impediment-appending PR merged, **every other open PR conflicted on this one file** — four hand-resolutions across three PRs in one ~40-minute run, each mechanical, each also consuming the one-update-per-run budget THR-735's drain depends on. **The obvious fix had a blocker the ticket had not found:** the generator spliced blocks into the existing file *in place* and threw when it was absent, so "stop committing it" alone would have broken `prebuild` on every fresh clone and in CI. The artifact is **split** instead — a committed 235-line **template** (hand-authored chrome) and an untracked **render** (chrome + data) — so the part that churns left the tree and the part in the tree stopped churning. **The consumer audit cleared the approach on evidence:** the render is absent from `wiki-manifest.json`, sits outside `public/` so Vite never serves it, is imported by no `src/` file, and falls outside `vercel.json`'s `ignoreCommand`; `Design/*` was already gitignored and it was tracked only because impediment #139 force-added it. **Losslessness proven, not assumed** — deleting the render and regenerating from the template alone reproduces the previously committed 6,423 lines **byte-identical**. **Untracking did not buy coverage from the gate**, which the Done-when named: `check:generated-freshness` gains an `UNCOMMITTED_GENERATED_PATHS` branch asking the two questions that still have teeth — does the generator still produce it, is it still untracked — the second catching the `git add -f` that started this. `replaceGeneratedBlock` now throws on missing markers rather than returning its input: silence merely went stale in the in-place design and is fatal when rendering from a template, where it ships a dashboard reporting **zero impediments**. Falsified: force-adding the render fails exactly one of six new pins. **Only #358's dashboard half is claimed** — the duplicate-number half stays with `check:impediment-ids` (THR-881), residue on docs-only PRs tracked as THR-931.

**Eight approved opening paragraphs shipped and none of them rendered — and the field had two readers missing, not one (THR-932, 2026-08-01).** Every vertical-slice encounter carries its scene-setting first paragraph verbatim in `openings.<settingClass>` (THR-884 envelopes), and the attended stage showed only the close-up step prose: The Unsafe Bridge opened at "The nails have risen…", dropping "The bridge sags… 'It holds,' she says…" entirely. **The prose was never cut; it shipped and could not reach the screen.** Two independent gaps, either of which alone is fatal. **(A)** The compile-openings-into-`{frag:opening}` step existed *only* in the raw-entry converter, so on a **direct-authored** `UnifiedActionTemplate` `openings` was validated by `validateSettingEnvelope` and then read by nothing — a field with no reader. **(B)** Neither stage adapter threaded `contextFragments` into the narrative context, so `enrichProse` stripped every `{frag:*}` token to an empty string at the attended surface while the *resolution* path rendered the same template correctly. **(B) is the half the ticket did not name**, and finding it mattered: compiling the envelope without it would have produced a green test suite and an unchanged screen. Fix is one shared `compileOpeningEnvelope` applied at both authoring paths, so there is a single semantic instead of two that drift — and it **prepends** rather than replacing, because the converter's old replace semantics discarded the authored step paragraph while the approved slice text is exactly `openings.wayside + '\n\n' + BRIDGE_STEP.narrativeTemplate`. **Unifying the converter is behavior-neutral, verified rather than assumed**: no raw `EncounterEntry` in the corpus authors `openings` (the only other author is a test fixture), so the replace path had zero shipped users. **The unbound-setting fallback is load-bearing for the review route itself** — `?spawn` places at the agent's current location (a Sacred Grove in practice), which matches no declared class, so falling through to blank would have reproduced the bug on the exact URL used to verify the fix; the `'*'` default is the first declared class in canonical order. **The regression guard was falsified, not asserted:** reverting the one-line `.map()` turns 2 tests red naming `encounter.slice.unsafe_bridge`, and the corpus sweep fails on an empty population so it cannot go vacuous the way the original THR-884 sweep did. Verified live at 1920×1080 on the ticket's own `?spawn=` URL for both `unsafe_bridge` (`wayside`) and `grateful_kin` (`rural`), 0 console errors.

**A test suite that failed on documentation, and generators that dragged docs onto the code track (THR-922, 2026-08-01).** Two separate couplings made "reporting an impediment" behave like a code change. **(A)** `scripts/__tests__/impediment-log.test.ts` read `Docs/impediments.md` **live** and asserted against it, so an append could redden `npm test` — and the damage landed on the wrong PR: CI *skips* tests on the docs-only PR carrying the append (THR-491), so a bad edit shipped green and failed the **next unrelated code PR**, a red suite charged to whoever touched code next. Parser *behaviour* now pins against a small hand-authored fixture; every *live-corpus* invariant — population floors, no-dropped-lines, duplicate ids — moved into `check:impediment-ids`, which is blocking and, since THR-909, runs in the `Docs gates` job on documentation PRs. The fixture is deliberately **not** a snapshot of the live log: a copy rots and re-imports the coupling in a new costume. **(B)** `src/data/ul-dashboard.generated.json` and `public/system-interface-map-reference.html` are generated *from* docs but land *outside* them, so a UL-shard or canon-page edit regenerated a non-doc file and paid the full ~15-minute gate (measured on commit `cc7a61ca`, a pure documentation deliverable). Both are now excluded **by exact path** from `ci.yml`'s `code` filter and added to its `docs` filter, with CLAUDE.md's classification grep updated to match. **Excluding the artifact cannot weaken detection** — a code source is its own path and stays in the filter — which is what makes this safe rather than a hole. **The ticket's membership predicate did not survive contact with the manifest:** it said "inputs *all* live inside the doc-excluded paths", but the interface-map page declares two `scripts/*.ts` sources alongside its canon page, so the predicate was widened to "*at least one* doc source, non-doc output" (`pure` vs `mixed`) — the harm is identical and so is the safety argument. **Every guard was falsified, not asserted:** a deliberate duplicate id fails `check:impediment-ids` (exit **1**) while `npm test` passes (exit **0**) — Done-when 2 exactly; stripping the paragraph entries trips the population floor; emptying the allowlist makes `check:generated-freshness` name both couplings; deleting one `ci.yml` line turns the drift test red. Mutex on THR-909 cleared before landing (its `docs-check` job exists). CLAUDE.md's stale caution that those gates "do not run in CI at all" was corrected in the same pass — Part B's safety argument depends on it. Residual: `Docs gates` is not yet a **required** check (THR-931).

**Every design artifact entered the repo from its first draft (THR-918, 2026-08-01).** A concept rewritten five times in a day paid branch → commit → push → PR → plan-doc lint → merge → Linear transition each time, and may never have reached an executor at all. The cost was never compute — docs-only PRs merge in 30–60s and run no tests (THR-917) — it was **session ceremony and the implicit signal that a half-formed thought is a versioned artifact**. Plan docs now have two lifecycle stages: **exploratory** (Obsidian vault, no git/PR/CI/lint, rewrite freely) and **committed** (`Docs/plans/`, full governance, unchanged), with promotion triggered by exactly one event — the issue moving toward Ready for Dev. **The vault folder already existed and was already load-bearing**, so the answer to "where?" was to stop inventing: `Brainstorms/` holds 10 hand-curated drafts, has a documented frontmatter convention, and is **cited as a source by canon pages today** (`Docs/canon/cosmology.md` names `brainstorm-cosmological-symmetry.md` as its canonical iteration record) — which is also the evidence that a non-git path in a citation breaks nothing. The discoverability question was answered by reading the consumers rather than assuming: `lint:plan-doc` only lints *staged* files under `Docs/plans/`, `rebuild-plans-index` globs the same directory, and `check:process` invokes `lint:plan-doc --staged` — so a vault draft is invisible to all three **by construction**, and none of them requires a `Plan doc:` line to resolve. Exploratory issues therefore carry a **`Draft:`** line and `Plan doc:` stays reserved for a committed doc, which keeps THR-921's proposed promotion-time liveness gate valid rather than pre-broken. **Governance does not apply to exploratory drafts, confirmed against the authoritative file rather than assumed** — intent-judge scores the plan doc, design-audit writes into the plan-doc tail, the three-pillar rule gates the handoff; none can run against a vault draft, so governance attaches *at promotion*, in full. **The Brainstorm companion is not weakened — it is the promotion product**: the durable half of the exploratory thinking becomes the committed `…-brainstorm.md`, which is why `design-governance.md`'s companion requirement needed no edit. Loss risk stated rather than buried: the vault is not git-backed, an unpromoted draft has no history, and that is the accepted price of zero ceremony. Residual tension filed, not silently left (THR-944): grill-me synthesis still lands in `Docs/plans/`.

**Spawned encounters had no second chapter, and the reason was not the one on the ticket (THR-934, 2026-08-01).** `?spawn` passed `courtPosition: 'the_first'` as a *notification-time override* and wrote nothing to the graph, so only the spawn-time notification carried it; every later tick re-derived visibility from the graph and lost it. **The ticket's stated cause was wrong in a way that mattered** — it read the target as unthreaded, `getVisibilityDepth(null) → 'none'`, so steps 2+ generated nothing. In fact `collectThreadedAgents` already synthesizes a `the_first` entry for the ascendant's avatar (which is what `@hero` resolves to), so the notification *was* generated every time. What killed it is that the synthetic entry stamps `attentionMode: 'auto_resolve'`, which hands `buildEncounterNotification` a non-null `autoResolveTick`, and `shouldAutoOpenEncounterNotification` reads a non-null tick as "do not open now" — **the beat existed and simply never surfaced**, which is precisely why the symptom read as "resolves silently" rather than "never happens". The same synthetic entry carries `threadEdgeId: ''`, and the Threads panel lists *real* edges, so there was no row to anchor the THR-664 encounter badge to — one cause, both reported symptoms. Fix threads the target for real (`ensureDebugSpawnThread`, an upsert that also **retunes** a pre-existing `auto_resolve` thread, since `?seeded`'s Kael arrives on exactly that setting and the spawn would otherwise inherit the thing suppressing it), so visibility, interrupts, the Threads row and the badge all follow from one truth with no further special-casing. **The regression pins the mechanism, not the symptom:** a test asserting "a step-2 notification exists" would have passed *before* the fix, so the assertion is `autoResolveTick === null`, paired with a falsification twin that deletes the written edge and asserts the notification is present but non-null. Red baseline confirmed — 4 of the new tests fail with the production change reverted. Verified live at 1920×1080 on the ticket's own `?spawn=` URL: commit step 1, one `__DEBUG.tick(1)`, and the step-2 stage pops unprompted with Vara carrying a Threads row and the badge `"Snow on the Pass, step 2 of 2 · wavered — open encounter"`. **One Done-when clause is not reachable in this build and is filed rather than claimed** (THR-943): "clicking the badge reopens the stage" has no state to test at `the_first`/`pause`, because dismissing the stage resolves the notification and retires the badge with it. Unblocks THR-906/THR-907.

**The vertical slice is live and the feedback loop is running (THR-902 map, 2026-07-31).** The 5-encounter roster is signed off (Shrine Offering + four THR-883 slice encounters), PR #1132 merged and deployed, and Christian is testing on Vercel via one-click `?spawn=` URLs. First live findings shipped same-session: THR-925 (safe-center fix — overflowing encounter prose was unreachable by scroll below 1920×1080) and THR-926 (what/why tooltips on the nudge test panel: motive, reach, objective, difficulty, factors, forecast, essence). Held for the verdict session: the old-veil/new-shell UI mix. Remaining on the map: readiness gap check (THR-906), then Christian's five-verdict session (THR-907).
**Recently completed (2026-07-22, marathon):**
- **M3 Dynamic Economy nearly complete** — THR-669 (route events + the trade-route pipeline revived: three stacked engine bugs meant no route had ever survived), THR-670 (cargo route lines + tooltips on HexMapV2), THR-617 (P3 power couplings: systemic monopolies with live chronicle prose, sphere drift from cargo flows, faction gold term, scarcity arcs with intervention encounters). THR-618 partial: all five divine economic verbs now `[IMPL]` (reveal vein / guide caravan / sour mine shipped, bless/blight were live) and the Flow Web extraction checkpoint is decided (**defer** — `Docs/plans/2026-07-22-flow-web-extraction-checkpoint.md`); only the THR-611-coordinated essence bridge remains.
- **War project complete** — THR-628 (battle depth: prepared defense, tactical events, breach surfacing, battle IPK), THR-629 (siege gravity-well repair: dead capability gates fixed, relief_march + sabotage live), THR-630 (Phase D: notable-agenda roster + five families incl. the Campaign war hand-off that opens war to the nations, NotablesPanel, power-vacuum verification).
- **Scene Integration complete incl. follow-ups** — chain A–F shipped earlier; THR-712 (guild-tier sweep: cast figures, one genuine bond, 44 continuity seeds, permanent guild prose-QA gate) and THR-713 (381 invented-name literals across seven branching encounters retired to `{cast:*}`).
- **Cowork migration complete** — THR-677 ports live (daily grooming, workflow retro, project hygiene), THR-684 attachment-pipeline prompts written, THR-704 scheduler hygiene, monthly-rulebook-review registered.
- **Social Systems: secrets & favors activated** — THR-724 turned a fully-built, entirely silent subsystem on. Generation had a *dead read site* (not merely thin content), consequences had zero callers, the consequence module was a two-way fork, and both player verbs were unreachable. All four fixed; 0 → 18 secrets and 0 → 4 revelations per 120-tick run, and the three pre-seeded interface-registry rows are LIVE with run evidence.

## Milestone Status

Tracked in [Linear Projects](https://linear.app/threadbare/projects). Active: Thematic Pressure & Living World (war system now fully live), Content Architecture (scene integration complete). See Linear for per-project state — this file does not duplicate it.

## Where things live

- Shipped work: `Docs/project-history.md` (one-liners) + Linear Done
- Rationale: `Docs/changelog.md`
- Wiring: `Docs/plans/wiring-checklist.md` + `Docs/plans/2026-04-16-systemic-wiring-guide.md`
- Rules of play: `Docs/canon/rulebook.md` (+ quick-reference)
- Christian-facing: `Design/briefing.md` / `Design/user-actions.md` (hourly refresh)

## Active Backlog Ideas

None parked here — file ideas as Linear issues (predicates, not counts; see `Docs/canon/process.md`).
