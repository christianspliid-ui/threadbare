# Project Status
> Updated 2026-06-27 (THR-511).

## Current Focus
**Ascendant Beats — Divine Cadence (new):** THR-500 Director + THR-509 primitives + THR-503 reach gate + THR-502 home seat + THR-507 debug + THR-501 turn-1 floor + THR-508 imbue card + THR-505 pool library/unlock catalog + THR-506 delivery-beat adapter + THR-517 beat resolve path + THR-511 consecrate card shipped. **Beats are now playable end-to-end** (offer→enter→resolve closes via THR-517). Expression toolkit split at implementation planning: `imbue` (THR-508) + `consecrate` (THR-511) shipped; `bestow`/`anoint` → THR-512/513 (each needs a new consumer subsystem); consecrate relic-mint variant → THR-518. Remaining: spine authoring (THR-504); pool prose templates + encounter-screen handoff for template-backed beats (THR-514); card→drawer/bucket wiring for the new cards (THR-515).
**Action System & Unlocks:** THR-501 turn-1 floor (Move + Investiture only across all surfaces) shipped — unblocks THR-504 spine beats.
**Encounter Experience — active:** Phases A1–A3, B1–B7, C1–C4, D1–D3, E1–E2, F1–F2, G1–G3 ✅. Remaining is F-phase integration (mount EffectRegistration components into hero panel / cast tile / scene state surfaces) and Phase H post-v1 polish.

## Milestone Status
- **v1.0 Foundation:** Shipped 2026-03-30 — Phases 1-18 + M2.5 (81 plans, 1533 commits)
- **v1.1 Optimization:** Shipped — Phases 19-22 (determinism, wiring, performance, hygiene)
- **Encounter Format Migration (Now):** ✅ Phases 0-9 complete. All guilds migrated.
- **Content Architecture (Now):** ✅ THR-86/88/239 shipped.
- **UI Visual Overhaul — Design System v1 (Now):** ✅ Project complete. THR-178 Deferral shipped 2026-05-08.
- **Continuous Improvement (Now):** THR-303/305/306/307/309/311/312/313/314/315/316/354/355/356/357/358/359/266/396/404 ✅. THR-304 Phase 5 series complete (5a vault THR-356 ✅, 5b repo THR-359 ✅). THR-360 (Category B code residue) queued.
- **Social Systems Expansion (Now):** THR-28/27/30/51/29/41/34/31/35 shipped. ✅ THR-254/253. THR-78 queued.
- **Thematic Pressure & Living World (Next):** ✅ THR-19/122/125/126/80/128/127. THR-87 blocked by THR-116.

- ✅ 2026-06-27: THR-511 shipped (Divine Cadence) — **consecrate** sustained-control spawn bridge + card. **Engine:** extended `spawnControlEffect` (`controlEffectSpawn.ts`) to accept a **location-node target** (was hex-only) — resolves the location's `hexCol`/`hexRow`, sets `targetNodeId = locationId`, and carries the THR-509 spec fields `perTickThreadAuras` + `upkeepArtifactId` onto the spawned `ControlEffect` (consumer side `phaseControlEffects`/`getUpkeepStatus`/`applyCoLocatedThreadAura` already shipped). New optional `graph` param; 3 call sites in `unifiedActionResolution.ts` pass `state.graph`; non-hex target with no graph still returns null (backward-compat). **Content:** authored `action.consecrate` — `durationMode:'sustained'` + `controlSpec` (`perTickCost {spirit: CONSECRATE_PERTICK}`, `perTickThreadAuras [{magnitude: CONSECRATE_DEVOTION_PER_TICK}]` faith-spread), `targetCategories:['location']`, `targetSubtypes:['temple','shrine']`; new constants `CONSECRATE_ESTABLISH_COST`/`CONSECRATE_PERTICK`. 5 new tests (location-target resolve, fail-soft no-graph/no-coords/wrong-type, THR-509 field passthrough, end-to-end aura advances a co-located thread's `ticksAtCurrentTier`). Full suite **11740 green**, vite build green, 30-tick CLI smoke (seed 42 medium, 398 agents, 42 events) clean. Browser-verify exempt: engine/content-only, no UI surface (card→drawer wiring is THR-515). Relic-mint variant (dynamic `upkeepArtifactId` + permanent artifact) deferred to THR-518. PR pending.
- ✅ 2026-06-27: THR-517 shipped (Divine Cadence #8) — **Beat resolve path: offer→enter→resolve wiring.** The Director (THR-500) only *offered* beats (set `ascendantBeats.pending`); nothing cleared them in the running sim. New `resolvePendingBeat(state, {chosenActionId?, outcome?}, templateResolver?)` (`ascendantBeat.ts`): looks the pending beat up across spine∪pool∪delivery (new exported `getBeatDefinitionById`), applies its grants into `unlockedActionIds` (dedup, `action.unlock.granted` via beat per id), then records the `BeatRecord` + clears `pending` via the existing pure `resolveAscendantBeat`. Selection beats grant exactly one chosen option; non-selection grant all. Fail-soft: unknown beat / unresolvable `templateId` clears gracefully (new `ascendant.beat.skipped` reason `missing_template`), never wedges; whole body try/caught. **UI** (new `AscendantBeatModal.tsx`): pending pool beats surface as a top-center thread-tug offer banner (`AscendantBeatOfferBanner`) the player enters; spine beats auto-open modally (gated by `interruptsSuppressed`). The entered-beat modal presents kind-derived prose + either a Receive CTA (non-selection) or a generic **choose-1-of-N picker** (selection) over the beat's grants; resolution flows through `resolvePendingBeat` → grants land in the drawer (already reads `unlockedActionIds`). **Debug**: `__DEBUG.resolveBeat(chosenActionId?)` (+ `BeatBridge`/`debug-bridge.d.ts` `DebugResolveBeatResult`) and a BeatsDebugTab "Enter & Resolve" control. 7 new engine tests; full suite **11734 green**, vite build green, 30-tick CLI smoke (seed 42 medium, 398 agents, 42 events) clean, zero net-new `tsc -b` errors (3483 baseline, THR-489). Browser-verified at 1920×1080 (worktree server, `?view=game&seeded&size=medium`): spine beat auto-opened ('Opening' → Receive → +bind_thread_agent,observe_agent, pending cleared, history recorded), selection beat banner→picker→Imbue granted **only** `action.imbue`; 0 console errors. Plumbing only — prose-rich encounter-screen handoff for template-backed beats is content (TODO(THR-514)). Unblocks THR-504 (spine) + THR-514 (pool prose). PR pending.
- ✅ 2026-06-27: THR-506 shipped (Divine Cadence #7) — Delivery-beat adapter for THR-452 branching content. New `src/engine/deliveryBeatAdapter.ts` maps every registered branching encounter (`LOCATION_BRANCHING_ENCOUNTER_TEMPLATES`, 23 of THR-452's ~30 unreachable-in-normal-sim templates) into a `delivery` `BeatDefinition` (`beat.delivery.<sourceId>`, `templateId`=source, cadence trigger, normalised `DELIVERY_BEAT_WEIGHT`=0.1 so the 23-entry group stays ~10% of pool draws). The Director (`ascendantBeat.ts`) now merges `eligibleDeliveryBeats(history)` (dedup against already-delivered beats — the live "currently-valid" eligibility gate) into the cadence draw pool, so once the offer→enter→resolve path (THR-514) lands, divine visions surface unreachable content **without re-applying the encounter's mortal-pathing prereqs** (the whole point: sidestep reputation/mark/court gates). `forceOfferBeatById` extended to host delivery beats; `BeatScheduledTrace`/`BeatOfferedTrace` gain optional `templateId` so traces name the wrapped encounter. New CLI `beat` command (`status` / `list [delivery]` / `fire <beatId>`); `__DEBUG.listBeats` adds a `delivery` source + `beatSchedule.eligiblePool` reflects the merged set. Base `ASCENDANT_BEAT_POOL` stays delivery-free (existing THR-505 tests unchanged). **Done-when demonstrated:** `beat fire beat.delivery.enc.courtyard_duel` in a seed-42 CLI run offered the delivery beat with `source template: enc.courtyard_duel` and the `ascendant.beat.scheduled` trace `→ enc.courtyard_duel`. 9 new tests (`deliveryBeatAdapter.test.ts`); full suite **11727 green**, vite build green, 30-tick CLI smoke (seed 42 medium, 398 agents, 42 events) clean, zero net-new `tsc -b` errors (baseline red, THR-489). Browser-verify exempt: engine/data/CLI + types only, no UI-render surface touched (debug-bridge listing is a headless read). PR pending.
- ✅ 2026-06-27: THR-505 shipped (Divine Cadence #6) — Pool beat starter library + unlock catalog. Populated the previously-empty `ASCENDANT_BEAT_POOL` (`src/data/ascendant-beat-content.ts`) with 14 cadence-triggered pool beats (6 introduction / 6 investment / 2 selection); the Director now surfaces living-world beats once the spine is exhausted instead of skipping `empty_pool`. New unlock catalogue (plan §4.3): `ASCENDANT_ACTION_BUCKETS` (generic / unlockable-generic / reach-gated + `requiresReach` field for future cards) + `collectGrantedActionIds()` deriving `beatId→grants` from the `BeatDefinition`s (no drift-prone second map). **All grants grounded in real shipping templates only** (`bind_thread_agent` / `bind_thread_location` / `observe_agent` / `action.imbue`) — no vapor ids. 9 new tests (`ascendantBeatPool.test.ts`): Director draws ≥3 distinct pool beats over a 60-tick run (seeded/deterministic), catalogue covers every granted action, every granted + bucketed id resolves to a real `UnifiedActionTemplate`. Full suite **11718 green**, vite build green, 30-tick CLI smoke (seed 42 medium, 398 agents, 42 events) clean, zero net-new `tsc -b` errors (3483 baseline). Authored against the **shipped** minimal `BeatDefinition`/`drawFromPool` shape; deferrals: prose-rich templates + offer→enter→resolve UI path → THR-514; expand pool/buckets as consecrate/bestow/anoint + reach-gated cards ship → THR-515; eligibility predicates + identity (reach/sphere) draw bias (needs Director draw-signature change) → THR-516. Browser-verify exempt: content/data-only (`src/data` + tests), no UI surface touched. PR pending.
- ✅ 2026-06-27: THR-508 shipped (Divine Cadence) — **imbue** expression card (the one card composing THR-509 primitives end-to-end with zero new consumer wiring). New `action.imbue` UnifiedActionTemplate (`targetCategories:['artifact','artifact_legendary']`, cost `IMBUE_ESSENCE_COST`=4) whose `onSuccess` fires a new `imbue_item` GraphOp, intercepted in `unifiedActionResolution.ts` (mirroring `anoint_successor`) → `applyImbueItem` (`src/engine/ascendantExpression.ts`): reads the ascendant's **primary sphere** (`getAscendantPrimarySphere`), picks a sphere-flavored `AttachmentEffect` via THR-509 `pickSphereFlavoredEffect` (seeded local PRNG), and **appends it to the target artifact's `properties.effects`**. Genuinely wired (not dead content): `collectAttachmentEffects` reads `properties.effects` off `possesses`-edge artifacts, so the imbued power applies to the holder — proven by an integration test. New registered `ascendant_expression` trace category. 10 new tests (`ascendantExpression.test.ts`); full suite **11709 green**, vite build green, 30-tick CLI smoke (seed 42 medium, 398 agents, 42 events) clean, **zero net-new `tsc -b` errors** (3483 baseline). Browser-verified at 1920×1080 (`?view=game&seeded`, worktree server): dynamic-import confirmed `action.imbue` live in build with `imbue_item` op; `__DEBUG.grantUnlock` accepts it; drawer correctly hides it until unlocked (turn-1 Core = Move+Investiture only); 0 console errors. **Toolkit split** (plan §4.4 anticipated): `consecrate`/`bestow`/`anoint` → THR-511/512/513 — each needs a new consumer subsystem (location-sustained spawn bridge / agent casting / chosen-power consumer). Systemic-wiring guide + wiring-checklist updated. PR pending.
- ✅ 2026-06-27: THR-501 shipped (Action System & Unlocks) — Turn-1 floor = Move + Investiture across every action surface; retired the legacy intervention wheel + gated the agent hand and AscendantBar Self/Rare. **Substrate:** `STARTER_ACTION_IDS=[]` + `STARTER_ACTION_COUNT=0` (`actionUnlock.ts`); stripped `starter:true` from the 12 ex-Starter-12 templates (`unified-action-templates.ts`) — the empty floor + `isActionRevealed` now hide every former starter until a beat unlocks it. **Agent hand:** retired `getAgentWheelSlots` (the tier-based radial wheel — no unlock concept, on the rejected-approaches list) from `useAgentInteraction`; interventions now flow through the unified `divine.*` templates via `getTargetActionSlots` (new curated `AGENT_INTERVENTION_TEMPLATES` = 8 divine + observe + thread-creation), now passing `unlockedActionIds` (was omitted → the gate's backward-compat branch revealed all cards). `wheel.ts::getAgentWheelSlots` flagged `@deprecated`. **AscendantBar:** `selectActionTray` gates Self/Rare behind the unlock set (`isActionRevealed`); `ActionsBlock` hides an empty tier group → turn-1 shows Core only. Core (Move + Investiture, hardcoded) stays always-on. Tests updated (targetActions filter-8, debug-bridge, useAgentInteraction-effects mock). Full suite 11699 green, vite build green, 30-tick CLI smoke (seed 42 medium, 398 agents, 42 events) clean, `tsc --noEmit` clean. Browser-verified at 1920×1080 (`?view=game&seeded`): `__DEBUG.listStarterActions()===[]`; AscendantBar Actions·2 → Core·2 (Move + Investiture), no Self/Rare groups; select Kael → detail panel opens with zero intervention cards / no drawer; no former-starter hex verbs; 0 console errors. Blocks THR-504 (spine beats deliver the unlocks). Note: pre-existing unused `INTERVENTION_DEFINITIONS` import in `useAgentInteraction.ts` left untouched (not orphaned by this change). PR pending.
- ✅ 2026-06-27: THR-502 shipped (Divine Cadence #3) — Home seat (throne) + mana income. New optional `AscendantProperties.homeSeatLocationId` (`src/types/influence.ts`) + tunable `ESSENCE_PER_SEAT` (1.0, `influence-content.ts`, exposed in both CMS registries). Both essence paths gain one seat term: `computeEssenceGeneration` (engine) + `computeEssenceIncome` (HUD mirror) add `ESSENCE_PER_SEAT` for the live `homeSeatLocationId` location, **deduped against the place-of-power loop** so the seat is a named higher-yield place of power (replaces, not stacks). Throne modeled as a `location` node + `controls` edge (NOT a new node type) — no `thread`-to-location edge (deferred to spine beat #5: thread edges are actor-targeting). New `setHomeSeat(graph, ascendantId, locationRef?)` engine helper (resolves by id/prefix/name; auto-picks capital→city→first when omitted) wired to the encounter debug bridge + exposed as `__DEBUG.setHomeSeat()`. HexMapV2 seat signifier (gold ring + crown) in `LocationIconMesh` keyed off new `LocationNode.isHomeSeat`, threaded from GameView's `locationNodes` memo. 10 new tests (seat income on both paths incl. no-stack + fail-soft; setHomeSeat property/edge/default; LocationIconMesh +1 seat sprite). Full suite 11696 green, vite build green, engine smoke (seed 42 medium, tick 30, 398 agents) clean, zero net-new `tsc -b` errors. Browser-verified at 1920×1080: `__DEBUG.setHomeSeat()`→"Ardenmor Keep" (loc_0), +1 essence/tick; seat hex renders gold ring+crown signifier; 0 console errors. Note: a seat change rebuilds the location layer (existing locations-change behavior) → one-time zoom reset (spine sets the seat once early in normal play). PR pending.
- ✅ 2026-06-27: THR-503 shipped (Divine Cadence #4) — Reach gate in `targetActions` + persisted ascendant reach. Both ascendant-creation paths (`createAscendant`, `createAscendantFromIdentity`) now persist `domainAffinities` on the ascendant node (`AscendantProperties.domainAffinities`); new reader `getAscendantDomainAffinities(graph, ascendantId)`. New optional `UnifiedActionTemplate.requiresReach?: ReachDomain` + tunable `REACH_GATE_MIN_AFFINITY` (0.2, `influence-content.ts`). New filter-9 reach gate in `getTargetActionSlots()`: a reach-gated card is **hidden** (like the unlock gate, never aspiration) unless the ascendant's affinity in that reach ≥ floor — a **permanent two-domain membership filter** per the director's settled premise (fixed primary+secondary reach for the whole run). Fail-soft: omitted affinities → fail-open; unknown reach → ungated + warn. Both drawer hooks (`useTargetActions`, `useAgentInteraction`) read affinities from the node. Trace gains optional `filteredByReach`. 8 new tests (6 gate + 2 persistence); full suite 11685 green, vite build green, engine smoke (seed 42 medium, tick 30, 398 agents) clean, zero net-new `tsc -b` errors (3483 baseline). Browser-verified at 1920×1080: live witness ascendant node carries `{eye:4,veil:3,shadow:2}`, served-code gate hides off-reach card / shows on-reach, 0 console errors. PR pending.
- ✅ 2026-06-26: THR-500 shipped — Ascendant Beat Director foundation (Divine Cadence substrate). New `phaseAscendantBeatDirector` orchestrator phase (registered post-doom / pre-resolution): spine-first, then cadence-gated seeded-PRNG pool draw, max-one-pending, **only offers** beats (resolution flows through the existing encounter pipeline). New flat `GameState.ascendantBeats` (`AscendantBeatState`: spineCursor/pending/history/lastBeatTurn) + types in new `src/types/ascendantBeat.ts`; init in `gameInit`. New `unlock_action` aftermath effect grows the run-scoped unlock set — **reuses existing `unlockedActionIds`** (the field the drawer already filters via `isActionRevealed`) rather than renaming to `runUnlockedActionIds`, honoring the handoff's additive-only + THR-501 mutex; emits `action.unlock.granted`, idempotent + fail-soft on unknown id. 5 trace categories added (`ascendant.beat.{scheduled,offered,skipped,resolved}`, `action.unlock.granted`) — interfaces + `TraceEntry` union + `TRACE_CATEGORIES`. Tunables + catalogue in new `src/data/ascendant-beat-content.ts` (BEAT_BASE_INTERVAL/JITTER/MIN_GAP/MAX_PENDING, SPINE_TRIGGER_TURNS, BEAT_KIND_WEIGHTS) + a minimal turn-gated spine (rich spine/pool authoring deferred to child issues). 11 new tests (`src/engine/__tests__/ascendantBeat.test.ts`); full suite 11660 green, vite build green, zero net-new `tsc -b` errors (3483 baseline, THR-489); 30-tick CLI smoke (seed 42 medium) fired spine beat `beat.spine.opening` on turn 1 (398 agents, no throw). PR pending.
- ✅ 2026-06-27: THR-509 shipped — Reusable ascendant action primitives (Divine Cadence foundation; unblocks THR-508 cards). New `src/engine/ascendantPrimitives.ts` with four pure, fail-soft, traced (`ascendant_primitive`) building blocks: **(1) `relic_upkeep_substitute`** — `getUpkeepStatus(effect, graph)`; `ControlSpec`/`ControlEffect` gain `upkeepArtifactId?`, and `phaseControlEffects` waives `perTickCost` while the relic lives, lapses (`upkeep_relic_destroyed`, new `LapseReason`) when it's destroyed. **(2) `co_located_thread_aura`** — `applyCoLocatedThreadAura(graph, locationId, ascendantId, spec, tick)` advances a thread-edge field (default `ticksAtCurrentTier`) for every threaded agent co-located with a location (+contained sublocations); carried on new `ControlEffect.perTickThreadAuras`, applied each tick. First consumer: consecrate faith-spread (`CONSECRATE_DEVOTION_PER_TICK`). **(3) `chosen_status_grant`** — `applyChosenStatusGrant(graph, nodeId, domain, byAscendantId, tick)` flags `node.properties.chosen` + records a power from `(nodeType × ReachDomain)` `CHOSEN_POWER_TABLE` scaffold (faction = `actor`). **(4) `sphere_flavored_effect`** — `pickSphereFlavoredEffect(sphere, rng)` picks an `AttachmentEffect` from `SPHERE_EFFECT_TABLE` via injected seeded PRNG. Shared spec/constants in `src/types/ascendantPrimitives.ts` (avoids engine→types cycle). 18 new tests incl. faith-spread→tier-promotion integration; full suite 11678 green, vite build green, engine smoke (seed 42 medium, tick 30, 398 agents) clean, zero net-new `tsc -b` errors. Systemic wiring guide + wiring-checklist updated. PR pending.
- ✅ 2026-06-26: THR-494 shipped (THR-493 G1) — Ascendant-reach test fixtures + harness identity param. New `src/data/__fixtures__/ascendant-reach-fixtures.ts`: canonical 1:1 `REACH_PRIMARY_SPHERE` map (Force→Iron … Entropy→Shadow), `REPORT_ASCENDANT_REACHES` list, 8 deterministic `AscendantIdentity` fixtures (one per primary reach; secondary sphere = next Creation sphere cyclically; pinned reach = top `domainAffinities`), `getAscendantReachIdentity` lookup with fail-soft error listing valid reaches. New `src/testing/ascendantReachHarness.ts` `buildAscendantReachState(reach, seed, {mapSize, cosmology})` over `initializeGameStateFromIdentity`. Wired `--ascendant <reach>` / `--ascendants a,b` / `--all-ascendants` into `scripts/gameplay-report.ts` (identity path; prints `primaryReach=` per run); added `resetDecisionCache()` to runOnce so every report run is deterministic. 5 fixture/determinism tests; full suite 11649 green, vite build green, zero net-new tsc errors. Unblocks the Content Health Report matrix. PR pending.
## Archived to project-history.md
- 2026-06-23 batch (status trim): THR-476, THR-488, THR-486, THR-487, THR-492
- 2026-06-27 batch (status trim): THR-481, THR-477, THR-491, THR-479, THR-472, THR-475, THR-473, THR-463, THR-462
- 2026-06-13 batch (status trim): THR-461, THR-460, THR-250, THR-378
- 2026-06-12 batch (status trim): THR-451, THR-62, THR-453, THR-455, THR-456, THR-442, THR-449, THR-446, THR-381
- THR- and earlier — see project-history.md
- 2026-05-06 batch: THR-311, THR-312, THR-313, THR-314, THR-315, THR-316, THR-317, THR-320, THR-321, THR-322, THR-323, THR-336
- 2026-05-07 batch: THR-324, THR-325, THR-329, THR-339, THR-340, THR-341, THR-349, THR-350, THR-354, THR-355, THR-356, THR-357, THR-358, THR-359
- 2026-05-08 batch: THR-139, THR-176, THR-177, THR-178, THR-179, THR-215, THR-265, THR-266, THR-267, THR-289, THR-292, THR-326, THR-330, THR-331, THR-332, THR-333, THR-334, THR-335, THR-338, THR-343, THR-344, THR-345, THR-352, THR-353, THR-360, THR-361, THR-362, THR-363, THR-365, THR-368, THR-374, THR-387, THR-388
- 2026-05-09 batch: THR-268, THR-389, THR-391
- 2026-05-10 batch: THR-394
- 2026-05-11 batch: THR-393, THR-403, THR-408, THR-411
- 2026-05-12 batch: THR-413, THR-404, THR-397, THR-398, THR-399, THR-405, THR-416, THR-409
- 2026-05-16 batch: THR-416 (via THR-409 worktree cleanup closeout), THR-249, THR-248, THR-377, THR-447
- 2026-06-11 batch: THR-406, THR-450, THR-452, THR-457

## Active Backlog Ideas
- **TB-105–108 Thematic Pressure & Living World Pass** (omen agendas, cool failure, doom identity, intent/activity visibility)
- **TB-095–099 Social Systems Expansion** (v1.2 — designed, ready to sequence)
- TB-071 Economy Second Pass · TB-069 Location NPCs · TB-051 Monster Encounters residual · TB-037 Onboarding

Full backlog: [Linear (Threadbare team)](https://linear.app/threadbare) · Completed work: `Docs/project-history.md` + Linear "Done" state · Pre-Linear history: `.planning/BACKLOG_HISTORY.md`
