# Project Status
> Updated 2026-04-19.
## Current Focus
**Encounter Format Migration — Phase 4 Builders Fellowship complete, remaining guilds next.** 115 legacy EncounterTemplates migrating to UnifiedActionTemplate. Phase 0 engine prerequisites shipped (THR-110–118). Phase 1 TG (THR-89) + Phase 2 AC (THR-91) + Phase 3 CG (THR-92) + Phase 4 BF (THR-93) complete. Reputation polarity tagging complete (THR-33). Design doc: `Docs/plans/2026-04-16-encounter-template-migration.md`.

## Milestone Status
- **v1.0 Foundation:** Shipped 2026-03-30 — Phases 1-18 + M2.5 (81 plans, 1533 commits)
- **v1.1 Optimization:** Shipped — Phases 19-22 (determinism, wiring, performance, hygiene)
- **Encounter Format Migration (Now):** ✅ Phase 0 complete. ✅ Phase 1 TG (THR-89). ✅ Phase 2 AC (THR-91). ✅ Phase 3 CG (THR-92). ✅ Phase 4 BF (THR-93). Merge held on BF until THR-134 U4 closes.
- **Content Architecture (Now):** Shell/primitive work — stateful shells (Phase 2), progress/service shells (Phase 3), starter libraries (Phase 4), governance (Phase 5). ✅ THR-86 shipped (routine template structural variety). THR-88 next.
- **Attention Tier Model (Now):** ✅ THR-16 curator metadata wiring shipped. UI integration ongoing.
- **Social Systems Expansion (Now):** THR-28/27/30/51 shipped. THR-29/78 queued.
- **Thematic Pressure & Living World (Next):** THR-19 Omen Agenda shipped. THR-87 (cool failure prose) blocked by THR-116.
- **Agent Success Redesign (Next):** Phases 1-4 shipped. Phases 5-8 queued.
- **Rarity Model (Next):** Three deferred Phase D items.
- **Procedural Hex Vignettes (Next):** Phases 2-5 queued.
- **Prose Content Quality Pass (Archived 2026-04-16):** Scope subsumed. THR-86/88 → Content Architecture; THR-87 → Thematic Pressure; THR-82/83/84/85 → Encounter Format Migration.
- **Next up:** Phase 4 content migration — next guild faction encounter templates.

## Recent Completions (2026-04-18)
- **THR-168 — UI Overhaul 01: Design tokens foundation:** Added 12 sphere color tokens + `-bright` variants, 8 semantic `--type-*` font-shorthand tokens, `--accent-gold-glow` (was already present), and 8 `[data-reach="..."]` → `--reach-sphere` CSS mapping rules to `src/index.css`. Replaced Google Fonts `@import` with local `@font-face`; woff2 files (Cinzel variable, Alegreya Sans 4 weights) copied to `public/fonts/`. Additive only — no existing tokens changed. Fonts verified loaded. Foundation for issues 2–5.

## Recent Completions (2026-04-19)
- **THR-154 — Ruins Layer PR 6 (worldgen elder_ruin density pass):** `src/content/ruins/archetypes.ts` — vault/temple/battlefield archetype descriptors + weighted random picker. `src/engine/ruins/elderRuinSeeding.ts` — full worldgen seeding pass: historical territory detection via `belongs_to` cultureLayer edges, settlement exclusion, water terrain skip, per-hex cap (1 standard / 3 capital), province-role density/magnitude bias, sphere alignment from culture or terrain affinity, `ruins.density_seeded` trace with byArchetype/byMagnitude/bySphere breakdown. Wired into `gameInit.ts` after `seedMonsterLairs`. Ruins debug tab added to DebugPanel. 7 tests.
- **THR-166 — Register craftsmanship reputation axis for BF (deferral from THR-93):** Renamed all 17 `bf.craft_work`/`bf.construction_work`/`bf.fellowship_work`/`bf.master_craft` tally keys to `stone.positive` in `builders-fellowship-encounter-content.ts`. Delta values preserved. Removed v1-substitution header comment. Added regression test to `phaseReputationTraits.test.ts` asserting BF tallies promote to Steadfast Builder at Level 1.
- **THR-150 — Ruins Layer PR 2 (clue lifecycle + Narrative Gravity):** `src/engine/ruins/clueLifecycle.ts` — `selectClueRecipient` (6-channel Narrative Gravity weighted-random, saga tier floor, score breakdown traces), `produceClueConsequence`, `consumeCluesOnConvergence` (prune → `knows_of` familiarity edges), `spawnClueFromEvent` (aftermath hook), `phaseClueDecay` (Phase 6.654, TTL-based knows_clue_of expiry), `findAnyRuinId` (dynamic `$nearest_ruin` resolution). `spawn_clue` aftermath effect kind added to `unifiedAction.ts` + handled in `encounterAftermath.ts`. Phase 6.654 wired in orchestrator after phaseSecretsFavors. 5 encounter templates seeded with spawn_clue hooks (oracle-consulted, silent-chamber, infiltrators-approach, veiled-consultation, blinded-oracle). CluesDebugTab added to DebugPanel. `knows_clue_of` + `knows_of` registered in edgeSchema. 11 tests including 1000-trial Kael/Mira worked example (193.0:14.0 ≈ 93%:7%).

## Recent Completions (2026-04-18)
- **THR-149 — Ruins Layer PR 1 (foundation):** 16 `ruins.*` trace categories registered in `src/types/trace.ts`. `knows_clue_of` edge type added to `src/types/graph.ts`. New `src/types/knowledge.ts`: `ClueSource`, `CluePrecision`, `KnowsClueOfEdgeProperties`. `'elder_ruin'` + `'place_of_power'` added to `LocationSubtype`. New `src/engine/ruins/constants.ts`: 56 named constants (clue decay/bias/spawn, delve concurrency/consequence/beat, PoP streams, worldgen density, Narrative Gravity tier gates). Foundation-only — no tick-phase wiring. Codex review: beat-duration comment, magnitude-gap comment, `SAGA_CLUE_MIN_TIER: AttentionTier` typing. Downstream PRs THR-150–156 unblocked.
- **THR-161 — CI/CD Pipeline Setup (TB-121):** CI workflow (`ci.yml`) already in place from April 13 commit. Audit corrected inaccurate CLAUDE.md claim ("Vercel runs tests" → GitHub Actions is the gate; Vercel runs only `vite build`). Branch protection blocked by GitHub private-repo limitation (requires Pro); documented as impediment #56 with exact settings for user to apply. Codex review: wording sharpened. Remaining user action: enable branch protection once GitHub Pro is available.
- **THR-160 — TB-120 Test Suite Repair Sprint:** `npm test` green on main. Root cause: `encounter-liveness.contract.test.ts` second test missing `{ timeout: 120_000 }` — hit vitest 5s default under full-suite load (passes in isolation). Also deleted `FACTION_ENCOUNTER_TEMPLATES.toHaveLength(10)` brittle count assertion. Codex review: clean. Deferral: `factionQuestAndReputation.test.ts` line 212 still has `toHaveLength(10)` — logged as THR-165.
- **THR-148 — Portfolio-pinning mechanism (Narrative Gravity prerequisite):** `portfolioManager.ts` with `pinAgent`/`unpinAgent`/`isPortfolioPinned`/`getPortfolioPinnedAgents` functions. `PORTFOLIO_MAX_PINNED=7` cap enforced. `PortfolioPinResult` return type with `success`/`message`/`pinnedCount`. Trace categories `portfolio.pinned`/`portfolio.unpinned`. Debug bridge wired: `pin`/`unpin` CLI commands in `debugCommands.ts`, `window.__DEBUG.pinAgent`/`unpinAgent` on the bridge, `CommandTab.tsx` handler. `AgentNodeProperties` interface (docs-only). 12 tests. Codex: bridge fallback `pinnedCount:0`, non-actor guard tests added.
- **THR-51 — Agent Initiatives system (complete):** Six multi-tick initiative types (Found Organization, Recruit Party, Organize Festival, Consecrate Holy Site, Commission Quest, Establish Spy Network). Axiological + wealth + capability scoring competes with encounters in `phaseAgentDecision`. `phaseInitiativeProgress` (Phase 2.32) advances active initiatives: failure conditions, checkpoint capability rolls, sabotage (extension + fail chance), completion with all 8 outcome types including `create_faction` (generates runtime `FactionDefinition` → `seedFactionFromDefinition` → `state.dynamicFactionDefinitions`) and `spawn_encounter` (plants `PendingEncounterSeed` for quest pickup next tick). `getFactionDefinition(id, dynamicDefs)` helper added to `factionNetwork.ts`. Two divine actions: inspire + sabotage. UI: initiative progress card in AgentDetailPanel. 18 tests.
- **THR-33 — Reputation polarity tagging:** 27 explicit `reputationPolarity: 'positive' | 'negative'` tags applied to `EncounterTemplate` instances across 8 content files (army, civic-guard, encounter-content, faction, lorekeepers-covenant, mercenary, merchant-consortium, secret, underking-court). Tags follow the 5-rule rubric: Rule 2a (explicitly prosocial/lawful → positive), Rule 2b (explicitly harmful/criminal → negative), Rule 2c (sanctioned institutional duels → positive), Rule 3 (sole template in type bucket). ~126 L2-fallthrough templates deliberately left untagged (ambiguous/context-dependent). Total under the 40-tag cap.
- **THR-92 — Phase 3: Civic Guard encounter migration:** 15 `EncounterTemplate` objects in `civic-guard-encounter-content.ts` converted to `UnifiedActionTemplate`. Civic Guard voice prose (duty weight, moral clarity tested by reality, institutional gravity). Full enrichment wiring: `{name}`, pronouns, `{location}`, conditional blocks (`{?has_faction}`, `{?has_ally}`, `{?no_faction}`). Aftermath: hidden marks (corruption, watch presence, siege vulnerability), encounter seeds (gate_duty→investigate_disturbance, patrol→pursue_suspect, raid→purge_corruption), intelligence grants (grain/network/patrol intel), reputation tallies (cg.watch_work, cg.senior_work, cg.elite_command, cg.checkpoint_work). Engine fixes: `phaseAgentDecision` now checks `getUnifiedTemplateById` independently (supports fully-migrated templates with no EncounterTemplate fallback); `debugEncounterTools` shim populates `locationTypes` from `locationSubtypes`; `buildGateDutyEncounterStageModel` updated to accept `UnifiedActionTemplate` directly.
- **THR-147 — Reputation-gated encounters final tranche:** 5 `UnifiedActionTemplate` encounters: `the-executioners-commission` (Iron- required-negative), `the-unmarked-crossing` (Gold- required-negative), `the-silent-chamber` (Veil- required-negative), `the-jury-of-the-ruined` (Stone- required-negative), `the-blinded-oracle` (Eye- required + Iron+ blocks — novel mixed-polarity gate). Engine fix: `filterByReputationGates` now makes blocks win over unlocks. `HiddenMarkCategory` extended with `reputation_note`/`divine_favor`. TRANCHE extended to 15 entries. 5 concept-art PNGs. Codex: positive-path test confirmed present. Closes Content Architecture THR-147.
- **THR-146 — Reputation-gated encounters middle tranche:** 5 `UnifiedActionTemplate` encounters: `the-merchants-favor` (Gold+), `the-oracle-consulted` (Eye+ standalone), `the-star-pilgrim` (Star+ required / Star- blocked), `the-infiltrators-approach` (Shadow+), `the-renowned-duel` (power.renown novel gate, Iron reach). encounterGates updated for 6 traits. 10-entry TRANCHE fixture. Codex: persistence values, deduped supportRoles, canonical sublocation-type.tavern ID. 5 concept-art webps.
- **THR-30 — Secrets & Favors Information Economy v1:** `knows_secret_of` + `owes_favor` graph edges with full cap enforcement. `secretGeneration.ts`: `generateSecret()` from graph state (faction memberships, hostile edges, conflict history → typed secret with magnitude); `createSecretEdge/createFavorEdge` with MAX cap enforcement. `phaseSecretsFavors` at Phase 6.653: decay, tension escalation, expiry. Encounter aftermath: `secret_discovery` + `favor_creation` switch cases wired. GraphOps: `reveal_secret` (actor reveals their own secret about target), `call_in_favor` (actor redeems favor target owes to them), `plant_secret` (creates fabricated secret). 3 divine action templates (Reveal Secret, Call in Favor, Plant Secret). 6 secret discovery encounter templates in `secret-encounter-content.ts`. `favorGeneration` metadata on 4 existing templates. Leverage section on `AgentDetailPanel` (secrets held/exposed, favors owed/owing). Secrets & Favors debug tab in DebugPanel. 17 tests + 2 Codex-recommended regression tests for edge-direction correctness.
- **THR-137 — Intelligence reliability decay (deferral from THR-113):** `phaseIntelligenceDecay` at phase 6.71 decays `IntelligenceRecord.reliability` linearly by 0.001/tick (~12%/game day) after a 20-tick grace window, floor 0.0. Threshold crossings (`reliable→uncertain`, `uncertain→dubious`) emit `intelligence_decay` TickEvents with authored staleness prose. `reliabilityBand()` helper extracted as shared contract. `IntelligenceDecayedTrace`. 11 unit tests + 2 contract tests + 3 integration tests.
- **THR-32 — Reputation-gated encounter first tranche:** 5 `UnifiedActionTemplate` encounters: `warlords-tribute` (iron, required-positive), `shadow-court-audience` (shadow, blocked-by-negative), `pilgrims-offering` (heart, required+blocked), `the-veiled-consultation` (veil+eye, multi-trait AND), `the-stones-judgement` (stone, tier-sensitive L3). 7 `encounterGates` arrays populated in `reputation-trait-content.ts`. 30-test contract suite. `REPUTATION_GATED_README.md` rubric (6 rules). 5 concept-art JPGs in `/public/concept-art/encounters/`. All registered in `UNIFIED_ACTION_TEMPLATES`.
- **THR-133 — Move emitTrace out of setGameState updater for hidden marks (deferral from THR-112):** `consumeMatchingMarks` already returned `{ nextState, tracesToEmit }` (pure); updated `GameView.tsx` caller to destructure the result and store traces in `pendingMarkTraces` (same pattern as `pendingAftermathMutations`), emitting them after `setGameState` completes — fixes StrictMode double-emit of `hidden_mark_revealed` traces in dev. Updated `hiddenMarkReveal.test.ts` and `phaseHiddenMarkDecay.test.ts` to assert on `result.nextState` / `result.tracesToEmit` instead of the old flat `GameState` return shape.
- **THR-143 — Wire encounter-history event nodes for caused_by edges (deferral from THR-116):** `createUnifiedActionEventNode()` added to `encounterEventNode.ts` — creates `event` graph nodes for unified-action step resolutions (parallel to the legacy `createEncounterEventNode`). `UnifiedAction` gains `eventNodeId`, `pendingCausationSourceEventId`, `spawnedFromSeedId/Label`; `PendingEncounterSeed` gains `sourceEventNodeId`. `executeStepResult` creates a node per step; emits `caused_by` edge on first step when `pendingCausationSourceEventId` is set, then clears the marker. `applyEncounterAftermathReaction` passes `action.eventNodeId` into seeds. `evaluateEncounterSeeds` removes the broken try/catch blocks, sets causation fields on spawned actions when the seed carries a source event node. Only actor-typed targets get `participated_in` edges. 37 tests across 2 files.
- **THR-144 — alone/outnumbered predicates (deferral from THR-116):** `classifyCoLocatedActor` helper in new `actorClassification.ts` classifies co-located actors via `relates_to` sentiment (±0.35 threshold) → shared `factionId` → faction-to-faction relation → neutral. `buildPredicateContext` now traverses the graph to compute real `allyCount`/`enemyCount` (was always 0). `PredicateContext` gains `allyCount` and `enemyCount` fields. 3 tunable constants in `effect-constants.ts`. 12 unit tests. Systemic wiring guide updated with Capability 11.
- **THR-15 — Culture-seeded phonetic name generation:** Each culture builds a deterministic `CulturePhoneticSignature` at worldgen (vowels, onset/coda consonants, syllable templates, orthography). All agent/NPC/settlement naming routes through layered picker (phonetic 35% → curated pool → phonetic fallback → generic). `culturePhonetics.ts` engine module, `culture-phonetic-pools.ts` data, extended `pickCulturalName`, `cultureGenerator` registration, wired into `npcSeeding`/`agentLifecycle`/`worldSeed` (including wonder/wilderness/anomaly/seeded-individual paths). Codex fixes: phonetic second-pass reachability + 3 unwired call sites. `CulturePhoneticsInspector` Debug Panel Cultures tab. 6 CMS phoneme pool entries. Styleguide slot. 14 culturePhonetics tests. 2 new trace categories. Systemic wiring guide appendix.
- **THR-91 — Phase 2: Arcane Circle encounter migration:** 15 `EncounterTemplate` objects in `arcane-circle-encounter-content.ts` converted to `UnifiedActionTemplate`. Arcane Circle voice prose (wonder/obsession, sentences that build then snap). Full enrichment wiring: `{name}`, pronouns, `{location}`, conditional blocks (`{?has_faction}`, `{?has_artifact}`, `{?has_ally}`), aftermathConfig reactions. Aftermath: encounter seeds (anomaly→planar_probe→seal_the_breach chain), hidden marks (arcane exposure, forbidden knowledge), intelligence grants, reputation tallies. Registered directly in `UNIFIED_ACTION_TEMPLATES` (bypasses `migrateEncounterTemplate`). `getFactionEncounterById` AC delegation removed. Tier-4 test cap updated 2→12 for guild capstone parity. Codex review: clean.
- **THR-16 — Curator metadata wiring:** `CuratorDecisionTrace` extended with `isChainStage`, `isFinalChainStage`, `factionThreadCount`, `matchesAmbition`. `classifyChainStage()` added to `encounterChains.ts` (mirrors `computeChainBonus` semantics). `agentPursuesReach()` extracted from `encounterScoring.ts` and reused by `phaseAttention.ts`. Faction thread count: Set-based walk via `member_of` + `thread` edges. All 4 signals wired on both unified-action and legacy encounterProgress paths. Codex review: legacy path `factionThreadCount` also wired. 3 new test files (18 tests total).
- **THR-118 — Wiring guide correction pass:** 6 corrections to `Docs/plans/2026-04-16-systemic-wiring-guide.md`: `How to verify` line added to Capability 1; Capability 5 restructured to clearly separate engine-internal helpers from the 18 authored aftermath effect kinds; `40 effect types` conflation fixed in Part 8; Capabilities 8/9 reordered; trace category reference table added to Part 5; downstream skill review confirmed no inherited overstatements. Phase 0 now complete.
- **THR-116 — Causation edges + conditional aftermath effects:** `when?: EffectPredicate` gate on every `EncounterAftermathReactionEffect`. 4 thread-mutation effect kinds. `caused_by` EdgeType. `{cause:label}`/`{cause:ticksAgo}` prose placeholders. 6 trace categories. 10 tests. Codex clean.
- **THR-86 — Routine template structural variety:** `ShapedTemplate[]` with 5 shapes, 23 event types, shape rotation. `enrichProse()` path. 9 tests.

## Active Backlog Ideas
- **TB-105–108 Thematic Pressure & Living World Pass** (omen agendas, cool failure, doom identity, intent/activity visibility)
- **TB-095–099 Social Systems Expansion** (v1.2 — designed, ready to sequence)
- TB-071 Economy Second Pass · TB-069 Location NPCs · TB-051 Monster Encounters residual · TB-037 Onboarding

Full backlog: `.planning/BACKLOG.md` · Completed work: `Docs/project-history.md`
