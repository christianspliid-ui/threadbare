# Project Status

> Updated 2026-08-06. Detailed per-ticket narratives that used to live here are archived: shipped-work one-liners in `Docs/project-history.md`, full rationale rows in `Docs/changelog.md`, and the wiring detail in `Docs/plans/wiring-checklist.md`. This file stays ≤60 lines by contract.

## Current Focus

**The strip was empty on the one path players actually reach, and the only honest capture of it needed a declared substitution (THR-994, 2026-08-06).** A THR-646 deferral. `GameView` picks `buildSimpleEncounterStageModel` whenever a notification's `UnifiedAction` no longer resolves — **the common case for any encounter the player did not open mid-flight** — and that adapter's header carried `title`, `locationLabel: ''`, `threatLabel`, `threadTier` and nothing else. `ContextStrip` gates every element it draws on precisely the missing fields, so THR-636's headline "whose encounter is this, and where" answer rendered as a zero-height container with no children, while `onShowOnMap` sat wired with nothing to call it. The fix reads every value from data the adapter **already receives**, so `GameView.tsx` is untouched — which also kept the diff clear of THR-1005's open PR on that file. **`locationLabel` returns `''` where the unified adapter returns `'Unknown Location'`, and that is a decision rather than a shortcut:** `ContextStrip` rejects the placeholder *by name*, so it would render as an invisible gap either way — the empty string is the same outcome stated honestly, and Law 4 wants a designed fallback, not a lie. **Second half:** resolved history rows now carry `outcome` / `outcomeWord` / `reachLabel` / `afterimage` / `replayNarrative`, because the veil's replay body reads `replayNarrative || afterimage || ''` and the rows supplied neither — the step navigator entered replay correctly over a blank body. There is no frozen `stepProseHistory` on this path (that record belongs to a live action), so replay text is the step's own authored prose re-enriched under its outcome band, the fallback the unified adapter already documents. **The tests render the real `EncounterVeil` rather than asserting on the returned model**, as the Done-when demands, because the defect is an *invisible element* — a shape assertion passes happily against a header whose fields nothing reads; falsified against the pre-fix tree at 7 of 8, the 8th being a guard against over-filling unresolved rows that correctly passes both ways. **The browser evidence is a declared substitution and the reason is a finding in itself:** the fallback cannot be reached on demand in a seeded run, because the auto-open scan surfaces encounters while their action is still *live* (so every naturally-opened veil takes the unified adapter) and the 20-tick `RESOLVED_ACTION_RETENTION_TICKS` window never opens before the doom clock ends the run — four runs, ~45 min, logged as impediment #455 with the adapter discriminator, since the two paths render the same component and their screenshots are visually identical. Captured by mounting the production veil over the **live** `gameState` at 1920×1080 (real graph, real resolved action `ua_34`, real actor): `Vessen · Iron · Meadowcity · Show on map` with portrait tile, `childCount: 3, height: 30px` against the ticket's measured `0` and `0`. (THR-927's entry moved to `project-history.md` to hold the 60-line cap; it already carries a ✅ there.)

**The rule was broken upstream of the component, so the component rendered the violation faithfully (THR-1004, 2026-08-06).** Director review, second statement of the same law: *"the fundamentals of presenting the game concepts that are manipulated in the aftermath … are not presented with the images and links and tooltips that we usually do. i shouldn't have to repeat this."* His screenshot also carried `0.04 star growth`, `+0.050`, `star.positive tally shifted by +1.00` and `left 1 reward, 3 skill shifts` — magnitudes to two decimals and a raw internal key, on a mortal-facing surface. **The chip module was not the culprit and its header said so** ("sentences are authored prose") — true of the authored half and false of the game, because roughly half the change set is engine-*derived* and those producers were emitting debug-grade template literals that the surface then drew faithfully. Fixed at the source: `engine/aftermathWords.ts` now owns every derived sentence, bands each delta the way `difficultyWord` already bands its own, and resolves `star.positive` through the reach vocabulary; the numbers stay on the trace. **The second half is that a surface cannot honour the UI Law by parsing English**, so the producer declares what it named — `EncounterAftermathChange.concepts` — and the chip attaches the tooltip, the entity tile and the link from that. The link is routed by *kind*: `onSelectEntity` opens a faction or artifact sheet, because handing an artifact id to the agent drawer is a dead link that looks live. `resolveUnifiedReward` was widened to return the instantiated node id, which is what turns the prize chip from a name-hashed fallback tile into the item's own art plus a working link. **Encoded so it stops needing to be said**: the Law is now a named checklist item in `frontend-ui` §7 and in `Docs/canon/design-governance.md` § UI pillar, with the two failure modes named in Anti-Patterns. Verified at 1920×1080 on `encounter.shrine_offering`: ten chips, zero numerals, zero raw keys, reach tooltip open on the chip, item art and link on the prize. (THR-771's entry moved to `project-history.md` to hold the 60-line cap.)

**The ending of an encounter did not say which encounter it was the ending of (THR-1003, 2026-08-06).** Director review of the shipped THR-971 chip surface: *"there is nothing in the aftermath UI that helps the player understand that it is part of the same encounter. The encounter title is not there at all."* The step view carries full identity chrome — encounter title, agent context strip (portrait, name, reach chip, location, Show on map), clickable step navigator. The aftermath carried a hand-rolled row of flat green dots in the top-left corner reading `resolved`, and nothing else: **it said resolved without ever saying resolved *what*.** Fixed by giving the aftermath the same header the step view renders, from the same components rather than copies — `StepNavigator` gains a `mode="resolved"` reading and `ContextStrip` is reused unchanged, so the two surfaces cannot drift apart the way a forked header would. The corner dot row is deleted; the navigator now shows each step in its own outcome colour (`held` / `wavered` / `held` on the verification run) instead of flattening three different endings into one green. **Two decisions the ticket did not ask for but the reuse forced.** The aftermath path renders no replay view, so every dot is `disabled` in resolved mode — a control that silently does nothing is worse than no control. And the count is derived, not asserted: an encounter can stop before its last step, so the label reads `all 3 resolved` only when it did, and `1 of 2 resolved` when it did not. The `AFTERMATH` marker moves below the divider in the veil's gold, because the title styling it used to share now belongs to the encounter title above it and two identical ghost lines read as two titles. Browser-verified end to end at 1920×1080: `encounter.shrine_offering` played to resolution shows *Leave a Shrine Offering*, Vara's strip (Star, Sacred Grove), and the three-dot navigator complete. (THR-582's entry moved to `project-history.md` to hold the 60-line cap.)

**A guard that cannot see the failure it was built for, because the failure removes its witness (THR-1001, 2026-08-06).** `check:task-heartbeat` flags a lane as stalled only when a **sibling** of equal-or-tighter cadence kept firing — the clause that stops a powered-off machine reading as a fleet of broken lanes. It follows that a silence stopping *every* lane at once has no witness and reports `ok`, and the probe's own test asserts this. That is precisely the shape of the 62-hour 2026-08-03→08-05 gap: three hourly lanes, two weekdays, nothing written to `main` or `ops`, no lane self-diagnosing it, found a week later by `weekly-workflow-retro` and reasonably read as an outage. It was a controlled pause on token/usage limits, and Christian flagged it will recur. **Widening the sibling clause would have destroyed the guard to reach its blind spot**, so this is a second probe, `check:lane-silence`. **It reads commit history, not `lastRunAt`, against the ticket's own suggested first cut** — on resume the scheduler fires a catch-up burst that clusters every lane's `lastRunAt` at the wake edge (measured 2026-07-31: a Monday-only weekly task reporting a Friday `lastRunAt` within 36 ms of two hourly lanes), so at the single moment this check matters, every lane looks current and the gap is invisible; commits are stamped when the work happened. **The pause marker is the half that keeps it from crying wolf**: `~/.claude/threadbare-pause.json`, any content, presence is the declaration — outside the repo because setting it must not require an agent, a session or a commit, and a token limit is exactly when none exist. It suppresses both this probe and the per-lane stall verdict. Two guards stop the marker becoming the new silence: an expired `until` covers that window only, and a marker still set while the lanes are demonstrably writing reports `pause-stale` and asks to be deleted. **The ceiling is stated rather than oversold** — this probe runs inside a lane that goes silent too, so detection is retrospective: a repeat surfaces within 6h of the fleet returning rather than at the next weekly retro, and live paging would need a watchdog outside the fleet, deliberately not attempted. Threshold measured, not guessed: 6h against a largest-healthy-gap of 3h00m on real `ops` history. Three live dry-runs re-measured the incident at **61.9h** from a data source the retro never touched. (THR-979's entry moved to `project-history.md` to hold the 60-line cap.)

**A 400-seed loop was measuring a value the seed cannot move, and the only thing that surfaced it was someone else's PR going red (THR-1000, 2026-08-06).** `playerCastBalance.test.ts`'s `castDistribution` rebuilt a full `WorldGraph`-backed `GameState` **inside** its `SEEDS = 400` loop, and three of its five callers read only `capability` / `probability` — both computed pre-roll, before `resolveActionShared` touches the rng, which decides only *which outcome* a cast lands on. So 399 of every 400 casts in those tests recomputed a constant. **The cost is invisible while it passes**, which is why it was found the expensive way: the 8-call inertness test sat at 113% of the 5s vitest default, PR #1313 added a test file and four JPEGs, and it failed twice in a row (5646ms → 6132ms — re-running made it *slower*) as the sole red in a 14910-test run, holding an armed auto-merge that could never fire. Fixed by hoisting the fixtures and adding `castReadout`, a single-resolution helper for the pre-roll reads; **the assertion is untouched** — same four difficulties, same two scales, same `new Set(probabilities).size === 1` — it just costs 8 casts instead of 3200, and the difficulty range is deliberately not narrowed to buy time because its width *is* the property. **The deliverable is the guard, not the speed-up.** The change rests on a claim about where the rng enters, and a load-bearing claim argued only in a comment is the shape this repo keeps getting burned by: `preRollStable` now goes false if any seed ever reads a different capability or probability, and a new test pins `castReadout` to the loop's own value with a full-seed-count non-vacuity check. Injection-proven both directions — perturbing one seed's capability reddens the stability flag, pointing the readout at the wrong cell reddens the equality plus two downstream tests. **Hoisting was proven neutral by measurement, not argument:** all 7 cells return byte-identical capability, probability and outcome counts on both trees, which is the check that would have caught a mutating resolver had `PLAYER_CAST_PUSH_ENABLED` not been `false`. Whole file 16ms against ~7s; the target test 0ms against 5646ms. Deliberately **not** fixed by raising the timeout — that hides the next test to reach the cliff instead of removing it. (THR-825's entry moved to `project-history.md` to hold the 60-line cap.)

**The blocker was never the wiring, and the surface that should have shown the result does not contain these cards at all (THR-769, 2026-08-06).** Four `company.*` verbs — Bless this Company, Draw Together, Reunite, Sunder — had shipped artless under THR-74 and THR-732 because the executor lane could not author assets, and THR-732 had deliberately declined to add map entries pointing at files that did not exist: **a mapped-but-missing asset renders a broken image, which is strictly worse than the artless fallback.** That trade-off is sound, but it was resting on nothing — no gate proved the mapped paths were real. So the deliverable is not only the four plates (1376×768 JPEG q95, violet `#cc66ff` for the three spirit verbs, pale teal `#8fd4c0` for entropy-aligned Sunder) but the assertion that **every** path in `ACTION_ART` resolves to a file on disk, falsified against a deliberately broken entry before acceptance. **Wiring cost four lines instead of eight because THR-740 landed first** — `codexRegistry.ts` now imports and spreads the canonical map, so one entry reaches every surface; `actionArt.ts`'s own header still claimed a second parallel map existed and is corrected. **The find is what happened when the Codex was opened to photograph the result:** `getAllCodexEntries()` buckets action entries by an explicit list of id prefixes (`divine.`/`invest.`, `action.`, `hex.`, `loc.`, `sub.`, `artifact.`, plus hand-listed thread ids) and has **no `company.` arm**, so all four verbs are absent from the catalog entirely — 181 action entries across six tabs, none of them a company verb, while `__DEBUG.listActions()` returns all four as live. Pre-existing, not a regression here, filed as **THR-999** with the invariant the fix should pin (every drawer-reaching prefix represented in the catalog, so the *next* new prefix fails a test rather than shipping invisible). Verified by mounting the production `ActionCard` for all four at 1920×1080: each `<img>` reports `naturalWidth/Height` 1376×768, `complete`, in-viewport, 0 console errors. (THR-923's entry moved to `project-history.md` to hold the 60-line cap.)

**A content table was keyed to a cosmology the engine no longer speaks, and the read site's `?? []` is why nothing ever said so (THR-770, 2026-08-06).** `GROUP_NAME_SPHERE_ADJECTIVES` supplied the sphere-flavored adjective a god-founded company takes into its name, keyed by `hunger`, `mind`, `spirit`, `blood`, `stone`, `tide`, `flame`, `dusk` — of which exactly **two** are sphere ids the engine can produce. The other six matched nothing, so six of eight alignments drew no flavor at all, with no crash, no warning, and no failing test. **The silence is the finding, not the typo:** the table was internally consistent, its consumer handled a miss gracefully, and every gate the repo owns passed for the life of the feature — a table can only ever be proven to equal itself, which is why the guard here is the *generator's output*, requiring that naming the same companies under a given sphere produce a different name set than naming them with none. A dead key leaves it byte-identical, which is precisely the state that shipped; reintroducing the defect reddens it. **The retired vocabulary was salvaged rather than rewritten, and where it landed is the evidence for what went wrong:** `stone`'s words moved to `matter`, `flame`'s to `energy`, `dusk`'s to `entropy` — exactly the spheres those *reaches* map to under `REACH_TO_SPHERE`, so the pool had been authored against reaches and keyed as spheres. All 12 real ids are now authored, Foundation included, because a caster's primary genuinely can be one (`hunger-catalog.ts` carries `primary: 'light'`) — and the table is typed `Record<SphereName, …>`, so a renamed sphere now fails to compile instead of going quiet. **The second half is that the pool was dead for Draw Together no matter how it was spelled:** the verb never passed a sphere, because the company does not exist when the op fires. The caster's sphere now rides on the pulled mortals as `convergePullSphere` and the formation scan reads it back off whichever member it gathered — deliberately only from members whose pull is *still open*, the same predicate that attributed the cause, so a stale stamp cannot colour a company it did not gather. Registered as an interface contract for the reason the ticket exists: a write/read key mismatch here produces a perfectly ordinary name and no signal. (THR-980's entry moved to `project-history.md` to hold the 60-line cap.)

**Both numbers were measured and both were kept, and the measurement found that the thing one of them describes never reaches the roll (THR-766, 2026-08-06).** THR-728 had to guess two constants to open the outcome ladder for player casts; this was the balance pass. **The verdict on the god curve is keep 6 / 0.5, and the argument is stronger than "it measures fine"** — a fresh god reads capability 0.168 off-domain / 0.231 secondary / 0.354 primary, landing 64.8% success-at-cost / 28.0% clean / 4.3% near-miss / 3.0% surge at `local` scale over 400 seeded streams. The reason not to raise it is that at `local` and `personal` the scale floor clamps authored difficulty to **zero**, so `ASCENDANT_CAST_BASE_RAW` is the *only* term that moves the distribution there — it is not a starting point but the entire range `reachPractice` has to walk, and raising it to 10 puts a fresh god at 50/41 before the first cast, spending the Deepening arc up front. **The risk-word verdict is keep [0.25, 0.45], because the premise the ticket was filed on does not reproduce.** It reported 7-of-10 cards reading *perilous*; measured against the live list the spread is 19% / 50% / 30%, and every widening candidate makes it worse ([0.35, 0.55] → 49/38/13, [0.40, 0.60] → 61/29/10, both dominated by `steady`). **The gap was in how the pool was read, and it is the trap worth carrying forward:** `getTargetActionSlots` gates on `targetCategories` and *never* on `actorAffinities`, so reading the castable pool through `actorAffinities: ['ascendant']` yields **8** templates where the drawer actually builds **519** — a 65× under-count that makes any spread measurement vacuous while looking like diligence. **The real find is that neither remedy the ticket offered could have worked:** `applyScaleDifficultyAdjust` clamps difficulty to `max(0, capability − floor)`, which is 0 at both `personal` (0.70) and `local` (0.65) for any fresh god, and those two scales are **85% of the slot list** (410 + 29 of 519). A card reading *"A perilous working."* and one reading *"A steady working."* resolve identically; widening thresholds or re-pricing templates changes only which word prints. Filed as **THR-998** rather than fixed here — it is a redesign with three directions of very different blast radius, and direction 2 ("say something else on the card") wants Christian's read. **The deliverable is the verdict made falsifiable**, not the two unchanged numbers: `playerCastBalance.test.ts` pins the capability band, the outcome shares, the three-word spread with a non-empty-population guard, and — deliberately — the inertness itself, which is the assertion that must go red when THR-998 lands. Both halves injection-proven before being accepted: `BASE_RAW = 10` reddens three tests (crooked 0.3125 against the `>0.55` bound), `[0.40, 0.60]` reddens the spread on the 60% dominance bound at 0.607. No template difficulty changed, so Done-when 3 is satisfied by having nothing to justify — and the rarity-band sweep the ticket asked for was run (tier 1: 104 of 368 in band, 258 above; tier 4: 0 of 5) and deliberately not acted on, since re-pricing is cosmetics-only under the same clamp.

**A comment described the intended state, the code did the opposite, and the comment had been describing it for long enough that nobody re-read it (THR-740, 2026-08-06).** `codexRegistry.ts` line 66 read "Import the canonical ACTION_ART registry so codex thumbnails stay in sync with the ActionCard without duplicating entries" — and line 68 declared its own 79-row `ACTION_ART` copy directly beneath it. THR-727 had extracted the canonical lookup to `Game/actionArt.ts` for `ActionCard`/`DivineReceiptModal`/`AscendantBeatModal` and left this one behind, so every new card art needed two edits or the codex silently drifted. **The consolidation is behaviour-neutral and that is what made it safe to do mechanically**: the diff run before deleting anything proved the codex copy was a strict *subset* — 79 rows against 87, zero keys unique to it, zero paths disagreeing — so nothing could be lost, and the 8 canonical-only keys are the legacy bare divine aliases (`dream`, `omen`, …) that no codex entry id ever hits. Checked rather than assumed: those 8 do not collide with `ITEM_ART_BASE`, which matters because `ACTION_ART` spreads *last* into `ITEM_ART` and a collision would have silently repointed an item plate. **The regression guard is the deliverable, not the deletion** — a one-line consolidation reverts the moment someone pastes a second map back, so the test asserts exactly one file under `src/` declares `ACTION_ART`, falsified against the pre-fix tree (it names both files and fails). Its two companions pin the drift itself: every codex action thumbnail equals `getActionArt(id)`, and one probe per action family. All three carry non-empty-population guards, because the sampled-entries assertion passes vacuously if the catalog ever stops resolving. **A verification trap worth carrying forward:** the browser probe that was supposed to prove the duplicate was gone used `/ACTION_ART\s*:/` against the Vite dev server's module source — but Vite serves *transformed* TS with the type annotation stripped, so it read "clean" for both files including the one that legitimately still declares the map. It was a false negative dressed as evidence; the corrected probe matches `:` or `=` and discriminates properly (logged as impediment #437).

**Four surfaces hand-rolled the same player-cast sequence, and the copy nobody mirrored an edit into has been untraced its whole life (THR-739, 2026-08-06).** The agent drawer (`useAgentInteraction.handleWheelSlotClick`) and the non-agent drawer (`GameView.handleNonAgentSlotClick`) each carried their own buffs → RNG → `createUnifiedAction` → essence-clamp → `recentEvents` sequence, and the drift was already on the record rather than hypothetical: the non-agent path shipped **without a dispatch toast** until THR-727 fixed both paths *separately*. **The find is the one THR-727 did not reach.** The non-agent drawer never wrote the `ACTION_START` timeline event at all — so every cast the player has ever made at a location, hex, artifact or faction was absent from the ascendant's causal trail (NFP #2), while the identical cast aimed at an agent was recorded. No gate could see it: both paths dispatch, both resolve, both produce a Divine Receipt, and the missing half is an *absence* in a trail nobody diffs. `src/engine/playerCastDispatch.ts` now holds `preparePlayerCast` + `commitPlayerCast`, and **all four** production constructors route through it — both drawers, the intervention confirm, and `__DEBUG.fireAction` — so `source: 'player'` appears exactly once outside tests. **Two optional params rather than one rigid shape, and the reason is a real bug avoided:** `applyBuffs: false` + an explicit `essencePaid` exist because the intervention path has already priced its cast through `executeIntervention` and the debug bridge fires at list price — running the buff pass there would consume a one-shot Recede/Focus on a cast that never saw the discount, spending the player's buff on nothing. That is pinned by a test asserting the buff survives on the node, not left to a comment. **Surface-specific feedback deliberately stays with the surface** — sphere audio, particle burst, drawer close, card-play animation, and each drawer's own dispatch copy — because the alternative is an engine module owning UI; only the `buffParenthetical` string is shared, since that is the one piece the two drawers had already worded by accident rather than by decision. **The adjacent cleanup turned out to be a prop kept alive by its own tests:** `ActionCard`'s `outcomeBand` had no production caller and could not have one — `playing` is dispatch-time state and the band is unknowable until the action resolves ticks later, which is exactly why THR-727 made the Divine Receipt the band surface. Its three asserting tests were the only callers in the repo; removing the prop and the band-keyed overlay with them leaves a band-agnostic spent face and the tests that prove it. Two behaviour deltas stated rather than smuggled: the non-agent drawer and the debug bridge both **gain** the `ACTION_START` event, which is the drift closure, not a regression. (THR-983's entry moved to `project-history.md` to hold the 60-line cap; it already carries a ✅ there.)

**Tier advancement was strengthening a number nothing reads, and making it correct is not the same as making it run (THR-723, 2026-08-06).** `attachmentTierAdvancement` scaled possession-edge `modifiers {iron: …}` by 1.5 per tier, but `modifiers.ts` has exactly **one** production reader — `visibility.ts`, and it only ever asks for `los_range`. So every capability key it scaled was argument-level dead: written, never queried, invisible to a symbol grep because both sides of the contract have live hits. It now scales the artifact's `stat_contribution` effects, the substrate `collectStatContributions` → `computeRawScore` actually reads since THR-718. **The clamp is the non-obvious half:** 1.5 compounds, so a legitimately-authored 1.5 iron reaches ~5.1 by tier 4 against the `ITEM_STAT_BAND_LEGENDARY` ceiling of 2.0 that a content contract test enforces on every catalog — unclamped, advancement is a sanctioned bypass of the exact power budget THR-718 built to hold items in check. **Partition by `REACH_DOMAINS` membership, not a `los_range` allowlist**, so the next live attribute someone adds keeps working instead of being silently killed by a hardcoded name. **The scope was held deliberately, and that is the part worth remembering.** The module has **zero production callers** — its only importers are its own test and the contract registry — so none of this has ever executed in a real game. The ticket's *first* comment said so and flagged wiring as "plausibly a user-facing design call"; a later promotion comment called the ticket a "clean, scoped technical fix", and `pull-work` reads only the latest comment absent a `Reopened` label, so following the skill literally would have surfaced only the reassuring summary (impediment #434). Whether items strengthening over time is a mechanic this game has is Christian's call, not an executor's — split to **THR-996** with the three options framed in game terms; the surviving reach-keyed *seed* writes in `gameInit.ts`/`seedAttachments.ts` are **THR-997**, so both LEAKED rows now point at live tickets rather than the one this PR closes. Found in passing: the row claimed `readSites: ['src/engine/orchestrator.ts']`, which has never imported the module — a transcribed intention, precisely what this registry exists to expose. All 6 new guards confirmed failing against the pre-fix source before being accepted as evidence. (THR-930's entry moved to `project-history.md` to hold the 60-line cap.)

**The handoff step already said "only after the plan doc is merged", and twice in one week it wasn't (THR-921, 2026-08-06).** A plan doc that lives only on an unmerged `docs/plan-*` PR 404s in every executor worktree, because all of them are cut from `origin/main` — so `pull-work` Step 6 ("read the plan doc before touching code") has nothing to read and the run is spent bouncing, reconstructing the design from the issue body, or chasing a branch. THR-884 (impediment #321) named its doc correctly in *both* places and was still unreadable; THR-887 (#325) had the plan doc **and** the wiki page its Done-when required both on open PR #1129, making the ticket unsatisfiable by construction. **The two-place rule protects against a missing reference, not a missing file** — which is why the prose instruction was never going to be enough and the fix is a command. `plan-doc-liveness-predicate.ts` holds extraction + classification; three lanes now cite it instead of three prose copies (the drift THR-836's `coordination-block-predicate.ts` exists to prevent, and that THR-895 later found had opened anyway). **`STRANDED` and `MISSING` are deliberately different verdicts, because they have opposite dispositions:** stranded means the ticket is ready and its artifact is minutes from merging, so the orchestrator *holds* rather than declining and `pull-work` prefers a checkpoint over a bounce; missing means the promotion was premature. A bare "plan doc not found" collapses both and sends the next session hunting a typo when the real fix is merging an open PR. **Scoped to `Docs/plans/` + `Docs/audits/` rather than "every repo path named", as the ticket's fix text read literally** — `src/` paths in a ticket are usually paths it exists to *create*, so checking those reports an error on most well-formed tickets, which is exactly the false-positive rate THR-895 documents as training readers to ignore a lint. **A skipped open-PR scan stays `UNRESOLVED`, never folded into `MISSING`** (THR-828): a check that did not run must not tell a lane to hold for a reason nobody verified. Honest scope limit: the board lint rides `check:process`'s Linear branch, which cannot execute without `LINEAR_API_KEY` — so the composition was lifted into the importable predicate and unit-tested there (impediment #433), with the I/O half proven live through the CLI in all four verdict states. (THR-828's entry moved to `project-history.md` to hold the 60-line cap.)

**A counter was moved off module scope by deleting it, and the sweep that came with it found a feature that has never once fired (THR-817, 2026-08-06).** `bandOpposition` minted synthetic band-counter action ids from a module-scope sequence, so a second same-seed run in one process continued the first run's numbering and two provably identical simulations serialised different ids — measured at tick 82, `ua_band_counter_11` against `ua_band_counter_6`, every other field byte-identical. THR-816 had papered this over with a per-test `resetBandCounterIds()`. **The fix derives the id from the action it answers rather than relocating the sequence onto `SimulationRuntime` as the ticket proposed, and the reason generalises:** a relocated counter still holds a value that depends on *how many* counters were minted earlier, while a derived id depends only on *what* is being answered — so uniqueness becomes a property of construction instead of bookkeeping. Three facts make the derivation total: at most one counter per initiator action, a counter never becomes an initiator itself, and the pool holding them is local to one tick. The reset seam is deleted rather than kept, and its **absence in the determinism test is now load-bearing** — restoring a call there would re-hide the regression. **The enumerated sweep resolved into four tiers, and only the fourth was defective:** 15 counters reset per tick by `resetEventCounters()`, one self-scoping on a tick sentinel (`tugCounter`), 9 persistent-id counters reset at the session boundary by `resetDecisionCache()` — including `npcSeeding.npcCounter`, the ticket's "strong second candidate", which turns out to have been wired correctly all along — and three that belonged to a scope whose function did not call them. Two of those three (`surveyEventCounter`, `questHookEventCounter`) mint `TickEvent` ids and had reset seams with **zero production callers**, only their own tests, while sitting squarely inside `resetEventCounters()`'s stated scope; that docstring's coverage claim is corrected to say it is a membership list that has been wrong, because nothing goes red when an entry is missing — ids stay unique, they just stop being reproducible, and the cost surfaces as a determinism failure far from the omission. **The third instance produced the run's real find.** `lairEscalation.adjacentLairCounter` carried the comment "reset handled by id format", which is false — embedding tick/col/row makes an id readable, not stable — but the non-empty guard on its would-be regression test failed `expected 0 to be greater than 0`, and chasing that proved `spawnAdjacentLair` is **unreachable by construction**: its separation guard measures against a lair list that includes the source lair, and every hex neighbour sits at `|Δcol|+|Δrow| ∈ {1,2}` under a minimum separation of 3, so a lair is always too close to its own neighbours and all six `continue` every time. The counter cannot advance, so it is classified unreachable rather than leaking and its reset is wired defensively with the unreachability stated in place; the authored 15%-per-neighbour spread has never fired in play and the existing test asserted only `not.toThrow()`, which is why it survived — filed as THR-995. The two vacuous tests were deleted rather than weakened to pass. (THR-972's entry moved to `project-history.md` to hold the 60-line cap.)

**The rule deciding which gate track every session owes was hand-copied to five files, and drifted three times without ever going red (THR-988, 2026-08-05).** CI's `detect` job is the authority on what counts as a docs-only diff; the predicate was then re-typed into CLAUDE.md, AGENTS.md, `Docs/canon/process.md`, the `pull-work` skill and the tb-opus-pickup mirror. **All three recorded drifts dropped the same two fragments** — the THR-922 doc-sourced artifacts — and all three were found *incidentally*, while sweeping for something else, because the failure is in the **expensive** direction: a documentation PR silently pays the full ~15-minute code gate. Nothing goes red, so nothing surfaces it. **The canonical source already existed and nothing consumed it:** `DOC_EXCLUDED_GLOBS` + `DOC_TO_CODE_ALLOWLIST` were pinned to `ci.yml` by `docs-code-decoupling.test.ts`, but the prose was pinned to nothing, and the one assertion that touched it checked a substring of *one* file out of five. `scripts/docs-only-predicate.ts` now derives the predicate from those constants, `npm run classify:diff` runs it, and `npm run check:predicate-copies` gates the copies. **Behavioural equivalence, not string equality — the load-bearing design call.** The copies legitimately differ in shape (process.md and AGENTS.md inline the pattern mid-sentence to *explain* the two trailing paths), and the canonical glob order differs from the order the prose reads best in, so byte-matching would force cosmetic churn and fail on a harmless reflow. Each copy is instead **compiled and executed** against a fixture corpus and must classify every fixture identically — stricter where it matters, since inspection alone cannot prove the fragments compose. **The gate runs in `Docs gates`, not `npm test`, and that placement is the point:** every copy is a documentation file, so the PR that breaks one skips `Test · Typecheck · Build` by design; a guard living only in the suite would redden the *next unrelated code PR* instead — failure mode A that `docs-code-decoupling.test.ts` exists to forbid. The vitest test calls the same function for the code direction (a PR moving `ci.yml` or the constants), one implementation, two surfaces. **The prose copies deliberately stay inline** rather than being replaced by "run the script": they also explain the two paths, and a fresh worktree with no `node_modules` can still paste a grep. Injection-proven in both directions — dropping the two artifact fragments fails with each named, and widening to `^src/` fails with "would skip the suite". **Scope limit stated rather than papered over:** the tb-opus-pickup prompt the lane actually executes is unversioned at `~/.claude/scheduled-tasks/`, so no repo gate can reach it; this pins the mirror. (THR-970's entry moved to `project-history.md` to hold the 60-line cap.)

**A verification ticket could not be completed because one of the three things it was asked to photograph cannot render (THR-646, 2026-08-06).** THR-636 shipped three encounter-context surfaces in PR #549 and deferred the browser capture; the capture pass finally ran and found the deferral had been hiding two defects rather than a missing screenshot. **The encounter toast card has no producer** — `EncounterNotificationCard` renders only under `toast.encounterCard` at `ToastStack.tsx:124`, and `grep -rn "encounterCard\s*:" src/ scripts/` returns **nothing**: a type, a reader, and a component, with no writer anywhere including tests. Because the field is optional this type-checks cleanly, so no gate could catch it — the optional-field-with-no-writer shape. Confirmed live off the `ToastStack` fiber: `{ toastCount: 4, withEncounterCard: 0 }`, every toast taking the plain-text branch THR-636 set out to replace (filed THR-993). **The context strip is empty on the path players actually hit** — `GameView.tsx:1308-1320` falls back to `buildSimpleEncounterStageModel` whenever the unified action has been reaped, and that adapter's header carries `title`/`locationLabel: ''`/`threatLabel` and none of `agentName`, `portraitUrl`, `reachLabel` or `hexCol/hexRow`, which is every field `ContextStrip` gates on — so it renders a zero-height box with no children (`childCount: 0` measured in the DOM) while `onShowOnMap` sits wired with nothing to call it. The same adapter emits history without `replayNarrative`, so the step navigator enters replay correctly (`replaying 1 of 2`, gold-ringed dot, "← Return to the present") over an empty body (filed THR-994). **The component is not at fault and that is what the second capture proves:** a debug-spawned unified encounter renders the strip properly — `Kael · Stone · Ardenmor Keep` with its portrait tile — so only the fallback model starves it. Engine side is healthy: `getStepProse` returned 3 records with prose, outcomes and reaches. Console clean apart from the absent TTS sidecar; the `amb_evt_17` duplicate-key spam reproduced live is THR-853, already queued. No code changed — the deliverable is the evidence and the two tickets. (THR-970's entry archived to `project-history.md` to hold the 60-line cap.)

**An army's larder now hangs off the trade web, so cutting a road is a way to break a host you never fight (THR-626, 2026-08-05).** THR-614 shipped armies with no supply model — a self-contained starvation clock at a fixed siege tick and a Gold-proxy `underfunded` term — and deferred the coupling here. `phaseArmySupply` (post-economy, after `route_events`, every 4 ticks) resolves each army's line with a bounded BFS over the **shipped** `road` + `trades_with` conduits to the nearest location its own faction controls whose `resourceBalance` clears a floor; throughput decays per hop and is multiplied by 0.4 when any hop carries `threatened`, so **banditry strangles a line rather than severing it** — the more interesting state, because the army can still act while it degrades. Nothing moves in transit (pull-derived, never token-pushed; there are no wagons), the larder is private, and `supplyTier` (`supplied | strained | starving`) is the only read surface. **Supply is a second axis, not a rename of the first:** `underfunded` is the faction failing to *pay*, `unsupplied` is the web failing to *feed*, so a rich faction whose roads are cut still starves — the state the whole ticket exists to make reachable. `supplied` costs 0 attrition by construction, keeping this additive over TB-073's balance rather than a retune. **The THR-618 checkpoint named this ticket as one of two re-open triggers for extracting a Flow Web primitive and required the design to compare against `tradeRoute.ts`/`routeEvents.ts` and either reuse or extract — it reuses**: no second manifest type, no second decay clock, no new node or edge type; a supply line is a *derived path over relationships that already exist*, which is precisely why severing a trade route is felt at the front. Siege starvation swaps its source too — a defender with an intact unthreatened relief line holds out 2× longer, and `hasReliefLine` deliberately excludes the self-host case, because a besieged city is nearly always a stocked controlled location and asking whether it can feed *itself* answers yes right up until it doesn't, which is the fixed clock being replaced. **Anomaly materialization surfaced a shipped-but-unreachable content gap:** `army.threshold.mutiny` is THR-104 prose whose spawn map `THRESHOLD_ENCOUNTER_TEMPLATES` has zero non-test consumers, so four cohesion thresholds only ever produced a *notification* — starving armies now give one of them its first real trigger, alongside two new authored templates. **A fail-soft of mine hid the feature completely and the trace still read healthy** — `s.seedId.startsWith(...)` throws on the undefined ids the live seed pool genuinely holds (4 of 7 at tick 132), the per-army `catch` swallowed it, and `0 seeded` looked like equilibrium; found by predicting which scan ticks *should* fire and checking one, fixed with a guarded read, and pinned by a regression test falsified against the pre-fix build. The same unguarded read in `routeEvents.ts` throws outright wherever a trade web exists — filed as THR-992. (THR-834's entry archived to `project-history.md` to hold the 60-line cap.)

**Recently completed (2026-07-22, marathon):**
- **M3 Dynamic Economy nearly complete** — THR-669 (route events + the trade-route pipeline revived: three stacked engine bugs meant no route had ever survived), THR-670 (cargo route lines + tooltips on HexMapV2), THR-617 (P3 power couplings: systemic monopolies with live chronicle prose, sphere drift from cargo flows, faction gold term, scarcity arcs with intervention encounters). THR-618 partial: all five divine economic verbs now `[IMPL]` (reveal vein / guide caravan / sour mine shipped, bless/blight were live) and the Flow Web extraction checkpoint is decided (**defer** — `Docs/plans/2026-07-22-flow-web-extraction-checkpoint.md`); only the THR-611-coordinated essence bridge remains.
- **War project complete** — THR-628 (battle depth: prepared defense, tactical events, breach surfacing, battle IPK), THR-629 (siege gravity-well repair: dead capability gates fixed, relief_march + sabotage live), THR-630 (Phase D: notable-agenda roster + five families incl. the Campaign war hand-off that opens war to the nations, NotablesPanel, power-vacuum verification).
- **Scene Integration complete incl. follow-ups** — chain A–F shipped earlier; THR-712 (guild-tier sweep: cast figures, one genuine bond, 44 continuity seeds, permanent guild prose-QA gate) and THR-713 (381 invented-name literals across seven branching encounters retired to `{cast:*}`).
- **Cowork migration complete** — THR-677 ports live (daily grooming, workflow retro, project hygiene), THR-684 attachment-pipeline prompts written, THR-704 scheduler hygiene, monthly-rulebook-review registered.
- **Social Systems: secrets & favors activated** — THR-724 turned a fully-built, entirely silent subsystem on. Generation had a *dead read site* (not merely thin content), consequences had zero callers, the consequence module was a two-way fork, and both player verbs were unreachable. All four fixed; 0 → 18 secrets and 0 → 4 revelations per 120-tick run, and the three pre-seeded interface-registry rows are LIVE with run evidence.

## Milestone Status

Tracked in [Linear Projects](https://linear.app/threadbare/projects). Active: Thematic Pressure & Living World (war system now fully live), Content Architecture (scene integration complete). See Linear for per-project state — this file does not duplicate it.

## Where things live

- Shipped work: `Docs/project-history.md` (one-liners) + Linear Done
- Rationale: `Docs/changelog.md`
- Wiring: `Docs/plans/wiring-checklist.md` + `Docs/plans/2026-04-16-systemic-wiring-guide.md`
- Rules of play: `Docs/canon/rulebook.md` (+ quick-reference)
- Christian-facing: `Design/briefing.md` / `Design/user-actions.md` — **on the `ops` branch**, not `main` (THR-947): `git fetch origin ops --quiet && git show origin/ops:Design/briefing.md`

## Active Backlog Ideas

None parked here — file ideas as Linear issues (predicates, not counts; see `Docs/canon/process.md`).
