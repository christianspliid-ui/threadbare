# Project Status

> Updated 2026-07-30. Detailed per-ticket narratives that used to live here are archived: shipped-work one-liners in `Docs/project-history.md`, full rationale rows in `Docs/changelog.md`, and the wiring detail in `Docs/plans/wiring-checklist.md`. This file stays ≤60 lines by contract.

## Current Focus

**A gate that refused nearly everything it was shown (THR-836, 2026-07-31).** `pull-work` Step 3 demanded three coordination lines — `Suggested model`, `Parallel-safe with`, `Mutex with` — on every candidate's latest comment, with no exemption. Both authors of that block were handoff-path only: the design-session handoff (retired with THR-654) and the orchestrator's T1 **promotion**. The executor files its own deferrals *directly* into `Ready for Dev`, bypassing T1 — so **those issues were born failing the gate**, and it produced one of two outcomes on nearly every pickup: a lost run, or a hand-written reversal re-derived from scratch each time (THR-834/THR-817 zero-comment; THR-804 claimed past it by hand; THR-778 bounced at 05:03 and re-offered at 06:03). The fix separates the two things the gate was conflating. **A block is an artifact of coordination between two parties**; a ticket the lane filed for itself, naming the file it means to change, was never coordinated by anyone, so demanding it is a category error. Step 3 now classifies before deciding: a **self-scoped** ticket (description names a repo path or a backticked file/symbol) is *claimed*, with the executor deriving and posting the three lines under a labelled heading; only an **unscoped** one bounces. **A bounce now also routes** — comment, move to `Todo`, continue to the next candidate in the same run — because refusing without removing from the queue is a spin loop wearing a gate's clothes, which is exactly what re-offered THR-778 hourly until a human moved it by hand. The writer side was closed in the same pass so the derivation is a fallback rather than the norm: CLAUDE.md's Definition of Done now requires the block as the **first comment** on any issue filed straight into `Ready for Dev`, and the orchestrator's rule was widened from "every promotion" to promotions *and* direct filings (its two-write create path produced zero-comment issues with no later step that would ever author one). The rule is **machine-checked, not prose-only**: `classifyCoordinationBlockGap` in `scripts/coordination-block-predicate.ts` is the single predicate both `check:process` and Step 3 cite, reporting `warn` for self-scoped and `error` for unscoped, with 16 tests pinning **both** directions — a predicate that only ever answered "self-scoped" would make the gate vacuous while every test still passed.

**A branch point that could not branch (THR-894, 2026-07-31).** `ActionStepBranch` selects its variant by a recorded `choiceId`, and the only thing that ever recorded one was the retired player-pick (`authoredChoices`) — so a branch authored *today* could only ever resolve down `fallback`, on every seed, forever. The fork existed in the type and nowhere in the world. This generalizes the one shipped personality-driven selector, Meet The First (THR-868), so the **mortal** decides in-encounter forks from who they are, leanable by the god's hand. A branch declares `decidedBy: { axis }` with variants keyed exactly `positive`/`negative`; at the deciding step the engine reads the mortal's **live** axis position (standing profile *plus* accumulated drift), adds the net `poleLean` of the cards the god actually committed, and records the pole through the **existing** choice-history path — so `resolveStepDefinition` reads it as an ordinary choice and no second branch-resolution route exists to drift out of sync. **Generalizing by copying the meeting's summing loop was the obvious move and the wrong one** — that is the `assignAmbitionToActor` failure (THR-885), two implementations of one rule that diverge the first time either is tuned — so the arithmetic was extracted to `encounters/poleLean.ts` and the meeting now *calls* it, its 49 tests green untouched. **The loop closes on drift:** the decided pole writes through `applyDriftMagnitude`, the same accumulator `phaseChoiceResolution` uses, so decay, threshold crossings and the `archetype_drift_register` reveal all see it with no parallel store — a mortal who keeps taking the cunning fork measurably becomes likelier to take it again. **Two decisions are recorded so they are not re-litigated:** the seeded coin is drawn *only* inside the neutral band (drawing it unconditionally would desynchronise two runs differing solely in how convinced a mortal was), and the meta pair `courage_prudence` has no canonical `${reach}_axis` id, so it keys drift on its own pair name — canonical readers simply do not find it, which keeps a *designed* authoring axis decidable rather than an edge case that refuses. **A card's lean counts only on its own axis**, and a deciding step dealing no card on the branch's axis **warns** rather than fails: legal (the mortal decides alone), but far more often it means a card names the wrong axis and abstains silently — the THR-844 shape. Variant keys are validated at build time for the same reason. **Zero content ships here** (THR-883 owns the first choice-designed encounters), so the interface-map row is badged **LEAKED** with its remediation ticket rather than LIVE: a green badge has to mean traffic, not intent (THR-614). Non-vacuous by falsification — the 23-test suite was re-run with the resolution call removed and 9 went red, the 14 that stayed green being the pure-arithmetic and validator tests that correctly do not depend on the wiring.

**A panel that repeated the same three scene facts every run (THR-892, 2026-07-30).** Implements the variance rule Christian approved on the THR-883 mockup review: *a factor line earns its place only if it could have read differently on another run*. A static authored line reads identically every firing, so it cannot inform a decision — it is priced into `difficulty` and belongs in the prose. The panel now **derives** what varies: the actor's capability in the step's reach, plus one line per named world modifier (equipment, terrain, faction, sphere, conditions, divine attention, rule overrides). **The load-bearing constraint was "one read path, never a parallel computation"** — a panel re-deriving its own numbers eventually disagrees with the roll and quotes a factor the roll never applied. So `computeResolutionModifiers` now emits a **named** `contributions` list from the *same* walk that produces its totals, and the lines are a pure projection of it; the three `compute*` functions sum the list they collect, with caps applied by trimming the overflowing tail so the emitted values sum to exactly the previously-returned number (all 30 pre-existing modifier tests pass untouched, both cap tests included). **Omens, doom stage and season derive nothing, and that is a finding rather than a gap** — `forecastAction` reads only `ResolutionInput.actionModifiers`, and no omen/doom/season read exists anywhere in the pipeline that fills it, so a line for them would name a cause the roll never applied; recorded N/A with the read path cited rather than faked. **`carryoverFactorLines` is the one authored factor that survives the rule**, keyed on the band the *previous* step rolled: step 0 never draws one, an unauthored band draws nothing, and a declared delta rides the existing `carryover:<outcome>` channel to both the panel floor and live resolution with **zero** new rng. Sentence templates ship as half-sentences (`'{actor} carries {source}.'`) rather than label/value pairs, which makes canon rule 1 structurally unviolatable instead of merely documented. Every line class carries a **falsification twin** — remove the source, the line must vanish — and the two THR-820 tests asserting the panel is *exactly* the authored set were correctly broken and rescoped. The carryover-forecast test asserts `forecastInput.actionModifiers` rather than the probability, because the capability-0 fixture sits on the 0.05 floor where a +0.1 modifier is absorbed: the floor is real behaviour, it just cannot witness the wiring. Browser-verified at a genuine 1920×1080 (server identity proved by fetching the new module through Vite first): **"Vara is oblivious in eye."** renders live beside the exemplar's three authored lines, 0 console errors.

**The card row was quoting a price the commit path did not charge (THR-890, 2026-07-30).** The UI pillar of the locked card format — the shipped nudge test panel becomes the real card: picture band, keyword chip, title, cost pips, effect + odds, flavor quote, with the step's base forecast rendered in the same pip vocabulary so one odds language covers the whole surface. **The bug the rewrite surfaced is the one THR-885 had already built the fix for.** `buildNudgeHand` judged affordability on `effectiveNudgeCost` and `totalNudgeCost` deducted it, but the stage adapter built its card models from the raw authored `essenceCost` — so a signature card was shown dear and billed cheap, and a reopened committed hand reported a total nobody was charged. Both now read the shared helper, and the playable and dimmed passes were collapsed into one mapping function so they cannot drift on price, keyword or cost channels again. **The keyword is derived, never declared.** A card's type comes from `libraryCardId` → `NUDGE_CARD_LIBRARY`; adding a `typeId` to `StepNudge` would mint a second authority on what a card *is*, so a one-off authored option simply prints no chip. Icons key on the live `NudgeCardTypeId` union, making a new type without an icon a build failure rather than a blank chip. **The pip ladder is twenty ~5% steps in four tiers of five, with shape changing per tier** — shape is the accessibility channel and colour the secondary one, and each row carries its reading as an `aria-label` ("Strong, 3 of 5") since a screen reader spelling out geometric characters is worse than silence. Tiering is pure display over raw stored numbers. **The golden exemplar could not exercise its own surface and had to be extended:** authored before THR-887, none of its options carried a library id, so the keyword chip and the alternate-cost row were unreachable in any browser pass — it now carries three *universal-core* ids (a sphere-signature id would engage the repertoire gate and change which cards the hand deals) and two cost channels its band prose already implied. **The row scrolls sideways, not down** — wrapping was tried first and measured worse: the stage column fits four cards, so a five-card hand wrapped and the height cap clipped the second line mid-card.

**Reading card access off the type would have handed one sphere's signature to every god (THR-887, 2026-07-30).** Ticket C of the THR-883 split — the Repertoire: a card library keyed to who the god is, and the first live surface of sphere-governed identity. Types → families → members, where progression grants a new *member* of a family the god already plays (same verb, different twist), never a bigger number. **The design's "⁺" notation turned out to be load-bearing and nearly cost a bug.** `order` signs Insurance and `energy` signs Boost — types that are *also* universal core — so a type-level access check resolves them `full` for everybody, handing order's signature Insurance to all twelve spheres. Access is therefore read **per member**: a member declaring its own `sphere` is gated on that sphere, and only a sphere-less member falls back to its type. A test written against a chaos/life god caught it, along with a second real bug in the same run — hunger uniques were being filtered out by sphere before the unlock check ever saw them, when a hunger unique is *identity, not alignment* and must ignore sphere entirely, exactly as an echo card does. **The plan and wiki both said twelve hungers; `HungerId` has ten.** `haunt` and `illuminate` do not exist, and minting one means authoring a full `HungerDefinition` that worldgen and the remembrance flow then seed against — a design decision, not an implementation detail — so the map ships keyed `Record<HungerId, …>` over the live ten (adding a hunger now fails the build until its card is named) and **THR-891** decides whether to mint the two or correct the docs. **The echo card is not an optional field with no writer.** Its selector needed a source, and none existed: nothing tracked which cards a run played. So `StepNudge.libraryCardId` (the shared identity across templates that per-template `id`s cannot provide) plus a `cardPlayTally` written at nudge commit and reset at cycle transition — both sides land here. Selection is chosen, never rolled, and tie-breaks finally on card id so a saved run replays identically. **The interface-map rows were LEAKED on first generation and that was correct** — nothing in production called `buildRepertoire`. Rather than badge around it, the gate got wired: `repertoireCardIds` on `NudgeHandContext` withholds a card the god does not hold, fed from `ascendantIdentity` and deliberately left `undefined` when identity is absent, since defaulting a legacy run to "no spheres" would silently withhold every signature card — a behavior change dressed as a fallback. Library ships **structure without prose**: every member gated, priced and dealable, `title`/`quote` optional and absent until THR-883's format locks; an unauthored card renders as its own keyword.

**The golden exemplar now speaks the locked format, and the skills that author against it agree with themselves again (THR-883, 2026-07-30, in progress).** With THR-884/THR-885 shipped, the three remaining prototype deliverables landed: **The Swollen Ford** replaces the Darkhollow Vault as the golden exemplar — authored end-to-end in the locked format (a `rural`+`wayside` envelope with per-class openings, two six-card hands cut from the 21-type library with generic faces and mechanism-stating effect lines, a doom-priced Bargain, a detection-priced Heavy Hand, one rider per hand, two liveness-pinned grants, a hidden trait card and a hidden group card), with the executable checklist extended to assert envelope validity, cost-channel pricing, and grant liveness against it. The **authoring contract** (`nudge-authoring-spec.md` + both pipeline skills) was rewritten to the pivot — and the deep finding was that the encounter-pipeline **agent prompts still mandated the rejected `authoredChoices` approach-card model** three days after their own SKILL.md went nudge-native; the subagent reads the prompt, not the warning (impediment #325, THR-888's class). **THR-890** filed Ready for Dev with the full card-row UI spec (anatomy, pip vocabulary, hidden/dimmed rules, exemplar-driven browser Done-whens). Remaining under THR-883: retrofit of the 7 old conversions (now cheap — re-cut hands from the library), then the corpus.

**Six skills were instructing agents to call a tool that does not exist (THR-795, 2026-07-30).** A freshness sweep over `last_validated_against`, and the headline finding is not staleness in the abstract — it is that `vault-log`, `vault-ingest`, `vault-enrich`, `vault-lint`, `vault-query` and `gamedocumenter` all still taught the **Obsidian MCP** path that THR-654 closed on 2026-07-21. `.mcp.json` configures `codesight` and nothing else, so every `obsidian_get_file_contents` / `obsidian_append_content` call those skills prescribe targets an absent tool. `vault-log` led with "Step 1: Try Obsidian MCP" and `gamedocumenter` asserted the exact inversion of current truth — *"the vault is served via MCP, NOT the filesystem. You CANNOT use `Read`/`Edit`/`Write` on vault files"*. That MCP-first ordering is the same one that silently dropped ~12 `log.md` appends over 8 days (impediments #66/#71/#75/#86), so the skills were not merely out of date, they were routing agents back into a closed failure. All six now read filesystem-first against `OBSIDIAN_VAULT_PATH` with fail-loud-if-unset. **The second class was gate theater**: `testing-patterns`, `qa-orchestrator` and `blender-to-hexmap` each listed `npx tsc --noEmit` as their type check — a no-op here (root `tsconfig.json` sets `files: []`, exits 0 unconditionally, THR-686) — now `npm run check:typecheck`, the THR-693 ratchet. **The ticket's own framing did not survive contact.** Its "19 skills" was 21 at the stated date and 23 by the ~8-week predicate (THR-688 rule A: work the predicate, not the snapshot), and its hopeful "mitigating factor — these probably defer to a current Canon page" held for **4 of 23**. Findings were mechanical, not impressionistic: a path-existence sweep, an npm-script sweep (all clean), and a version check against `package.json` (`hexmap-core` accurate on all seven). Two false positives were run down rather than reported — `image-manipulation`'s scripts are **skill-local** and its documented API matches exactly, and `attachment-pipeline`'s `.agents/` mention is a correct historical note. Real content fixes: `content-worldbuilding` claimed 244 nodes/18 categories where `world-model.json`'s own `meta` reads **253/19** (now with the re-derive command inline so it cannot rot again), `ubiquitous-language` omitted the **Traits** shard from both its load table and proposal template, `hexmap-layers` still told readers bare `?view=game` reaches Meet-The-First (THR-874 established it cannot), and three skills named retired `preview_*` MCP tools. Split out: **THR-888** (encounter-pipeline's agent prompt still instructs the no-op, contradicting its own SKILL.md — the subagent reads the prompt, not the warning), **THR-889** (CLAUDE.md's browser-verify trigger names `src/views/` and `src/styles/`, neither of which exists).

**Five of six card dispatch hooks needed no new machinery — the sixth was missing because its write was duplicated (THR-885, 2026-07-30).** Ticket B of the THR-883 authoring-framework split, and the finding is an architectural one. A nudge card's world changes are typed as `EncounterAftermathReactionEffect[]` and applied by the existing `applyEncounterAftermathReaction`, so The Omen, The Balm, The Cache, The Long Game and The Favor reach their owning systems through the doors `emit_omen` / `remove_condition` / `spawn_artifact` / `hidden_mark` / `favor_creation` already use. Sharing the vocabulary *is* the design: a card that plants an omen must not mint a second path to the omen system, which is the failure the systems inventory exists to prevent. **The two host-side additions each closed a real gap rather than adding a card-specific path.** `assign_ambition` exists because reactive ambition templates had **no assignment path at all** outside `ambitionTick` (THR-812 / THR-726) — the find-or-create-node-then-write-`pursues`-edge sequence had been copied three times *inside* tick phases, and that duplication is precisely why nothing outside them could assign an ambition; extracting `assignAmbitionToActor` is what makes the new effect a reuse rather than a fourth copy. `applyRawDetectionDelta` exists because every prior detection writer priced by `EncounterChoiceCost` band, and a band can only **raise** pressure, so The Veil (the same help given unwitnessed) had no channel to write through; the signed entry point lives in the module that owns detection pressure and shares its clamp. **The Gambit sits last in the rider priority on purpose** — it is the only rider that can worsen an outcome, so a hand holding both it and a protective card keeps the protection. **The sphere discount is quoted and charged by one function** (`effectiveNudgeCost`, shared by the affordability check and the deduction), because showing a card as affordable and then deducting the authored price is the bug a discount feature otherwise ships with; an authored-free card stays free, since free is a decision and not a number a discount arrives at. **The liveness gate is deliberately self-falsifying:** the shipped corpus authors zero card grants (content is paused behind THR-883), so a sweep over it would print PASS while checking nothing — the test re-injects a dead reference and asserts the sweep catches it. That matters because the failure it guards is silent by construction: a dead ref no-ops inside the applier while the card's fiction still prints, telling the player something happened that did not (THR-844's 66-of-138 dead reveal families). All three interface-map rows ship **deliberately not badged LIVE**, carrying `deferralTicket: THR-883` — a green badge has to mean traffic, not intent (THR-614). **THR-886** filed for The Compulsion, the one hook not shipped: its host API needs the decision pipeline's `ScoredCandidate[]`, which aftermath cannot obtain, and synthesizing them would have been exactly the parallel-path failure the plan forbids — so it stopped rather than fake it, and the three candidate fixes differ in what the card *means*, making it a design call.

**Authors were writing the 20-subtype list by hand, so prose went placeless (THR-884, 2026-07-30).** Ticket A of the THR-883 authoring-framework split. The engine has always gated templates per location — `locationSubtypes`, enforced by `encounterCache` — so nothing was broken in the engine; what failed was **practice**, stamping placeless prose with all 20 subtypes, which scene-built prose turns into a lie (a hamlet rest scene cannot happen in a capital). Authors now declare a **setting envelope** from a closed 8-class vocabulary and one table expands it; the cache filter is untouched. **The constraint that shaped the design was the plan's "do not build a second resolution mechanism":** `setting` joined `place`/`counterpartRole` as a third THR-573 identity axis, and `resolveSettingVariant` builds the same `ContextFragmentSet` shape (base text as the `'*'` default) and delegates to `resolveFragment` — so a per-class opening and a per-card `fictionBySetting` share one lookup chain and cannot drift on what "no entry for this class" means. **The vocabulary deliberately covers 20 subtypes, not all ~50.** `LocationSubtype` carries ~30 more worldgen overlay kinds (sphere wonders, lairs, anomalies) that no encounter targets; folding them into classes would have invented coverage — the THR-844 dead-vocabulary failure in a new place — so they stay unclassed and take the default opening rather than a blank. **The corpus sweep is vacuous today, and the tests say so out loud.** Content is paused behind THR-883, so no shipped template declares `settings` or `openings`; a corpus-only completeness test would have printed PASS while checking nothing. The rule lives in an exported `validateSettingEnvelope` with five falsification cases (typo'd class, declared-class-without-opening, opening-for-undeclared-class, registers-nowhere, opts-out-entirely) that prove each guard fires **now**, leaving the sweep as a live regression guard for the moment content lands. All three read sites resolve the location through the cache's own exported `locationTypeFromProperties`, so the class the prose binds to is the class the template was filtered in under. The coverage matrix ships **advisory floors, unset** by design — a thin cell is a scene not yet written, not a build failure — and reports 440 drawable templates, no cell ≤2, 22 templates drawable only at unclassed subtypes, and one family at 71% single-kind cards.

**A roadmap that told the substrate check to green-field already-shipped code (THR-763, 2026-07-30).** `.planning/ROADMAP.md` is declared in CLAUDE.md as still maintained, so an agent running the mandatory **Step 0.6 substrate-existence check** may trust it — and it said `resource_delta`, `action_trigger` and `choice_set` were "still pending". All three are shipped, and `action_trigger` had just been *extended* by THR-719, so a plan built on that line would have collided with freshly merged code. This is the THR-614 failure mode with the trap pre-laid in a doc. **Re-derived by grep rather than from the ticket's own table (THR-688 rule A), which had itself aged** — the line numbers it cited for `src/types/effects.ts` had all moved. Verified truth: **all six** Phase 1 primitives are declared and dispatched, and **Phase 2 is shipped too** — `flip_table`/`clearance_gate` run through a dedicated tick phase (`phaseEffectShells.ts`) with duplicate-gain policy and result-band support in `effect-constants.ts`. That settles the grooming comment's open half: Phase 2's missing Linear ticket was **not** a gap to fill but shipped work the roadmap never caught up to, so it is struck as delivered rather than ticketed — filing from a source already classified as stale is how you manufacture a duplicate of shipped work. `task_progress` genuinely returns **0 hits** (Phase 3 accurate, THR-54). **The obvious "close the class" fix was checked and rejected on evidence:** the ticket floated replacing the list with a pointer to the generated `systems-inventory.md`, but that file registers engine *subsystems and tick phases*, not effect primitives — it carries 1 incidental hit across all six — so the pointer would have been false. Instead the section gained a dated freshness caveat naming the real ground truth (`effects.ts` + the two dispatch sites). **Phase 22 reconciled honestly, not optimistically:** the Code Hygiene project is Done (2026-04-23), but both its tickets closed as **Duplicate** and `DebugPanel.tsx` is still 244 lines with no sub-component split — so the row reads "Closed (superseded)" with the unmet criterion stated, rather than a "Complete" that would relaunder the same trap.

**The dev URL that documented itself as the route to a beat it could never reach (THR-874, 2026-07-30).** Every browser Done-when on the Meet-The-First surface was unrunnable, and CLAUDE.md's URL table pointed at the URL *least* able to run it: bare `?view=game` was documented as "use when testing the MeetTheFirst flow itself", but it supplies **no ascendant identity**, and `GameView` mounts the flow only on `meetingState && ascendantIdentity` — so the beat could never render there. `?view=game&seeded` supplies identity but also pre-bonds The First, making `isMeetTheFirstAvailable` false. The two dev seeding actions shared **one** boolean (`preSeeded` → `devSeedTheFirst` + `devSeedAscendantTestPackage`), so no combination of flags could ask for identity-without-First; split into `seedFirst` / `seedTestPackage` and added `?firstunmet`. **The first fix was necessary and not sufficient, which is the finding.** With The First unbonded the beat became *available* — the "The First awaits" affordance rendered and `threadCount` was 0 — but the auto-trigger still never fired, because it separately gates on the avatar standing at a settlement and the ascendant starts at `loc.start`, a **`shrine`**, which is deliberately not in the settled allowlist. Loosening that production gate to suit a dev URL would have changed live behavior for every game (the allowlist exists because the dilemmas describe merchants, guards and councils), so the dev entry **relocates** instead: `devPlaceAvatarAtSettlement` is dev-only, fail-soft, and returns `null` rather than throwing when no settlement or avatar exists. The allowlist itself was **duplicated inline** in the trigger and would have been duplicated again in the seeder — exported as `MEETING_SETTLED_LOCATION_SUBTYPES` and consumed by both, since a drift between those two copies is precisely what makes the beat silently unreachable from the one URL built to reach it. Browser-verified at a genuine 1920×1080: `?view=game&firstunmet&size=medium` + `__DEBUG.tick(6)` → `getMeetingState()` returns `beat: 'sensing'` (it returned `null` on both old URLs), 0 console errors, no horizontal overflow. THR-868's plan doc and the CLAUDE.md table are corrected in the same PR — the stale line is the reason the gap shipped.

**Two surfaces disagreeing because neither said which axis it was showing (THR-869, 2026-07-30).** Christian read the sheet's "Primary Matter / Secondary Mind" against the bar's "Primary Stone / Secondary Gold / Secondary Iron" as a contradiction. It was not one — those are the two orthogonal axes, **spheres** and **reaches**, and neither surface named itself. **Three separate divergences stacked into one apparent contradiction**, which is why labelling alone would not have fixed it: the sheet showed spheres unlabelled, listed all 8 reaches in fixed catalog order (the god's primary buried 7th, no rank marking), and fed them from **frozen day-one `startingDomainAffinities`** through the **mortal** tier ladder, while the bar showed live ranked progression. The sheet's Dominion section now reads the same `selectReachRows` the bar does — one live read, so the two can never disagree again — and both surfaces gained an explicit axis caption. **The register is the part that is content, not plumbing.** `ASCENDANT_REACH_REGISTER` gives all 8 reaches a 5-word god-scale ladder plus a mortal-past echo line, because the mortal lexicon was dressing a god in words like `Trained`, `Rootless` and `Destitute`; its test pins the full 8×5 set with `toEqual` rather than a count, and asserts no register word collides with the mortal ladder — **with a non-vacuous guard proving the mortal set really does contain the named failure cases**, so the check cannot pass by comparing against an empty set. `computeTier` returns 1–10 against a 5-band register, so the two-tiers-per-band mapping is a named constant with clamping and non-finite fallback rather than an inline expression. Browser-verified at 1920×1080: sheet and bar render identical reach ids *and* identical tier words, and `__DEBUG.getAscendantProgression()` matches both. **Ships none of the parked sphere-governance pivot (THR-870)** — reaches remain the gating axis today; this is presentational only.

**A testing lever for judging WS5 by playing, not hunting for tug badges (THR-878, 2026-07-30).** Christian couldn't tell whether the Nudge Model content migration was improving the game because most encounters resolve at background/shaping attention tier — silent or a small badge — so new content was effectively invisible in normal play. `?forceencounters` (URL flag, works on the deployed build too) makes every threaded agent's encounter render and pop up as if they were The First: full prose, full 3-choice set, immediate interrupt. Dormant/unthreaded agents stay invisible — this widens what a threaded agent shows, it doesn't surface the whole world. **THR-879** filed separately (Deferral) for the bigger, permanent ask — collapsing the four-tier watched/retinue/the_first/dormant model to a binary threaded=full/unthreaded=invisible — since that touches shipped UI (ThreadTugBadge, RetinuePanel) and the attention-story-reference wiki page, and should wait for feedback from using this flag first.

**SHIPPED — the first encounter no longer teaches the rejected model (THR-868 / WS6, 2026-07-30).** Meet The First is every player's first interactive surface, and its Testing beat used to let the player *pick which of two authored formative moments is true* — the exact "choosing between authored futures" interaction the nudge program rejected. It now runs **three fate rolls**: two formative tests and the bond as climax. Engine (`resolveFormativeTest` / `resolveBondTest` / `applyMeetingOutcomes`, reusing the real attended ladder — no parallel resolver), UI (the WS2 `NudgePhaseShell` consumed **whole**, not forked, inside the existing cinematic wrapper), and the universal bond test all landed across three passes. **The plan was wrong about the substrate in two directions that both type-check** (#311): its band tables were keyed on `ForecastTier` values — the *pre-roll* words — not the six-value `StepOutcome` a hand resolves onto, so a five-key table would have silently written a zero shift for the sixth band; and its claim that trait seeds "already flow through the existing path" was false, since `createAgentFromMeeting` never reads that field (**THR-872**). **Browser verification earned its keep twice over**, catching two defects 31 green engine tests passed over: `MEETING_TEST_CAPABILITY = 0.5` against difficulty 0.5 forecasts **p=0.09, `doomed`** — the sigmoid is not centred at equal capability/difficulty, so every test opened unwinnable (retuned to 0.8; pinned by *shape* tests, not the constant — #313); and `NudgePhaseShell` renders card strings verbatim, so a literal `{agent.name}` reached the player on every card. **This pass closed the ticket with the register rewrite and the scene-art audit, and the audit is the finding worth carrying.** The prose corpus — 24 sensing vignettes and both hunger-keyed god-voice tables — moved off the lyrical register onto plain/descriptive, measured rather than asserted: vagueness hits **14 → 0**, register failures **1 → 0**, scored with the real `scoreProseEntry` (the detector list is wider than the spec doc — `someone`, `very`, `the tension` all count, which is why the first pass still tripped 14). Then the audit of all 32 scene files found **three defect classes, two of them hard doctrine violations**: `prison-cell.jpg` has the retired choice mechanic *painted into it* as two UI buttons — "GRANT MERCY" / "IMPOSE JUDGMENT" — so the art kept teaching the model the code had stopped teaching; and `plague-ward.jpg` renders an individuated child's face against ruling 10's one-likeness rule. **plague-ward was the reachable one** — any dilemma tagged `sacrifice` or `compassion` scored it top and rendered it. Two more carry baked-in caption text. Quarantined 5, re-pointed to already-compliant assets sitting unregistered on disk, widened the pool **8 → 16** (24 of 32 were unregistered, so thin-tag draws cycled two backdrops). Both fixes are pinned by executable invariants **with negative controls**, and when the defect was re-injected to falsify the doctrine guard it independently reproduced the original reachability finding. Rulebook §7 gained its first **Meet The First** section — the run's guaranteed opening encounter had no rules-of-play entry at all — plus a quick-reference block; the taste-profile's "meeting prose is the quality bar" entry was corrected, since it pointed at the register just retired. **Remaining, split out and not blocking: 63 of 64 Batch A templates (THR-875, predicate measured), replacement art for the 5 quarantined assets (THR-876), the dev-URL gap that makes the meeting unreachable in the browser (THR-874, #316), UL terms (THR-873), and the discarded trait-seed path (THR-872).**

The hourly `tb-opus-pickup` lane and `keep-work-flowing-cc` briefing are live; new work flows through Linear (Threadbare team).

**Recently completed (2026-07-22, marathon):**
- **M3 Dynamic Economy nearly complete** — THR-669 (route events + the trade-route pipeline revived: three stacked engine bugs meant no route had ever survived), THR-670 (cargo route lines + tooltips on HexMapV2), THR-617 (P3 power couplings: systemic monopolies with live chronicle prose, sphere drift from cargo flows, faction gold term, scarcity arcs with intervention encounters). THR-618 partial: all five divine economic verbs now `[IMPL]` (reveal vein / guide caravan / sour mine shipped, bless/blight were live) and the Flow Web extraction checkpoint is decided (**defer** — `Docs/plans/2026-07-22-flow-web-extraction-checkpoint.md`); only the THR-611-coordinated essence bridge remains.
- **War project complete** — THR-628 (battle depth: prepared defense, tactical events, breach surfacing, battle IPK), THR-629 (siege gravity-well repair: dead capability gates fixed, relief_march + sabotage live), THR-630 (Phase D: notable-agenda roster + five families incl. the Campaign war hand-off that opens war to the nations, NotablesPanel, power-vacuum verification).
- **Scene Integration complete incl. follow-ups** — chain A–F shipped earlier; THR-712 (guild-tier sweep: cast figures, one genuine bond, 44 continuity seeds, permanent guild prose-QA gate) and THR-713 (381 invented-name literals across seven branching encounters retired to `{cast:*}`).
- **Cowork migration complete** — THR-677 ports live (daily grooming, workflow retro, project hygiene), THR-684 attachment-pipeline prompts written, THR-704 scheduler hygiene, monthly-rulebook-review registered.
- **Social Systems: secrets & favors activated** — THR-724 turned a fully-built, entirely silent subsystem on. Generation had a *dead read site* (not merely thin content), consequences had zero callers, the consequence module was a two-way fork, and both player verbs were unreachable. All four fixed; 0 → 18 secrets and 0 → 4 revelations per 120-tick run, and the three pre-seeded interface-registry rows are LIVE with run evidence.

## Milestone Status

Tracked in [Linear Projects](https://linear.app/threadbare/projects). Active: Thematic Pressure & Living World (war system now fully live), Content Architecture (scene integration complete). See Linear for per-project state — this file does not duplicate it.

## Where things live

- Shipped work: `Docs/project-history.md` (one-liners) + Linear Done
- Rationale: `Docs/changelog.md`
- Wiring: `Docs/plans/wiring-checklist.md` + `Docs/plans/2026-04-16-systemic-wiring-guide.md`
- Rules of play: `Docs/canon/rulebook.md` (+ quick-reference)
- Christian-facing: `Design/briefing.md` / `Design/user-actions.md` (hourly refresh)

## Active Backlog Ideas

None parked here — file ideas as Linear issues (predicates, not counts; see `Docs/canon/process.md`).
