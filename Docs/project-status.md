# Project Status

> Updated 2026-08-02. Detailed per-ticket narratives that used to live here are archived: shipped-work one-liners in `Docs/project-history.md`, full rationale rows in `Docs/changelog.md`, and the wiring detail in `Docs/plans/wiring-checklist.md`. This file stays ≤60 lines by contract.

## Current Focus

**Strict mode is dead, no merge queue is coming, and four surfaces still said otherwise (THR-983, 2026-08-02).** THR-946 pursued GitHub's merge queue as the structural fix for the `BEHIND` livelock — an armed green PR knocked stale by someone else's merge that could never merge itself. The queue turned out to be **unavailable on a personal-account repo**, so THR-735's fallback was taken instead: the classic branch-protection rule on `main` was **deleted**, leaving ruleset `15479914` as the sole protection surface with `strict: false` and two required checks. A green PR now merges regardless of tip movement. **The premise was verified against the live API rather than taken from the ticket** — `.../branches/main/protection` returns `404 Branch not protected`, which is now the *healthy* reading and the kind of signal an auditor would otherwise report as protection being off. The sweep corrected CLAUDE.md (CI note, merge bullet, ops-branch rationale), `pull-work` Step 0.8 and its closeout, the task registry, and two scheduled-task prompts with their live copies. **The judgment call was the docs-drain merge-yield gate, and it was retired rather than repaired.** Its sole rationale was the ~18-minute re-stale cost of advancing `main`'s tip; that cost is now zero, so the gate guarded nothing. Deleting it also removes at the source a contradiction three consecutive sessions each re-derived by hand (impediments #393, #406, #411): the gate's prose named PRs *"armed and waiting on checks"* while its mechanism classified **every** open PR, so one permanently-conflicted or deliberately-held code PR suppressed the docs fast-track **forever**. THR-953 asked for the durable fix; removal is it. (THR-902's entry moved to `project-history.md` to hold the 60-line cap.)

**The merge-health probe answered "no PRs are waiting to merge" while the repo's only open PR had been unmergeable for 77 hours (THR-930, 2026-08-02).** THR-897 fixed how PRs *inside* the armed set are classified and left the set itself as the filter — `listArmedPrs` dropped everything with `autoMergeRequest === null` — so an unarmed conflicted PR was not misclassified, it was **absent from the input**. The probe was correct against its own docstring and wrong as a health signal, which is the worst shape this class takes: nothing to notice. Caught by reproduction, not by reading — this run's own Step 0.8 reported `counts.conflicted: 0` and *"No PRs are waiting to merge."* seconds before `gh pr list` returned exactly one open PR, #1114, `DIRTY` and unarmed since 2026-07-30 while THR-860 held an In-Dev slot the whole time. Membership is now **open and non-draft**; `armed` moves from gating entry to a per-PR field. **Drafts stay excluded on a principle, not for tidiness** — a draft is the single case where "not trying to merge yet" is the author's explicit signal rather than something inferred from a missing flag. **Two consequences had to be handled or the widening would trade one wrong answer for another.** `updateCandidate` stays armed-only, because `gh pr update-branch` on an unarmed PR refreshes a branch nothing is waiting to merge — a no-op dressed as progress; an unarmed `BEHIND` PR is now reported *and* explicitly not proposed, with its own summary wording so the sweep never prints `oldest first (#null)`. And the staleness clock had to change source: an unarmed PR has no `enabledAt`, and the old `|| Date.now()` fallback would have read every one of them as **zero minutes old on every run** — a conflicted PR reporting as brand new forever, so the field is renamed `clockStartMs` to make the two events non-interchangeable at the type level. Unarmed conflicts run deliberately slower tiers (6h escalate / 24h abandoned, against 90m / 12h) because absent arming justifies patience, not silence — #1114 clears the slower bar by 53 hours. Verified against the live PR: `verdict: "abandoned"`, `needsChristian: true`, `conflictFiles: ["Docs/project-status.md"]`. (THR-947's entry moved to `project-history.md` to hold the 60-line cap.)

**A scheduled lane failed 88 times running and the only reason anyone found out was an agent going looking (THR-834, 2026-08-02).** `Stale Claim Sweep` was red **88 out of 88 runs** across six weeks, twice daily, deterministically — and `Weekly Drift Scan` was the same shape for four consecutive weeks (THR-683), starving the retro's first input while every surface reported healthy. Nothing read either, because **no lane read Actions run history at all**: GitHub emails the *actor* on a failed scheduled run and a scheduled run has no interactive actor, so the mail goes nowhere; `Test · Typecheck · Build` is watched only because it gates merges. This is the exact mirror of THR-755's vacuous-green class — a gate whose **red** goes unread for 88 runs is as decorative as one whose green means nothing. `scripts/check-workflow-health.ts` is the missing reader, wired into `keep-work-flowing-cc` step 2.6. **Membership is derived from `.github/workflows/` on every run**, never a hardcoded list, because a list would rot the first time a lane is added and rot *silently*, which is the failure being fixed. **The three-way split is the whole subtlety, not a nicety:** `all-red`, `never-run` and `disabled` must stay apart, since a lane with no runs yet is not a defect and GitHub auto-disables schedules on repos idle 60 days — collapsed into green/red that reads as a silent all-red and teaches the reader to skip the section. `disabled` outranks run history for the same reason: a lane GitHub has stopped running may still carry five green runs, and reading those as health hides a dead lane behind its own good past. **Only `event=schedule` runs are judged** — a manual `workflow_dispatch` green proves the code works, not that the schedule fires, and counting it would let one debugging dispatch mask an unbroken run of scheduled reds. The window is a **run count, not a duration**, because 48 hours is six slots for a twice-daily lane and zero for a weekly one. Verified against real history rather than fixtures via `--before`: both the 88/88 case and the drift-scan streak fire `all-red`/`needsChristian`, today reads healthy, `--strict` exits 1 and 0 respectively. (THR-942's entry moved to `project-history.md` to hold the 60-line cap.)

**The lint asserted a pass for coverage it did not have, and the check it skipped was the one guarding the queue (THR-828, 2026-08-02).** `check:process` gates three of its most valuable assertions — recent plan references, orphan issues, Ready-for-Dev handoff keywords — on `LINEAR_API_KEY`, which a scheduled lane legitimately runs without. It recorded that as one warning and then printed `check:process passed with 1 warning(s).` A reader who greps the pass string gets a true match and a false impression; this is worse than an unread advisory gate, because the output actively claims coverage it never had. **The fix is deliberately not to hard-fail without credentials** — that breaks the scheduled lane for no correctness gain — but that *"passed" must not describe a run whose checks were skipped*. A skipped sub-check now registers in its own list, forces a `passed-with-gaps` verdict, and is named with its reason in the summary line; exit codes are untouched, so `passed-with-gaps` still exits 0. **Skips are tracked separately from findings on purpose:** a skip is not a finding, it is the *absence* of one, and conflating the two is precisely how three un-executed assertions were reported as a pass. **Four skip sites, not one** — the unset key, each of the two Linear fetches failing independently, and the `npm_execpath`-gated plan-doc lint — so the summary tracks real coverage rather than a constant, proven by the delta across three live runs (4 skips bare, 4 with distinct fetch-failure reasons on a bogus key, 3 under npm where the plan-doc lint actually ran). **A second-order defect fell out of the same read and is fixed here:** the plan-reference fallback reported a hard **error** when it found no match, but a failed fetch leaves the node list empty and is indistinguishable from "searched and found nothing" — so a network blip manufactured an error against every plan file. It is now gated on the fetch having actually returned. Verdict logic lives in its own module because `check-process.ts` runs `void main()` at import, the same reason `coordination-block-predicate.ts` sits beside it. The covered-Linear arm is pinned by unit test rather than a live run: no `LINEAR_API_KEY` exists in this sandbox. The ticket's adjacent note (two permanently-unclearable `check:canon-staleness` warnings) is deliberately **not** folded in. (THR-939's entry moved to `project-history.md` to hold the 60-line cap.)

**The weekly scan deduped on a title that carries the run date, so it could never recognise last week's issue (THR-756, 2026-08-02).** The drift scan files one Linear issue per red signal and guarded against duplicates with `findIssueByExactTitle` — but the title is `Drift scan [<runDate>]: <signal> — <summary>`, so the date makes every comparison false even when the drift is *identical*. Five pairs were filed that way across the 07-22 and 07-24 runs alone (THR-706↔THR-746, THR-707↔THR-747, THR-708↔THR-748, THR-709↔THR-749, THR-710↔THR-750), each doubling the open count for one underlying signal and each needing a groomer to hand-dedupe. Dedup is now **signal-scoped**: one query fetches the open `drift-scan` issues, the signal name is recovered from the title by a position-anchored parse, and a match is refreshed in place — title and description updated to the run's data, plus a comment recording the previous title so week-over-week movement in the counts survives the overwrite (NFP #2). **The matcher is position-anchored rather than a substring test on purpose**, because the Linear query filters on the label alone: a `coupling creep` issue whose *summary* mentions UL drift, or a human's ticket named "Rework the UL drift signal thresholds", would both be adopted and then overwritten by a naive `includes`, and that case is what the new tests actually discriminate. **"Open" is asserted three ways** — state type plus null `completedAt` *and* null `canceledAt` — because the five duplicates sit in Linear's **Duplicate** state, which surfaces as its own status while setting `canceledAt`; testing the timestamps makes exclusion independent of how any terminal state happens to be typed, and a closed duplicate must never become a refresh target. **The one surface no test reaches is the GraphQL filter shape** (`LINEAR_API_KEY` is unset in the sandbox, so the query is unvalidated against the live schema) — which is why the lookup is wrapped: a rejected filter logs one warning and falls back to the old exact-title path, so the worst case of an unvalidated query is the *status quo ante* rather than a dead scan. The second Done-when needed no work: the five 07-22 duplicates were already closed with `duplicateOf` relations by the 2026-07-25 grooming run. First live proof accrues on the next Friday scan. (THR-735's entry moved to `project-history.md` to hold the 60-line cap.)

**Five director notes on the slice, and the one that looked cosmetic was hiding a determinism bug (THR-972, 2026-08-02).** Christian's chat review of the test panel and nudge cards, four directives quoted verbatim into the ticket. **The motive moved from a chip below the prose to the scene's opening line above it** — it had been answering "why is this mortal here" *after* the player finished reading the scene it was meant to frame. Twelve variants across all four `MotiveSource` values (the fourth, `choice`, is new authorship; the director's own examples survive as the first variant of the other three, grammar-polished only), selected by a stable hash of action id + step index. **That selector is where the real find was.** The obvious helper, `hashEntityId`, is djb2 — multiplier 33 — and **33 ≡ 0 (mod 3)**, so every positional term above the last character vanishes modulo a 3-long pool: 24 distinct action ids all selected the same line. The variant-spread test caught it (a determinism test alone passes happily on a constant, which is why the suite asserts *spread* as well as *stability*); replaced with FNV-1a, whose multiplier is coprime to 3 and 11. **Directive 2** swaps the tiered reach PNG *and* its text label for the shared icon set's `ReachIcon`, the reach name surviving as the chip's accessible name plus the existing `reach.*` tooltip chain; `reachIconUrl` had exactly one reader and is removed rather than left dead. **Directive 4** binds a scales glyph and the difficulty word inside one frame, so `FAIR` reads as the bar being cleared rather than as an adjective on the scene. **Directive 5 is the one carrying a recommendation the director may veto:** a card's essence *price* and its odds *contribution* both rendered as small rows of repeated glyphs — two vocabularies meaning opposite things, separated only by glyph shape at 12–13px. Split rather than merely enlarged: price becomes a framed gold token, bare pip rows now mean *effect on the odds* everywhere, and a three-entry legend names all three vocabularies where the hand begins. **Directive 1 shipped its code half and filed the rest honestly (THR-981):** the header portrait lookup was archetype-only and is now the bespoke→archetype superset the resolver itself uses, but the slot still shows a letter tile on the review route because `?spawn=` stages on **the ascendant's avatar**, which `__DEBUG.resolveEntityVisual` confirms has no art at all — a content gap no render fix can close.

**The ending never said what it planted, and the screen that was supposed to say it could not render at all (THR-971, 2026-08-02).** Christian, slice review: *"all the stuff in the original prototype we made is not there."* Two defects, one visible and one not. The visible one: the aftermath drew generic actor-moments/highlights/changes inherited from the pre-nudge modal, with no chip taxonomy — so the slice's real `encounter_seed` plantings (the crossroads plants The Full Moon Collection ~132 ticks out) were invisible, which is THR-907 verdict 3 failing at the UI layer. Shipped `buildAftermathConsequences.ts`, a pure mapping from the *existing* vocabulary onto the mockup's five chips — prize/standing/toll/wound/seed — plus a `mark` fail-soft bucket so an unrecognised kind renders rather than vanishing; seeds are read from the resolved variant's reaction effects, so authors never state a consequence twice. **The invisible one was found by trying to verify the visible one:** `EncounterVeil` memoised `narratableProse` *after* its aftermath early return, so the moment a mounted veil crossed into aftermath mode React saw one fewer hook and threw `Rendered fewer hooks than expected`, sending the entire ending to the error boundary. That is the natural play path, it predates this ticket (identical structure at HEAD; this diff adds zero hooks), and it means the surface being redesigned could not render at all. Hoisted above every early return, pinned by a transition regression test. **Two deviations from the ticket's stated sources, both forced and both documented:** `toll` cannot read `consequence.quintessenceEvent` (it lives on `OutcomeConsequence`, is consumed per-step by the pending queue, and is never persisted onto `UnifiedAction` — THR-978), so it reads authored losses instead; and the approved mockup the ticket measures against does not exist on any branch (THR-980). **Filed rather than folded in: THR-979** — driving the crossroads live, the aftermath resolved to `fallback` both times, so the authored variants may be unreachable in play and the Full Moon seed never planted; the chip surface correctly showed no seed chip for a plants-nothing ending, which is the falsification case working.

**A field added "for the pip row" had no renderer, and the seam was invisible because both sides shipped green (THR-970, 2026-08-02).** Christian, slice review: *"the icons designating the modifiers magnitude are not here as agreed."* `EncounterStageFactorLineModel.delta` was added by THR-892 with a doc comment naming its purpose — signed, 0–1, "for the pip row", absent meaning draw nothing — and THR-890 rendered pips on cards and on the forecast but never on factor lines. Neither ticket was wrong on its own terms; the handoff sentence ("the model carries the number, THR-890 renders it") was the whole specification, and nothing tested the join, so both closed green with the feature unbuilt. Fix is the missing render in `NudgePhaseShell`: an `OddsPips` row beside each factor line that carries a delta, at `FACTOR_PIP_SIZE`/`FACTOR_PIP_GAP` so re-proportioning stays a number change (NFP #1). **The guard asserts both arms of the contract in one pass** — a test proving only that pips *appear* cannot distinguish "renders the delta" from "draws a row on every line", which is precisely what the contract forbids; a third case pins *which* lines populate each arm so a future adapter change dropping every delta cannot leave both arms vacuously passing. Verified live at 1920×1080 on the ticket's own `?spawn=encounter.slice.unsafe_bridge` URL: `nudge-factor-pips-skill:stone` renders "Fated, 2 of 5" beside "Vara is monumental in stone", the authored "threads shifting" line renders none. 0 net-new console output. **One finding filed rather than folded in (THR-977):** the `skill:*` line's delta is *absolute capability* while its siblings carry *forecast contributions* — two scales rendering in one vocabulary. That is a model-side question owned by THR-892's field, and this ticket was scoped to render the delta as-is.

**Every encounter ended the same way regardless of how it ended, and no amount of content skill could have fixed it (THR-969, 2026-08-02).** Christian, chat review of the vertical-slice aftermath: *"all the stuff in the original prototype we made is not there."* `BranchAwareAftermathConfig` keyed aftermath variants on the `choiceId` of one step and on nothing else — and because most linear templates ship `variants: {}`, a **choice-less encounter could only ever render its single `fallback`**. So `encounter.slice.unsafe_bridge` produced byte-identical prose, chips, and reaction buttons whether the traveler crossed clean or went into the river. This was a structural gap, not a content one: the mockup's defining feature was **inexpressible by construction**, and a content pass against it would have failed no matter how well written. `AftermathVariant.byOutcome` now maps a `UnifiedActionOutcome` to per-band overrides of `overview` / `changes` / `reactionPrompt` / `reactions`, authorable on a choice-keyed variant *or* on the `fallback`, so the two axes compose: resolution is **choice → outcome band → base variant → fallback**, the band layering onto whichever variant the choice already picked, field by field, so a band may restate only what actually differs. **The band keys on the seven-value `UnifiedActionOutcome`** — deliberately not the five-band `EncounterOutcomeBand` (a trace type), the six-value `StepOutcome`, or `OutcomeBand` from `outcomeConsequences.ts`, each of which type-checks while being the wrong domain, which is the trap `StepNudge.bandProse` already documents. **The duplicate resolver was the real hazard.** The engine's aftermath assembly and the encounter-stage adapter each carried their *own* copy of the choice lookup, so threading the outcome through one and not the other would have left the UI rendering the un-banded ending the engine had already resolved past — invisibly, since both are green. Both now call one shared `resolveAftermathVariant`. Additive throughout (NFP #6): a band-less config resolves to the **identical object** it always did, pinned by an identity (`toBe`) assertion rather than deep equality, and an unauthored band or absent outcome degrades to the un-banded variant rather than throwing. Content re-authoring of the five slice aftermaths is THR-973, which was filed blocked on this and is now unblocked; the consequence-chip surface is THR-971.

**The merge queue's agent half is in, and the whole change is deliberately inert until one click (THR-946, 2026-08-02).** THR-735 decided merge queue as the structural fix for the `BEHIND` livelock — an armed green PR knocked stale by someone else's merge never merges on its own, and the sweep that drains them loses one merge per advance of `main`'s tip by construction. `ci.yml` now fires on `merge_group`, which is a **hard prerequisite of enabling the queue rather than an enhancement**: without it the required checks never report on a merge group and *every* queued PR stalls until timeout. Landing the trigger before the settings click is the order the plan's fail-soft table requires. **`dorny/paths-filter` cannot classify a merge group** — it derives its diff base from a push/PR payload that this event does not carry — so a third producer of `code`/`docs` computes the same predicate from `git diff --name-only <base_sha> HEAD`. **The docs-only skip is preserved inside the queue, deliberately:** running the full suite on every merge group is simpler, but it would put each hourly briefing and orchestrator sweep through the ~10.5-minute suite and batch them behind one another — the very congestion the queue is being adopted to remove, and a latency argument rather than a cost one since public runners are free. That is only safe if `skipped` can never mean *unclassified*, so THR-768's vacuous-gate reasoning was re-derived for this event: every failure mode in the new step — unresolvable `base_sha`, `git diff` error, malformed pattern — exits non-zero under `set -euo pipefail`, fails `detect`, and the existing guard converts that into a **failing** required check. **Two GitHub-expression traps were caught in design, both of which fail silently and only after the queue is live:** `A && 0 || 1` yields `1` on *both* branches because `0` is falsy, so the conditional `fetch-depth` is quoted `'0'`/`'1'`; and the output fallback `steps.filter.outputs.code || steps.filter-merge-group.outputs.code` is correct *because* `'false'` is a non-empty, hence truthy, string while a skipped step's output is `''` — the asymmetry is load-bearing and is commented as such. The third copy of the docs predicate is pinned structurally **and behaviourally** — the pattern string is executed as a `RegExp` against doc/code fixtures and cross-checked against `isDocPath` — falsified in two arms before shipping. **Done-whens 2 and 3 are strictly post-click and are not claimed:** a real code PR and a real docs PR landing through the queue, and `linear-autoclose` firing on a queue-produced merge. Autoclose is expected sound by inspection (it triggers on `pull_request: [closed]` and on `push` to `main`; a queue merge produces both) — which is exactly what Done-when 3 exists to convert into a measurement. **THR-945's script is retired by never having been written**: no disturber-pays script exists, and THR-947 removed the exhaust that was its whole premise.

**Encounter prose can now be listened to instead of read — but only half the ticket had a surface left to land on (THR-348, 2026-08-02).** The D3 spec (THR-336, May) is now implemented: `speakEncounter(text, voice, options) → TtsHandle` over the existing dual-mode Kokoro narration service, plus a `ProseTtsButton` control, wired to the encounter prose the player actually reads. **The handle's `cancel()` is generation-scoped, not global** — the service is a singleton shared with HexChronicle, so a naive `cancel()` would silence whatever unrelated narration had started since; the handle captures the utterance id at dispatch (via a new `currentUtteranceId` accessor) and stops only while it still owns playback, which a test falsifies by dispatching twice and asserting the stale handle is inert. **The promise resolves at dispatch, not at completion**, because spec line 1 returns a promise *and* line 3 requires `cancel()` to abort in-flight generation — resolving at completion would make the handle useless by the time you hold it; `handle.done` carries completion separately. Context metadata stays inert per spec line 4, pinned by a test asserting the backend receives exactly three arguments. **The second Done-when could not be delivered and was not faked.** "Detail page bodies can be played as TTS" names `DetailModal` / `DetailModalStackProvider` / `DetailPage/*` — and `DetailModalStackProvider` and `<DetailModal />` appear in JSX in **exactly three test files and nowhere else**, with no `App`/`GameView` mount and zero non-test importers of `openDetailPage`. That is the same green-tested-and-unreachable shape as the THR-950/951/952 prune candidates, and the *second* time today a May-era post-v1 encounter ticket has named a surface the Nudge Model pivot orphaned (THR-346 → THR-962). Wiring TTS there would produce evidence no player can observe, so it is filed as **THR-966**, mutexed behind THR-951's disposition call. The adapter and the button are surface-agnostic by construction, so whichever way that resolves it is a call-site change. Verified live at 1920×1080 on `?spawn=encounter.slice.unsafe_bridge` with `__DEBUG.getOpenModals()` returning `["EncounterVeil"]`; the only console error is the expected `localhost:3001` TTS-server refusal, which is exactly the fail-soft path — no local server, so the control renders the opt-in affordance rather than throwing.

**The operating manual told every docs PR to hand-check a gate the platform already enforces (THR-967, 2026-08-02).** `Docs gates` became a **required status check** on `main` on 2026-08-01 (THR-931), but CLAUDE.md's docs-only track still said it was "not yet a required status check" and instructed sessions to read its conclusion rather than trust the merge gate — advice that costs time on every docs-only closeout, resting on a premise that had become false. Corrected, together with the detail that made it hard to confirm: the check landed in **ruleset `15479914`** (enforcement `active`), *not* the classic branch-protection rule, so `main` is governed by two surfaces and an auditor inspecting only the classic one will find `Docs gates` absent and reasonably conclude it was never done — the confirming `gh api` command is now inline. Re-verified live this run rather than taken from the filing comment. The stale sentence also read as a live tracking pointer to THR-931, making a shipped change look open. (THR-347's entry moved to `project-history.md` to hold the 60-line cap.)

**A grooming rule instructed the lane to empty most of the executor's queue, and it had been obeyed at random (THR-968, 2026-08-02).** `daily-backlog-grooming`'s prompt said `Deferral`-labeled issues "should be in Idea or Todo unless actively worked". CLAUDE.md § *Prioritization* says the opposite as the **first** place any executor looks: Ready-for-Dev deferrals in active projects are the top of the queue. A run obeying its own prompt literally would have moved every such issue out in one automated pass — each move individually justified by the prompt, nothing downstream able to tell a correctly-filed deferral from a mis-parked one, and the resulting near-empty board reading as a *pipeline gap* rather than as damage. That it had not happened was prior runs quietly declining to act, which is its own defect: **a rule obeyed at random is worse than no rule**, because the next run has no way to know the correct reading is "ignore this one". The prompt was the side to fix — CLAUDE.md is what `pull-work` and the executor lane actually implement. Replaced with a **readiness** predicate rather than a state one, so the real concern the old rule gestured at survives: flag deferrals that are genuinely unclaimable (no Done-when, or no coordination block anywhere), which is exactly what `pull-work` Step 3 bounces, and never move one out on account of the label. Both copies moved in the same PR — the live prompt is outside version control and the repo mirror is the tracked copy, so editing either alone is a no-op or gets reverted by the next mirror audit. (THR-935's entry moved to `project-history.md` to hold the 60-line cap.)

**A rule written so a third gate would not need a bespoke note had a third gate all along, unnamed (THR-976, 2026-08-02).** THR-896 generalized "run every tree-diffing freshness gate as the last action before `git push`" — then named exactly two members, 3b and 3c. The **step-2 typecheck ratchet** is a third and was never listed: `check:typecheck -- --update` snapshots `typecheck-baseline.json` at the instant it runs, so anything written afterwards is measured against a baseline that predates it. **The omission had already cost a ship.** THR-969's PR #1264 legitimately lowered the baseline 3481 → 3478, then wrote the change's own test, which carried one net-new error; the ratchet read 3479 against a committed 3478, and both the commit body *and* the PR body cited `check:typecheck → OK, 3478`. The required check went red, auto-merge stayed armed and could never fire, and the PR sat ~100 minutes reading as shipped from every surface except the check rollup — surfacing only because the next hourly sweep flagged it for an unrelated reason and the rollup was read on the way past (impediment #402). Fixed at both ends: step 2 now says re-run the ratchet last whenever you refresh the baseline, and the general rule states its membership as **three**, with the #1264 failure as the worked example — a rule stating only its principle is what let two revisions omit the member it most needed. **The other two named surfaces needed nothing, and that is recorded rather than assumed:** `pull-work` carries no code-track gate ordering to drift from (its only gate text is the drain's docs track), and `Docs/canon/process.md` points at CLAUDE.md rather than restating it. Extended beyond the ticket's list to this lane's own prompt (`tb-opus-pickup`, live copy + repo mirror), which carried the same two-member rule and is what the executor actually reads.

**The god's hunger vocabulary disagreed with itself, and the ticket's premise was the thing that was wrong (THR-891, 2026-08-02).** `HungerId` listed **ten** hungers while four shipped surfaces already spoke for **twelve** — the remembrance catalog the player actually picks from (fully authored, with art for all twelve), the meeting prose register (test-pinned in both directions), the Repertoire plan doc, and the wiki page. The ticket framed this as a Christian call, on the premise that adding `haunt`/`illuminate` meant *minting* two hungers; they were already minted, so the question collapsed to a technical verdict — which surface is stale — and `HungerId` was corrected to twelve. Chasing that exposed a larger defect underneath: `AscendantIdentity.hungerId` is stored **dotted** (`hunger.witness`) while the card library keys **bare** (`witness`), and the call site bridged them with `as HungerId` — a cast that type-checks and is false, so the lookup missed for *every* god and **no hunger-unique card was ever dealt to anyone**. Same bug in the Meet-The-First bond beat, where every god silently got `godVoiceFallback` instead of their authored line. One `toHungerId` narrowing helper now owns the conversion, with tests that fail if the cast returns.

**A skill instructed its subagent to do the one thing its own hard rule forbade, so the instrumentation was never written and nobody noticed (THR-762, 2026-08-02).** `intent-judge`'s Procedure step 8 told the judge to append a row to `Docs/judge-metrics/`, while its Hard rules said "**Never edits any file**". The judge obeyed the hard rule — correctly — and wrote nothing, so the contradiction produced **no error, no warning, and no metrics**, for as long as the skill has existed. The write now belongs to the **spawning session**, which is where it always belonged: the judge staying read-only is the anti-rubber-stamp property the hard rule exists for (a judge that writes nothing cannot launder its own record), and two of the seven columns are the spawner's to measure anyway — it is the only party that observes spawn-to-verdict latency, and a judge asked to time its own run has no clock before it starts. **The same instruction lived in a second file the ticket did not name**, `.claude/commands/intent-judge.md`, where the step sat in the invoker's list but never said so. **The find worth more than the fix:** the line justifying the whole directory claimed "`retrospective` skill consumes Fridays" — and `.claude/skills/retrospective/SKILL.md` contains **zero** references to judge-metrics and never has, so the weekly aggregation has never run. That matters because intent-judge's **Kill criteria are all defined over those aggregates** ("≥95% Allow with zero overrides → rubber-stamping"), which makes them unfalsifiable as written: no one has ever been able to evaluate whether this skill should be retired. Filed as **THR-957** and the false claim corrected in place rather than deleted, since the intent was real and only the wiring is missing. Also pinned the filename spelling — the old `YYYY-WW` spec produced two misnamed files (`2026-20.md`, `2026-27.md`) whose own headings carry the canonical `W` form.

**Recently completed (2026-07-22, marathon):**
- **M3 Dynamic Economy nearly complete** — THR-669 (route events + the trade-route pipeline revived: three stacked engine bugs meant no route had ever survived), THR-670 (cargo route lines + tooltips on HexMapV2), THR-617 (P3 power couplings: systemic monopolies with live chronicle prose, sphere drift from cargo flows, faction gold term, scarcity arcs with intervention encounters). THR-618 partial: all five divine economic verbs now `[IMPL]` (reveal vein / guide caravan / sour mine shipped, bless/blight were live) and the Flow Web extraction checkpoint is decided (**defer** — `Docs/plans/2026-07-22-flow-web-extraction-checkpoint.md`); only the THR-611-coordinated essence bridge remains.
- **War project complete** — THR-628 (battle depth: prepared defense, tactical events, breach surfacing, battle IPK), THR-629 (siege gravity-well repair: dead capability gates fixed, relief_march + sabotage live), THR-630 (Phase D: notable-agenda roster + five families incl. the Campaign war hand-off that opens war to the nations, NotablesPanel, power-vacuum verification).
- **Scene Integration complete incl. follow-ups** — chain A–F shipped earlier; THR-712 (guild-tier sweep: cast figures, one genuine bond, 44 continuity seeds, permanent guild prose-QA gate) and THR-713 (381 invented-name literals across seven branching encounters retired to `{cast:*}`).
- **Cowork migration complete** — THR-677 ports live (daily grooming, workflow retro, project hygiene), THR-684 attachment-pipeline prompts written, THR-704 scheduler hygiene, monthly-rulebook-review registered.
- **Social Systems: secrets & favors activated** — THR-724 turned a fully-built, entirely silent subsystem on. Generation had a *dead read site* (not merely thin content), consequences had zero callers, the consequence module was a two-way fork, and both player verbs were unreachable. All four fixed; 0 → 18 secrets and 0 → 4 revelations per 120-tick run, and the three pre-seeded interface-registry rows are LIVE with run evidence.

## Milestone Status

Tracked in [Linear Projects](https://linear.app/threadbare/projects). Active: Thematic Pressure & Living World (war system now fully live), Content Architecture (scene integration complete). See Linear for per-project state — this file does not duplicate it.

## Where things live

- Shipped work: `Docs/project-history.md` (one-liners) + Linear Done
- Rationale: `Docs/changelog.md`
- Wiring: `Docs/plans/wiring-checklist.md` + `Docs/plans/2026-04-16-systemic-wiring-guide.md`
- Rules of play: `Docs/canon/rulebook.md` (+ quick-reference)
- Christian-facing: `Design/briefing.md` / `Design/user-actions.md` — **on the `ops` branch**, not `main` (THR-947): `git fetch origin ops --quiet && git show origin/ops:Design/briefing.md`

## Active Backlog Ideas

None parked here — file ideas as Linear issues (predicates, not counts; see `Docs/canon/process.md`).
