# Project Status
> Updated 2026-07-19 (THR-605 — **Slice 3 of 5 — `sub.vision` → `scry_sublocation` real effect.** The "Place Memory" vision action, previously a no-op, now reveals concealed knowledge on the target sublocation's hex: resolves the sublocation up to its hex (`resolveLocationToHex`, three-tier) and flips every unrevealed `knows_secret_of` edge held by any actor there to `revealed: true`. Genuinely consumed — `agentDetail.ts` lists only *unrevealed* secrets (so revealing surfaces them in the agent panel) and `phaseSecretsFavors.ts` exempts revealed secrets from decay (so they persist). A "read" crudType whose world change is the reveal, deterministic (no PRNG/magnitudes), hex-granular (the load-bearing awareness rule), graph+ctx only → a `graphOpExecutor.ts` case auto-routed via `graphOnlyOps`, like Slice-1 fortify + the Slice-2 artifact trio. **Design correction vs the plan:** the plan's "reveal into player fog visibility" isn't reachable from a one-shot graph op — the `VisibilityMap` is re-derived per-tick from LOS sources (avatar/retinue/scry-court positions), so a single mutation can't persist a lit hex; scoped to the secret-reveal instead (the genuinely-consumed half of the plan's intent, sanctioned broadening per the kill-criteria). Dropped the `curseConcealed` reveal — it has no reader, so flipping it would be write-only theatre. `sub.vision` catalog badge flips `not wired`→`wired · template` (regenerated `action-catalog.generated.json`, `none` 4→3, `0 unauthored effects`); authored `technicalEffect` added. 6 new fail-soft tests + 20 targeted green (scry + fortify + artifact trio); `vite build` green; `tsc -b` net-new 0 (3542=3542); 30-tick CLI smoke clean (389 agents). Engine + content-data only → browser-verify exempt. **THR-605 now COMPLETE (2026-07-19)** — final Slice 4 `plant_trap` (`sub.trap`) shipped via the resolution-intercept path (needs `state.pendingEncounterSeeds`, unlike the graph-executor-case slices 1–3): `applyPlantTrap` resolves the intended victim present at the trapped sublocation (or its hex) and seeds the authored `encounter.trap.sprung` beat against them — consumed by `evaluateEncounterSeeds`, which spawns a real failable negative encounter whose harm lands via `condition`-weighted reward pools (no bespoke harm op). Substrate correction: the encounter-seed pathway has no hex-arrival gate, so the plan's "fires on the next agent to arrive" was unreachable — target a present victim instead. `sub.trap` badge flips `not wired`→`wired · template` (`none` 3→2; residual `sub.sanctify`/`sub.sanctify_tavern` out of this issue's 6-action scope → THR-662). Systemic-wiring-guide 6-op aggregation added. 7 new fail-soft tests + 27 targeted green; `vite build` green; `tsc -b` net-new 0 (3542=3542); 30-tick CLI smoke clean (389 agents). Closes via `Fixes THR-605`. — THR-576 — **Docs/plans generated index.** New `scripts/rebuild-plans-index.ts` (mirrors `rebuild-index.ts`: pure fs/path, esbuild→node bundle, `--dry-run`/`--check`) emits `Docs/plans/INDEX.md` — an exhaustive, deterministic catalog of every top-level plan doc (**544 plans across 6 months**, 2 undated), one line each: date · linked title · linked Linear issue (structured `linear_issue`/`Fixes` keyword → first `THR-` fallback) · declared status, with a ⚠️ *superseded by* marker resolved from each doc's own stale status **and** other docs' structured `**Supersedes:**`/`superseded_by:` back-references (6 flagged; inline "replaced by" game-mechanics prose deliberately ignored to avoid false positives). Grouped year-month, newest first. Wired into the `check:process` advisory lint as `rebuild-plans-index:check` (up-to-date→exit 0, stale→warn; `PLANS_INDEX_CHECK=blocking` to escalate) — the in-repo cadence hook, mirroring `generate-systems-inventory:check`. `Docs/canon/README.md` now points at the index as the fallback discovery path under the curated Canon layer. Build tooling + docs only, zero `src/` touched; full suite **12481 green (828 files)**, vite build green, `tsc -b` net-new 0. Closes via `Fixes THR-576` on the PR. — THR-602 — **Game Manual Wiki W17 — `twilight-worldsoul-reference.html` ("Twilight, Echoes & the World-Soul") — the FINAL page; all 17 Manual pages now complete.** Authored the W17 dual-layer served manual page (16→17 of 17) + registered it in `public/wiki-manifest.json`; `npm run generate-design-wiki` re-injected nav into all 21 pages. Player layer: how a run ends (the Unmaking you witness, not fight), the harvest of Legacy/Monument/Relic echoes scaled by triumphant/somber/bittersweet ending, the World-Soul carrying echoes across cycles as they degrade + binding each run into a chronicle volume, the next world as a response not a sequel. **The load-bearing honest call (highest-DESIGN-density page of the set):** two parallel run-end models coexist — WIRED is `echo.ts`+`cycleEnd.ts`+`chronicle.ts` (Legacy/Monument/Relic = `echo.ts` `nodeTypeToEchoType`; IMPL: `phaseSphereAggregation` orch:3024, doom-expiry twilight entry orch:2338, `TWILIGHT_TICKS=7` countdown, `computeHarvest` scoring, echo carry-over + chronicle binding), while `worldSoul.ts` Fundament+Resonance ledger is defined+unit-tested but has **zero non-test callers** — DESIGN. Also DESIGN (flagged, not shown as real): echoes seeding the next world (`collectInjections` has no consumer), divine-echo selection (HarvestScreen: "selection coming in a future update", empty array passed), weakened twilight play (`successPenalty` unwired), a re-drawn cosmos (`transitionToNewCycle` continues the same graph). OPEN: rulebook Open Q #6 referenced. UL gap flagged (no shard for World-Soul/Twilight/Harvest/Echo). 6 IMPL / 10 DESIGN / 2 OPEN flags, 10-row constants table. `check:design-wiki` OK (21 pages), `check:wiki-freshness` OK, `vite build` green, full suite 12481 green (828 files); browser-verified @1920×1080 (4 sections, nav active, scrollWidth 1905 ≤ innerWidth 1920 no h-overflow, 0 console errors — WebGL-less DOM page, screenshot times out from headless CC so the DOM/console readout is the artifact per W1–W16 precedent). HTML+manifest only, zero `src/` touched. Closes via `Fixes THR-602` on the PR. — THR-601 — **Game Manual Wiki W16 — `attention-story-reference.html` ("Attention, the Digest & the Chronicle").** Authored the W16 dual-layer served manual page (15→16 of 17 Manual pages; only W17 remains) + registered it in `public/wiki-manifest.json`; `npm run generate-design-wiki` re-injected nav into all 20 pages. Player layer: the renewing attention pool (scarce by design — attending your First is half cost), the curator that surfaces thread tugs and promotes turning points to spotlighted story beats, the digest (Read the Threads + per-thread story-so-far), and the three-tier prose engine (Routine/Notable/Chronicle) taught through interactive prose keywords, never numbers. **The load-bearing honest call:** attention pool / tiers / curator / thread tugs / pacing governor are IMPL & wired in `phaseAttention` (`orchestrator.ts:2669`), but the **digest buffer is appended every tick yet never pruned at runtime** — `pruneDigestBuffer` / `DIGEST_BUFFER_RETENTION` (48) have no production caller (tests only), so the reading surfaces self-limit by lookback window (`STORY_DIGEST_LOOKBACK_TICKS` 36) → flagged **DESIGN/dormant**. IPK = Interactive Prose Keyword (`ProseKeyword.tsx`) — the "learning engine" in `canon/prose.md`'s sense (learn by hovering keywords, not an inference/player-modelling system). W16 has UL shard coverage (`Prose.md`: IPK, Chronicle Entry, Narrative Event, Thread Tug), cited as authority. 7 IMPL / 4 DESIGN / 2 OPEN flags, 17-row constants table. `check:design-wiki` OK (20 pages), `check:wiki-freshness` OK, `vite build` green, full suite 12481 green (828 files); browser-verified @1920×1080 (4 sections, nav active, scrollWidth 1905 ≤ innerWidth 1920 no h-overflow, 0 console errors — WebGL-less DOM page, screenshot times out from headless CC so the DOM/console readout is the artifact per W1–W15 precedent). HTML+manifest only, zero `src/` touched. Closes via `Fixes THR-601` on the PR. — THR-600 — **Game Manual Wiki W15 — `stealth-rivals-reference.html` ("Stealth, Detection & Rival Gods").** Authored the W15 dual-layer served manual page (14→15 of 17 Manual pages; W16/W17 remain) + registered it in `public/wiki-manifest.json`; `npm run generate-design-wiki` re-injected nav into all 19 pages. **The load-bearing honest call:** the tree holds *two* detection systems and only one is live — **region-level detection pressure** (`phaseDetectionPressure`, wired `orchestrator.ts:2640`; each encounter choice raises a region's pressure by choice-cost × sphere-visibility, decays 0.005/tick, crossing 1.0 seeds a `shadow.rival_strike`) ships **IMPL**, while the per-actor **two-audience escalation model** in `stealth.ts` (mortal suspicion 3/8/15, rival scrutiny 5/12/20) is authored + unit-tested but has **no live caller** — flagged **DESIGN/dormant**, and the player layer is written around the region model that actually runs. Rivals (`generateRivals` 2–4 per world from opposing spheres; 4 temperaments), THR-66 four-phase schemes (rumor→materialization→response→crack, escalation 0–3, counter-play), hidden marks (delayed reveal + decay), and ageing intelligence (6 categories, reliability decay) all **IMPL & wired** — grep-verified against `orchestrator.ts`. `regionDetection.ts` correctly excluded (it is geographic clustering, not stealth). 9 IMPL / 5 DESIGN / 2 OPEN flags, 18-row constants table. `check:design-wiki` OK (19 pages), `check:wiki-freshness` OK, `vite build` green; browser-verified @1920×1080 (5 sections, nav active, no h-overflow scrollW=clientW=1265, 0 console errors — WebGL-less DOM page, screenshot times out from headless CC so the DOM/console readout is the artifact per W1–W14 precedent). HTML+manifest only, zero `src/` touched. Closes via `Fixes THR-600` on the PR. — THR-575 — **CLAUDE.md single-executor diet (re-reconciled).** The human-approved opening-coordination diet (PR #484, chat-gated 2026-07-04) had gone stale/conflicting after 15 days, so it was re-implemented fresh against current `main`: opening "Cowork vs Claude Code" + "Prioritization" sections rewritten to a reference card + 3 pointers (`process.md` / coordination-protocol / `pull-work` SKILL), **933→621 words (−33%)**, with a 19-keyword audit proving every load-bearing rule survives — including the rules added since the PR was cut (THR-645 plan-doc-in-description, Rules 1–10, the THR-608 chat-only user-review block). Component-count refreshed 166→264 (this session's codesight scan); the dated per-file high-impact importer list left to THR-572. Docs-only → vite build green, browser-verify exempt. Closes via the `Fixes THR-575` keyword; PR #484 superseded/closed. — THR-659 — **Orphaned action-card inspector.** `window.__DEBUG.listUnreachableActions()` + a DebugPanel "Orphaned Cards" tab report player-castable templates no run can ever surface — a pure, deterministic set difference over static registries (mirrors `proseQualityReport`): player-reachable universe = `actorAffinities` includes `ascendant` (the drawer's player-vs-mortal line), minus static beat grants (`collectGrantedActionIds()`), starters, and — **the plan's "confirm before coding" catch** — the 8 dynamic reach-signatures (`invest.*`, which no static `grantsActionIds` can name, THR-523). **Finding: 106 unreachable of 132** player cards, far past the 4 previously hand-tracked; detects only, grants nothing. New pure module + test (9) + tab + `__DEBUG` bridge; 12481 tests green (828 files), vite build green, `tsc -b` net-new 0, browser-verified @1920×1080 (tab renders 106 rows within viewport, filter narrows to `loc.fortify`, live `__DEBUG` matches CLI, 0 console errors — WebGL screenshot times out from headless CC, `__DEBUG`+DOM readout is the artifact per prior closeouts). Complete single-shot deliverable — closes via the `Fixes THR-659` keyword on this PR. — THR-658 — **Substrate visibility for design agents.** Generated `Docs/canon/systems-inventory.md` (`npm run generate-systems-inventory`) — grep-able map of 24 subsystems with aliases (incl. legacy `TB-073`), implementing domains + tick phases, and an ACTIVE/DORMANT badge from a 120-tick seed-42 run; plus a mandatory `## Substrate inventory` step in the design workflow (CLAUDE.md Step 0.6), a substrate tooth in the pillar-auditor + intent-judge (auto-REVISE on green-field duplication), and a rulebook `[IMPL — dormant]` completeness rule. Closes the THR-614 failure where a green-field war plan was drafted atop ~3,300 lines of wired-but-dormant war code. Process/tooling only; 12473 tests green, no `src/` touched. See changelog. Prior: THR-614 — **War system woken up (seam 1 of 4: activation).** The full TB-073 army/battle/siege system was built + wired but dormant — no *ambition-driven* army ever spawned. Root causes fixed (engine-only, additive): (1) army-spawn was a one-shot at ambition-*creation*, so a faction that became eligible while already holding a standing military ambition (seed-42's Civic Guard) never raised one — now retried each evaluation interval via `maybeSpawnArmy` (`isEligibleForArmySpawn` caps at `MAX_ARMIES_PER_FACTION`, so no spam); (2) spawned armies had `objective: null` and sat forever — new `selectArmyObjective` marches them to the nearest hostile-controlled settlement (deterministic, fail-soft); (3) armies mustered inside a *sublocation* (a gatehouse room, off the location adjacency graph) so they couldn't path out — now resolved up to the parent settlement (three-tier model). Verified on the standard seed 42 medium: ambition army raised @tick 10, resolved siege @tick 11 (exit criterion: army+battle <30 ticks); 120-tick run stays bounded (peak 3 concurrent armies/battles — not a runaway). `ARMY_SPAWN_IRON_TIER_MIN` left at 4 (loosening to 3 was a no-op on tested seeds, 2 risked constant war). 12473 tests green, vite build green, `tsc -b` net-new 0, 30-tick CLI smoke (389 agents). Issue stays In Dev (keyword-free). **Seam 3 (inspectability) shipped 2026-07-18:** the DebugPanel "Armies" tab already existed + worked (built in phase-22 — the seam-1 note's "grep-confirmed absent" was wrong about the tab); what was genuinely missing was the headless `__DEBUG.getArmies()` / `getBattles()` bridge methods, now added (additive, dev-only, tree-shaken in prod) so an automated run can assert "did war fire?" without a browser. Ground-truth graph read (no monster-faction filter) mirroring the tab's `armyState`/`battleState` reads; resolves faction/commander/location/objective-target names. 12473 tests green, vite build green, `tsc -b` net-new 0; live `__DEBUG.getArmies()` in the seeded browser returned 2 fully-resolved Host armies (Iron Wolves / Scarlet Company), 0 console errors. **Seam 4 (plan+rulebook reconciliation) shipped 2026-07-18:** added rulebook §9 "The World at War" (`Docs/canon/rulebook.md` — canon previously had zero army/battle mentions) with honest `[IMPL]` flags across armies-as-nodes / spawn gates / cohesion attrition + 4 threshold encounters / momentum battles / sieges / spotlight-as-intervention / scaled destruction, plus honest gaps (dead `territorial_expansion` gate, pending `quintessence`→`cohesion` rename, plan-vs-shipped phase-band deviation); added an Implementation-Status banner to the green-field plan doc `2026-07-05-autonomous-notables.md`; and posted substrate-corrections on the three downstream phase issues (THR-628 spotlight pool + siege constants already shipped, no `conflict-config.ts`; THR-629 sieges already built; THR-630 refugee/ruins aftermath already built — only notable-agenda families are green-field). Docs-only → browser-verify exempt. **Seam 2 (`cohesion` rename) shipped 2026-07-18 — the final seam; the four-seam re-scope is complete.** Renamed the army-health concept `quintessence`→`cohesion` (ending the collision with cosmological Quintessence) across `ArmyState.cohesion`/`cohesionMax`, `ARMY_COHESION_BASE`, `COHESION_THRESHOLDS`, army/battle/siege engine + data + the `__DEBUG.getArmies()` bridge fields (`cohesion`/`cohesionMax`/`cohesionPct`) + the DebugPanel Armies tab label + ~10 test files + the W14 manual page; cosmological quintessence (phaseQuintessence, agent/ascendant quintessence, the `quintessenceDelta` DigestEntry field) deliberately untouched. Full suite **12473 green (827 files)**, vite build green, `tsc -b` net-new 0 (3542=3542), 30-tick CLI smoke clean (389 agents); browser-verified in the seeded medium world — `__DEBUG.getArmies()` returns the renamed `cohesion` fields (Iron Wolves host 100/100/100, no `quintessence` key) and the Armies tab renders "Cohesion: 100.0 / 100 (100%)", 0 console errors. THR-614 closes via the keyword on this PR. — THR-640 — **Motive-Receipt Foreshadowing Phase C (content depth): object-case pronouns + Reach-flavored motive clauses.** Fixed the two object-position pronoun bugs in `foreshadowing-content.ts` (`moves {subject} closer` → `{object}`; `this is {subject} all over` → `{object}`) with a new realizer `objectPronoun` helper + `{object}`/`{Object}` typed slots wired into both composers. Added `MOTIVE_CLAUSES_BY_REACH` (ambition/personality/intel/divine × 3 relevant Reaches × 3 variants) so receipt-driven S2 reads with Reach flavor — `composeReceipt` prefers the reach sub-pool, keys `pull:kind/reach`. Extended the receipt agreement sweep to fill object slots + a static object-position lint (subject slot after a copula/transitive verb/object preposition fails). 12463 tests green, vite build green, `tsc -b` clean in touched files, 30-tick CLI smoke (389 agents). Items 3 (`intentPhrase` backfill) + 4 (authored marquee overrides) deferred to THR-657 — both blocked on Phase-B wiring that never landed (`intentPhrase` field/consumption don't exist; override selection runs on stubbed signals and bypasses the typed realizer). Browser-verify exempt (content-only; correctness proven by the agreement/object-case sweep). Plan `Docs/plans/2026-07-05-thr-631-motive-receipt-foreshadowing.md` (orphaned @ cd1ee374). — THR-651 — **Pure Claude Code Migration Phase 1: skill-tree audit (port/retire `.agents/`-only skills).** Ported `design-council` + `playtest-interface` into `.claude/skills/` (now shared, `check:skill-sync` green at 41); `playtest-interface`'s port gated on — and passed — a live browser-MCP check from a CC session (`?view=game&seeded`, WebGL canvas rendered, `__DEBUG.snapshotScene()` → 711 hexes). Retired (no port; stay in `.agents/` until Phase 3 THR-654): `content-catalog-manager` (no 60-day usage) + the `defuddle`/`json-canvas`/`obsidian-{bases,cli,markdown}` set (CC does vault work via `OBSIDIAN_VAULT_PATH`). CLAUDE.md allowlist + Domain-Skills rows updated; `.agents/` itself untouched. Skills/docs-only → vite build green, browser-verify exempt. — THR-650 — **Pure Claude Code Migration Phase 1: `keep-work-flowing` CC equivalent.** New `.claude/skills/keep-work-flowing-cc/SKILL.md` + hourly scheduled task (cron `20 * * * *`, effective ~:28 with jitter — non-quarter-hour, avoids the `:45` Cowork slot during parallel run) that replaces the Cowork PM chat brief with two committed files: **`Design/briefing.md`** (the inbox — queue health starved/healthy/backed-up + blocked/stale flags, home-tree freshness ping per retro E1, things-needing-Christian in THR-608 plain language, generated-at timestamp) and a refreshed **`Design/user-actions.md`** (home-tree numbers, cadence→hourly-CC, Linear-from-scheduled trending-resolved, retired-Codex item superseded). `.gitignore` un-ignores the two PM-inbox files (`Design/*` + `!` exceptions) so the recurring task commits without `git add -f`. First briefing seeded from live data (home tree flagged 37-behind/dirty; also surfaced a real clock-skew catch — sandbox `date` was ~8h behind the authoritative scheduler clock). Commits on substantive change only, never a close keyword. Heartbeat visible via `list_scheduled_tasks`. 12460 tests green, vite build green, skill-sync clean (new skill is `.claude`-only); browser-verify exempt. Plan `Docs/plans/2026-07-17-pure-claude-code-migration.md`. — THR-649 — **Pure Claude Code Migration Phase 1: `design-session` skill + coordination-protocol reframing.** New `.claude/skills/design-session/SKILL.md` encodes the former Cowork design role as a CC session type — design-governance checklist, canon Step 0, Linear In Design→Ready-for-Dev transitions, handoff coordination block — with the one mechanical change: a CC design session **commits its own plan doc directly** via a `docs/plan-*` PR (CI-gated, merged immediately), no `plan-pending-commit` label / hourly flush / auto-flush. `Docs/plans/2026-04-13-linear-coordination-protocol.md` reframed "Cowork" → "design session (CC)" (session-type banner, Two-Session-Types section, Workflow-States owner column, Role-Boundaries, Design-Session-Start/Handoff, exit-gate labels); flush pipeline left intact + described as the coexisting legacy path until THR-654/Phase 3. Docs/skills-only → vite build green, tests unaffected; browser-verify exempt. Plan `Docs/plans/2026-07-17-pure-claude-code-migration.md`. — THR-637 — **Entity Visual Header pattern shipped (UI Visual Overhaul).** One primitive (`EntityVisual`, hero/portrait/chip) + one resolver (`resolveEntityVisual`) + authored fallback tables (`entity-visual-fallbacks.ts`, 6 id-hashed Threadbare gradients + category glyphs) replace three ad-hoc image mechanisms. Four core surfaces retrofitted: EncounterVeil context-strip character + aftermath actor moments, AgentProfileModal portrait header (knowledge-gated via new `ENTITY_VISUAL_MIN_KNOWLEDGE='recognised'`, hoisting the inline `!== 'stranger'` checks), LocationView hero landscape (now real `pickConceptArt` images, not gradient placeholders). Knowledge-gate is person-kinds-only + fail-open (locations/encounters/items never gated, THR-138). `__DEBUG.resolveEntityVisual(id|name)` + styleguide section + `Docs/design-system/entity-visual-header.md`. 18 resolver unit tests; full suite **12460 green (826 files)**; vite build green; browser-verified @1920×1080 (styleguide 3×2 tiers + live AgentProfileModal art portrait; `__DEBUG` confirms agent-art/location-art-via-terrain/agent-fallback; console clean). THR-638 art batches slot into the resolver's per-kind switch as one branch each. — THR-648 — **Pure Claude Code Migration Phase 0: exported Cowork memory → `Docs/ways-of-working.md`.** Distilled the durable collaboration/taste/decision-authority facts from the Cowork memory store (60+ facts) that had no repo home — verdicts-vs-recommendations, creative-director pushback, grey-zones-upfront, no-open-questions-in-docs, design-expansively/implement-conservatively, good-enough/safe-enough, the narrative tiebreaker, marketing-evokes-not-explains — excluding anything already in CLAUDE.md/canon and all retired-with-Cowork mechanics. Scheduled-task registry (both runtimes) snapshotted into the Linear issue for rollback. Docs-only → 12442 tests green, vite build green; browser-verify exempt. Plan `Docs/plans/2026-07-17-pure-claude-code-migration.md`. Migration siblings still Ready for Dev: THR-650/651 (THR-649 shipped 2026-07-18). — **THR-613 (Player Action Progression v1) — Slice 3b-tail shipped (2026-07-18), the whole plan now complete:** the §5.B three-state grammar (Held / Within-reach / Another-life) lives in the **Codex** (not the flood-prone live drawer — the human-reviewed design call routed it there), reachable pre-filtered from the character sheet + Signatures readout ("What you could still become →"). The eight `invest.*` reach signatures are now catalogued so "Another life" is legible; the lock keys on `requiresReach` (never the flavor `reach`), so universal cards are never mis-greyed; `getTargetActionSlots` untouched. Reuses the shipped `SignaturePathState` grammar (one source of truth). 12473 tests green, `tsc -b` net-new 0, browser-verified @1920×1080 (overlay pre-filtered to acquirable, all 3 states, 0 console errors). Issue stays In Dev (keyword-free) for Cowork to close. Deferred: THR-656 (art for `loc.open_markets`).)

## Current Focus
**Agent Personality — the Core foundation layer (THR-542, all 4 slices shipped):** a second, more fundamental personality layer beneath the 8 reach axes — five plain virtue↔vice continuums of *who an agent is* (`coreProfile`: Warm↔Cold, Hopeful↔Bitter, Forgiving↔Vengeful, Humble↔Proud, True↔False), kept distinct from reach (`axiologicalProfile`) and capability (`domainCapabilities`). Engine foundation (slice 1) live: registry, seed/colour/bend mechanics, the `core_personality` tick phase. **Content (slice 2 / THR-544) live:** a 60-entry Core origin-vignette library (`core-origin-vignettes.ts`) now layers authored character on top of the PRNG seed at birth (`seedCoreProfileWithVignettes`, recorded on `coreOriginVignettes`), and 10 Core emergent-trait defs (`core-trait-content.ts`, new `subcategory:'core'`) are granted/released by the phase on emergence/fade (silent born-extreme grant). Canon-safe framing preserved: Core = *which way* you bend, distinct from Quintessence = *how bendable* — goodness never a Quintessence pole; Core traits carry no scoring/capability. **Star re-scope (slice 3 / THR-545) live:** with the inner-outlook reading now owned by Core `core_hope` (Hopeful↔Bitter), Star's reach axis re-points from Beacon/Inspiring↔Anchor/Discouraging to **Beacon (Guiding) ↔ Wrecker (Misleading)** (wayfinding/fate) across `axisRegistry.ts` + its independent test oracle, `Docs/canon/cosmology.md` & `encounters.md` Star rows, and the THR-524 canon table; the 18 Star origin-vignettes re-themed from morale/hope (now duplicate of `core_hope`) to wayfinding. **UI (slice 4 / THR-546) live:** the agent character sheet (`OverviewTab`) now renders a **Core** section — the 5 continuums as labelled tracks (virtue↔vice) with a marker per value, positioned above Nature so the layering reads bedrock→expression; `coreProfile` is surfaced through `AgentInfoCardData` at intimate+ knowledge (same gate as `axiologicalProfile`). Deferred: **THR-547** (re-theme the comet encounter's pre-THR-524 Wanderer/Anchor framing).
**Repo Health — `npm test` green on main (THR-489):** the months-long red baseline is cleared — full suite **11966 green (783 files)**, stable ×3. Resolved by the WS1–WS3 consolidation + intervening fixes; the live run reconciled clean against the historical known-failing checklist (no `.skip` masking). CI's required "Test · Typecheck · Build" gate is trustworthy again; merge=Done is load-bearing. Triage/closure doc: `Docs/plans/2026-06-30-test-suite-stabilization-triage.md`. (`tsc -b`'s ~3494-error baseline is a separate concern, out of scope.)
**Ascendant Beats — Divine Cadence (new):** Director + primitives + reach gate + home seat + debug + turn-1 floor + full expression toolkit (imbue/consecrate/bestow/anoint) + pool library + delivery adapter + beat resolve path shipped. **Beats are playable end-to-end** (offer→enter→resolve) and **pool beats now read bespoke per run (THR-514):** each cadence pool beat is backed by a Threadbare-voice content template with `enrichProse` placeholders, rendered enriched in the beat modal. **Onboarding spine complete (THR-504):** the full 5-beat scripted arc (The First → The Seat → A Thing Left Behind → The First Word → A Path Opens) ships with authored Threadbare prose. **Director pool draw eligibility-filtered + identity-biased (THR-516).** **Beat-driven graph seeding (THR-520):** resolving Beat 1 seats the throne (mana income ticking) at The First's settlement; Beat 2 mints + threads a sphere-flavored artifact — no manual card-firing. **Expression cards bucketed + pool-granted (THR-515):** consecrate/bestow/anoint now sit in `ASCENDANT_ACTION_BUCKETS` (`unlockable-generic`) and are granted by three deeper-expression investment pool beats (pool 14→17). **Consecrate relic variant shipped (THR-518):** `action.consecrate-relic` mints a permanent relic at establishment via `ControlSpec.mintUpkeepRelic` (handled in `spawnControlEffect`), waiving all per-tick upkeep while the relic lives and lapsing on its destruction — a pure-data second card, granted alongside `action.consecrate`. **Pool-beat template aftermath + introduction-group binding (THR-522, last engine tail):** `resolvePendingBeat` now runs a matched template's `aftermathConfig.fallback.reactions` through the shared aftermath resolver on resolution (additive; grant-only fallback when none declared) — beats can express `unlock_action`/`encounter_seed`/graph ops via their template, not only the descriptor. Introduction beats bind their specific un-introduced culture/faction into `PendingBeat.boundNodeIds` at offer time and name it via the new `{group}` enrichment placeholder (inspectable in the DebugPanel Beats tab). **Reach signatures acquired end-to-end (THR-523):** the 8 `invest.<reach>.<name>` signatures are catalogued `reach-gated` in `ASCENDANT_ACTION_BUCKETS` and granted per-run via `BeatDefinition.grantsReachSignature` — Beat 4 grants the primary, a new `beat.pool.invest.reach_signature` pool beat grants the secondary (gated by the new `unacquired_reach_signature` predicate). Remaining: per-beat identity tags (THR-514). **Reach signatures (THR-548/549/550):** sphere-power scaling helper + per-(reach×sphere) individualization scaffold + Iron's `signature_warhost` (mark faction `mobilized` + raise an army-node force) and Veil's `sphere_influence_amplify` (THR-551 — sustained sphere rift: ControlEffect amplifies local sphere influence up to a cap + seeded per-tick chaos-pulse leak, all sphere-scaled) shipped; **Stone's `spawn_unique_location` (THR-552) shipped** — mints a one-of-a-kind `location` (unique flag + `controls` edge), dedup by `uniqueTag` (idempotent), optional legendary-relic forge reusing the `spawn_artifact` path.
**Onboarding & First-Run Experience:** THR-504 scripted spine beats 0–4 shipped — the opening arc that delivers the run's first action cards.
**Action System & Unlocks:** THR-501 turn-1 floor (Move + Investiture only across all surfaces) shipped. THR-519 self-updating Action Catalog shipped — `scripts/generate-action-catalog.ts` emits `public/action-catalog.generated.json` from the live registry (164 ascendant-facing templates, code-true buckets incl. Beats verbs); `public/action-catalog.html` renders it (filter/sort/drawer), wired into the Design Reference Wiki.
**Encounter Experience — active:** Phases A1–A3, B1–B7, C1–C4, D1–D3, E1–E2, F1–F2, G1–G3 ✅. Remaining is F-phase integration (mount EffectRegistration components into hero panel / cast tile / scene state surfaces) and Phase H post-v1 polish.

## Milestone Status
- **v1.0 Foundation:** Shipped 2026-03-30 — Phases 1-18 + M2.5 (81 plans, 1533 commits)
- **v1.1 Optimization:** Shipped — Phases 19-22 (determinism, wiring, performance, hygiene)
- **Encounter Format Migration (Now):** ✅ Phases 0-9 complete. All guilds migrated.
- **Content Architecture (Now):** ✅ THR-86/88/239 shipped.
- **UI Visual Overhaul — Design System v1 (Now):** ✅ Project complete. THR-178 Deferral shipped 2026-05-08.
- **Continuous Improvement (Now):** THR-303/305/306/307/309/311/312/313/314/315/316/354/355/356/357/358/359/266/396/404/510 ✅. THR-304 Phase 5 series complete (5a vault THR-356 ✅, 5b repo THR-359 ✅). THR-360 (Category B code residue) queued.
- **Social Systems Expansion (Now):** THR-28/27/30/51/29/41/34/31/35 shipped. ✅ THR-254/253. THR-78 queued.
- **Thematic Pressure & Living World (Next):** ✅ THR-19/122/125/126/80/128/127. THR-87 blocked by THR-116.

- 🚧 2026-07-05: THR-631 **Phase A + Phase B (emission + consumption)** shipped, issue stays **In Dev** (Motive-Receipt Foreshadowing — Thematic Pressure & Living World; phased, no close keyword). **Phase A** killed the two visible bugs at root — typed-slot agreement realizer (`realizer.ts`) + composed-generic clause pool — so no encounter title lands in a place slot and agreement holds for he/she/they. **Phase B emission** kept the scorer's discarded per-candidate contribution terms as a `MotiveReceipt` on the agent node at the `phaseAgentDecision` selection commit (`motiveReceipt.ts` + `__DEBUG.getMotiveReceipt`). **Phase B consumption (this run)** makes the receipt *drive the prose*: `composeReceipt.ts` renders S1 knowledge (real intel tier) → S2 pull (top contribution **kind**) → S3 expectation (real forecast tier + low-intel hedge) → optional S4 stake (2nd kind, weight-gated); `readMotiveReceipt.ts` wires **both** resolvers to the same receipt (one causality source, two surfaces); new `MOTIVE_CLAUSES`/`EXPECTATION_BY_FORECAST`/`STAKE_CLAUSES` content (≥3–4 variants, THR-609 plain, agreement-safe); `tooltipProse` (S2-only) + trace `receipt`/`compositionKeys` additive; `__DEBUG.getForeshadowing` returns them. Self-maintaining agreement sweep in `composeReceipt.test.ts` (14 tests, derives conjugations from the real `conjugate`). Full suite **12362 green (818 files)**, vite build green, 0 net-new `tsc -b` (3544→3542), 30-tick CLI smoke clean (389 agents; 16 live receipts by tick 40; receipt-driven prose rendered end-to-end for he/she/they). Engine+content+debug only → browser-verify exempt. **Remaining (In Dev):** full resolver module-merge + tooltip-component S2 render + DebugPanel receipt table (UI slice); Phase D wiring docs. (Scorer-sourced divine/bond/reputation/proximity kinds shipped as THR-641; Phase C content depth shipped as THR-640.)
- 🚧 2026-07-05: THR-611 Slices 1–5 shipped, issue stays **In Dev** (Divine Economy — Essence Sources; multi-session core, no close keyword). **Slice 1 = ENGINE SUBSTRATE:** an `essenceSource` property bag on existing host nodes (no new node/edge type — a generalization of the place of power): `SourceKind`/`SourceTier`/`EssenceSource` types, pure `deriveSourceTier`/`sourceTierMultiplier`/`sourceDepthMultiplier`/`BASE_SOURCE_INCOME`, graph logic `computeSourceIncome` (typed per-sphere income + portfolio DR) / `migrateControlledPlacesOfPower` (income-neutral) / `recomputeControlledSourceTiers`, a `phaseEssenceSources` tick phase (O(controlled hosts), one aggregate trace/tick), typed income folded into both `computeEssenceGeneration` + `computeEssenceIncome` (legacy income provably unchanged), `__DEBUG.getEssenceSources()`. **Slice 2 = the BUILD → DEFEND VERB LOOP:** three new graph-op kinds (`consecrate_source` types a controlled host to the ascendant's primary sphere + ensures a `controls` edge so typed income begins; `sanctify_source` raises sanctity toward flowering ~2× income; `defend_source` clears contestation/desecration + restores sanctity) with fail-soft executor cases (tier re-derived via the Slice-1 helpers; new `SANCTITY_DEFEND_RESTORE`), plus three Star-reach ascendant templates (`loc.consecrate_source`/`loc.sanctify_source`/`loc.defend_source`) with real step ops and Threadbare plain-register prose — the user directive ("actions that progress essence income") made real. **Slice 3 = VERB SURFACING:** one investment Ascendant Beat `beat.pool.invest.the_wellspring` ("The Wellspring") grants the three source verbs via the shipped `resolvePendingBeat` path — three `unlockable-generic` `ASCENDANT_ACTION_BUCKETS` entries (universal, sphere-flavored, not two-domain reach-locked — `reach:'star'` is the cosmic axis, not a `requiresReach` gate) + a Threadbare plain-register content template; empty THR-501 starter floor left intact (surfacing is the earned beat grant). **Slice 4 = the FIND → CLAIM front half:** worldgen seeds a small number of **latent** `placeOfPower` sources (`seedLatentEssenceSources`, `LATENT_SOURCE_SEED_COUNT=6`) onto wild-interest locations — hidden (`discoveredBy` undefined) + typed by their own **locale** sphere; two new graph-op kinds `find_source` (reveal latent sources within `SOURCE_DISCOVERY_RANGE_HOPS=3` hexes) and `claim_source` (bind a *discovered* source with a `controls` edge → typed income flows; Find→Claim prerequisite), two Star/Eye templates (`loc.find_source`/`loc.claim_source`) added to `the_wellspring`'s grant (now the whole find→claim→build→defend loop) + `unlockable-generic` buckets. A claimed ley-nexus pours its country's sphere (distinct from a consecrated shrine → the god's sphere). **Slice 5 = DEBUGPANEL ESSENCE-SOURCES TAB (first UI-pillar slice):** a new `EssenceSourcesDebugTab` (registered in `DebugTabContent`) reads live graph state and lists **every** host with an `essenceSource` bag — latent / discovered / controlled — with kind, derived tier (colour-coded), sphere typing, sanctity, contested/desecrated state, plus the ascendant's typed per-sphere source income; makes the whole seed→find→claim→build→defend substrate legible in one place (the `getEssenceSources` bridge only showed *controlled* sources). Cumulative: full suite **12325 green (814 files)**, vite build green, 0 net-new tsc errors (3544=3544 baseline). Browser-verified @1920×1080 (seeded medium): tab renders live "6 sources — 0 controlled, 0 discovered, 6 latent" (the Slice-4 seed) with per-source cards locale-typed (energy/time/entropy/life/matter); console 0 errors; `__DEBUG.getEssenceSources()` = `{sources:[],sourceIncome:{}}`. **Remaining THR-611 slices:** (a) **Player-UI** (portfolio income-by-source panel, WebGL hex source signifiers, IPK/codex/chronicle-tier-transition, `source_*` trace categories); (b) **discovery/consecration/defend encounters** + rulebook Divine Economy section + Game-Manual-Wiki essence page.
- 🚧 2026-07-05: THR-613 **Slice 1 (engine substrate)** shipped, issue stays **In Dev** (Player Action Progression v1 — Action System & Unlocks; phased multi-session core, no close keyword). God-side capability growth: `reachPractice` accrues per resolved in-domain player action (`accruePlayerReachPractice` in `unifiedActionResolution`; `PLAYER_PRACTICE_PER_ACTION × difficultyScaling × diminishing`, secondary reach lags), folded into Domain Capability via one added term in `computeRawScore` → the same agent sigmoid (one source of truth). New leaf phase `phaseAscendantProgression` (before the Beat Director) derives live tier per permanent reach, compares a persisted `reachTierSnapshot`, and on an upward crossing sets `ascendantBeats.pending` to a **Deepening beat** (new additive `'deepening'` `BeatKind`, `beat.deepening.<reach>`) — Director skips on `pending`, so Deepening preempts the cadence draw with zero Director change (mutex-safe seam w/ THR-611). Snapshot seeds silently on first run; ≤1/tick; unenqueuable crossing re-detects next tick (no double-fire); gated to spine-exhausted + no-pending. `src/data/player-progression.ts` tunables (+ CMS registry); traces `ascendant.progression.practice`/`tier_up`/`deepening_enqueued` (+ `control_release` declared for Slice 4); `__DEBUG.getAscendantProgression()`. 11 contract tests (Done-when: a tier crossing fires exactly one enqueue). Full suite **12373 green (819 files)**, vite build green, 0 net-new `tsc -b` (3542=3542), 30-tick CLI smoke clean (389 agents). Engine + data + debug + one `AscendantBeatModal` presentation entry → browser-verify exempt. **Remaining THR-613 slices:** Slice 2 content (8 Deepening beats + milestone/discovery breadth, coord. THR-611); Slice 3 UI legibility (ActionDrawer three-state grammar + ascendant-bar tier readout + Deepening modal); Slice 4 Covenants panel + `release_control`.
- ✅ 2026-07-19: THR-605 **COMPLETE — all 6 no-op ascendant actions wired** (Six no-op ascendant actions — real effects; Action System & Unlocks; closes via `Fixes THR-605`). Slices 1–3 (graph-executor-case ops) + Slice 4 (resolution-intercept `plant_trap`) all shipped; systemic-wiring-guide 6-op aggregation added. **Slice 4 — `plant_trap` (`sub.trap`):** the one op needing full `GameState` (mutates `state.pendingEncounterSeeds`), so it rides the resolution-intercept path (mirrors `imbue_item`) not a graph-executor case. `applyPlantTrap` resolves the intended victim present at the trapped sublocation (or its hex, hex-granular) and seeds the authored `encounter.trap.sprung` beat against them → `evaluateEncounterSeeds` spawns a real failable negative encounter whose harm lands via `condition`-weighted reward pools (the faction-encounter substrate; no bespoke harm op). Substrate correction (grep-verified): the seed pathway has no hex-arrival gate, so the plan's "fires on the next agent to arrive" was unreachable — target a present victim instead. Catalog `none` 3→2; the residual two (`sub.sanctify`/`sub.sanctify_tavern`) are out of this issue's 6-action scope (flagged by THR-604) → follow-up **THR-662**. 7 new fail-soft tests + 27 targeted green; vite build green; `tsc -b` net-new 0 (3542=3542); 30-tick CLI smoke clean (389 agents). Engine + content-data only → browser-verify exempt. Prior slices for reference — **Slice 1 — `loc.fortify`:** new composed GraphOp `fortify_location` (graph-executor case, auto-routed via `graphOnlyOps` — graph+ctx only, mirrors the THR-611 essence ops) raises the target location's `fortificationMultiplier` (seed from subtype base → +`FORTIFY_MULTIPLIER_BONUS` per cast → clamp `FORTIFY_MULTIPLIER_MAX`; consts in `types/battle.ts`); closed the substrate gap — siege **setup** momentum (`siegeResolution`) now reads `fortificationMultiplier ?? base` (was base-only). **Slice 2 — artifact trio (`attune`/`curse`/`nullify`):** three graph-executor ops writing the same `properties.effects[]` the effect walker reads off any possessed/bonded artifact (genuinely consumed). `attune_artifact` appends the ascendant's-sphere positive passive (RNG-free `SPHERE_EFFECT_TABLE[sphere][0]`) + stamps `attunedSphere`; `curse_artifact` appends a concealed per-tick quintessence drain (`CURSE_QUINTESSENCE_DRAIN`, inverse of the bestow regen boon) + `cursed`/`curseConcealed`; `nullify_artifact` clears `effects[]` + flags to inert (inverse of imbue/attune/curse). Plan substrate corrections: dropped the `aligned_with` edge (schema forbids artifact sources + unread for artifacts = theatre), dropped the non-existent "enchant tier property"; deferred the curse holder-hidden-mark (needs GameState/resolution-intercept) to **THR-661**. All three `onSuccess`-wired + authored `technicalEffect` → catalog badges flip `not wired` → `wired · template` (`0 unauthored effects`). 10 new fail-soft tests; targeted 2355 green, vite build green, `tsc -b` 0 net-new (3542=3542), 30-tick CLI smoke clean (389 agents). Engine+content-data only → browser-verify exempt. **Remaining THR-605 slices:** `scry_sublocation`, `plant_trap` + trap beat, + systemic-wiring-guide 6-op aggregation.
- ✅ 2026-07-19: THR-641 shipped (Motive-Receipt Foreshadowing deferral — Thematic Pressure & Living World) — **scorer-sourced contribution kinds.** The four folded scorer terms now surface as labeled `MotiveReceipt` contributions, so all 14 receipt kinds are reachable: `divine` (value-overlay delta, computed by resolving the profile with vs. without the divine overlay — gated to agents actually under influence), `bond` (hoisted `computeBondModifier`), `reputation` (`computeReputationScoringBonus`), `proximity` (new `PROXIMITY_RECEIPT_SCALE / (1 + hexDistance)`, receipt-only descriptive). Four raw fields added to `ScoredCandidate`; `extractRawTerms` owns presentation scaling (mirrors rarity). No new clause content (entries already authored) — clauses fire automatically via `composeReceipt`'s dynamic kind lookup. 12485 tests green (828 files, +4 new), vite build green, `tsc -b` net-new 0 (3542=3542), 30-tick CLI smoke clean (389 agents); live proof: `proximity` now appears in real receipts (5 of 19 at tick 60). Engine + debug only → browser-verify exempt (issue-declared).
- ✅ 2026-07-05: THR-636 shipped (Encounter Experience) — **Encounter context UX.** Structured encounter toast cards + EncounterVeil context strip (portrait/name → detail, location + "Show on map", reach chip) + outcome-coloured interactive step navigator with resolved-step **replay**. Engine `StepProseRecord` capture-at-resolution (seeded rng threaded into `enrichProse`; legacy path summary-only by design) + `stepProseHistory?` on `UnifiedAction` + notification context fields (`totalSteps/outcomeBand/hex*/locationLabel` via `resolveLocationToHex`) + `encounter_step_prose_recorded` trace + `__DEBUG.getStepProse` + `OUTCOME_BAND_WORDS` lexicon. Aligned `StepProseRecord` fields with `ChapterStepRecord` (THR-603) per plan. 12362 green (818 files), vite build green, `tsc -b` 0 net-new (3542=3542), 30-tick CLI smoke clean (389 agents); browser @1920×1080: 0 console errors + `__DEBUG.getStepProse` wired — **live-surface screenshots deferred** (scripted-onboarding gating; unit tests cover render).

## Archived to project-history.md
- 2026-07-05 batch (THR-613 Slice-1 closeout): THR-639 (full detail in project-history.md)
- 2026-07-05 batch (THR-611 Slice-1 closeout): THR-609, THR-66 (full detail in project-history.md) · (THR-612 closeout): THR-608 (user-review-interface docs, Continuous Improvement)
- 2026-07-05 batch (THR-631 Phase-B closeout): THR-610 · (THR-610 closeout): THR-571, THR-584 · 2026-07-04 batch (THR-571 closeout): THR-590, THR-588, THR-587 (full detail in project-history.md)
- 2026-07-04 batch (status trim, THR-604 closeout): THR-580 (full detail in project-history.md)
- 2026-07-04 batch (status trim, THR-590 closeout): THR-586 (full detail in project-history.md)
- 2026-07-04 batch (status trim, THR-588 closeout): THR-562, THR-561
- 2026-07-04 batch (status trim, THR-587 closeout): THR-541, THR-523
- 2026-07-04 batch (status trim, THR-586 closeout): THR-554, THR-555 · (THR-585 closeout): THR-553, THR-552
- 2026-07-02 batch (status trim, THR-562 closeout): THR-470, THR-529 · (THR-561 closeout): THR-530, THR-528
- 2026-07-02 batch (status trim, THR-559 closeout): THR-527, THR-531
- 2026-07-01 batch (status trim, THR-541 closeout): THR-539, THR-536
- 2026-07-01 batch (status trim, THR-553/555 closeout): THR-534, THR-466, THR-533
- 2026-07-01 batch (status trim, THR-552 closeout): THR-520, THR-521, THR-510
- 2026-07-03 batch (status trim, THR-580 closeout): THR-545 · 2026-06-30 batch (THR-544/561 closeout): THR-544, THR-516, THR-514
- 2026-06-30 batch (status trim): THR-513, THR-512, THR-511, THR-506, THR-505, THR-504
- 2026-06-28 batch (status trim): THR-509, THR-494, THR-503, THR-500, THR-502, THR-501
- 2026-06-23 batch (status trim): THR-476, THR-488, THR-486, THR-487, THR-492
- 2026-06-27 batch (status trim): THR-517, THR-481, THR-477, THR-491, THR-479, THR-472, THR-475, THR-473, THR-463, THR-462, THR-508
- 2026-06-13 batch (status trim): THR-461, THR-460, THR-250, THR-378
- 2026-06-12 batch (status trim): THR-451, THR-62, THR-453, THR-455, THR-456, THR-442, THR-449, THR-446, THR-381
- THR- and earlier — see project-history.md
- 2026-05-06 batch: THR-311, THR-312, THR-313, THR-314, THR-315, THR-316, THR-317, THR-320, THR-321, THR-322, THR-323, THR-336
- 2026-05-07 → 2026-05-12 batches: see `Docs/project-history.md` (pointer-only; full detail archived there)

## Active Backlog Ideas
- **TB-105–108 Thematic Pressure & Living World Pass** (omen agendas, cool failure, doom identity, intent/activity visibility)
- **TB-095–099 Social Systems Expansion** (v1.2 — designed, ready to sequence)
- TB-071 Economy Second Pass · TB-069 Location NPCs · TB-051 Monster Encounters residual · TB-037 Onboarding

Full backlog: [Linear (Threadbare team)](https://linear.app/threadbare) · Completed work: `Docs/project-history.md` + Linear "Done" state · Pre-Linear history: `.planning/BACKLOG_HISTORY.md`
