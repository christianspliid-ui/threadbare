# Project Status
> Updated 2026-07-04 (THR-597 — **Game Manual Wiki W12 — world-map-reference "The World: Hexes, Terrain & Sight".** Twelfth Manual page (Tier 2, living world): the hex world as a grid you can shape; the three nested position tiers (hex → location → sublocation, one rung at a time); terrain and how divine influence, corruption, and per-location magical saturation remake it; and the **two distinct hex-distance sight systems** — the god's fog of war (`visibility.ts`: unexplored → visible → remembered; avatar/retinue/scry LOS ranges 0/1/0) versus each mortal's own encounter awareness (`encounterAwareness.ts`: per-reach hop budget, rejected location-hop). Designer Notes lead with the load-bearing decision — **hexes are flat `GameState.tiles[]` array state, not graph nodes** (hex actions produce `HexMutation[]` applied in `phaseHexState`); magical saturation clarified as a per-*location* property (not per-hex-tile) to avoid the common conflation. 9 verified `sources` globs (all 5 plan globs resolved; enriched with the `visibility`/`hexMutation` types, `terrain-transformation-content`, `hexMath`). `generate-design-wiki` re-injected nav (15 pages). Full suite **12189 green (802 files)**, vite build green, `tsc --noEmit` exit 0, `check:design-wiki`/`check:wiki-freshness` OK (public/JSON only — no `src/` runtime, engine-smoke N/A). Browser-verified @1920×1080 (Playwright on built `dist/` via literal static server on a probed-free port — #167 hijack sidestepped): nav active W1→W12, 4 player sections, 3 tier rows, 4 callouts, 14 flags (9 IMPL/2 DESIGN/3 OPEN), 14-row constants table, no horizontal overflow (1905≤1920); sole console entry an environmental favicon 404. Prior: THR-596 — Game Manual Wiki W11 factions-cultures-reference "Factions, Cultures & Secrets" (grep-corrected two agent status verdicts: schism weights + secrets/favors both IMPL).)

## Current Focus
**Agent Personality — the Core foundation layer (THR-542, all 4 slices shipped):** a second, more fundamental personality layer beneath the 8 reach axes — five plain virtue↔vice continuums of *who an agent is* (`coreProfile`: Warm↔Cold, Hopeful↔Bitter, Forgiving↔Vengeful, Humble↔Proud, True↔False), kept distinct from reach (`axiologicalProfile`) and capability (`domainCapabilities`). Engine foundation (slice 1) live: registry, seed/colour/bend mechanics, the `core_personality` tick phase. **Content (slice 2 / THR-544) live:** a 60-entry Core origin-vignette library (`core-origin-vignettes.ts`) now layers authored character on top of the PRNG seed at birth (`seedCoreProfileWithVignettes`, recorded on `coreOriginVignettes`), and 10 Core emergent-trait defs (`core-trait-content.ts`, new `subcategory:'core'`) are granted/released by the phase on emergence/fade (silent born-extreme grant). Canon-safe framing preserved: Core = *which way* you bend, distinct from Quintessence = *how bendable* — goodness never a Quintessence pole; Core traits carry no scoring/capability. **Star re-scope (slice 3 / THR-545) live:** with the inner-outlook reading now owned by Core `core_hope` (Hopeful↔Bitter), Star's reach axis re-points from Beacon/Inspiring↔Anchor/Discouraging to **Beacon (Guiding) ↔ Wrecker (Misleading)** (wayfinding/fate) across `axisRegistry.ts` + its independent test oracle, `Docs/canon/cosmology.md` & `encounters.md` Star rows, and the THR-524 canon table; the 18 Star origin-vignettes re-themed from morale/hope (now duplicate of `core_hope`) to wayfinding. **UI (slice 4 / THR-546) live:** the agent character sheet (`OverviewTab`) now renders a **Core** section — the 5 continuums as labelled tracks (virtue↔vice) with a marker per value, positioned above Nature so the layering reads bedrock→expression; `coreProfile` is surfaced through `AgentInfoCardData` at intimate+ knowledge (same gate as `axiologicalProfile`). Deferred: **THR-547** (re-theme the comet encounter's pre-THR-524 Wanderer/Anchor framing).
**Repo Health — `npm test` green on main (THR-489):** the months-long red baseline is cleared — full suite **11966 green (783 files)**, stable ×3. Resolved by the WS1–WS3 consolidation + intervening fixes; the live run reconciled clean against the historical known-failing checklist (no `.skip` masking). CI's required "Test · Typecheck · Build" gate is trustworthy again; merge=Done is load-bearing. Triage/closure doc: `Docs/plans/2026-06-30-test-suite-stabilization-triage.md`. (`tsc -b`'s ~3494-error baseline is a separate concern, out of scope.)
**Ascendant Beats — Divine Cadence (new):** Director + primitives + reach gate + home seat + debug + turn-1 floor + full expression toolkit (imbue/consecrate/bestow/anoint) + pool library + delivery adapter + beat resolve path shipped. **Beats are playable end-to-end** (offer→enter→resolve) and **pool beats now read bespoke per run (THR-514):** each cadence pool beat is backed by a Threadbare-voice content template with `enrichProse` placeholders, rendered enriched in the beat modal. **Onboarding spine complete (THR-504):** the full 5-beat scripted arc (The First → The Seat → A Thing Left Behind → The First Word → A Path Opens) ships with authored Threadbare prose. **Director pool draw eligibility-filtered + identity-biased (THR-516).** **Beat-driven graph seeding (THR-520):** resolving Beat 1 seats the throne (mana income ticking) at The First's settlement; Beat 2 mints + threads a sphere-flavored artifact — no manual card-firing. **Expression cards bucketed + pool-granted (THR-515):** consecrate/bestow/anoint now sit in `ASCENDANT_ACTION_BUCKETS` (`unlockable-generic`) and are granted by three deeper-expression investment pool beats (pool 14→17). **Consecrate relic variant shipped (THR-518):** `action.consecrate-relic` mints a permanent relic at establishment via `ControlSpec.mintUpkeepRelic` (handled in `spawnControlEffect`), waiving all per-tick upkeep while the relic lives and lapsing on its destruction — a pure-data second card, granted alongside `action.consecrate`. **Pool-beat template aftermath + introduction-group binding (THR-522, last engine tail):** `resolvePendingBeat` now runs a matched template's `aftermathConfig.fallback.reactions` through the shared aftermath resolver on resolution (additive; grant-only fallback when none declared) — beats can express `unlock_action`/`encounter_seed`/graph ops via their template, not only the descriptor. Introduction beats bind their specific un-introduced culture/faction into `PendingBeat.boundNodeIds` at offer time and name it via the new `{group}` enrichment placeholder (inspectable in the DebugPanel Beats tab). **Reach signatures acquired end-to-end (THR-523):** the 8 `invest.<reach>.<name>` signatures are catalogued `reach-gated` in `ASCENDANT_ACTION_BUCKETS` and granted per-run via `BeatDefinition.grantsReachSignature` — Beat 4 grants the primary, a new `beat.pool.invest.reach_signature` pool beat grants the secondary (gated by the new `unacquired_reach_signature` predicate). Remaining: per-beat identity tags (THR-514). **Reach signatures (THR-548/549/550):** sphere-power scaling helper + per-(reach×sphere) individualization scaffold + Iron's `signature_warhost` (mark faction `mobilized` + raise an army-node force) and Veil's `sphere_influence_amplify` (THR-551 — sustained sphere rift: ControlEffect amplifies local sphere influence up to a cap + seeded per-tick chaos-pulse leak, all sphere-scaled) shipped; **Stone's `spawn_unique_location` (THR-552) shipped** — mints a one-of-a-kind `location` (unique flag + `controls` edge), dedup by `uniqueTag` (idempotent), optional legendary-relic forge reusing the `spawn_artifact` path.
**Onboarding & First-Run Experience:** THR-504 scripted spine beats 0–4 shipped — the opening arc that delivers the run's first action cards.
**Action System & Unlocks:** THR-501 turn-1 floor (Move + Investiture only across all surfaces) shipped. THR-519 self-updating Action Catalog shipped — `scripts/generate-action-catalog.ts` emits `public/action-catalog.generated.json` from the live registry (164 ascendant-facing templates, code-true buckets incl. Beats verbs); `public/action-catalog.html` renders it (filter/sort/drawer), wired into the Design Reference Wiki.
**Encounter Experience — active:** Phases A1–A3, B1–B7, C1–C4, D1–D3, E1–E2, F1–F2, G1–G3 ✅. Remaining is F-phase integration (mount EffectRegistration components into hero panel / cast tile / scene state surfaces) and Phase H post-v1 polish.

## Milestone Status
- **v1.0 Foundation:** Shipped 2026-03-30 — Phases 1-18 + M2.5 (81 plans, 1533 commits)
- **v1.1 Optimization:** Shipped — Phases 19-22 (determinism, wiring, performance, hygiene)
- **Encounter Format Migration (Now):** ✅ Phases 0-9 complete. All guilds migrated.
- **Content Architecture (Now):** ✅ THR-86/88/239 shipped.
- **UI Visual Overhaul — Design System v1 (Now):** ✅ Project complete. THR-178 Deferral shipped 2026-05-08.
- **Continuous Improvement (Now):** THR-303/305/306/307/309/311/312/313/314/315/316/354/355/356/357/358/359/266/396/404/510 ✅. THR-304 Phase 5 series complete (5a vault THR-356 ✅, 5b repo THR-359 ✅). THR-360 (Category B code residue) queued.
- **Social Systems Expansion (Now):** THR-28/27/30/51/29/41/34/31/35 shipped. ✅ THR-254/253. THR-78 queued.
- **Thematic Pressure & Living World (Next):** ✅ THR-19/122/125/126/80/128/127. THR-87 blocked by THR-116.

- ✅ 2026-07-04: THR-584 shipped (Agent Success Redesign) — **Critical afterimages for the top-20 hot encounter templates (outcome-ladder §C2).** Authored both crit bands (`criticalSuccessAfterimage`/`criticalFailureAfterimage`) on the climactic step of the 20 highest-selection-share templates (from the committed `2026-07-03-kpi-report.json` `templateConcentration` funnel, seeds 42/99/7): 13 linear in `src/data/encounter-content.ts` + 7 branching in `src/data/encounters/*.ts`. Fields already existed + consumed at `encounter.ts:100-103` (`selectBandAfterimage`) — this fills the rare tails so a crit no longer falls back to generic prose once THR-571 revives them (the sparse-authoring flag from W7/THR-592). Small additive wiring: exposed the two fields on the `EncounterEntry` step type + mapped through `toUnifiedTemplate` (legacy raw format the 13 linear templates use; `ActionStep` runtime schema untouched). Voice per `Docs/canon/prose.md`: crit-success = rare brilliance, crit-failure = **cool failure with a hook** (a doubt that travels home, a rival who now knows, a seeded mistranslation). 56 afterimages (70 strings incl. branch fallbacks). **Verified:** 70 strings scored against the real `scoreProseEntry` prose-quality scorer — **all band PASS, 0 FAIL/0 WARN**, 20/20 covered; full suite **12189 green (802 files)** incl. `lint-encounter-content` + `unified-action-templates`; vite build green; `tsc --noEmit` exit 0; 30-tick CLI smoke clean (tick 30, 391 agents, no exceptions). **Browser-verify exempt:** content-only (prose-QA report is the artifact). Known gap noted: `collectAuthoredProse` doesn't sweep step afterimages, so they're absent from the DebugPanel QA corpus — verified via scoped scorer instead. Parallel-safe with THR-571 (content vs engine). PR pending.
- ✅ 2026-07-04: THR-590 shipped (Game Manual Wiki) — **W5: `public/threads-court-reference.html` — "Threads, the Court & The First".** Fifth Manual-section page, per plan `Docs/plans/2026-07-03-game-manual-wiki.md` §5 W5 (source spine `Docs/canon/rulebook.md` **§6 "Your Resources"** — the plan's "§5" predates the §5-Cosmology renumber). Dual-layer: player-manual "How it plays" (the thread as a **two-way bond** via paired ladders — your investment tiers Unaware→Curious→Recognized→Devoted→Enthralled vs the mortal's awareness unaware→intuition→faith→communion, independent clocks; the four **court positions** the_first/retinue/watched/dormant; **The First's** doom-clock-scheduled Campbellian arc Call→Road of Trials→Crisis→Ordeal→Return with the Return algorithmic; **familiarity** as *player-side* progressive disclosure stranger→transparent, held distinct from awareness; **the trade** — depth costs rising upkeep + a louder divine signature to rivals), then Designer Notes with 10 IMPL / 6 DESIGN / 3 OPEN flags: `ThreadEdgeProperties`/`InfluenceTier 0–4`/`CourtPosition`/`CampbellianPhase`/`AspectEdgeProperties` (`src/types/influence.ts`), maintenance/`checkTierPromotion`/`dropAgent` consequence ladder (`src/engine/influence.ts`), `TIER_MAINTENANCE`/`TIER_PROMOTION_THRESHOLDS`/economy constants (`src/data/influence-content.ts`), the doom-scheduled journey + `executeReturn` convergence (`journeyEngine.ts`/`returnEngine.ts`/`journey-content.ts`), and the familiarity fog-of-war engine + `FAMILIARITY_GAINS`/thresholds (`familiarity.ts`/`types/familiarity.ts`); a 9-row constants table + canon/UL/Deep-Reference links. **Two honest drift notes surfaced (not minted as open Qs):** code `TIER_NAMES` (`{Unaware,Touched,Devoted,Champion,Aspect}`) still lags the rulebook-resolved five-name ladder + separate-Aspect-apex verdict (Open Q #1, code follow-up pending); the influence `CourtPosition` name-collides with the encounter-role "Court Position" in `Docs/ubiquitous-language/Encounters.md` (UL-disambiguation candidate). The plan's one-line "familiarity — how mortals come to know you" is corrected: the `familiarity` engine is *you* knowing *them*, the mortal-knows-god direction is the `awareness` field. Registered `section:"Manual"` as the 5th entry (after W1–W4, preserving W1→W5 reading order; Manual heads the hub) with 9 `sources` globs (all verified to resolve, per the W4 dead-glob precedent); `npm run generate-design-wiki` regenerated the hub + re-injected nav into all 8 pages. Style matches the parchment/Cinzel W1–W4 pages verbatim (retained the Google-Fonts `@import`; new `.bond` paired-ladder / `.court` / `.journey` grids, reused `.callout`). **Verified:** full suite **12183 green (801 files)**; vite build green (page in `dist/`); no `.ts` touched (tsc N/A — `public/` HTML + manifest JSON only; net-new type errors structurally impossible); `check:design-wiki` OK (8 pages), `check:wiki-freshness` OK. Browser-verified @1920×1080 (Playwright against the built `dist/` served with a literal static server on a unique port — port-hijack precedent #167; static DOM page, no WebGL): screenshot + DOM assertion (nav injected & "Threads, the Court & The First" active in W1→W5 order; 5 player sections; 2 bond ladders; 4 court cards; 5 journey phases; 10/6/3 flags; designer-notes open; 9 constants rows; `documentElement.scrollWidth 1905 ≤ innerWidth 1920`, no overflow); sole console entry an environmental `favicon.ico` 404. Mutex with THR-585 + other Manual pages (shared manifest). PR pending.
- ✅ 2026-07-04: THR-588 shipped (Game Manual Wiki) — **W3: `public/turn-structure-reference.html` — "The Three-Beat Turn".** Third Manual-section page, per plan `Docs/plans/2026-07-03-game-manual-wiki.md` §5 W3 (source spine `Docs/canon/rulebook.md` §3). Dual-layer: player-manual "How it plays" (the turn is turn-based — the world waits until you advance it, "do nothing is always valid"; scan → curated moment → aftermath as three beat-cards; why the order is load-bearing — scan-before-moment = you chose to look, moment-before-aftermath = consequences anchored to a witnessed chapter, aftermath-before-next-scan = the world already moved; time in ticks — 2 in-world hours, 12/day — via a dawn-to-dawn time strip; encounters as rationed chapters, 3–6 per session planned target, not per-tick), then Designer Notes with 3 IMPL / 4 DESIGN / 2 OPEN flags: the `runTick(state, scryTargets, runtime): GameState` single-step contract (`src/engine/orchestrator.ts`), `TICKS_PER_DAY`=12 / `ATTENTION_BASE_CAPACITY`=6 (`src/data/attention-constants.ts`), the scan/portfolio surface [DESIGN] (Attention Tier Model project), the 3–6/session pacing target [DESIGN] (`Docs/plans/2026-05-04-encounter-experience-design-plan.md`; rulebook Open Q #4 deferred to KPI), a pointer to the Tick Cycle Reference for the full phase order, a constants table, and canon/UL/Deep-Reference links (no new open questions minted). Registered `section:"Manual"` as the 3rd entry (after W1/W2, preserving W1→W3 reading order; Manual still heads the hub) with `sources:["src/engine/orchestrator.ts"]`; `npm run generate-design-wiki` regenerated the hub + re-injected nav into all 6 pages. Style matches the parchment/Cinzel W2 page verbatim (kept the Google-Fonts `@import` for cohesion). **Verified:** full suite **12183 green (801 files)**; vite build green (page copied to `dist/`); `tsc -b` **0 net-new** (no `.ts` touched — all baseline errors in `src/types/*.ts`, THR-489); `check:design-wiki` OK (6 pages), `check:wiki-freshness` OK. Browser-verified @1920×1080 (Playwright against the built `dist/` served with a literal static server on a unique port — the shared preview port hijack precedent, impediment #167; static DOM page, no WebGL): screenshot + DOM assertion (nav injected & "The Three-Beat Turn" active in W1→W3 order; 5 player sections; 3 beat cards Scan/Curated Moment/Aftermath; 3/4/2 flags; designer-notes open; 3 tunables rows; `documentElement.scrollWidth 1905 ≤ innerWidth 1920`, no overflow); sole console entry an environmental `favicon.ico` 404. Mutex with THR-585 + other Manual pages (shared manifest). PR pending.
- ✅ 2026-07-04: THR-587 shipped (Game Manual Wiki) — **W2: `public/cosmology-reference.html` — "Reaches, Spheres & Quintessence".** Second Manual-section page, per plan `Docs/plans/2026-07-03-game-manual-wiki.md` §5 W2. Dual-layer: player-manual "How it plays" (two orthogonal axes — Eight Reaches = what you do / Twelve Spheres = what fuels it; the Foundation four framed as elder magic met in the world, not chosen at remembrance, + Creation-eight opposition pairs; an orthogonality-in-play worked example; each Reach's virtue↔vice archetype axis with plain behavioral words; the Domain Capability 10-rung word-ladder shown via Iron Frail→Cataclysmic; Quintessence as narrative centrality/threadbare-ness, explicitly **not** a ninth Reach, with the Flesh-migration note), then Designer Notes with 7 IMPL / 3 DESIGN / 2 OPEN flags sourced from `Docs/canon/cosmology.md` (authority) + `ReachDomain`/`NARRATIVE_LEXICON` (`src/types/traits.ts`), `SPHERE_OPPOSITES`/`SPHERE_ALLIES`/`COSMOLOGY_PRESETS` (`src/engine/cosmology.ts`), `phaseQuintessence`/`phaseSphereAggregation`/`capabilityGrowth`, `QUINTESSENCE_THRESHOLDS` (`src/types/quintessence.ts`), plus a constants table and canon/UL/Deep-Reference links. Foundation-not-at-chargen flagged **DESIGN** (framing, no hard-coded lock). Registered `section:"Manual"` as the 2nd entry (after W1, preserving W1→W2 order; Manual still heads the hub); `npm run generate-design-wiki` regenerated the hub + re-injected nav into all 5 pages. Style matches the parchment/Cinzel W1 page verbatim (kept the Google-Fonts `@import` for cohesion). **Verified:** full suite **12183 green (801 files)**; vite build green (page copied to `dist/`); `check:design-wiki` OK (5 pages), `check:wiki-freshness` OK. Browser-verified @1920×1080 (Playwright against the built `dist/` served literally — the shared preview port 5199 was held by a stale worktree; static DOM page, no WebGL): screenshot + DOM assertion (nav injected & "Reaches, Spheres & Quintessence" active in W1→W2 order; 7 player sections; 2 lore tables; 10 ladder rungs; 7/3/2 flags; designer-notes open; `documentElement.scrollWidth 1905 ≤ innerWidth 1920`, no overflow); sole console entry an environmental `favicon.ico` 404. Mutex with THR-585 + other Manual pages (shared manifest). PR pending.
- ✅ 2026-07-04: THR-585 shipped (Game Manual Wiki) — **W0: wiki freshness guardrail, manifest sections & plan-doc carrier.** Built plan `Docs/plans/2026-07-03-game-manual-wiki.md` §4. `public/wiki-manifest.json`: added optional per-page `sources` globs (the freshness contract), `Manual`/`Deep Reference` sections (existing 3 pages → Deep Reference, each with a tight `sources` glob), fixed `Nine`→`Eight Reaches` blurb, emptied backlog (`opening-system` absorbed by W1). New **advisory** `scripts/check-wiki-freshness.ts` + `npm run check:wiki-freshness`, chained into `check:process`: diffs the branch vs `WIKI_FRESHNESS_BASE` (=`origin/main`) and warns when a changed file matches a page's `sources` but the page HTML wasn't updated; constants `WIKI_FRESHNESS_MODE`(advisory|blocking)/`WIKI_FRESHNESS_BASE` (NFP #1); fail-soft on no-diff / malformed-glob / missing-manifest (NFP #4). `check-design-wiki.ts` extended to hard-validate the `sources` shape. `Docs/design-reference-wiki.md` documents the Manual section, dual-layer page template & freshness contract; CLAUDE.md gains the "Update the Design Reference Wiki" DoD bullet. Committed Cowork companion files the flush doesn't cover (game-manual-wiki brainstorm, judge-metrics 2026-W27). **Verified:** functional lint test (WARN@advisory exit 0, FAIL@blocking exit 1, reverts clean); `check:design-wiki` OK; `check:wiki-freshness` OK; full suite **12183 green (801 files)**; vite build green; generated hub asserted (Deep Reference header + 3 cards, no stale Systems/backlog/Nine-Reaches). Browser-verify exempt: manifest/tooling/docs, no `src/` UI path (plan confirms). Mutex with THR-586–602 (shared manifest). PR pending.
- ✅ 2026-07-03: THR-580 shipped (Engine Observability & Performance) — **Tick-loop observability instrumentation (E1–E5 + U1–U3) + large-map stall diagnosis.** Built the THR-570 plan. **E4** dedicated timing ring in `traceBuffer.ts` (`emitTiming`/`emitPhaseTiming`/`getTimingTraces`/`enable`+`disableProfiling`/`isProfilingEnabled`/`clearTimingTraces`, `TIMING_BUFFER_SIZE=4000`, profiling **off by default** — one boolean check per phase when off, NFP #7) so high-volume phase-profile traces stop evicting semantic traces from the shared ring; re-routed **all** `tick_phase_profile` (registered `runRegisteredPhases`, the 4 heavy inline suspects, + the 3 legacy actor-counter emitters) to it. **E1** `runInlinePhase()` in `orchestrator.ts` times `agent_decision`/`agent_movement`/`unified_action_progress`/`encounter_progression` (additive, preserves `phaseEventCounts`; measured around the thunk only — determinism, NFP #3). **E2** `distance_matrix_rebuild` trace + `SimulationRuntime.distanceMatrixRebuildCount` (`ensureDistanceMatrix` gains a `tick` arg). **E3** per-tick `tick_profile` rollup (total ms, slowest phase, agent count, rebuild flags). **E5** `aggregatePhaseTimings()` pure fn (avg/max/p95). **U1** PhasesDebugTab reads the timing ring: avg/max/p95 columns, inline-phase section, tick_profile header, profiling toggle. **U2** CLI `profile [N]`/`profile phases`. **U3** `__DEBUG.getPhaseTimings/enableProfiling/disableProfiling`. 4 contract tests migrated to `getTimingTraces()`. **Phase 2 diagnosis (THR-581):** seed 42 `--map large`, `profile 80` — `agent_decision` dominates (291ms avg, 459ms max @ ~1026 agents), and the slowest tick (40, 1499ms) had **no** DM-rebuild ⇒ the stall is super-linear `phaseAgentDecision`, not a rebuild storm; separately the distance matrix is capped at 1200 while large maps reach ~1777 locations mid-run (577+ unindexed → `getDistance→Infinity`). Deferrable ~46-inline-phase tail → THR-582. Full suite **12183 green (801 files)**, vite build green, `tsc -b` **0 net-new** (baseline red only; new files clean), engine smoke tick 30 (medium)/80 (large) clean. **Browser-verified @1920×1080:** Phases tab renders profiling toggle + tick_profile header + avg/max/p95 + "4 inline phases timed"; `__DEBUG.getPhaseTimings()` returns 24 phases after `enableProfiling()`+ticks; 0 console errors. Wiring-checklist updated (`tick_profile`, `distance_matrix_rebuild`). PR pending.
## Archived to project-history.md
- 2026-07-04 batch (status trim, THR-590 closeout): THR-586 (full detail in project-history.md)
- 2026-07-04 batch (status trim, THR-588 closeout): THR-562, THR-561
- 2026-07-04 batch (status trim, THR-587 closeout): THR-541, THR-523
- 2026-07-04 batch (status trim, THR-586 closeout): THR-554, THR-555 · (THR-585 closeout): THR-553, THR-552
- 2026-07-02 batch (status trim, THR-562 closeout): THR-470, THR-529 · (THR-561 closeout): THR-530, THR-528
- 2026-07-02 batch (status trim, THR-559 closeout): THR-527, THR-531
- 2026-07-01 batch (status trim, THR-541 closeout): THR-539, THR-536
- 2026-07-01 batch (status trim, THR-553/555 closeout): THR-534, THR-466, THR-533
- 2026-07-01 batch (status trim, THR-552 closeout): THR-520, THR-521, THR-510
- 2026-07-03 batch (status trim, THR-580 closeout): THR-545 · 2026-06-30 batch (THR-544/561 closeout): THR-544, THR-516, THR-514
- 2026-06-30 batch (status trim): THR-513, THR-512, THR-511, THR-506, THR-505, THR-504
- 2026-06-28 batch (status trim): THR-509, THR-494, THR-503, THR-500, THR-502, THR-501
- 2026-06-23 batch (status trim): THR-476, THR-488, THR-486, THR-487, THR-492
- 2026-06-27 batch (status trim): THR-517, THR-481, THR-477, THR-491, THR-479, THR-472, THR-475, THR-473, THR-463, THR-462, THR-508
- 2026-06-13 batch (status trim): THR-461, THR-460, THR-250, THR-378
- 2026-06-12 batch (status trim): THR-451, THR-62, THR-453, THR-455, THR-456, THR-442, THR-449, THR-446, THR-381
- THR- and earlier — see project-history.md
- 2026-05-06 batch: THR-311, THR-312, THR-313, THR-314, THR-315, THR-316, THR-317, THR-320, THR-321, THR-322, THR-323, THR-336
- 2026-05-07 batch: THR-324, THR-325, THR-329, THR-339, THR-340, THR-341, THR-349, THR-350, THR-354, THR-355, THR-356, THR-357, THR-358, THR-359
- 2026-05-08 batch: THR-139, THR-176, THR-177, THR-178, THR-179, THR-215, THR-265, THR-266, THR-267, THR-289, THR-292, THR-326, THR-330, THR-331, THR-332, THR-333, THR-334, THR-335, THR-338, THR-343, THR-344, THR-345, THR-352, THR-353, THR-360, THR-361, THR-362, THR-363, THR-365, THR-368, THR-374, THR-387, THR-388
- 2026-05-09 batch: THR-268, THR-389, THR-391
- 2026-05-11 batch: THR-393, THR-403, THR-408, THR-411
- 2026-05-12 batch: THR-413, THR-404, THR-397, THR-398, THR-399, THR-405, THR-416, THR-409
- 2026-05-16 batch: THR-416 (via THR-409 worktree cleanup closeout), THR-249, THR-248, THR-377, THR-447
- 2026-06-11 batch: THR-406, THR-450, THR-452, THR-457

## Active Backlog Ideas
- **TB-105–108 Thematic Pressure & Living World Pass** (omen agendas, cool failure, doom identity, intent/activity visibility)
- **TB-095–099 Social Systems Expansion** (v1.2 — designed, ready to sequence)
- TB-071 Economy Second Pass · TB-069 Location NPCs · TB-051 Monster Encounters residual · TB-037 Onboarding

Full backlog: [Linear (Threadbare team)](https://linear.app/threadbare) · Completed work: `Docs/project-history.md` + Linear "Done" state · Pre-Linear history: `.planning/BACKLOG_HISTORY.md`
