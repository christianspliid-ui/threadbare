# Project Status
> Updated 2026-07-04 (THR-585 — **Game Manual Wiki W0 — freshness guardrail, manifest sections, plan-doc carrier.** `public/wiki-manifest.json` gains optional per-page `sources` globs + `Manual`/`Deep Reference` sections (the 3 existing pages → Deep Reference, each with a tight `sources` glob); blurb drift `Nine`→`Eight Reaches` fixed; `opening-system` dropped from backlog (absorbed by the Manual's run-lifecycle page). New **advisory** `scripts/check-wiki-freshness.ts` + `npm run check:wiki-freshness` (chained into `check:process`): warns when changed code matches a page's `sources` globs but the page HTML wasn't updated in the same diff (`WIKI_FRESHNESS_MODE=advisory`, `WIKI_FRESHNESS_BASE=origin/main`; fail-soft on no-diff / bad-glob / missing-manifest; functional-tested WARN@advisory exit 0, FAIL@blocking exit 1). `check-design-wiki.ts` extended to validate `sources` shape. `Docs/design-reference-wiki.md` documents the Manual section + dual-layer page template + freshness contract; CLAUDE.md gains the "Update the Design Reference Wiki" DoD bullet (mechanizes the 2026-07-03 working agreement). Carried Cowork companions (game-manual-wiki brainstorm + judge-metrics 2026-W27). Full suite **12183 green (801 files)**, vite build green, hub regenerated (single Deep Reference section, 0 planned). Browser-verify exempt — manifest/tooling/docs only, no `src/` UI path; generated hub content asserted (Deep Reference header, 3 cards, no stale Systems/backlog/Nine-Reaches). Prior: THR-580 — **Tick-loop observability instrumentation (E1–E5 + U1–U3) + large-map stall diagnosis (Engine Observability & Performance).** Dedicated profiling timing ring (`traceBuffer`: `emitTiming`/`emitPhaseTiming`/`getTimingTraces`/`aggregatePhaseTimings`, off by default), `runInlinePhase()` timing the 4 heavy inline suspects, `distance_matrix_rebuild` trace+counter, per-tick `tick_profile` rollup, extended PhasesDebugTab (avg/max/p95 + inline section + tick_profile header + profiling toggle), CLI `profile [N]`, `__DEBUG.getPhaseTimings/enableProfiling`. **Diagnosis (THR-581):** large-map stall is `phaseAgentDecision` super-linear (~291ms avg @1026 agents), not a distance-matrix rebuild storm; matrix capped at 1200 while large hits ~1777 locations mid-run. Deferrable inline-phase tail → THR-582. Full suite **12183 green (801 files)**, vite build green, `tsc -b` 0 net-new, engine smoke tick 30/80 clean, browser-verified @1920×1080 (Phases tab + toggle render; `__DEBUG.getPhaseTimings()`=24 phases; 0 console errors). Prior: THR-579 — **Decomp Phase 2 step 1 (arch decomposition, THR-572): extracted the GameView top-bar cluster into `GameView/GameViewTopBar.tsx`.** First leaf of the `GameView.tsx` (3835-LOC god-component) leaf-first decomposition. The header/HUD strip — SimulationControls, WorldSoulIndicator, AttentionPoolIndicator, DoomBar, OmenIndicator, RivalsButton, Read-the-Threads icon, Settings gear + `SettingsPanel` — moved verbatim into a new presentational subcomponent under `src/components/Game/GameView/`, receiving `gameState` + discrete values/setters via `GameViewTopBarProps` (props-down; **all state stays owned by `GameView.tsx`, `gameState` shape untouched**). `GameView.tsx` shrinks −150/+38 lines; the 8 top-bar-only component imports relocated to the leaf. Pure extraction, zero behavior change. Full suite **12183 green (801 files)**, vite build green, `tsc -b` **0 net-new** (3548=3548, stash-diff proven). Browser-verified @1920×1080 (seeded medium run): top bar renders full-width (1920×94) at viewport top, settings-toggle opens `SettingsPanel` (Fog/Organic/Music/Debug labels), `__DEBUG.listActions()`=545, 0 console errors. Prior: THR-578 — **Decomp Phase 0 (arch decomposition, THR-572): refreshed CLAUDE.md high-impact importer counts via codesight** — `graph.ts` 370→531; the `# Codesight — Codebase Intelligence` list now covers all 10 files at ≥100 importers (5 newly crossed: `unifiedAction.ts` 278, `agent.ts` 129, `types/graph.ts` 125, `influence.ts` 107, `simulationRuntime.ts` 106), refreshed from codesight AST (not grep) + the stale "370" Blast Radius example. Doc-only; Browser-verify exempt. Prior: THR-577 — **Decomp Phase 1 (arch decomposition, THR-572): last two module-scope engine caches → `SimulationRuntime`.** The detail-page cache (`detailPageGenerator`) and entity-prose cache (`proseGenerator`) now live on `SimulationRuntime` as `detailPageCache`/`proseCache` (+ `detailPageCacheTick`/`proseCacheTick` guards, cleared by `resetRuntimeCaches`) — honoring the load-bearing "engine caches owned per session, not module scope" rule, killing cross-session bleed. Both accessors take an optional trailing `runtime` (present → cached, absent → composed fresh — matching the `foreshadowingCache`/`threadStoryCache` degradation); `runtime` threaded from `GameView` into `LocationView`/`HexChronicle`/`AgentInfoCard`. KPI's `BRANCHING_TEMPLATE_CACHE`/`RICH_TEMPLATE_CACHE` **intentionally** stay module-scope with a justification comment — provably static across sessions (pure function of the compile-time template registry, no bleed). Full suite **12183 green (801 files)**, vite build green, 30-tick CLI smoke (seed 42 medium, 391 agents, 53 events) clean. Browser-verify exempt: additive optional-prop threading, deterministic prose output byte-identical, covered by 72+ component tests. Prior: THR-567 — **Character-sheet personality section: layered born→marked→becoming→now (Agent Personality & Moral Drift, wave-2 #8 — final integration).** Extends the shipped `OverviewTab` Personality section (intimate+) so it tells the full moral story, not just the "becoming" chips. Two layers added: (a) **live per-axis position** — a new `MoralAxisRow` compact bar (mirrors the Core section's `CoreContinuumRow`; virtue=gold / vice=rose) per moral axis with signal, reading `card.axiologicalProfile` (signed ±1 → 0–1 via the canonical `signedToCanonical01` bridge); (b) **contributing rows** grouped under their axis — the enumerable "born as → marked by" provenance. New `card.personalityContributors` on `AgentInfoCardData` fed by an additive read-only `getPersonalityContributorDisplays` (`agentDetail.ts`, same intimate+ gate as `personalityTraits`): origin vignettes resolved from `node.originVignettes` via a new `getOriginVignetteById` (THR-561), **and** permanent traits carrying `axisContributions` (the slot a formative-mark trait uses). Landed-code correction: the THR-529 `axiological_mark_apply` effect moves the baseline *in place* with no trait/provenance, so its rows aren't enumerable — only the live position (axis bar) reflects it (documented in the builder). Emergent `personalityTraits` path + becoming beat untouched; near-neutral axes filtered by `AXIS_SIGNAL_EPSILON` so a bland agent isn't padded with 8 flat tracks. 4 new tests; full suite **12181 green (801 files)**, vite build green, `tsc -b` **0 net-new** (3547=3547, stash-diff proven). Browser-verified @1920×1080 (live seeded run, The First's `AgentProfileModal`/`OverviewTab` at intimate: 6 axis bars + 5 origin-vignette contributor rows grouped under their axes + 3 becoming chips; 0 console errors; page does not scroll). Prior: THR-562 — **Threshold → emergent personality traits: wave-2 #3 completion.** The engine phase already shipped under THR-527 (registered `personalityTraitEmergePhase`, hysteresis grant/release, 16 `subcategory:'personality'` trait defs, `personality_trait_emerged` trace + "becoming" TickEvent) and was re-based onto the unified live position by THR-559 — so #3's real delta was the **notification + UI pillar** the engine-only THR-527 left. **Notification:** the becoming `TickEvent` carried no `notification` directive, so `routeNotifications` silently dropped it — added `notification:{channel:'toast'}` to `becomingEvent()` (toast, not alert, so distinct same-tick becomings aren't collapsed by the alert icon+tick dedup; navigable to the agent via `actorId`) + a `personality_trait_emerged→'lifecycle'` case in `eventTypeToCategory`. **UI:** emergent personality traits now render in their own **Personality** section on the live character sheet (`AgentProfileModal`/`OverviewTab`, NOT the orphaned `AgentDetailPanel`) — virtue/vice-tinted chips (gold / muted-rose) with flavor-on-hover; new `card.personalityTraits` on `AgentInfoCardData` (populated intimate+ from `subcategory:'personality'` has_trait edges, pole/reach parsed from the canonical id) and pulled **out of** the generic `allTraits` chips so they aren't double-shown. `personality` added to the trait category color/label maps (CMS registry live badge + the orphaned panel, kept in sync). Browser-verified @1920×1080 (production `AgentProfileModal` rendered with a live-state transparent card — Personality section shows Disloyal/Misleading/Perceptive, excluded from Traits; Chronicle feed shows the "X has become …" beats).)

## Current Focus
**Agent Personality — the Core foundation layer (THR-542, all 4 slices shipped):** a second, more fundamental personality layer beneath the 8 reach axes — five plain virtue↔vice continuums of *who an agent is* (`coreProfile`: Warm↔Cold, Hopeful↔Bitter, Forgiving↔Vengeful, Humble↔Proud, True↔False), kept distinct from reach (`axiologicalProfile`) and capability (`domainCapabilities`). Engine foundation (slice 1) live: registry, seed/colour/bend mechanics, the `core_personality` tick phase. **Content (slice 2 / THR-544) live:** a 60-entry Core origin-vignette library (`core-origin-vignettes.ts`) now layers authored character on top of the PRNG seed at birth (`seedCoreProfileWithVignettes`, recorded on `coreOriginVignettes`), and 10 Core emergent-trait defs (`core-trait-content.ts`, new `subcategory:'core'`) are granted/released by the phase on emergence/fade (silent born-extreme grant). Canon-safe framing preserved: Core = *which way* you bend, distinct from Quintessence = *how bendable* — goodness never a Quintessence pole; Core traits carry no scoring/capability. **Star re-scope (slice 3 / THR-545) live:** with the inner-outlook reading now owned by Core `core_hope` (Hopeful↔Bitter), Star's reach axis re-points from Beacon/Inspiring↔Anchor/Discouraging to **Beacon (Guiding) ↔ Wrecker (Misleading)** (wayfinding/fate) across `axisRegistry.ts` + its independent test oracle, `Docs/canon/cosmology.md` & `encounters.md` Star rows, and the THR-524 canon table; the 18 Star origin-vignettes re-themed from morale/hope (now duplicate of `core_hope`) to wayfinding. **UI (slice 4 / THR-546) live:** the agent character sheet (`OverviewTab`) now renders a **Core** section — the 5 continuums as labelled tracks (virtue↔vice) with a marker per value, positioned above Nature so the layering reads bedrock→expression; `coreProfile` is surfaced through `AgentInfoCardData` at intimate+ knowledge (same gate as `axiologicalProfile`). Deferred: **THR-547** (re-theme the comet encounter's pre-THR-524 Wanderer/Anchor framing).
**Repo Health — `npm test` green on main (THR-489):** the months-long red baseline is cleared — full suite **11966 green (783 files)**, stable ×3. Resolved by the WS1–WS3 consolidation + intervening fixes; the live run reconciled clean against the historical known-failing checklist (no `.skip` masking). CI's required "Test · Typecheck · Build" gate is trustworthy again; merge=Done is load-bearing. Triage/closure doc: `Docs/plans/2026-06-30-test-suite-stabilization-triage.md`. (`tsc -b`'s ~3494-error baseline is a separate concern, out of scope.)
**Ascendant Beats — Divine Cadence (new):** Director + primitives + reach gate + home seat + debug + turn-1 floor + full expression toolkit (imbue/consecrate/bestow/anoint) + pool library + delivery adapter + beat resolve path shipped. **Beats are playable end-to-end** (offer→enter→resolve) and **pool beats now read bespoke per run (THR-514):** each cadence pool beat is backed by a Threadbare-voice content template with `enrichProse` placeholders, rendered enriched in the beat modal. **Onboarding spine complete (THR-504):** the full 5-beat scripted arc (The First → The Seat → A Thing Left Behind → The First Word → A Path Opens) ships with authored Threadbare prose. **Director pool draw eligibility-filtered + identity-biased (THR-516).** **Beat-driven graph seeding (THR-520):** resolving Beat 1 seats the throne (mana income ticking) at The First's settlement; Beat 2 mints + threads a sphere-flavored artifact — no manual card-firing. **Expression cards bucketed + pool-granted (THR-515):** consecrate/bestow/anoint now sit in `ASCENDANT_ACTION_BUCKETS` (`unlockable-generic`) and are granted by three deeper-expression investment pool beats (pool 14→17). **Consecrate relic variant shipped (THR-518):** `action.consecrate-relic` mints a permanent relic at establishment via `ControlSpec.mintUpkeepRelic` (handled in `spawnControlEffect`), waiving all per-tick upkeep while the relic lives and lapsing on its destruction — a pure-data second card, granted alongside `action.consecrate`. **Pool-beat template aftermath + introduction-group binding (THR-522, last engine tail):** `resolvePendingBeat` now runs a matched template's `aftermathConfig.fallback.reactions` through the shared aftermath resolver on resolution (additive; grant-only fallback when none declared) — beats can express `unlock_action`/`encounter_seed`/graph ops via their template, not only the descriptor. Introduction beats bind their specific un-introduced culture/faction into `PendingBeat.boundNodeIds` at offer time and name it via the new `{group}` enrichment placeholder (inspectable in the DebugPanel Beats tab). **Reach signatures acquired end-to-end (THR-523):** the 8 `invest.<reach>.<name>` signatures are catalogued `reach-gated` in `ASCENDANT_ACTION_BUCKETS` and granted per-run via `BeatDefinition.grantsReachSignature` — Beat 4 grants the primary, a new `beat.pool.invest.reach_signature` pool beat grants the secondary (gated by the new `unacquired_reach_signature` predicate). Remaining: per-beat identity tags (THR-514). **Reach signatures (THR-548/549/550):** sphere-power scaling helper + per-(reach×sphere) individualization scaffold + Iron's `signature_warhost` (mark faction `mobilized` + raise an army-node force) and Veil's `sphere_influence_amplify` (THR-551 — sustained sphere rift: ControlEffect amplifies local sphere influence up to a cap + seeded per-tick chaos-pulse leak, all sphere-scaled) shipped; **Stone's `spawn_unique_location` (THR-552) shipped** — mints a one-of-a-kind `location` (unique flag + `controls` edge), dedup by `uniqueTag` (idempotent), optional legendary-relic forge reusing the `spawn_artifact` path.
**Onboarding & First-Run Experience:** THR-504 scripted spine beats 0–4 shipped — the opening arc that delivers the run's first action cards.
**Action System & Unlocks:** THR-501 turn-1 floor (Move + Investiture only across all surfaces) shipped. THR-519 self-updating Action Catalog shipped — `scripts/generate-action-catalog.ts` emits `public/action-catalog.generated.json` from the live registry (164 ascendant-facing templates, code-true buckets incl. Beats verbs); `public/action-catalog.html` renders it (filter/sort/drawer), wired into the Design Reference Wiki.
**Encounter Experience — active:** Phases A1–A3, B1–B7, C1–C4, D1–D3, E1–E2, F1–F2, G1–G3 ✅. Remaining is F-phase integration (mount EffectRegistration components into hero panel / cast tile / scene state surfaces) and Phase H post-v1 polish.

## Milestone Status
- **v1.0 Foundation:** Shipped 2026-03-30 — Phases 1-18 + M2.5 (81 plans, 1533 commits)
- **v1.1 Optimization:** Shipped — Phases 19-22 (determinism, wiring, performance, hygiene)
- **Encounter Format Migration (Now):** ✅ Phases 0-9 complete. All guilds migrated.
- **Content Architecture (Now):** ✅ THR-86/88/239 shipped.
- **UI Visual Overhaul — Design System v1 (Now):** ✅ Project complete. THR-178 Deferral shipped 2026-05-08.
- **Continuous Improvement (Now):** THR-303/305/306/307/309/311/312/313/314/315/316/354/355/356/357/358/359/266/396/404/510 ✅. THR-304 Phase 5 series complete (5a vault THR-356 ✅, 5b repo THR-359 ✅). THR-360 (Category B code residue) queued.
- **Social Systems Expansion (Now):** THR-28/27/30/51/29/41/34/31/35 shipped. ✅ THR-254/253. THR-78 queued.
- **Thematic Pressure & Living World (Next):** ✅ THR-19/122/125/126/80/128/127. THR-87 blocked by THR-116.

- ✅ 2026-07-04: THR-585 shipped (Game Manual Wiki) — **W0: wiki freshness guardrail, manifest sections & plan-doc carrier.** Built plan `Docs/plans/2026-07-03-game-manual-wiki.md` §4. `public/wiki-manifest.json`: added optional per-page `sources` globs (the freshness contract), `Manual`/`Deep Reference` sections (existing 3 pages → Deep Reference, each with a tight `sources` glob), fixed `Nine`→`Eight Reaches` blurb, emptied backlog (`opening-system` absorbed by W1). New **advisory** `scripts/check-wiki-freshness.ts` + `npm run check:wiki-freshness`, chained into `check:process`: diffs the branch vs `WIKI_FRESHNESS_BASE` (=`origin/main`) and warns when a changed file matches a page's `sources` but the page HTML wasn't updated; constants `WIKI_FRESHNESS_MODE`(advisory|blocking)/`WIKI_FRESHNESS_BASE` (NFP #1); fail-soft on no-diff / malformed-glob / missing-manifest (NFP #4). `check-design-wiki.ts` extended to hard-validate the `sources` shape. `Docs/design-reference-wiki.md` documents the Manual section, dual-layer page template & freshness contract; CLAUDE.md gains the "Update the Design Reference Wiki" DoD bullet. Committed Cowork companion files the flush doesn't cover (game-manual-wiki brainstorm, judge-metrics 2026-W27). **Verified:** functional lint test (WARN@advisory exit 0, FAIL@blocking exit 1, reverts clean); `check:design-wiki` OK; `check:wiki-freshness` OK; full suite **12183 green (801 files)**; vite build green; generated hub asserted (Deep Reference header + 3 cards, no stale Systems/backlog/Nine-Reaches). Browser-verify exempt: manifest/tooling/docs, no `src/` UI path (plan confirms). Mutex with THR-586–602 (shared manifest). PR pending.
- ✅ 2026-07-03: THR-580 shipped (Engine Observability & Performance) — **Tick-loop observability instrumentation (E1–E5 + U1–U3) + large-map stall diagnosis.** Built the THR-570 plan. **E4** dedicated timing ring in `traceBuffer.ts` (`emitTiming`/`emitPhaseTiming`/`getTimingTraces`/`enable`+`disableProfiling`/`isProfilingEnabled`/`clearTimingTraces`, `TIMING_BUFFER_SIZE=4000`, profiling **off by default** — one boolean check per phase when off, NFP #7) so high-volume phase-profile traces stop evicting semantic traces from the shared ring; re-routed **all** `tick_phase_profile` (registered `runRegisteredPhases`, the 4 heavy inline suspects, + the 3 legacy actor-counter emitters) to it. **E1** `runInlinePhase()` in `orchestrator.ts` times `agent_decision`/`agent_movement`/`unified_action_progress`/`encounter_progression` (additive, preserves `phaseEventCounts`; measured around the thunk only — determinism, NFP #3). **E2** `distance_matrix_rebuild` trace + `SimulationRuntime.distanceMatrixRebuildCount` (`ensureDistanceMatrix` gains a `tick` arg). **E3** per-tick `tick_profile` rollup (total ms, slowest phase, agent count, rebuild flags). **E5** `aggregatePhaseTimings()` pure fn (avg/max/p95). **U1** PhasesDebugTab reads the timing ring: avg/max/p95 columns, inline-phase section, tick_profile header, profiling toggle. **U2** CLI `profile [N]`/`profile phases`. **U3** `__DEBUG.getPhaseTimings/enableProfiling/disableProfiling`. 4 contract tests migrated to `getTimingTraces()`. **Phase 2 diagnosis (THR-581):** seed 42 `--map large`, `profile 80` — `agent_decision` dominates (291ms avg, 459ms max @ ~1026 agents), and the slowest tick (40, 1499ms) had **no** DM-rebuild ⇒ the stall is super-linear `phaseAgentDecision`, not a rebuild storm; separately the distance matrix is capped at 1200 while large maps reach ~1777 locations mid-run (577+ unindexed → `getDistance→Infinity`). Deferrable ~46-inline-phase tail → THR-582. Full suite **12183 green (801 files)**, vite build green, `tsc -b` **0 net-new** (baseline red only; new files clean), engine smoke tick 30 (medium)/80 (large) clean. **Browser-verified @1920×1080:** Phases tab renders profiling toggle + tick_profile header + avg/max/p95 + "4 inline phases timed"; `__DEBUG.getPhaseTimings()` returns 24 phases after `enableProfiling()`+ticks; 0 console errors. Wiring-checklist updated (`tick_profile`, `distance_matrix_rebuild`). PR pending.
- ✅ 2026-07-02: THR-562 shipped (Agent Personality & Moral Drift) — **Threshold → emergent personality traits + hysteresis (wave-2 #3) — notification + UI completion of the THR-527 engine.** The threshold phase, 16 personality trait defs, `personality_trait_emerged` trace and "becoming" TickEvent already shipped under **THR-527** (registered in `phases/index.ts`, hysteresis grant/release, 10 passing unit tests) and were re-based onto THR-559's unified `clamp(baseline+drift)` live position — so #3's genuine remaining work was the two pillars THR-527 (engine-only) left. **Notification (engine):** the becoming `TickEvent` had no `notification` directive, so `routeNotifications` (`notificationRouter.ts:107`) silently dropped it — added `notification:{channel:'toast'}` in `becomingEvent()` (a toast, not an alert: distinct same-tick becomings survive the alert icon+tick dedup; `actorId`→agent nav) and a `personality_trait_emerged → 'lifecycle'` case to `eventTypeToCategory`. **UI:** new `card.personalityTraits` field on `AgentInfoCardData` (`agentDetail.ts` `getPersonalityTraitDisplays` — intimate+, from `subcategory:'personality'` has_trait edges, pole/reach parsed from `trait.personality.<reach>.<pole>`), pulled **out of** the generic `allTraits` chips; the live sheet (`OverviewTab`) renders a distinguished **Personality** section (subtitle "Who X has become — the mark of their choices."; virtue=gold / vice=muted-rose chips, flavor-on-hover). Targets the **live** sheet (`AgentProfileModal`/`OverviewTab`) — the issue's literal `AgentDetailPanel` is orphaned dead code (only its own test references it); `personality` added to the category color/label maps there + the CMS registry (live badge) for completeness. 4 new/extended tests (phase notification directive, `eventTypeToCategory`+toast routing, `agentDetail` personalityTraits populated & excluded from allTraits, `AgentProfileModal` renders the Personality section+chips). Full suite **12178 green (801 files)**, vite build green, `tsc -b` **0 net-new** (3547=3547, normalized error sets identical to baseline — proven by stash-diff), 30-tick CLI smoke (seed 42 medium, 391 agents, 53 events; +40-tick run: 83 personality trait edges, becoming events carry the toast directive). **Browser-verified @1920×1080:** production `AgentProfileModal` rendered from a live-state transparent card — Personality section shows Disloyal/Misleading/Perceptive (excluded from the Traits chips below), Chronicle feed shows "Muha has become Brave/Disloyal/Reckless" beats; console clean of personality-related errors (only pre-existing `ua_ua_6_promotion` key collisions). PR pending.
- ✅ 2026-07-02: THR-561 shipped (Agent Personality & Moral Drift) — **`axisContributions` field + origin-vignette birth seeding (wave-2 #2).** Wired the origin-vignette content library's missing consumer (the "ORIGIN_VIGNETTES has no consumer" gap flagged by THR-526/THR-529). **Engine:** new pure `src/engine/personality/originBaseline.ts` (`drawOriginVignettes` distinct+seeded, `vignetteAxisContribution` reach→axisId + pole→sign, `sumAxisContributions` folds maps & skips/counts unknown axis ids, `computeOriginBaseline` lays the summed **canonical 0–1** delta onto the existing signed baseline via the registry `signedToCanonical01`/`canonical01ToSigned` bridges); new idempotent tick phase `src/engine/phases/personalityOriginSeed.ts` (slot `post-economy`, `beforePhase:['personality_trait_emerge']`) seeds each unseeded mortal individual via `mulberry32(hash(seed,id))`, merges `profileUpdates` onto `axiologicalProfile`, records `originVignettes` + `originVignettesSeeded`. **Additive, not destructive** — vignettes nudge the generated baseline (mirroring Core's `seedCoreProfileWithVignettes`), so worldgen behavior is preserved (zero integration-test drift). **Schema:** new optional `axisContributions?: Partial<Record<string,number>>` on `TraitDefinitionProperties` — per canonical axis id, signed 0–1 delta, kept entirely separate from `domainContributions` (personality ≠ capability), consumed only by the baseline computation (forward-carrier for permanent formative-mark traits). **Trace:** `personality_origin_seeded` registered (`TraceCategory` + `TRACE_CATEGORIES` + typed `PersonalityOriginSeededTrace`), emitted as ONE aggregate per tick (`details.kind`: `seeded`|`unknown_axis`) — never per-agent (ring-buffer discipline); auto-surfaces as a DebugPanel filter chip. Constant `ORIGIN_VIGNETTES_PER_AGENT`=5 (`originConstants.ts`, NFP #1). Content requirement already satisfied (144 vignettes, 18/reach × 8 reaches, 72/72 poles). 21 new tests (11 baseline + 10 phase) + `phaseRegistry.equivalence` baseline updated. Full suite **12175 green (801 files)**, vite build green, 30-tick CLI smoke (seed 42 medium, 391 agents, 53 events) clean + verified seeding fires (agents carry provenance + layered baselines). Browser-verify exempt: types/data + engine only (DebugPanel gains one auto-registered category chip via `TRACE_CATEGORIES`, no `src/components/` edit). PR pending.
- ✅ 2026-07-01: THR-541 shipped (Encounter Format Migration) — **THR-470 follow-up: `beatsPerChunk` windowed-numerator / full-tick-denominator KPI fix.** The `threaded_beats_per_10t` KPI shared the exact artifact THR-470 fixed for branching fires — `buildThreadedBeats` counted rich resolved actions from the `RESOLVED_ACTION_RETENTION_TICKS=20`-pruned `state.unifiedActions` but divided by the full tick count, undercounting long runs. **Engine (same shape as THR-470):** added `SimulationRuntime.threadedBeatsTotal`, incremented once per newly-resolved "rich" action (multi-step ≥3 OR branching) at the orchestrator resolved-action cleanup (beside `branchingFiresTotal`, before pruning); `buildThreadedBeats` prefers the lifetime counter (windowed fallback when no runtime, for pure-state tests). Extracted the rich predicate into an exported cached `isRichTemplate` shared by the fallback + the increment so both classify identically; historical `getAnyEncounterById`-only resolution preserved (behavior unchanged, only the window removed). `KpiRuntimeView` gains `threadedBeatsTotal?`. 2 new tests mirroring THR-470. Full suite **12124 green (798 files)**, `tsc --noEmit` exit 0, vite build green, 30-tick CLI smoke (seed 42 medium, 390 agents, 50 events) clean. Acceptance: seed 42 @ tick 60 — pruned snapshot 21 actions/17 resolved yet `threadedBeats.total=14` (2.33/10t), cumulative counter surviving pruning. Content/UI N/A (dev/diagnostic metric, no player-facing render). Browser-verify exempt: engine-only. PR pending.
- ✅ 2026-07-01: THR-523 shipped (Ascendant Beats — Divine Cadence) — **Reach-gated bucket catalogue + signature acquisition beats — the reach-signature epic's catalogue-consumer leaf.** Two pillars. **Content/data:** the 8 `invest.<reach>.<name>` signatures (THR-553/555) are now catalogued in `ASCENDANT_ACTION_BUCKETS` as `{ bucket: 'reach-gated', requiresReach }` (drift-guarded against `REACH_SIGNATURE_CONTENT_TEMPLATES`), so the shipped `getTargetActionSlots` reach gate hides out-of-domain signatures; a Threadbare-voice content template (`beat.pool.invest.reach_signature`, "A Second Mastery") backs the new pool beat. **Engine:** a new per-run dynamic-grant mechanism — the plan's "primary at Beat 4 / secondary at pool" can't map onto static data because the reach model is an affinity-bag (2–3 domains, no static primary/secondary) and a `selection` beat grants exactly one chosen id (so Beat 4 can't grant both a god-path *and* a signature via selection). Solution: `BeatDefinition.grantsReachSignature: 'primary'|'secondary'`, resolved in `resolvePendingBeat` from the ascendant's affinity-ranked reaches (`resolveReachSignatureGrant`, deterministic — affinity desc, `REACH_DOMAINS` tie-break) → the run's signature id, applied orthogonally to `grantsActionIds`. **Beat 4 "A Path Opens"** keeps its dreamer/prophet/patron `selection` and *also* unconditionally grants the primary signature; new `beat.pool.invest.reach_signature` investment pool beat grants the secondary, gated by the new `unacquired_reach_signature` eligibility predicate (retires once every in-domain signature is learned). Fail-soft: no-op when a slot has no ranked reach. THR-505 coverage tests stay green (8 new `invest.*` ids bucketed + resolvable + `requiresReach` declared). 10 new tests (ranking, dynamic grant on both beats, eligibility, bucket drift guard); full suite **12122 green (798 files)**, `npx tsc --noEmit` exit 0 + `tsc -b` **0 net-new** (changed files clean; lone pre-existing `ascendantBeatSeeding.ts` error unchanged), vite build green, 30-tick CLI smoke (seed 42 medium, 390 agents, 50 events) clean. Browser-verify N/A: engine/content/data only, no `src/components/`|`src/views/` file touched; plan §5 marks UI N/A (beats render through the shipped `AscendantBeatModal`). Design deviation noted in the Linear closeout (dynamic-grant vs the plan's literal "both selection beats"). Plan `Docs/plans/2026-06-30-ascendant-reach-signatures.md` §4.3–4.4 (uncommitted home-worktree copy, pending `flush-plan-docs`). PR pending.
- ✅ 2026-07-01: THR-554 shipped (Ascendant Beats — Divine Cadence) — **Reach-signature hex signifiers + DebugPanel/`__DEBUG` surfacing (warhost / rift / wonder).** The UI pillar for the THR-555 engine-backed trio: a new HexMapV2 signifier layer marking each signature's on-map footprint. **Engine:** pure selector `src/engine/reachSignatureMarkers.ts` (`buildReachSignatureMarkers(graph, controlEffects)` → `ReachSignatureMarker[]`) projects the three footprints — warhost = an `actor` w/ `properties.warhost===true` resolved to hex via `located_at`; rift = a live `ControlEffect` carrying `perTickSphereInfluence` at `targetHexCol/Row`; wonder = a `location` w/ `generatedBy==='spawn_unique_location'` — deterministic, id-sorted, fail-soft. **UI:** new render layer `REACH_SIGNATURE_SIGNIFIER` (renderOrder 8.8, LAYER_Z 0.078, between rarity ring 8.7 and locations 9); `ReachSignatureSignifierMesh.ts` mirrors the rarity-signifier pattern — one canvas-drawn glyph sprite per marker (Iron muster-ring+crossed-blades red, Veil vesica/tear tinted to the amplified sphere w/ a pulse, Stone monument tan), dedicated `reachSignatureMarkers`-keyed rebuild effect + tick-driven rift pulse + zoom-visibility sync (regional+) + dispose, all in `HexMapV2.tsx`; `GameView` memo threads the prop (keyed on graph/controlEffects/worldVersion). **Debug:** `window.__DEBUG.listSignatures()` (8 signatures × run-unlock status + ascendant primary-sphere power multiplier) + `fireSignature(reach)` (grants the unlock → `runUnlockedActionIds`, materializes a minimal DEV footprint for the engine-backed reaches, returns the sphere-scaled magnitude); DebugPanel Beats tab gains a **Reach Signatures** block surfacing `runUnlockedActionIds` + the primary-sphere multiplier + a fire control. Constants in `reachSignatureVisualConstants.ts` (NFP #1). 17 new tests (selector detection/fail-soft/determinism ×10, mesh build/pulse/dispose ×7); full suite **12113 green (798 files)**, `tsc --noEmit` exit 0, vite build green, 30-tick CLI smoke (seed 42 medium, 390 agents, 50 events) clean. **Browser-verified @1920×1080 (Claude-in-Chrome):** all three WebGL glyphs render (warhost red on keep, rift blue tear on its hex, wonder as the GREAT WORK location) + DebugPanel Reach Signatures block (primary sphere `mind`, ×0.60, iron/veil/stone ⚙✓unlocked); `__DEBUG.listSignatures()` confirms `runUnlockedActionIds` gained all 3 w/ scaled magnitude; console clean (268 msgs, 0 errors/warnings). PR pending.
- ✅ 2026-07-01: THR-555 shipped (Ascendant Beats — Divine Cadence) — **The 3 engine-backed reach signatures (Iron/Warhost, Veil/Rend the Gate, Stone/Great Work) — `UnifiedActionTemplate`s consuming the shipped effect trio.** Authored in `src/data/reach-signature-content.ts`, spread into `UNIFIED_ACTION_TEMPLATES`: `invest.iron.warhost` (aftermath `signature_warhost` on the target faction **+** a step `anoint_faction` op → chosen-banner `chosen_status_grant`), `invest.veil.rend_the_gate` (sustained `sphere_influence_amplify` rift), `invest.stone.great_work` (`spawn_unique_location` `master_forge` + legendary relic via `artifactForgeTier`). The effect resolvers read a **literal** target id, so static content declares sentinels bound at fire time by the new `bindReachSignatureTargets` pass (`encounterAftermath.ts`, mirrors the shipped `$target` idiom): `$target`→`action.targetId` (faction/location), `$primary`→the caster's primary Creation Sphere (the rift amplifies the ascendant's own sphere), Stone's `$target`→the target location's hex. 3 bespoke `SIGNATURE_MATRIX` cells at each signature's reach × primary sphere (`SIGNATURE_BESPOKE_BASE_VALUE`=5). Reach-gated via `requiresReach` (THR-503); **not** touching `ASCENDANT_ACTION_BUCKETS` (THR-523). Full suite **12096 green (796 files)**, `tsc -b` **0 net-new** (3549=3549, changed files clean), vite build green, 30-tick CLI smoke (seed 42 medium, 390 agents, 50 events) clean. Browser-verify exempt: content/data + engine binding glue, no UI-pillar file; cards not runtime-reachable until THR-523/554 wire acquisition. Plan §3.7/§4.1/§4.5 (uncommitted, pending `flush-plan-docs`). PR pending.
## Archived to project-history.md
- 2026-07-04 batch (status trim, THR-585 closeout): THR-553, THR-552
- 2026-07-02 batch (status trim, THR-562 closeout): THR-470, THR-529
- 2026-07-02 batch (status trim, THR-561 closeout): THR-530, THR-528
- 2026-07-02 batch (status trim, THR-559 closeout): THR-527, THR-531
- 2026-07-01 batch (status trim, THR-541 closeout): THR-539, THR-536
- 2026-07-01 batch (status trim, THR-553/555 closeout): THR-534, THR-466, THR-533
- 2026-07-01 batch (status trim, THR-552 closeout): THR-520, THR-521, THR-510
- 2026-07-03 batch (status trim, THR-580 closeout): THR-545 · 2026-06-30 batch (THR-544/561 closeout): THR-544, THR-516, THR-514
- 2026-06-30 batch (status trim): THR-513, THR-512, THR-511, THR-506, THR-505, THR-504
- 2026-06-28 batch (status trim): THR-509, THR-494, THR-503, THR-500, THR-502, THR-501
- 2026-06-23 batch (status trim): THR-476, THR-488, THR-486, THR-487, THR-492
- 2026-06-27 batch (status trim): THR-517, THR-481, THR-477, THR-491, THR-479, THR-472, THR-475, THR-473, THR-463, THR-462, THR-508
- 2026-06-13 batch (status trim): THR-461, THR-460, THR-250, THR-378
- 2026-06-12 batch (status trim): THR-451, THR-62, THR-453, THR-455, THR-456, THR-442, THR-449, THR-446, THR-381
- THR- and earlier — see project-history.md
- 2026-05-06 batch: THR-311, THR-312, THR-313, THR-314, THR-315, THR-316, THR-317, THR-320, THR-321, THR-322, THR-323, THR-336
- 2026-05-07 batch: THR-324, THR-325, THR-329, THR-339, THR-340, THR-341, THR-349, THR-350, THR-354, THR-355, THR-356, THR-357, THR-358, THR-359
- 2026-05-08 batch: THR-139, THR-176, THR-177, THR-178, THR-179, THR-215, THR-265, THR-266, THR-267, THR-289, THR-292, THR-326, THR-330, THR-331, THR-332, THR-333, THR-334, THR-335, THR-338, THR-343, THR-344, THR-345, THR-352, THR-353, THR-360, THR-361, THR-362, THR-363, THR-365, THR-368, THR-374, THR-387, THR-388
- 2026-05-09 batch: THR-268, THR-389, THR-391
- 2026-05-11 batch: THR-393, THR-403, THR-408, THR-411
- 2026-05-12 batch: THR-413, THR-404, THR-397, THR-398, THR-399, THR-405, THR-416, THR-409
- 2026-05-16 batch: THR-416 (via THR-409 worktree cleanup closeout), THR-249, THR-248, THR-377, THR-447
- 2026-06-11 batch: THR-406, THR-450, THR-452, THR-457

## Active Backlog Ideas
- **TB-105–108 Thematic Pressure & Living World Pass** (omen agendas, cool failure, doom identity, intent/activity visibility)
- **TB-095–099 Social Systems Expansion** (v1.2 — designed, ready to sequence)
- TB-071 Economy Second Pass · TB-069 Location NPCs · TB-051 Monster Encounters residual · TB-037 Onboarding

Full backlog: [Linear (Threadbare team)](https://linear.app/threadbare) · Completed work: `Docs/project-history.md` + Linear "Done" state · Pre-Linear history: `.planning/BACKLOG_HISTORY.md`
