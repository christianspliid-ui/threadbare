# Project Status
> Updated 2026-06-30 (THR-530).

## Current Focus
**Ascendant Beats — Divine Cadence (new):** Director + primitives + reach gate + home seat + debug + turn-1 floor + full expression toolkit (imbue/consecrate/bestow/anoint) + pool library + delivery adapter + beat resolve path shipped. **Beats are playable end-to-end** (offer→enter→resolve) and **pool beats now read bespoke per run (THR-514):** each cadence pool beat is backed by a Threadbare-voice content template with `enrichProse` placeholders, rendered enriched in the beat modal. **Onboarding spine complete (THR-504):** the full 5-beat scripted arc (The First → The Seat → A Thing Left Behind → The First Word → A Path Opens) ships with authored Threadbare prose. **Director pool draw eligibility-filtered + identity-biased (THR-516).** **Beat-driven graph seeding (THR-520):** resolving Beat 1 seats the throne (mana income ticking) at The First's settlement; Beat 2 mints + threads a sphere-flavored artifact — no manual card-firing. **Expression cards bucketed + pool-granted (THR-515):** consecrate/bestow/anoint now sit in `ASCENDANT_ACTION_BUCKETS` (`unlockable-generic`) and are granted by three deeper-expression investment pool beats (pool 14→17). **Consecrate relic variant shipped (THR-518):** `action.consecrate-relic` mints a permanent relic at establishment via `ControlSpec.mintUpkeepRelic` (handled in `spawnControlEffect`), waiving all per-tick upkeep while the relic lives and lapsing on its destruction — a pure-data second card, granted alongside `action.consecrate`. Remaining: reach-gated `invest.*` grants once those cards are authored (THR-523, blocked); per-beat identity tags (THR-514).
**Onboarding & First-Run Experience:** THR-504 scripted spine beats 0–4 shipped — the opening arc that delivers the run's first action cards.
**Action System & Unlocks:** THR-501 turn-1 floor (Move + Investiture only across all surfaces) shipped. THR-519 self-updating Action Catalog shipped — `scripts/generate-action-catalog.ts` emits `public/action-catalog.generated.json` from the live registry (164 ascendant-facing templates, code-true buckets incl. Beats verbs); `public/action-catalog.html` renders it (filter/sort/drawer), wired into the Design Reference Wiki.
**Encounter Experience — active:** Phases A1–A3, B1–B7, C1–C4, D1–D3, E1–E2, F1–F2, G1–G3 ✅. Remaining is F-phase integration (mount EffectRegistration components into hero panel / cast tile / scene state surfaces) and Phase H post-v1 polish.

## Milestone Status
- **v1.0 Foundation:** Shipped 2026-03-30 — Phases 1-18 + M2.5 (81 plans, 1533 commits)
- **v1.1 Optimization:** Shipped — Phases 19-22 (determinism, wiring, performance, hygiene)
- **Encounter Format Migration (Now):** ✅ Phases 0-9 complete. All guilds migrated.
- **Content Architecture (Now):** ✅ THR-86/88/239 shipped.
- **UI Visual Overhaul — Design System v1 (Now):** ✅ Project complete. THR-178 Deferral shipped 2026-05-08.
- **Continuous Improvement (Now):** THR-303/305/306/307/309/311/312/313/314/315/316/354/355/356/357/358/359/266/396/404/510 ✅. THR-304 Phase 5 series complete (5a vault THR-356 ✅, 5b repo THR-359 ✅). THR-360 (Category B code residue) queued.
- **Social Systems Expansion (Now):** THR-28/27/30/51/29/41/34/31/35 shipped. ✅ THR-254/253. THR-78 queued.
- **Thematic Pressure & Living World (Next):** ✅ THR-19/122/125/126/80/128/127. THR-87 blocked by THR-116.

- ✅ 2026-06-30: THR-530 shipped (Agent Personality & Moral Drift) — **Autonomous in-encounter choice (the one genuinely new subsystem).** Breakdown #6. Today only the player branches: non-hero agents resolve mechanically and their authored aftermath reactions are **never applied** — the world they shape never happens. **Engine:** new pure `reactionChooser.ts` (`computeAxisLeans` = `axiologicalProfile` baseline + drift on ±1; `inferReactionLean` infers each reaction's reach-pole from its effects — `${reach}.positive/.negative` `reputation_tally` keys → ±1, actor-self `reputation_score` delta → the encounter reach; `chooseAlignedReaction` = deterministic argmax `Σ reactionLean·agentLean·weight`, lowest-index tie-break, `aligned:false` → `reactions[0]`). New registered tick phase `autonomous_aftermath` (slot `post-resolution`) scans resolved actions of non-player, non-threaded agents (excl. thread/avatar holders), applies the chosen reaction via `applyEncounterAftermathReaction`, nudges drift toward the chosen pole (THR-528 applicator), emits `reaction_selected` trace w/ per-option scores; idempotent via additive `UnifiedAction.autonomousAftermathApplied`. `resolveAftermathContextForAgent`'s `reactions[0]` default replaced with the chooser (CLI/debug benefit too). Constants `PERSONALITY_REACTION_WEIGHT` (kill-criteria lever), `PERSONALITY_REACTION_DRIFT_DELTA`, `PERSONALITY_AUTONOMOUS_AFTERMATH_MAX_PER_TICK` (NFP #1). **Content/UI:** N/A v1 (variety scales with authored reaction poles; character sheet is THR-532). 29 new tests + phaseRegistry equivalence baseline updated. Full suite **11966 green** (783 files), `tsc -b` **0 net-new** (3494 baseline), vite build green, 30-tick CLI smoke (390 agents) clean; 300-tick traced run fired the chooser 51× with non-index-0 variety (chosenIndex `[[0,49],[1,2]]`). Browser-verify exempt (no UI file). PR pending.
- ✅ 2026-06-30: THR-528 shipped (Agent Personality & Moral Drift) — **Choice → drift: authored poles + apply + decay-to-baseline.** Breakdown #4 — the "what recent choices are making them" layer. **Content:** `AuthoredChoiceCard` gains authored `moralAxis`/`pole`/`magnitude` (0.05–0.20), retiring the inferred `toEncounterArchetypePole` heuristic as the primary path (kept as fallback); 4 branching encounters migrated as proof (road-ambush/iron, the-merchants-favor/gold — incl. a `withdrawn`→**vice** divergence the heuristic would read as virtue, shadow-court-audience/shadow, the-star-pilgrim/star; 9 cards, one-line local-heuristic preference each). **Engine:** shared resolvers `resolveEncounterArchetypePole` (authored wins, used in `buildLiteEncounterContract`) + `resolveChoiceDrift` (card → `{axisReach, moralAxisPole, driftMagnitude}` for a commit builder). New live-position accessor `liveAxisPosition(baseline, drift)` + `driftDeltaFor` — live = clamp(baseline + drift) on ±1; drift is a *temporary delta* decaying to zero, so the live position **rests at the (mutable) baseline, not neutral**. `phaseChoiceResolution` reads the baseline (`axiologicalProfile[valuePair]` via the THR-536 registry) and emits `livePosition` on the `choice_resolved` trace; `phaseDriftDecay` switched to the plan-named `PERSONALITY_DRIFT_DECAY_PER_TICK` (old name kept as `@deprecated` alias); new `PERSONALITY_DRIFT_DELTA_*` constants (NFP #1). **UI:** none — live position via the existing DriftVisualiser (sheet rows are THR-532). 14 new tests; full suite **11945 green** (781 files), `tsc --noEmit` clean, vite build green, 30-tick CLI smoke (seed 42 medium, 390 agents, 49 events) clean. Browser-verify exempt (no UI file). **Scope note:** the live `PendingChoiceCommit` construction sites (player-commit UI + autonomous chooser) are breakdown items #6/#8 — this ships the authoring + application + decay machinery + resolvers, unit-tested, ahead of them; the 24 per-encounter local contract-builder copies remain pre-existing duplication (consolidation is a follow-up). PR pending.
- ✅ 2026-06-30: THR-527 shipped (Agent Personality & Moral Drift) — **Threshold → emergent personality traits (+ hysteresis).** Issue #3 of the personality breakdown — the "who they currently are" layer. New registered tick phase `personality_trait_emerge` (slot `post-economy`, `src/engine/phases/personalityTraitEmerge.ts`): for each **individual** mortal agent × canonical axis, reads the standing axis position (`axiologicalProfile[axis.valuePair]`, legacy ±1) normalized to the canonical 0–1 scale (`0.5 + 0.5·v` — the plan's "threshold layer reads the profile"; survives the THR-537/538 scale migration untouched), grants the **virtue** trait at ≥0.8 / **vice** at ≤0.2, and releases inside a `0.65`/`0.35` **hysteresis** dead-band so traits don't flicker. Fires a "becoming" `tickEvent` (`"<name> has become <Word>."`). **Content:** 16 personality trait defs (8 axes × poles, `src/data/personality-trait-content.ts`), `subcategory:'personality'` (distinct from `reputation`), generated from `CANONICAL_AXES` (pole labels can't drift), each carrying per-reach `scoringModifiers` and **empty `domainContributions`** — the load-bearing invariant: personality steers *selection*, never *competence*. New optional `scoringModifiers` field on `TraitDefinitionProperties`; `computeReputationScoringBonus` generalized to consume `personality` traits too (the bias path). Named constants `PERSONALITY_TRAIT_VIRTUE_THRESHOLD`/`VICE_THRESHOLD`/`RELEASE_BAND`/`SCORING_MODIFIER` (NFP #1); typed `PersonalityTraitEmergedTrace` (NFP #2); fail-soft per the plan table (missing profile → skip; missing def → skip+warn-trace). UI character-sheet rows are **THR-532** (later wave). 10 new phase tests + the `phaseRegistry.equivalence` baseline updated (also made eviction-robust). Verification: full suite **11930 green** (780 files), `tsc --noEmit` clean + `tsc -b` **0 net-new** (3492 = 3492 baseline), `vite build` green, 30-tick CLI smoke (seed 42 medium, 390 agents, 49 events) clean; engine-smoke proof — a 40-tick traced run had **73 individual agents** crystallize personality traits (e.g. Tam→Disloyal, Gilded Pact excluded as non-individual), **0 non-individual** actors affected. Browser-verify exempt (no UI pillar file — the becoming beat surfaces via the existing event feed; sheet rows are THR-532). PR pending.
- ✅ 2026-06-30: THR-531 shipped (Agent Personality & Moral Drift) — **Personality → encounter selection weighting (strong & legible).** Strengthened the existing `axiologicalProfile → computeDesireScore → desireMultiplier` selection path (`encounterScoring.ts`) so agents clearly gravitate toward encounters matching their dominant axes. New named constant `PERSONALITY_SELECTION_WEIGHT = 2.0` (`agent-behavior-constants.ts`, NFP #1; 1.0 = pre-THR-531 baseline, the plan's reversible "kill-criteria" lever) amplifies the raw axiological alignment into a labeled `personalityBias = axiologicalScore × weight` term that feeds the desire multiplier. Added `personalityBias` (+ raw `axiologicalScore`) to `ScoredCandidate` and to `ScoringTrace.topCandidates` for inspectability (NFP #2). Fully additive — only one `ScoredCandidate` constructor exists; `curator.ts`/`clueLifecycle.ts` have separate local types. 2 new unit tests prove the `personalityBias = axio × weight` invariant + trace surfacing; full suite **11920 green** (779 files), `vite build` green, 30-tick CLI smoke (seed 42 medium, 390 agents) clean. **Engine-smoke evidence of personality-shaped selection:** a 40-tick traced run surfaced 222 candidates with nonzero `personalityBias` (e.g. axio 0.902 → bias 1.804 → desire 2.422, selected; misaligned axio −0.953 → desire floored 0.011). Three pillars: Engine = entirety; Content N/A; UI N/A (bias observable via `ScoringTrace` in the debug panel). Browser-verify exempt (no UI file touched). PR pending.
- ✅ 2026-06-30: THR-539 shipped (Agent Personality & Moral Drift) — **Origin-vignette content library (content half of THR-526).** New engine-free data module `src/data/origin-vignettes.ts`: **144 pre-history vignettes** keyed by `(reach, pole, magnitude)` — all 8 reaches × both virtue/vice poles × **9 each**, magnitudes skewed to the small deltas (per bucket 4×0.05 / 3×0.10 / 1×0.15 / 1×0.20; **112 ≤0.10 vs 32 >0.10**) so drawing several at birth yields a roughly normal baseline (central-limit) rather than extremes. Threadbare-voice one-liners ("show the soil, not the flower"), generic/reusable — no proper nouns, locations, or gendered assumptions. **Keyed on `(reach, pole)` only — no `axisId`/registry import** — so it's parallel-safe with the THR-536/537/538 registry trie; the reach→`axisId` map + signed-delta derivation (virtue → +mag toward 1.0, vice → −mag toward 0.0) belong to THR-526's engine half. Typed `OriginVignette` contract (`{id,text,reach,pole,magnitude}`) + 9-assertion coverage shape-test (`origin-vignettes.test.ts`: ≥130 total, every bucket ≥8, only the 4 magnitudes, small-delta skew, unique `origin.<reach>.<pole>.<slug>` ids, trimmed text). `tsc --noEmit` clean, scoped vitest **9 green**, vite build green. Engine-smoke + browser-verify exempt (isolated content data — no tick-loop/orchestrator/agent-decision/UI file). Consumed later by THR-526's birth-seeding hook; rendering is THR-532. PR pending.
- ✅ 2026-06-30: THR-536 shipped (Agent Personality & Moral Drift) — **Canonical axis registry module (525a — additive, zero migration).** New `src/types/axisRegistry.ts`: the load-bearing contract all 8 downstream personality issues (THR-526–533) import. Maps each of the 8 reaches → `{ reachDomain, axisId: '${reach}_axis', valuePair, virtue:{role,word}, vice:{role,word} }` using the **THR-524-approved** vocabulary (Iron Protector/Brave↔Conqueror/Power-Hungry … Star Beacon/Inspiring↔Anchor/Discouraging), with the two relocated terms encoded at their **new** reaches (Seer Veil→Eye, Manipulator Veil→Shadow). The `valuePair` legacy bridge is sourced from the real `REACH_VALUE_PAIR` keys (`agent.ts`), **not invented**. Documents the canonical **0–1 scale** (0.5 neutral / virtue 1.0 / vice 0.0) via `AXIS_NEUTRAL`/`AXIS_VIRTUE`/`AXIS_VICE`. Helpers `getAxisByReach` / `getAxisById` / `getPoleLabel` + `CANONICAL_AXIS_IDS`. **Strictly additive** — no consumer re-pointed, no scalar retired, no old name reconciled (that's 525b/THR-537). 18 new tests (`axisRegistry.test.ts`: all 8 axes × both poles vs an independent oracle, relocation check, bridge-key check, helper round-trips/undefined paths); types suite **254 green**, vite build green, **zero net-new `tsc -b` errors**. Engine-smoke exempt (no tick/orchestrator/agent-decision file); browser-verify exempt (no UI surface). PR pending.
- ✅ 2026-06-30: THR-533 shipped (Agent Personality & Moral Drift) — **Prose readability pass on branching encounters + flesh fix.** House-style calibration per the creative director's "too literary" note: retired the recurring "the particular X of Y" tic across all 24 tic-bearing branching-encounter files (~68 instances → 0; 3 legitimate idioms preserved — "nothing particular against", "a particular tier of institutional trust", "no one in particular"). Minimal de-tic (delete the filler word, keep the existing article — "the particular weight of X" → "the weight of X"), holding the Threadbare voice; text bodies only, **zero structural change** (65 insertions / 65 deletions, pure 1:1 line swaps; encounter-structure tests 264 green). Also fixed the stale `reach: 'flesh'` in `tavern-encounter-content.ts` (4× — Tavern Brawl step + Drinking Contest) → `'iron'` per the cosmology canon's documented Flesh-migration rule ("athletics → Iron"); the issue's "→Quintessence" referred to TB-075's *thematic* meta-property migration, not a literal reach value (`'quintessence'` is not a `ReachDomain` and would be a net-new type error). Content-only (no engine/UI/types touched) — engine-smoke + browser-verify N/A. Verification: full suite **11891 green** (777 files), `tsc -b` **0 net-new** (only the pre-existing THR-489 test-file baseline), vite build green. Out-of-scope note: `reward-attachment-catalog.ts:1696` carries the same stale `flesh` value (left for separate tracking). PR pending.
- ✅ 2026-06-29: THR-534 shipped (Agent Coordination Protocol) — **Harden `flush-plan-docs` against false auto-close.** PR #428 re-closed unimplemented THR-525 via an OLD `Closes <linear-url>` template even though THR-510 had already scrubbed the authoritative SKILL.md. Reinforced the SKILL.md prohibition (now explains GitHub's **native** Linear integration closes on the URL form regardless of the custom `linear-autoclose.yml` guard — body-scrubbing is the only effective defense; cites the #428→THR-525 incident), bumped `last_validated_against`→2026-06-29, mirrored to `.agents/`; neutralized the residual stale-template copy-paste hazard in the 2026-05-12 plan doc with a SUPERSEDED banner; logged impediment #162. Pre-merge CI guard tracked separately as THR-535. Docs/skill-only (zero `src/`); `vite build` green, `check:skill-sync` green. PR pending.
- ✅ 2026-06-29: THR-466 **complete (5 of 5)** (Encounter Format Migration) — **Non-local-scale branching encounters.** Closes the content gap: the authored player-choice layer no longer tops out at local stakes. Batch 1 (2/5, prior run): **The Comet at the Turning** (cosmic, star) + **The Wall of the Mason-Lord** (regional, stone), plus the engine pre-check that proved the gap was *content* not engine (firing path has zero scale references). Batch 2 (3/5, this run): **The Page Beneath the Saint** (regional, veil — Seer↔Manipulator: archivist's buried truth vs. a regional faith), **The Verdict That Burns** (regional, eye — Witness↔Judge: a just verdict that burns the region), **The Granaries in the Famine Year** (regional, gold — Patron↔Extractor: cornering a region's grain, with intervention=mercy / restraint=bleeding as a deliberate inversion of the restraint-is-mercy shape). All `story_beat`/rarity-4, two god-action branches (active intervention + "let them handle it" restraint pole), full dual aftermath suites with cool failure at every branch, `// TODO(THR-498)` repeatability markers, registered in both template arrays. 39 new tests; full suite green (777 files, 11891 tests; migration count 26→29, legendary cap 12→16 for the marquee batch), vite build green, CLI smoke spawned both new regional encounters through a 30-tick run clean. Browser-verify N/A (content/data; UI pillar N/A — uses shipped encounter surface).
- ✅ 2026-06-29: THR-520 shipped (Divine Cadence) — **Beat-driven graph seeding: auto-thread seat/artifact on spine-beat resolution.** Closes the THR-504 deferral — the THR-517 resolve contract only *grants cards*; the throne/artifact the onboarding narrates appeared only when the player manually fired them. New additive `seedsGraph?: BeatGraphSeed` tag on `BeatDefinition` (`home_seat` | `threaded_artifact`); `resolvePendingBeat` now runs the seed on resolution and records `BeatRecord.seededNodeIds` (existing locked grant/spine tests untouched — they carry no tag). **Engine** (new `src/engine/ascendantBeatSeeding.ts`): `home_seat` resolves The First → its `located_at` settlement (climbs a sublocation to its parent), calls `setHomeSeat` (begins `ESSENCE_PER_SEAT` income), falls back to setHomeSeat's deterministic default when no First; `threaded_artifact` mints a sphere-flavored artifact via the THR-509 `pickSphereFlavoredEffect` (seeded `mulberry32(seed+tick)`, deterministic), `thread`s it (ascendant→artifact), and grants it to the bonded First via a `possesses` edge (skipped fail-soft when no First). Beat 0 deliberately seeds nothing (`MeetingEncounter` already threads The First — no double-thread). New registered `ascendant.beat.seeded` trace (the add_node/add_edge surface) + `BeatSeededTrace`. Fail-soft throughout (no ascendant/location/sphere → empty seed + `failSoft` trace, never throws/wedges). 16 new tests (`ascendantBeatSeeding.test.ts`); **full suite 11821 green**, vite build green, 30-tick CLI smoke (seed 42 medium, 398 agents, 42 events) clean. Browser-verify exempt: engine/types/data + tests only, no UI pillar file touched (the GameView resolve call is unchanged — seeding is transparent; the seat signifier/HUD read the now-set graph state through existing surfaces). Wiring-checklist + systemic-wiring-guide updated. PR pending.
- ✅ 2026-06-28: THR-510 shipped (Continuous Improvement) — **Stop plan-doc/impediment flush PRs from auto-closing referenced issues.** Root cause: the `flush-plan-docs` skill emitted closeable issue refs — a `Closes <issue-url>` PR body, an issue-id-bearing branch (`docs/plan-flush-<THR-XXX>`), and a `THR-XXX` commit subject — so GitHub→Linear swept every referenced issue to Done on merge (hit 3×: the Ascendant-Beats batch, THR-501, THR-519). **Fix (two layers):** (1) **skill scrub** — flush commit subject, PR title/body, and branch are now ID-/keyword-/URL-free (branch keyed on the plan-doc basename; body cites the doc + issue *title*; the issue↔PR link lives only in the Step-3 confirmation comment), plus a new Hard Rule; mirrored to `.agents/`. (2) **deterministic belt** — `linear-autoclose.yml` now imports a shared unit-tested helper (`scripts/linearAutoclose.mjs`: `extractCloseableIssueIds` + `isDocsFlushContext`) and **skips auto-close entirely for `docs(plan): batch flush` / `docs/plan-flush-*` PRs** (added `actions/checkout` so the inline script can import it). 12 new tests (incl. proof that a bare ID and a `Closes <issue-url>` URL do NOT match — only `Closes THR-NNN` does); legit `docs: closeout THR-XX` PRs still close. Full suite **11810 green**, `tsc --noEmit` clean, vite build green. No `src/`/engine/UI touched (browser-verify + engine-smoke N/A). PR pending.
- ✅ 2026-06-28: THR-521 shipped (Continuous Improvement) — **Design-wiki generator: self-maintaining hub + auto-injected nav.** New `public/wiki-manifest.json` (single source of truth) + `scripts/generate-design-wiki.ts` (regenerates `public/design-wiki.html` hub from the manifest **and** re-injects a managed nav between `<!--WIKI-NAV-->` markers into every manifest page — replace-if-present / insert-after-`<body>` if absent; active page gets `aria-current`; idempotent; nav carries scoped `wiki-nav__*` styles so it renders on any page). Wired into `prebuild` (mirrors `generate-ul-dashboard`), so `npm run build` rebuilds the hub + re-applies nav — a page regenerated by another skill gets its nav back next build. Guardrail `scripts/check-design-wiki.ts` (chained into `check:process`) fails if a served `public/*-reference.html` is missing from the manifest or vice-versa. Retrofitted the two existing pages (`encounters-agents-reference.html`, `tick-cycle-reference.html`); `encounter-actor-systems` skill updated (nav is generator-owned, don't hand-write) + mirrored to `.agents/`; CLAUDE.md pointer added; pattern doc `Docs/design-reference-wiki.md` authored. **Divergence from issue (sandbox state never landed):** the pattern doc and the `opening-system.html`/`action-catalog.html` pages didn't exist on `main` (THR-519 shipped docs only) — manifest seeded with the 2 real pages; the 2 missing ones listed as "to document next" backlog cards in the hub (move to `pages` when authored). Full suite **11797 green + 1 pre-existing flaky timeout** (`debug-bridge.test.ts`, passes in isolation at 1160ms ≪ 5000ms; THR-489 baseline; zero `src/` files touched), `tsc --noEmit` clean, vite build green (hub + manifest land in `dist/`). Browser-verified at 1920×1080 (worktree vite:5199): hub renders (2 linked + 2 planned cards), cross-link → encounters page with injected nav + active highlight, only console error a benign `favicon.ico` 404. PR pending.
- ✅ 2026-06-28: THR-514 shipped (Divine Cadence) — **Pool-beat content templates (prose + enrichProse).** The THR-505 pool beats carried only scheduling descriptors; the player-facing prose was a generic kind-derived placeholder ("The world calls…"). Now each of the 14 cadence pool beats is backed by a Threadbare-voice `UnifiedActionTemplate` (new `src/data/ascendant-pool-beat-templates.ts`, `id === beatId`) with long-form `description` carrying `enrichProse` placeholders (`{name}`/`{artifact:any}`), and each pool `BeatDefinition` sets `templateId === beatId`. **Registration without leak:** the 14 templates are deliberately NOT in `UNIFIED_ACTION_TEMPLATES` (which feeds the ascendant hand/Codex/CMS/structural tests) — beats aren't castable cards; instead `getUnifiedTemplateById` checks the pool as a third lookup, which is all `resolvePendingBeat`'s `templateResolver` + the modal need. **UI:** `GameView` resolves the beat's template and runs `name`+`description` through `enrichProse` against the god's anchor mortal (The First → any threaded actor; fail-soft to kind copy if no subject), passing a `proseOverride` to `AscendantBeatModal` (new optional prop; spine beats keep `SPINE_BEAT_PRESENTATION`). Unlock still flows from `grantsActionIds` (the THR-517 resolve contract grants from the descriptor, not template aftermath; richer template-aftermath resolution = TODO(THR-520)). 8 new tests (`ascendantPoolBeatTemplates.test.ts`: templateId===beatId, resolver contract, no-UNIFIED-leak, no-ascendant-affinity, enrichProse leaves no raw `{tokens}`). Full suite **11798 green**, `tsc --noEmit` clean, vite build green. Browser-verified at 1920×1080 (`?view=game&seeded&size=medium`, worktree server): fired `beat.pool.invest.the_worthy_mortal` → offer banner → modal reads "…the way **Kael Thornweaver** once did…" (run-specific), resolve granted `bind_thread_agent`; selection beat `shape_of_devotion` → enriched intro + Imbue/Observe 1-of-N picker; 0 console errors. Engine-smoke N/A (no `src/engine`/tick file touched). PR pending.
- ✅ 2026-06-28: THR-516 shipped (Divine Cadence) — **Director pool draw: eligibility predicates + identity (reach/sphere) bias.** Closes the THR-505 deferral — the shipped `drawFromPool(pool, rng)` took no world state, so the Director could neither drop a beat with nothing to say nor lean toward the god's identity. **Types:** new serializable `BeatEligibility` (`always` / `unintroduced_group` / `unthreaded_target`) + `BeatIdentityBias` ({reach?, sphere?}), added as optional `eligibility?`/`identity?` on `BeatDefinition` (`src/types/ascendantBeat.ts`) — fully additive. **Engine** (`ascendantBeat.ts`): exported `isBeatEligible(beat, state)` (unintroduced_group = #culture/faction actors > #resolved introduction beats; unthreaded_target = #actor+location nodes > #ascendant `thread` edges; fail-open on throw/unknown kind, NFP #4) + `computeIdentityBias(beat, state)` (reach = `BASE + SLOPE × domainAffinities[reach]`; sphere = primary/secondary-match bonus on `sphereAlignment`; multiplicative; 1 when no `identity`/no ascendant); `drawFromPool` gains optional 3rd `identityBias` arg (existing two-arg callers unchanged). The Director cadence branch now `pool.filter(isBeatEligible)` → `drawFromPool(eligible, rng, b => computeIdentityBias(b, state))`, and the scheduled trace's `poolSize` reflects the **eligible** pool. **Content:** new tunables `BEAT_REACH_BIAS_BASE/SLOPE` + `BEAT_SPHERE_BIAS_PRIMARY/SECONDARY/NONE` (`ascendant-beat-content.ts`, NFP #1); pool beats tagged — introduction → `unintroduced_group`, investment → `unthreaded_target`. 16 new tests (`ascendantBeatIdentity.test.ts`): per-predicate eligibility + fail-open + "Director never offers an ineligible beat"; identity multipliers + "drawFromPool draws an aligned beat more often" (deterministic, ~75%). Full suite **11790 green**, vite build green, 30-tick CLI smoke (seed 42 medium, 398 agents, 42 events) clean, **zero net-new `tsc -b` errors** (3483 baseline, THR-489). Browser-verify N/A: engine/types/data + tests only, no UI pillar touched (beat presentation is THR-517/THR-507). Per-beat `identity` reach/sphere tags on the shipping pool are a content decision → TODO(THR-514); the draw honours them the moment they're set. Pre-existing note: beat cadence constants aren't in the CMS registry (THR-500/505 gap) — these new constants follow that precedent (named/tunable in source). Wiring-checklist + systemic-wiring-guide updated. PR pending.
- ✅ 2026-06-28: THR-504 shipped (Onboarding & First-Run) — **Scripted onboarding spine beats 0–4.** Expanded `ASCENDANT_SPINE` (`ascendant-beat-content.ts`) from 3 turn-gated placeholders to the full 5-beat arc with real-id grants: Beat 0 The First (`bind_thread_agent`+`observe_agent`), Beat 1 The Seat (`bind_thread_location`), Beat 2 A Thing Left Behind (`action.imbue`), Beat 3 The First Word (`divine.persuade`), Beat 4 A Path Opens — a **selection** forking into one of three god-paths (`divine.dream`/`divine.omen`/`divine.inspire`). Authored per-beat Threadbare-voice presentation (`SPINE_BEAT_PRESENTATION` — eyebrow/title/long-form prose/cta, player-as-god second person), consumed by `AscendantBeatModal` (prefers authored copy over the generic kind-placeholder); `GameView` auto-opens + locks all `beat.spine.*` ids modally (covers the selection Beat 4) via new `isSpineBeatId`. Extended `ASCENDANT_ACTION_BUCKETS` (+4 `divine.*` unlockable-generic). 6 new spine tests (`ascendantSpine.test.ts`) + locked THR-500/505/517 contract intact. **Full suite 11774 green**, vite build green, 30-tick CLI smoke (seed 42 medium, 398 agents, 42 events) clean. Browser-verified (`?view=game&seeded`, worktree:5175, viewport 1528×1226 — host display clamps 1920×1080 via OS scaling): all 5 beats render their authored prose; Beat 4 picker shows Dream/Omen/Inspire with real descriptions; selection grants **exactly** the chosen path; OBSERVE reveals in the drawer post-Beat-0; 0 console errors. **Scope note:** the substrate resolve (THR-517) is catalogue-driven — a beat *grants cards*; the player fires them to thread the actor/location/artifact. Beat-driven auto-seeding at resolution (plan §4.1 `add_node`/`setHomeSeat`) deferred → THR-520. PR pending.
- ✅ 2026-06-27: THR-517 shipped (Divine Cadence #8) — **Beat resolve path: offer→enter→resolve wiring.** The Director (THR-500) only *offered* beats (set `ascendantBeats.pending`); nothing cleared them in the running sim. New `resolvePendingBeat(state, {chosenActionId?, outcome?}, templateResolver?)` (`ascendantBeat.ts`): looks the pending beat up across spine∪pool∪delivery (new exported `getBeatDefinitionById`), applies its grants into `unlockedActionIds` (dedup, `action.unlock.granted` via beat per id), then records the `BeatRecord` + clears `pending` via the existing pure `resolveAscendantBeat`. Selection beats grant exactly one chosen option; non-selection grant all. Fail-soft: unknown beat / unresolvable `templateId` clears gracefully (new `ascendant.beat.skipped` reason `missing_template`), never wedges; whole body try/caught. **UI** (new `AscendantBeatModal.tsx`): pending pool beats surface as a top-center thread-tug offer banner (`AscendantBeatOfferBanner`) the player enters; spine beats auto-open modally (gated by `interruptsSuppressed`). The entered-beat modal presents kind-derived prose + either a Receive CTA (non-selection) or a generic **choose-1-of-N picker** (selection) over the beat's grants; resolution flows through `resolvePendingBeat` → grants land in the drawer (already reads `unlockedActionIds`). **Debug**: `__DEBUG.resolveBeat(chosenActionId?)` (+ `BeatBridge`/`debug-bridge.d.ts` `DebugResolveBeatResult`) and a BeatsDebugTab "Enter & Resolve" control. 7 new engine tests; full suite **11734 green**, vite build green, 30-tick CLI smoke (seed 42 medium, 398 agents, 42 events) clean, zero net-new `tsc -b` errors (3483 baseline, THR-489). Browser-verified at 1920×1080 (worktree server, `?view=game&seeded&size=medium`): spine beat auto-opened ('Opening' → Receive → +bind_thread_agent,observe_agent, pending cleared, history recorded), selection beat banner→picker→Imbue granted **only** `action.imbue`; 0 console errors. Plumbing only — prose-rich encounter-screen handoff for template-backed beats is content (TODO(THR-514)). Unblocks THR-504 (spine) + THR-514 (pool prose). PR pending.
## Archived to project-history.md
- 2026-06-30 batch (status trim): THR-513, THR-512, THR-511, THR-506, THR-505
- 2026-06-28 batch (status trim): THR-509, THR-494, THR-503, THR-500, THR-502, THR-501
- 2026-06-23 batch (status trim): THR-476, THR-488, THR-486, THR-487, THR-492
- 2026-06-27 batch (status trim): THR-481, THR-477, THR-491, THR-479, THR-472, THR-475, THR-473, THR-463, THR-462, THR-508
- 2026-06-13 batch (status trim): THR-461, THR-460, THR-250, THR-378
- 2026-06-12 batch (status trim): THR-451, THR-62, THR-453, THR-455, THR-456, THR-442, THR-449, THR-446, THR-381
- THR- and earlier — see project-history.md
- 2026-05-06 batch: THR-311, THR-312, THR-313, THR-314, THR-315, THR-316, THR-317, THR-320, THR-321, THR-322, THR-323, THR-336
- 2026-05-07 batch: THR-324, THR-325, THR-329, THR-339, THR-340, THR-341, THR-349, THR-350, THR-354, THR-355, THR-356, THR-357, THR-358, THR-359
- 2026-05-08 batch: THR-139, THR-176, THR-177, THR-178, THR-179, THR-215, THR-265, THR-266, THR-267, THR-289, THR-292, THR-326, THR-330, THR-331, THR-332, THR-333, THR-334, THR-335, THR-338, THR-343, THR-344, THR-345, THR-352, THR-353, THR-360, THR-361, THR-362, THR-363, THR-365, THR-368, THR-374, THR-387, THR-388
- 2026-05-09 batch: THR-268, THR-389, THR-391
- 2026-05-10 batch: THR-394
- 2026-05-11 batch: THR-393, THR-403, THR-408, THR-411
- 2026-05-12 batch: THR-413, THR-404, THR-397, THR-398, THR-399, THR-405, THR-416, THR-409
- 2026-05-16 batch: THR-416 (via THR-409 worktree cleanup closeout), THR-249, THR-248, THR-377, THR-447
- 2026-06-11 batch: THR-406, THR-450, THR-452, THR-457

## Active Backlog Ideas
- **TB-105–108 Thematic Pressure & Living World Pass** (omen agendas, cool failure, doom identity, intent/activity visibility)
- **TB-095–099 Social Systems Expansion** (v1.2 — designed, ready to sequence)
- TB-071 Economy Second Pass · TB-069 Location NPCs · TB-051 Monster Encounters residual · TB-037 Onboarding

Full backlog: [Linear (Threadbare team)](https://linear.app/threadbare) · Completed work: `Docs/project-history.md` + Linear "Done" state · Pre-Linear history: `.planning/BACKLOG_HISTORY.md`
