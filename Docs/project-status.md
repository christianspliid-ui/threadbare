# Project Status
> Updated 2026-07-05 (Motive-receipt foreshadowing — **Phase B consumption shipped (phased; tracking issue stays In Dev).** The emitted `MotiveReceipt` now *drives the prose*: a new receipt-driven composer (`src/engine/foreshadowing/composeReceipt.ts`) assembles S1 knowledge (real intel tier) → S2 pull (top contribution **kind**) → S3 expectation (real completionProb forecast tier, em-dash hedge tail below `briefed`) → optional S4 stake (2nd kind, gated by `STAKE_CLAUSE_MIN_WEIGHT`); tooltip render = S2 only. A `readMotiveReceipt` helper (`receiptRead.ts`; matches templateId+locationId, rejects stale/mismatched) wires **both** resolvers (`getEncounterForeshadowing.ts` tooltip path + `encounterForeshadowing.ts` panel path) to read the same receipt off the agent node — one causality source, two surfaces. New content in `foreshadowing-content.ts` (`MOTIVE_CLAUSES` ×14 kinds, `EXPECTATION_BY_FORECAST` ×5 tiers, `STAKE_CLAUSES` curated + `DEFAULT_STAKE_CLAUSES` fallback; ≥3–4 variants, THR-609 plain voice, agreement-safe). `ForeshadowingResult.tooltipProse` + trace `receipt`/`compositionKeys` additive; `__DEBUG.getForeshadowing` now returns `tooltipProse`+`receipt`. Emit payloads typed (`Omit<ForeshadowingResolutionTrace,…>`) to sidestep the emitTrace Omit-union collapse — net `tsc -b` **down** (3544→3542). Full suite **12362 green (818 files)**, vite build green, 30-tick CLI smoke clean (389 agents; 16 live receipts by tick 40; receipt-driven prose rendered end-to-end for he/she/they). Engine+content+debug only (no `src/components/`) → browser-verify exempt. **Remaining (In Dev):** full resolver module-merge + tooltip-component S2 render + DebugPanel receipt table (UI slice); scorer-sourced divine/bond/reputation/proximity kinds; Phase C content depth; Phase D wiring docs. Prior: THR-635 — **Hex map washed-out colors fixed + sepia removed — COMPLETE.** User-reported whitish tint: `HexFillMesh` wrote instance colors with unflagged `setRGB` (linear interpretation) while fog-culling writes flagged `THREE.SRGBColorSpace` — double gamma encode washed the base map at game start. All four `setRGB` calls now flagged; per user directive the sepia remembered-hex tint is removed entirely (remembered hexes keep full terrain color; layer gating + parchment overlay unchanged; orphaned `toSepia`/`SEPIA_*` deleted). Full suite **12319 green (814 files)**, vite build green, browser-verified @1920×1080. Prior: THR-615 — **Mortal Economy P1 — resource activation — COMPLETE.** The decorative resource layer is now live and felt. Step 0 confirmed resources are location-property `Record<string,ResourceInstance>` (not the schema's `resource` node), seeded by terrain — a new `pre-economy` phase `phaseResourceStockTiers` (pure derivation in `resourceEconomy.ts`) sets a coarse **scarce/adequate/surplus** tier per resource per location from supply−demand (population proxy = prosperity + sublocation density), spotlight-capped at 40 (coarse beyond), first-derivation silent, `touchWorld` on change. An 18-entry **resource-class table** (category · sphere · baseValue · scarcitySensitivity) drives it; the aggregate `resourceBalance` feeds an additive prosperity equilibrium term (`RESOURCE_BALANCE_PROSPERITY_WEIGHT` 0.15). **Famine/Glut** get `resource_scarcity`/`resource_glut` chronicle triggers; a **livelihood thread tug** fires when a bonded mortal's home staple crosses into scarce/surplus (Gold reach, deduped, on the existing attention machinery). UI: prose-only **Livelihood** line on LocationView (Famine/Glut as economic IPK keywords) + DebugPanel **Economy** tab + `resource_stock_tier_change` trace; new W18 Manual page `livelihoods-reference.html`. Full suite **12277 green (809 files)**, vite+prebuild green, 30-tick CLI smoke clean (389 agents; seed-42 medium tiers scarce 4/adequate 81/surplus 53), browser-verified @1920×1080 (live crossings + prosperity `resourceBalance`; Economy tab render; console 0 app errors). P2–P4 (trade cargo, route events, faction control, sphere drift, divine verbs) remain. This unblocks THR-620 (rival economic scheme family). Prior: THR-612 — **World volume wave 1 (content-only) — COMPLETE.** Step 0 substrate check split the schema/design classes to deferrals (new location subtypes → THR-622, delve archetypes → THR-623, NPC-role model → THR-624 [the 56-permutation `NpcRole` grid is full by design], backstory follow-on → THR-625) and shipped the data-driven rest. **`SUBTYPE_ESTABLISHING_PROSE` 20→53 keys** — 3 baseline-register variants (`{name}`) for all 33 existing location subtypes that spawn with no establishing prose (sphere-wonders, wilderness-interest, anomaly/economy, monster, lair, `elder_ruin`/`place_of_power`); fail-soft resolver (`proseResolvers.ts:103`) → purely additive (worldgen seed-42 medium spawns 15/33; `elder_ruin` ×88 alone had zero prose before). **`HISTORICAL_CULTURE_TEMPLATES` 8→12** (+sand_kings/frost_wardens/green_below/salt_factors) for ruin-descriptor variety. **Name pools** expanded (FOUNDATION 20→28, SPHERE 15→20, GENERIC 80→96). All prose plainspoken-Malazan baseline (THR-609 mutex satisfied). Full suite **12261 green (807 files)**, vite build green, 30-tick CLI smoke clean (389 agents); content-data-only. Prior: THR-610 — **Surface technical effect descriptions in-game — COMPLETE.** The authored `technicalEffect` (THR-604, all 165 catalog entries) + derived `effectSource` wiring badge now show in-game, not only the external catalog: a set-off **"Effect"** block on the ActionDrawer focused card (below the card, above the sphere flavor) and a matching **Effect** section + colour-coded wiring badge in the Codex detail panel. Single source of truth extracted to `src/data/actionEffectSource.ts` (`effectSourceFor` + `EFFECT_SOURCE_LABELS`/`_BADGE_COLORS`, mirroring `action-catalog.html`) — the catalog generator, ActionDrawer, and Codex all read it so the badge never drifts. Plumbed `technicalEffect`+`effectSource` onto `WheelSlot` via the single `getTargetActionSlots` builder (the retired `getAgentWheelSlots` is gone) and onto `CodexEntry` via the shared action mappers (extended to all action categories, not just divine — same treatment as the catalog). Fallback: no `technicalEffect` → block hidden entirely (no empty label). Catalog `.generated.json` byte-identical (output-identical refactor). Full suite **12261 green (807 files)**, vite build green, 30-tick CLI smoke clean (391 agents), browser-verified @1920×1080 (live Codex Effect block screenshot + DOM assertion: exact text + "wired · template" badge; console 0 errors; ActionDrawer block covered by 2 new focus-state regression tests). Prior: THR-66 — **Rival activation via multi-phase schemes — COMPLETE.** Rivals now launch four-phase **schemes** (rumor → materialization → response → crack) riding the shipped THR-225 composition phase runner: `phaseRivalActions` (orchestrator) launches on a rival's action tick, invests world-flags each tick to arm the next phase, and executes each phase's concrete **move** (sphere push, `sponsors_scheme` edge bind, hostility bump, crack payoff) when the runner activates it — attributed via `ActiveComposition.sponsorRivalId`/`schemeFamily` + `RivalState.schemes` summary + `sponsors_scheme` edge. Escalation tier (doom stage blended with player thread-tier, `computeRivalEscalationTier`) gates concurrency/speed/ambition. Counter-play loop: player presence at the target (thread/controls/holds_place_of_power) stalls then fails the scheme, leaving a cool-failure chronicle beat (half-thwarted stays half-thwarted). Two families ship (`corruptive`, `territorial`, baseline register, ≥3 variants/beat); economic family split to **THR-620** (blocked on THR-615). UI: RivalPanel scheme cards + 4-phase chips, Chronicle (runner), launch/crack toasts, HexMapV2 sphere-tinted rival-influence overlay (`buildRivalInfluenceMarkers` → `RivalInfluenceMesh`), DebugPanel via `__DEBUG.getRivalSchemes()`/`forceRivalScheme()`. Four `rival.*` traces; all pacing/escalation numbers in `src/data/rival-scheme-config.ts`. **Full suite 12259 green (807 files)**, vite build green, 30-tick CLI smoke clean (391 agents), browser-verified @1920×1080 (organic scheme launch confirmed live at tick 9; RivalPanel scheme cards render — 2 cards "phase 1 of 4" + 4 phase-chip dots; console clean). Prior: THR-609 — **Voice register calibration (plainspoken Malazan) — COMPLETE (Steps 1–3, Passes A–C).** Register fails driven **151→0** across the 864-entry corpus. The three-register model (baseline default · character · peak rationed lyricism; interactive text always plain) is now canon in `Docs/canon/prose.md` + the 6 authoring skills + baseline/peak exemplars, plus a deterministic enforcement floor: a `registerCompliance` dimension on the THR-490 prose-QA harness (avgSentenceLength · rareWordDensity via an ornate-diction denylist · figurativeDensity · interactivePlainness), additive optional `register?` field, all thresholds named constants, DebugPanel Prose QA `reg.` column + summary counts. Calibrated so the plan's plain example passes / ornate fails / peak doom-transition passes; corpus register fails driven from 151 (all-baseline) to 0 across Passes A–C. THR-609 shipped. Full suite **12232 green (806 files)**, vite build green, browser-verified @1920×1080. Prior: THR-571 — **Outcome-ladder final tranche: KPI re-band + U1 DOM readers — Agent Success Redesign closed.** Design gate (Christian, "accept the capability-poor world") re-fit the bands so scrape-through (`success_at_cost`) is the *expected* success texture and clean/crit are rare signals: at-cost ceiling 0.45→0.70, total-success floor 0.55→0.50, clean floor 0.20→0.03, crit-failure ceiling 0.05→0.15, failure ceiling 0.40→0.50. Added lifetime counters (`resolvedActionsTotal`/`cleanSuccessTotal`/`critSuccessTotal`) so clean/crit read from a stable full-run numerator, not the noisy window; crit-success is **advisory** (report-but-don't-gate). U1 DOM built: DebugPanel KPI tab full Outcome Ladder section + advisory dimming; ChapterView final-outcome lexicon band ("won, at a price" / rare-tier gold-red for crits). **`gameplay-report` all-green** seeds 42/99/7 × 120t (at-cost 47–58%, clean 6–7.5%, crit-fail 8–12%, failure→story 100%). Rebased the prior E1–E4/C1/U1-data stack onto main (merged THR-603 chapter-archival conflict). Full suite **12216 green (805 files)**, vite build green, 30-tick CLI smoke clean, 0 net-new `tsc` errors. Browser: bridge assertions (`getOutcomeDistribution`/`getKpiReport` advisory row) + KPI-tab DOM-render confirmed; PNG capture non-functional in the automated sandbox (documented limitation).)

## Current Focus
**Agent Personality — the Core foundation layer (THR-542, all 4 slices shipped):** a second, more fundamental personality layer beneath the 8 reach axes — five plain virtue↔vice continuums of *who an agent is* (`coreProfile`: Warm↔Cold, Hopeful↔Bitter, Forgiving↔Vengeful, Humble↔Proud, True↔False), kept distinct from reach (`axiologicalProfile`) and capability (`domainCapabilities`). Engine foundation (slice 1) live: registry, seed/colour/bend mechanics, the `core_personality` tick phase. **Content (slice 2 / THR-544) live:** a 60-entry Core origin-vignette library (`core-origin-vignettes.ts`) now layers authored character on top of the PRNG seed at birth (`seedCoreProfileWithVignettes`, recorded on `coreOriginVignettes`), and 10 Core emergent-trait defs (`core-trait-content.ts`, new `subcategory:'core'`) are granted/released by the phase on emergence/fade (silent born-extreme grant). Canon-safe framing preserved: Core = *which way* you bend, distinct from Quintessence = *how bendable* — goodness never a Quintessence pole; Core traits carry no scoring/capability. **Star re-scope (slice 3 / THR-545) live:** with the inner-outlook reading now owned by Core `core_hope` (Hopeful↔Bitter), Star's reach axis re-points from Beacon/Inspiring↔Anchor/Discouraging to **Beacon (Guiding) ↔ Wrecker (Misleading)** (wayfinding/fate) across `axisRegistry.ts` + its independent test oracle, `Docs/canon/cosmology.md` & `encounters.md` Star rows, and the THR-524 canon table; the 18 Star origin-vignettes re-themed from morale/hope (now duplicate of `core_hope`) to wayfinding. **UI (slice 4 / THR-546) live:** the agent character sheet (`OverviewTab`) now renders a **Core** section — the 5 continuums as labelled tracks (virtue↔vice) with a marker per value, positioned above Nature so the layering reads bedrock→expression; `coreProfile` is surfaced through `AgentInfoCardData` at intimate+ knowledge (same gate as `axiologicalProfile`). Deferred: **THR-547** (re-theme the comet encounter's pre-THR-524 Wanderer/Anchor framing).
**Repo Health — `npm test` green on main (THR-489):** the months-long red baseline is cleared — full suite **11966 green (783 files)**, stable ×3. Resolved by the WS1–WS3 consolidation + intervening fixes; the live run reconciled clean against the historical known-failing checklist (no `.skip` masking). CI's required "Test · Typecheck · Build" gate is trustworthy again; merge=Done is load-bearing. Triage/closure doc: `Docs/plans/2026-06-30-test-suite-stabilization-triage.md`. (`tsc -b`'s ~3494-error baseline is a separate concern, out of scope.)
**Ascendant Beats — Divine Cadence (new):** Director + primitives + reach gate + home seat + debug + turn-1 floor + full expression toolkit (imbue/consecrate/bestow/anoint) + pool library + delivery adapter + beat resolve path shipped. **Beats are playable end-to-end** (offer→enter→resolve) and **pool beats now read bespoke per run (THR-514):** each cadence pool beat is backed by a Threadbare-voice content template with `enrichProse` placeholders, rendered enriched in the beat modal. **Onboarding spine complete (THR-504):** the full 5-beat scripted arc (The First → The Seat → A Thing Left Behind → The First Word → A Path Opens) ships with authored Threadbare prose. **Director pool draw eligibility-filtered + identity-biased (THR-516).** **Beat-driven graph seeding (THR-520):** resolving Beat 1 seats the throne (mana income ticking) at The First's settlement; Beat 2 mints + threads a sphere-flavored artifact — no manual card-firing. **Expression cards bucketed + pool-granted (THR-515):** consecrate/bestow/anoint now sit in `ASCENDANT_ACTION_BUCKETS` (`unlockable-generic`) and are granted by three deeper-expression investment pool beats (pool 14→17). **Consecrate relic variant shipped (THR-518):** `action.consecrate-relic` mints a permanent relic at establishment via `ControlSpec.mintUpkeepRelic` (handled in `spawnControlEffect`), waiving all per-tick upkeep while the relic lives and lapsing on its destruction — a pure-data second card, granted alongside `action.consecrate`. **Pool-beat template aftermath + introduction-group binding (THR-522, last engine tail):** `resolvePendingBeat` now runs a matched template's `aftermathConfig.fallback.reactions` through the shared aftermath resolver on resolution (additive; grant-only fallback when none declared) — beats can express `unlock_action`/`encounter_seed`/graph ops via their template, not only the descriptor. Introduction beats bind their specific un-introduced culture/faction into `PendingBeat.boundNodeIds` at offer time and name it via the new `{group}` enrichment placeholder (inspectable in the DebugPanel Beats tab). **Reach signatures acquired end-to-end (THR-523):** the 8 `invest.<reach>.<name>` signatures are catalogued `reach-gated` in `ASCENDANT_ACTION_BUCKETS` and granted per-run via `BeatDefinition.grantsReachSignature` — Beat 4 grants the primary, a new `beat.pool.invest.reach_signature` pool beat grants the secondary (gated by the new `unacquired_reach_signature` predicate). Remaining: per-beat identity tags (THR-514). **Reach signatures (THR-548/549/550):** sphere-power scaling helper + per-(reach×sphere) individualization scaffold + Iron's `signature_warhost` (mark faction `mobilized` + raise an army-node force) and Veil's `sphere_influence_amplify` (THR-551 — sustained sphere rift: ControlEffect amplifies local sphere influence up to a cap + seeded per-tick chaos-pulse leak, all sphere-scaled) shipped; **Stone's `spawn_unique_location` (THR-552) shipped** — mints a one-of-a-kind `location` (unique flag + `controls` edge), dedup by `uniqueTag` (idempotent), optional legendary-relic forge reusing the `spawn_artifact` path.
**Onboarding & First-Run Experience:** THR-504 scripted spine beats 0–4 shipped — the opening arc that delivers the run's first action cards.
**Action System & Unlocks:** THR-501 turn-1 floor (Move + Investiture only across all surfaces) shipped. THR-519 self-updating Action Catalog shipped — `scripts/generate-action-catalog.ts` emits `public/action-catalog.generated.json` from the live registry (164 ascendant-facing templates, code-true buckets incl. Beats verbs); `public/action-catalog.html` renders it (filter/sort/drawer), wired into the Design Reference Wiki.
**Encounter Experience — active:** Phases A1–A3, B1–B7, C1–C4, D1–D3, E1–E2, F1–F2, G1–G3 ✅. Remaining is F-phase integration (mount EffectRegistration components into hero panel / cast tile / scene state surfaces) and Phase H post-v1 polish.

## Milestone Status
- **v1.0 Foundation:** Shipped 2026-03-30 — Phases 1-18 + M2.5 (81 plans, 1533 commits)
- **v1.1 Optimization:** Shipped — Phases 19-22 (determinism, wiring, performance, hygiene)
- **Encounter Format Migration (Now):** ✅ Phases 0-9 complete. All guilds migrated.
- **Content Architecture (Now):** ✅ THR-86/88/239 shipped.
- **UI Visual Overhaul — Design System v1 (Now):** ✅ Project complete. THR-178 Deferral shipped 2026-05-08.
- **Continuous Improvement (Now):** THR-303/305/306/307/309/311/312/313/314/315/316/354/355/356/357/358/359/266/396/404/510 ✅. THR-304 Phase 5 series complete (5a vault THR-356 ✅, 5b repo THR-359 ✅). THR-360 (Category B code residue) queued.
- **Social Systems Expansion (Now):** THR-28/27/30/51/29/41/34/31/35 shipped. ✅ THR-254/253. THR-78 queued.
- **Thematic Pressure & Living World (Next):** ✅ THR-19/122/125/126/80/128/127. THR-87 blocked by THR-116.

- 🚧 2026-07-05: THR-631 **Phase A + Phase B (emission + consumption)** shipped, issue stays **In Dev** (Motive-Receipt Foreshadowing — Thematic Pressure & Living World; phased, no close keyword). **Phase A** killed the two visible bugs at root — typed-slot agreement realizer (`realizer.ts`) + composed-generic clause pool — so no encounter title lands in a place slot and agreement holds for he/she/they. **Phase B emission** kept the scorer's discarded per-candidate contribution terms as a `MotiveReceipt` on the agent node at the `phaseAgentDecision` selection commit (`motiveReceipt.ts` + `__DEBUG.getMotiveReceipt`). **Phase B consumption (this run)** makes the receipt *drive the prose*: `composeReceipt.ts` renders S1 knowledge (real intel tier) → S2 pull (top contribution **kind**) → S3 expectation (real forecast tier + low-intel hedge) → optional S4 stake (2nd kind, weight-gated); `readMotiveReceipt.ts` wires **both** resolvers to the same receipt (one causality source, two surfaces); new `MOTIVE_CLAUSES`/`EXPECTATION_BY_FORECAST`/`STAKE_CLAUSES` content (≥3–4 variants, THR-609 plain, agreement-safe); `tooltipProse` (S2-only) + trace `receipt`/`compositionKeys` additive; `__DEBUG.getForeshadowing` returns them. Self-maintaining agreement sweep in `composeReceipt.test.ts` (14 tests, derives conjugations from the real `conjugate`). Full suite **12362 green (818 files)**, vite build green, 0 net-new `tsc -b` (3544→3542), 30-tick CLI smoke clean (389 agents; 16 live receipts by tick 40; receipt-driven prose rendered end-to-end for he/she/they). Engine+content+debug only → browser-verify exempt. **Remaining (In Dev):** full resolver module-merge + tooltip-component S2 render + DebugPanel receipt table (UI slice); scorer-sourced divine/bond/reputation/proximity kinds; Phase C content depth; Phase D wiring docs.
- 🚧 2026-07-05: THR-611 Slices 1–5 shipped, issue stays **In Dev** (Divine Economy — Essence Sources; multi-session core, no close keyword). **Slice 1 = ENGINE SUBSTRATE:** an `essenceSource` property bag on existing host nodes (no new node/edge type — a generalization of the place of power): `SourceKind`/`SourceTier`/`EssenceSource` types, pure `deriveSourceTier`/`sourceTierMultiplier`/`sourceDepthMultiplier`/`BASE_SOURCE_INCOME`, graph logic `computeSourceIncome` (typed per-sphere income + portfolio DR) / `migrateControlledPlacesOfPower` (income-neutral) / `recomputeControlledSourceTiers`, a `phaseEssenceSources` tick phase (O(controlled hosts), one aggregate trace/tick), typed income folded into both `computeEssenceGeneration` + `computeEssenceIncome` (legacy income provably unchanged), `__DEBUG.getEssenceSources()`. **Slice 2 = the BUILD → DEFEND VERB LOOP:** three new graph-op kinds (`consecrate_source` types a controlled host to the ascendant's primary sphere + ensures a `controls` edge so typed income begins; `sanctify_source` raises sanctity toward flowering ~2× income; `defend_source` clears contestation/desecration + restores sanctity) with fail-soft executor cases (tier re-derived via the Slice-1 helpers; new `SANCTITY_DEFEND_RESTORE`), plus three Star-reach ascendant templates (`loc.consecrate_source`/`loc.sanctify_source`/`loc.defend_source`) with real step ops and Threadbare plain-register prose — the user directive ("actions that progress essence income") made real. **Slice 3 = VERB SURFACING:** one investment Ascendant Beat `beat.pool.invest.the_wellspring` ("The Wellspring") grants the three source verbs via the shipped `resolvePendingBeat` path — three `unlockable-generic` `ASCENDANT_ACTION_BUCKETS` entries (universal, sphere-flavored, not two-domain reach-locked — `reach:'star'` is the cosmic axis, not a `requiresReach` gate) + a Threadbare plain-register content template; empty THR-501 starter floor left intact (surfacing is the earned beat grant). **Slice 4 = the FIND → CLAIM front half:** worldgen seeds a small number of **latent** `placeOfPower` sources (`seedLatentEssenceSources`, `LATENT_SOURCE_SEED_COUNT=6`) onto wild-interest locations — hidden (`discoveredBy` undefined) + typed by their own **locale** sphere; two new graph-op kinds `find_source` (reveal latent sources within `SOURCE_DISCOVERY_RANGE_HOPS=3` hexes) and `claim_source` (bind a *discovered* source with a `controls` edge → typed income flows; Find→Claim prerequisite), two Star/Eye templates (`loc.find_source`/`loc.claim_source`) added to `the_wellspring`'s grant (now the whole find→claim→build→defend loop) + `unlockable-generic` buckets. A claimed ley-nexus pours its country's sphere (distinct from a consecrated shrine → the god's sphere). **Slice 5 = DEBUGPANEL ESSENCE-SOURCES TAB (first UI-pillar slice):** a new `EssenceSourcesDebugTab` (registered in `DebugTabContent`) reads live graph state and lists **every** host with an `essenceSource` bag — latent / discovered / controlled — with kind, derived tier (colour-coded), sphere typing, sanctity, contested/desecrated state, plus the ascendant's typed per-sphere source income; makes the whole seed→find→claim→build→defend substrate legible in one place (the `getEssenceSources` bridge only showed *controlled* sources). Cumulative: full suite **12325 green (814 files)**, vite build green, 0 net-new tsc errors (3544=3544 baseline). Browser-verified @1920×1080 (seeded medium): tab renders live "6 sources — 0 controlled, 0 discovered, 6 latent" (the Slice-4 seed) with per-source cards locale-typed (energy/time/entropy/life/matter); console 0 errors; `__DEBUG.getEssenceSources()` = `{sources:[],sourceIncome:{}}`. **Remaining THR-611 slices:** (a) **Player-UI** (portfolio income-by-source panel, WebGL hex source signifiers, IPK/codex/chronicle-tier-transition, `source_*` trace categories); (b) **discovery/consecration/defend encounters** + rulebook Divine Economy section + Game-Manual-Wiki essence page.
- ✅ 2026-07-05: THR-612 shipped (Content Architecture) — **World volume wave 1 (content-only).** Step 0 substrate check verified each class end-to-end (grep beat the Explore agent's first-pass verdict — it missed the real `LocationSubtype` union and over-classed two closed unions as data-driven). Delivered the data-driven classes, split the schema/design ones to deferrals. **`SUBTYPE_ESTABLISHING_PROSE` 20→53 keys:** 3 baseline-register variants (`{name}` placeholder) for all 33 existing location subtypes that spawn but had no establishing prose (10 sphere-wonders, 6 wilderness-interest, 10 anomaly/economy, 3 monster, 2 lair, `elder_ruin`/`place_of_power`); resolver is fail-soft (`proseResolvers.ts:103`) so additions are purely additive. **`HISTORICAL_CULTURE_TEMPLATES` 8→12** (+sand_kings/frost_wardens/green_below/salt_factors — worldgen samples 2–4/world → more ruin descriptor/legacy variety). **Name pools:** FOUNDATION 20→28, SPHERE 15→20, GENERIC 80→96. **Deferred (engine/design splits):** new location subtypes → THR-622, delve archetypes → THR-623, NPC role model beyond the full 56-permutation grid → THR-624, backstory strata content follow-on → THR-625. All prose plainspoken-Malazan baseline (THR-609 mutex satisfied — canon step 1 + Passes A–C fully merged before pickup). **Verified:** full suite **12261 green (807 files)**, vite build green, 30-tick CLI smoke clean (389 agents), worldgen seed-42 medium spawns 15/33 newly-prosed subtypes (elder_ruin ×88), end-to-end module check confirms 53 keys / 12 cultures. Browser-verify exempt: content-data-only (3 `src/data/**` files, zero UI-pillar), consumed by existing unchanged resolvers. PR pending.

## Archived to project-history.md
- 2026-07-05 batch (THR-611 Slice-1 closeout): THR-609, THR-66 (full detail in project-history.md) · (THR-612 closeout): THR-608 (user-review-interface docs, Continuous Improvement)
- 2026-07-05 batch (THR-631 Phase-B closeout): THR-610 · (THR-610 closeout): THR-571, THR-584 · 2026-07-04 batch (THR-571 closeout): THR-590, THR-588, THR-587 (full detail in project-history.md)
- 2026-07-04 batch (status trim, THR-604 closeout): THR-580 (full detail in project-history.md)
- 2026-07-04 batch (status trim, THR-590 closeout): THR-586 (full detail in project-history.md)
- 2026-07-04 batch (status trim, THR-588 closeout): THR-562, THR-561
- 2026-07-04 batch (status trim, THR-587 closeout): THR-541, THR-523
- 2026-07-04 batch (status trim, THR-586 closeout): THR-554, THR-555 · (THR-585 closeout): THR-553, THR-552
- 2026-07-02 batch (status trim, THR-562 closeout): THR-470, THR-529 · (THR-561 closeout): THR-530, THR-528
- 2026-07-02 batch (status trim, THR-559 closeout): THR-527, THR-531
- 2026-07-01 batch (status trim, THR-541 closeout): THR-539, THR-536
- 2026-07-01 batch (status trim, THR-553/555 closeout): THR-534, THR-466, THR-533
- 2026-07-01 batch (status trim, THR-552 closeout): THR-520, THR-521, THR-510
- 2026-07-03 batch (status trim, THR-580 closeout): THR-545 · 2026-06-30 batch (THR-544/561 closeout): THR-544, THR-516, THR-514
- 2026-06-30 batch (status trim): THR-513, THR-512, THR-511, THR-506, THR-505, THR-504
- 2026-06-28 batch (status trim): THR-509, THR-494, THR-503, THR-500, THR-502, THR-501
- 2026-06-23 batch (status trim): THR-476, THR-488, THR-486, THR-487, THR-492
- 2026-06-27 batch (status trim): THR-517, THR-481, THR-477, THR-491, THR-479, THR-472, THR-475, THR-473, THR-463, THR-462, THR-508
- 2026-06-13 batch (status trim): THR-461, THR-460, THR-250, THR-378
- 2026-06-12 batch (status trim): THR-451, THR-62, THR-453, THR-455, THR-456, THR-442, THR-449, THR-446, THR-381
- THR- and earlier — see project-history.md
- 2026-05-06 batch: THR-311, THR-312, THR-313, THR-314, THR-315, THR-316, THR-317, THR-320, THR-321, THR-322, THR-323, THR-336
- 2026-05-07 batch: THR-324, THR-325, THR-329, THR-339, THR-340, THR-341, THR-349, THR-350, THR-354, THR-355, THR-356, THR-357, THR-358, THR-359
- 2026-05-08 batch: THR-139, THR-176, THR-177, THR-178, THR-179, THR-215, THR-265, THR-266, THR-267, THR-289, THR-292, THR-326, THR-330, THR-331, THR-332, THR-333, THR-334, THR-335, THR-338, THR-343, THR-344, THR-345, THR-352, THR-353, THR-360, THR-361, THR-362, THR-363, THR-365, THR-368, THR-374, THR-387, THR-388
- 2026-05-09 batch: THR-268, THR-389, THR-391
- 2026-05-11 batch: THR-393, THR-403, THR-408, THR-411
- 2026-05-12 batch: THR-413, THR-404, THR-397, THR-398, THR-399, THR-405, THR-416, THR-409
- 2026-05-16 batch: THR-416 (via THR-409 worktree cleanup closeout), THR-249, THR-248, THR-377, THR-447

## Active Backlog Ideas
- **TB-105–108 Thematic Pressure & Living World Pass** (omen agendas, cool failure, doom identity, intent/activity visibility)
- **TB-095–099 Social Systems Expansion** (v1.2 — designed, ready to sequence)
- TB-071 Economy Second Pass · TB-069 Location NPCs · TB-051 Monster Encounters residual · TB-037 Onboarding

Full backlog: [Linear (Threadbare team)](https://linear.app/threadbare) · Completed work: `Docs/project-history.md` + Linear "Done" state · Pre-Linear history: `.planning/BACKLOG_HISTORY.md`
